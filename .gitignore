claude-code-tdd-agent-crew-scaffold/
.DS_Store
.gstack/

# Rust workspace build output (root ./Cargo.toml builds shared + daemon here).
/target

# Transient watchdog lock (scheduled-tasks daemon).
.claude/scheduled_tasks.lock

# Python bytecode cache (the §5.0 contract-verify offline tests, P4.0b-T).
__pycache__/
