# ARCHITECTURE.md — NexusOps

> **Status:** Binding architecture contract for the MVP. Finalized by `/arch-finalize` (Brain 2, Claude/Opus) from the `/arch-draft` rough draft + planning artifacts + a 14-dimension adversarial gap audit (`docs/gap-audits/`), with load-bearing decisions confirmed by the project owner on 2026-06-07.
> **Audience:** Project owner; `tasks-gen`; the cc-crew `/tdd` build crew; technical reviewers.
> **Primary implementation constraint:** Solo build driven largely by AI agents (cc-crew). macOS-only MVP. The owner has chosen a **comprehensive MVP** (both harnesses live, full session survival, full Brain action plans) over a minimal slice — sequence accordingly (§19).
> **Build posture:** **production-grade** (recorded in `.scaffolding/manifest.json` at the 2026-06-11 scaffolding upgrade, M-0006). The *scope* stays the comprehensive MVP locked at §0.1 — posture governs the *quality bar*: error paths, idempotency, observability, security hardening, and deploy/rollback are first-class tasks ordered early, not deferred polish; the `IMPLEMENTATION_PLAN.md` phase-exit checklist carries the posture-gated dependency-audit / whole-system-security / perf-budget rows.
> **Architecture sentence:** *A desktop-first, local-runtime cockpit whose detached Rust daemon is the single, audited mutator of all state — every change is a typed, risk-classified, approved Action recorded as an immutable event; the UI reads projections, agents and the Project Brain only propose intents, and the local machine is the trust boundary.*
> **Build contract:** This file is the source of truth. `tasks-gen` must bind every task to a `§N` anchor here and must not invent architecture; if a task needs architecture absent here, flag it. Companion (non-binding) detail lives in `docs/planning/*` and `docs/gap-audits/*`; the authoritative upstream specs are `docs/product/PRD.md`, `docs/product/PRODUCT_CANON.md`, `docs/architecture/{SHARED_OBJECT_MODEL,EVENT_MODEL_AND_AUDIT_TRAIL,PROJECT_BRAIN_INTERFACE,DESKTOP_FIRST_RUNTIME}.md`, `docs/domains/{ACTION_GATEWAY,WORKFLOW_PACKS,CC_CREW_WORKFLOW_PACK}.md`, `docs/ux/{UX_INFORMATION_ARCHITECTURE,UI_COMPONENT_INVENTORY}.md`.
> **Anchor stability:** `§N` anchors are **stable IDs** — never reused or reordered (LESSONS-style). New sections append; they are not inserted mid-document. `tasks-gen` and the area `CLAUDE.md` cross-doc-invariants table bind to these + to Appendix A.
> **Tag legend:** `[LOCKED]` · `[PENDING-SPIKE]` · `[MVP]` · `[P1]` · `[P2]` · `[DEFERRED]` · `[OPEN]`.

---

## §0 — Decision Ledger

The reconciled, once-and-only-once record of every load-bearing decision. The 11 ADRs (full rationale in `docs/planning/DECISIONS.md`) plus the finalize-pass resolutions of the audit's coupled shared-contract cluster. Where the upstream specs drifted, the canonical choice is recorded here and is binding.

### §0.1 — Owner decisions (2026-06-07)
| # | Decision | Resolution |
|---|---|---|
| O-1 | Codex in MVP | **Both Claude Code AND Codex are MVP-blocking** (owner kept the comprehensive scope; the PRD-permitted Codex-as-P1 was declined). Codex risks (§9.1, RISKS TR-02/TR-08) are managed in-MVP, not deferred. |
| O-2 | Session survival | **Full resume-or-replay in MVP** (`claude --resume` / `codex thread/resume` with accurate alt-screen VT re-render; replay+relaunch as fallback). The #1 risk (TR-01) is accepted into MVP with mandated deterministic test seams (§14). |
| O-3 | Brain write-path | **Full multi-step bundled Brain action plans + step-by-step approval in MVP** (PRD §25 steps 14-16 as-is). The Gateway MUST support `ActionPlan` (not just single `ActionRequest`) in MVP (§6.2). |
| O-4 | Claude supervision mode | **RESOLVED 2026-06-12 = PTY-PRIMARY** (away-authority; the `OQ-HARN-SPIKE-7` drain DISSOLVED — the June-15 billing split is documented, so the SDK-pool-hard-stop-vs-interactive-exempt asymmetry is docs-determinable). Drive via interactive PTY + `PreToolUse` hooks; SDK = secondary signal. The `can_use_tool`-holes mitigations (O-13) stay locked regardless. SDK-primary remains an additive future option (the daemon-internal trait is unfrozen). Flagged for user return-review (Decisions-tabled). §9.1. |
| O-5 | Design system | **Adopt `NexusOps-ui-kit` (tokens + components + "Graphite Arc") as the canonical design system** (2026-06-07 prototype review). Color is a re-hueable first pass. The UI reconciliation is folded into §11; detail in `docs/ui-review/UI_RECONCILIATION.md`. |
| O-6 | PR Review Workspace | **Full PR Review surface in MVP** (a review mode in Code/Diff: header/checks/reviews/mergeability/risk/Brain-evidence/agent-summary + merge actions; merge re-fetches GitHub per §7.2 and routes through the Gateway at risk≥3). §11.2. |

### §0.2 — Architecture ADRs (locked spine; status-bearing)
| ADR | Decision | Status |
|---|---|---|
| 001 | All-Rust: Rust **daemon** + **Tauri 2.x** shell; **macOS-only** MVP | `[LOCKED]` |
| 002 | **Detached long-lived daemon from day one** owns PTYs/store/Gateway/adapters/Brain/locks; UI reattaches over IPC | `[LOCKED]` |
| 003 | **SQLite (WAL)**, single-writer = daemon; events + projections + projection_offsets + outbox (one txn); FTS5; `user_version` migrations; artifacts by path+hash | `[LOCKED]` |
| 004 | Action Gateway = **`GatewayPort` trait, exposed over Unix-domain-socket IPC**, JSON-RPC; execution **in-daemon**; agents/Brain/UI submit **intents** only | `[LOCKED]` |
| 005 | Project Brain = **stdio MCP sidecar** (FastMCP 3.x, daemon-owned lifecycle) | `[LOCKED-PENDING-SPIKE]` — macOS sidecar notarization `#11992` (`OQ-PLAT-SPIKE-1`); fallback = FastMCP streamable-HTTP on loopback (§13.1) |
| 006 | **One `HarnessAdapter` contract over two lifecycle models** — Claude (SDK/PTY) + Codex (`app-server`) | `[LOCKED]`; Claude drive-mode `[PENDING-SPIKE]` (O-4) |
| 007 | **Dual git** (git2 reads / git-CLI mutations), **octocrab** (+gh-token bootstrap), Linear PKCE/key, **keyring** crate | `[LOCKED]`; git2 read-path on `extensions.relativeworktrees` repos re-verified by `OQ-INT-SPIKE-6` (§24) |
| 008 | Cross-restart locks = **SQLite lease table** (owner + monotonic fencing token + heartbeat) + `pidlock` single-instance | `[LOCKED]` |
| 009 | Terminal: **`portable-pty` in daemon → headless VT → Tauri Channel → xterm.js (WebGL)**; backpressure mandatory | `[LOCKED]` |
| 010 | Survival: UI-restart → reconnect-live; daemon-restart → **resume-or-replay** (O-2) | `[LOCKED]` |
| 011 | Secrets in OS keychain; **Developer ID signing + notarization = early release-blocker** | `[LOCKED]` |

### §0.3 — Shared-contract reconciliations (resolved here; were drifting across specs)
Resolved in dependency order (ID format → actor enum → event envelope → state machines → task model), per the gap-audit completeness critic.
| # | Contract | Canonical resolution | Anchor |
|---|---|---|---|
| R-1 | **ID format** | **Prefixed ULID** `<prefix>_<ULID>` (Crockford base32, lexicographically sortable). External IDs (`branch_name`, `commit_sha`, `pr_number`, `linear_issue_id`, `architecture_anchor`) kept native. | §5.2 |
| R-2 | **Actor/requester enum** | Canonical **audit** actor (`Event.actor_type`) = EM §7 set: `user, project_brain, action_gateway, workflow_runtime, local_runner, session_adapter, integration_syncer, system, remote_client, automation_policy` (EM's `remote_device` → **`remote_client`**). **Request-time** `ActionRequest.requester_type` (AG §9.7) aliases map: `agent_session→session_adapter`, `workflow_pack→workflow_runtime`, `system_policy→automation_policy`. Mapping table is binding (Appendix A). | §5.1, §6.2, §7.1 |
| R-3 | **Event envelope** | EM §6 envelope is canonical; actor/source/sensitivity/visibility enums inlined at §7.1; `remote_client` per R-2. | §7.1 |
| R-4 | **Session state machine** | **17 states** — PRD SESS-3 is authoritative: add **`changes_ready`** (was missing from SOM/UX/DATA_MODEL). Waiting token canonicalized to **`waiting_on_human_input`** (event `SessionWaitingOnHumanInput`). | §5.1 |
| R-5 | **Approval vs ActionRequest** | **Split into two machines.** Approval = `{requested, previewed, awaiting_approval, approved, denied, edited, auto_approved_by_policy, expired, cancelled, escalated}`. ActionRequest/execution = `{submitted, previewed, policy_decided, awaiting_approval, approved, denied, expired, cancelled, queued, executing, succeeded, failed, partially_succeeded, rolled_back, rollback_failed}`. ActionRequest is a **9th** first-class state machine. | §5.1 |
| R-6 | **AgentTeam state machine** | Promoted to a first-class machine (SOM §14, 9 states); MVP exercises `draft/starting/active/waiting_on_human/blocked/completed/archived`; a `agent_teams` registry row + `proj_agent_team` projection back the `active_teams` counter. | §5.1 |
| R-7 | **ExecutionProfile / WorkflowInstance / Worktree enums** | ExecutionProfile = `{available, active, in_use, rate_limited, auth_expired, misconfigured, disabled, unknown}` (config vs runtime split, §7.2). WorkflowInstance = PRD WF-3 set with `ready_for_team_run` (not `_mode`); team-run-in-progress tracked on AgentTeam, not the instance. Worktree status = **derived**: git-sync axis (clean/dirty/untracked/conflicts/ahead/behind) + lifecycle overlay (creating/locked/pr_open/merged/prunable/deleted) collapsed by a precedence function (locked/conflicts > dirty > ahead/behind > clean), §7.2. | §5.1, §7.2 |
| R-8 | **Task vs PlanTask** | **One `tasks` table** with `kind ∈ {plan_task, external_task}` + a **superset** state machine; Plan View renders the plan-task subset (`not_started, ready, in_progress, in_review, blocked, done, deferred`); external tasks render the GitHub/Linear subset. Resolves SOM §37 Q2 (ADR-012). | §5.1, §7.2 |
| R-9 | **Illegal/terminal transitions** | Each machine declares its legal edge set + terminal/absorbing states (Appendix A links the enums); an event implying an out-of-set transition is logged as a projection-degraded marker (EM §23), never silently applied. | §5.1, §17 |
| R-10 | **Hash-chain** | `payload_hash`/`previous_event_hash` columns reserved; tamper-evidence chain `[DEFERRED]` (post-MVP). | §7.1 |

---

## Executive summary

NexusOps is a **desktop-first, local-runtime AI engineering control plane** for macOS: one cockpit to **dispatch, supervise, review, deliver, and remember** the work of many AI coding agents (Claude Code + Codex) across multiple local projects (`PRD §1-2`). The **Session** is the atomic operational unit, and a strict chain of ownership (Project → Session → Worktree/Branch → Terminal → Task → Harness → Execution Profile → Diffs → Commits → PR → Brain episode → Events) is preserved on every surface.

The system is **three local processes** plus the agent subprocesses they supervise (§4): (1) the **NexusOps Daemon** (Rust, detached, long-lived) — the trust core and the *only* mutator of state, owning the append-only SQLite event store (sole writer), the Action Gateway (single mutation chokepoint), the harness adapters, PTYs, git/worktree ops, integration syncers, the lease-lock manager, the projection engine, and the Brain sidecar's lifecycle; (2) the **NexusOps UI** (Tauri 2.x — Rust host + system WebView) — a *reattaching client* that reads projections and submits intents over a Unix-domain-socket `GatewayPort`, renders xterm.js terminals, and never writes the DB; (3) the **Project Brain sidecar** (Python/FastMCP, stdio MCP) — a *sibling product* that reasons over project memory and **proposes** action plans but never executes (Brain internals are out of scope here; only the seam is specified).

It is an **event-sourced, projection-driven, capability-gated** design (§7): every important fact is an immutable event in append-only SQLite; the UI reads rebuildable projections; every mutation is a typed, risk-classified, previewed, approved, audited `ActionRequest`/`ActionPlan` through the Action Gateway (§6). Three architectural laws hold (§4.2): single mutation chokepoint; events-are-facts-UI-reads-projections; reason (Brain) vs execute (platform). Both harnesses expose a structured supervision + approval surface that maps onto the Gateway (Claude's `can_use_tool`; Codex's `app-server` approvals) so status and worktree/task association are reliable without scraping terminals (§9.1) — with the important caveat that `can_use_tool` is *not* a complete chokepoint (subagent/background/permission-mode holes, §9.1, O-13), mitigated by default-mode-only + `PreToolUse` defense-in-depth.

The MVP proves the full thesis via the **PRD §25 demo** (§19.1) and is deliberately **comprehensive** (owner decision): both harnesses live, full session survival, full Brain action plans. The hardest risks — terminal survival fidelity, Codex protocol churn, the Claude Agent-SDK credit pool, and the macOS Python-sidecar notarization — are tracked as managed risks and pre-build spikes (§24), not silently assumed away.

---

## §1 — Goals & non-goals

**Goals (MVP)** `[LOCKED]` (`PRD §15`, scoped by the §25 demo):
- One cockpit supervising many local agent sessions (Claude Code + Codex) with reliable, non-scraped status and worktree/task association.
- Session atomic; full ownership chain everywhere; attention-first ordering (`PRD §5.2`).
- Every mutation typed + previewed + risk-classified + approved + audited through the Action Gateway; **no mutation path bypasses it** (INV-SEC-1, §15).
- Append-only event store + rebuildable projections; immutable audit; fail-closed on audit-write; crash-recoverable; UI restart-safe; agents survive UI quit AND resume/replay across daemon restart (O-2).
- Worktree-isolated agent work; git basics + PR creation; manual task/plan/session/worktree/PR linking.
- Project Brain drawer: read + evidence chips + **multi-step action-plan propose/preview/approve** (O-3); Brain never executes directly.
- Workflow-pack **detection** (cc-crew first); platform fully usable on **basic** projects (no pack).
- Local trust boundary; secrets in OS keychain; redaction before persist/embed/sync; consented setup.

**Non-goals (MVP)** `[DEFERRED]` (`PRD §15 Non-Goals`, §17, DFR §6): web app; cloud/remote runner; multi-user collaboration; iOS companion (seam designed, not built); full IDE replacement; bidirectional Linear sync; fully autonomous merges; Brain policy-automation; full cc-crew personalization/upgrade UI; full transcript→commit linking; Windows/Linux; multi-repo projects; hash-chain tamper-evidence; **agent egress isolation** (largest residual risk, §15); Project Brain internals (sibling product).

---

## §2 — Product definition & scope

Authoritative: `PRD §1-7`, `PRODUCT_CANON §2-16` (not restated). Load-bearing framings the architecture honors: five responsibilities (Dispatch · Supervise · Review · Deliver · Remember/Reason); six product layers + two cross-cutting systems (Brain, Workflow Packs); 20 screens (§11); MVP/P1/P2 tiers (§19).

---

## §2.5 — Subsystem dependency DAG & parallelization seams `[LOCKED — derived 2026-06-11]`

> Added at the production-grade posture migration (2026-06-11). Nodes are the §6 daemon modules plus the three cross-cutting surfaces (`shared/`, `ui/`, packaging); edges = "must exist before / consumes the contract of," cross-checked against the as-built Phase-0/1 code. The `IMPLEMENTATION_PLAN.md` **Parallelization plan (Track map)** is *derived from* this section refined by per-task `Depends on:` edges — this section is the architecture-level authority; the Track map is its plan-level rendering. New §N anchor appended in numbering space; no existing anchor moved.

```
                 shared/ contracts (§5.0–§5.3, §7.1)   [BUILT 0.5 — the fan-out trigger]
                       │
      ┌────────────────┼─────────────────────────────────────────┐
      ▼                ▼                                         ▼
  Redactor (§15) ─► eventstore (§7) ─► projections (§7)       ui (§11)  [BUILT P6]
   [BUILT 1.1/1.7/      │ [BUILT] │       [BUILT 1.2]          mock GatewayPort + fixtures;
    2.0-SEC]            │         └─► outbox (§7) [BUILT 1.3]  real port ← ipc reads [BUILT];
                        ▼                                      mutation seam ← gateway [P2]
                   locks (§5.1/§17)  [BUILT 1.4]
                        │
      policy (§15) ─────┤
        [P2.2]          ▼
                   gateway (§6 — the INV-SEC-1 chokepoint)  [P2]  ◄── ipc (§6.4)
                        │                                          reads [BUILT 1.5/1.6] · submit_* [P2]
   ┌───────────┬────────┼───────────────┬───────────────────┐
   ▼           ▼        ▼               ▼                   ▼
 git (§9)  integrations harness (§9.1)  brainclient (§13.1) workflow (§13.2)
 [P5]      (§9) [P7]    [P3 — claude ∥ codex ∥ Fake]  [P8 — decoupled]  [P9]
                            │
                            ▼
                      terminal (§9, ADR-009 — display-only) [P3]
 usage (§11/§18) [P3+] ← harness telemetry        notifier (§10) [P10] ← outbox
                        ▼
              packaging/signing (§16) + PRD §25 demo (§19.1)  [P10 — all-hands converge]
```

**Binding edges:** `shared/` before everything (the 0.5 freeze is the fan-out trigger) · Redactor before persist/embed/sync (§15 — all three sinks) · `eventstore` before {`projections`, outbox drain, `locks`, `gateway` audit writes} · `locks` before `gateway` execution (the fencing oracle `locks::validate_held`) and before every executor · {`policy` + `eventstore` + `locks`} before `gateway` · **`gateway` before every mutating executor/adapter** — `git` mutations (§9), `integrations` writes (§9), `harness` intercept→intent (§9.1), Brain proposals (§13.1), `workflow.command.invoke` (§13.2) — this is INV-SEC-1 expressed as a build edge · `ipc` depends on the GatewayPort **trait** + `shared/` wire types, not on gateway internals (transport ∥ pipeline — ADR-004/§4.3) · `terminal` is display-only, parallel to status derivation, never a state source (§9.1) · `brainclient` is decoupled — the core loop never blocks on Brain (§13.1) · packaging/signing precedes production keychain guarantees (§16, THREAT_MODEL T-13).

**Parallelization seams (load-bearing):**
- **S1 — frozen contracts** (`shared/` → all): lets independent tracks build against frozen enums/IDs/schemas, mocking the other side — validated in practice by the ui track building Phase 6 entirely on `shared/` 0.5 + `MockGatewayPort`.
- **S2 — GatewayPort trait** (§6.1): transport (`ipc`) ∥ pipeline (`gateway`) ∥ callers (ui, future RemoteClient).
- **S3 — HarnessAdapter trait** (§9.1): Claude adapter ∥ Codex adapter ∥ `FakeHarness` (tests).
- **S4 — executor-adapter interface** (§12, AG §15.3): `git`/`integrations` edges build against the frozen Gateway interface (mocked until P2 lands).
- **S5 — Brain decoupling** (§13.1): the Brain arm parallelizes freely; only its Gateway-intent seam is shared.
- **S6 — display-only terminal** (§9.1): terminal host ∥ adapter status pipelines.
- **S7 — ui vs daemon** (§14): mock GatewayPort + fixture projections; the ui's mutation/intent seam unblocks only at the gateway mutation-method freeze (P2).

**Forced-serial spine (the critical path):** `shared/` freeze → `eventstore` → **`gateway`** → `harness` intercept → survival (P4) → demo (P10). The bottleneck is the **gateway**: every mutating feature queues behind its mutation-method + `ActionRequest`/`ActionPlan` + §6.3-catalog freeze.

**§2.5-seam shared-contract models** (the brief-template schema-snapshot rule keys off this list — an invariant touching one of these on a crossed edge requires the snapshot test): EventEnvelope + enums (§7.1) · the 22 IDs (§5.2) · the 10 status machines (§5.1) · GatewayPort wire surface (§6.1/§6.4) · ActionRequest/ActionPlan/Approval/ActionResult (§6.2) · ActionTypeCatalog (§6.3) · HarnessAdapter normalized types (§9.1) · EventTypeRegistry (§7.1) · BrainEventMapping (§13.1).

---

## §3 — Locked architecture decisions

See **§0.2** (ADR spine, with status) and `docs/planning/DECISIONS.md` (full ADRs: options, rationale, fallbacks, what-would-change-this). Product decisions: `PRODUCT_CANON §17`.

---

## §4 — System overview

### §4.1 — Process topology `[LOCKED — ADR-001/002]`
```text
macOS host = trust boundary (DFR §5)
 ┌ NexusOps UI (Tauri: Rust host + WebView) ─ reattaching client; xterm.js; 20 screens
 │     ↓↑  UDS GatewayPort (JSON-RPC: intents + projection reads + subscriptions; Terminal Channel)
 ┌ NexusOps Daemon (Rust, detached, long-lived) ─ THE TRUST CORE, sole mutator & sole DB writer
 │   Action Gateway · SQLite event store (WAL) · projection engine · harness adapters ·
 │   terminal mgr (PTY) · git/worktree mgr · GitHub/Linear syncers · lease-lock mgr ·
 │   brainclient · ipc(UDS) · policy · usage · notifier
 │     ↓↑ stdio MCP (tools=propose; notifications→events)   → Project Brain sidecar (Python/FastMCP)
 │     ↓↑ PTY + SDK/app-server (can_use_tool / approvals)   → Agent subprocesses (Claude / Codex)
 │     ↓↑ octocrab / git CLI (tokens ← keychain)            → GitHub / Linear / git remotes
```

### §4.2 — The three architectural laws `[LOCKED]`
1. **Single mutation chokepoint.** All state change flows through the Action Gateway in the daemon; agents/Brain/UI/future-RemoteClient submit *intents* only; only the Gateway executes and only the daemon writes the DB. Enforced + tested as **INV-SEC-1** (§15).
2. **Events are facts; the UI reads projections.** Immutable append-only events; rebuildable projections drive the UI; source-of-truth discipline per §7.2.
3. **Reason vs execute split.** Brain plans/queries/proposes; the platform executes; Brain has zero direct privileged operations (`PBI §1`).

### §4.3 — Why three processes `[LOCKED — ADR-002]`
A PTY child dies (SIGHUP) when its owner exits, so "agents survive UI quit" (`PRD DESK-7`) requires a detached daemon. The daemon keeps the trust core (credentials, mutations, audit) a small memory-safe Rust surface separate from the restartable UI, and forces the Gateway behind an explicit IPC seam from day one. The future iOS companion reuses the **GatewayPort trait + ActionRequest model** (not the UDS transport, which is local-only; a future relay/tunnel terminates at a daemon-side adapter that re-injects intents — §13.1). Cost: daemon lifecycle (single-instance via `pidlock`, stale-socket reclaim, UI↔daemon version handshake, orphan/zombie kill) — mitigated (§16, §17).

---

## §5 — Domain model

Authoritative object catalog: `SOM` (~30 objects). Persistence: §7, `docs/planning/DATA_MODEL.md`. Four canonical chains (`SOM §35`) are invariants the event/data model keeps traceable: ticket→merge, plan→implementation, brain-action, workflow-personalization.

### §5.0 — Contract source-of-truth & propagation `[LOCKED — owner 2026-06-07; OQ-DATA-SPIKE-5]`
The cross-language representation of every shared contract (the §5.1 status enums, the §5.2 IDs + ULID format, the §7.1 actor/source enums, §6.1/§6.4 GatewayPort + wire schemas, the §7.1 `EventTypeRegistry`, the §6.3 `ActionTypeCatalog`, Appendix A models) follows **one mechanism** — **Option A**, locked by the owner (Phase 0.5 / `OQ-DATA-SPIKE-5`):

1. **Rust `shared` crate = the native contract authority.** The trust core owns its own types as idiomatic Rust (load-bearing IDs are **newtypes**, enums are `serde`-closed). No type in the trust core is generated; the daemon, as the single authority + sole mutator, authors the contract.
2. **`schemars` → JSON Schema = a first-class, published, versioned interchange artifact** (not a throwaway build output): checked into `shared/contracts/schema/`, stamped with `CONTRACT_VERSION`, regenerated from the Rust authority, and **CI-diff-gated** (a test fails when the checked-in schema ≠ what Rust emits — the same drift-gate pattern as the Codex app-server schema, `OQ-HARN-SPIKE-4`). This neutral artifact is what the **sibling Python Brain** and any external consumer bind to (no Rust import).
3. **TS (Zod) + Python (Pydantic) are generated, drift-caught consumers** of the published schema; the UI's "parse, don't trust" boundary uses the generated Zod validators.
4. **Closed-enum / reject-unknown end-to-end** — unknown contract values are rejected at every language boundary (`serde` closed enums → JSON-Schema `enum` → `z.enum` → Pydantic), preserving the fail-closed posture (§15/§17).

Rationale (single-authority thesis · preserves the newtype typing posture · schemars is already the IPC-schema mechanism · the reject-unknown safety property maps cleanly; an external IDL/codegen-into-the-trust-core was rejected as inverting authority + generating bare types in the safety-critical module). Recorded as a direct architecture-doc note on the owner's lock; a user-invoked `/arch-finalize` re-validation (natural at Phase-0 exit) would re-scrutinize it like any other anchor. **Re-validated + upheld unchanged at the Phase-0-exit `/arch-finalize` re-run (2026-06-07): the frozen `shared/` crate matches this anchor byte-for-value (CI schema-diff gate green); §5.0 stands ratified as written.**

### §5.1 — Status state machines (canonical, reconciled) `[LOCKED — R-4..R-9]`
Ten machines (the 8 of the `/arch-draft` rough draft + **ActionRequest** [R-5] + **AgentTeam** promoted to first-class [R-6]). **All ten are frozen** in `shared/` (the first 9 at 0.5; the 10th, **ExecutionProfile**, at **0.5b** — 4.0b-1 L1, 2026-06-12, CONTRACT 0.24.0; 9 values incl. `credit_exhausted`, terminal `{disabled}` only) (§5.0; Appendix A). Enums are binding; each declares terminal states; illegal transitions → degraded marker (R-9, §17). Stored as `TEXT status` on the named projection/registry row.

| Machine | Canonical states (terminal in **bold**) | Stored on |
|---|---|---|
| **Session** (17, R-4) | creating, starting, active, thinking, running_command, editing_files, running_tests, waiting_on_permission, waiting_on_human_input, waiting_on_external_service, idle, stale, changes_ready, **failed**, **completed**, **archived**, **killed** | `proj_session.status` |
| **Task** (R-8 superset) | unassigned, queued, assigned, ready, in_progress, blocked, needs_clarification, in_review, changes_ready, pr_opened, needs_review, requested_changes, done, deferred, **merged**, **closed**, **abandoned** | `tasks.status` (kind-scoped subsets) |
| **Worktree** (derived, R-7) | git-axis {clean,dirty,untracked_files,conflicts,behind_base,ahead_of_base} + overlay {creating,locked,pr_open,merged,prunable,**deleted**} via precedence fn | `proj_worktree.status` (derived) |
| **PullRequest** | draft, open, checks_pending, checks_failing, needs_review, changes_requested, approved, mergeable, conflict, **merged**, **closed** | `proj_pull_request.status` (GitHub-authoritative cache, §7.2) |
| **WorkflowInstance** (R-7) | not_detected, pack_available, needs_personalization, personalization_in_progress, generated_review_required, active, ready_for_team_run, degraded, drift_detected, upgrade_available, **archived**, **detached** | `workflow_instances.status` |
| **ProjectBrain** | not_configured, indexing, ready, partial_index, stale, graph_degraded, transcript_ingestion_off, transcript_ingestion_active, reindex_required, **error** | daemon-cached, `brain_status_reported_at` staleness (§7.2) |
| **Approval** (R-5) | requested, previewed, awaiting_approval, **approved**, **denied**, **edited**, **auto_approved_by_policy**, **expired**, **cancelled**, **escalated** | `approvals.status` + `proj_approval_queue.status` |
| **ActionRequest** (R-5, NEW) | submitted, previewed, policy_decided, awaiting_approval, approved, **denied**, queued, executing, **succeeded**, **failed**, **partially_succeeded**, **rolled_back**, **rollback_failed**, **cancelled**, **expired** | `action_requests.status` |
| **ExecutionProfile** (R-7) | available, active, in_use, rate_limited, auth_expired, misconfigured, **disabled**, unknown, credit_exhausted | `execution_profiles.status` (config vs runtime split) |
| **AgentTeam** (R-6) | draft, starting, active, waiting_on_human, blocked, reconciling_outputs, **completed**, **failed**, **archived** | `agent_teams.status` / `proj_agent_team` |

**Time-derived transitions** (R-9): `stale` (Session/Brain/Profile) and git-cache staleness are read-time derivations from a liveness clock + threshold (configurable policy; Session default = heartbeat-age > 3× `refreshInterval`). On projection rebuild, `stale` is **recomputed** from `last_heartbeat_at` (not replayed) — §7.2.

**Legal-edge sets (R-9) — codified 2.1b.** The ActionRequest + Approval machines' **legal transition edges** (not just the terminal sets) are now pinned in `daemon/src/gateway/{request,approval}::can_transition` (fail-closed default-deny; the canonical source). The load-bearing **INV-SEC-1 safety property**: the ONLY edges reaching `executing` are `awaiting_approval→approved→queued→executing` (the human-approval gate) or `policy_decided→queued` (the §6.3 risk-0 policy-allow auto-path, gated on `PolicyDecision=allow`; unreached pre-2.2 — the 2.1b policy is a require-approval-for-all stub) — no edge reaches execution skipping both gates (security-reviewer-verified, 2.1b L1). **Reconcile (away-authority lead ruling, 2026-06-11, fold into 2.1b; flagged for user return-review + next `/arch-finalize`):** `ActionRequest::denied` is now **terminal** (bolded above) — the 0.5 freeze omitted it from the terminal set (oversight), but a denied request never executes (INV-SEC-1) + has no legitimate forward edge, matching the gateway guard's sink treatment + the Approval machine (whose `denied` is already terminal). The fix is the Rust `is_terminal()` terminal-set + the `test_terminal_states_marked` expected set — **NOT serialized → no wire/schema/CONTRACT/version change** (a `prj_→eprj_`-class Rust-internal reconcile, even lower-stakes than the 2.1a status-name rename). **2.4 L1/L5 edge additions** (`can_transition`; the statuses were already frozen): the **rollback edges** `succeeded`/`partially_succeeded → rolled_back`/`rollback_failed` (L1; `rolled_back`/`rollback_failed` are TRUE sinks — rollback is NOT a backdoor to re-execute; the rollback DRIVER is later-phase) + **`Queued→Failed`** (L5; the crash-reconcile of a `queued` orphan that never ran). Rust-internal guard-table edges, no wire change.

### §5.2 — Shared IDs + ID format `[LOCKED — R-1; PBI §3]`
The **22 shared IDs** (PBI §3) are the cross-product contract: `workspace_id, project_id, repo_id, worktree_id, branch_name, commit_sha, session_id, agent_team_id, execution_profile_id, workflow_pack_id, workflow_instance_id, workflow_command_id, implementation_plan_id, plan_task_id, architecture_anchor, linear_issue_id, github_issue_number, pr_number, action_request_id, event_id, artifact_id, evidence_item_id`. Platform-minted IDs use **prefixed ULIDs** (`sess_…`, `evt_…`, `wt_…`, `act_…`); external IDs keep native values. **Harness→session_id:** Claude id is settable (1:1); Codex has no settable id → platform mints `sess_<ULID>`, maps the returned `thread_id` keyed on `(cwd, thread_id)` via `harness_session_map`; re-association after restart via `thread/list?cwd=` (harness is authoritative; the map is a re-derivable cache, §7.2).

### §5.3 — Desktop-addendum objects `[LOCKED status; objects PROPOSED→adopted]`
Closes the SOM gap (`DFR §7`). MVP-live: **LocalRunner** (the daemon's execution surface; sessions bind to it; minted per daemon start), **EventProjection** (projection catalog/metadata, §7), and the local **Device** (the stable desktop-host identity, registered at cold-start per §16 — register-if-absent; LocalRunner is per-start and cannot be the stable host). `[DEFERRED]` for iOS: **RemoteClient** (maps to `requester_type=remote_client`/`actor_type=remote_client`, R-2) **and the multi-device/pairing dimension of Device** (multiple hosts, remote trust-boundary anchoring). **Device-role = MVP-live desktop-host, USER-RULED Option A 2026-06-10** — reconciles §5.3 to the binding §16 "registers desktop-host Device" step (the freeze's blanket `[DEFERRED]` on Device was stale, surfaced by 1.6a-L3 actually registering one); `DesktopObjectKind::Device.is_deferred()` is now `false` (true only for RemoteClient). `OQ-DATA-SPIKE-5` reconciles these into SOM. Full fields: Appendix A, `DATA_MODEL §6`.

---

## §6 — Daemon module architecture

Rust modules; clear ownership; all writes funnel through the Gateway → event store. (`docs/planning/ARCHITECTURE_DRAFT §6` table, corrected: the harness module detail is **§9.1**.)

`gateway` (§6.1/§6.2/§6.3) · `eventstore` (§7) · `projections` (§7) · `harness` (§9.1) · `terminal` (§9, ADR-009) · `git` (§9) · `integrations` (§9) · `locks` (§5.1/§7) · `brainclient` (§13.1) · `workflow` (§13.2) · `ipc` (§6.4) · `policy` (§15) · `usage` (§11/§18) · `notifier` (§10, DESK-8).

### §6.1 — `GatewayPort` contract `[LOCKED — ADR-004]`
A Rust trait implemented **in the daemon process** (the executor runs in-daemon, *not* in a separate process and *not* in the UI); all out-of-daemon callers (UI, future RemoteClient) reach it **only** over the UDS transport (§6.4). JSON-RPC method surface (binding; Appendix A):
| Method | Params → Result | Class |
|---|---|---|
| `submit_action` | `ActionRequest` → `ActionAck{action_request_id,status}` | mutation-intent |
| `submit_action_plan` | `ActionPlan` → `PlanAck{plan_id,...}` | mutation-intent (O-3) |
| `preview_action` | `action_request_id` → `ActionPreview` | query |
| `approve` | `approval_id, edits?, step_id?` → `Ack` | decision |
| `deny` | `approval_id, reason` → `Ack` | decision |
| `get_projection` | `name, scope, page` → `ProjectionPage` | query (read-only) |
| `subscribe` | `{projection|events|terminal, filter}` → stream handle | subscription |
| `get_capabilities` | → `Capabilities{protocol_version,...}` | query |
Only `submit_*` are mutation entrypoints; `get_*`/`subscribe` are read-only. Agents/Brain feed intents *into* these (MCP tools/approvals → `submit_*`); they are never the transport.

### §6.2 — Gateway core data model `[LOCKED — AG §9]`
Inlined contracts (full fields: Appendix A, sourced AG §9.1-§9.9/§12.2): `ActionRequest`, `ActionPlan` + `ActionPlanStep` + `ActionDependency` (bundled plans, MVP per O-3), `ActionPreview`, `Approval`, `ActionResult`, `ActorRef` (R-2), `ResourceRef` (21 types), `EvidenceRef`, `PolicyDecision{allow|require_approval|require_step_approval|deny|downgrade|needs_more_context}`. **Risk 0-4** (AG §7) drives preview depth + approval; risk ranges (e.g. `git.delete_worktree` 3-4) are resolved per-action in §6.3. Critical (4) actions are never in approve-all by default. **Stale-precondition re-check** (AG §16.4): after lease+fencing acquisition and immediately before `execute()`, re-resolve resources + re-read the live source (§7.2); on mismatch → `ActionFailed(stale_precondition)`, regenerate preview, **require fresh approval if the previewed diff/resource changed** (never execute a different mutation than was approved). **[IMPLEMENTED 2.4 L4]** (`d779671`): a `PreconditionOracle` seam (consulted AFTER L3 lease+fencing, BEFORE the executor) — `NullPreconditionOracle` is the 2.4 production default (the real per-action_type git2/octocrab/session re-read lands Phase 5/7); `Changed` → regenerate the preview (through the §15 gate) → `ActionFailed{StalePrecondition}` (COMMIT) → `Err`→ §6.4 `precondition_stale` (the re-approvable card). "Fresh approval" = re-submission (no Executing→AwaitingApproval back-edge); 2.4 conservatively fails on ANY change (the finer effect-changed discrimination needs real preview-diffing, Phase 5/7).

### §6.3 — MVP action-type catalog `[LOCKED — AG §28.2]`
The MVP action types, each with a **binding per-type contract** (params schema, locked risk, required preview class, idempotency-key formula, executor) — full table is Appendix A row `ActionTypeCatalog`. The MVP set (owner kept comprehensive scope, so this is the full ~21 at freeze, not a trimmed Tier-0): `brain.ask, brain.sync, brain.summarize_session, project.rescan, workflow.detect, workflow.command.invoke, plan.link_task, session.create, session.attach_terminal, session.send_message, session.pause, session.resume, git.status, git.diff, git.create_worktree, git.create_branch, github.create_pr_draft, github.create_pr, linear.link_issue, linear.create_issue, code.open_file, review.request_agent_fix`. **As-built = 28 at CONTRACT 0.33.0** (additive per-phase, all snapshot-pinned): `session.kill`/`session.profile_change` (4.0b-1) · `git.stage_hunk`/`git.unstage_hunk`/`git.discard_hunk` (4.0b-ui1) · **`integration.connect`** (edges-029, Wave-C — `ExecutorKind::Integration`). `workflow.command.invoke` with a null `input_schema` is risk-floored to require approval (cannot be standing-granted) — preserving the typed-actions invariant at the approval gate (OQ-WP-5).

### §6.4 — IPC wire contract `[LOCKED — ADR-004]`
One framing: **4-byte big-endian length prefix + JSON body** (newline-framing dropped). Handshake: `HelloFrame{protocol_version,client_kind,app_version}` → `HelloAck{protocol_version,daemon_version,capabilities}` | `VersionSkewError`. Fixed `MAX_FRAME_SIZE`. JSON-RPC error codes: `version_skew, frame_too_large, unknown_method, unauthorized_peer, policy_denied, precondition_stale, protocol_error`, **`fencing_conflict`, `internal_error` (added 2.4 L1, lead-ruled away-authority Option A — the §17 safety failures drive three DISTINCT §11.5 cards: `fencing_conflict`=never-auto-resolved hard-conflict [rule #6], `precondition_stale`=re-approvable, `internal_error`=fail-closed integrity alert; collapsing them would render fencing as re-approvable → break rule #6 at the UI; Decisions-tabled)**. The **Terminal Channel** (ADR-009) multiplexes over the same UDS socket by a frame-type tag: output `{terminal_id,seq,bytes}`, input `{terminal_id,bytes}`, and explicit backpressure control frames (pause/resume + high/low watermark) realizing app-level flow control. Peer auth: §15 (`getpeereid`).

> **AS-BUILT note (3.4 Terminal Channel freeze, 2026-06-12 — CONTRACT 0.21.0; LESSON §24):** the frames froze in `shared/src/ipc.rs` as **`TerminalOutputFrame{terminal_id,seq,data}`** (daemon→client) · **`TerminalInputFrame{terminal_id,data}`** (client→daemon) · **`TerminalControlFrame{terminal_id,kind:TerminalControlKind(pause|resume)}`**, with **`ServerFrame::TerminalOutput`** filling the previously-reserved Terminal tag slot (the §6.4 frame-mux discriminant). **Encoding = JSON-base64 MVP** — `data` is base64 so raw PTY bytes ride the unchanged 4-byte-len+JSON codec (LESSON §7); the **binary fast-path was DEFERRED by the 3.5 §18 benchmark (2026-06-12, data-justified)** — the daemon JSON-base64 pipeline sustains **~3,554 MiB/s** (~100× the xterm.js 5–35 MB/s consuming ceiling; base64 = 1.3335× wire expansion), so base64 is **not** the bottleneck and the binary fast-path's marginal value is small (revisit only if a real consumer ceiling rises materially; see §18 unbudgeted-hot-paths). `terminal_id` is a **daemon runtime handle** (a `String` wire / typed newtype via the §14 IdGen seam), NOT one of the frozen-22 `IdKind` (the `subscription_id` precedent). A **`TerminalProcessExited{terminal_id,exit_code,signal}` event** (§7.1) records PTY death as a **non-mutation observation event** (write-actor, NOT the Gateway — the `DeviceRegistered`/`TelemetrySampled` precedent, LESSON §10/§23). The daemon PTY host (`portable-pty` behind a `Pty`/`FakePty` seam, **display-only #9 — never a status source**) + watermark backpressure (a pure `next_terminal_action` classifier, LESSON §12; bounded buffer, never back-pressures the write-actor) land daemon-side; the ui xterm.js host (6.3d) + the headless-VT/scrollback-fidelity follow-on consume this frozen contract.

> **AS-BUILT note (075a, 2026-06-17 — the headless-VT model; LESSON §57):** the daemon-internal `HeadlessVt` (`daemon/src/terminal/vt.rs`, wrapping `vt100` 0.16.2 — Approach A, Decision-20, verified at authoring) maintains a headless terminal **screen + scrollback** from the PTY byte stream — **DISPLAY-only (#9, never a status source**; a scoped grep-pin). It consumes the **frozen** Terminal Channel (NO contract change; CONTRACT stays 0.38.0). Two `vt100` traps banked in LESSON §57 (`scrollback()`=scroll-position-not-count → a `set_scrollback(MAX)` probe cached behind `&self`; the probe guards on `alternate_screen()` else the capacity-0 alt grid clobbers the count). The 075-arc remainder: serialize + replay-fidelity-vs-golden-corpus = **075b**; the `TerminalSession` read-pump tap + the `ScrollbackStore` seam + the `decide_resume`→`Replayed` recovery wiring (`runtime/recovery.rs`) = **075c**; the §15-gated durable scrollback store (Redactor-gated persist + 0600 sidecar pointer + retention; security-reviewer every layer) that makes `Replayed`-after-daemon-restart reachable = **075d** (user-ruled in-scope).

**[IMPLEMENTED 1.5]** (`daemon/src/ipc/` + `shared/src/ipc.rs`; **CONTRACT_VERSION → 0.11.0**, the cross-language IPC contract). The §6.1 **read surface** is live (`get_projection`/`get_capabilities`/`subscribe`); **the mutation methods (`submit_action`/`preview_action`/`approve`/`deny`) are LIVE [IMPLEMENTED 2.1b]** (peer-auth-FIRST over UDS; `ActionAck`/`Ack`; the §6.2 pipeline behind them; `submit_action_plan`/`PlanAck` → 2.1c). Framing = 4-byte BE length prefix + JSON body, **`MAX_FRAME_SIZE` = 8 MiB** (oversized rejected from the prefix **before** body alloc). Peer-auth = **`getpeereid()` FIRST in the per-connection handler** (reject uid≠daemon-uid; **NOT `SO_PEERCRED`**; safety rule #7; unsafe FFI isolated + fail-closed); socket perms defense-in-depth. **Two version axes:** **`CONTRACT_VERSION`** (the §5.0 schema/codegen artifact) vs **`protocol_version`** (the wire-handshake skew check; **`SUPPORTED_PROTOCOL_RANGE = {min:1,max:1}`**, daemon-authored). **`protocol_error`** added to the error-code set (lead-ratified, away-authority 2026-06-08 — a bad-first-frame / handshake-required / malformed-frame / bad-params violation; `unknown_method` reserved for a genuine unknown method name). Frame-type multiplexing = an internally-tagged **`ServerFrame`** (`frame_type: rpc_response | subscription_push`; **Terminal reserved**) over the **unchanged** codec — the Terminal-Channel raw-byte encoding (JSON-base64 vs a binary fast-path) is a **Phase-3 decision with throughput data**. Read methods use **read-only WAL** (single-writer preserved). `bind()`/accept-loop spawn + the live subscribe delta-source (`EventStore::append`→broadcast→subscriber) **LANDED 1.6b** (`26898a8`/`9448035`/`8d06ed7`/`f9c31b1`; `main.rs` `#[tokio::main]` = the production entry; reads + the subscribe **SOURCE** are LIVE in-process — every committed append publishes a `ProjectionDelta`; the socket subscribe-**SERVE** push is carved → **1.6d**). LESSON §7 + §9. `(P1.5, brief 007; runtime P1.6b, brief 028)`

---

## §7 — Data & state model

Authoritative: `docs/planning/DATA_MODEL.md` (full DDL). One daemon-owned SQLite DB (WAL) at `~/Library/Application Support/NexusOps/nexusops.db`; single writer = the daemon; large artifacts content-addressed on disk; harness transcripts referenced in place (Codex rollout dir pre-created 0700, files 0600 — §15). Tables: `events`, `object_refs`, the MVP projections (`proj_*`), `projection_offsets`, `outbox`, `leases`, `artifacts`, `tasks` (R-8), registry tables (projects/repositories/execution_profiles/workflow_instances/integration_connections/command_registry/agent_teams), `action_requests`/`approvals`, `harness_session_map`, FTS5.

**MVP projections** (rebuildable; tracked by `projection_offsets`): ProjectActivity, Session, ApprovalQueue, Worktree, **PullRequest** (added — §7.2), PlanProgress, ProjectGraph, **AgentTeam** (R-6), AuditTrail, UsageLedger. A single event may update multiple projections **within the one event-commit transaction**; the UI subscription pushes deltas so all subscribed surfaces (sidebar, graph, queue) reflect the same event coherently (demo step 7 fan-out, §14 test).

### §7.1 — Event envelope contract `[LOCKED — EM §6; R-2/R-3]`
Required: `event_id, seq, event_type, event_version, occurred_at, recorded_at, workspace_id, actor_type, actor_id, source_type, source_id, correlation_id, sensitivity, redaction_status, payload_json, schema_version`. Optional: `project_id, session_id, agent_team_id, causation_id, action_request_id, approval_id, workflow_run_id, object_refs, idempotency_key, visibility, redaction_engine_version, payload_hash, previous_event_hash` (last two reserved, R-10). `seq` is the canonical order (not `occurred_at`; clocks skew — both kept). **Enums** (inlined; frozen in `shared/` across 1.1 — envelope + source/sensitivity/visibility @ **0.6.0** (df753aa); `redaction_status` + gate @ **0.7.0**, redaction commit): `actor_type` (R-2, 10 values incl. `remote_client`); **`source_type` = the closed MVP set of 15** (`desktop_ui, local_daemon, action_gateway, terminal_supervisor, claude_code_adapter, codex_adapter, git_executor, github_syncer, linear_syncer, workflow_pack_runtime, project_brain_indexer, project_brain_retriever, project_brain_action_planner, usage_meter, remote_relay` — EM §8 listed these as "Examples"; declared **closed** at the 0.5.0/§5.0 freeze: reject-unknown per §15 fail-closed; a new source = additive §5.0 value); `sensitivity` = `public|internal|confidential|secret|restricted` (EM §9; terminal output defaults `restricted`); `visibility` = `user|project|workspace|system`; **`redaction_status` = `unredacted|redacted`** (§15 — the writer fail-closes: an event may NEVER persist `redaction_status='unredacted'`; `redaction_engine_version` records which Redactor processed it — prefix vs the 1.7 entropy engine; quarantine diverts → `SensitiveOutputRedacted`). `correlation_id` mandatory; `causation_id` = immediate prior `event_id` (typed `EventId`). The **MVP event-type registry** (enumerated types + per-type payload schema + version + consuming projection + sensitivity default) is Appendix A row `EventTypeRegistry` — the contract golden-log tests bind to.

### §7.2 — Source-of-truth matrix + re-read invariant `[LOCKED]`
Every state has exactly one authoritative source; the daemon never treats a projection as truth when a live source exists. **General invariant:** *for every state class whose SoT is a live/remote source, the Gateway MUST re-read the authoritative source after acquiring the lease and before executing; the cached projection is never sufficient for a mutation decision* (realizes AG §16.4).

| State class | Source of truth | Re-read / staleness rule |
|---|---|---|
| Event-derived (`proj_*`) | the `events` log | rebuildable; `projection_offsets` |
| Durable registry | the row | not rebuildable; mutations still emit audit events |
| Gateway state | `action_requests`/`approvals` rows | canonical for execution; `proj_approval_queue` is a may-lag read-only projection; row + projection updated in one txn. **Executor read-source `[2.3]`:** the **approve** path executes off the **durable row** (`request::load`; canonical here — in-memory inputs are gone at approve-time, possibly post-restart); the **risk-0 auto-execute** path executes off the **in-memory reconciled `ActionRequest`** (no redacted re-read, since it is available in-process). The row stays redacted at rest throughout; real-input-fidelity for the approve path is owned when the real (side-effecting) executors land (Phase 3/5/7/8). |
| Locks | `leases` table | authoritative; expired → reclaim w/ new fencing token |
| **Git/FS** | the repo/worktree | **re-read git2 live before any mutation**; `git_checked_at` staleness on cache |
| **PullRequest** | **GitHub (octocrab)** | local `proj_pull_request` is a synced cache w/ own `pr_checked_at`; **re-fetch before merge/checks decision** |
| **PlanTask structure** | the parsed plan file (MVP_TASKS.md) | `last_parsed_sha`; re-parse on change → `ImplementationPlanUpdated`; status is event-derived in `proj_plan_progress`; `tasks` row is the durable spine |
| **WorkflowInstance pack contents** | `.scaffolding/manifest.json` on disk (read-only to platform) | re-hash on scan/git events → `WorkflowInstanceDriftDetected`; re-validate before any `workflow.command.invoke`/personalization |
| Harness session/usage | the harness transcript/thread + SDK/app-server stream | **never scrape PTY for machine state**; resume via `claude --resume`/`codex thread/resume`; `harness_session_map` is a re-derivable cache (harness authoritative) |
| ExecutionProfile **config** | the row | canonical |
| ExecutionProfile **runtime** (active/in_use/rate_limited/auth_expired) | live: session state + adapter telemetry + keychain self-test | **re-derived on daemon restart** (not trusted from the persisted row) |
| Secrets | OS keychain | only `keychain_ref` pointers in DB; never in events/rows |
| Brain memory/index | Brain's own store | daemon caches `last reported` status + `brain_status_reported_at`; failed ping downgrades to degraded |

Field-level note on `proj_session`: `status`=event-derived (replayable); `stale`=daemon-time-derived (recomputed on rebuild, not replayed); `context/token/cost`=harness telemetry with `metric_quality ∈ {exact,estimated,unavailable}` (NULL context-% allowed for Codex — UI renders "unknown", not 0%).

---

## §8 — User & system flows

Authoritative: `PRD §9` (14 flows) + `UX §10` (A-M). Each MVP requirement maps to a flow (the requirements→flow matrix is the §14 coverage gate). Component-path table (the audit-added flows are included):

| Flow | Component path |
|---|---|
| **First-time setup** (PRD §9.1, was unmapped) | UI wizard → `gateway` intents → detection executors (`harness`/`git`/shell probes) + `integrations`(gh/Linear auth bootstrap) + `brainclient`(status) + `policy`(approval/profile defaults) → consent steps (§16 consent map); idempotent/reversible/skippable |
| Add local project (PRD §9.2) | UI → `gateway` → `git`(detect, git2) + `workflow`(detect→readiness) + `brainclient`(status) → registry rows + events → projections |
| Start from plan task (PRD §9.4) | UI → `gateway`(create_worktree=git CLI, session.create) → `locks`(lease+token) → `harness`(launch) → `terminal`(PTY) → events |
| **Start Codex session** (was unmapped; O-1) | UI → `gateway`(session.create, harness=codex) → `harness`(app-server `thread/start{cwd}` → map id → `harness_session_map`) → `locks` → events; restart re-assoc via `thread/list?cwd=` |
| **Start blank session** (PRD §9.5, was unmapped) | UI(New Session) → `gateway`(session.create) → same executors minus plan-link |
| Respond to permission (PRD §9.8) | `harness`(can_use_tool / app-server approval) → `gateway`(ActionRequest, risk) → `proj_approval_queue` → UI approve → execute → events |
| Review diff (PRD §9.9) | UI → `git`(git2 diff) → review workspace; ask-agent = `gateway` intent |
| **Commit & push** (was unmapped) | UI → `gateway`(commit/push, risk per GIT-5; force-push/protected ≥3) → `git`(CLI) → events |
| **Manual linking** (TASK-6, was unmapped) | UI(inspector link) → `gateway`(link intent risk 1) → link event + `object_refs` → projections |
| **GitHub/Linear intake** (PRD §9.3, was unmapped) | `integrations`(octocrab/Linear read) → `tasks`(external_task rows) cached as projection → dispatch via session.create |
| Ask Brain + action plan (PRD §9.11/§9.12; O-3) | UI → `brainclient`(MCP tool) → Brain proposes **multi-step ActionPlan** → `gateway`(preview/step-approve) → execute → events → Brain re-indexes |
| Create PR (PRD §9.10) | `gateway`(github.create_pr) → `integrations`(octocrab) → outbox → events; PR linked to session/worktree/task |
| **Recovery: UI restart** (DESK-7) | UI → `ipc`(handshake) → projection re-read → terminal re-stream (daemon alive) |
| **Recovery: daemon restart** (O-2) | replay projections → re-read git2 → ping Brain → reclaim leases(new token) → **per-session survival ladder (§8.1): `reattached_live` (broker-held live turn) → `resumed` (`--resume`/`thread/resume`) → `replayed` (scrollback+relaunch w/ banner) → `relaunched` (+ "restart session")**; reconcile orphaned `executing` actions (§17) |
| **Agent / PTY / app-server failure (daemon alive)** (§17) | supervisor detects child exit → `SessionFailed`+`TerminalProcessExited` → fail in-flight action + release lease → resume affordance in Human Input Queue |

### §8.1 — Survival EXTENSION: B2-strict live-reattach via a detachable-terminal broker `[user-ruled 2026-06-12 (away-authority, return-review); binding 4.1a]`

The §8 "Recovery: daemon restart" row above specifies **B2-achievable** survival — `--resume`/`thread/resume` relaunch-and-resume-from-transcript. The user ruled in the stronger **B2-strict**: the agent process **outlives the daemon**, and the daemon **reattaches to the live in-flight turn** on restart. Recorded as an authorized architecture EXTENSION (eyes-open re: the broker cost + the HITL-only survival verification) — **not** a silent expansion.

**Why it needs a new subsystem (the grounded constraint).** Under the cat-4-resolved PTY-primary adapter (§9.1), `claude` / `codex app-server --stdio` run as **daemon children** — the daemon holds the PTY master / stdio pipes. Daemon death closes them → SIGHUP / EOF → the agent **terminates** (exactly what 3.4 already models via `TerminalProcessExited`). So for the agent to survive the daemon it must **not** be a direct daemon PTY-child: B2-strict requires a **detachable-terminal broker** (tmux/abduco-class — a surviving PTY holder the daemon attaches/detaches to), a `SessionLauncher` impl that drops in behind the 4.0a launcher seam (the `TODO(4.1)` swap-point).

**The survival ladder (the per-session daemon-restart decision).** On daemon restart — after rebuild projections → re-read git2 → ping Brain → reclaim leases (new fencing token, §5.1/§17) — each session resolves to one **`ResumeMode`** (frozen in `shared/` at 4.1a, CONTRACT 0.29.0), in **strict precedence**:
1. **`reattached_live`** — the broker holds a SURVIVING live session → reattach to the in-flight turn (B2-strict; the highest fidelity).
2. **`resumed`** — no live survivor, the harness supports native resume AND a resume handle exists → `--resume`/`thread/resume` relaunch-and-resume-from-transcript (a NEW process continuing the conversation).
3. **`replayed`** — no native resume → serialized-scrollback replay + relaunch.
4. **`relaunched`** — none of the above (or a resume attempt failed) → fresh relaunch + a "restart session" affordance (§17 / §11.4).

The daemon emits the resumed-vs-replayed-vs-reattached signal the §11.4 UI surface renders.

**Testability split (§14).** The resume/replay/reattach **DECISION logic is deterministic + test-first** — a pure **total classifier** (`decide_resume`, `daemon/src/harness/resume.rs`) over availability inputs (broker-live / resume-capability+handle / scrollback), FakeHarness/FakeBroker-driven, **harness-agnostic** (Claude + Codex shapes); landed **4.1a**. The **LIVE broker-reattach SURVIVAL** (the agent actually outliving the daemon + reconnecting to a live turn) is a live-process property → a **labelled 0.1/0.3-HITL verify-only follow-on**, not deterministically unit-testable. The broker subsystem itself + the bootstrap restart caller (rebuild → reclaim-leases → `decide_resume` → emit) land **4.1b**.

> **AS-BUILT note (075b–d, 2026-06-17 — the `replayed` rung is now LIVE; LESSONS §57/§58):** the headless VT model (`terminal/vt.rs`, 075a) + its serialize/replay (075b) + the producer→`ScrollbackStore`-seam→recovery wiring (075c) + the durable §15-redacted `FileScrollbackStore` (`scrollback/`, 075d) make the **`replayed` rung reachable on daemon restart** — `decide_resume`'s `has_scrollback` input is now fed (via `has_restorable_content()` — scrollback rows OR a non-blank screen, so a mid-alt-screen session still re-renders) from a persisted, redacted scrollback snapshot; `decide_resume` itself stays byte-unchanged (the caller populates the input, LESSON §36). The §6.4 wire stays frozen (0.21.0; no contract). `reattached_live` (the broker) + `resumed` (live `--resume`/`thread/resume`) remain the 4.1b-2 / 0.1-HITL follow-ons; only the deterministic `replayed`-scrollback half is live. An alt-screen-ACTIVE snapshot captures the alt screen but not the hidden normal buffer (a pinned two-buffer limitation; full fix = a raw-byte-retain follow-on).

**Scope note.** The only fidelity B2-strict gains over B2-achievable is the literal in-flight turn at an abrupt crash (largely reconstructed from the persisted transcript anyway) — bought with the broker subsystem + a non-deterministic survival path. The user accepted that cost, eyes open. _(Origin: deep-dive `docs/planning/P4-deep-dive-live-drive-loop-and-survival.md` §7.1/§7.2/§8.1; Decisions-tabled, return-review-flagged.)_

> **AS-BUILT note (4.1b-1, 2026-06-13 — the restart-recovery orchestration is LIVE; `decide_resume` now driven; LESSON §38):** the deterministic daemon-restart session-recovery orchestration landed (`475068b` C1 + `1e68f20` C2; brief 059). On restart — after `cold_start` (pidlock/open/migrate/register/`reconcile_orphans`) + the runtime/supervisor are up (`main.rs:202`, pre-accept-loop) — **`recover_sessions_on_restart`** enumerates the non-terminal `proj_session` rows (read-only WAL) → per session builds `ResumeInputs` → **`decide_resume`** (4.1a, now driven in production — its Step-7.5 reachability closed) → dispatches the strategy over a launcher/**`Broker` seam** (`FakeBroker` now; the real detachable-terminal broker = **4.1b-2**, LIVE survival = the 0.1/0.3-HITL follow-on) → emits a **`SessionRecovered{mode, replayed_event_count, execution_profile_id}` OBSERVATION event** (the §11.4 resumed-vs-replayed-vs-reattached bit; CONTRACT 0.31.0). **Recovery-spawn = a System-actor recovery, NOT a Gateway Action (Q1 ruled (a), lead/user 2026-06-13):** the daemon re-materializing its OWN already-approved prior session = a *realization* of LESSON §10 (the daemon's own lifecycle is substrate, not a proposer-gated Action; INV-SEC-1 governs untrusted-proposer intents), extended to a structurally-analogous case — NOT a new fork. **4 load-bearing INV-SEC-1 conditions hold (security-reviewer CLEAR):** (1) recovery only re-materializes a GENUINELY-approved prior session (identity + §15 #8 profile sourced from the COMMITTED prior `SessionStarted`; the `is_recoverable_status` predicate — never fabricates authority for a never-approved session); (2) AUDITED (the `SessionRecovered` event via the §15-gated write-actor, rule #5); (3) §15 #8 profile PRESERVED (original `SessionStarted` profile — no re-resolution/account-hop); (4) the recovered agent's SUBSEQUENT tool calls STILL route through the live interception→Gateway (INV-SEC-1 holds for the agent's mutations — the recovery spawn grants ZERO new authority). Reusing `session.create` was rejected ((b)): it mints a FRESH `SessionId` (orphans the original) + re-approves the already-approved + buys zero safety. The deterministic orchestration is test-first (7 unit + 2 wiring + 1 contract); the LIVE broker survival = the §14 HITL follow-on.

> **AS-BUILT note (4.1b-2, 2026-06-13 — the tmux detachable-terminal broker is LIVE; the deterministic surface landed; LESSON §39):** the real surviving-PTY broker realizing the §8.1 B2-strict design landed (`0b9f78a`; brief 060; USER-ruled mechanism **A = tmux**; security-reviewer CLEAR ×6). New sessions launch INTO a detached tmux session (`tmux new-session -d -s nexusops-<sess_…> -c CWD -- env <derived> claude <args>`) so the agent can outlive the daemon; on restart the **`TmuxBroker`** (`Broker` impl) detects survivors via `tmux list-sessions -F` so `decide_resume` reaches **`ReattachedLive`**. tmux is an **OPTIONAL upgrade** — a startup **probe** (`tmux -V`) drives a pure **`select_survival_backend`** yielding a CONSISTENT pair (tmux → `TmuxLauncher`+`TmuxBroker`; absent → the non-surviving `PtyLauncher`+`NoSurvivorBroker` = **graceful-degrade to B2-achievable**: resume/replay, never `ReattachedLive`). The **O-13 #10 / §15 #8 enforcement surface is preserved INTACT through the tmux layer** (security-CLEAR): the `TmuxLauncher` wraps the **UNCHANGED `ClaudeLaunchSpec`** (tmux = transport, NOT a new #10 owner — the single live-spawn site, Option-A), and the §15 #8 env hygiene rides an **`env(1)` wrapper (`env -u K`/`K=V`) derived from `spec.env_mutations()`** (tmux `-e` can't UNSET → can't strip a stale `ANTHROPIC_API_KEY`; `env -u` strips at exec time regardless of tmux's server env — the strip is a property of the constructed argv → deterministically pinned). The deterministic surface (command-construction · probe · `list-sessions` parse · selection · wiring) is test-first (+13); the **LIVE survival** (the agent actually outliving the daemon + the lossy alt-screen VT-reattach) + the live recovery-dispatch re-materialization remain the labelled **0.1/0.3-HITL verify-only follow-on** (`DeferredRecoveryDispatch` stays a no-op; the audited `SessionRecovered` signal is the recovery effect). The settings-write fail-closed leaf is structurally enforced (`write_settings()?`-first; security-CLEAR + a new-session fail-closed substitute test) but its deterministic unit-test + the #10-through-tmux LIVE check are follow-ons (a `ClaudeLaunchSpec` settings-path-injection hardening slice + an **e2e-acceptance-matrix manual line** — the matrix is the durable home for an un-unit-testable safety surface). Daemon-internal behind the FROZEN `Broker`/`SessionLauncher` seams — **NO CONTRACT bump**.

---

## §9 — Integration architecture

`[LOCKED — ADR-007]`. **Git:** `git2-rs` for hot structured reads (status/diff/log/branch/worktree-list); **git CLI for ALL mutations + worktree lifecycle** (terminal parity + single chokepoint). `OQ-INT-SPIKE-6` (2026-06-07) **resolved the relative-worktree read question empirically: libgit2 ≥ 1.9.4 (git2 0.21) CAN fully read `extensions.relativeWorktrees` repos** (open/statuses/branches/head/worktree-list/find_worktree/diff) — the earlier ADR-007 "fix unreleased" premise is superseded; relative-worktree repos **no longer force CLI reads**. The CLI-read fallback is **retained** only for the separate, still-unverified **sparse-checkout misreport** gap. Mutations remain CLI-only regardless (the chokepoint invariant, not a libgit2 limitation). **GitHub:** `octocrab` (typed REST+GraphQL) for issues/PRs/checks/merges; auth bootstrap = reuse `gh auth token` else OAuth Device Flow. **Linear:** `@linear/sdk`/GraphQL; auth-code+PKCE (loopback) or pasted key; 24h refresh; budget query complexity. **Integration-failure contract** (§17): transient (429 Retry-After / 5xx → outbox backoff) vs terminal (401/403 → `*SyncFailed` + profile→auth_expired + "re-authenticate" card; keychain-unavailable → hard fail). Staged sync (inherited): link (P0) → one-way (P1) → bidirectional (P2). **Credentials:** `keyring` crate (covers iOS future), explicit per-OS feature flags + startup self-test; macOS keychain ACLs need a stable Developer ID (§16).

### §9.1 — Harness adapter layer `[LOCKED — ADR-006; Claude mode RESOLVED = PTY-primary (cat-4, 2026-06-12)]`
One **`HarnessAdapter` trait** — `{launch, stream_status, intercept_mutation, read_transcript, telemetry_heartbeat, resume, capabilities}` — with **normalized return types** (Appendix A): `NormalizedStatus` (the 17 Session states), `TelemetrySample{tokens_in,tokens_out,context_pct:Option<f32>,cost,quality}`, `MutationIntercept{tool,params,decision_sink}` (both Claude `can_use_tool` and Codex `requestApproval` implement it), `TranscriptRef{path,hash,is_in_place}`, `ResumeResult`, and a **`HarnessCapabilities`** struct (PRD HARN-5's 10 fields: supportsTerminal/Resume/TranscriptRead/ToolCallParsing/UsageMetadata/ContextMetadata/CommandInjection/Subagents/Hooks/CloudTasks) driving per-capability UI degradation (e.g. `supportsContextMetadata=false` for Codex → render "unknown").

> **AS-BUILT note (3.1 contract freeze, 2026-06-12 — CONTRACT 0.20.0; LESSON §23):** the §2.5-seam freeze landed the **consumer-facing DATA types** in `shared/` — `TelemetrySample{tokens_in,tokens_out,context_pct:Option<f32>,cost_estimate,metric_quality}` (prose `cost`/`quality` → AS-BUILT **`cost_estimate`/`metric_quality`**, matching the `proj_usage_ledger` DDL), `MetricQuality`(exact|estimated|unavailable), `TranscriptRef`, `HarnessCapabilities`(10) — plus a new **`TelemetrySampled` event** (§7.1) that feeds the `proj_usage_ledger` projection. **`NormalizedStatus` is the frozen `status::Session`(17) re-exported, not a new enum.** Telemetry is a **non-mutation observation event**: appended via the single write-actor through the §15 redactor (audited + immutable), **NOT** routed through the Action Gateway — INV-SEC-1 (§15) governs FS/git/external/session-**state** mutations, and a telemetry sample is none (the `DeviceRegistered`/`AuditIntegrityViolation` System-event precedent, §16/§17). The **`HarnessAdapter` trait + `MutationIntercept` + the coverage matrix stay daemon-internal + unfrozen** (sync object-safe `Box<dyn>` per the `ActionExecutor` seam; 3.2/3.3 async-ify). **`ResumeResult` is deliberately NOT frozen in 0.20.0** — resume/survival is a Phase-4 §8/§17 design (the ui provisional already pins `ResumeMode` as an enum), so it freezes in `shared/` there to avoid a breaking reshape. Live `telemetry_heartbeat → TelemetrySampled` emission + the real launch/status/intercept drive loop land with the adapters (3.2/3.3).

> **AS-BUILT note (3.3a, 2026-06-14 — the CodexAdapter observe core is LIVE; LESSON §42):** the second harness adapter landed (`c4a4652`; brief 063; security-CLEAR; NO CONTRACT bump) — the `CodexAdapter` implements the SAME frozen §9.1 trait as the `ClaudeAdapter` (no new trait). The divergence is the PARSER: two Codex vocabularies (the rollout-JSONL `session_meta`/`turn_context`/`response_item`/`event_msg` + the `exec --json` `ThreadEvent` stream) → ONE `CodexSignal` → ONE pure `derive_status` fold (the §25 `ClaudeSignal` precedent), producing turn-scoped **NON-TERMINAL** status only (terminal = the process-exit signal, a later live slice — a turn boundary ≠ session death, R-9 sink respected); tool calls classified BY SEMANTICS (`CodexToolKind`, NOT vendor names — the OSS-vs-desktop divergence); `TelemetrySample` from `token_count` (cost_estimate=0.0, Codex carries no cost — pricing = a 3.3d follow-on); the observe trait methods (`capabilities`/`stream_status`/`read_transcript`) + the `push_signal` seam the 3.3c live stream drives. Status from the structured stream NEVER PTY bytes (#9). Fixture-tested (synthesized secret-free golden fixtures; the OSS-version refresh = HITL). **The arc continues:** launch/auth/umask (3.3b) · the CAT-1 PreToolUse interception + `--sandbox` defense-in-depth (3.3c — design surfaces lead→user) · telemetry emission (3.3d) · the LIVE drive loop (HITL, the 4.0b-2-smoke analog).

> **AS-BUILT note (3.3b, 2026-06-14 — the CodexAdapter launch/auth/profile/umask/resume mechanism is LIVE; LESSON §43):** the Codex launch path landed (`16962c9` C1 §15 #8 auth/profile · `a822015` C2 §15 #11 umask · `a571a62` C3 launch-spec/resume-handle; brief 064; security-reviewer CLEAR; NO CONTRACT bump; the two §15 pins each its OWN commit) — mechanism-built, **NO live spawn** (the binding condition: the reachable live `codex` + its interception land together at 3.3c, the 042→043 / 4.0b-1→4.0b-2 precedent). `CodexLaunchSpec` = a PURE `codex exec [resume <UUID>] --json --sandbox <s> --ask-for-approval <a> --model <m> --profile <p>` argv where a bypass is impossible by construction (`--sandbox` always present; no `--yolo`/`--dangerously-bypass-approvals-and-sandbox`/`--full-auto` in argv OR source — the `ClaudeLaunchSpec --permission-mode default` enforcement-surface analog). **§15 #8 (no silent account-hop):** `resolve_codex_auth` pins EXACTLY ONE auth (refuse ambiguous — ≥2 distinct methods OR same-method conflicting-value env vars; same-value dup → canonical-pin+strip; compare-the-secret-then-DROP — the transient `AuthSources` has no `Debug`, only a keychain-ref + the var-NAME enter the daemon-internal `CodexExecutionProfile`, no-Debug-leak rule #4), and the env-hygiene strips every auth env var except the pinned → **exactly one auth source reaches the child**; the containment fields (`sandbox`/`approval_policy`) are required (no `Default`) → the slice ships NO silent containment value (the cat-1 sandbox-VALUE call is 3.3c). **§15 #11 (Codex rollout-files hardened):** `harden_codex_dirs` pre-creates `~/.codex/sessions` at 0700 (mode-verified) + `CODEX_CHILD_UMASK=0o077`, FAIL-CLOSED (Codex otherwise leaves sessions/ 0755 + rollouts 0644; the live "doesn't chmod back" verify = HITL). The resume handle keys off the rollout UUID GATED on the rollout file's existence (present→`Resumed`/missing→`Relaunched`) → the harness-agnostic `decide_resume` (§36) via `CodexAdapter::resume_inputs`. **The arc continues:** the CAT-1 `PreToolUse`→Gateway interception + the `--sandbox` defense-in-depth + the real spawn-site launcher (3.3c — design surfaces lead→user) · telemetry emission (3.3d) · the LIVE drive loop + the umask/app-server-resume live checks (HITL).

> **AS-BUILT note (3.3c, 2026-06-17 — the CAT-1 Codex INV-SEC-1 interception is LIVE [mechanism]; defense-in-depth hook+sandbox; LESSON §56):** the `nexusops-codex-gate` `PreToolUse` adjudicator (`eb358e1`; brief 066; security-reviewer CLEAR 0/0/0/0 EVERY layer; **B2 ruled** away-log Decision 18; NO CONTRACT bump) routes every Codex tool-call through the EXISTING 4.0b-2 Gateway adjudication loop UNCHANGED — swapping ONLY the I/O envelope (Codex `PreToolUse` stdin/stdout ↔ the daemon `intercept` RPC) + branching ONLY the parser via an OPTIONAL daemon-internal `harness` discriminator on `HookPayload` (the B2 fork): codex→a Codex classifier (`exec_command`/`shell`/`local_shell`→`agent.bash` · `apply_patch`→`agent.file_edit` · `mcp__*`→`agent.mcp_tool` Adjudicate · unknown→`CoverageGap`→deny); absent/claude→the EXISTING Claude classifier, byte-identical (the default/absent tag = the strictest reading). **PIN-1 (by-construction safety):** the discriminator is trusted-binary-stamped (the `--harness` flag), NEVER parsed from agent stdin → a spoof only ever selects a more-conservative classifier (both arms deny-unknown; risk catalog-authoritative + daemon-side) → INV-SEC-1 holds regardless of the tag value. **Defense-in-depth (the genuinely-new-vs-Claude pin — Codex docs call `PreToolUse` "a guardrail, not a complete enforcement boundary"):** the hook is adjudication+audit; **`--sandbox workspace-write`** (writes scoped to `cwd + writable_roots = {worktree + per-profile approved}`, `network_access=false`, no bypass flag — USER-confirmed; grammar verified live: `workspace-write` reads ALL files, `writable_roots` is a **WRITE** boundary only — INV-SEC-1 governs mutation) is the OS containment layer; both together = INV-SEC-1 for Codex. Mechanism-built, **NO live spawn** (the LIVE interception + `--sandbox` containment proof = the 0.1/0.3-HITL follow-on, the 4.0b-2-smoke analog).

> **AS-BUILT note (3.3d, 2026-06-17 — Codex telemetry emission is LIVE [mechanism]; LESSON §27 EXTENSION):** the Codex telemetry path landed (`389ee28` L1 hoist + `1f6664c` L2; brief 074; code-quality CLEAN [2 med + 4 low fixed in-slice]; NO CONTRACT bump) — L1 HOISTED the 5 harness-neutral 044 primitives (`telemetry_sample`/`UsageReading`/`TelemetryEventSink`/`UsageSource`/`TelemetrySinkFactory`) from `claude/telemetry.rs` to a harness-neutral `harness/telemetry.rs`, RE-EXPORTED from `claude::telemetry` (every consumer byte-identical), so the 2nd harness reuses the seam without coupling; L2 wired the `CodexAdapter` emission seam (`push_usage`/`telemetry_heartbeat`/`poll_telemetry` + the injected sink) reusing the delta-emit / context-gauge-pass-through pattern (§27). **The genuinely-new Codex divergence:** Codex `token_count` has NO cost field (Claude reads upstream `total_cost_usd` = `Exact`) → a PURE LOCAL per-model pricing table (`codex_cost_estimate`; indicative rates + a refresh-TODO; an unknown/absent model→`0.0`, never fabricated) derives the cost, and `metric_quality` is honestly **DOWNGRADED `Exact`→`Estimated`** for any locally-derived cost (the Claude-Exact vs Codex-Estimated honesty line, §11.4). The Codex cumulative source is `total_token_usage` (the documented **snapshot-vs-cumulative trap** the live ingress MUST honor — build CUMULATIVE readings, never feed per-turn snapshots). Mechanism-built — the production sink-bind + the live CUMULATIVE `token_count` ingress + the pricing-rate refresh = the live-Codex follow-on.

**Per-harness mutation-coverage matrix** (binding; the conformance suite asserts per category per harness, §14):
| Tool category | Claude (`can_use_tool`) | Codex (`app-server` approval) |
|---|---|---|
| direct bash / file-edit | intercepted **in `default` mode only** | intercepted (`commandExecution`/`applyPatch`) |
| subagent (Task) | **NOT guaranteed** (inherits parent mode; bypassed under acceptEdits/bypass) | n/a |
| background subagent | **bug #27203** — bypasses callback | n/a |
| MCP tools (`mcp__*`) | falls through to mode+callback (direct only) | covered (`mcp_tool_call` elicitation) |

**O-13 (locked regardless of O-4):** NexusOps-driven Claude sessions run **`default` permission mode only** (never acceptEdits/bypass/auto, so subagents can't inherit a bypass); add **`PreToolUse` hooks + deny-rules as a redundant interception layer**; **forbid background subagents** until #27203 is fixed or a pinned-version proves coverage. Mutation interception is **defense-in-depth**, not solely `can_use_tool`.

- **Claude adapter:** drive-mode **RESOLVED = PTY-PRIMARY** (the cat-4 `[PENDING-SPIKE]` settled 2026-06-12 away-authority — Decisions-tabled / IMPLEMENTATION_PLAN.md; `OQ-HARN-SPIKE-7 §3` Branch A): drive via **interactive PTY** + `PreToolUse` hooks + deny-rules + statusLine heartbeat; the **SDK is a secondary status/telemetry signal, NOT the drive transport**. Decided from DOCS, no empirical drain — the June-15-2026 billing split is documented (SDK/`-p` draws a separate capped monthly credit pool that **hard-stops with no fallback**; interactive runs on auto-resetting rolling-window limits, exempt → a control plane cannot let a supervised run die on the capped pool). The coarser hook-based interception is acceptable because the O-13 mitigations stay LOCKED regardless. Status from the structured hook stream (`SessionStart`/`PreToolUse`/`PostToolUse`/`Notification`/`Stop`) + transcript JSONL; telemetry merged from `ResultMessage.usage` + statusLine (`refreshInterval` — `rate_limits.{five_hour,seven_day}.used_percentage` + context-%) + transcript.
> **AS-BUILT note (3.2-part-1, 2026-06-12 — observe path; LESSON §25):** the `ClaudeAdapter` observe path landed (brief 042) — an O-13-compliant `launch()` (interactive PTY via the 3.4 `PortablePtyHost`, **`default`-mode-only, no `-p`, no bg subagents**, generated per-session settings [0600, NEVER the user's `~/.claude/settings.json`], fail-closed on settings-serialize → no hook-less session; the spec IS the #10 enforcement surface), a pure **`derive_status(prev,ClaudeSignal)→Session`** (the §5.1 17-state from STRUCTURED hook/transcript signals, **never PTY bytes #9**; fail-closed exit, R-9 terminal-sink), and `read_transcript()`. The **`HarnessAdapter` trait reshaped** (daemon-internal/UNFROZEN per the §9.1-AS-BUILT-3.1 mandate / LESSON §23): **`Send+Sync`→`Send`** + **`launch(&self)`→`launch(&mut self)`** (the real adapter owns a `Box<dyn Pty>` [Send-only] + a mutable status machine; security-reviewer SAFE — no cross-thread sharing needs Sync). A **`PtySpawner` object-safe factory** (`terminal/mod.rs`) produces the `Box<dyn Pty>` `launch()` injects. The **interception (`MutationIntercept`→Gateway, INV-SEC-1) + the hook-receiver ingress + telemetry emission are 3.2-part-2 (brief 043)**; `resume()` + the production session-lifecycle drive caller = **Phase 4**.
> **AS-BUILT note (3.2-part-2/043, 2026-06-12 — the cat-1 INV-SEC-1 interception; LESSON §26):** the first real harness mutation chokepoint landed (5 layers, security PASS every layer; the core adversarially-verified). A Claude `PreToolUse` hook → the daemon **hook-receiver** → a `MutationIntercept` → an **adjudication-only `ActionRequest`** routed through the **EXISTING** Gateway pipeline (Option A — one chokepoint; the §6.3 catalog gains an `agent.*` **agent-mutation family** + `ExecutorKind::Adjudication`, CONTRACT→0.22) → policy/approval → an **audit-written-BEFORE-verdict** `MutationVerdict{Allow|Deny}` back to the hook. The daemon **ADJUDICATES + AUDITS; the agent executes** the Allow'd tool (the ActionRequest TERMINATES at the verdict — `PolicyDecided`/`Approved`, never `Queued`/`executing`; **no daemon executor** — code-enforced). **Q2-conservative:** read-only auto-allows; every mutating tool requires approval by default (no auto-allow until a user standing-grant). **O-13 coverage-gap (the crux):** only DirectToolUse in `default` mode → Adjudicate; **MCP/Task/bg-subagent/non-default → DENY** (an un-interceptable mutation is a bypass) — two-layer (receiver-deny + the hook-INDEPENDENT `permissions.deny:["mcp__*","Task"]`). The **fail-open hook-MISS residual is CLOSED** by defense-in-depth: per-call hook (sole allow-authority) + NO `permissions.allow` + the non-interactive empty-cache block + `permissions.deny` + a 5s hook timeout → a hook-miss has nothing to fall through to → BLOCK = fail-CLOSED. A blocked dangerous attempt is audited (**record-then-deny**, A1: `ActionRequested`+`ActionDenied{None, reason}`; `ActionDenied.approval_id`→`Option`, CONTRACT→0.23). **Tradeoffs (Decisions-tabled, return-review):** supervised Claude can't use MCP tools (MVP); the split P4 tool-policy (benign-internal `TodoWrite` auto-allow vs the `WebFetch`/`WebSearch` network-egress policy). The live drive-loop wiring (the `AgentMutationPolicy` runtime swap + `route_intercept`→live-hook + the per-session `decision_sink`) = **Phase 4** (its own security pass).

> **AS-BUILT note (3.2-part-2/044 telemetry, 2026-06-12 — the §9.1 emission path; LESSON §27):** the Claude adapter's `telemetry_heartbeat` emission landed (brief 044; `feat(harness)` `09f34aa`; NON-safety; CONTRACT held 0.20.0 — no `shared/` change). The adapter tracks cumulative usage (`ResultMessage.usage`/`total_cost_usd`, defensively parsed — tolerant of the external upstream's unknown fields, fail-closed on missing/wrong-typed required → never a fabricated reading) and emits **per-heartbeat DELTAS** (`current − last_emitted`, first = full-from-zero), NEVER cumulative — `proj_usage_ledger` SUMs, so a cumulative emit over-counts; **context-% rides as a CURRENT gauge** (the projector MAXes it), never a delta; `cost_estimate` = the delta of Claude's reported cumulative cost (no local pricing table); `metric_quality` Exact w/ context, Estimated w/o (never a faked 0%, §11.4). Emitted as a non-mutation `TelemetrySampled` OBSERVATION event (write-actor, NOT the Gateway — LESSON §23) through an injected `TelemetryEventSink` (the 3.4 `TerminalEventSink` precedent); the daemon-Clock UTC-Z `occurred_at` (write-actor-stamped, LESSON §5) gates the projector's `bucket_day` (fail-closed on non-Z). **3.2 (the Claude adapter) is now COMPLETE** — observe (042) + interception (043) + telemetry (044). The production sink-bind + the periodic pump (statusLine `refreshInterval`) + the trait async-ify + the live transcript/statusLine ingestion I/O = the NAMED **Phase 4** deferral (with a non-monotonic-cumulative-cost clamp→≥0 + a `metric_quality` degrade-on-guard as P4 live-data hardening).

> **AS-BUILT note (4.0a, 2026-06-12 — the opt-3 session-lifecycle spine; the live drive loop opens; LESSON 28):** the **`daemon/src/session/` module** landed (P4.0a; deep-dive §8 — forks ruled **opt-3** · **B2-strict** · broker-in-scope) — the production *driver* of the §9.1 adapter. A **`SessionSupervisor`** (spawn/track-by-id/route/reap, **NO auto-restart** — restart = 4.2) over a `HashMap<SessionId, mailbox>` + a `JoinSet<(SessionId, Session)>` (the write-actor/JoinSet idiom, LESSON 9; a panicked actor maps to `(id, Failed)` so `live_count` stays exact), spawned in `main.rs` alongside the drainer/reaper/accept-loop (the §10 background-task pattern, mirrors `spawn_reaper`; `SupervisorHandle` = the 4.0b `session.create` driver entry, wired+reachable, not driven); a **`SessionActor`** (a Tokio task + an mpsc mailbox + the §5.1 Session status state, owning a `Box<dyn HarnessAdapter>` + a terminal read-pump) driving one session's lifecycle, status from the adapter's structured stream **never PTY-scraped (#9)**; a **`SessionLauncher` seam** (`LaunchedSession{session_id, Box<dyn HarnessAdapter>, TerminalSession}`) with `FakeLauncher` + `PtyLauncher` — **the B2-strict survival broker is a drop-in `SessionLauncher` impl at 4.1** (`TODO(4.1)` swap-point). **Q1 RESOLVED = the SYNC `HarnessAdapter` trait is driven via `spawn_blocking`, NOT async-ified** (only `launch` + the PTY `read_step` block; `stream_status`/`telemetry_heartbeat` are cheap inline field reads) — **NO `async-trait` dep; the §9.1 trait is UNCHANGED** (this SUPERSEDES the pre-build "async-ify" assumption; LESSON 9/23/25). **Cat-1 boundary held STRUCTURALLY** — the `session/` module imports no `crate::runtime`/`crate::eventstore`/`crate::gateway` → event emission + mutation are *compile-time impossible* (status publishes to an in-memory observer; the pump's exit lands in a no-op sink) → **NO live agent / no emission / no mutation in 4.0a**. The live Claude launch + the INV-SEC-1 interception + the Gateway `session.create`/`session.kill` executors land TOGETHER as the cat-1 **4.0b** (own security pass — launching a session is itself a Gateway mutation + a live agent is unsafe un-intercepted). The status-poll tick (5 ms scaffold → push-based hook/transcript ingestion) + the kill-path pump-unblock (`pty.kill()` so a long-running live agent's pump terminates + shutdown stays time-bounded) are named 4.0b/4.2 live-path follow-ons. 320→327 tests; no `shared/` change, no CONTRACT bump.

> **AS-BUILT note (4.0b-2, 2026-06-13 — the cat-1 live INV-SEC-1 drive loop goes LIVE; security CLEAR ×3; LESSONS 25-amended/26-extended/28-extended):** the §9.1 adapter + the 4.0a spine + the 043 interception are now WIRED LIVE — the daemon launches + supervises a REAL `claude`, every tool-call adjudicated. Co-landed ATOMICALLY (brief 051; commits foundation→C1→C1b→C2→C3, round seal `bd7523b`): **(1)** the **reachable IPC `session.create`** (the UI/IPC live-launch path; requester=`User` SERVER-set, never client-trusted — no agent path) → `submit_action` → the registered `SessionExecutor`(`ExecutorKind::Session`) → the **live `PtyLauncher`** (Option A — the launcher OWNS the single live-claude spawn site + the O-13 **#10 enforcement-surface INLINE**: default-mode/no-`-p`/no-bg/0600 settings, **fail-closed on settings-write**; the **#10 LOCATION moved `ClaudeAdapter::launch()`→`PtyLauncher`, content IDENTICAL** — LESSON §25 amendment); **(2)** the **live interception** — the production policy swapped to **`AgentMutationPolicy`** (`main.rs`), a Claude `PreToolUse` hook → the **`nexusopsd hook` subcommand** (a thin fail-closed UDS client, 360s read-timeout) → the IPC **`intercept`** method → `route_intercept_live` on the write-actor → the Gateway adjudication → a per-session **`DecisionRegistry`** (the decision_sink: `register`/`resolve`/`cancel_session`) + the **`resolve_verdict` §6.2 5-min wall-clock wait** (fail-closed on timeout/cancel/session-death; exactly-once, first-terminal-wins, ambiguity→Deny), bridged sync↔async via a std `sync_channel` (**NO `block_on`** on a blocking serve thread; `Handle::try_current()` keeps the sync serve tests sync); **(3)** **call-2 best practice** (USER-ruled) — an adjudication audit-WRITE-fault → Deny + a raised **durable, DB-INDEPENDENT `IntegrityAlarm`** (an fsync'd 0600 `integrity-incidents.jsonl`, content-free by construction; the structured `GatewayDenyKind::AuditWriteFailed`); **(4)** the **kill-path** — the `SessionActor` extracts a `PtyKiller` BEFORE the pump + `kill()`s on Kill/shutdown so a long-running live agent's `spawn_blocking` PTY-read unblocks (shutdown stays time-bounded); **(5)** **env-hygiene/correlation** — the spawned `claude` child strips `ANTHROPIC_API_KEY` (rides subscription/OAuth auth, §15 #8) + carries `NEXUSOPS_SESSION_ID` (the hook→session key; untrusted → a drop-only `cancel_session` predicate — a spoof can only Deny, never Allow). The **binding condition flipped from BY-CONSTRUCTION ("mechanism built, no live caller") → ENFORCED-BY-THE-INTERCEPTION** — co-residency pinned by the inverted guard `tests/session_executor.rs::test_live_session_create_has_interception`. **CONTRACT 0.27.0** (the L1 d.2 split tool-policy: `agent.todo_write` risk-0 the LONE auto-allow, `agent.web_fetch`/`agent.web_search` risk-2 approval-gated — the catalog IS the allowlist, unclassified→`CoverageGap`→Deny). 294→299 tests. **F1** (register-after-commit window → spurious Deny, fail-SAFE) + **F2** (intercept-wait/UI-approve permit-pool sharing) = fail-safe follow-ons; **4.0b-2c** (the systemic audit-backbone circuit-breaker — fail-stop on N-consecutive/unrecoverable audit failure) = the next required slice. The 0.1-HITL empirical validation (the exact Claude hook grammar; the hook-miss-blocks closure) = the smoke harness.
> **AS-BUILT note (4.0b-2-smoke, 2026-06-13 — the 0.1-HITL dev-drive; Option G; LESSON §30):** to make the live loop demonstrable, `session.create` gained an **additive optional `initial_prompt`** (ad-hoc JSON params, **NO CONTRACT bump** — stays 0.27.0) threaded IPC `session_create` → `ActionRequest.inputs` → `SessionExecutor::execute_create` → `launched.terminal.write(prompt + \r)` POST-launch, exactly-once, opt-in — fed to the session PTY via the **existing `TerminalSession::write` seam** (NOT the O-13 argv; the #10 enforcement surface is UNCHANGED). **FAIL-SOFT** (a PTY-write error degrades — the session still launches + auto-executes; the prompt-feed is a dev convenience, not a safety I/O — **LESSON §30**: classify an I/O by whether the safety INVARIANT depends on its success, not by the path it rides). INV-SEC-1 preserved — the prompt only makes claude ACT; every tool-call still routes through the unchanged `intercept` adjudication (security-reviewer PASS confirms the interception/#10-argv/`ClaudeSettings` untouched). A thin **`nexusopsd smoke` dev-client** (feature-gated `dev-client`; the `hook`-subcommand precedent — a synchronous UDS client) drives `create`/`queue`/`approve`/`deny`/`kill`/`audit` for the runbook (`docs/runbooks/smoke-harness-live-drive-loop.md`). brief 053; commits `e79fecb`(C1)/`91fc1fe`(C2); +7 tests. The interactive-terminal UX (live output forward + input) = the parked ui-track **6.3d** (Option B), NOT this dev rig.
> **AS-BUILT note (4.1a, 2026-06-13 — `resume()` return frozen; LESSON §36):** the §9.1 `resume()` return type `ResumeResult` (deliberately deferred at the 3.1 freeze, above) is now **frozen in `shared/`** — `ResumeResult{mode: ResumeMode, replayed_event_count: u64}`, `ResumeMode`(4: `resumed|replayed|relaunched|reattached_live` — the B2-strict survival ladder §8.1), `RecoveryState`(3: `recovering|recovered|recovery_failed` — the ui provisional, frozen verbatim); **CONTRACT 0.28.0→0.29.0** (§2.5-seam snapshot + 3-way verify). The resume/replay/reattach **DECISION logic** landed as a pure total classifier (`daemon/src/harness/resume.rs::decide_resume`, FakeHarness/FakeBroker, harness-agnostic). The **production caller** (the bootstrap restart path) + the **detachable-terminal broker** `SessionLauncher` impl = **4.1b**; LIVE broker survival = the 0.1/0.3-HITL follow-on (§8.1). NON-safety (a survival data type + a pure classifier; INV-SEC-1 untouched).

- **Codex adapter** (O-1, live in MVP): `codex app-server --stdio` JSON-RPC. **MVP depends only on stable methods** (`thread/start`, `thread/resume`, `thread/list`, `turn/start`, `thread/status/changed`, `item/commandExecution/requestApproval`) — *not* `experimentalApi`-gated ones. Handle modern + legacy approval shapes (or pin a min Codex version). Treat `-32001` overload as transient/retryable. Pin Codex version + regenerate the app-server schema bundle in CI on every bump (`OQ-HARN-SPIKE-4`). Rollout dir hardened (§15).
- **Pinned version tuple** (validated by spikes, CI golden-fixture + schema-diff gate): `{Claude Code CLI, @anthropic-ai/claude-agent-sdk, Codex CLI}` recorded in §16 version-compat matrix; documented min-supported floor.

---

## §10 — Automation / background jobs

Daemon Tokio tasks: projection workers (fold events, advance offsets in-txn); outbox drainers (Brain MCP, GitHub/Linear syncers, **notifier**, optional JSONL mirror; classify retryable vs terminal); heartbeat/status pollers (Claude statusLine `refreshInterval` + Codex push; derive `stale` by age); lease reaper (expire + new fencing token); git watcher (refresh worktree/PR caches via git2 + git hooks); WAL checkpointer (`wal_checkpoint(TRUNCATE)`); sidecar supervisor (Brain MCP ping + restart/backoff + process-group kill; in-flight `brain.*` calls carry a timeout and fail on EOF). **`notifier`** (DESK-8, MVP-nice): consumes `SessionWaitingOnHumanInput/Permission`, `CheckFailed`, `SessionCompleted` → macOS UserNotification; notification permission is a setup-wizard consent step (§16); lock-screen previews are redacted (§15).

> **AS-BUILT note (4.3, 2026-06-14 — the deterministic background-jobs core is LIVE; LESSON §41):** the **WAL checkpointer** (`spawn_wal_checkpointer`, PASSIVE on a 60s interval; `Command::WalCheckpoint` → `EventStore::wal_checkpoint` on the write-actor's OWN connection — never a rogue writer, forbidden #3) + the **session-staleness poller** (`spawn_staleness_poller`, 30s interval / 120s threshold; a pure `is_stale` recompute over read-only `proj_session`, computed/rebuilt not event-folded — the §7.2 live-read precedent) landed (`81c9242`; brief 062; security-CLEAR), both joining the `spawn_drainer`/`spawn_reaper` long-lived-task family. NO CONTRACT bump (daemon-internal jobs + a maintenance PRAGMA command). The staleness poller is **correct-but-DORMANT** until a `last_heartbeat_at` production writer lands (a follow-on). **DEFERRED to their producers (the producer-gated scoping test):** the **sidecar (Brain) supervisor** → Phase 8 (deferred) · the **integration auth/rate-limit + network-offline failure events** → Phase 5/7 (the `*SyncFailed` event types exist in `shared/`; the integration executors emit them later). The daemon-owned §17 failure events (fail-closed/audit-integrity / projection-degraded / fencing-conflict) already emit. **Phase-4 deterministic core complete.**

---

## §11 — Frontend architecture

`[LOCKED — ADR-001]`. Tauri 2.x (Rust host + WebView). **Projection-driven reattaching client**: reads projections + submits intents over UDS `GatewayPort`; holds no authoritative state; never writes the DB. Terminals: xterm.js (WebGL) over the Terminal Channel with app-level backpressure (§6.4, ADR-009); approval prompts are **structured cards outside terminal text**. This section folds in the prototype review (O-5): the binding UI contracts surfaced by the 6-lens review of `NexusOps-ui-kit` (detail: `docs/ui-review/UI_RECONCILIATION.md` + the six `docs/ui-review/*.json` lenses).

### §11.1 — Canonical design system `[LOCKED — O-5]`
The **`NexusOps-ui-kit`** (tokens + component library + the "Graphite Arc" direction) is the canonical design system `tasks-gen` references and the build implements against. Two-tier oklch tokens (primitive→semantic) keep color a **re-hueable first pass** (a re-hue touches ~13 hue-family primitives only); product code references the semantic layer. Component inventory (carry forward): Button/IconButton; StatusPill/RiskBadge/UsageMeter/AttentionMarker; Badge/HarnessBadge/ProfileBadge/MetaChip; SessionRow/GraphNode/DiffHunk/EvidenceChip. **Never color alone** is a binding invariant — every status renders on ≥3 non-color channels (glyph + text label + intensity/motion); critical uses a grayscale-safe hazard hatch.

### §11.2 — Screen → surface map (20 screens) `[LOCKED]`
Present in the prototype and carried forward (16/20), with sound folds: Worktree/Git/PR Control Center → Code/Diff tabs; Usage Dashboard → Settings tab. **Build/expand:** **(1) First-Launch Setup Wizard** (absent — §11.4, §16); **(10) PR Review Workspace = FULL surface** (O-6) — a PR-review *mode* in Code/Diff with PR header, checks, reviews/comments, mergeability, risk summary, Brain evidence, agent-session summary, and Approve/Merge/Squash/Rebase/Request-changes/Ask-agent-to-fix-checks; the **Merge control triggers a fresh GitHub re-fetch** (§7.2) and routes merge/force-push/protected-branch through the Gateway at risk≥3 (§6.3, §15); **(4) Sessions List/Board** as a dense table (not only Command-Center groups); **Events/Audit detail inspector** (correlation/causation/sensitivity/payload). **Deferred:** Workflow Personalization Review UI `[P1]` (§19.2) — but the MVP Gateway still renders the multi-step personalization plan (§11.5); Remote Access / iOS pairing `[P2]`.
> **Nav-model note** (human-confirmed 2026-06-08, after a ui-track drift): the **content-view-switch hosts the main content surfaces only** — Command Center / Project Graph / Sessions / (later) Code-Diff. **Settings + the Project Brain are reached from the TopBar**, not the view-switch (Settings opens the Settings surface in the content pane; the Brain opens its drawer, §11.5). The **Usage Dashboard is a tab *inside* Settings** (not a top-level surface). Pinned here so the view-switch doesn't re-accrete Settings/Brain entries.
> **History-nav semantics** (ui-track P6.4, `022`): the TopBar **back/forward** traverse a content-view navigation history with **browser semantics** — `navigate` to a new view pushes and **truncates** any forward entries; back/forward move the cursor without mutating the stack; `navigate` to the current view **collapses to a no-op that preserves** any forward entry. **All four content views are uniform history entries** — Settings participates even though it is *reached* via the TopBar (the nav-model governs how a view is reached, not its history membership). Back/forward are **`disabled` at the history boundaries** (never a dead click — §11.6). History keyboard shortcuts (e.g. Cmd-[ / Cmd-]) are out of scope for the MVP.
> **Sessions filter facets** (ui-track P6.3, `024`): the Sessions table filters on **status + free-text** (name | project, case-insensitive); options derive from the present rows; filtered-empty is a distinct state from the empty projection. **Project scoping is owned by the active-project switcher** (the Shell pre-filters the table's rows), so there is intentionally **no within-table project facet**.

### §11.3 — Status rendering binding `[LOCKED]`
StatusPill (and every status surface) binds its **status keys to the §5.1 canonical enum strings verbatim** (`proj_*.status`), snake_case; display labels are a separate copy layer. It must render **every state of all 10 machines** — notably the full **17 Session states incl. `changes_ready`** (kit currently uses ~7). **One canonical `status → attention-rank` table** (covering all 10 machines) is the single source for sidebar weight, queue membership, and sort order — **no silent fall-through to idle** (today `waiting_on_permission`/conflict/stale/blocked floor to 0 and never enter "Needs my attention"); ordering per PRD §5.2. Approval and ActionRequest render as **two distinct status surfaces** (R-5). Worktree status is the **derived two-axis precedence** value (§7.2).

### §11.4 — Net-new UI surfaces `[LOCKED]` (the prototype predates these)
- **Daemon-connection indicator** (connected/reconnecting/disconnected), distinct from LocalRunner health, + a **global READ-ONLY degraded mode** that disables every intent-submitting control (Gateway approve/deny, Dispatch, Brain Run-via-Gateway, commit/push) with a "daemon unavailable — reconnecting" banner + Retry/Repair (§4/§12/§16).
- **Survival/recovery UX** (O-2): post-restart recovery banner; per-session **`ResumeMode` indicator — `reattached_live` / `resumed` / `replayed` / `relaunched`** (the 4-value survival ladder §8.1, frozen `shared/` 4.1a CONTRACT 0.29.0; the prior "resumed-(live) vs replayed-(relaunched)" bit extended to 4); "Restart session" affordance for `relaunched`/recovery-failed sessions (`RecoveryState=recovery_failed`) (§8/§8.1/§17).
- **Codex context-% = "unknown"** (never a number/0%) per `supportsContextMetadata=false`; carry `metric_quality` on all telemetry (§9.1/§7.2).
- **First-Launch Setup Wizard + macOS consent/TCC map** — stepper of idempotent/reversible/skippable Gateway intents; consent card + denied-degraded + repair for keychain ACL, notification permission, Full Disk Access, launchd Background Item (SMAppService), AppleEvents (§16).
- **Fencing/hard-conflict card** in the Human Input Queue (never auto-resolved); **fail-closed/audit-integrity alert** + "unknown outcome"/"partially succeeded"/"rollback failed" treatments in Audit + HIQ (§15/§17).
- **Approval card = REAL risk + policy** (②-mini, 2026-06-13): the human-approval card binds the **typed `ApprovalQueueRow`** (Appendix A; `risk_level` + `policy_decision: PolicyDecision` — the rule fired / approval requirement / risk reasoning), served through a **typed read** (`get_projection(ApprovalQueue)`→`read_approval_queue_typed`, fail-closed) — no loose-JSON parse on the human-approval path. The authoritative `PolicyDecision` is persisted §15-redacted at approval-open + sibling-read into the read model (§7). Unblocks the ui live-mutation L2.
- **Agent-SDK credit-pool meter** in Usage + on Claude profiles (near-exhaustion + hard-stop), distinct from token spend (§9.1).
- **Native desktop notification settings** (per-type toggles for the UX §11.5 types; permission state; "previews redacted") wired to the notifier (§10/§16); **version-skew + update states** (§6.4/§16); **degraded/offline/stale** variants as first-class (§17/§13.1).

> **Safety-surface rendering note** (2026-06-08, ui-track P6.4d-2): the MVP renders the §17 safety surfaces — the **fencing/hard-conflict card** + the **fail-closed/audit-integrity alert** — at the **Shell level** (the alert near the banner stack; the card in a Shell-level safety host), fixture-driven until the daemon §17 schema freezes. The **full 7-group Human Input Queue host** these surfaces ultimately live in lands in **Phase 8** (§11.5) and absorbs them. The contract end-state is unchanged (card/alert in the HIQ); this documents the MVP staging. Resolution/acknowledge/restart are **parked daemon-1.5 intents** rendered disabled-but-present (never-auto-resolved #6 / non-dismissible #5 / `canSubmitIntent`-gated). The `partially_succeeded`/`rollback_failed` treatments **reuse the frozen `ActionRequest` enum**; the net-new `unknown_outcome`/`fencing_conflict`/audit-integrity states are UI-provisional pending the §17 freeze.

### §11.5 — Action Gateway & Brain UI `[LOCKED — O-3]`
The **Gateway Review Modal accepts an `ActionPlan` (1..N steps)** (single ActionRequest = N=1) and renders per-step rows (step #, action type, target, **risk 0-4**, preconditions, **preview status** incl. pending/unavailable+reason/stale, rollback, step status) + affected ResourceRefs/EvidenceRefs/permissions/audit-note, with the **full Screen-16 controls** (approve-all-eligible [critical/4 excluded], approve-step `step_id`, edit-before-approve [re-preview+re-risk; stale-precondition→fresh approval], remove-step, require-manual-execution, deny-with-reason, save-as-policy→`policy_grant`). **Brain "Run via Gateway" submits the exact reviewed `plan.steps`**; Brain stays propose-only. Brain drawer: add **`Actions` mode**; make modes functional; full scope chips that constrain retrieval; header shows live ProjectBrain status + grounded-at/staleness + privacy/transport; per-answer confidence/verification. Human Input Queue: all **7 groups** (add Needs-clarification, Project-Brain-action-plans, Agent-team-escalations) + full card actions + **expiration**; stamp `actor_type=project_brain` on Brain-originated items.

### §11.6 — Accessibility invariants `[LOCKED — PRD §14.8 MUST; tested §14]`
- **Project Graph list/table fallback** — functionally equivalent (same nodes+edges, status, ownership, attention), Graph|List toggle, keyboard-reachable (OBS-6; absent today).
- **Global `:focus-visible` ring** on every interactive control (tokens exist, unapplied today).
- **Every drag semantic has a non-drag equivalent** (TASK-5): task-chip overflow {Dispatch new / Send to session / Delegate to team}, session-row "Add task as context", Dispatch-dialog target selector.
- Graph nodes keyboard-operable OR the list fallback is the designated keyboard surface; node names include type+status+attention. Capacity meters show threshold on a non-color channel. AttentionMarker rail co-locates glyph+label.
- **Composite widgets** (tablist; later the ProjectSwitcher radiogroup/listbox) use **WAI-ARIA roving tabindex** — exactly one member in the tab order, Arrow/Home/End move focus + **automatically activate** (panels display without latency), the focused index read at event time (never a stale closure). The §9 reachability audit is roving-aware (a `tabIndex=-1` roving member is reachable iff its tablist has exactly one tabstop) and covers visible `role="tabpanel"`. _(ui-track P6.4, `023`.)_

### §11.7 — Component contract fixes `[LOCKED]`
UsageMeter renders the exact/estimated/unavailable accuracy label in **all variants** (ring drops it today) and shows "unknown" (not 0%/empty) when unavailable/NULL. SessionRow adds `model` + team/role (when `agent_team_id` set) and maps every waiting_on_* state into its attention rank. GraphNode adds team_lead/orchestrator/Task/Workflow-command node types (full OBS-2/UX §9 set). EvidenceChip models the 5-state freshness lifecycle (live/stale/moved/unverified/unavailable, each text/aria-labeled) + `confidence`. **External-IDE open** (EDIT-10) as a low-risk action in the Code/Diff flow.

---

## §12 — Backend / daemon strategy

`[LOCKED]`. The Rust daemon is the backend; no server. Detached (launchd/`setsid`), single-instance (`pidlock`), survives UI restarts. Concurrency: one serialized write-actor owns SQLite writes (= the chokepoint); projection reads use read-only WAL connections; long-lived work is Tokio tasks (§10). The daemon owns the Brain sidecar + all agent subprocesses (process-group kill on shutdown). **Extension points:** the `HarnessAdapter` trait (§9.1) and the executor-adapter interface (AG §15.3) — new harnesses/action-types bind here. **Determinism-for-testability** (§14): all ID minting, time, and randomness flow through injectable `IdGen`/`Clock` providers (seedable in tests).

---

## §13 — Brain seam & Workflow Packs

### §13.1 — Project Brain seam `[LOCKED-PENDING-SPIKE — ADR-005; PBI]`
Brain = stdio MCP sidecar; daemon-owned lifecycle. **Brain tools = proposals/queries** fed into the Gateway as intents (R-2 actor `project_brain`); **never direct writes** (INV-SEC-1). Opens no port. **MCP-notification→event mapping** (binding; Appendix A row `BrainEventMapping`): each Brain signal (`notifications/resources/updated`, `…/list_changed`, Tasks status, ProjectBrain status report) → a platform `event_type` (`BrainIndexStarted/Completed/Failed`, `BrainSourceIngested`, ProjectBrain status) with the read-back call the adapter makes and `actor_type=project_brain` stamping. **Brain outbox payload** = the event envelope minus restricted/secret fields, `object_refs` preserved by shared ID, redacted per §15 (`OQ-BR-9`). In-flight Brain crash → timeout → fail the `brain.*` action (retryable for idempotent queries); partial proposals discarded. **Degrades gracefully when absent/stale** (platform never hard-depends on Brain) — BUT the **§25 demo requires a reachable Brain** (precondition, §19.1). **Spike `OQ-PLAT-SPIKE-1`** (macOS sidecar notarization #11992); **fallback = FastMCP streamable-HTTP on 127.0.0.1 + per-launch loopback token** (same server code, keeps the demo working — chosen over the under-specified user-installed-CLI path). iOS reuses the **trait + intent model**, not the UDS transport (§4.3).

### §13.2 — Workflow Packs `[LOCKED]`
Optional; basic projects fully work; detection **advisory** → explicit readiness checks; cc-crew is the first pack (parsers for `MVP_TASKS.md` + `ARCHITECTURE.md §N` anchors). Personalization runs as Gateway action plans (`[P1]` UI, but the action model is MVP-ready). Workflow-owned manifests (`.scaffolding/manifest.json`) read-only to the platform (re-validated before invoke, §7.2). **Trust gate (§15):** any pack command of type `shell_command`/`hook`, or any pack with `trustLevel != bundled_trusted`, routes every script/binary exec through the Gateway at risk≥3 with explicit approval; the policy engine reads the pack `securityProfile` and DENIES forbidden-by-default ops (WORKFLOW_PACKS §16.3). MVP: cc-crew **detection** only (personalization/team/TDD-tracker deferred, §19).

---

## §14 — Testing strategy

Tiered taxonomy with explicit CI gates; **every architecture invariant has ≥1 falsifiable test**; the RISKS.md `TR-*`/`PR-*` test signals map into these tiers (traceability table maintained alongside `tasks-gen`).
- **Unit** (merge gate): state-machine legal-transition tables; idempotency-key canonicalization (same semantics → same key); fencing-token monotonicity.
- **Contract-with-fixtures** (merge gate): **HarnessAdapter conformance suite** run against **recorded fixtures** — Claude (golden SDK-message-stream + statusLine JSON + transcript JSONL per pinned CLI) and Codex (recorded app-server JSON-RPC replayed over a fake stdio child); asserts the **per-harness coverage matrix** (§9.1) per category, not a single shared pass. Gateway per-action tests (risk, preview/dry-run, idempotency dedup) bind to the §6.3 catalog.
- **Daemon-integration-with-fakes** (merge gate): event-store golden-log → rebuild projections → assert; **rebuild-equivalence** (full replay == incremental fold); crash-recovery via a **fault-injection hook** (SIGKILL/abort the write-actor at named checkpoints: mid-apply, post-event-pre-projection, mid-execute) → replay → consistent; **fail-closed** (inject audit-write failure → assert mutation did NOT apply); **fencing-conflict** (fake-clock lease expiry → stale-token write rejected); multi-projection fan-out coherence (one `SessionStarted` updates `proj_session`+`proj_project_graph` in one txn); **FakeHarness/FakePty** for survival + orphan-reaping + resume-vs-replay without real agents.
- **Frontend** (merge gate): projection-view component tests over fixtures (attention-ordering, degraded states, approval cards, graph list-fallback **a11y equivalence**, drag→non-drag paths); UI-restart reattach (mock GatewayPort); terminal backpressure UX.
- **Security** (merge gate): redaction **property/fuzz** test (secret-shaped inputs through every event/preview/audit path → zero unredacted leaks + `SensitiveOutputRedacted` emitted); **architecture-invariant test** proving the Brain-seam crate cannot reach the eventstore-writer or executor crates (INV-SEC-1 / TR-10); injection via stdin-not-argv + 10MB cap; Codex rollout `stat==0600`.
- **Performance** (gate vs §18 budgets): assert each PRD §19.6 metric against its committed budget; the **SQLite single-writer load test** (`OQ-DATA-SPIKE-3`) with committed thresholds (target N=20 concurrent mutating agents; intent-commit p95 budget; reads sub-100ms) — run before single-writer freeze, ceiling documented.
- **Live-agent smoke** (nightly, NOT a merge gate): real pinned CLIs.
- **Demo e2e** (release gate): the PRD §25 path driven via a Tauri driver against a daemon seeded with FakeHarness (or live, with demo preconditions §19.1).
- **Determinism prerequisites** (§12): injectable `Clock`/`IdGen` make all of the above non-flaky.

---

## §15 — Security & trust boundaries

Consolidated model: `docs/planning/THREAT_MODEL.md`. Binding invariants:
- **INV-SEC-1 (no-bypass):** *there exists no code path by which an agent, the Brain, or the future phone mutates FS/git/external/session state except via a typed Action that passed policy + approval and produced an audit event.* Enforcement points: Claude `can_use_tool`+`PreToolUse` (§9.1/O-13), Codex host-routed approvals, Brain proposal-only seam, UI intents-only, fail-closed-on-audit-write. Tested by the §14 architecture-invariant test (no executor reachable except via the Gateway pipeline; an event row exists for every mutation).
- **Redaction-before-persist (wired, not prose):** the eventstore single-writer routes every payload through a shared `Redactor` (owned by `policy`) before INSERT; an event may **never** persist with `redaction_status='unredacted'`. The SAME redactor gates all three sinks — **persist, embed (Brain), sync** — so unredacted content never crosses the Brain `[c]` or integration boundaries. MVP engine: curated high-recall token-prefix set (`ghp_`,`github_pat_`,`sk-`,`xox`,`AKIA`,PEM,JWT) + Shannon-entropy fallback on `KEY=value` lines (`OQ-SEC-2`). On a high-confidence secret that can't be safely redacted → **quarantine the event** + emit `SensitiveOutputRedacted` (EM §23). **[IMPLEMENTED 1.7]** (`c795668`/`f807913`; engine `prefix-entropy-v2`): prefix/PEM + Shannon-entropy on `KEY=value` (base64/whitespace/quote-aware, sub-run-scored vs padding-dilution) + bare high-entropy runs, all masked **in-place**; the quarantine-**divert** (original event diverted, `SensitiveOutputRedacted` recorded in its place — content-free, sensitivity Internal, CONTRACT 0.14.0) is wired + `ForcesQuarantine`-tested but **MVP-unreached** (the redactor masks everything). The §15 fail-closed gate + a property/fuzz pin hold for the catchable set. LESSON §13. **[EXTENDED 2.0-SEC — human-ruled Option B, 2026-06-11]** (engine `prefix-entropy-v2`→**`v3`**): a third **JSON-`"key":"value"` value pass** masks a high-entropy quoted value at the KV-confidence bar (sub-run-scored so prose/paths/URLs are spared), guarded by a **value-shape ID-allowlist** (git-SHA/ULID/UUID spared) — closing residual (a) for **≥20-char** JSON-value secrets at **zero precision cost**. **Measured `prefix-entropy-v3` envelope** (synthetic corpus, 12 secrets / 11 non-secrets): **recall_catchable 1.0 / precision 1.0 / FP-rate 0.0** (overall 0.75); per-category recall Prefixed 1.0 · KvValue 0.75 · JsonValue 0.667 · BareRun 0.5. **OQ-SEC-2 resolved; the recall envelope is now MEASURED + ACCEPTED-AND-OWNED** (the 2.0-SEC corpus is a living regression fixture, recall-floor/FP-ceiling pinned): surviving residuals — **(b)** hex≈git-SHA (~4.0 bits, **irreducible by threshold tuning** — lowering the bar masks real git-SHAs) · **(c)** adversarial split-into-<20-char · **(<20ch-a)** sub-20-char JSON values — stay accepted; primary control remains keychain-refs-only (rule #5; the redactor is defense-in-depth for accidental leaks). _(2.0-SEC re-isolated the 1.7 `test_bare_run_length_boundary` to a bare-string payload — the JSON pass now correctly masks a ≥20-char JSON value, intended (a)-closure not a regression.)_
- **Secrets never in events/payloads:** enforced by the redactor + a §14 test; only `keychain_ref` pointers in DB.
- **Idempotency-key derivation = one-way hash of RAW inputs `[IMPLEMENTED 2.3, lead-ruled Option A]`:** `action_requests.idempotency_key` is a **one-way SHA-256 of the RAW (unredacted) inputs** (catalog `idempotency_formula`: `FromInputs`/`NaturalResourceRef`/`None`). It **by-design bypasses the `inputs_json` row-redactor** because dedup correctness requires raw-input determinism (hashing redacted inputs would false-dedup two actions differing only in a secret → the 2nd silently dropped). This is rule-#4-compliant: a one-way hash is a **fingerprint, not the secret** — the keychain-refs-only convention (real secrets never inline) is the load-bearing control; the key is **catalog-derived, never requester-supplied** (recorded-not-trusted; a proposer-chosen key can't force a collision-to-suppress). **C/HMAC-with-a-keychain-key is the documented hardening** with two upgrade triggers: a real low-entropy-inline-secret threat, OR the `idempotency_key` data ever **leaving the local trust boundary** (off-machine backup/sync), at which point "the DB holds no recoverable secrets" becomes load-bearing against an off-machine attacker. Decisions-tabled (away-authority, flagged for user review); LESSON §20.
- **UDS peer-auth (macOS):** **`getpeereid()`** (NOT `SO_PEERCRED`, which is Linux-only); reject `uid != daemon-uid`; socket perms (0700 dir / 0600 node) are defense-in-depth, not the primary control. Capability-token in handshake `[DEFERRED]` (single-user MVP).
- **Dangerous-command / credential-read policy** (PRD §13.4, named + testable): shell tool-calls matching credential/secret-read patterns (`~/.ssh`, `security find-generic-password`, token-shaped env dumps, keychain-helper output) or non-allowlisted network egress are risk 3-4 requiring approval, emitting `CredentialAccessAttempted`/`DangerousCommandDetected`.
- **Execution-profile binding** (no silent account-hopping, ToS): profile resolved at approval time, recorded in `SessionStarted`; any change requires a new approval (re-preview+re-risk, AG §14.4 critical-field rule); Brain may never set/change a profile; usage-transparency-only, no auto-routing (`OQ-INT-6`).
- **Workflow-pack script gating:** per §13.2.
- **Codex rollout:** **pre-create + own `~/.codex/sessions` at 0700** before launching Codex (closes the 0644 TOCTOU window, #21660), plus first-read chmod 0600 belt-and-suspenders; §14 asserts no rollout file is ever observable at 0644.
- **Scrollback-snapshot sidecar — redaction-before-persist applied `[IMPLEMENTED 075d, 2026-06-17]`:** the daemon persists a per-session VT scrollback snapshot (for the §8.1 `replayed` rung) as a §15-redacted **SUBSTRATE** artifact (the event-store / transcripts family, LESSON §10 — NOT a Gateway Action): a 0700-dir / 0600-file sidecar keyed by `session_id` (`scrollback/`). The redaction-before-persist invariant binds it like the event store (the redaction bullet above): the **ONLY serializable on-disk type holds POST-redaction text** (a raw secret structurally cannot reach the bytes — the §16 co-located-gate), and redaction is **fail-closed** (a non-clean redaction / quarantine-divert → NO sidecar; scrollback lost → `relaunched`, availability degrades, the invariant holds). Reuses the canonical Redactor; 0700/0600 set-absolute+re-read-verify (the Codex-rollout pattern, fail-closed). USER-ruled (2026-06-17) **redacted-PLAIN-text** (formatting dropped in the persisted form; the live re-render stays formatted; cell-level formatted redaction = a deferred fidelity upgrade). **Accepted residual:** unlike events (keychain-refs-only primary control), terminal scrollback has NO keychain-refs backstop → the §13 recall envelope is the sole control (the same best-effort-redaction + 0600 + local-trust posture as harness transcripts; security-reviewer CLEAN ×7 layers). LESSON §58.
- **Largest residual (accepted, MVP):** **no agent egress isolation** — Gateway gating bounds *mutations*, not data exfiltration over the agent's own allowed model-API/network egress; the only MVP reduction is keeping secrets off agent-reachable surfaces (redaction + keychain-only). Recorded as a security non-goal; egress firewalling `[DEFERRED]`.
- MVP threat non-goals: multi-user RBAC, remote/network attackers beyond localhost, compromised OS, cloud runner, agent sandboxing, hash-chain tamper-proofing.

---

## §16 — Deployment, bootstrap & lifecycle

`[LOCKED — ADR-001/011]`. macOS-only. Tauri bundler → signed/notarized `.app` + first-party updater.
- **Build/sign/notarize** (release gate, `OQ-PLAT-SPIKE-1`): Developer ID Application cert; hardened runtime on; entitlements incl. `com.apple.security.cs.allow-unsigned-executable-memory` for the PyInstaller sidecar (evaluate `disable-library-validation` for spawning child processes); **deep-sign order**: inner `.dylib`/`.so` → sidecar exe → daemon binary → `.app`; `notarytool` submit + `stapler staple`; `spctl`/`codesign --deep` CI gate. State whether the detached daemon is signed within the `.app` or as a standalone Developer-ID binary launched by launchd.
- **First-run bootstrap** (ordered, binding): (1) bundler installs a launchd plist (Background Item via SMAppService) OR the UI double-fork+setsid spawns the daemon; (2) daemon acquires `pidlock`, reclaims stale UDS socket, creates app-support dir, creates+migrates DB, registers desktop-host `Device`+`LocalRunner`, binds UDS; (3) UI connects + handshake; (4) setup wizard (the PRD §7.1 surface, now in §11) runs detection + profile creation as idempotent Gateway intents. Daemon-start-failure → explicit degraded UX.
- **Consent / OS-permission map** (PRD §9.1 "consented", §14.7 degraded): enumerate each macOS gate the locked architecture triggers — keychain ACL (post-signing), **notification permission** (DESK-8), **Full Disk Access** (daemon reads arbitrary repos + `~/.claude`/`~/.codex` transcripts), **launchd Background Item approval** (SMAppService), AppleEvents (external-IDE) — each with its setup-wizard step + degraded-state on denial.
- **App-update while daemon running** (`OQ-PLAT-7`, promoted to MVP-blocking): UI stages the new `.app` → sends `prepare_for_update` intent → daemon checkpoints (`wal_checkpoint(TRUNCATE)`), persists scrollback, then **gracefully drains + exits so the new bundle's daemon relaunches via launchd** with resume-or-replay (O-2); refuses if a high-risk action is mid-execute. MVP may require "quit live sessions before update" if hot-swap proves unsafe — state the chosen guarantee.
- **DB migrations** (forward-only `user_version`) **+ backup/rollback** (binding): before any migration that raises `user_version`, copy `nexusops.db` → `nexusops.db.bak-<from_version>`; on failure, restore + surface "update failed, rolled back to vN"; an `app_version ↔ min/max user_version` floor lets a downgraded binary detect "DB newer than I understand" and refuse-safely. Raw events are the irreplaceable spine — never migrate without the backup.
- **Version-compatibility matrix** (binding): `app_version` → IPC handshake range → min/max DB `user_version` → event-envelope version → pinned agent-CLI/SDK tuple (§9.1). Mismatch rules: UI↔daemon handshake refuse+relaunch; daemon↔DB migrate-up-or-refuse; daemon↔sidecar MCP `initialize` version check.
- **Uninstall/reset** (`[P1]`): `shutdown_and_uninstall` (unload launchd, process-group-kill daemon+sidecar+agents, optionally purge app-support + keychain refs) with a "preserve local data" option.

**[IMPLEMENTED 1.6a]** (`daemon/src/bootstrap.rs`, `48b7a2c`/`f1de088`). The cold-start **ordering** is realized: `create_dir_all`(app-support) → **`PidLock::acquire` (the first refusing gate, strictly before any DB write — forbidden #3)** → `EventStore::open` (pragmas → version-floor → migrate-with-backup/rollback → catch-up-replay → outbox crash-recovery) → `DaemonVersionInfo`. The **DB migrations backup/rollback + the `user_version` floor** were already in place from 1.1 (`migrations.rs`); bootstrap **composes** them. The version-compat matrix's **enforcing** floor at bootstrap is the DB `user_version` check (refuse-safe on a DB newer than the binary); `app_version`↔protocol-range is enforced at the 1.5 handshake; the agent-CLI/SDK tuple + the sidecar-MCP `initialize` check remain §9.1/§13.1 **deferred** (`DaemonVersionInfo` reports the tuple, only the DB floor refuses). **LANDED 1.6b** (`26898a8`/`9448035`/`8d06ed7`/`f9c31b1`, brief 028; LESSON §9): the UDS `bind()`/accept-loop (semaphore cap + unlink-before-bind) + the daemon-runtime task spawns (the **write-actor** = a dedicated blocking thread + mpsc; the outbox drainer + lease reaper async interval loops; the broadcast subscribe-**source**, publish-after-commit) + `main.rs` `#[tokio::main]`. (The socket subscribe-**serve** push **LANDED 1.6d** `93f70a5` — broadcast Receiver→socket, close-on-lag resync + a structurally-dedicated single-writer subscribe connection; LESSON §12.) **LANDED 1.6a-L3** (`980e1a0`; LESSON §10): desktop-host `Device` (register-if-absent — stable per-host identity across restarts) + per-start `LocalRunner` registration, appended as **System-actor events** (`actor_type=System`, reserved `SYSTEM_WORKSPACE_ID` sentinel) via the write-actor append path — `DeviceRegistered`/`LocalRunnerRegistered`, **CONTRACT_VERSION 0.11.0→0.12.0**; user-ruled Option B (mechanism = system-event, not a Gateway Action) + Option A (Device-role = MVP-live desktop-host, reconciling §5.3). LESSON §8/§10. `(P1.6a)`

---

## §17 — Failure-mode contract

The single consolidated table (extends EM §23) — each row has an **owning module** and a **binding behavior**. (Detail: `docs/gap-audits/D03-failures.json`.)
| Failure | Owner | Behavior |
|---|---|---|
| Event-write fails (audit-required = all risk≥1) | `eventstore`/`gateway` | **Fail closed**: commit the authoritative `ActionExecution*` event in the same txn boundary as / strictly before acknowledging the mutation; write-failure aborts with a typed `GatewayError`. If a side effect already applied but its terminal event can't be written → emit `ActionPartiallySucceeded` best-effort + hard audit-integrity alert. **[IMPLEMENTED 2.4 L2]** (`8ea9e20`): `execute()` splits into txn-A (Queued→Executing+ActionStarted, COMMITS) → executor OFF the write-actor → txn-B (terminal event); a txn-B write failure → rollback → the action stays durably `executing` (→ L5 reconcile), NEVER acked succeeded. Side-effect-applied (`ExecutionOutcome.side_effect_applied`) + txn-B fails → txn-C emits `ActionPartiallySucceeded` (the loud record IS the audit-integrity alert, NOT the seq-keyed `AuditIntegrityViolation`); if txn-C also fails → `AuditWriteFailed` → stays `executing`. **Fail-closed is UNIFORM/risk-agnostic** (one shared `execute()` path; a deliberate over-satisfaction of the "risk≥1" scoping — not an INV-SEC-1 mandate, since risk-0 is mutation-free). The §14 fault-injection hook (a cfg-gated `fault-injection` Cargo feature via the self-dev-dependency idiom; compiled out + un-armable in release) is the deterministic test vehicle. **[IMPLEMENTED 4.0b-2c (`bf0ad74`)] — the SYSTEMIC fail-stop layer over the per-action fail-closed.** Per-action faults already deny + raise the durable §17 integrity alarm (the C2 sink, `daemon/src/integrity.rs`); a **daemon-wide circuit-breaker** (`daemon/src/gateway/circuit_breaker.rs`) declares **SYSTEMIC** failure — **N=5 consecutive audit-write faults** OR **one clearly-unrecoverable class** (`SQLITE_FULL`/`CORRUPT`/`IOERR`/`NOTADB` = disk-full/DB-corruption/IO) — and **fail-stops via quiesce-and-refuse** (RULED-B 2026-06-13, the user-ruled fail-stop posture): a **latched** Gateway breaker denies every mutation (`GatewayError::AuditBackboneDown` → §6.4 `internal_error`) WITHOUT attempting an audit-write, while **reads/subscribe/terminals stay live**; the durable systemic alarm (`IntegrityKind::AuditBackboneSystemicFailure`, content-free) + the queryable latched `is_tripped()` state are the loud signal. **Never auto-resets** (restart-only — the rule-#6 never-auto-resolved ethos). The consecutive run resets ONLY on a confirmed audit commit (`Ok`); every non-audit-fault error is **no-op** (so denial-spam cannot mask a systemic fault). **SCOPE:** gates the **Gateway mutation backbone** (submit/plan/approve/deny + the live intercept; gate+feed at the write-actor — the single Gateway-driver chokepoint, forbidden #2/#3 — NOT `gateway_txn`, which sits below the gateway layer), NOT the observation-event substrate (Telemetry/Terminal/Device/`AuditIntegrityViolation` — write-actor-direct, not mutation-audit) nor cold-start `reconcile_orphans` (boot-time, breaker freshly-zeroed). security-reviewer CLEAR; +12 tests. LESSON §31. |
| Agent / PTY / app-server dies (**daemon alive**) | `harness`/`terminal` | Supervisor detects child exit → `SessionFailed`+`TerminalProcessExited`/`TerminalPTYFailed`; in-flight `ActionRequest` → `ActionFailed`, release lease/token; offer "restart session" in Human Input Queue. Codex: distinguish pipe-drop (reconnect + `thread/list?cwd=`) from process crash (relaunch). **[IMPLEMENTED 4.2 (`463abc0`/`98488f2`) — the cascade HEAD]**: a supervisor `(id, Failed)` reap → a `SessionFailed {}` System-actor OBSERVATION event (a runtime/ death-driver over the cat-1 supervisor reap seam via an injected `SessionDeathSink`; write-actor through the §15 gate, NOT the Gateway — LESSON §10/§23/§40; emitted ONLY on a `Failed` reap, a clean `Killed`/`Completed` is not a failure) → the `proj_session` projector folds → `status=Failed` → the "restart session" affordance (rendered from `status=Failed`; the full 7-group HIQ host is Phase 8). CONTRACT 0.31.0→0.32.0; security-reviewer CLEAR (cat-1 boundary held). **The cascade ARMS DEFERRED (lead-ruled known-shape-vs-defer — shapes depend on unbuilt producers):** fail-in-flight-`ActionRequest`→`ActionFailed` → Phase 5/7 (agent mutations are adjudication-only; no executing session-action exists) · release-lease/token → session-lease introduction (sessions don't acquire leases) · the Codex pipe-drop-vs-crash distinction → 3.3. The live SIGKILL/orphan-survival check = the §14 HITL surface. |
| Daemon crash mid-action (side effect applied, terminal event not) | `gateway`/daemon recovery | On restart, scan `action_requests WHERE status='executing'`; re-derive real-world state via idempotency key (git2 re-read / octocrab GET / **lease check = `locks::validate_held` live-lease authority**, per the fencing row below) → transition succeeded/failed + emit the missing terminal event; un-reconcilable → surface "unknown outcome". **[IMPLEMENTED 2.4 L5]** (`37cfa97`): `recovery::reconcile_orphans` (wired into `bootstrap::cold_start`) scans `status IN ('executing','queued')` (plan_id-AGNOSTIC — single + N-orphan plan-cascade alike) → per orphan, one atomic txn drives it terminal + emits the missing event: **`executing`** (maybe-applied) → `ActionFailed{UnknownOutcome}` (the loud audit-integrity record; in 2.4 un-reconcilable — no real git2/octocrab re-read yet, Phase 5/7 re-derive a definite outcome via the idempotency_key + `validate_held`) + **KEEPS** the idempotency_key (dedup-protects a double-run); **`queued`** (never ran) → `ActionFailed` + **CLEARS** the idempotency_key (safe to re-submit — closes the permanent-dedup re-run lockout) via a new §5.1 `Queued→Failed` edge. The reaper frees the dead lease. |
| Projection update fails / corruption | `projections` | Mark `projection_offsets.state='degraded'`, skip bad event, never corrupt raw events; startup validates `schema_version` vs projector version → auto-rebuild on mismatch; MVP ships a debug/CLI rebuild (UI rebuild `[P1]`); silent logical divergence is NOT auto-detected in MVP (documented limitation). |
| Stale precondition (preview≠reality at execute) | `gateway` | Re-read live source after lock; on mismatch → `ActionFailed(stale_precondition)`, regenerate preview, require fresh approval if the diff/resource changed (§6.2). |
| Lease expiry of paused holder / fencing conflict | `locks`/`gateway` | Stale-token write → `ActionFailed(fencing_conflict)` + conflict event; rejected session → blocked/conflict state; **hard-conflict card in Human Input Queue (never auto-resolved)**. **"Stale" = NOT a live lease — expired OR superseded** (Option B, human-ratified 2026-06-08, 1.4): the gateway's authority check is `locks::validate_held` = owner-match AND `token == fencing_token` AND `expires_at > now`; an **expired** holder is rejected **even if unsuperseded** (the zombie-holder window), not only when a higher token was minted. The gateway MUST heartbeat/renew (or re-acquire) a long-running action so it isn't fenced by its **own** lease expiry. |
| Integration auth-expiry / rate-limit | `integrations` | Transient (429 Retry-After / 5xx) → outbox backoff; terminal (401/403) → `*SyncFailed` + profile→auth_expired + "re-authenticate" card; outbox row → dead only after bounded retry budget; keychain-unavailable → hard fail. |
| Brain sidecar crash mid-call | `brainclient` | MCP call timeout/EOF → fail the `brain.*` action (retryable for idempotent queries); supervisor respawns (backoff); partial proposals discarded; never blocks the core loop, never leaves a dangling `executing` Brain action. |
| Network loss (offline) | `integrations` | Local cockpit (sessions/terminals/git/Gateway/Brain) fully operational; integration reads serve last projection with "stale (offline)" badge; integration writes queue in outbox → drain on reconnect (idempotent); originating action shows "queued (offline)", not failed. |
| Duplicate / unknown-type / corrupt-payload / clock-skew events | `eventstore` | dedup via `idempotency_key`/`event_id`; unknown version → store raw + degraded marker, don't crash; corrupt `payload_json` → quarantine + audit-integrity event; use both `occurred_at`+`recorded_at`. **[IMPLEMENTED 1.6c — Option C]**: the replay read path (shared by `catch_up_replay` + `rebuild`) quarantines a corrupt (reconstruction-fail) **or** unredacted row into a daemon-internal `quarantine` table (raw spine untouched), marks the offset degraded, and **continues so the daemon starts**; it emits a loud `AuditIntegrityViolation` System-actor event per newly-quarantined seq (structural/content-free reason, §15; idempotent across restart **and** rebuild via `audit_emitted` + idempotency_key + `ON CONFLICT(seq) DO NOTHING`; **CONTRACT_VERSION → 0.13.0**). Unknown `event_version` → **degraded-only** (not an integrity violation; a newer binary folds it). LESSON §11. |

---

## §18 — Performance / non-functional budgets

PRD §19.6 success metrics get committed budgets, an owning module, and a §14 performance-tier assertion (numbers are MVP targets; remaining `[OPEN]` exact values confirmed by the §14 load test + a measurement spike). The **event-store rows below are MEASURED + committed** by the single-writer load test `OQ-DATA-SPIKE-3` (resolved 2026-06-07; `docs/spikes/OQ-DATA-SPIKE-3.md`); the **write rows were re-baselined to the AS-BUILT Gateway submit path by the 2.5 benchmark** (2026-06-11 — **USER-RULED Option A**, realistic-load model; see the §14 guard table + note below).
| Metric | Budget (MVP target) | Owner |
|---|---|---|
| App launch time | < 2 s to interactive shell | UI + daemon bootstrap |
| Project scan time | < 3 s typical repo | `git`/`workflow`/`brainclient` |
| Terminal attach latency | < 250 ms — **AS-BUILT ~2.5 ms p95 (3.5 §18 bench, 2026-06-12; p50 2.0 / p99 3.2 ms; ~98× headroom). Floor scope = terminal-host attach (`/bin/echo` spawn→first flushed `TerminalOutputFrame`); EXCLUDES agent cold-start + the cross-track UDS hop + xterm render (6.3d), foldable when 6.3d lands** | `terminal`/`ipc` |
| Graph render time | < 500 ms for a typical project graph | UI/`projections` |
| Diff open latency | < 500 ms | `git`(git2)/UI |
| Event write latency (intent→committed) | p95 < 100 ms at N=20 concurrent agents (SLO) — **AS-BUILT ~6 ms p95 realistic (full Gateway submit, 2.5; p50 ~2.5 ms); 5.35 ms was the 0.4 raw-append proxy** | `eventstore`/`gateway` |
| Reader latency under write load | p95 < 100 ms at N=20 (SLO) — **AS-BUILT 0.022 ms (2.5) / ≤ 0.38 ms raw (0.4)** | `eventstore`/`projections` |
| Brain drawer response latency | < 1.5 s first token (Brain-bound) | `brainclient`/Brain |
The event-write and drawer budgets bound the central performance bet of the projection-driven UDS + single-writer design (§14 load test quantifies the single-writer ceiling).

**Event-store load test — committed thresholds.** Two distinct measurements bound the write path:
- **Raw event-store append** (`OQ-DATA-SPIKE-3`, resolved 2026-06-07; `docs/spikes/OQ-DATA-SPIKE-3.md`) — a single bare INSERT, no redactor/projections/outbox: **5.35 ms fresh / 8.44 ms @1M**, ~4–5.3k appends/s; the single writer holds p95 < 100 ms through **N=100**. This was the *proxy* for the write budget before the Gateway existed.
- **Full Gateway submit** (intent→committed-and-executed; 2.5 `daemon/benches/event_write.rs`, AS-BUILT against the real `submit_action` → CatalogPolicy → CatalogExecutor → write-actor) — a risk-0 submit = **3 commits × ~8–12 statements each** (event INSERT + §15 redactor-v3 scan + audit projector incl. FTS + 4 projections + outbox + object_refs + the row). This is the path users actually wait on: realistic (non-saturating) submit p95 **~6 ms** (p50 ~2.5 ms); sustained **~1,378 events/s** capacity.

**USER-RULED §18 re-baseline (2026-06-11, Option A — realistic-load model).** The §14 CI guards now gate the **AS-BUILT Gateway submit** under a **realistic non-saturating** load model. The prior 30/75 ms / 1,500 numbers were the *raw-append proxy* — a path that does not exist in production once the mandatory §15 redactor + in-band projections sit on every commit — and are **retired**. The user-facing SLO stays **p95 < 100 ms**; the CI guard sits tighter, at the as-built realistic baseline + margin, so a regression surfaces long before the ceiling:

| §14 CI regression guard (AS-BUILT Gateway submit) | Committed threshold | Basis (2.5 bench, as-built) |
|---|---|---|
| Gateway-submit p95 @ N=20 (realistic, non-saturating) | **< 25 ms** | ~6 ms p95 realistic (p50 2.5 ms; ≈4× margin; well under the 100 ms SLO) |
| Reader p95 under write load @ N=20 | < 10 ms | 0.022 ms as-built / ≤ 0.38 ms raw |
| Sustained throughput (single writer) | floor **≥ 1,000 events/s** | 1,378 ev/s capacity (459 submits/s; events/s == commits/s — the §18-basis unit) |
| _(reported, not gated)_ Gateway-submit p99 @ N=20 realistic | reported | ~7.8 ms |
| _(reported, not gated)_ Saturating-stress ceiling @ N=20 zero-think-time, 30k | p95 ~122 ms · ~844 ev/s | pathological worst case, **not** a realistic-use bound |
| _(reported, not gated)_ Single-writer ceiling sweep | N=100 fresh + @1M-events | nightly (→ 2.6); raw-append held < 100 ms through N=100 |

> Throughput is gated on **events-committed/s** (== commits/s here; 1 event/commit) — the §18-basis unit — not submits/s (≈ events/s ÷ 3); per-submit latency is the user-facing unit. **Secondary 2.5 finding** (Carry-forward, future perf-hardening): the audit projector's FTS5 DELETE+INSERT-per-event cost grows with event count (throughput halved 15k→30k under saturation) — accept-and-document at MVP scale; the Phase-10 lever is **(D) out-of-band projections / background checkpoint**.

WAL config confirmed: `synchronous=NORMAL`, `fullfsync=OFF` (ADR-003). **Phase-1 (1.1) implementation note:** the p99/max tail is the inline `wal_autocheckpoint` (not lock contention) — consider a background-checkpoint thread (`wal_autocheckpoint=0` + periodic manual `PASSIVE` checkpoint off the hot path) to flatten it; not a blocker. Caveat: macOS `fsync()` ≠ `F_FULLFSYNC`, so power-loss could lose the last WAL frames since the previous checkpoint (durable against app/OS crash; hash-chain tamper-evidence is post-MVP).

**Unbudgeted hot paths — deliberate deferral (owner-recorded 2026-06-11, production-posture migration; (a) RESOLVED 2026-06-12 by the 3.5 bench):** two architecture-named hot paths carried **no numeric budget by explicit decision**, not omission. **(a) Terminal Channel throughput/backpressure** (§6.4/§9) — **NOW MEASURED + COMMITTED (3.5 §18 benchmark, 2026-06-12):** the AS-BUILT `read_step`→`flush`+JSON-base64 pipeline sustains **~3,554 MiB/s · ~28,400 frames/s** (128 KiB mean frame, flowing/un-paused), and the watermark bound holds empirically (max buffered ≤ high+chunk; pauses == resumes — LESSON §24). **Committed budget (reported at bench cadence, not hard-gated): sustained Terminal-Channel throughput ≥ 1,000 MiB/s** — un-gated because the as-built (~3,554 MiB/s) runs **~100× over the xterm.js 5–35 MB/s consuming ceiling** (the real bar) and MiB/s is machine-variance-prone; promoting it to a hard CI gate would need the bench to add a throughput assertion (deferred as low-value at this headroom). **§6.4 encoding decision (data-justified):** base64 costs **1.3335× wire expansion**; since the pipeline runs ~100× over the consuming ceiling, base64 is **not** the bottleneck → **the §6.4 binary fast-path is DEFERRED** (marginal value small; revisit only if a real consumer ceiling rises materially). **(b) daemon-restart recovery latency** (resume-or-replay time-to-recovered-sessions, §8/§17) **stays deferred** — committed when the Phase-4 survival benchmark task lands. The phase-exit perf row now ticks the committed (a) budget; only (b) ticks `n/a — deferred (deliberate)`; every other §18 row keeps its committed budget + CI guard.

---

## §19 — MVP boundaries & deferred work

Tiers: `PRD §15-17`. The owner chose a **comprehensive MVP** (O-1/O-2/O-3): both harnesses, full survival, full Brain action plans. This is a large slice; sequence per §19.1 and treat the §24 spikes as gates.

### §19.1 — MVP technical slice (built to the architecture contract; **acceptance = the E2E final-smoke matrix** `docs/runbooks/e2e-acceptance-matrix.md`, NOT a single PRD §25 demo path — user-decided 2026-06-14) `[MVP]`
Build order (invariants → spine → demo path), revised for the audit (bootstrap precedes the daemon skeleton; harness steps reflect both-in-MVP):
0. **Pre-build spikes** (§24): `OQ-PLAT-SPIKE-1` (sidecar notarization), `OQ-HARN-SPIKE-7` (Claude mode + credit pool), `OQ-DATA-SPIKE-3` (write-contention numbers), `OQ-HARN-SPIKE-4` (Codex schema pin), `OQ-INT-SPIKE-6` (git2/octocrab), `OQ-DATA-SPIKE-5` (gap objects).
1. **First-run bootstrap + signing + version-compat** (§16) — must exist before the daemon can ship.
2. **Daemon skeleton + SQLite event store + projections + UDS GatewayPort + pidlock** (§4/§6/§7).
3. **Action Gateway** (risk, preview, approval **incl. ActionPlan + step-approval per O-3**, audit, idempotency, lease/fencing) — the §6.3 action catalog.
4. **Tauri shell** reading projections (Command Center, Project Home/Graph, Sessions, Human Input Queue, Gateway modal, Setup Wizard, Usage Dashboard).
5. **Project add + detection** (git2/CLI; workflow/cc-crew detect; Brain status) + **Execution Profiles** (keychain).
6. **Claude adapter** (mode per `OQ-HARN-SPIKE-7`; `can_use_tool`/`PreToolUse` per O-13) + **Codex adapter** (`app-server`, O-1) behind the one contract; **terminal** (portable-pty→xterm.js).
7. **Full session survival** (O-2): UI-restart reconnect + daemon-restart resume-or-replay + the §17 failure contract + the §14 deterministic test seams.
8. **Permission flow → Human Input Queue → approve via Gateway**; **code/diff review**; **commit/push**.
9. **Project Brain drawer** (read + evidence + **full multi-step action-plan** propose/preview/step-approve, O-3) — demo Brain beats; **demo precondition: reachable Brain + non-exhausted Claude profile**.
10. **PR creation** (octocrab) + task/session/worktree/PR linking + events; **GitHub/Linear read/link**.

### §19.2 — Deferred `[P1]/[P2]`
Agent Team View + `/team-start` orchestration; cc-crew personalization/upgrade UI + TDD slice tracker; PR checks + agent-fix; one-way/bidirectional Linear sync; Brain policy-automation; conflict resolver; usage budgets; iOS companion; Windows/Linux; multi-repo; hash-chain; egress isolation; UI projection-rebuild; uninstall/reset UI.

---

## §20 — Alternatives considered

Per-decision options/fallbacks: `docs/planning/DECISIONS.md` + `RESEARCH.md`. Roads not taken: Electron/Node stack (footprint, ABI tax, JS chokepoint, no iOS reuse) — fallback if Rust velocity/Tauri sidecar signing blocks; in-process topology (can't survive UI quit) — rejected per ADR-002, survives only as a theoretical regression path *for MVP*; Codex `exec --json` (coarser) — fallback to `app-server`; Brain embedded/loopback-HTTP — HTTP is the documented iOS/notarization-fallback; DuckDB/KV/libSQL — rejected vs SQLite.

---

## §21 — Diagrams

Tiering is authoritative from `docs/planning/DIAGRAM_PLAN.md` (corrected from the draft's flat list). **P0 — embed inline in this doc:** D0 system/trust map (§4.1), D1 Gateway pipeline + event/approval chain (§6), **D1a** the synchronous in-harness mutation-intercept round-trip (agent ↔ adapter ↔ Gateway ↔ approval ↔ allow/deny/rewrite — the load-bearing safety mechanic, §9.1), D2 event/projection/outbox flow (§7), D3 Session lifecycle + status-derivation (§5.1; **17 states**, no PTY scraping), D8 PRD §25 demo sequence (§19.1). **P1 — linked:** D4 adapter two-model seam, D5 the 4 canonical chains, D6 trust boundaries, D7 Brain seam, **fencing-token rejection** sequence, **daemon-restart resume-or-replay branch** (promoted from P2 given O-2's risk). **P2:** D9b UI-restart/orphan-kill, D10 deploy.

---

## §22 — Repo scaffold

`[OPEN]` confirmed at scaffold-generate. Daemon (Rust) crates: `gateway, eventstore, projections, harness (+claude/+codex), terminal, git, integrations, locks, brainclient, workflow, ipc, policy, usage, notifier, model (shared types/IDs/enums)`. `ui/` (Tauri: thin Rust host + WebView frontend). `shared/` (IPC JSON-RPC schema, the 22-ID contract, the event-type registry). `brain/` referenced (sibling, not built here). cc-crew personalizes this after this doc is finalized.

---

## §23 — Open questions & spikes

Build-gating spikes (must resolve/plan before/within the relevant `tasks-gen` phase; full register `docs/planning/OPEN_QUESTIONS.md`):
- `OQ-PLAT-SPIKE-1` — macOS notarization of the bundled Python sidecar (Tauri externalBin #11992); fallback = loopback-HTTP Brain (§13.1).
- `OQ-HARN-SPIKE-7` (NEW) — `can_use_tool` mutation-path coverage (direct/subagent/background/MCP × permission mode) **and** the Agent-SDK credit-pool impact → resolves O-4 (Claude SDK-vs-PTY primary).
- `OQ-HARN-SPIKE-4` — Codex `app-server` version-pin + CI schema-regen; modern-vs-legacy approval handling.
- `OQ-DATA-SPIKE-3` — SQLite single-writer write-contention load test with committed thresholds (§18).
- `OQ-DATA-SPIKE-5` — reconcile the 4 desktop-addendum objects into SOM.
- `OQ-INT-SPIKE-6` — git2 read-path survival on `extensions.relativeworktrees` repos; octocrab `pulls().merge()` + GitHub-App token spot-check.
Other `[OPEN]`: redaction engine specifics (`OQ-SEC-2`, MVP answer in §15), event-schema versioning (`OQ-DATA-9`), exact §18 budget numbers, App-update hot-swap-vs-quit guarantee (§16).

---

## Appendix A — Model / contract inventory

The canonical home for every cross-doc-invariant model — mirrored in the area `CLAUDE.md` cross-doc-invariants table. A field change on any model here requires editing this appendix **and** the model's `§` section in the same commit round.

> **Frozen in code (0.5 / `OQ-DATA-SPIKE-5`, 2026-06-07).** The four foundational contract surfaces below — the status state machines (§5.1), the 22 shared IDs + prefix map (§5.2), the actor enum (§7.1/R-2), and the desktop-addendum objects (§5.3) — are now codified in the `shared/` Rust authority crate per the **§5.0 Option-A** mechanism (Rust = authority → schemars JSON-Schema artifact → generated Zod/Pydantic). **Update (4.0b-1 L1, 2026-06-12):** the **ExecutionProfile** runtime-state enum — previously held for 0.5b pending cat-4 — is now **FROZEN at 0.5b** (cat-4 resolved = PTY-primary; CONTRACT 0.24.0; 9 values incl. `credit_exhausted` the SDK hard-pool stop, terminal `{disabled}` only). All ten §5.1 machines are frozen.

| Model | Section | Fields (summary) |
|---|---|---|
| **Event envelope** | §7.1 | **Frozen in `shared/` across 1.1: envelope + 3 enums @ 0.6.0 (df753aa); `redaction_status` + `redaction_engine_version` @ 0.7.0 (redaction commit).** event_id, seq, event_type, event_version, occurred_at, recorded_at, workspace_id, actor_type, actor_id, source_type, source_id, correlation_id, **redaction_status**, causation_id?(=EventId), action_request_id?, approval_id?, session_id?, agent_team_id?, workflow_run_id?, idempotency_key?, sensitivity, visibility, **redaction_engine_version?**, payload_json, payload_hash?(rsvd), previous_event_hash?(rsvd), schema_version, app_version. Enums frozen: source_type(15, **closed**), sensitivity(5), visibility(4), **redaction_status(unredacted\|redacted, §15)**. |
| **Actor/Requester enum + mapping** | §5.1/§7.1 | audit actor_type (10, incl. remote_client); requester_type aliases agent_session→session_adapter, workflow_pack→workflow_runtime, system_policy→automation_policy |
| **GatewayPort** | §6.1/§6.4 | submit_action, submit_action_plan, preview_action, approve, deny, get_projection, subscribe, get_capabilities. **[IMPLEMENTED 1.5] READ surface live** (get_projection/subscribe/get_capabilities) over the UDS transport (4-byte-len+JSON framing, `getpeereid` peer-auth, HelloFrame/HelloAck/VersionSkewError handshake, `ServerFrame` frame-mux, `protocol_error`); **[IMPLEMENTED 2.1b] mutation methods LIVE** — `submit_action`/`preview_action`/`approve`/`deny` over UDS (peer-auth-FIRST, post-handshake; `ActionAck`/`Ack` wire types; `submit_action_plan`/`PlanAck` **[IMPLEMENTED 2.1c]** (`b9e00a1`) over UDS). Cross-language IPC contract @ **CONTRACT_VERSION 0.19.0** (2.1b 0.16.0 → 2.1c 0.17.0 [`PlanAck`] → 2.2 0.18.0 [`ActionTypeCatalog` + `PolicyDecision`+3] → **2.4 L1 0.19.0** [`ActionPartiallySucceeded` event + structured `ActionFailed.error: ActionError` + IpcErrorCode `fencing_conflict`/`internal_error`] → **3.1 0.20.0** [the §9.1 normalized types + `TelemetrySampled`] → **3.4 L1 0.21.0** [the **Terminal Channel** frames]); `protocol_version` `{min:1,max:1}`. **[IMPLEMENTED 3.4 — CONTRACT 0.21.0]** the **Terminal Channel** (§6.4/ADR-009) frozen: `TerminalOutputFrame{terminal_id,seq,data}` · `TerminalInputFrame{terminal_id,data}` · `TerminalControlFrame{terminal_id,kind:TerminalControlKind(pause\|resume)}` + **`ServerFrame::TerminalOutput`** (the reserved Terminal frame-mux slot filled); **encoding = JSON-base64 MVP** (`data` base64 over the unchanged codec; **binary fast-path DEFERRED — 3.5 bench data-justified** (~3,554 MiB/s ≈ 100× the consuming ceiling, §18)); `terminal_id` = a daemon runtime handle (not a frozen-22 IdKind). LESSON §24. **[IMPLEMENTED 4.0b-ui1 — CONTRACT 0.28.0]** (`9848513`) the §6.1 **`get_diff(worktree_id, file) → DiffResult{hunks}`** read RPC — request-on-demand git2-LIVE read (read-only; NOT a `Diff` projection — diffs are large/transient, the §7.2 worktree-live-read precedent); resolves `worktree_id→proj_worktree.path` over read-only WAL (typed `NotFound` until P5.2 populates proj_worktree) → the new `daemon/src/git/` read-only module (`read_diff`; NEW git2 dep, read-only). Frozen types: `DiffResult{hunks:Vec<Hunk>}` · `Hunk{header,old_start,old_lines,new_start,new_lines,lines:Vec<DiffLine>}` · `DiffLine{kind:DiffLineKind(context\|added\|removed),content}` · `GetDiffParams{worktree_id,file}`. **`IpcErrorCode` 9→10: + `NotFound`** (the read-not-found case; `precondition_stale`/`internal_error` were semantically wrong). §2.5-seam snapshots; 3-way verify 38==38==38 @ 0.28.0. LESSON §33. **[IMPLEMENTED 4.0b-2-F2 — §6.4/§10 availability]** (`4a7572a`) the **intercept-wait permit-class split** — a reserved sub-bound on the accept pool so concurrent 5-min approval-waits can't starve the UI: a 2nd `Semaphore(wait_cap = MAX_CONNECTIONS − RESERVED = 48 of 64)`; a parked intercept-wait holds it alongside its general permit → at most `wait_cap` general permits held by waits → `RESERVED=16` general slots ALWAYS free for UI `approve`/`deny` / reads / subscribe. **Wait-class exhaustion → fail-closed Deny** (`INTERCEPT_SATURATED_REASON`, distinct from the timeout Deny; the wait is NEVER entered — no park, **no bypass**, fail-SAFE). Factored as a PURE `intercept_verdict_with_wait_class` seam + `InterceptWaitClass` wrapper (the LESSONS §9/§12 pure-classifier family); the live handler delegates with the real wait class (/wired: wait_cap=48 in production). **Daemon-internal (accept-loop/IPC) — no `shared/` surface, no CONTRACT bump.** security-reviewer CLEAR. LESSON §34. |
| **ActionRequest** | §6.2 | **Frozen 2.1a** (`shared/src/actions.rs`; CONTRACT 0.15.0; snapshot-pinned). action_request_id, project_id?, action_type, requester_type, requester_id, resource_refs[], inputs, risk_level(0-4), idempotency_key?, fencing_token?, **status(15) ⇒ `ActionRequestStatus` $def** (renamed Option B), preview?, created_at. (AG sec 9.1's richer draft — policyContext/preconditions/dryRunRequired/source/intent/context — reconciled OUT: context→envelope columns, policy/preconditions→2.2/2.4, expiry→Approval.) |
| **ActionPlan / ActionPlanStep / ActionDependency** | §6.2 | **Frozen 2.1a.** ActionPlan{plan_id(`aplan_`), title, steps[], dependencies[], overall_risk, approval_mode(approve_all\|step_by_step\|mixed\|blocked)} — **rollback_plan?→2.4**; summary/created_at/evidence_refs additive-later. ActionPlanStep{step_id, label, action_request, required, can_skip, rollback_action_type?, status(`ActionRequestStatus`)}. ActionDependency{step_id, depends_on_step_ids[]} (shape chosen 2.1a — AG referenced but never defined). Plan = grouping-over-action_requests; the `action_plans` metadata table + nullable `plan_id` FK = a DATA_MODEL §2.9 addition **[IMPLEMENTED 2.1c]** (`b9e00a1`; MIGRATION_8 — `action_plans{plan_id,project_id,requester_type,requester_id,title,overall_risk,approval_mode,created_at}` + `action_requests.plan_id` FK + `approvals`/`proj_approval_queue` generalized to nullable `action_request_id`+`plan_id`). |
| **Approval** | §6.2/§5.1 | **Frozen 2.1a.** approval_id(`appr_`), action_request_id?, plan_id?, required_approver (= `RequiredApprover{kind, actor?}`, kind∈current_user\|project_owner\|actor; `ActorRefBody{actor_type:RequesterType, actor_id}`), **status(10) ⇒ `ApprovalStatus` $def** (renamed Option B), risk_level, scope(single_action\|plan\|policy_grant), decided_by?, decided_at?, expires_at — **constraints?→2.2**. |
| **ActionResult** | §6.2 | **Frozen 2.1a.** action_request_id, status (`ActionResultStatus`: succeeded\|failed\|partially_succeeded\|cancelled), created_resources[], changed_resources[], emitted_events(Vec\<EventId\>), error?(Option\<String\> — **stays `Option<String>`; the structured `ActionError` type was frozen 2.4 L1 and is carried on the `ActionFailed` event; aligning `ActionResult.error`→`ActionError` defers to when `ActionResult` is first emitted, Phase 3+**), rollback_available. |
| **ResourceRef / EvidenceRef / PolicyDecision** | §6.2 | **Frozen 2.1a.** ResourceRef{type(`ResourceType`: **20** — AG sec 9.8 verbatim; the §6.2 "21" is a count-reconcile, the 21st NOT invented), id, uri?} (**displayName→additive-later**, id-resolvable). EvidenceRef{type(`EvidenceType`,11), id, **label** (folded — AG-required, irreducible Brain evidence text), confidence?(`EvidenceConfidence`,4)}. PolicyDecision{status(`PolicyDecisionStatus`,6), reasons[], **required_approvals(Vec\<RequiredApprover\>), constraints(Vec\<String\>), safer_alt?(Option\<String\>) — added 2.2 [IMPLEMENTED `1b45e9d`], minimal Q3 shapes** (the real constraint/safer-alt engine is post-MVP; `required_approvals`/`constraints` left empty in 2.2, the pipeline hardcodes `current_user`)}. |
| **ActionTypeCatalog** (per-type) | §6.3 | **[IMPLEMENTED 2.2]** (`shared/src/catalog.rs`; `1b45e9d`; CONTRACT 0.18.0; §2.5-seam snapshot-pinned). The frozen **22-entry** catalog — per action_type `{locked_risk(0-4), preview_class, executor_kind, idempotency_formula, requires_resource_refs, params_schema_present}` + a fail-closed `lookup` (None for any non-catalog type — never default-allow). **AS-BUILT locked risk:** **0** (auto-execute-eligible; read/inspect/propose-only, mutation-free) `brain.ask, project.rescan, workflow.detect, git.status, git.diff, code.open_file` · **1** `session.attach_terminal, session.pause` · **2** `session.create, session.resume, session.send_message, plan.link_task, linear.link_issue, linear.create_issue, git.create_worktree, git.create_branch, github.create_pr_draft, brain.sync, brain.summarize_session` · **3** `github.create_pr, review.request_agent_fix` · **4** (critical) `workflow.command.invoke` — **lead-ruled away-authority** (the §6.3 cannot-be-standing-granted/OQ-WP-5 floor is structurally risk-4; Decisions-tabled; flagged for user review). `preview_class`/`executor`/`idempotency_formula` **[REALIZED 2.3]** (`f4ebeaf` idempotency+dedup · `c244294` preview framework · `957fe65` executor framework; CONTRACT held 0.18.0 — all daemon-internal; +sha2): the `ActionExecutor` trait (validate/preview/execute/optional-rollback) + `CatalogExecutor` dispatch by `executor` (`ExecutorKind`) + the `preview_class` dispatch + `idempotency_formula` derivation/dedup are realized as the **FRAMEWORK** — the per-namespace executor **bodies** + the per-class **real previews** are side-effect-free STUBS until their owning phase (git2/octocrab/session/Brain = Phase 3/5/7/8; the trait is the seam, not the side effect). Idempotency = one-way SHA-256 of raw inputs (§15 precedent); preview-impossible escalates the **preview envelope** risk only (never the gating risk); INV-SEC-1 holds (executor reachable only via the 3 gated seams; grep-confirmed side-effect-free). LESSON §20. **[3.2-part-2/043 — the agent-mutation family, CONTRACT 0.22]** a separate `AGENT_MUTATION_ACTION_TYPES` const (`agent.bash`/`agent.file_edit`/`agent.file_read`/`agent.mcp_tool`; read-only=risk-0, mutating=risk-2 conservative) + a new **`ExecutorKind::Adjudication`** (wire `adjudication`) — the Claude interception's typed Actions, **adjudication-only** (terminate at the verdict, NO daemon executor — the `CatalogExecutor::execute` Adjudication arm is a fail-safe → Failed). **MVP_ACTION_TYPES stays 22** (the agent family is machine-internal). LESSON §26. **[4.0b-1 — the session-lifecycle catalog, CONTRACT 0.25.0; MVP 22→24]** `session.create` **risk-2→risk-0** + NEW `session.kill` **risk-0** + NEW `session.profile_change` **risk-2** (`d0b7425`): session.create/kill = **risk-0 audited auto-allow** (away-ruled — risk-1-not-approval-gated was contradictory per LESSON §19; the faithful vehicle for "routine, audited, no per-launch approval"), guarded by **5 pins** — (a) `SessionStarted` audit emitted → INV-SEC-1 preserved; (b) §15 #8 profile recorded-at-start; (c) **`session.profile_change` = approval-gated** (the no-silent-account-hop gate lives there, NOT on the start); (d) the risk-0 relaxation is NARROW to session-lifecycle (the no-non-zero-auto-execute pin intact); (e) UI/IPC-initiated only (no agent/Brain path at risk-0). The §6.2 policy added a **`RISK0_AUTO_EXECUTE_ALLOWLIST`** that gates ALL risk-0 auto-execute (a safe-direction broadening of the lead's literal "mutations only" — the policy can't distinguish mutation from read-only without a catalog field, so gating every risk-0 is strictly more defensive; allowlist↔catalog biconditional pinned). The **`SessionExecutor`** (`ExecutorKind::Session`, `gateway/session_executor.rs`, `5e8faed`) drives the 4.0a supervisor over the NON-LIVE launcher + emits `SessionStarted{execution_profile_id}` **in-txn** (via `ExecutionOutcome::Succeeded.emitted_events` appended atomic with `ActionSucceeded` in txn-B, fail-closed); the §15 #8 ExecutionProfile binding is recorded-at-start. **Mechanism-built, NO production caller** (the binding condition held BY CONSTRUCTION — not wired in `main.rs`, no IPC method; the live wiring = 4.0b-2). **[4.0b-R1a — the executor-registration seam, daemon-internal, NO CONTRACT change]** `CatalogExecutor` is now a per-namespace **registry** (`handlers: HashMap<ExecutorKind, Arc<dyn ActionExecutor>>` + `register()`) dispatching `execute`/`rollback` to the registered handler **else today's structured stub** (incremental — a namespace goes live when its handler registers); the §6.3 `requires_resource_refs` precondition + the **INV-SEC-1 `Adjudication` fail-closed guard run BEFORE dispatch** (an adjudication action never reaches a handler even if one is registered — execute-time guard, security-PASS). 4.0b-2 registers `SessionExecutor` here; edges registers Project/Git/Github/Linear at P5/P7. LESSON §20 (refined). **[4.0b-2 L1, CONTRACT 0.26.0→0.27.0]** 3 new `agent.*` types extend `AGENT_MUTATION_ACTION_TYPES` to realize the user-ruled **d.2 split tool-policy** (the live interception's tool-allowlist): `agent.todo_write`(risk-0, the **LONE auto-allow** on `RISK0_AUTO_EXECUTE_ALLOWLIST` — provably no FS/git/external/exfil surface, adjudication-only) · `agent.web_fetch`+`agent.web_search`(risk-2, **approval-gated** — the data-exfil dimension). **The CATALOG IS the explicit enumerated allowlist** (not a category heuristic) — an unclassified tool → `CoverageGap`→Deny (fail-closed); adversarial-tested. Additive (MVP-22 untouched, the 043 agent-family precedent); the 043 route-EVERY-intercepted-tool-through-the-Gateway audit-before-verdict invariant preserved. The live wiring (the `decision_sink`/`intercept` transport + the real-Claude launch + the interception) = the cat-1 4.0b-2 L2. LESSON §26 (extends). **[4.0b-ui1, CONTRACT 0.27.0→0.28.0; MVP 24→27]** (`9848513`) the cross-track ui-6.3e freeze: 3 new `git.*` types — `git.stage_hunk`/`git.unstage_hunk` **risk-2** · `git.discard_hunk` **risk-3** (all `executor_kind=Git`, `requires_resource_refs`; bodies = R1-A registry stubs until Phase-5) — + a new **`standing_grant_eligible: bool`** field (the §6.2 **non-standing-grant floor**): `git.discard_hunk`=false (destructive/irreversible → ALWAYS per-action human approval, never standing-granted) + `workflow.command.invoke`=false (the risk-4 floor unified onto ONE mechanism); the plan approve-all exclusion generalized to `risk==Level4 OR !standing_grant_eligible` (BOTH disjuncts kept; a drift-invariant test pins every risk-4 ⇒ !standing_grant_eligible). `git.discard_hunk` `preview_class=Diff`. **Resource-ref hunk encoding (FROZEN convention):** `ResourceRef{resource_type=File, id="{worktree_id}\x1f{file}\x1f{old_start},{old_lines},{new_start},{new_lines}"}` — `\x1f` unit-separator; hunk-precise (distinct hunks → distinct `NaturalResourceRef` keys); read↔mutate consistent (the ui forms it from `get_diff`'s `Hunk`; the Phase-5 git executor parses it back; pathological `\x1f`-in-filename = a Phase-5 escape/validate hardening; conformance-tested at the real-executor+ui land). security-reviewer CLEAR. LESSON §32. **[edges-029 / Wave-C — CONTRACT 0.32.0→0.33.0; MVP 27→28]** (`355eddf`) `integration.connect` **risk-2** + a NEW **`ExecutorKind::Integration`** (wire `integration` — a keychain-write/registration namespace, NOT a network sync): the `IntegrationExecutor` REGISTERS an integration connection (emits `IntegrationConnectionRegistered{connection_id,provider,keychain_ref,account?}`). **REGISTRATION-ONLY** (§15 + LESSON §20 forced — a risk-2 action executes off the §15-redacted durable row, so a token in `inputs_json` is masked-by-execute-time → the mutator STRUCTURALLY cannot carry the token; inputs `{provider, keychain_ref POINTER, account?}`, NO token; **§15 #4 holds BY CONSTRUCTION**; the token→keychain WRITE is a separate DEFERRED non-Gateway/HITL mechanism — the `keyring` crate is not yet a dep, folds with H1). `standing_grant_eligible=**false**` (`entry_no_standing_grant` — the §6.2 **credential floor**: an authorization-establishing action is never folded into a plan-level approve-all, always a per-action human approval, the `git.discard_hunk` precedent; **USER-ratification pending** at the edges return-review [keep false / relax to true]). INV-SEC-1 no-bypass (the executor holds only `Box<dyn IdGen>`, emits via `emitted_events`, reachable ONLY via the catalog-gated pipeline at risk-2→approval, NOT on the `RISK0_AUTO_EXECUTE_ALLOWLIST`); defense-in-depth secret-shaped-`keychain_ref` reject via the canonical `PrefixRedactor` (LESSON §13) read-only. **MVP_ACTION_TYPES 27→28.** `shared/` snapshot + 3-way verify @ 0.33.0; security-reviewer **full invariant PASS**. LESSON §49. |
| **EventTypeRegistry** (MVP subset) | §7.1 | per event_type: event_version, payload schema, sensitivity default, consuming projection(s). **First entry `SessionStarted` implemented 1.2 (`shared/src/events.rs`; CONTRACT_VERSION → 0.8.0). `DeviceRegistered` + `LocalRunnerRegistered` added 1.6a-L3** (System-actor cold-start registration; `actor_type=System`; CONTRACT_VERSION → 0.12.0). **`AuditIntegrityViolation`{seq,reason} added 1.6c** (§17 degradable-replay quarantine record; System-actor; CONTRACT_VERSION → 0.13.0). **`SensitiveOutputRedacted`{original_event_type,reason,detector} added 1.7** (§15 redactor quarantine-divert record; content-free; sensitivity Internal; CONTRACT_VERSION → 0.14.0; 3-way verify @ 0.14.0). **The 8-type Gateway `ActionExecution*` family added 2.1b** (`a45f2e3`→`2c83b8f`): `ActionRequested`{action_type,risk_level,requester_type} · `ActionApprovalRequested`{approval_id} · `ActionApproved`{approval_id,decided_by?} · `ActionDenied`{approval_id,reason} · `ActionExpired`{approval_id} · `ActionStarted`{} · `ActionSucceeded`{} · `ActionFailed`{error} — identity (actor/action_request_id/project_id/resource_refs) on the envelope columns, payload = delta only; emitted ONLY by the Gateway via the write-actor append (the §15-gated path); **CONTRACT_VERSION → 0.16.0** (additive; bundle + 3-way verify @ 0.16.0). **No new event types in 2.1c/2.2** — CONTRACT advanced 0.17.0 (`PlanAck`/`PlanStepAck`) → 0.18.0 (`ActionTypeCatalog` + `PolicyDecision`+3) via non-event additions. **`ActionPartiallySucceeded`{reason} added 2.4 L1** (`89bf300`; the §17 side-effect-applied-but-terminal-event-unwritable record; `reason` = a redaction-safe STRUCTURAL string; the loud audit-integrity alert) + **`ActionFailed.error` String→structured `ActionError`** (internally-tagged on `kind`: `audit_write_failed`/`stale_precondition`/`fencing_conflict`/`unknown_outcome`/`executor_error{message}`; renders as a `oneOf`, ServerFrame precedent); **CONTRACT_VERSION → 0.19.0** (the §6.4 `fencing_conflict`/`internal_error` codes + the §5.1 rollback/`Queued→Failed` edges ride this bump; §2.5-seam snapshot + local byte-diff verified; cross-language 3-way is CI-gated). **`TelemetrySampled`{sample,model?,execution_profile_id?} added 3.1** (`shared/src/events.rs`; the §9.1/§18 harness telemetry **observation** event — feeds `proj_usage_ledger`; identity on the envelope columns, payload = the `TelemetrySample` + the model/exec-profile rollup dims the envelope lacks; **non-mutation → appended via the single write-actor, NOT the Gateway** — INV-SEC-1 routes state mutations, the `DeviceRegistered`/`AuditIntegrityViolation` System-event precedent; live emission lands with the real adapters 3.2-part-2/043); **CONTRACT_VERSION → 0.20.0** (additive; §2.5-seam snapshot + 3-way verify). **`TerminalProcessExited`{terminal_id,exit_code?,signal?} added 3.4 L1** (`shared/src/events.rs`; the §6.4/§9/§17 PTY-death record — a **non-mutation observation event**, appended via the single write-actor NOT the Gateway [the `DeviceRegistered`/`TelemetrySampled` System/adapter-actor precedent, LESSON §10/§23]; emitted by the terminal host on detected child exit, from the OS exit status — never output-parsed [#9]; the §17 SessionFailed→ActionFailed→lease cascade that consumes it is P4); **CONTRACT_VERSION → 0.21.0** (additive; §2.5-seam snapshot + 3-way verify). **`ActionDenied.approval_id` relaxed `String`→`Option<String>` at 3.2-part-2/043 L5** (the agent-mutation policy-deny **record-then-deny**, A1: a policy-deny has no approval → `None`; human-deny still `Some(appr_)`; the projector keys `denied` off the event_type, not the payload — breaks no consumer; daemon-internal/un-consumed/additive-tolerant); **CONTRACT_VERSION → 0.23.0**. **The 11-type Phase-5/7 wiring family + `Provider`(github\|linear) added 4.0b-R1b** (`150ad83`; cross-track edges-R1; **`shared/`-only, NO daemon emission** — edges' P5/P7 executors emit via `EmittedEvent` later): **P5.1** `ProjectRescanned`{is_git,repo_root?,remote_url?,branch?,detached,is_dirty,workflow_pack,cc_crew,plan_file?,brain,scanned_at} (one coarse event; **`remote_url` §15 contract = strip-userinfo-`user:token@`-at-source by the emitter + the redactor backstop, rule #5** — a generic URL password is outside the recall envelope, LESSON §13; enforcement = edges P5.1) · **P5.2** `WorktreeCreated`{worktree_id,path,branch_name,base_branch?} · `BranchCreated`{branch_name,base?} · `WorktreeMerged{}`/`WorktreePrunable{}`/`WorktreeDeleted{}`/`WorktreeLocked{}` (the 4 overlay transitions = **empty-payload**, identity on envelope, the `ActionStarted{}` precedent; `WorktreeStatusRefreshed` is **NOT** an event — git-axis = a live-read projection cache) · **P7.1** `PullRequestSynced`{pr_number:u64,status(§5.1 `PullRequest`),branch,base,mergeable?,checks_summary?,pr_checked_at} · `IntegrationConnectionRegistered`{connection_id,provider,keychain_ref,account?} (**`keychain_ref` = a non-secret POINTER only, rule #4**) · `GithubSyncFailed`+`LinearSyncFailed`{provider,reason,failed_at} (**non-auth variant only**; `reason` = a redaction-safe STRUCTURAL class-name; `provider` redundant-but-spec'd kept — §17 mandates distinct per-provider types + the edges projector reads `provider` uniformly; the `auth_expired` variant DEFERRED to edges P7.1 [the 0.5b gate is now LIFTED but it needs a §17/INV-SEC re-review]). `pr_number: u64` (non-negative external natural — positivity structural at the parse boundary; the precedent for external GitHub naturals). **CONTRACT_VERSION → 0.26.0** (additive; §2.5-seam snapshots + 3-way verify 37==37==37 — the gate restored at 4.0b-T). **`SessionRecovered`{mode(`ResumeMode`),replayed_event_count:u64,execution_profile_id?(`ExecutionProfileId`)} added 4.1b-1** (`1e68f20`; the §8/§8.1/§17 daemon-restart recovery signal — the §11.4 resumed-vs-replayed-vs-reattached indicator; a **non-mutation OBSERVATION event** appended via the single write-actor, **NOT the Gateway** — the System-actor recovery is the daemon re-materializing its OWN approved session [Q1=(a), LESSON §10/§23 precedent]; profile from the COMMITTED prior `SessionStarted`); **CONTRACT_VERSION → 0.31.0** (additive; §2.5-seam snapshot + 3-way verify 40==40==40). **`SessionFailed {}` (empty-payload) added 4.2** (`463abc0`; the §17 supervised-child-death signal — emitted on a supervisor `(id, Failed)` reap; the §11.4 "restart session" affordance via `proj_session.status=Failed`; a **non-mutation OBSERVATION via the write-actor, NOT the Gateway** — the System-actor death-observation, LESSON §10/§23/§40; **EMPTY-payload** — the forensic "why" is the correlated `TerminalProcessExited{exit_code,signal}`, a `reason` is additive-later when distinguishing signal exists [every death reaps indistinguishably as `Failed` now]; the §17 cascade arms — fail-in-flight-action/release-lease/Codex-distinction — deferred to Phase 5/7 / session-leases / 3.3); **CONTRACT_VERSION → 0.32.0** (additive; §2.5-seam snapshot + 3-way verify 40==40==40). **Accretes per phase as event types land.** |
| **MVP projections + `projection_offsets` + `object_refs`** | §7/§7.2 | 10 `proj_*` (ProjectActivity, Session, ApprovalQueue, Worktree, PullRequest, PlanProgress, ProjectGraph[node+edge], AgentTeam, AuditTrail, UsageLedger) + `object_refs` + `projection_offsets`. **Implemented 1.2**: in-band fold in the event-commit txn; offsets advance same-txn (never ahead of rows); status cols bind the §5.1 enums; catch-up replay + full rebuild (raw events untouched). Projector bodies = 4 Phase-1-feedable + 6 re-homed to producing phases (DDLs for all 10). **[②-mini 4.0b-ui2, 2026-06-13 — CONTRACT 0.30.0]** `proj_approval_queue` (+ the `approvals` registry row) gain a `policy_decision_json` column: the authoritative `PolicyDecision` (`gateway/pipeline.rs:154`, previously computed-then-dropped) is persisted **§15-redacted** on the `approvals` row at approval-open (`gateway/approval.rs`, the `request::insert` dual-gate precedent, LESSON §16) and **sibling-read** into `proj_approval_queue` (IMMUTABLE field, rebuild-safe, LESSON §17). Single-action → the real decision; plan-level/per-step → NULL (follow-on). LESSON §37. |
| **ApprovalQueueRow** (first frozen projection-row) | §7/§7.2/§11.5 | **[Frozen ②-mini 4.0b-ui2 — CONTRACT 0.30.0; `shared/src/projections.rs`; §2.5-seam snapshot + 3-way verify; `8c4b948`]** the FIRST typed `shared/` projection-row (the `proj_approval_queue` provisional→generated reconcile). 14 typed wire fields: `approval_id` · `action_request_id?`/`plan_id?`/`project_id?`/`session_id?`/`agent_team_id?` · **`risk_level: RiskLevel`** · **`status: Approval`**(10) · **`requester_type: RequesterType`** · `requester_id` · `preview_summary?` · `requested_at` · `expires_at?` · **`policy_decision: Option<PolicyDecision>`** (the §6.2 frozen type — the rule fired / approval requirement / risk reasoning the §11.5 human-approval card renders). `deny_unknown_fields`; optionals-as-null; the internal `sort_key`/`updated_at_seq` are OMITTED from the wire row. **Typed serve:** `get_projection(ApprovalQueue)` routes to `read_approval_queue_typed` (deserialize each DB row → `ApprovalQueueRow` → serialize; **fails CLOSED** on a mis-typed `policy_decision_json` — no loose-JSON on the human-approval path, pin #2); other projections keep the generic JSON read. Field names match the ui provisional where aligned. The remaining projection-row provisionals (SessionRow/ProjectActivityRow/PullRequestRow/AuditEventRow) follow this pattern. LESSON §37. |
| **PullRequestRow** (2nd frozen projection-row) | §7/§7.2/§11.2 | **[Frozen P7.2 — CONTRACT 0.34.0; `shared/src/projections.rs`; §2.5-seam snapshot + 3-way verify; `e748874`]** the 2nd typed `shared/` projection-row (the `proj_pull_request` provisional→generated reconcile; the ui PR Review Workspace §11.2 consumer). 9 BASIC wire fields: `pr_id` (the `{repo_id}#{pr_number}` composite PK) · `project_id?`/`repo_id?` · `pr_number: Option<u64>` · `title?` · **`status: PullRequest`**(11, the frozen §5.1 enum, reject-unknown) · `head_branch?`/`base_branch?` · `pr_checked_at?`. `deny_unknown_fields`; optionals-as-null; DDL-match nullability (`pr_id`/`status` non-Option); the internal `updated_at_seq` OMITTED. **[Enriched D5a/P4.6 — CONTRACT 0.36.0; `proj_pull_request` + the row both gain `mergeable: Option<bool>` + `checks_summary: Option<String>`]** the P7.2 "basic-now + SPREAD" enrichment landed LOCKSTEP (the fail-closed typed serve forces it atomic): MIGRATION_13 (ALTER-only, `SUPPORTED_USER_VERSION` 12→13, the historical CREATE untouched) adds the 2 columns; the `PullRequestProjector` folds them from `PullRequestSynced.mergeable?`/`checks_summary?` (INSERT + DO UPDATE, None→NULL, rebuild-safe); the 2 typed row fields + the typed-serve SELECT + the CONTRACT bump all land together. `mergeable` is the FIRST bool projection column → `read_pull_request_typed` coerces SQLite-INTEGER(0/1)→JSON-bool in the READ layer (the `shared/` contract stays a pure `Option<bool>`). LESSONS §53. **Typed serve:** `get_projection(PullRequest)` routes to `read_pull_request_typed` (deserialize each `proj_pull_request` row → `PullRequestRow` → serialize; **fails CLOSED** on a mis-typed / extra-column row — the contract↔projection drift fails closed; the `read_approval_queue_typed` precedent). §15 no-secrets by construction (ULIDs/branches/status/timestamp — no `remote_url`/token; the data is already §15-redacted on the `PullRequestSynced` event). The ui regenerates from 0.34.0 (`pr_number` string→number + `toPrItems` id→`pr_id` are the ui's regen deltas). LESSON §37. |
| **SessionRow** (3rd frozen projection-row) | §7/§7.2/§8.1/§11.4 | **[Frozen D2 (P4.4) — CONTRACT 0.35.0; `shared/src/projections.rs`; §2.5-seam snapshot + 3-way verify; `7fc8bc7`]** the 3rd typed `shared/` projection-row (the `proj_session` provisional→generated reconcile; the §11.4 resumed-vs-replayed recovery display). 10 wire fields: `session_id` · **`project_id`** (non-Option — DDL `NOT NULL` + the fold guarantees it) · **`status: Session`**(17, frozen §5.1, reject-unknown) · `display_name?`/`harness?`/`model?`/`execution_profile_id?` · **`resume_mode: Option<ResumeMode>`**(4) · `replayed_event_count?`/`recovered_at?`. `deny_unknown_fields`; optionals-as-null; OMIT `updated_at_seq` + the not-yet-consumed columns (basic-now + SPREAD). **The fold:** `SessionRecovered` (emitted by 4.1b-1's `recover_sessions_on_restart`, previously a DEAD event) → a `SessionProjector` arm (mirrors the `SessionFailed` arm; UPDATE `resume_mode`/`replayed_event_count`/`recovered_at`, derive-from-event rebuild-safe LESSON §17; does **NOT** change `status` — owned by `SessionStarted`/`SessionFailed`); MIGRATION_12 (ALTER ×3, `SUPPORTED_USER_VERSION` 11→12, the historical CREATE untouched). **Typed serve:** `get_projection("Session")`→`read_session_typed` — a `SESSION_ROW_WIRE_FIELDS` retain-whitelist (the 22-col table → the 10 wire keys, additive-column-safe) + a drift-proof whitelist-matches-struct test, fail-closed. §15 no-secrets by construction. **ui regen (0.35.0):** the ui must extend its `ResumeMode` shadow **2→4** (`+relaunched`/`+reattached_live`) before consuming the serve (else a `relaunched`/`reattached_live` value fails its Zod parse) + `title`→`display_name` + `project_id` optional→non-Option. LESSON §37. |
| **ReviewRow** (4th frozen projection-row) + the review vertical | §7/§7.2/§11.2/§9/§6.3 | **[Frozen D5b-1 (P4.6) — CONTRACT 0.36.0→0.37.0; `90cadd2`]** a NEW subscribe-able projection vertical for structured PR reviews: the **`ReviewSynced`** event (§7.1; `{review_id:u64,pr_number:u64,reviewer,state:ReviewState,submitted_at?,body?,review_synced_at}`; `body` free-form user text → rides the §15 redaction gate, security-CLEAR) + a frozen **`ReviewState`** VALUE enum (`approved`/`changes_requested`/`commented`/`dismissed`/`pending`; reject-unknown; a plain value enum, NOT a `status_machine!` — a review IS a state, no lifecycle) + the typed **`ReviewRow`** (`shared/src/projections.rs`, `deny_unknown_fields`; `review_id` PK [GitHub-globally-unique, NOT a composite] + `pr_number?`/`project_id?`/`repo_id?`/`reviewer`/`state`/`submitted_at?`/`body?`) served typed/fail-closed (`get_projection(Review)`→`read_review_typed`) + **`ProjectionName::Review`** (the §6.1 closed-set add — the CONTRACT cost of subscribe-ability; partially closes the D4b future-CONTRACT flag) + `proj_review` (MIGRATION_14, `SUPPORTED_USER_VERSION` 13→14, REBUILD_TABLES) + the `ReviewProjector` (fold keyed by `review_id`; `project_id`-envelope + `repo_id`-sibling-read; rebuild-safe) + the `deltas_for_event` Review nudge. **[Producer D5b-2 — CONTRACT 0.37.0→0.38.0; `76b7f4a`; MVP_ACTION_TYPES 28→29]** the **`github.sync_reviews`** catalog action (§6.3; **risk-1** — the github-READ precedent: a NETWORK read is not risk-0 auto-execute [rate-limit/exposure] but below the risk-2 github writes; `requires_resource_refs`, `idempotency=None`, `side_effect_applied=false`, `standing_grant_eligible=true`): the `GithubExecutor` fetches a PR's reviews (octocrab `list_reviews`, §46 sync-executor) + emits one `ReviewSynced` per review; failure → `GithubSyncFailed`. The full D5 review vertical is live end-to-end (action → reviews → proj_review → typed serve → ui §11.2). LESSONS §37/§46/§54/§55. |
| **Outbox** (transactional) | §7/§12/§17 | `out_` id (**daemon-internal — NOT one of the 22 frozen contract IDs**); `destination`/`status` enums (daemon-internal, not a `shared/` contract). Rows written in the event-commit txn (recorded-iff-intended); the **§15 *sync* sink** (payload from the already-redacted event, filter-only); at-least-once drainer (backoff + retryable/terminal + bounded dead-letter; `reset_in_flight` on `open`). **Implemented 1.3** (drainer Tokio spawn 1.6-wired). |
| **HarnessAdapter trait + normalized types** | §9.1 | **[Frozen 3.1 — CONTRACT 0.20.0; `shared/src/harness.rs` + `daemon/src/harness/mod.rs`; LESSON §23]** methods{launch,stream_status,intercept_mutation,read_transcript,telemetry_heartbeat,resume,capabilities}. **SHARED (frozen DATA types):** `TelemetrySample{tokens_in,tokens_out,context_pct:Option<f32>,cost_estimate,metric_quality}`, `MetricQuality`(exact\|estimated\|unavailable), `TranscriptRef{path,hash,is_in_place}`. **`NormalizedStatus` = the frozen `status::Session`(17) re-exported** (adapters map INTO it; no forked enum). **DAEMON-INTERNAL (unfrozen — 3.2/3.3 async-ify the drive loop; sync object-safe `Box<dyn>` per the `ActionExecutor` seam):** the trait itself, `MutationIntercept{tool,params,decision_sink}` (non-serializable sink). **`ResumeResult` FROZEN 4.1a** (CONTRACT 0.29.0; was deferred at 3.1) — `ResumeResult{mode: ResumeMode, replayed_event_count: u64}` + `ResumeMode`(4)/`RecoveryState`(3) now in `shared/` (the dedicated **Survival/recovery schema** row below; the §8.1 ladder; `decide_resume` classifier). **Naming reconcile:** §9.1 prose `cost`/`quality` → AS-BUILT `cost_estimate`/`metric_quality` (DDL match). **[3.2-part-1 reshape, 2026-06-12 — daemon-internal/UNFROZEN, LESSON §25]** the trait reshaped for the real adapter: **`Send+Sync`→`Send`** + **`launch(&self)`→`launch(&mut self)`** (the real `ClaudeAdapter` owns a `Box<dyn Pty>` [Send-only] + a mutable status machine — cannot be `Sync`/`&self`; security-reviewer SAFE). A **`PtySpawner` object-safe factory** (`terminal/mod.rs`) produces the `Box<dyn Pty>` `launch()` injects. **Claude adapter = PTY-primary** (cat-4 resolved): the O-13 `launch()` spec (default-mode-only, no `-p`, no bg subagents — the #10 enforcement surface) + the pure `derive_status(prev,ClaudeSignal)→Session` (STRUCTURED signals, never PTY bytes #9) + `read_transcript()` landed; the `MutationIntercept`→Gateway interception (043) + the `telemetry_heartbeat` emission (044 — per-heartbeat DELTAS, context-% gauge, `TelemetrySampled` via an injected sink, daemon-Clock UTC-Z bucket; LESSON §27) **LANDED → 3.2 COMPLETE**; `resume()` + the production drive caller = P4. **[IMPLEMENTED 4.0c (`c0a935d`) — the production telemetry path is LIVE]** the `WriteHandle`-backed `WriteActorTelemetrySink` (constructed in `runtime/`, injected into the `ClaudeAdapter` via an **opaque `TelemetrySinkFactory`** so `session/` stays cat-1 import-grep-clean) + a **periodic pump** (the `SessionActor` telemetry tick → `HarnessAdapter::poll_telemetry()` → `ClaudeAdapter` drains an injectable `UsageSource` → the 044 `push_usage` emit-on-ingest) + the **cumulative-baseline high-water-mark clamp** in `push_usage`'s storage (fixes the dip-then-climb over-count; the pure `telemetry_sample` keeps its per-delta floor). Emit = fire-and-forget `WriteHandle::try_append_observation` (`try_send`, soft-degrade, never back-pressures the writer — LESSONS §9/§30; an OBSERVATION event, NOT the Gateway — ZERO `Action*`, INV-SEC-1 untouched); the launcher↔write-actor bootstrap cycle breaks via a deferred `OnceLock` handle slot. **NON-safety; NO CONTRACT bump** (`TelemetrySample` frozen 0.20.0). The **live `UsageSource` ingress** (hook-receiver/statusLine feed) stays the **P4** follow-on; the real `workspace_id`/`project_id` threading = **P5**. LESSON §35. |
| **HarnessCapabilities** | §9.1 | **[Frozen 3.1 — SHARED, CONTRACT 0.20.0]** the 10 PRD-HARN-5 bool fields: `supports_terminal/resume/transcript_read/tool_call_parsing/usage_metadata/context_metadata/command_injection/subagents/hooks/cloud_tasks` — drives per-capability UI degradation (`supports_context_metadata=false` for Codex → context "unknown", never faked 0%). |
| **Per-harness mutation-coverage matrix** | §9.1 | **[Frozen 3.1 — DAEMON-INTERNAL const, verbatim §9.1 / O-13]** tool-category × harness → `Guaranteed`\|`GuaranteedInDefaultMode`\|`BestEffort`\|`NotGuaranteed`\|`Unsupported`\|`NotApplicable`: Claude direct=GuaranteedInDefaultMode, subagent=NotGuaranteed, background-subagent=Unsupported(#27203), mcp=BestEffort; Codex direct/mcp=Guaranteed, subagent/bg=NotApplicable. The §14 conformance suite asserts it; the UI degrades off `HarnessCapabilities`, not this matrix. |
| **Survival/recovery schema** | §8.1/§9.1 | **[Frozen 4.1a — CONTRACT 0.29.0; `shared/src/harness.rs`; §2.5-seam snapshot + 3-way verify; LESSON §36]** the §8.1 B2-strict survival ladder's wire types: **`ResumeMode`**(4: `resumed|replayed|relaunched|reattached_live` — strict-precedence ladder, the B2-strict 4th value) · **`RecoveryState`**(3: `recovering|recovered|recovery_failed` — the ui provisional `provisional.ts`, frozen verbatim) · **`ResumeResult`**`{mode: ResumeMode, replayed_event_count: u64}` (the §9.1 `resume()` return; `replayed_event_count` non-zero only on `replayed`). Reconciles the ui provisional `ResumeMode`(2→4)/`RecoveryState`; the ui `RecoveryStatus{state,affectedSessions?}` aggregate + the post-restart recovery event/projection reconcile at 4.1b/4.3. The resume/replay/reattach **DECISION logic** = a pure total classifier (`daemon/src/harness/resume.rs::decide_resume`, daemon-internal, FakeHarness/FakeBroker, harness-agnostic). **[4.1b-1 — CONTRACT 0.31.0; `1e68f20`]** the production caller `recover_sessions_on_restart` (`main.rs:202`) drives `decide_resume` on restart + emits a NEW **`SessionRecovered{mode: ResumeMode, replayed_event_count: u64, execution_profile_id: Option<ExecutionProfileId>}`** observation event (the §11.4 indicator; EventTypeRegistry row below); recovery-spawn = a System-actor recovery (Q1=(a), §8.1 AS-BUILT, the 4 INV-SEC-1 conditions). The **detachable-terminal broker = 4.1b-2** (LIVE survival = HITL). NON-safety data types; the recovery orchestration is security-CLEAR (no-2nd-mutator). |
| **Status state machines (10 total)** | §5.1 | Session(17), Task(superset), Worktree(2-axis: git+overlay, derived), PullRequest, WorkflowInstance, ProjectBrain, Approval(10), ActionRequest(15), AgentTeam, **ExecutionProfile**(9) — **all 10 frozen in `shared/`** (the first 9 at 0.5; **ExecutionProfile frozen at 0.5b** — 4.0b-1 L1 2026-06-12, CONTRACT 0.24.0, 9 values incl. `credit_exhausted`, terminal `{disabled}` only; cat-4 resolved = PTY-primary). _(§5.1 prose header reconciled to "Ten machines" at the Phase-0-exit `/arch-finalize` re-run, 2026-06-07: 9 frozen + ExecutionProfile held → 0.5b.)_ **2.1a (Option B, value-preserving — away-authority lead ruling, flagged for user return-review + next `/arch-finalize`):** the `ActionRequest`/`Approval` enums' **schema `$def` names** are `ActionRequestStatus`/`ApprovalStatus` (their pre-existing Rust aliases), freeing the bare `ActionRequest`/`Approval` `$defs` for the §6.2 MODELS (schemars keys `$defs` by bare type-name → collision, LESSON §15). Wire VALUES (15/10) + Rust type-names unchanged — a `prj_→eprj_`-class frozen-NAME reconcile. |
| **22 shared IDs + ID format** | §5.2 | the 22 IDs; prefixed-ULID newtypes; **canonical id_kind prefix map (frozen 0.5)** — 16 platform-minted: `ws_ proj_ repo_ wt_ sess_ team_ prof_ pack_ wfi_ cmd_ plan_ task_ act_ evt_ artf_ evid_`; 4 desktop: `dev_ rc_ lr_ eprj_`; 6 external (branch_name, commit_sha, architecture_anchor, linear_issue_id, github_issue_number, pr_number) = native, no prefix. harness_session_map{session_id,harness,harness_native_id,cwd,rollout_path}. **`IdKind` = the PBI cross-product 22 ONLY (frozen, untouched).** Non-cross-product platform ids are sibling categories: **desktop** (`dev_`/`lr_`/`eprj_`/`rc_`) off `DesktopObjectKind`; **2.1a Gateway objects `ApprovalId`(`appr_`) + `ActionPlanId`(`aplan_`) off `GatewayObjectKind`** (via `gateway_minted_id!`, mirroring `desktop_minted_id!`) — UI-contract but not Brain-cross-product (RULED Option A 2026-06-11; same ULID format + fail-closed parse; the 22-member `IdKind` + its contract test are unchanged). |
| **Desktop-addendum objects** | §5.3 | LocalRunner, EventProjection, **Device (desktop-host) — MVP-live** (registered at cold-start §16, register-if-absent; user-ruled Option A 2026-06-10; `is_deferred()` true only for RemoteClient); RemoteClient + the iOS multi-device/pairing dimension of Device (deferred) |
| **BrainEventMapping** | §13.1 | MCP signal → platform event_type + read-back call + actor stamping |
| **Version-compatibility matrix** | §16 | app_version ↔ IPC handshake range ↔ DB user_version range ↔ event-envelope version ↔ agent-CLI/SDK tuple |
| **Lease** | §5.1/§7.2/§17 | **Implemented 1.4** (`daemon/src/locks/`). One row per `(resource_id, lease_kind)`: owner_id, fencing_token, acquired_at, heartbeat_at, expires_at. `fencing_token` = **persisted monotonic high-water mark** (acquire/reclaim mints +1 under `BEGIN IMMEDIATE`; release/reap keep it; survives restart). **Authority = a LIVE lease** (Option B, human-ratified): `validate_held` = owner-match AND `token == fencing_token` AND `expires_at > now`; "stale" = expired **OR** superseded → `fencing_conflict` (safety rule #6, never auto-resolved). pidlock single-instance = std advisory file lock. **Daemon-internal** — no `lease_` ID, not a §5.1 status machine, UI/Brain don't read `leases` → **no `shared/` surface / no CONTRACT_VERSION bump**. |
| **Audit-backbone circuit-breaker + integrity alarm** | §17/§15 #5 | **[IMPLEMENTED 4.0b-2c (`bf0ad74`)]** (`daemon/src/gateway/circuit_breaker.rs` + `daemon/src/integrity.rs`). The §17 fail-secure layer — the single audited mutator does not operate when it cannot audit. **Daemon-internal** — **no `shared/` surface / no CONTRACT_VERSION bump** (`IntegrityKind::AuditBackboneSystemicFailure` is daemon-internal; `GatewayError::AuditBackboneDown` reuses the §6.4 `internal_error` wire code). A **latched** breaker trips on **N=5 consecutive** audit-write faults OR one **unrecoverable class** (disk-full/DB-corruption/IO) → **quiesce-and-refuse** (every mutation fail-closed-denied WITHOUT an audit-write; reads stay live; **never auto-resets** — restart-only). Resets ONLY on a confirmed audit commit (`Ok`); no-op on non-audit-fault errors (denial-spam can't mask a systemic fault). Gates the Gateway mutation backbone (submit/plan/approve/deny + the live intercept; gate+feed at the write-actor), NOT the observation substrate nor cold-start reconcile. The durable off-DB alarm (the C2 sink) + the queryable `is_tripped()` are the loud signal. LESSON §31. |

---

## §24 — (reserved — see §23 for open questions & spikes)

*Anchor reserved to keep §23 = Open Questions stable; spike list lives in §23.*
