# NexusOps `daemon/` — Build Guide

> **You're in `daemon/`.** This file plus root `CLAUDE.md` both load. The root file covers global project conventions + shared comm rules (track-prefix, escalation taxonomy, messaging budget); this file owns code-area conventions for the Rust daemon (trust core).

## Launch protocol

| Working on... | cwd | Loads |
|---|---|---|
| Planning / docs / commits | repo root (`NexusOps/`) | root `CLAUDE.md` only |
| the Rust daemon (trust core) code | `daemon/` | this `CLAUDE.md` + root |

<!-- For a multi-area project, add a row per additional code area. -->

If you find yourself fighting the wrong conventions, check your cwd.

## Session start/end protocol

**At session start:**
1. Read `IMPLEMENTATION_PLAN.md` (repo root) **by section, not whole** — `grep -n "^##" IMPLEMENTATION_PLAN.md` for offsets, then Read with offset/limit just "Currently in progress" + the active phase. (The file grows; never load it whole.)
2. Confirm with the user what feature this session is targeting.
3. Read the relevant section of `ARCHITECTURE.md` from the lookup table below.

**At session end** (only when the user explicitly says we're done):

1. **Implementer runs `/session-end`.** Implementer writes ONLY:
   - `daemon/` code files (the slice's implementation)
   - test files (the slice's tests)
   - dependency manifest / lockfile (deps the slice adds)
   - `docs/sessions/<NNN>-<date>-<topic>.md` (session doc, created at `/session-end` Step 5)

   **Implementer must NOT touch (all orchestrator territory).** *This list is the canonical statement
   of the territory rule — `/session-end`, the brief template, and the generated
   `scripts/guards/territory-guard.sh` PreToolUse hook (which mechanically enforces it in team mode)
   all point here.*
   - `IMPLEMENTATION_PLAN.md`
   - `daemon/LESSONS.md`
   - `daemon/CLAUDE.md` (entire file — both the Cross-doc invariants table AND the Lessons logged index)
   - `ARCHITECTURE.md`
   - `docs/orchestrator-briefing.md` / `docs/tdd-brief-template.md` / `docs/briefs/` / `docs/runbooks/`
   - other top-level deliverable / design docs
   - `.gitignore` and root-level dotfiles (unless adding a new artifact to ignore, flagged at Step 9)

   At Step 10: **explicit `git add <path>` per slice file; never `git add -A`/`.`; never stage an orchestrator-territory file.** Changes to any orchestrator-territory file (a new cross-doc model, a lesson, an arch note) are **flagged at Step 9**, not edited here — the orchestrator writes them hot (root `CLAUDE.md` + the Step-9 matrix).

2. **Orchestrator runs `/orchestrate-end`** for round close-out + Carry-forward triage + round terminal commit + push.

## Lookup table — where to find canonical info

Don't paste these sections into the prompt. Grep the file:section, read only what you need. `/check-arch <topic>` dispatches off this table.

| Topic | File (relative to repo root) | Section |
|---|---|---|
| Action Gateway / mutation invariant | `ARCHITECTURE.md` | §X |
| Lessons logged (full prose) | `daemon/LESSONS.md` | by lesson # |

<!-- Starts near-empty. Add a row whenever a topic is looked up twice. Populate as the project accretes. -->

**Code intelligence & docs (when available):** prefer a code-intelligence MCP / docs MCP over grep+read loops — see root `CLAUDE.md` "Code intelligence & docs."

## Stack

<!-- ▼ EXAMPLE BLOCK [id=area-stack]: stack quick-reference for implementer sessions. Canonical stack lives in root CLAUDE.md + ARCHITECTURE.md; this is the cheat sheet. ▼ -->

- **Runtime:** Rust (stable, edition 2021) · cargo.
- **Framework:** Tokio async runtime — rusqlite (event store + projections), portable-pty (agent harness), git2 (read-only repo introspection), octocrab (GitHub), keyring (secret refs), rmcp (MCP host).
- **Validation:** serde + serde_json (schemars for JSON-RPC schemas).
- **Lint / types / tests:** clippy / rustc via `cargo check` / `cargo test`.
- **Role:** this is the **trust core** — the sole DB writer and the sole mutator of all state. Every change is a typed, risk-classified, approved Action recorded as an immutable event; agents, the UI, and the Project Brain only *propose* intents to this daemon.

<!-- ▲ END EXAMPLE BLOCK [id=area-stack] ▲ -->

## Standard commands

```bash
# Install deps (run once; re-run when the manifest changes)
cargo fetch

# Run the dev server (if applicable)
cargo run -p nexusopsd

# Tests
cargo test

# Quality
cargo clippy --all-targets -- -D warnings
cargo fmt --check
cargo check --all-targets

# Preflight (use before saying "done" with a feature)
# fmt-check is FIRST — /tdd Step 8 was clippy-only, which let 0.5 (06f9576) land unformatted (needed style follow-up 407be7c). fmt is part of the gate.
cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo check --all-targets && cargo test
```

## TDD protocol

**Write the failing test first.** Applies to deterministic code — see the TDD posture in root `CLAUDE.md` for what is test-first vs. exempt.

**Commit per slice when practical.** Never bundle a safety-critical slice with anything else.

## Forbidden patterns

<!-- ▼ EXAMPLE BLOCK [id=forbidden-patterns]: forbidden patterns — 3-5 narrow, enforceable, domain-specific rules. Shape: "Don't <pattern X> because <reason / past incident>; use <alternative Y>." Test-pin them where possible. Starts small; accretes as lessons surface. ▼ -->

Do not:

1. **Write code without a failing test first** (for deterministic code). Even one-line functions.
2. **Mutate state outside the Action Gateway** — the daemon is the single audited mutator (INV-SEC-1); every change must flow through a typed `ActionRequest` so it is risk-classified, approved, and recorded as an immutable event. Never reach around the Gateway to flip state directly; submit an `ActionRequest`.
3. **Write `nexusops.db` from anywhere but the single daemon write-actor** — there is exactly one writer task. Every other path (UI projections, queries, edge modules) opens a **read-only WAL connection**. Concurrent writers corrupt the event log / break ordering; route all writes through the write-actor.
4. **Scrape PTY output to infer agent status** — terminal bytes are display-only and lie about lifecycle. Read status from the SDK event stream / Codex app-server push; never regex the PTY to decide an agent's state.
5. **Put secrets in events, payloads, logs, or rows** — persist **keychain references only**, and run the Redactor over any payload before it is persisted or emitted (INV-SEC). A secret in the immutable event log cannot be unwritten.
6. **Use git2 for mutations** — git2 is **read-only** (repo/branch/diff introspection). All worktree, branch, commit, and merge operations go through the **git CLI as Gateway actions** so they are typed, approved, and audited. Never call a git2 mutating API.

**Enforcement patterns (machine-readable — `/preflight` warn-greps the staged diff against these).**
One `grep -E` (or `ast-grep`) expression per line, each tied to a numbered rule above. Rules that can't
be expressed as a pattern carry a `pin:` (test ref) or `accepted:` note on the rule itself instead.

```forbidden-patterns
# <rule 2>: <pattern — e.g.>  datetime\.now\(\)
# <rule 3>: <pattern>
```

<!-- ▲ END EXAMPLE BLOCK [id=forbidden-patterns] ▲ -->

## Cross-doc invariants — schema/docs mirroring

Several typed models in this codebase are **contracts** mirrored in `ARCHITECTURE.md` and indexed in the table below. The architecture doc is the canonical contract; the model is the executable enforcement. Drift produces silent disagreement.

**Authoring discipline (orchestrator owns this table).** The implementer never edits this table or `ARCHITECTURE.md` directly — it flags a field add/remove/rename at Step 9 as a `Cross-doc invariant change`; the orchestrator writes the row + the arch edit hot the same round (see root `CLAUDE.md` + `docs/orchestrator-briefing.md`). Commits stagger; the working tree stays aligned within the round.

| Model | `ARCHITECTURE.md` section | Notes |
|---|---|---|
| Status state machines (10 total) | §5.1 / Appendix A | **All 10 frozen in `shared/`** (first 9 at 0.5): Session(17), Task, Worktree(2-axis), PullRequest, WorkflowInstance, ProjectBrain, Approval(10), ActionRequest(15), AgentTeam. Wire = snake_case `TEXT`. The 10th, **ExecutionProfile, FROZEN at 0.5b** (4.0b-1 L1 2026-06-12, CONTRACT 0.24.0; 9 values incl. `credit_exhausted` the SDK hard-pool stop, terminal `{disabled}` only; cat-4 resolved = PTY-primary). **2.1a (Option B, value-preserving reconcile):** the `ActionRequest`/`Approval` status enums' **schema `$def` names** are `ActionRequestStatus`/`ApprovalStatus` (their pre-existing Rust aliases) — freeing the bare `ActionRequest`/`Approval` `$defs` for the §6.2 MODELS; wire VALUES (15/10) + Rust type-names unchanged. |
| 22 shared IDs + prefix map | §5.2 / Appendix A | **Frozen in `shared/` (0.5)** as prefixed-ULID newtypes. 16 platform: `ws_ proj_ repo_ wt_ sess_ team_ prof_ pack_ wfi_ cmd_ plan_ task_ act_ evt_ artf_ evid_`; 4 desktop: `dev_ rc_ lr_ eprj_`; 6 external = native. `parse()` fail-closes on wrong-prefix/bad-ULID (§15). **`IdKind` = the PBI cross-product 22 ONLY (frozen, untouched).** Non-cross-product platform ids live in sibling enums: desktop (`dev_`/`lr_`/`eprj_`/`rc_`) off `DesktopObjectKind`; **2.1a Gateway objects `ApprovalId`(`appr_`)/`ActionPlanId`(`aplan_`) off `GatewayObjectKind`** via `gateway_minted_id!` (RULED Option A — UI-contract but not Brain-cross-product; same ULID format + fail-closed parse). |
| Actor enum (R-2) | §7.1 / §5.3 / Appendix A | **Frozen in `shared/` (0.5).** 10 values incl. `remote_client` (legacy EM §7 `remote_device` superseded). |
| Desktop-addendum objects (4) | §5.3 / Appendix A | **Frozen in `shared/` (0.5).** LocalRunner + EventProjection + **Device (desktop-host) MVP-live** — Device registered at cold-start §16 (register-if-absent; user-ruled Option A 2026-06-10; `is_deferred()` true only for RemoteClient). RemoteClient + the iOS multi-device/pairing dimension of Device stay deferred. `dev_`/`lr_` ids via `desktop_minted_id!` off `DesktopObjectKind` (frozen-22 untouched). |
| Contract SoT mechanism | §5.0 | Option A: Rust authority → schemars JSON-Schema (`shared/contracts/schema/`, versioned, diff-gated) → generated Zod/Pydantic. The pattern for every future contract addition. |
| Event envelope + enums | §7.1 / Appendix A | **Frozen in `shared/` across 1.1: envelope + `source_type`(15, **closed**)/`sensitivity`(5)/`visibility`(4) @ 0.6.0 (df753aa); `redaction_status`(unredacted\|redacted, §15) + `redaction_engine_version` @ 0.7.0 (redaction commit).** EventEnvelope + ObjectRef; `deny_unknown_fields` (reject-unknown); `redaction_status`/`redaction_engine_version` = new DATA_MODEL §2.1 columns; the writer **fail-closes** (never persists `redaction_status='unredacted'`); `causation_id` = `EventId`. |
| §6.2 Gateway core data model (`ActionRequest` + family) | §6.2 / Appendix A | **Frozen 2.1a** (`shared/src/{actions,time,gateway_ids}.rs`; CONTRACT_VERSION **0.15.0**; `feat(shared)`). 10 models: `ActionRequest`, `ActionPlan`, `ActionPlanStep`, `ActionDependency{step_id,depends_on_step_ids[]}`, `ActionPreview` (envelope; 6 typed previews→2.3), `Approval`, `ActionResult`, `ResourceRef`, `EvidenceRef{type,id,label,confidence?}`, `PolicyDecision{status,reasons}` (stub — `.{requiredApprovals,constraints,saferAlt}`→2.2). + 9 enums (`RequesterType`, `RiskLevel`={int 0-4}, `ApprovalScope`, `ApprovalMode`, `PolicyDecisionStatus`, `ActionResultStatus`, `ResourceType`(20 — AG verbatim; 20-vs-21 doc-reconcile open), `EvidenceType`(11), `EvidenceConfidence`) + `RequesterType→ActorType` (R-2) map + `Timestamp` newtype (RFC3339/date-time). `deny_unknown_fields` + optionals-as-null (stable snapshot). Binding source = Appendix A + DATA_MODEL §2.9 (AG sec 9 = origin); deferred fields each marked →owning slice. §2.5-seam snapshot-pinned (`shared/tests/contract.rs`). **[2.1b LIVE] the staged pipeline + `action_requests`/`approvals` rows + `submit_action`/`preview_action`/`approve`/`deny` (UDS, peer-auth-first) + the 8-type `ActionExecution*` event family + §15 row-redaction (`inputs_json`/`resource_refs_json`) — CONTRACT 0.16.0; per-transition atomic {row+event} fail-closed; security PASS ×3.** **[2.1c LIVE]** `submit_action_plan`/`PlanAck` + the `action_plans` table (MIGRATION_8 — + `plan_id` FK + approvals/proj generalization to nullable `action_request_id`+`plan_id`) + the `proj_approval_queue` projector + subscribe-delta + the approve-cascade (critical never cascaded) — **CONTRACT 0.17.0**; whole-plan atomic fail-closed; security PASS ×3. **[2.2 LIVE]** `PolicyDecision` +3 fields (`required_approvals`/`constraints`/`safer_alt`, minimal Q3 shapes) + the §6.3 `ActionTypeCatalog` (row below) + `CatalogPolicy` + the risk-0 auto-execute path — **CONTRACT 0.18.0**. LESSONS §14/§15/§16/§17/§18/§19. |
| §6.3 ActionTypeCatalog (per-type risk/policy) | §6.3 / Appendix A | **[IMPLEMENTED 2.2]** (`shared/src/catalog.rs`; `1b45e9d`; **CONTRACT 0.18.0**; §2.5-seam snapshot-pinned). The frozen **22-entry** catalog — per action_type `{locked_risk(0-4), preview_class, executor_kind, idempotency_formula, requires_resource_refs, params_schema_present}` + a fail-closed `lookup` (None → never default-allow). Risk-0 set **mutation-free** (auto-execute-eligible); `workflow.command.invoke` = **risk-4** (lead-ruled away-authority — the §6.3 cannot-be-standing-granted/OQ-WP-5 floor is structurally risk-4; Decisions-tabled, user-review-flagged). `CatalogPolicy` (`daemon/src/gateway/catalog.rs`) resolves risk from the catalog (**authoritative, never `req.risk_level`**); recorded risk reconciled at submit; risk-0→allow auto-executes (strict catalog-risk-0 re-gate, adversarial-pinned); unknown-type→`PolicyDenied`. `preview_class`/`executor`/`idempotency_formula` NAMED → realized 2.3. LESSON §19. **[3.2-part-2/043 — the agent-mutation family, CONTRACT 0.22]** a separate `AGENT_MUTATION_ACTION_TYPES` const (`agent.bash`/`agent.file_edit`/`agent.file_read`/`agent.mcp_tool`; read-only=risk-0, mutating=risk-2 conservative) + a new **`ExecutorKind::Adjudication`** (wire `adjudication`) — the Claude interception's typed Actions, **adjudication-only** (terminate at the verdict, NO daemon executor — the `CatalogExecutor::execute` Adjudication arm is a fail-safe → Failed). **MVP_ACTION_TYPES stays 22** (the agent family is machine-internal). LESSON §26. **[4.0b-1, CONTRACT 0.25.0; MVP 22→24]** `session.create` **risk-2→0** + NEW `session.kill` **risk-0** + NEW `session.profile_change` **risk-2** (away-ruled risk-0 audited auto-allow; **5 pins** [SessionStarted-audit/§15-#8-profile-at-start/profile_change-approval-gated/risk-0-NARROW/UI-IPC-only] + a `RISK0_AUTO_EXECUTE_ALLOWLIST` gating ALL risk-0 auto-execute, allowlist↔catalog biconditional pinned). The `SessionExecutor` (`ExecutorKind::Session`) drives the 4.0a supervisor + emits `SessionStarted{execution_profile_id}` **in-txn** (`ExecutionOutcome.emitted_events` atomic with `ActionSucceeded`, fail-closed); §15 #8 binding recorded-at-start; mechanism-built, NO production caller (binding-condition BY CONSTRUCTION → 4.0b-2). **[4.0b-R1a, daemon-internal, NO CONTRACT change]** `CatalogExecutor` = a per-namespace **registry** (`register()`-or-stub dispatch, incremental); the `requires_resource_refs` precondition + the **INV-SEC-1 `Adjudication` guard run BEFORE dispatch** (execute-time, security-PASS); 4.0b-2 registers `SessionExecutor`, edges P5/P7 register theirs. LESSON §20 (refined). **[4.0b-2 L1, CONTRACT 0.26.0→0.27.0]** 3 new `AGENT_MUTATION_ACTION_TYPES` realize the user-ruled d.2 split tool-policy (the live interception's tool-allowlist): `agent.todo_write`(risk-0, the **LONE auto-allow** on `RISK0_AUTO_EXECUTE_ALLOWLIST` — provably no FS/git/external/exfil, adjudication-only) · `agent.web_fetch`+`agent.web_search`(risk-2, **approval-gated** — the exfil dimension). **The CATALOG IS the explicit enumerated allowlist** — an unclassified tool → `CoverageGap`→Deny (fail-closed); adversarial-tested. Additive (MVP-22 untouched, the 043 precedent); the 043 route-EVERY-tool-through-the-Gateway audit-before-verdict invariant preserved. The live wiring (the `decision_sink`/`intercept` transport + the real launch) = 4.0b-2 L2. LESSON §26 (extends). **[4.0b-ui1, CONTRACT 0.27.0→0.28.0; MVP 24→27]** (`9848513`) the cross-track ui-6.3e freeze: 3 new `git.*` catalog types — `git.stage_hunk`/`git.unstage_hunk` **risk-2** · `git.discard_hunk` **risk-3** (all `executor_kind=Git`, `requires_resource_refs`, bodies = R1-A registry stubs until Phase-5) — + a new **`standing_grant_eligible: bool`** catalog field (the §6.2 **non-standing-grant floor**): `git.discard_hunk`=false (destructive/irreversible → ALWAYS a per-action human approval, never standing-granted) + `workflow.command.invoke`=false (the risk-4 floor unified onto ONE mechanism); the plan approve-all exclusion generalized to `risk==Level4 OR !standing_grant_eligible` (BOTH disjuncts kept; a drift-invariant test pins every risk-4 ⇒ !standing_grant_eligible). `git.discard_hunk` `preview_class=Diff` (shows the exact hunk lost). **Resource-ref hunk encoding (FROZEN convention):** `ResourceRef{resource_type=File, id="{worktree_id}\x1f{file}\x1f{old_start},{old_lines},{new_start},{new_lines}"}` — `\x1f` unit-separator delimiter; hunk-precise (distinct hunks → distinct `NaturalResourceRef` keys, no false dedup); read↔mutate consistent (the ui forms it from `get_diff`'s `Hunk`, the Phase-5 git executor parses it back; a pathological `\x1f`-in-filename is a Phase-5 escape/validate hardening; conformance-tested when the real executor+ui land). security-reviewer CLEAR. LESSON §32. **[edges-029 / Wave-C, CONTRACT 0.32.0→0.33.0; MVP 27→28]** `integration.connect` **risk-2** + a new **`ExecutorKind::Integration`** (wire `integration`; a keychain-write/registration namespace, NOT a network sync) — the `IntegrationExecutor` REGISTERS an integration connection (emits `IntegrationConnectionRegistered`); **REGISTRATION-ONLY** (§15 + LESSON §20 forced — a risk-2 action executes off the §15-redacted durable row, so a token in `inputs_json` is masked-by-execute-time → the mutator STRUCTURALLY can't carry it; inputs `{provider, keychain_ref POINTER, account?}`, NO token; §15 #4 holds BY CONSTRUCTION; the token→keychain WRITE is a deferred non-Gateway/HITL mechanism). `standing_grant_eligible=**false**` (`entry_no_standing_grant` — the §6.2 **credential floor**: an authorization-establishing action is never bulk-grantable, the `git.discard_hunk` precedent; **USER-ratification pending** [keep false / relax to true]). INV-SEC-1 no-bypass (holds only `Box<dyn IdGen>`, emits via `emitted_events`, risk-2→approval, NOT on the risk-0 allowlist); defense-in-depth secret-shaped-ref reject via the canonical `PrefixRedactor` read-only. **MVP_ACTION_TYPES 27→28.** security-reviewer full invariant PASS. LESSON §49. **[D5b-2 / P4.6, CONTRACT 0.37→0.38; `76b7f4a`; MVP 28→29]** `github.sync_reviews` **risk-1** (`ExecutorKind::Github`, `requires_resource_refs` [a Repo ref], `idempotency=None` [a re-sync RE-RUNS, not dedup], `side_effect_applied=false` [a clean read rollback], `standing_grant_eligible=true`) — a read-that-emits action: the `GithubExecutor` fetches a PR's reviews (octocrab `list_reviews` via the seam, §46 captured-Handle+block_on+timeout) + emits one `ReviewSynced` per review; failure → `GithubSyncFailed`. **risk-1 is the github-READ precedent** (a NETWORK read is NOT risk-0 auto-execute — an untrusted proposer hammering the GitHub API is a rate-limit/exposure vector — but below the risk-2 github WRITES; INV-SEC-1 no-bypass [emits only via the gateway txn-B], security full PASS). LESSONS §46/§55. **[083/P4.7 — CONTRACT 0.45.0; MVP 31→32]** `integration.set_live_writes` **risk-2** (`ExecutorKind::Integration`, `requires_resource_refs=false` — `connection_id` input-carried, the `integration.connect` precedent; `standing_grant_eligible=`**`false`** — the §6.2 credential/live-enablement floor, LESSON §49) — the live-writes governance flip. The `IntegrationExecutor` arm validates `connection_id` references a REGISTERED connection via a `ConnectionLookup` seam (→ **fail-closed on unknown**, the §62 resolve-on-unknown precedent) + emits `IntegrationLiveWritesSet`. **UI/IPC-requester-only** (`CatalogPolicy::decide`→Deny BEFORE risk for Agent/Brain/Pack/System — the §60/§61 github-mutation gate generalized). **MVP_ACTION_TYPES 31→32** (the prior D9 `github.merge_pr` + D10 `github.submit_review` brought the prose-trailed count 29→31; verified against `shared/src/catalog.rs`). LESSON §64. **[085/P5.3b — CONTRACT 0.46.0; MVP 32→33]** `profile.set_keychain_ref` **risk-2** (NEW **`ExecutorKind::Profile`** [wire `profile`; profiles are their own domain — the `ExecutorKind::Integration` precedent], `requires_resource_refs` [the audited profile id], `entry_no_standing_grant` — the §6.2 credential floor, LESSON §49/§32) — the proposer-reachable AUDITED pointer-record (the P5.3a 🔴 carry-forward; `register_profile` is never proposer-called). The `ProfileExecutor` records the `keychain_ref` POINTER onto the canonical `execution_profiles` row via a pipeline-txn-B `profiles::apply_secret_set` (forbidden-#3 forces the row UPDATE onto the write-actor; §63 ref-from-audited-id; §62 rebuild-preserves). **UI/IPC-requester-only** (the §60/§64 deny-before-risk gate extended to `profile.*`). The keychain SECRET write itself is the NON-Gateway peer-authed `profile.set_secret` IPC trigger (the §49/§64 substrate split — its own cat-1 commit). **MVP_ACTION_TYPES 32→33.** LESSON §65. **[W1-git-stage `b3728c6` / W1-git-discard `4e38559` — the 3 `git.*` executor BODIES now LIVE]** the R1-A registry stubs are realized: `git.stage_hunk`/`unstage_hunk` (stage/unstage the INDEX) + `git.discard_hunk` (reverse-apply to the WORKING TREE). The risk-3 destructive `git.discard_hunk` body adds a `displayed_hunk_sha256` content-hash **verify-before-destroy** — the UI sends it over the open `inputs` map (**FROZEN canonicalization, daemon↔UI:** lowercase-hex sha256 over the hunk body, per-line `origin-byte + DiffLine.content` verbatim, `@@` header excluded; vector `2980a50…c39ee6`); the daemon hashes the EXACT bytes it will apply → verify==destroy by construction (a partially-staged hunk fails closed). **Contract-NEUTRAL** (no catalog/resource-ref change; the hash rides `inputs` → no CONTRACT bump, no UI regen). LESSON §72/§73. |
| Event-type registry (`EventTypeRegistry`) | §7.1 / Appendix A | **`SessionStarted` = first entry (1.2, `shared/src/events.rs`); CONTRACT_VERSION → 0.8.0. `DeviceRegistered` + `LocalRunnerRegistered` added 1.6a-L3** (System-actor cold-start registration via the write-actor; `actor_type=System`; reserved `SYSTEM_WORKSPACE_ID` sentinel); **CONTRACT_VERSION → 0.12.0**. **`AuditIntegrityViolation`{seq,reason} added 1.6c** (§17 degradable-replay quarantine record; System-actor; **CONTRACT_VERSION → 0.13.0**). **`SensitiveOutputRedacted`{original_event_type,reason,detector} added 1.7** (§15 redactor quarantine-divert record; content-free; sensitivity Internal; **CONTRACT_VERSION → 0.14.0**; 3-way verify @ 0.14.0). **The 8-type Gateway `ActionExecution*` family added 2.1b** (`ActionRequested`{action_type,risk_level,requester_type}/`ActionApprovalRequested`{approval_id}/`ActionApproved`{approval_id,decided_by?}/`ActionDenied`{approval_id,reason}/`ActionExpired`{approval_id}/`ActionStarted`{}/`ActionSucceeded`{}/`ActionFailed`{error}; identity on the envelope columns, payload=delta; emitted ONLY by the Gateway via the write-actor append; **CONTRACT_VERSION → 0.16.0**; bundle + 3-way @ 0.16.0). **`TelemetrySampled`{sample(`TelemetrySample`),model?,execution_profile_id?} added 3.1** (§9.1/§7.1/§18 — the harness telemetry OBSERVATION event; identity on the envelope columns, payload = the sample + the model/exec-profile rollup dims the envelope lacks; **non-mutation → written via the single write-actor, NOT the Gateway** (LESSON §10/§23 family); feeds `proj_usage_ledger`; live emission lands with the real adapters 3.2-part-2/043); **CONTRACT_VERSION → 0.20.0**. **`TerminalProcessExited`{terminal_id,exit_code?,signal?} added 3.4 L1** (§6.4/§9/§17 — the PTY-death OBSERVATION event; emitted by the terminal host on detected child exit from the OS exit status, never output-parsed [#9]; **non-mutation → write-actor, NOT the Gateway** (LESSON §10/§23 family); the §17 SessionFailed/lease cascade that consumes it is P4); **CONTRACT_VERSION → 0.21.0**. **`ActionDenied.approval_id` relaxed `String`→`Option<String>` at 3.2-part-2/043 L5** (the agent-mutation policy-deny **record-then-deny**, A1: a policy-deny has no approval → `None`; a human-deny still `Some(appr_)`; the projector keys `denied` off the event_type, not the payload — breaks no consumer; daemon-internal/un-consumed/additive-tolerant); **CONTRACT_VERSION → 0.23.0**. **The 11-type Phase-5/7 wiring family + `Provider`(github\|linear) added 4.0b-R1b (cross-track edges-R1; CONTRACT_VERSION → 0.26.0; `shared/`-only, NO daemon emission — edges' P5/P7 executors emit via `EmittedEvent` later)** — **P5.1** `ProjectRescanned`{is_git,repo_root?,remote_url?,branch?,detached,is_dirty,workflow_pack,cc_crew,plan_file?,brain,scanned_at} (one coarse event; **`remote_url` §15 = strip-userinfo-`user:token@`-at-source + redactor-backstop, rule #5** — a generic URL password has no prefix, outside the recall envelope LESSON §13; enforcement = edges P5.1 emission); **P5.2** `WorktreeCreated`{worktree_id,path,branch_name,base_branch?}/`BranchCreated`{branch_name,base?}/`WorktreeMerged`{}/`WorktreePrunable`{}/`WorktreeDeleted`{}/`WorktreeLocked`{} (the 4 overlay transitions = **empty-payload**, identity on the envelope, the `ActionStarted{}` precedent; `WorktreeStatusRefreshed` is NOT an event — live-read cache); **P7.1** `PullRequestSynced`{pr_number:u64,status(§5.1 `PullRequest`),branch,base,mergeable?,checks_summary?,pr_checked_at}/`IntegrationConnectionRegistered`{connection_id,provider,keychain_ref,account?} (**`keychain_ref` = non-secret POINTER only, rule #4**)/`GithubSyncFailed`+`LinearSyncFailed`{provider,reason,failed_at} (**non-auth variant only**; `reason` = a redaction-safe STRUCTURAL class-name; `provider` redundant-but-spec'd kept per §17 distinct-types + the edges uniform-read; the `auth_expired` variant DEFERRED to edges P7.1 [0.5b gate now LIFTED, needs §17/INV-SEC re-review]). `pr_number: u64` (non-negative external natural — positivity structural at parse). Snapshots §2.5-seam; 3-way verify 37==37==37 @ 0.26.0. Per-type payload schema, contract-first per §5.0 (schemars → schema → Zod/Pydantic); the golden-log tests bind. **`SessionRecovered`{mode(`ResumeMode`),replayed_event_count:u64,execution_profile_id?} added 4.1b-1** (`1e68f20`; the §8.1 daemon-restart recovery signal — §11.4 resumed-vs-replayed-vs-reattached; **non-mutation OBSERVATION via the write-actor, NOT the Gateway** — the System-actor recovery [Q1=(a): the daemon re-materializing its OWN approved session; LESSON §10/§23/§38]; profile from the committed `SessionStarted`); **CONTRACT_VERSION → 0.31.0** (additive; 3-way verify 40==40==40). **`SessionFailed {}` (empty-payload) added 4.2** (`463abc0`; the §17 supervised-child-death signal — a supervisor `(id, Failed)` reap → the §11.4 restart affordance via `proj_session.status=Failed`; **non-mutation OBSERVATION via the write-actor, NOT the Gateway** [System-actor death-observation, LESSON §10/§23/§40]; **EMPTY-payload** — the "why" is the correlated `TerminalProcessExited`, `reason` additive-later; the cascade arms deferred to Phase 5/7 / session-leases / 3.3); **CONTRACT_VERSION → 0.32.0** (additive; 3-way verify 40==40==40). **`ReviewSynced`{review_id:u64,pr_number:u64,reviewer,state(`ReviewState`),submitted_at?,body?,review_synced_at} added D5b-1** (`90cadd2`; the §7.1 structured-PR-review event feeding `proj_review`; emitted by the `github.sync_reviews` Gateway action via `emitted_events` [D5b-2]; `body` = free-form user text, rides the §15 redaction gate; **CONTRACT_VERSION → 0.37.0**). **`PullRequestMerged`{pr_number:u64, merge_commit_sha:Option<String>, merged_at} added D9/P4.7** (`e544544`; `deny_unknown_fields`; the §7.1 PR-merge audit event → folds `proj_pull_request.status`→terminal `merged`; emitted by the `github.merge_pr` **cat-1** Gateway action via `emitted_events` [the §15-gated path]; `merged_at` = daemon-clock, reasons-structural §15; **CONTRACT_VERSION → 0.41.0**, 3-way verify 41==41==41). **`ReviewSubmitted`{review_id:u64, pr_number:u64, reviewer, state(`ReviewState`), body:Option<String>, submitted_at, commit_id:Option<String>} added D10/P4.7** (`de201c0`; `deny_unknown_fields`, reuses the frozen `ReviewState` value enum [NO new enum]; the §7.1 PR-review-submit audit event → folds `proj_review` (upsert by review_id, mirror `ReviewSynced`); emitted by the `github.submit_review` **cat-1** Gateway action via `emitted_events`; `body`/`submitted_at` from the create-response, `body` rides the §15 redaction gate; **CONTRACT_VERSION → 0.42.0**, 3-way verify 41==41==41). **`ExecutionProfileRegistered`{execution_profile_id, workspace_id, provider, harness, model?, account_alias?, keychain_ref?, usage_policy_json?, status, created_at} added P5.3a** (the System-actor registration event for the §2.8 `execution_profiles` durable registry — Option B user-ruled 2026-06-20; `actor_type=System` [LESSON §10 family]; `keychain_ref` = nullable §15 #4 POINTER, NO secret; the audit TRAIL while the canonical ROW is the source of truth — LESSON §62; **CONTRACT_VERSION → 0.43.0**, 3-way verify @ 0.43.0 [41 enums]; cold-start now emits 3 registration events: device + local_runner + execution_profile). **`PullRequestSynced` gains `head_sha: Option<String>` at P4.7/081** (the UI's anti-race SHA-pin source; sourced ONLY from `pr.head.sha` in `extract_pr_signals` [authoritative GET, never user/UI]; **CONTRACT_VERSION → 0.44.0**, MIGRATION_17; the daemon anti-race stays the LIVE 409 — `github_write.rs` untouched; LESSON §53-ext). **`IntegrationLiveWritesSet`{connection_id, enabled} added 083/P4.7** (`266162e`; `deny_unknown_fields`; the live-writes governance event — **NO secret**, a connection_id + bool only; emitted by the `integration.set_live_writes` Gateway action via `emitted_events`; folds `proj_integration_connection.live_writes_enabled` derive-from-event default OFF [LESSON §17/§64], **MIGRATION_18, `SUPPORTED_USER_VERSION` 17→18**; the validate-connection-REGISTERED-else-fail-closed gate is the §62 precedent); **CONTRACT_VERSION → 0.45.0** (additive; 3-way verify = the off-loop /phase-exit gate, LESSON §29 — flagged not-run-this-slice). **`ProfileSecretSet`{execution_profile_id} + `SessionProfileChanged`{session_id, execution_profile_id} added 085/P5.3b** (`1f6f0c0`; `deny_unknown_fields`; the §15 #8 / §7.2 profile-secret events — both **gateway-emitted** (the requester actor via the envelope, NOT System-actor — contrast P5.3a's cold-start `ExecutionProfileRegistered`). `ProfileSecretSet` carries the id ONLY — the daemon-DERIVED `nexusops/profile/<ULID>` pointer is RE-DERIVED for the canonical-row UPDATE, NOT carried, to dodge the §15 JSON-value redactor false-positive on a path-wrapped ULID (LESSON §65; the row holds the full pointer via `apply_secret_set`); `SessionProfileChanged` records the §15 #8 no-account-hop swap. **CONTRACT_VERSION → 0.46.0** (additive; NO migration — `keychain_ref` is the existing nullable column). **`ProjectRescanned` gains `name: Option<String>` added 092/P4.7** (`9bef853`; the friendly project name = basename of the scan path `req.inputs["path"]` — trailing-slash/root/empty/`.`/`..`→None — emitted by `ProjectExecutor`; **additive-OPTIONAL** so existing logged events replay as `None`, rebuild-safe; the `ActivityProjector` folds it into `proj_project_activity.name` [**MIGRATION_19**, `SUPPORTED_USER_VERSION` 18→19] — the LESSON 68 "registry-only event must also feed a UI-readable projection" surfacing; **CONTRACT_VERSION → 0.47.0** (additive; schema regen; 3-way verify 42 enums @0.47.0; the paired UI regen-to-0.47, LESSON 69). LESSON 68/69. Accretes per phase as event types land. |
| §9.1 HarnessAdapter trait + normalized types | §9.1 / Appendix A | **Frozen 3.1** (`shared/src/harness.rs`; **CONTRACT 0.20.0**; `feat(harness)`; §2.5-seam snapshot-pinned). **SHARED (frozen):** the normalized DATA types the UI/Brain consume — `TelemetrySample{tokens_in,tokens_out,context_pct:Option,cost_estimate,metric_quality}`, `MetricQuality`(exact\|estimated\|unavailable), `TranscriptRef{path,hash,is_in_place}`, `HarnessCapabilities` (the 10 PRD-HARN-5 fields), `TelemetrySampled` event (row above). `NormalizedStatus` = the frozen `status::Session`(17) **re-exported** (adapters map INTO it; no forked enum). **DAEMON-INTERNAL (unfrozen — `daemon/src/harness/mod.rs`):** the `HarnessAdapter` trait `{launch,stream_status,intercept_mutation,read_transcript,telemetry_heartbeat,resume,capabilities}` (sync, object-safe `Box<dyn>` per the `ActionExecutor` seam — 3.2/3.3 async-ify the drive loop), `MutationIntercept{tool,params,decision_sink}` (non-serializable sink), the **per-harness mutation-coverage matrix** (verbatim §9.1 / O-13), `ResumeResult` (**FROZEN 4.1a — CONTRACT 0.29.0**; `ResumeResult{mode,replayed_event_count}` + `ResumeMode`(4)/`RecoveryState`(3) now in `shared/` — the Appendix-A **Survival/recovery schema** row + the §8.1 ladder + the `decide_resume` classifier; LESSON §36), `FakeHarness`. LESSON §23. **[3.2-part-1 reshape, 2026-06-12 — LESSON §25]** the trait reshaped for the real adapter: **`Send+Sync`→`Send`** + **`launch(&self)`→`launch(&mut self)`** (the real `ClaudeAdapter` owns a `Box<dyn Pty>` [Send-only] + a mutable status machine; security-SAFE). **`ClaudeAdapter` (PTY-primary, cat-4 resolved) observe path LANDED** (`daemon/src/harness/claude/{mod,status}.rs`; brief 042): the O-13 `launch()` spec (default-mode-only, no `-p`, no bg subagents, generated 0600 settings never the user's `~/.claude/`, fail-closed-on-serialize — the #10 enforcement surface) + the pure `derive_status(prev,ClaudeSignal)→Session` (STRUCTURED signals, never PTY bytes #9; fail-closed exit, R-9 terminal-sink) + `read_transcript()`; a **`PtySpawner`** object-safe factory (`terminal/mod.rs`) produces the `Box<dyn Pty>` `launch()` injects. **[3.2-part-2/043 interception LANDED — LESSON §26]** the `MutationIntercept`→Gateway INV-SEC-1 chokepoint is AS-BUILT (5 layers, security PASS every layer; the core adversarially-verified): a Claude `PreToolUse` hook → daemon **hook-receiver** → an **adjudication-only `ActionRequest`** through the EXISTING Gateway (audit-BEFORE-verdict; the daemon ADJUDICATES+AUDITS, the agent executes the Allow'd tool — NO daemon executor, code-enforced) + the O-13 coverage-gap compensation (DirectToolUse-default-mode→Adjudicate; MCP/Task/bg/non-default→**DENY** via the hook-independent `permissions.deny` + the cheap-closure fail-closed); realized as the §6.3 agent-mutation family + `ExecutorKind::Adjudication` (CONTRACT 0.22) + `ActionDenied.approval_id`→`Option` record-then-deny (CONTRACT 0.23). **[3.2-part-2/044 telemetry LANDED — LESSON §27]** the `telemetry_heartbeat` emission path is AS-BUILT (NON-safety; CONTRACT held 0.20.0): the adapter emits per-heartbeat token/cost DELTAS (not cumulative — `proj_usage_ledger` SUMs) + context-% as a gauge (projector-MAX'd), as a `TelemetrySampled` OBSERVATION event via an injected `TelemetryEventSink` (the `TerminalEventSink` precedent; write-actor, not Gateway); daemon-Clock UTC-Z `occurred_at` gates `bucket_day`. **3.2 (the Claude adapter) is now COMPLETE** (observe + interception + telemetry). The production sink-bind + periodic pump + trait async-ify + live transcript/statusLine ingestion, and `resume()` + the production drive caller (the runtime `AgentMutationPolicy` swap + `route_intercept`→live-hook wiring) = P4 (its own security pass). **[IMPLEMENTED 4.0c (`c0a935d`) — the production telemetry path is LIVE]** the `WriteHandle`-backed `WriteActorTelemetrySink` (constructed in `runtime/`, injected into the `ClaudeAdapter` via an **opaque `TelemetrySinkFactory`** so `session/` stays cat-1 import-grep-clean) + a **periodic pump** (the `SessionActor` telemetry tick → `HarnessAdapter::poll_telemetry()` → `ClaudeAdapter` drains an injectable `UsageSource` → the 044 `push_usage` emit-on-ingest, unchanged) + the **cumulative-baseline high-water-mark clamp** in `push_usage`'s storage (fixes the dip-then-climb over-count; the pure `telemetry_sample` keeps its per-delta floor). Emit = fire-and-forget `WriteHandle::try_append_observation` (`try_send`, soft-degrade, never back-pressures the writer — LESSONS §9/§30; an OBSERVATION event, NOT the Gateway — ZERO `Action*`, INV-SEC-1 untouched). The launcher↔write-actor bootstrap cycle breaks via a deferred `OnceLock` handle slot. **NON-safety; NO CONTRACT bump** (`TelemetrySample` frozen 0.20.0). The **live `UsageSource` ingress** (hook-receiver/statusLine feed) stays the **P4** follow-on (the pump ticks but emits nothing in prod until then) + the real `workspace_id`/`project_id` threading = **P5**. LESSON §35. |
| MVP projections (10 `proj_*`) + `object_refs` + `projection_offsets` | §7 / §7.2 / DATA_MODEL §2.2–§2.4 | **Implemented 1.2** (`daemon/src/projections/`). Folded **in-band in the event-commit txn**; offsets advance same-txn (never ahead of rows); **status columns bind the frozen §5.1 enums** (reject-unknown). Projector BODIES = 4 Phase-1-feedable (Session/ProjectGraph/AuditTrail/ProjectActivity); 6 re-homed to producing phases (all 10 DDLs present). LESSONS §4. **[②-mini 4.0b-ui2 — CONTRACT 0.30.0; `4e9579d`+`8c4b948`]** `proj_approval_queue` (+ the `approvals` row) gain a `policy_decision_json` column (MIGRATION_9; the §15-redacted authoritative `PolicyDecision` persisted at approval-open, sibling-read rebuild-safe) + the **FIRST frozen projection-row `ApprovalQueueRow`** (`shared/src/projections.rs`; 14 typed fields incl. `risk_level: RiskLevel`/`status: Approval`/`policy_decision: Option<PolicyDecision>`; `deny_unknown_fields`) served **typed/fail-closed** (`get_projection(ApprovalQueue)`→`read_approval_queue_typed`) — no loose-JSON on the human-approval path; unblocks the ui-L2 card. LESSONS §16/§17/§37. **[P7.2 — CONTRACT 0.34.0; `e748874`]** the **2nd frozen projection-row `PullRequestRow`** (`shared/src/projections.rs`; 9 BASIC fields incl. `status: PullRequest`/`pr_number: Option<u64>`; `deny_unknown_fields`; `updated_at_seq` omitted) served typed/fail-closed (`get_projection(PullRequest)`→`read_pull_request_typed`) from `proj_pull_request` (NO migration — the edges merge already projects it); `mergeable`/`checks_summary` NOT projected → a basic-now+SPREAD freeze. Unblocks the ui PR Review Workspace (§11.2/§7.2). LESSON §37. **[D5a/P4.6 — CONTRACT 0.36.0; `fa67c11`]** the SPREAD CONSUMED: `proj_pull_request` + `PullRequestRow` both gain `mergeable: Option<bool>` + `checks_summary: Option<String>` (MIGRATION_13 ALTER-only, `SUPPORTED_USER_VERSION` 12→13; the projector folds from `PullRequestSynced`; LOCKSTEP — the fail-closed typed serve forces column+fold+row-field+serve+bump atomic). `mergeable` = the FIRST bool projection column → INTEGER(0/1)→bool coercion in `read_pull_request_typed` (the contract stays pure `Option<bool>`). LESSONS §53. **[D2/4.4 — CONTRACT 0.35.0; `7fc8bc7`]** the **3rd frozen projection-row `SessionRow`** (`shared/src/projections.rs`; 10 wire fields incl. `status: Session`/`resume_mode: Option<ResumeMode>`/`project_id` non-Option; `deny_unknown_fields`) served typed/fail-closed (`get_projection("Session")`→`read_session_typed`, a `SESSION_ROW_WIRE_FIELDS` retain-whitelist for the 22-col table) — + the `SessionProjector` now folds **`SessionRecovered`** (previously DEAD; mirrors `SessionFailed`, derive-from-event rebuild-safe; MIGRATION_12 ALTER-only, `SUPPORTED_USER_VERSION` 11→12). The §8.1/§11.4 recovery display. LESSON §37. **[D3/4.5 — CONTRACT-neutral; `deltas_for_append` `writer.rs`]** the subscribe-delta source now publishes a **row-less Session `Upsert` nudge on every `proj_session`-mutating event** — `SessionFailed`/`SessionRecovered` added alongside `SessionStarted` (keyed on the same `::EVENT_TYPE` consts the projector uses) so a live subscriber re-reads after a death/recovery (the §11.4 cards no longer go stale); a set-agreement guard test pins the delta-source↔projector two-list agreement. `ProjectionDelta` frozen @0.11.0 (no `shared/` change). D4 generalizes to the other-4 projections. LESSON §51. **[D4a/4.5 — CONTRACT-neutral; `deltas_for_append` `writer.rs`]** the observation-path generalization: a `proj_usage_ledger` Upsert nudge on `TelemetrySampled` + a **BLANKET** `proj_audit_trail` Upsert on EVERY event (the audit projector folds every event), both `id: None` (payload-agnostic; the subscriber re-reads the aggregate/paged projection). A single append now yields a `Vec<ProjectionDelta>` → tests drain-and-find. Selective projections (usage) keep a §51 set-guard + a negative arm; the blanket projection (audit) needs none. `proj_audit_trail` is **cross-path** — its gateway-fed half (Action* events) lands in **D4b** (the gateway accumulator). LESSON §51 (extended). **[D4b/4.5 — CONTRACT-neutral; the gateway-emitted-event delta COMPLETENESS SWEEP + the D3 SessionStarted production-gap FIX]** the gateway `emitted_events` path published NO delta → every gateway-emitted-event-fed projection was orphaned (the **FINDING**: production `SessionStarted` is gateway-emitted, NOT `Command::Append`, so D3's `deltas_for_append` Session arm was DEAD in prod — L2 session card stale on creation; pre-existing since 4.0b-1; lead-ruled fix-the-class). **Fix:** a SHARED `projections::deltas_for_event(event_type, &EventDeltaIds)` (in `projections/` — below both runtime+gateway, avoids a layer inversion) called by BOTH `deltas_for_append` (envelope ids) AND the gateway emitted-events loop (`pipeline.rs` `execute()` Ok(ack) arm, POST-commit, fire-and-forget — txn-B/INV-SEC-1 unperturbed, security-CLEAR). **Sweep (bounded by `ProjectionName`):** `SessionStarted`→Session+ProjectActivity+ProjectGraph · `WorktreeCreated`→Worktree · `PullRequestSynced`→PullRequest (id=`{repo_id}#{pr_number}` = the row PK) · every gateway cmd→AuditTrail blanket (`publish_after_commit`). **🔴 Future-CONTRACT flag:** `proj_project`/`proj_repository`/`proj_integration_connection` have NO `ProjectionName` variant → not subscribe-able → audit-blanket only; adding their subscribe-names is a CONTRACT change (out of scope; recorded). A revert-check pins the production-path tests (`tests/session_executor.rs`). LESSONS §51/§52. **[D5b-1/P4.6 — CONTRACT 0.36→0.37; `90cadd2`]** a NEW review projection vertical: `ReviewSynced` event + a frozen `ReviewState` **VALUE** enum (5 states, reject-unknown, NOT `status_machine!` — a review has no lifecycle) + the **4th frozen projection-row `ReviewRow`** (review_id PK + state/reviewer/body?/…) served typed/fail-closed (`get_projection(Review)`→`read_review_typed`) + **`ProjectionName::Review`** (a closed-set §6.1 add = the CONTRACT cost of subscribe-ability; partially closes the D4b future-CONTRACT flag) + `proj_review` (MIGRATION_14, `SUPPORTED_USER_VERSION` 13→14, REBUILD_TABLES) + the `ReviewProjector` (review_id key, repo_id sibling-read) + the `deltas_for_event` Review nudge. §15 `body` rides the redaction gate (security-CLEAR). Fixture-fed; producer = D5b-2. LESSONS §54. **[D6/P4.7 — CONTRACT 0.39.0; the PR-card diff-stats enrichment]** `proj_pull_request` + `PullRequestRow` + `PullRequestSynced` each gain `additions`/`deletions`/`changed_files`/`commits: Option<u64>` (MIGRATION_15 ALTER-only, `SUPPORTED_USER_VERSION` 14→15; the `PullRequestProjector` folds them None→NULL rebuild-safe; `read_pull_request_typed` serves them generically — SQLite-INTEGER→JSON-number→`Option<u64>`, NO coercion, unlike D5a's bool). Producer-capture in `extract_pr_signals` (GET-only octocrab PR fields; `fetch_pr_signals` already GETs → no new API call) threaded into the `github.create_pr` emit. **Producer-gating:** populated on `create_pr` now; existing synced PRs await the deferred PR-status-refresh sync (same gating as D5a mergeable/checks). The D5a/LESSON §53 LOCKSTEP. **[D9/P4.7 — CONTRACT 0.41.0; `c1cd0be`]** the `PullRequestProjector` folds **`PullRequestMerged`**→`proj_pull_request.status='merged'` for the `(repo_id, pr_number)` row, **derived from the EVENT TYPE** (rebuild-safe, LESSON §17; other cols untouched; rebuild-equivalence + UPDATE-0-rows healthy-no-op tested) + `deltas_for_event` maps `PullRequestMerged`→`ProjectionName::PullRequest` (id `{repo_id}#{pr_number}`), the nudge pinned via the real gateway execute (production-path, LESSON §51/§52). The terminal `Merged` lands via the cat-1 `github.merge_pr` write. LESSON §60. **[D10/P4.7 — CONTRACT 0.42.0; `cbae5aa`]** the `ReviewProjector` folds **`ReviewSubmitted`**→`proj_review` (upsert by `review_id`, the SAME fold as `ReviewSynced` — a submit over an existing synced review UPDATEs; derive-from-event rebuild-safe, LESSON §17) + `deltas_for_event` maps `ReviewSubmitted`→`ProjectionName::Review` (nudge pinned via the real gateway execute, LESSON §51/§52). The user-submitted review appears in `proj_review` immediately (not only post-`sync_reviews`). The cat-1 `github.submit_review` write. LESSON §61. **[P4.7/081 — CONTRACT 0.44.0; head_sha]** `proj_pull_request` + `PullRequestRow` + `PullRequestSynced` gain `head_sha: Option<String>` (MIGRATION_17 ALTER, `SUPPORTED_USER_VERSION` 16→17; folded None→NULL rebuild-safe; served as a TEXT **String passthrough — NO coercion**, the LESSON §53 contrast to `mergeable`'s INTEGER→bool). The UI's anti-race SHA-pin SOURCE (only from `pr.head.sha` in `extract_pr_signals`, authoritative GET; the daemon anti-race stays the LIVE 409, `github_write.rs` untouched). LESSON §53 (extended). |
| **`execution_profiles` durable registry (§2.8)** | §5.1 / §7.2 / §15 #8 / DATA_MODEL §2.8 / Appendix A | **Implemented P5.3a (Option B, user-ruled 2026-06-20; CONTRACT 0.43.0; MIGRATION_16, `SUPPORTED_USER_VERSION` 15→16).** The daemon's FIRST canonical OBJECT durable registry — the `execution_profiles` row IS the source of truth (written DIRECTLY via the LESSON §16 dual-gate {redact-row-before-INSERT + `ExecutionProfileRegistered` append} over `gateway_txn`; **NOT in `REBUILD_TABLES`** — a `rebuild()` does NOT clear it; the event is the audit trail, the row is canonical — contrast the LESSON §48 `proj_*` projections). Cold-start seeds ONE default (System-actor register-if-absent, LESSON §10; idempotent across restarts). `SessionExecutor::resolve_profile` is registry-backed + **fail-closed on unknown** (no silent mint — §15 #8 no-account-hop) via an injected `ProfileLookup` seam (prod `SqliteProfileLookup`, read-only WAL). `keychain_ref` POINTER-only (§15 #4; the secret WRITE + runtime re-derivation [§7.2] + a proposer-reachable register-Action = 5.3b/cat-1, its own pass). `prof_` id (LESSON §14). LESSON §62. **[085/P5.3b AS-BUILT — CONTRACT 0.46.0]** the secret WRITE + §7.2 runtime re-derivation are now LIVE: `profile.set_secret` (peer-authed INBOUND keychain write, cat-1) populates `keychain_ref` via the `profile.set_keychain_ref` audited Gateway action (`ProfileExecutor`; the row UPDATE rides pipeline txn-B via `apply_secret_set`, atomic with `ProfileSecretSet`, fail-closed; §63 ref-from-audited-id; **§62 rebuild PRESERVES `keychain_ref`** — the canonical row isn't event-replayed). Runtime `status` re-derives recompute-on-read (pure `derive_runtime_status`; the cold-start keychain self-test → `misconfigured`; the persisted `status` stays config, §7.2; DORMANT-until-ui-reader). `session.profile_change` executor body LIVE (the swap records `SessionProfileChanged`, fail-closed-on-unknown, §15 #8 no-account-hop). LESSON §65. **[W1-prof — the registry is now UI-READABLE]** the §6.1 `get_execution_profiles` read RPC (IPC row above) serves a typed secret-free `ProfileRow` over read-only WAL — the `keychain_ref` POINTER NEVER served (`has_credential: bool` only, §15 #4); CONTRACT 0.48.0, `1cbb712`. LESSON §70. |
| `outbox` (transactional outbox) | §7 / §12 / §17 / DATA_MODEL §2.5 | **Implemented 1.3** (`daemon/src/eventstore/outbox.rs`). **Daemon-internal** — `out_` id is **NOT** one of the 22 frozen contract IDs; `destination`/`status` enums are **not** a `shared/` contract (UI/Brain don't read the outbox). Rows written in the event-commit txn; the **§15 *sync* sink** (payload from the already-redacted event); at-least-once drainer (Tokio spawn 1.6-wired). LESSONS §5. |
| `Lease` (lease locks + fencing) | §5.1 / §7.2 / §17 / Appendix A | **Implemented 1.4** (`daemon/src/locks/`). One row per `(resource_id, lease_kind)`: `owner_id, fencing_token, acquired_at, heartbeat_at, expires_at`. **Daemon-internal** — no `proj_lease`, no `lease_` among the 22 frozen IDs, **not** one of the 10 §5.1 status machines (UI/Brain don't read `leases`) → **no `shared/` surface, no CONTRACT_VERSION bump** (outbox-analogous). Fencing token = **persisted monotonic high-water mark** (acquire/reclaim mints +1 under `BEGIN IMMEDIATE`; release/reap keep it; survives restart). **Authority = a LIVE lease** (Option B, §17): `validate_held` = owner + token-match + `expires_at > now`; "stale" = expired **OR** superseded → `fencing_conflict` (safety rule #6, never auto-resolved). pidlock single-instance via a std advisory file lock; reaper `reap_once` (Tokio spawn 1.6-wired). LESSONS §6. |
| IPC `GatewayPort` wire contract | §6.1 / §6.4 / §5.0 / Appendix A | **Implemented 1.5** (`daemon/src/ipc/` + `shared/src/ipc.rs`). **Cross-language `shared/` contract** (the ui's gateway-client consumes it) — HelloFrame/HelloAck/VersionSkewError + `IpcErrorCode` (7: incl. **`protocol_error`**) + the closed `ProjectionName` enum (`UsageLedger`,…) + RpcRequest/RpcResponse + **`ServerFrame`** (`rpc_response`\|`subscription_push`\|**`terminal_output`** [3.4 — the reserved Terminal slot filled]) + ProjectionDelta/DeltaKind/SubscribeParams + Capabilities → **CONTRACT_VERSION 0.11.0** (byte-diff gate + 3-way verify). **[IMPLEMENTED 3.4 — CONTRACT 0.21.0, §2.5-seam] the §6.4 Terminal Channel frames** — `TerminalOutputFrame{terminal_id,seq,data}` · `TerminalInputFrame{terminal_id,data}` · `TerminalControlFrame{terminal_id,kind:TerminalControlKind(pause\|resume)}` + `ServerFrame::TerminalOutput`; **encoding = JSON-base64 MVP** (`data` base64 over the unchanged 4-byte-len+JSON codec, LESSON §7; binary fast-path → 3.5-with-throughput); `terminal_id` = a daemon runtime handle (not a frozen-22 IdKind, the `subscription_id` precedent). LESSON §24. **Two version axes:** `CONTRACT_VERSION` (schema/codegen) vs **`protocol_version`** (wire handshake; `SUPPORTED_PROTOCOL_RANGE {min:1,max:1}` daemon-authored). §6.1 **READ surface live** (get_projection/subscribe/get_capabilities); mutation methods → P2. `getpeereid` peer-auth (rule #7); read methods over **read-only WAL** (single-writer). **Runtime LANDED 1.6b** (`main.rs` `#[tokio::main]` + write-actor + accept-loop + broadcast subscribe-source; reads + subscribe-SOURCE live) **+ subscribe-SERVE push LANDED 1.6d** (`93f70a5`; the broadcast Receiver→socket push, a `next_push_action` classifier [matching→Push, other→Skip, Lagged→**Stop=close-on-lag**, Closed→Stop], a **structurally-dedicated single-writer** subscribe connection [post-ack main loop read-only, sole push thread/conn]; closes the ui live-subscribe path). LESSONS §7 + §9 + §12. **[IMPLEMENTED 4.0b-ui1 — CONTRACT 0.28.0]** (`9848513`) the §6.1 **`get_diff(worktree_id, file) → DiffResult{hunks}`** read RPC — request-on-demand git2-LIVE read (read-only; NOT a `Diff` projection — diffs are large/transient, the §7.2 worktree-live-read precedent); resolves `worktree_id→proj_worktree.path` over read-only WAL (typed `NotFound` until P5.2 populates proj_worktree) → the new `daemon/src/git/` read-only module (`read_diff`; NEW git2 dep, read-only). Frozen types: `DiffResult{hunks:Vec<Hunk>}` · `Hunk{header,old_start,old_lines,new_start,new_lines,lines:Vec<DiffLine>}` · `DiffLine{kind:DiffLineKind(context\|added\|removed),content}` · `GetDiffParams{worktree_id,file}`. **`IpcErrorCode` 9→10: + `NotFound`** (the read-not-found case; `precondition_stale`/`internal_error` were semantically wrong). §2.5-seam snapshots; 3-way verify 38==38==38 @ 0.28.0. LESSON §33. **[IMPLEMENTED 4.0b-2-F2 — §6.4/§10 availability]** (`4a7572a`) the **intercept-wait permit-class split** — a reserved sub-bound on the accept pool so concurrent 5-min approval-waits can't starve the UI: a 2nd `Semaphore(wait_cap = MAX_CONNECTIONS − RESERVED = 48 of 64)`; a parked intercept-wait holds it alongside its general permit → at most `wait_cap` general permits are ever held by waits → `RESERVED=16` general slots ALWAYS free for UI `approve`/`deny` / reads / subscribe. **Wait-class exhaustion → fail-closed Deny** (`INTERCEPT_SATURATED_REASON`, distinct from the timeout Deny; the wait is NEVER entered — no park, **no bypass**, fail-SAFE). Factored as a PURE `intercept_verdict_with_wait_class` seam + `InterceptWaitClass` wrapper (deterministically unit-testable, the LESSONS §9/§12 pure-classifier family); the live handler delegates with the real wait class (/wired: wait_cap=48 in production). **Daemon-internal (accept-loop/IPC) — no `shared/` surface, no CONTRACT bump.** security-reviewer CLEAR. LESSON §34. **[D7/P4.7 — CONTRACT 0.39.0→0.40.0; `924ebcc`]** the §6.1 **`get_pr_diff(repo_id, pr_number, file?) → DiffResult`** read RPC — the remote-PR head-vs-base code-diff (the §11.2 Review tab; `get_diff` is worktree-scoped, no PR↔worktree FK). `+GetPrDiffParams`; `DiffResult`/`Hunk`/`DiffLine` REUSED. **The FIRST network read in the IPC read layer** — `Arc<dyn GithubReadClient>` + a captured `Handle` threaded through `serve_connection→dispatch` (LESSON §46 `block_on`+MANDATORY-timeout, fake-seamed); repo_id→owner/repo resolved off the exact `(repo_id,pr_number)` PR row → `proj_repository.remote_url`; unresolvable→`NotFound`. §15 no-leak STRUCTURAL (`GithubReadError`↛fieldless `IpcErrorCode`; userinfo-strip §44); read-only (INV-SEC-1 untouched). Mechanism-live, fetch-GATED (production client wired UNAUTHENTICATED → typed error until the deferred per-repo keychain auth + its MANDATORY security-re-review gate land). LESSON §59. **[083/P4.7 — CONTRACT 0.44.0→0.45.0; `4ca090a` freeze / `b61cad4` trigger]** the §6.1 **`connect_via_gh`** IPC method + 3 wire types (`ConnectViaGhParams`/`ConnectViaGhResult`/`ConnectViaGhStatus`, **NO token field**) — the "Connect via gh" TRIGGER: the daemon reads `gh auth token` (transient/`Zeroizing`) → keychain (per-account `keychain_ref`), **`getpeereid`-peer-authed FIRST** (§15 #7). **NOT a Gateway mutation** (the §49 non-Gateway credential-write; audit = the subsequent `integration.connect`) — security-ruled acceptable (NOT a fork/Finding). **The D7/§59 fetch-gate is now CLOSED:** the live keychain-backed octocrab clients (`octocrab_for(owner)` over the §63-audited owner, replacing `Octocrab::default()`) consume `resolve_authed_token` behind the `live_writes_enabled` toggle (default OFF → fail-closed unauth → existing `AuthFailed`); the in-arc live-path security re-review CLEAR. LESSON §64. **[W1-prof/WAVE-1 — CONTRACT 0.45.0→0.48.0; `1cbb712`]** the §6.1 **`get_execution_profiles → GetExecutionProfilesResult{profiles: Vec<ProfileRow>}`** read RPC — a request-on-demand read of the §2.8 `execution_profiles` registry over read-only WAL (the `get_diff` read-RPC precedent; NOT a projection — the registry has no `ProjectionName`, LESSON §62). **`ProfileRow{execution_profile_id, provider, harness, model?, account_alias?, status(ExecutionProfile), is_default, has_credential}`** + `GetExecutionProfilesResult` in `shared/src/ipc.rs` — served typed/fail-closed (LESSON §37; a corrupt row → whole-read `InternalError`). **§15 #4: the `keychain_ref` POINTER is NEVER served** — `ProfileRow` has no field for it; the reader derives `has_credential: bool` and never serves/logs/errors-with the pointer (security-reviewer CLEAR; pinned no-pointer-in-row + `deny_unknown_fields`-rejects-a-leaked-keychain_ref). `is_default` from the first `ExecutionProfileRegistered` event (cold-start provenance, no migration), SOFT-degrades to no-default if unresolvable (LESSON §30 vs row-data fail-closed §37, within one reader). §2.5-seam snapshot + 3-way verify 42==42==42 @ 0.48.0; paired UI regen = ui-085 (LESSON §69). LESSON §70. |
| `quarantine` (degradable replay, §17) | §17 / DATA_MODEL | **Implemented 1.6c** (`daemon/src/eventstore/`). **Daemon-internal** — MIGRATION_6 `quarantine` table (`seq` PK, structural reason, `detected_at`, `audit_emitted`); SUPPORTED_USER_VERSION 5→6; **no `shared/` surface, no CONTRACT_VERSION bump** (outbox/leases-analogous). The replay read path (shared by `catch_up_replay` + `rebuild`) quarantines a corrupt/unredacted row (raw spine untouched) + marks the offset degraded + continues (Option C, daemon starts); `ON CONFLICT(seq) DO NOTHING` preserves the record across rebuild/re-read. Drives the `AuditIntegrityViolation` event (the CONTRACT-bearing half — EventTypeRegistry row above). LESSONS §11. |

<!-- Populated as contract models land. The four 0.5 rows above are the frozen foundation (the serial neck). -->

## Module organization

<!-- ▼ EXAMPLE BLOCK [id=module-layout]: module layout + layer dependency rule. Replace with the project's real directory tree and import-direction DAG. ▼ -->

```
daemon/
  model/          # shared typed domain (Action, Event, RiskClass, projections schema) — depended on by everything
  eventstore/     # append-only event log writer (the single write-actor) + read-only WAL readers
  projections/    # derived read models rebuilt from the event log
  locks/          # resource/lease arbitration for the persistence core
  gateway/        # the Action Gateway — orchestrates + risk-classifies + approves; SOLE mutator
  harness/        # agent process supervision (PTY/SDK lifecycle) — submits intents to gateway
  session/        # opt-3 session lifecycle — SessionSupervisor + SessionActor drive the harness adapters; submit intents to gateway (NEVER writes the DB)
  terminal/       # portable-pty session handling — submits intents to gateway
  git/            # git2 read-only introspection + git-CLI actions — submits intents to gateway
  integrations/   # octocrab (GitHub) + external adapters — submit intents to gateway
  brainclient/    # Project Brain proposal client — submits intents to gateway
  workflow/       # multi-step plays composed of gateway actions
  ipc/            # exposes GatewayPort over a Unix domain socket (UDS)
```

Layer dependency direction (top depends on bottom, never reverse):

```
ipc                                            (edge: exposes GatewayPort over UDS)
harness · terminal · git · integrations ·      (executor/adapter edges:
  brainclient · workflow                        submit intents to gateway; NEVER write the DB)
        │  (propose intents)
        ▼
gateway                                         (sole mutator: orchestrates, risk-classifies, approves)
        │
        ▼
eventstore · projections · locks               (persistence core: single write-actor + read-only readers)
        │
        ▼
model                                           (shared typed domain — depended on by ALL, depends on none)
```

**No edge module writes the DB directly** — harness/terminal/git/integrations/brainclient/workflow/ipc are edges that submit intents to `gateway`; only the `eventstore` write-actor (driven by `gateway`) touches `nexusops.db` for writes. Cross-cutting layers can be imported from anywhere. Enforce the import direction mechanically with a test where possible — the test *is* the spec for the rule.

<!-- ▲ END EXAMPLE BLOCK [id=module-layout] ▲ -->

## Subagents

See `.claude/agents/README.md` for the canonical inventory + integration points.

<!-- ▼ EXAMPLE BLOCK [id=area-subagent-candidates]: area-specific subagent candidates — list candidates that would earn their keep specifically in this area (e.g. an ABI/types syncer for a frontend area, a Pyth/feed verifier for a contracts area). Build only on real friction. ▼ -->

<!-- ▲ END EXAMPLE BLOCK [id=area-subagent-candidates] ▲ -->

## Lessons logged from prior sessions

The full prose for each lesson lives in `daemon/LESSONS.md`. This index is the compact orientation surface.

**Lesson numbers are stable IDs** — once assigned, they don't change. New lessons get the next sequential number. `/session-end` proposes additions when it detects them; the user approves before the entry is written and a row is added here.

Lessons start at §1.

| # | Date | Topic | Rule (one-liner) |
|--:|---|---|---|
| 1 | 2026-06-07 | [Broken cargo shims](LESSONS.md#1) | `rustup default stable` won't fix *broken* (vs missing) `~/.cargo/bin` proxies — repoint each to the `rustup` binary, then verify with plain shims |
| 2 | 2026-06-07 | [Wire value is the contract; SoT = §5.0](LESSONS.md#2) | Freeze the snake_case wire value (not the identifier; pin with a round-trip test); author every contract in Rust `shared/` per §5.0, regenerating the published schema + consumers — never hand-write a consumer or invert the authority |
| 3 | 2026-06-07 | [Single-write-actor + atomic seq](LESSONS.md#3) | One writable `Connection` (write-actor); all else read-only WAL (`open_read_only`); assign canonical `seq` via `SELECT max+1`+`INSERT` in one `BEGIN IMMEDIATE` txn (atomic, not borrow-checker-only); inject `Clock`+`IdGen` for deterministic replay |
| 4 | 2026-06-07 | [In-band projections + offsets + recovery](LESSONS.md#4) | Fold projections in-band in the event-commit txn (after the redaction gate), each under its own SAVEPOINT (logic error → degrade+skip; Db error → fail closed); advance offsets in the same txn (never ahead of rows); recover via catch-up replay (strict `seq>last_seq`, no-op on current) + full rebuild (truncate a const derived-table list, replay-all, byte-equivalent, raw events untouched); bind status columns to the frozen §5.1 enums |
| 5 | 2026-06-08 | [Transactional outbox + §15 sync-sink](LESSONS.md#5) | Route external side-effects through the outbox: write rows in the event-commit txn (recorded-iff-intended); §15 sync sink (payload from the already-redacted event, filter-only; rebuild never re-emits); deliver at-least-once via deterministic `drain_once` (in_flight claim-before-deliver, reset-on-open, backoff + retryable/terminal + bounded dead-letter, idempotent consumers); UTC-`Z` timestamps for the lexical due-compare |
| 6 | 2026-06-08 | [Cross-restart leases + live-lease fencing + pidlock](LESSONS.md#6) | `leases` row per `(resource_id, lease_kind)` with a **persisted monotonic fencing high-water mark** (minted +1 on acquire/reclaim under `BEGIN IMMEDIATE`; release/reap keep it; survives restart); **authority is a LIVE lease** — `validate_held` = owner + token-match + `expires_at > now`, so "stale" = expired OR superseded (safety rule #6; gateway heartbeats long actions); single-instance via a **std advisory file lock** (OS-fd-held, auto-released on death, immune to PID reuse — never `kill(pid,0)`); daemon-internal (no `shared/` surface, no CONTRACT_VERSION bump) |
| 7 | 2026-06-08 | [UDS GatewayPort transport](LESSONS.md#7) | UDS server: 4-byte-BE-len+JSON framing (oversized rejected pre-alloc; `MAX_FRAME_SIZE` 8 MiB); **`getpeereid()` peer-auth FIRST** (reject uid≠daemon-uid; NOT `SO_PEERCRED`; isolated unsafe + fail-closed; test the *enforcement* path via an injectable `peer_uid`); handshake-first; **two version axes** (`CONTRACT_VERSION` schema/codegen vs `protocol_version` wire-skew `{1,1}` daemon-authored); LOCKED-error-set gap → escalate (`protocol_error`); frame-mux = internally-tagged `ServerFrame` over the **unchanged** codec (Terminal encoding = Phase-3 measured); read-only-WAL reads + closed-enum table map (no injection); ship the mechanism, wire the runtime source at 1.6 |
| 8 | 2026-06-08 | [Cold-start bootstrap ordering + FS-failure determinism](LESSONS.md#8) | Cold-start = `create_dir_all` → `PidLock` (first gate, strictly before any DB write — forbidden #3) → `EventStore::open` (compose the already-shipped migrate/backup-rollback/floor/replay; don't re-implement); the §16 version matrix enforces ONLY the DB `user_version` floor at bootstrap (rest = report-only/handshake/deferred); pin an FS-failure path via a pure `on_migration_failure` classifier + inline unit (preserve the original error context; distinct typed `RestoreFailed`; no `-1` sentinels) — not a forced disk fault |
| 9 | 2026-06-08 | [Daemon runtime — write-actor + publish-after-commit + unlink-before-bind](LESSONS.md#9) | Runtime = single write-actor (dedicated **blocking thread** + mpsc + a Clone+Send `WriteHandle`; reads always `open_read_only`, never the actor — forbidden #3); drainer/reaper = async interval tasks sending **bounded** commands (`MissedTickBehavior::Delay`); live subscribe = broadcast publishing **AFTER commit**, a lagging subscriber resyncs + NEVER back-pressures the writer (no wire frame / no CONTRACT bump); UDS bind = **unlink-before-bind** (pidlock makes a stale socket safe) + `spawn_blocking(serve_connection)` under a semaphore cap (pin rejection AND permit-release); `main.rs` `#[tokio::main]` closes the 1.3/1.4/1.5 runtime-deferral chain |
| 10 | 2026-06-10 | [Daemon self-registration = a System-actor event, not a Gateway Action](LESSONS.md#10) | The daemon's own cold-start lifecycle identity (desktop-host `Device` register-if-absent + per-start `LocalRunner`) is a **System-actor system event** (`actor_type=System`, `SYSTEM_WORKSPACE_ID` sentinel) appended via the write-actor — through the §15 redaction gate + projector fold (audited + immutable) but **NOT policy-gated** (INV-SEC-1 governs untrusted proposer intents, not the substrate those Actions record into; the Gateway is Phase 2). Desktop ids (`dev_`/`lr_`) mint via a `desktop_minted_id!` sibling keyed off `DesktopObjectKind` — frozen-22 `IdKind` untouched (`from_prefix==None`); object_refs payload-sourced (rebuild-safe). Binding §16 beats a stale frozen §5.3 row → escalate the reconcile (Device → MVP-live, Option A). |
| 11 | 2026-06-10 | [Degradable catch-up replay — quarantine + audit-integrity, Option C](LESSONS.md#11) | The replay read path (shared by `catch_up_replay` + `rebuild`) **degrades instead of aborting `open()`** on a bad event row so a single corrupt row can't stop the daemon (§17 Option C): corrupt (reconstruction-fail) **or** unredacted → **quarantined** (daemon-internal `quarantine` table, raw spine untouched, offset degraded, row skipped) + a loud `AuditIntegrityViolation` System-actor event per newly-quarantined seq (emitted post-`open` from the caller; structural/content-free reason §15; exactly-once across restart **and** rebuild via `audit_emitted` + idempotency_key + `ON CONFLICT(seq) DO NOTHING`); unknown `event_version` → **degraded-only** (Degraded≠Quarantined — forward-compat, not integrity). Healthy logs replay byte-identically (rebuild-equivalence preserved). |
| 12 | 2026-06-10 | [Socket subscribe-SERVE — close-on-lag resync + dedicated single-writer connection](LESSONS.md#12) | A push-stream over a shared socket must be **structurally single-writer**: make a subscribe connection **terminal/dedicated** (write the ack on the main thread, spawn the *one* push thread only on ack-success, main loop read-only-until-EOF after) — concurrent ack/RPC writes + push writes otherwise interleave + corrupt the frame stream (a HIGH). Mint the broadcast receiver **before** the ack (don't miss a just-after-ack delta). Drive the per-recv decision through a **pure `next_push_action` classifier** (Push/Skip/Stop) so it's unit-testable without timing flakiness; **`Lagged → Stop → close the connection`** IS LESSON §9's resync trigger (client reconnects + re-`get_projection`) — never silently continue (`ProjectionDelta` has no seq → a gapped stream is undetectable → silent divergence). Subscriber never back-pressures the writer (forbidden #3); `getpeereid` stays first (rule #7, post-auth/post-ack). |
| 13 | 2026-06-10 | [§15 redactor — entropy fallback + mask-in-place; quarantine-divert as the MVP-unreached net](LESSONS.md#13) | Secret detection = high-recall prefix set + Shannon-entropy on `KEY=value` (base64/quote-aware value span, **sub-run-scored** to resist padding-dilution) + bare-run masking — all **mask-in-place** (fail-closed, deterministic/golden-log-safe; engine `prefix-entropy-v2`). The quarantine→`SensitiveOutputRedacted` **divert** (original event diverted, content-free record in its place — namespaced `divert-{k}` dedup, sensitivity-reclassified Internal, fail-closed no-recurse guard) is the §15 "can't-safely-bound → divert" net, **wired + `ForcesQuarantine`-tested but MVP-unreached** (the real redactor masks everything) — §17-AIV-analogous. Prefer **mask-in-place over divert** when the span is boundable (a false divert loses the whole event; a false mask loses only the blob). A statistical detector has an inherent recall envelope; the §15 **gate** invariant holds regardless; recall acceptance is a human gate; the primary control is keychain-refs-only (rule #5). **[2.0-SEC — engine `v2→v3`, human-ruled Option B]** measured the envelope against a synthetic labeled corpus (recall_catchable 1.0 / precision 1.0 / FP 0.0; floor/ceiling regression-pinned, ratchets up only) + extended it: a JSON-`"key":"value"` value pass at the KV bar (sub-run-scored), guarded by a **value-shape ID-allowlist** (git-SHA/ULID/UUID), closes residual (a) for **≥20ch** at zero precision cost; residuals (b) hex≈SHA (irreducible by tuning), (c) adversarial-split, (<20ch-a) accepted-and-owned. Measure the detector against a corpus (never commit real secrets); extend recall via a different mechanism, not a looser threshold. |
| 14 | 2026-06-11 | [Binding contract = Appendix A + DDL; drafts are origin](LESSONS.md#14) | The freeze authority is `ARCHITECTURE.md` Appendix A + the `DATA_MODEL` DDL — `docs/domains/*.md` drafts are origin/rationale (usually richer). DDL-anchored rows are exhaustive (draft extras reconciled out); an abbreviated standalone row → consult the draft + freeze the load-bearing fields it abbreviated (required/irreducible→freeze e.g. `EvidenceRef.label`; id-resolvable/convenience→defer). Defer shape-owned-by-a-later-slice fields as typed-additive-later (non-breaking pre-consumer). FLAG every drop at Step-9 (reconcile the Appendix-A row + deferred-→owning-slice marker) — never silent; load-bearing drop = Finding. `accepted: process discipline (Step-9 cross-doc flag; the schema-snapshot test catches a dropped/renamed field)` |
| 15 | 2026-06-11 | [schemars/§5.0 freeze emission gotchas](LESSONS.md#15) | Three silent traps freezing a new model surface: (1) `$defs` keyed by **bare type-name** → same-named types across modules COLLIDE + one is silently dropped → disambiguate with `#[schemars(rename)]` (value-preserving `$def`-NAME change = frozen-reconcile, like `prj_→eprj_`); (2) the 3-way verify reflects **string enums only** → emit integer/format types as **bounded scalars** (`{integer,min,max}`/`{string,format}`) not enum-arrays, and lift a discriminated union to a **named top-level enum** (`{kind,…}`); (3) snapshot-pinned models serialize optionals as explicit `null` (**no `skip_serializing_if`**) for a stable field-name set. Recurs every freeze (2.2/3.1/8.1). `pin: shared/tests/contract.rs (schema-snapshot + 3-way verify)` |
| 16 | 2026-06-11 | [The INV-SEC-1 chokepoint](LESSONS.md#16) | The mutation chokepoint = a per-transition **atomic {durable-row write + authoritative-event append via the §15 gate} on the single write-actor, fail-closed** (event-write fail → rollback → no row/ack/side-effect). Compose via an extracted `append_in_txn` (public `append` = byte-identical wrapper; §15 gate strictly before INSERT; exactly ONE `INSERT INTO events`; events emit ONLY via the Gateway append handle — §14 "no-executor-except-via-pipeline + event-per-mutation" test). §15 is a **dual-gate**: events via append-gate AND **registry-row payload (`inputs_json`/`resource_refs_json`) via redact-before-INSERT** (rule #3/#4 — all caller-supplied row payload; untrusted proposers). Injectable stubs (policy=require-approval-for-all-risk-blind→2.2, executor=no-side-effect→2.3, fencing→2.4); execute phase = distinct seam from approve (real executors run OFF the write-actor); R-9 fail-closed legal-edge guards (execution only via the approval/policy-allow gates). `pin: daemon/tests/gateway.rs (INV-SEC-1 + fail-closed + redaction); security-reviewer every Gateway slice` |
| 17 | 2026-06-11 | [Projector sibling-read — immutable from registry, mutable from event](LESSONS.md#17) | A projector may read **IMMUTABLE** fields from a sibling durable-registry row in the same txn (the `object_refs`→graph precedent generalized to the gateway's `action_requests`/`approvals`/`action_plans`) — but MUST derive every **MUTABLE** field (the approval `status`) from the **EVENT TYPE**, never the row's current value: registry tables aren't in `REBUILD_TABLES`, so `rebuild()` reads them at FINAL state → mutable-from-row stamps the final state onto every historical row (non-deterministic). Gateway-event subscribe-deltas (mutation commands bypass `Command::Append`): thread a `ProjectionDelta` accumulator through the signature-stable-wrapped methods + publish post-commit (forbidden #3 / LESSON §9), keyed by the touched id. `pin: daemon/tests/gateway.rs (rebuild-equivalence) + tests/runtime.rs (delta)` |
| 18 | 2026-06-11 | [Bundled ActionPlan = durable grouping, not a new event type](LESSONS.md#18) | A bundled `ActionPlan` is a durable GROUPING (a thin `action_plans` table + a nullable `action_requests.plan_id` FK), NOT a new event type — `submit_action_plan` = ONE whole-plan atomic txn (fail-closed; per-step `Action*` events + the `plan_id` join reconstruct it; a plan-scoped event ties via `correlation_id=plan_id`, no envelope column). Approvals per `approval_mode`: ApproveAll = ONE plan-level approval (`action_request_id` NULL, `scope=Plan`) over non-critical + a per-step approval for each critical-4 step (**"critical never in approve-all"** — §6.2/§11.5 safety pin). The plan-level approval generalizes the single-action `approvals`/`proj_approval_queue` to `action_request_id` NULLABLE + `plan_id` — **conformance to the frozen §6.2 `Approval.action_request_id: Option`**, not a new contract. Reject a deferred/policy-assigned mode (Blocked) fail-closed at submit (no phantom `awaiting_approval`); Mixed → StepByStep. `pin: daemon/tests/gateway_plan.rs (whole-plan atomicity + critical-exclusion + Blocked-reject)` |
| 19 | 2026-06-11 | [Catalog-authoritative risk + the defense-in-depth auto-execute re-gate](LESSONS.md#19) | Risk is **catalog-authoritative**, never the requester's `req.risk_level` — `CatalogPolicy::decide` reads `catalog::lookup`; the recorded risk is overwritten to catalog risk at submit (audit integrity); an uncatalogued type → `PolicyDenied` (fail-closed, never default-allow). The FIRST no-human-approval path (risk-0 `allow` auto-queue) is gated strictly on `allow` AND catalog-risk-0 with a **defense-in-depth re-gate** (re-verifies catalog-risk-0 even when the policy returns `allow`, so a policy bug can't open a non-zero auto-queue — pinned by an adversarial `AllowAllPolicy`). EVERY risk-keyed safety gate (§11.5 approve-all critical-exclusion, the auto-execute gate) keys off catalog risk; an uncatalogued plan step rejects the whole plan (#11). `pin: daemon/tests/policy.rs (no-non-zero-auto-queue + adversarial AllowAllPolicy + catalog-critical-exclusion + reject-uncatalogued)` |
| 20 | 2026-06-11 | [Gateway executor/preview/idempotency framework — seams not side effects; idempotency = one-way hash of RAW inputs (§15)](LESSONS.md#20) | 2.3 realizes the §6.3 catalog's named `preview_class`/`executor`/`idempotency_formula` as a **FRAMEWORK** — the `ActionExecutor` trait (validate/preview/execute/optional-rollback) + `CatalogExecutor` dispatch by `ExecutorKind` to **side-effect-free per-namespace stubs** (real adapters land Phase 3/5/7/8; the trait is the seam, not the side effect; INV-SEC-1 holds — executor only via the 3 gated seams, grep-confirmed). **§15 precedent (lead-ruled A):** `idempotency_key` = one-way SHA-256 of RAW inputs (by-design bypasses the `inputs_json` redactor — dedup needs raw determinism; rule-#4-safe = fingerprint-not-secret + keychain-refs-only; catalog-derived not requester-supplied; C/HMAC hardening if data leaves the trust boundary). **§7.2 split:** auto-execute runs off in-memory inputs; approve runs off the durable redacted row (canonical). Preview-impossible escalates the **envelope** risk only (never the gating risk); 2.3 all-impossible is a per-phase transient. `pin: daemon/tests/executor.rs (idempotency + preview + executor/validate/§7.2-spy/reach); security-reviewer every Gateway slice` |
| 21 | 2026-06-11 | [The §17 failure-mode safety capstone — deterministic logic + seams; the cfg-gated fault-injection Cargo feature](LESSONS.md#21) | 2.4 builds the §17 safety behaviors as **deterministic LOGIC + seams** (fault-injection hook + fake `Clock` + fake `PreconditionOracle`; real git2/octocrab/session re-reads → Phase 3/5/7/8). The **§14 fault hook = a cfg-gated `fault-injection` Cargo FEATURE** (NOT `cfg(test)` — integration tests link the lib without it; the self-dev-dependency idiom) compiled OUT + un-armable in release (`nm`/`cargo tree` verified). **Fail-closed:** split `execute()` into txn-A (Executing+Started, COMMITS) → executor off-actor → txn-B (terminal); txn-B fail → stays `executing` (→crash-reconcile), never acked succeeded; side-effect-applied+txn-B-fail → txn-C `ActionPartiallySucceeded` (the loud alert) → txn-C-fail → `AuditWriteFailed`; UNIFORM/risk-agnostic. **Record-then-throw** (fencing L3 / stale-precondition L4): the `ActionFailed` COMMITS before the typed `Err` → distinct §6.4 cards (`fencing_conflict` never-auto-resolved vs `precondition_stale` re-approvable; never collapse). **Crash-reconcile** (`reconcile_orphans` in cold_start, plan_id-agnostic): `queued`(never ran)→CLEAR idempotency_key (safe re-submit); `executing`(maybe-applied)→`unknown_outcome`+KEEP key (double-run protect). `pin: daemon/tests/recovery.rs (fail-closed/fencing/stale-precondition/crash-reconcile/Q6-key); security-reviewer every Gateway slice` |
| 22 | 2026-06-12 | [Perf-budget benchmarks — AS-BUILT path + own cadence](LESSONS.md#22) | A perf benchmark drives the REAL production entry (not a Phase-0 raw-store proxy) under a defined load model (realistic-gated + saturating-reported); calibrate the CI guard on the as-built measurement + margin (tighter than the SLO), **retire proxy numbers**, match the throughput unit to the guard's basis (events/s == commits/s, NOT submits/s); run at the bench's OWN cadence (`[[bench]] harness=false` → /phase-exit + nightly, never `cargo test --workspace`); a §18-budget re-baseline is a **load-bearing escalation** (2.5 = USER-RULED Option A). `pin: daemon/benches/event_write.rs; cadence: /phase-exit perf row + nightly.yml` |
| 23 | 2026-06-12 | [Telemetry = observation event (not Gateway); §2.5-seam freeze splits shared data from the daemon SEAM](LESSONS.md#23) | Telemetry/usage/lifecycle the daemon WITNESSES is a non-mutation OBSERVATION event (System/adapter-actor → write-actor → §15 redactor → projector), NOT a Gateway Action — INV-SEC-1 routes state **mutations** (LESSON §10 family: Device/LocalRunner/AuditIntegrityViolation); a projection that needs it (`proj_usage_ledger`) is fed by such an event (`TelemetrySampled`). A §2.5-seam freeze: freeze ONLY the settled consumer-facing DATA types (`TelemetrySample`/`MetricQuality`/`TranscriptRef`/`HarnessCapabilities`, one CONTRACT bump; `NormalizedStatus`=`Session` re-export, not a fork), keep the trait/SEAM daemon-internal+sync+UNFROZEN (`Box<dyn>` executor-seam precedent; 3.2/3.3 async-ify), DEFER a later-phase-owned type (`ResumeResult`→P4 survival). Read fold semantics off DDL names (`*_max`=MAX gauge, plain=SUM); free-form dim LAST in a composite key. `pin: shared/tests/contract.rs (§9.1 snapshot + 0.20.0) + daemon/tests/projections.rs` |
| 24 | 2026-06-12 | [The embedded Terminal Channel — PTY display-only byte-pipe + OS exit signal (#9); backpressure = pure watermark classifier + OS-pipe backpressure](LESSONS.md#24) | The PTY host is a byte pipe + an OS **exit signal**, **never a status source** (#9, structurally grep-pinned scoped to the pure module); `TerminalProcessExited` comes from waitpid/EOF, not output-parsing, and is a non-mutation OBSERVATION event (write-actor via an injected sink, NOT the Gateway — LESSON §10/§23). Backpressure = a **pure `next_terminal_action` watermark classifier** (LESSON §12 reuse, timing-free) + **OS-pipe backpressure** (stop reading the child at the high watermark → bounded buffer, no OOM; DoS-bound `buffer ≤ high+chunk`), ~30fps batch via injected `Clock`; **never an unbounded queue, never back-pressures the write-actor** (forbidden #3 / LESSON §9). The §6.4 frames ride the **unchanged codec** (JSON-base64 MVP; `data` base64; binary fast-path → 3.5-measured); `terminal_id` = a runtime handle, NOT a 23rd LOCKED-22 `IdKind` (subscription_id precedent); untrusted ingress frames reject-unknown (internally-tagged `ServerFrame` → reject-unknown on the bare member, the `ProjectionDelta` precedent). `pin: daemon/tests/terminal.rs + shared/tests/contract.rs (§6.4 frames + 0.21.0); security-reviewer every terminal/harness slice` |
| 25 | 2026-06-12 | [The PTY-primary harness adapter — status = pure (prev,signal)→Session fold (never PTY bytes #9); the O-13 launch spec IS the #10 enforcement surface (fail-closed); a PtySpawner factory; the trait reshapes freely](LESSONS.md#25) | A harness adapter derives status as a **pure `(prev,ClaudeSignal)→Session` fold** over a typed STRUCTURED-signal enum (hook/app-server events), **never PTY bytes** (#9 — the signature + a scoped grep-pin); fail-closed exit (`(0,None)→Completed`, else→Failed); R-9 terminal-sink (no resurrection). Keep signal *ingestion* I/O a deferred seam (`push_signal`; sync pure core, LESSON §9). The **O-13 `launch()` spec IS the #10 enforcement surface** (interactive PTY, `--permission-mode default` only, no `-p`, no bg subagents — pinned *absent-by-construction*) and **fails CLOSED** (settings-serialize fail → `launch=Failed`, never a hook-less/uninterceptable session); generate per-session settings (0600), NEVER mutate the user's `~/.claude/settings.json`. Inject a **`PtySpawner` object-safe factory** for the non-deterministic spawn (the `Pty` handle isn't a spawner). The daemon-internal trait **reshapes freely** (Send+Sync→Send, launch &mut self — only §2.5-seam DATA types are frozen; security-SAFE); the production drive caller is a named Phase-4 deferral, interception+telemetry = 043. `pin: daemon/tests/claude_adapter.rs (O-13 spec + derive_status + #9 pin + fixture); security-reviewer every harness slice (#9/#10)` |
| 26 | 2026-06-12 | [The harness INV-SEC-1 interception — an agent mutation is a typed adjudication-only Action through the ONE Gateway pipeline (audit-BEFORE-verdict); an un-interceptable path is a bypass → deny+compensate](LESSONS.md#26) | Route an intercepted agent mutation (`PreToolUse` hook → receiver → `MutationIntercept`) as a **typed, risk-classified, adjudication-only `ActionRequest` through the EXISTING Gateway pipeline** (Q1=A, one chokepoint — not a parallel path; a §6.3 `agent.*` family + `ExecutorKind::Adjudication`). **Adjudication-only:** the daemon ADJUDICATES+AUDITS, the agent executes the Allow'd tool → the ActionRequest TERMINATES at the verdict (PolicyDecided/Approved, never Queued/executing — path stops earlier on existing edges, NO new state/R-9 edge), no-execute **code-enforced** (terminal + `CatalogExecutor::execute` fail-safe→Failed + RecordingExecutor-never-called). **Audit-BEFORE-verdict (§15 #5):** Allow only when submit/approve `Ok` (event committed); any Err / audit-fault → Deny (`FaultPoint::AuditEventWrite`). **Q2-conservative:** read-only auto-allows (≠ mutation); every mutating tool requires approval by default (no auto-allow until a user standing-grant); deny-rules a redundant layer (false-neg → require-approval, never silent allow). **Coverage-gap = the crux:** any un-interceptable path (MCP/Task/bg-subagent #27203 / non-default) is an INV-SEC-1 BYPASS → **DENY mechanically** (hook-INDEPENDENT `permissions.deny` — a default-mode hook-MISS fails OPEN); the residual CLOSES via defense-in-depth (per-call hook + NO `permissions.allow` + non-interactive empty-cache-block + 5s timeout). **A1 record-then-deny:** the agent deny path COMMITS `ActionRequested`+`ActionDenied{None,reason}` (`is_agent_mutation`-gated; `ActionDenied.approval_id`→`Option` — honest, a policy-deny has no approval; never a phantom). Generalizes to Codex 3.3; the live drive-loop wiring = P4 (own security pass). `pin: daemon/tests/claude_intercept.rs (adjudication-no-executor #13 + audit-before-verdict + coverage-matrix + deny-rule-no-false-allow + record-then-deny + cheap-closure); security-reviewer EVERY layer (cat-1)` |
| 27 | 2026-06-12 | [Harness telemetry — emit DELTAS not cumulative; context-% a gauge; via an injected sink; UTC-Z gates bucket_day](LESSONS.md#27) | A harness telemetry adapter emits per-heartbeat token/cost DELTAS (`current−last`, first=full-from-zero; the `proj_usage_ledger` SUMs → cumulative over-counts) + context-% as a CURRENT gauge (the projector MAXes it), never a delta; a non-mutation `TelemetrySampled` OBSERVATION event (write-actor not Gateway, LESSON §23) via an injected sink (the `TerminalEventSink` precedent); daemon-Clock UTC-Z `occurred_at` gates `bucket_day` (fail-closed non-Z, LESSON §5); defensively parse the external upstream (tolerate unknown, fail-closed on missing/bad required; `deny_unknown_fields` stays on OUR emitted contract); delta-tracking sink-INDEPENDENT; pin the full path with a real-append integration test using differential negative controls (cumulative mis-SUMs, deltaed-ctx mis-MAXes); production sink-bind/pump/async-ify = P4. Generalizes to Codex 3.3. `pin: daemon/tests/claude_telemetry.rs + tests/projections.rs Test 24` |
| 28 | 2026-06-12 | [The opt-3 session-lifecycle spine — the write-actor idiom applied to sessions; an EDGE actor driving the adapter via spawn_blocking (no async-trait), never writes the DB](LESSONS.md#28) | Build the session spine as LESSON §9's write-actor idiom applied to sessions: a `SessionActor` (Tokio task + mpsc mailbox + §5.1 status state, owns the adapter + a terminal read-pump; TRACKS the adapter-derived status [the adapter owns §5.1 via `derive_status`, §7.2/LESSON §25], reaps on `is_terminal()`, never re-validates edges, never PTY-scrapes #9) under a `SessionSupervisor` (`HashMap` + `JoinSet`; spawn/track/route/reap, **NO auto-restart** [=4.2]; a panicked actor → `(id,Failed)` so `live_count` stays exact; clean-shutdown Kills all + drains the JoinSet, no orphan). **Drive the SYNC `HarnessAdapter` via `spawn_blocking`, NOT async-ify** (only `launch`+`read_step` block; the rest are cheap inline reads) — no `async-trait` dep, the trait UNCHANGED (LESSON §23); a `spawn_blocking` read can't be `abort()`ed → the kill-path (`pty.kill()`) is a 4.0b/4.2 live-path follow-on. The session module is an EDGE actor (drives the adapter + proposes intents, NEVER writes the DB) — enforce the cat-1 boundary STRUCTURALLY (import-grep: no `runtime`/`eventstore`/`gateway` import → emission/mutation compile-time impossible; non-recursive — a submodule must extend the scan). A `SessionLauncher` seam (`LaunchedSession`) makes the B2-strict survival broker a drop-in. `pin: daemon/tests/session.rs (lifecycle + #9 + cat-1 import-grep + supervisor reap/no-restart/clean-shutdown); security-reviewer on 4.0b (cat-1)` |
| 29 | 2026-06-13 | [A contract gate that can sit DARK is a gap — pin codegen tooling versions + a gate-self-health check](LESSONS.md#29) | The §5.0 3-way verify (`shared/contracts/verify/`: Rust→schema→{zod,pydantic}) sat silently RED ~7 slices (since 043) on a `datamodel-code-generator` auto-update generating a standalone `MetricQuality` pydantic enum the `verify.py` extraction didn't pair — undetected because the AUTHORITATIVE Rust→schema (test-9 byte-diff, in `cargo test --workspace`) + zod stayed GREEN and the verify runs OFF the per-slice loop. CI-health, NOT correctness (the in-loop snapshot tests protected the contract). A gate off the per-slice loop with no self-health signal can sit dark while bumps land. **Pin codegen tooling versions + root-cause the extraction (forward-compat) + add a gate-self-health check (a green run proves it exercised every arm). When a verify is RED, stash-test FIRST** (stash the slice diff + re-run) to classify pre-existing-vs-regression (→ continue the cat-1 slice; fix the gate in a dedicated non-cat-1 slice at the boundary, never bundled into a cat-1 freeze). `pin: shared/contracts/verify/ (4.0b-T tooling-fix)` |
| 30 | 2026-06-13 | [Not every I/O on a safety path is safety I/O — a best-effort convenience degrades SOFT](LESSONS.md#30) | The daemon's default is fail-closed everywhere (settings-serialize → `launch=Failed` §25; audit-write → Deny+§17 alarm §26; hook-miss → Deny §26). But the 4.0b-2-smoke `pty.write(initial_prompt)` sits ON the safety-critical `session.create` execute path yet is NOT safety I/O — the INV-SEC-1 invariant (a live agent reachable only WITH the interception) holds regardless of whether the prompt lands (the interception adjudicates every tool call no matter how claude was prompted; a failed prompt-feed = "agent idle," never "un-intercepted agent"). So it degrades SOFT (session still spawns) — fail-closing it would convert a clean launch into a denial on a non-safety fault (an availability bug). **The test is not "is this I/O on a safety path?" but "is the safety INVARIANT a function of this I/O succeeding?"** — yes → fail-closed (settings write / audit append / redaction gate); invariant-independent convenience → degrade soft. Pin the soft-degrade with a write-error test. `pin: daemon/tests/session_prompt.rs::test_prompt_write_error_does_not_fail_the_session; security-reviewer classifies any new I/O on a safety path` |
| 31 | 2026-06-13 | [The audit-backbone circuit-breaker — SYSTEMIC fail-secure over per-action fail-closed; latched quiesce-and-refuse; reset only on a proven audit commit](LESSONS.md#31) | The single audited mutator must not OPERATE when it can't AUDIT *systemically* (the per-action fail-closed handles the one-off, LESSON §21/§26): a daemon-wide circuit-breaker over the audit-fault pipeline trips on **N=5 consecutive** faults OR an **unrecoverable class** (disk-full/corruption/IO `SQLITE_FULL/CORRUPT/IOERR/NOTADB`; transient + unclassified → recoverable, count-toward-N, never fast-trip) → **RULED-B quiesce-and-refuse** (latched; every mutation denied WITHOUT attempting the audit-write; reads/terminals stay live so the operator sees it; **never auto-resets** — restart-only, rule-#6 ethos). Reset the consecutive run ONLY on a **proven audit commit** (`Ok`) — a routine pre-audit reject (`PolicyDenied`/`Serialize`/`NotFound`) is **no-op**, never Reset, so denial-spam can't mask a systemic fault. Gate+feed at the **write-actor** (the single Gateway-driver chokepoint, forbidden #2/#3), NOT `gateway_txn` (below the gateway layer → DAG inversion); covers submit/plan/approve/deny + the live intercept (explicitly `is_tripped`-gated). Daemon-internal (`IntegrityKind::AuditBackboneSystemicFailure` + `GatewayError::AuditBackboneDown`→`internal_error`; no `shared/` surface, no CONTRACT bump). `pin: daemon/tests/circuit_breaker.rs; security-reviewer own pass (cat-1-adjacent §15/§17)` |
| 32 | 2026-06-13 | [A non-standing-grantable action — explicit catalog field generalizes the risk-4 floor onto ONE mechanism (both disjuncts + a drift-invariant test)](LESSONS.md#32) | Some actions must NEVER be standing-/bulk-approved regardless of risk class (`git.discard_hunk` = irreversible content loss → ALWAYS a per-action human approval). Express it as an EXPLICIT catalog field **`standing_grant_eligible: bool`** (default true; false for discard + workflow.command.invoke), generalize the §11.5 risk-4 approve-all exclusion `is_critical` → **`risk==Level4 OR !standing_grant_eligible`** + unify the old floor onto the field. **Keep BOTH disjuncts** (defense-in-depth) + a **drift-invariant test** (risk-4 ⇒ !standing_grant_eligible) so a future risk-4 add can't slip + the two can't disagree. Eligibility is its OWN axis (irreversibility/blast-radius), never inferred from risk (risk-3 github.create_pr stays grantable; risk-3 discard does not). `pin: daemon/tests/policy.rs (discard non-standing-grant adversarial + field + risk-4⇒non-grant invariant); security-reviewer on the §6.2 floor` |
| 33 | 2026-06-13 | [A diff is a request-on-demand git2-live read, NOT a projection — large/transient data doesn't belong in event-fed read models](LESSONS.md#33) | Projections (`proj_*`, event-fed, subscribe-pushed) are for small/durable/subscribe-worthy state — WRONG for large/transient/on-demand data like a file diff (big, ephemeral, only-on-open). Route it through a **request-on-demand §6.1 READ RPC reading the live source**: `get_diff(worktree_id,file)→DiffResult{hunks}` reads git2 LIVE (read-only, the §7.2 worktree-live-read precedent), resolving `worktree_id→proj_worktree.path` over read-only WAL — NO mutation/write-actor. A read RPC needs its own not-found code (added `IpcErrorCode::NotFound`; precondition_stale/internal_error are semantically wrong). First git2 use → a read-only `daemon/src/git/` module (Phase-5 git mutations register separately via R1-A; git2 stays read-only, forbidden #6). `pin: daemon/tests/get_diff.rs (structured hunks + read-only/NotFound); shared/tests/contract.rs (Hunk snapshot @ 0.28.0)` |
| 34 | 2026-06-13 | [A long-held wait on a shared accept pool starves it — give the wait its OWN bounded class (a reserved sub-bound below the general cap); exhaustion fails CLOSED, never a bypass](LESSONS.md#34) | A connection that BLOCKS for a long time while holding a slot from a shared bounded accept pool starves every other client (the live intercept's 5-min approval-wait exhausted `Semaphore(MAX_CONNECTIONS)` → the UI approve was REFUSED → pending intercepts timed out → degraded availability; a **circular starvation**). Fix = a **reserved sub-bound**: a 2nd `Semaphore(wait_cap = MAX − RESERVED)` the wait holds ALONGSIDE its general permit → `wait_cap<MAX` guarantees `RESERVED` general slots ALWAYS free for non-waits (no accept-time classification, no mid-connection release). **Wait-class exhaustion → fail-closed Deny** (distinct saturation reason; the wait NEVER entered, no park, **no bypass**); acquire the wait permit AFTER the audit commit (§15 #5 audit-before-verdict) but before the wait; RAII release on every terminal (no leak). Test as a **PURE `intercept_verdict_with_wait_class` seam** + prove the live wiring with **/wired** (`wait_cap=MAX−RESERVED` in production), not a flaky concurrent harness (the LESSONS §9/§12 pure-classifier family). Daemon-internal (no CONTRACT bump). `pin: daemon/tests/intercept_wait.rs; /wired the live handler; security-reviewer own pass` |
| 35 | 2026-06-13 | [A production observation-event sink holds the WriteHandle + is injected into the cat-1 edge via an OPAQUE factory; the launcher↔write-actor bootstrap cycle breaks via a deferred handle slot](LESSONS.md#35) | A non-mutation OBSERVATION event (telemetry/usage, LESSONS §10/§23) is written via the **write-actor** NOT the Gateway — but the cat-1 EDGE that drives the adapter (`session/`, import-grep no-`WriteHandle`) can't hold the `WriteHandle`. Pattern: construct the production sink in `runtime/` + inject it into the edge adapter via an **OPAQUE `TelemetrySinkFactory` trait** (the edge imports only the trait); emit **fire-and-forget** (`try_append_observation` = `try_send`, drop-on-full — never back-pressures the writer [forbidden #3], soft-degrades [LESSONS §30], still §15-gated); break the **launcher↔write-actor bootstrap cycle** with a **deferred `OnceLock` handle slot** (main builds the factory unbound → spawns the actor → `bind(handle)`; pre-bind drops, post-bind appends — test it); the pump rides the **session-actor tick** (`poll_telemetry` default-no-op; the real adapter drains an injectable `UsageSource` → emit), cadence proven by **/wired** + a pure unit (not a flaky timer); the **cumulative-baseline high-water-mark clamp** lives in the stateful ingest (`push_usage`), the pure `telemetry_sample` keeps its per-delta floor. `pin: daemon/tests/telemetry_pump.rs + claude_telemetry.rs; /wired the actor tick + factory inject + session/ cat-1 import-grep` |
| 36 | 2026-06-13 | [A multi-strategy recovery DECISION is a pure TOTAL classifier over availability inputs; attempt-failure/retry is the CALLER's loop](LESSONS.md#36) | A recovery flow that picks among strategies (the §8.1 survival ladder: reattach-live/resume/replay/relaunch) is a pure **TOTAL classifier over AVAILABILITY inputs**, NOT a state machine/the live attempt/its failure: `decide_resume(&ResumeInputs)->ResumeResult` reads only what's available (`broker_has_live_session`/`supports_resume`+`has_resume_handle`/`has_scrollback`) in **strict precedence** (broker-live→`ReattachedLive`·resume-capable+handle→`Resumed`·scrollback→`Replayed`·else→`Relaunched`), total `else` + pure → test-first (the LESSONS §9/§12/§25/§34 pure-classifier family). **Attempt-failure/retry is the CALLER's loop** ("resume failed→relaunch" = the 4.1b bootstrap re-calling with `has_resume_handle=false`, NOT a `resume_failed` classifier input — that would make it a state machine). **Harness-agnostic** (reads capability bits not harness identity → ONE fn for Claude+Codex; testable for both shapes via fakes NOW, only the live attempt defers e.g. live-Codex `thread/resume`→3.3). Test each rung's BOTH conjuncts (`capability-present,handle-missing`→falls through) + precedence + count-only-on-`Replayed`. The §14 split: the DECISION is deterministic; the LIVE survival it decides over is the 0.1/0.3-HITL follow-on (§8.1). `pin: daemon/tests/resume.rs (ladder precedence + Resumed-needs-both-conjuncts + harness-agnostic + count-only-on-replayed + total-else)` |
| 37 | 2026-06-13 | [Surface a computed artifact by persisting it on the registry row at compute-time (projector sibling-reads); freeze a safety-critical projection-row as a TYPED contract served typed/fail-closed](LESSONS.md#37) | A read model needing an authoritative artifact the engine COMPUTES (the `PolicyDecision`, `pipeline.rs:154`) must NOT recompute it in the projector (no policy engine in the read layer; drifts from the audited decision). Persist it on the REGISTRY row at compute-time + **sibling-read** it (LESSON §17 generalized — a computed *decision* artifact, IMMUTABLE → rebuild-safe; historical rows carry NULL, still rebuild-equivalent); **§15-redact the new payload before INSERT** even though engine-generated (co-locate INSIDE the insert helper — `approval::insert` takes `gtx`+`gtx.redact_row`, mirroring `request::insert`, the dual-gate LESSON §16; the no-secrets claim is a Step-9 gate, the redactor is the MECHANISM). When the consumer is a SAFETY surface (the human-approval card), freeze the projection-row as a TYPED `shared/` contract (the first one, `shared/src/projections.rs`: `ApprovalQueueRow` — type `risk_level`/`status`/`policy_decision: Option<PolicyDecision>`; `deny_unknown_fields`; OMIT internal `sort_key`/`updated_at_seq`; match the consumer's provisional names) + serve it TYPED (`get_projection` branches to `read_*_typed` deserialize→struct→serialize, **fails CLOSED** on mis-typed/NULL — no loose-JSON on the approval path; test BOTH Some+None). `pin: shared/tests/contract.rs (ApprovalQueueRow @0.30.0) + daemon/tests/gateway*.rs (persist+redaction+sibling-read+rebuild+typed-serve Some/None)` |
| 38 | 2026-06-13 | [Daemon-restart session recovery = enumerate-live → decide_resume → dispatch-over-the-seam; the recovery-spawn is a System-actor AUDITED recovery, NOT a Gateway Action](LESSONS.md#38) | Daemon-restart recovery = a deterministic orchestration over seams: `recover_sessions_on_restart` (`main.rs`, post-supervisor pre-accept-loop) enumerates non-terminal `proj_session` rows (`is_recoverable_status` §5.1) → per-session `ResumeInputs` → `decide_resume` (§36) → dispatch over a launcher/`Broker` seam (`FakeBroker`; the real detachable-terminal broker + LIVE survival = next slice/HITL). **Q1 ruled (a):** a recovery-RELAUNCH spawn is a **System-actor recovery event, NOT a Gateway Action** — the daemon re-materializing its OWN already-approved prior session is the LESSON §10 substrate (the daemon's own lifecycle, like Device/LocalRunner reg in the same §16 sequence), NOT a proposer-gated Action; INV-SEC-1 governs untrusted-proposer intents. (b) reusing `session.create` is WRONG (mints a fresh SessionId → orphans the original; re-approves the approved; zero safety gain). **(a) INV-SEC-1-faithful iff 4 conditions (sign-off gate + security pass):** (1) genuinely-approved-only — identity+§15 #8 profile from the COMMITTED prior `SessionStarted`, never fabricate authority; (2) AUDITED via a `SessionRecovered` observation event (§15-gated write-actor, rule #5); (3) §15 #8 profile PRESERVED (no account-hop); (4) the recovered agent's SUBSEQUENT tool calls STILL intercepted (zero new authority). No-2nd-mutator STRUCTURAL (no WriteHandle/submit in the orchestration sig). `pin: daemon/tests/recovery_restart.rs + recovery_restart_wiring.rs (committed-SessionStarted source + /wired decide_resume); security-reviewer (the 4 conditions)` |
| 39 | 2026-06-13 | [Detachable-terminal survival broker — wrap the unchanged spec (transport, not a new #10 owner); safety = a property of the argv (deterministic), live survival = HITL; optional upgrade + consistent selection + graceful-degrade](LESSONS.md#39) | B2-strict survival needs a detachable-terminal broker (surviving-PTY holder; the agent must NOT be a direct daemon PTY-child). Realize it by **WRAPPING the UNCHANGED `ClaudeLaunchSpec` inside the multiplexer** (`TmuxLauncher`: `tmux new-session -d -s NAME -- env <derived> claude <args>`) — **tmux is a TRANSPORT, not a new #10 owner** (single live-spawn site; #10/§15-#8 preserved by construction). The **safety invariant is a property of the CONSTRUCTED ARGV → deterministically pinned** even though the live survival is HITL: §15 #8 env hygiene rides an **`env(1)` wrapper (`env -u K`/`K=V`) derived from `spec.env_mutations()`** (tmux `-e` can't UNSET → can't strip a stale `ANTHROPIC_API_KEY`; `env -u` strips at exec time regardless of the server env); fail-closed-settings = `write_settings()?` FIRST; **consistent `select_survival_backend`** (never a surviving-launcher + non-surviving-broker mismatch). OPTIONAL upgrade: a startup **probe** → absent → `PtyLauncher` graceful-degrade (B2-achievable, never `ReattachedLive`); broker reports survivors fail-safe. LIVE survival (agent outliving daemon + lossy VT-reattach) + live recovery-dispatch = the 0.1/0.3-HITL follow-on (§14). A permanently-un-unit-testable safety surface → an **e2e-acceptance-matrix MANUAL line** (not an inline TODO). One cohesive file → ONE commit when security-CLEAR-as-a-whole (the safety-pin-own-commit rule assumes a SEPARABLE pin). `pin: daemon/tests/tmux_broker.rs; /wired select_survival_backend; security-reviewer (the #10/§15-#8 surface through tmux)` |
| 40 | 2026-06-14 | [A supervised-child DEATH is a System-actor observation over the cat-1 supervisor reap seam via an injected sink (the §35/§38 pattern generalized); known-shape-vs-defer applies to the payload too — empty-payload until distinguishing signal](LESSONS.md#40) | A supervised-child death = a **System-actor observation event** (`SessionFailed`, the LESSON §10/§23 non-Gateway family — written via the §15-gated write-actor, 0 `Action*`; a death is not a mutation) emitted by a **runtime/ driver over the cat-1 `session/` supervisor reap seam via an INJECTED sink trait** (`session/` holds only `Box<dyn SessionDeathSink>`, NEVER a `WriteHandle` — the boundary holds; the §35 telemetry / §38 recovery pattern, 3rd application). Emit ONLY on a `(id, Failed)` reap (clean `Killed`/`Completed` ≠ failure); the `proj_session` projector folds → `status=Failed` (mutable-from-event-type, rebuild-safe LESSON §17) → the §11.4 restart affordance; the reap→emit router is a PURE `handle_reaped`. **Known-shape-vs-defer applies to the EVENT PAYLOAD too:** at 4.2 every death reaps indistinguishably as `Failed` → a `reason` would be a constant placeholder → freeze **EMPTY-payload** `SessionFailed {}` (the `WorktreeMerged` precedent; the "why" is the correlated `TerminalProcessExited`), add `reason` additive-later when signal exists. The cascade ARMS (fail-in-flight-action → Phase 5/7 · release-lease → session-leases · Codex → 3.3) defer to their producers. `pin: daemon/tests/session_failed.rs; shared/tests/contract.rs (SessionFailed @0.32.0); the session/ cat-1 import-grep guard` |
| 41 | 2026-06-14 | [A periodic maintenance job is a long-lived Tokio interval task in the spawn_drainer/spawn_reaper family; a WRITE pass rides the single write-actor command (never a rogue writer); a derived signal is a live-read recompute; a sync DB read rides spawn_blocking](LESSONS.md#41) | A periodic maintenance job (WAL checkpoint / staleness recompute) = a long-lived Tokio interval task in the `spawn_drainer`/`spawn_reaper` family (LESSON 9 / §10): a deterministic `*_once` bounded pass + the spawn loop (`MissedTickBehavior::Delay`, shutdown-aware, survives a failed pass, never back-pressures the writer). **(a) A WRITE pass rides the SINGLE write-actor command** (`Command::WalCheckpoint` → the `wal_checkpoint` PRAGMA on the write-actor's own conn; NEVER a rogue writable connection — forbidden #3, grep-pinned + security-CLEAR; PASSIVE on the prod interval, the `WalCheckpointMode` enum keeps TRUNCATE a drop-in). **(b) A derived signal is a live-read recompute, NOT event-folded** — `stale` = a pure `is_stale(hb, now, threshold)` (strict `>`; None/unparseable → NOT stale) recomputed over read-only `proj_session` each poll (the worktree-status live-read precedent); prove recomputed-not-replayed via a **clock-flip on fixed rows** (stronger than a moot `rebuild` assertion when the source has no event producer). **(c) A sync DB read off the async loop rides `spawn_blocking`**; log a derived count **on-change only**. A job whose live SOURCE is unbuilt is **correct-but-DORMANT** (the staleness poller until a `last_heartbeat_at` writer lands — build the mechanism test-first; the source is the producer-gated follow-on). `pin: daemon/tests/jobs.rs; /wired the main.rs spawns; security-reviewer (single-writer)` |
| 42 | 2026-06-14 | [A SECOND harness adapter implements the SAME frozen §9.1 trait — no new trait; the divergence is the PARSER (two vendor vocabularies → one signal → one derive_status fold); classify tool calls by SEMANTICS not names; turn boundaries are NON-terminal (terminal = process-exit)](LESSONS.md#42) | A second harness adapter (Codex after Claude) implements the **SAME frozen §9.1 `HarnessAdapter` trait** — NO new trait (the trait froze once at 3.1, LESSON 25); the divergence is the PARSER. **Two vendor vocabularies → ONE signal enum → ONE pure `derive_status` fold** (Codex rollout-JSONL + `exec --json` ThreadEvent → one `CodexSignal` → the §25 fold). **Classify tool calls BY SEMANTICS not vendor names** (`CodexToolKind{ShellExec,FilePatch,McpTool,Other}`; OSS `shell`/`local_shell` vs desktop `exec_command` both map; 3.3c interception keys on the KIND). **A multi-turn session's turn boundaries are NON-TERMINAL** (turn-end→Idle); §5.1 Failed/Completed/Killed are terminal (R-9 sink) → mapping a turn abort→terminal would wrongly R-9-sink a recoverable session — the TERMINAL states derive from the **process-exit signal** (a later live slice), NOT transcript parsing. Status from the structured stream NEVER PTY bytes (#9, grep-pinned); untrusted input parsed tolerantly (no-panic). Test-first vs synthesized golden fixtures; the vendor-version fixture refresh + live drive = HITL. NON-cat-1; NO CONTRACT bump. `pin: daemon/tests/codex_adapter.rs; security-reviewer (#9 + untrusted-input parser)` |
| 43 | 2026-06-14 | [The SECOND harness's LAUNCH path mirrors the launch-spec-as-enforcement-surface (pure no-bypass argv); §15 #8 = resolve-and-PIN one auth (refuse ambiguous, compare-then-drop the secret, keychain-ref); §15 #11 = pre-create state dirs + umask the child, fail-closed; mechanism-first with NO spawn (the binding condition)](LESSONS.md#43) | A second harness's launch path mirrors the first's launch-spec-as-enforcement-surface (`ClaudeLaunchSpec`→`CodexLaunchSpec`, LESSON §25). **The spec is a PURE argv, bypass impossible by construction** (`codex exec … --sandbox <s> -a <a> -m <m> -p <p>`; `--sandbox` always, NO `--yolo`/`--dangerously-bypass`/`--full-auto` in argv OR source — the `--permission-mode default` precedent). **§15 #8 (no account-hop) = resolve-and-PIN exactly ONE auth:** `Err(Ambiguous)` on ≥2 methods OR same-method conflicting-VALUE env vars (don't own a contestable precedence; same-value dup → canonical-pin+strip; 0→`NoAuth`); **compare-the-secret-then-DROP** (transient `AuthSources`, no `Debug`) → only the var-NAME + a **keychain-ref** enter the profile (no-Debug-leak, pin with a per-METHOD canary incl. non-`sk-` OAuth); env-hygiene strips every auth var except the pinned → **exactly one source reaches the child**; containment fields required (no `Default`) → no silent value. **§15 #11 = pre-create `~/.codex/sessions` 0700 + umask-0077, FAIL-CLOSED** (precreate err→no launch — the invariant IS a function of the I/O, §30). **Resume handle = the vendor's native id GATED on the artifact's existence** (rollout-file present→`Resumed`/missing→`Relaunched`, NOT a non-empty session_id [always-true bug] → `decide_resume` §36). **Mechanism-first, NO spawn** (no `SessionLauncher`/`Command::new`/`.spawn` over `harness/codex/`, grep-pinned — the live spawn + interception + `--sandbox` land together next slice; 042→043 precedent). NON-cat-1; NO CONTRACT bump; the two §15 pins each its own commit. `pin: daemon/tests/{codex_auth,codex_perms,codex_launch}.rs; security-reviewer (§15 #8 single-source-pin + §15 #11 fail-closed umask)` |
| 44 | 2026-06-15 | [A credential in a git remote URL has NO key-prefix → it is OUTSIDE the §15 Redactor's recall envelope → strip the `user:token@` userinfo AT THE EMIT SOURCE; the Redactor is the BACKSTOP only](LESSONS.md#44) | A secret riding a git remote URL's **userinfo slot** (`https://user:token@host`) has no detector-visible prefix/entropy → it falls OUTSIDE the §15 prefix+entropy Redactor's recall envelope (LESSON §13) → the control is **strip-at-source, not redact-after:** the `ProjectExecutor` strips `user:token@` BEFORE the `ProjectRescanned` payload is built (`strip_userinfo`; last-`@`-in-authority; scp-style `git@host:path` left intact); the Redactor stays the BACKSTOP only. A credential with no detector-visible shape gets stripped at the producer. (edges-019 / pre-merge edges §30) `pin: daemon/tests/project_executor.rs (strip_userinfo cases)` |
| 45 | 2026-06-15 | [An external-CLI mutator MUST reject a leading-`-` operand fail-closed — a dash operand is parsed as a FLAG → the on-disk result diverges from the approved+audited Action (audit-integrity); a SHARED guard; STANDING for every external-CLI mutator](LESSONS.md#45) | A git-CLI mutator (forbidden #6 — never git2 for mutations) must REJECT a leading-`-` operand (`reject_dash_operands`): git parses a leading-dash operand as a FLAG → the executed mutation DIVERGES from the approved+audited Action (audit-integrity, INV-SEC-1-adjacent). Fail-closed + canonical option-before-operand order + STRUCTURAL reasons (no raw stderr to the §15 event). A SHARED helper across both git arms (`repo_path` exempt = cwd, not an operand). STANDING for every external mutator that takes inputs. (edges-020/021 / pre-merge edges §31) `pin: daemon/tests/git_executor.rs (dash-operand reject)` |
| 46 | 2026-06-15 | [A SYNC external-network executor runs on the write-actor's RAW std::thread (no entered runtime → `Handle::current()` PANICS, spawn_blocking WRONG) → captured runtime `Handle`+`block_on`; MANDATORY timeout (liveness); `*SyncFailed` via additive `FailedWithEvents`](LESSONS.md#46) | A SYNC `ActionExecutor::execute()` driving an async client runs on the write-actor's RAW `std::thread` (LESSON §9 — NOT a tokio worker, NO entered runtime) → `Handle::current()` PANICS and `spawn_blocking` is WRONG → inject a captured `tokio::runtime::Handle` + `handle.block_on(…)`. MANDATORY `tokio::time::timeout` on EVERY external call (the single write-actor serializes ALL mutations → an unbounded hang freezes the path — liveness). `*SyncFailed` on a FAILED action via the additive `ExecutionOutcome::FailedWithEvents` (terminal-non-auth via the shared `classify_sync_failure`; §15 `reason`=class-name). Tests: plain `#[test]` + a built `Runtime`, NEVER `#[tokio::test]`. (edges-023/024 / pre-merge edges §32) `pin: daemon/tests/{github_executor,linear_executor}.rs` |
| 47 | 2026-06-15 | [The §7.2 worktree git-axis is a LIVE-READ cache, NOT event-sourced — refresh via a non-Gateway/non-event write-actor command + a git-watcher interval task; persistence-core stays git-free; a rebuild RESETS the cache](LESSONS.md#47) | The §7.2 worktree git-axis is a LIVE-READ cache, NOT event-sourced (no `WorktreeStatusRefreshed` event) — refresh via a NON-Gateway/NON-event write-actor command (`RefreshWorktreeStatus`, the `DrainOnce`/`ReapLeases` family; single-writer forbidden #3) + a git-watcher interval task (the drainer/reaper precedent) + read-time `git_checked_at` staleness; a rebuild RESETS the cache (the event-folded axis stays rebuild-equivalent, LESSON §4/§17). Layer-clean: git read + `derive_worktree_status` in the runtime layer; persistence-core takes plain computed values (the edges-022 rule). A derived-from-disk signal is a live-read recompute, never an event fold (§41 generalized). (edges-026 / pre-merge edges §33) `pin: daemon/tests/worktree_refresh.rs; /wired the git-watcher spawn` |
| 48 | 2026-06-15 | [A `proj_*` REGISTRY projection is a PROJECTION, not the DATA_MODEL §2.8 durable registry — fold the coarse event, key by the AVAILABLE identity (envelope vs payload; no sibling-read when self-contained), every table in REBUILD_TABLES; defer the canonical-row/mutator model](LESSONS.md#48) | A `proj_*` registry projection is a PROJECTION, not the DATA_MODEL §2.8 durable registry (the canonical register-mutator + richer §2.8 fields is DEFERRED — lead-ruled at the R1b freeze). Fold the COARSE event (`ProjectRescanned`→`proj_project`+`proj_repository`; `IntegrationConnectionRegistered`→`proj_integration_connection`), keying by the AVAILABLE identity — the ENVELOPE `project_id` (edges-028) or the PAYLOAD `connection_id` (edges-030) — NO LESSON §17 sibling-read when the payload is self-contained. Every table in `REBUILD_TABLES` → rebuild-equivalent (LESSON §4/§17); `Decode`-degrade-and-skip on an unbindable payload (no-payload-echo). (edges-028/030) `pin: daemon/tests/{project_registry,integration_connections_proj}.rs (fold + rebuild-equivalence)` |
| 49 | 2026-06-15 | [A credential-registration mutator is registration-ONLY — the secret NEVER flows through a risk-≥1 action (it executes off the §15-REDACTED durable row → a token in inputs is masked-by-execute-time); §15 #4 holds BY CONSTRUCTION; standing_grant_eligible=FALSE (§6.2 credential floor)](LESSONS.md#49) | `integration.connect` (risk-2, `ExecutorKind::Integration`) is REGISTRATION-ONLY: the secret NEVER flows through the action. A risk-≥1 (approval-gated) action executes off the §15-REDACTED durable `action_requests` row (LESSON §20 §7.2-split), and the §16 dual-gate masks any `inputs_json` secret BEFORE persist → a token is GONE by execute-time → the mutator STRUCTURALLY cannot carry it. Inputs `{provider, keychain_ref POINTER, account?}`, NO token; §15 #4 holds BY CONSTRUCTION (no token slot). The token→keychain write is a SEPARATE deferred non-Gateway mechanism (`keyring`, HITL). Defense-in-depth: a secret-shaped `keychain_ref` rejected by REUSING the canonical `PrefixRedactor` (LESSON §13) READ-ONLY (no parallel detector). `standing_grant_eligible=FALSE` — the §6.2 credential floor (authorization-establishing → never bulk-grantable; the `git.discard_hunk` precedent, LESSON §32). (edges-029) `pin: daemon/tests/integration_connect.rs; security-reviewer (§15 #4 + INV-SEC-1 no-bypass)` |
| 50 | 2026-06-15 | [A per-migration "applies" test asserts its FLOOR (`user_version >= N`)+table existence, NOT the exact-latest version (else every new migration breaks the PRIOR migration's test); keep ONE exact-latest runtime pin; RUNTIME read not `assert!(CONST >= N)` (clippy)](LESSONS.md#50) | A per-migration "applies" test asserts its FLOOR (`user_version >= N` + the new table exists), NOT exact-latest `== N` — exact-latest couples the test to *being newest*, so the NEXT migration's `SUPPORTED_USER_VERSION` bump silently breaks the PRIOR migration's test; the floor survives bumps. Keep exactly ONE exact-latest runtime pin (`tests/gateway_plan.rs`) as the double-bump guard. Assert a RUNTIME read (`store.user_version() >= N`), NOT `assert!(CONST >= N)` (the latter trips clippy's `assertions_on_constants`). (edges-030) `pin: daemon/tests/gateway_plan.rs (exact-latest pin) + per-migration floor tests` |
| 51 | 2026-06-15 | [A projection's live-delta SOURCE and its PROJECTOR's folded-event set are two lists that MUST agree — a row-mutating event with no nudge is a silent stale-UI bug; key both on the same token-form (`::EVENT_TYPE` consts) + a set-agreement guard test](LESSONS.md#51) | `deltas_for_append` (the subscribe-delta source, write-actor `Command::Append`, publish-after-commit) and `Projector::apply` (which event types mutate the read model) are TWO LISTS that must be identical — an event the projector folds but the source omits mutates the row with NO nudge → the UI silently shows stale state (D3: `proj_session` folds SessionStarted/SessionFailed/SessionRecovered, the source nudged only on SessionStarted). Two drift modes: SET drift → a set-agreement **guard test** iterating the folded set + a "extend BOTH lists" comment; WIRE-VALUE drift → key the source on the **same `::EVENT_TYPE` consts the projector uses** (a rename follows both; a raw literal silently desyncs). Nudge stays **row-less by design** (subscriber re-reads via `get_projection`; row enrichment is a deferred future improvement per lag-resync → do NOT assert `row: None`). Generalizes to every subscribe-delta projection (D4 → the other-4). **D4a refinement:** a SELECTIVE projector (folds a specific set) gets the set-guard + a NEGATIVE arm; a BLANKET projector (folds every event, e.g. `proj_audit_trail`) gets an unconditional nudge + no set-guard; a single append → a `Vec<ProjectionDelta>` so tests drain-and-find; a cross-path projection needs an arm in BOTH `deltas_for_append` + the gateway accumulator. (D3/P4.5 · extended D4a) `pin: daemon/tests/runtime.rs (the session set-agreement guard + the 2 nudge tests + no-session_id no-op; the UsageLedger guard negative arm + the AuditTrail generic-event blanket test)` |
| 52 | 2026-06-15 | [When an event's append PATH migrates, a path-specific side-channel is silently orphaned — route it through a path-AGNOSTIC shared fn + pin behavior with a test driving the REAL production entry (a non-production-path test masks the orphan)](LESSONS.md#52) | A side-channel keyed to ONE append path breaks when the event's production path migrates. `deltas_for_append` runs only in the `Command::Append` arm; `SessionStarted` moved to the Gateway `emitted_events` path (`gtx.append` in txn-B, bypassing `Command::Append`) at 4.0b-1 → its nudge arm was ORPHANED in production (a created session never nudged; death/recovery still did — the tell-tale asymmetry). **(a)** Route a cross-cutting side-effect through ONE shared path-agnostic fn (`deltas_for_event`) every path calls (the §51 "one mapping" seen from the failure side). **(b)** A test must drive the REAL production entry — D3's suite was green because its tests appended via a direct `store.append` (the orphaned path), not the production gateway path → false confidence; pin by submitting `session.create` through the gateway execute (the `/wired` reachable-from-production discipline applied to side-effects). A found bug's regression test MUST drive the production path or the next refactor re-orphans it. (D3-FINDING/D4b/P4.5) `pin: daemon/tests/{runtime,gateway}.rs (D4b production-path test: session.create through the gateway publishes the deltas, not a direct store.append)` |
| 53 | 2026-06-16 | [Enriching an already-frozen fail-closed projection-row is a LOCKSTEP slice (column+fold+row-field+typed-serve+CONTRACT-bump atomic); the first bool projection column needs an INTEGER(0/1)→bool coercion in the read layer](LESSONS.md#53) | A typed projection-row served fail-closed (LESSON §37 — `read_*_typed` deserializes each row → the `deny_unknown_fields` struct) can't be enriched "column now, row later": a column without its row field → the serve rejects the extra column (fails closed); a row field without the column → a missing field. So a "basic-now + SPREAD" deferral (P7.2 froze `PullRequestRow` minus mergeable/checks) is consumed as ONE atomic slice — MIGRATION_N (ALTER-only, CREATE untouched §50) + the fold (None→NULL, rebuild-safe §17) + the row fields + the SELECT + the CONTRACT bump + the §2.5-seam snapshot + 3-way verify. **First bool projection column:** SQLite stores `bool` as INTEGER 0/1; a row-to-JSON read yields a JSON *number* that won't deserialize into `Option<bool>` → coerce number→bool in the READ layer (`daemon/ipc`), keeping the contract a pure `Option<bool>`; pin true/**false [the INTEGER-0 falsy edge]**/None. (D5a/P4.6) `pin: daemon/tests/projections.rs (fold + typed-serve true/false/None) + shared/tests/contract.rs (schema-snapshot @0.36.0 + 3-way verify) + daemon/tests/gateway_plan.rs (MIGRATION_13 pin)` |
| 54 | 2026-06-16 | [A NEW subscribe-able projection VERTICAL composes §37 (typed row) + §48 (proj_* fold) + §51 (delta nudge) + a NEW `ProjectionName` closed-set variant (the CONTRACT cost of subscribe-ability); a frozen VALUE enum (no lifecycle) uses the plain-value-enum idiom, NOT `status_machine!`](LESSONS.md#54) | Standing up a WHOLE new subscribe-able projection (vs enriching one, §53) = §37 (typed fail-closed `Row`) + §48 (the `proj_*` fold; key by the available identity — `review_id` is GLOBALLY unique → single-col PK, not a composite) + §51 (the `deltas_for_event` arm; pin a gateway-emitted event with the §52 production-path test) + the **NEW closed-set CONTRACT cost**: a projection with NO `ProjectionName` variant cannot be subscribed/served (audit-blanket-only, the D4b flag), so subscribe-ability REQUIRES adding a `ProjectionName` variant (a closed-set bump + the §2.5-seam snapshot + the `ALL` array). A frozen **VALUE** enum (a fixed classification — `ReviewState`; no lifecycle/transition) uses the plain-value-enum idiom (`derive` + `rename_all` + manual `ALL` + serde reject-unknown — the `RequesterType` precedent), NOT `status_machine!` (whose phantom all-false `is_terminal()` misrepresents a value enum as a lifecycle machine). (D5b-1/P4.6) `pin: shared/tests/contract.rs (Review{Synced,State,Row} frozen-shape + ProjectionName::ALL += Review @0.37.0) + daemon/tests/projections.rs (the ReviewProjector fold)` |
| 55 | 2026-06-16 | [An external-NETWORK read action is risk-1 NOT risk-0 (a local read auto-executes; a network read by an untrusted proposer is a rate-limit/exposure vector) + `standing_grant_eligible=true`; below the risk-2 external WRITES](LESSONS.md#55) | A catalog action that READS an EXTERNAL network resource is **risk-1**, not risk-0 — local-vs-network is the line: `project.rescan` (LOCAL FS read) is risk-0 (auto-execute, no external surface); `github.sync_reviews` (NETWORK read) must NOT auto-execute (an agent hammering the GitHub API = a rate-limit-exhaustion/data-exposure vector), so risk-1 (the lowest gated tier — one human approval) + `standing_grant_eligible=true` (the user can opt into auto-sync); below the risk-2 external WRITES (no mutation/credential). `idempotency=None` (a re-sync RE-RUNS, not dedup); `side_effect_applied=false` (a read = a clean rollback); INV-SEC-1 no-bypass (emit only via the gateway txn-B); the §46 sync-executor pattern; the octocrab→shared enum mapping is a PURE unit-tested fn (unknown→conservative floor), not the not-tested live edge. (D5b-2/P4.6) `pin: daemon/tests/github_executor.rs (emit/empty/validate/failure + map_review_state) + shared/tests/contract.rs (catalog entry @0.38.0); security-reviewer on the first external-READ classification` |
| 56 | 2026-06-17 | [A 2nd harness's INV-SEC-1 interception REUSES the 1st's Gateway loop by swapping ONLY the I/O envelope + a daemon-classifier harness-discriminator; for a vendor whose hook is "a guardrail not a boundary", `--sandbox` OS-containment is a MANDATORY defense-in-depth 2nd layer; the discriminator is trusted-binary-stamped, never agent-settable (PIN-1)](LESSONS.md#56) | Generalizing §26 to a 2nd harness: the daemon `intercept`→Gateway loop (submit_action/decision_sink/wait-class/verdict/audit-before-verdict/fail-closed) is harness-agnostic — reuse it UNCHANGED by swapping ONLY the I/O envelope + branching ONLY the parser on a harness tag. Codex diverges on the classifier (apply_patch/exec_command/mcp; Codex MCP=Guaranteed→Adjudicate vs Claude BestEffort→Deny), so B1 "handler untouched" fails the spec; **B2** = an OPTIONAL daemon-internal `harness` tag on `HookPayload` + a 2-arm `map_to_action_type`, the loop literally unchanged, the 1st-harness path byte-identical, the default/absent tag = the strictest reading (Codex tool→Claude classifier→UnmappedTool→DENY). The **genuinely-new** layer: a vendor whose hook is "a guardrail not a boundary" needs `--sandbox workspace-write` (writes scoped to cwd+writable_roots={worktree+approved}, network_access=false, no bypass) as MANDATORY OS containment (hook=adjudicate+audit, sandbox=contain; both=INV-SEC-1). Grammar note: workspace-write reads ALL files — writable_roots is a WRITE boundary only (assert write-confinement, never read). **PIN-1:** the discriminator is trusted-binary-stamped (`--harness` flag), never agent-settable → a spoof only HARDENS (both arms deny-unknown; risk catalog-authoritative); enforce mechanically. (3.3c/P3.3c) `pin: daemon/tests/codex_intercept.rs (PIN-1 + deny-unknown both arms + zero-auto-allow + sandbox-is-inv-sec-layer + no-live-spawn) + daemon/tests/codex_launch.rs (no-bypass literal); security-reviewer EVERY layer (cat-1)` |
| 57 | 2026-06-17 | [Headless VT model wraps `vt100` (display-only #9); `scrollback()`=position-not-count + the alt-grid-clobber](LESSONS.md#57) | A headless VT model wraps a purpose-built pure-Rust emulator (`vt100`) behind `HeadlessVt` (`terminal/vt.rs`) and is DISPLAY-only — status NEVER from the VT screen (#9 scoped grep-pin, the `terminal/mod.rs` precedent). Two vt100 traps: (a) `scrollback()` is the scroll POSITION not the filled count → derive filled via a `set_scrollback(MAX)` probe (clamps to filled) cached behind `&self` (computed in `process()`/`resize()`, view restored to 0); (b) the probe MUST guard on `alternate_screen()` — the alt grid (capacity 0) otherwise clobbers the cached normal-screen count to 0 mid-session (vim/htop). Tolerant parse (§42 family). Daemon-internal; §6.4 wire frozen 0.21.0 → NO contract; new `vt100` dep → `cargo audit` at /phase-exit + version-pin TODO. (3.4-VT/075a) `pin: daemon/tests/vt.rs (bytes→screen + scrollback-filled-not-capacity + alt-grid-clobber Test 8 + #9 display-only grep-pin + garbage-no-panic)` |
| 58 | 2026-06-17 | [A daemon survival artifact persisted to disk (VT scrollback) is a §15-redacted SUBSTRATE write (NOT a Gateway Action); the ONLY serializable on-disk type holds POST-redaction text (structural §16 co-located-gate); the `Replayed` signal keys on has-restorable-CONTENT so `decide_resume` stays byte-unchanged](LESSONS.md#58) | Serializing the headless VT (§57) for the §8/§17 `Replayed`-after-restart rung: (1) serialize via `vt100 state_formatted()` (visible, round-trips alt-screen) + plain scrollback rows, prove no-loss with idempotent `snapshot(from_snapshot(s))==s`; an alt-ACTIVE snapshot honestly reports `scrollback_len()==0` (the hidden normal buffer isn't captured — pin it, two-buffer fix deferred). (2) Wire producer→consumer over a cat-1-NEUTRAL `ScrollbackStore` trait in `terminal/` + the durable impl injected as `Arc<dyn>` (§35) so `session/` holds no `WriteHandle`; the `Replayed` signal is fed from `has_restorable_content()` (scrollback OR non-blank screen, via a `screen_nonblank` bit) so a mid-`vim` session re-renders AND `decide_resume` stays byte-unchanged (the caller populates inputs, §36). (3) The durable store is a §15-redacted SUBSTRATE write (LESSON §10), NOT a Gateway Action; redaction-before-persist binds it like the event store (#3); guarantee STRUCTURALLY — the ONLY `Serialize` on-disk type holds ONLY post-redaction output (`VtSnapshot` gets none) so a raw secret can't reach the bytes (§16 co-located-gate); fail-closed (non-clean redaction → no write → `Relaunched`, availability degrades not the invariant); reuse the canonical Redactor (§13/§49); 0700/0600 (§15 #11); atomic temp+rename per-`session_id` (single-writer-#3 is `nexusops.db`-specific). ①=A persisted=redacted-PLAIN-text (live re-render stays formatted; B deferred). (4) Accepted residual: scrollback has NO keychain-refs backstop → the §13 recall envelope is the sole control, the harness-transcript posture; test recall on realistic terminal-shaped text. (3.4-VT/075b–d) `pin: daemon/tests/{vt.rs,scrollback_recovery.rs,durable_scrollback.rs}; security-reviewer EVERY layer (cat-1/§15)` |
| 59 | 2026-06-19 | [First network read in the IPC read layer — a UI-initiated network read is a READ RPC (LESSON §33) + captured-Handle/block_on/MANDATORY-timeout (§46) + a fail-closed seamed client; an AGENT-initiated network read stays a risk-1 Gateway action (§55); the discriminator is the INITIATOR (trusted UDS peer vs untrusted proposer)](LESSONS.md#59) | `get_pr_diff` is the first network fetch in the IPC READ layer. A UI-initiated network read = a transient on-demand READ RPC (§33; the getpeereid-trusted local peer ≠ the §55 untrusted-proposer rate/exposure case → NOT a Gateway action; a returns-data read doesn't fit `ActionResult`); thread `Arc<dyn GithubReadClient>` through listener→serve_connection→dispatch with a captured `Handle`+`block_on`+MANDATORY `timeout` (§46, DoS-bound) + a `FakeGithubReadClient` seam; the production client may be wired UNAUTHENTICATED (mechanism-first §28/§43, live fetch auth-gated). §15 no-leak STRUCTURAL (`GithubReadError` can't reach the fieldless `IpcErrorCode`; userinfo-strip §44); a cross-project identity-resolution (repo_id→owner/repo off any projected PR) opens a confused-deputy axis when auth lands → a MANDATORY security-re-review gate on the auth-bootstrap (read-only PUBLIC safe at MVP). (D7/P4.7) `pin: daemon/tests/get_pr_diff.rs; security-reviewer (§15 no-leak + deferred AUTHZ)` |
| 60 | 2026-06-20 | [A GitHub WRITE mutation = risk-3 + `entry_no_standing_grant` + UI/IPC-requester-only (deny-before-risk); SHA-pinned to the approved head (anti-race) + explicit fail-closed `merge_method` (audit-integrity); success = a §15-redacted observation event folding the PR machine to terminal `Merged` — the create_pr executor precedent + shared classify](LESSONS.md#60) | `github.merge_pr` is the FIRST GitHub write beyond `create_pr`. A PR-merge mutation is **risk-3** + **`standing_grant_eligible=false`** (`entry_no_standing_grant`; the §6.2 floor is its OWN axis, NOT inferred from risk — LESSON §32; a drift test pins risk-3 AND non-grantable) + **UI/IPC-requester-only** (`CatalogPolicy::decide`→`Deny` BEFORE risk for Agent/Brain/Pack/System; the §15 #8 PIN-e gate generalized). The executor **SHA-pins** to the approved PR-head (anti-race — octocrab 409s if the head moved; so the human approves a SPECIFIC head) + an **explicit fail-closed `merge_method`** (audit-integrity — the audited Action names the exact method; `map_merge_method` unknown→Err, never a server default) + validates `requires_resource_refs` FIRST then EVERY operand BEFORE the network call. Success → `Succeeded{side_effect_applied, emitted:[PullRequestMerged]}` (a real merge happened → a txn-B fault yields the honest `ActionPartiallySucceeded`, LESSON §21); the §15-redacted `PullRequestMerged` folds `proj_pull_request`→terminal `merged` (derive-from-event, LESSON §17); failure → the SHARED `classify` (terminal-non-auth `GithubSyncFailed`, auth/transient `Failed`; reasons = STRUCTURAL class-names, never raw API text). Carry-forward: the live UI Merge button needs `head_sha` exposed on the PR row + the live authenticated merge shares D7's deferred keychain-auth re-review gate. (D9/P4.7) `pin: daemon/tests/github_merge_pr.rs (success/validate-each/timeout/failure-classes/partial/map_merge_method/requires-resource-ref) + daemon/tests/policy.rs (deny-non-UI + require-approval-UI + never-in-approve-all) + daemon/tests/projections.rs (terminal-fold rebuild-equiv); security-reviewer EVERY layer (cat-1)` |
| 61 | 2026-06-20 | [A GitHub review-SUBMIT is a communication/attestation write (not a code mutation) but gated IDENTICALLY to a code-write (risk-3 + `entry_no_standing_grant` + UI/IPC-only) because an `approve` carries merge-gate power; SHA-pinned to the reviewed head (`commit_id`) + fail-closed verdict + a §15-redacted `ReviewSubmitted` folding `proj_review` — the D9 merge_pr precedent; octocrab 0.53 `create_review` deprecated → drive the typed lower-level POST](LESSONS.md#61) | `github.submit_review` is the SECOND github write (after D9 merge_pr). A review-submit does NOT mutate branch/code state (a verdict/comment posting) but is gated IDENTICALLY — **risk-3** + **`entry_no_standing_grant`** (a verdict is a deliberate human attestation; an `approve` satisfies branch-protection → merge-gate power; bulk pre-approval = rubber-stamp) + **UI/IPC-requester-only** (the D9 `GITHUB_MUTATION_TYPES` policy const EXTENDED to `{merge_pr, submit_review}` — deny-before-risk). SHA-pin to the reviewed head (`commit_id` REQUIRED, audit-integrity, the merge_pr `sha` precedent) + an explicit fail-closed `map_review_event` (approve/request_changes/comment→octocrab `ReviewAction`, unknown→Err) + `body` conditionally-required (non-empty for request_changes/comment, optional for approve — GitHub's 422 rule). A NEW `ReviewSubmitted` event (the write-counterpart to `ReviewSynced`, the D9 `PullRequestMerged`-not-reuse precedent) folds `proj_review` by `review_id` (a submit over a synced review UPDATEs). **octocrab 0.53 trap:** `create_review` is deprecated/`pull_number`-only (`clippy -D` rejects it) → drive the upgrade-stable typed lower-level POST `/repos/{o}/{r}/pulls/{n}/reviews` (byte-equivalent, same URL-interpolation as every github write → no new injection surface). (D10/P4.7) `pin: daemon/tests/github_submit_review.rs (success/validate-each-operand/body-conditional/approve-empty/unknown-event-no-call/requires-resource-ref/timeout/failure-classes/txn-B-partial/map_review_event) + daemon/tests/policy.rs (deny-non-UI + require-approval-UI + never-in-approve-all) + daemon/tests/projections.rs (proj_review fold + submit-over-synced UPDATE); security-reviewer EVERY layer (cat-1)` |
| 62 | 2026-06-20 | [The DATA_MODEL-§2.8 durable registry is a CANONICAL row written DIRECTLY (NOT in REBUILD_TABLES, NOT event-rebuilt) via the LESSON-16 dual-gate atomic {row + audit-event} over `gateway_txn` — the event is the trail, the ROW is the source of truth; cold-start seed = System-actor register-if-absent; resolution fails CLOSED on unknown (no silent mint, §15 #8)](LESSONS.md#62) | P5.3a's `execution_profiles` = the daemon's FIRST canonical OBJECT registry (Option B, user-ruled — the §2.8 durable registry over the LESSON §48 event-sourced projection; the Gateway's action_requests/approvals are durable bookkeeping, this is the first domain-OBJECT registry with its own register-mutator). The row is the source of truth: written directly, **NOT in `REBUILD_TABLES`** (a `rebuild()` does NOT clear it — pin with `rebuild_does_not_clear_*`, the inverse of the §48 rebuild-equivalence test); `ExecutionProfileRegistered` is the §15-gated System-actor audit TRAIL, not the source. The register-mutator reuses `gateway_txn` as the atomic + redaction PRIMITIVE (NOT a policy-gated Action): one txn `redact_row` (§15 #3/#4 — `keychain_ref` POINTER-only by construction) before INSERT then `append`; event-write fault → rollback → no row (new `FaultPoint::RegistryEventWrite`); event stamped System-actor (LESSON §10, not policy-gated). Cold-start seeds ONE default register-if-absent (the `register_device` precedent; idempotent; **now 3 startup registration events** → compute `next_seq()`, don't hard-code). `resolve_profile` is registry-backed + fail-closed (`Some(unknown)`→Failed-before-launch, no mint; `None`→default; injected `ProfileLookup` seam, read-only WAL). Carry: the secret WRITE + runtime re-derivation (§7.2) = 5.3b; a PROPOSER-reachable register path MUST be a typed Gateway Action (own cat-1), never a direct `register_profile`. (P5.3a) `pin: daemon/tests/execution_profiles.rs + daemon/tests/session_executor.rs + shared/tests/contract.rs (ExecutionProfileRegistered @0.43.0); security-reviewer EVERY layer (§15 #4/#8 + INV-SEC-1)` |
| 63 | 2026-06-20 | [A mutation/IO executor resolves its EXTERNAL target from the AUDITED resource_ref/envelope identity (via the ONE shared resolution authority), NEVER from attacker-controllable `inputs` — audited == executed by construction; close the WHOLE confused-deputy axis (every site), not the cat-1 subset](LESSONS.md#63) | A github-write executor read `owner`/`repo` from literal `inputs["owner"/"repo"]` while the AUDITED identity was a separate resource_ref `repo_id` — a confused-deputy (approved repo X, executed repo Y). Resolve the target from the AUDITED identity via the ONE shared `repo_resolve` authority (the D7/LESSON §59 helper EXTRACTED to `daemon/src/integrations/repo_resolve.rs` + a `RepoOwnerRepoResolver` seam, prod `DbRepoResolver` read-only WAL), NEVER `inputs`; a divergent `inputs` target is IGNORED (pin per-site). The audited identity differs by op: PR-scoped (merge/submit/sync) keys on resource_ref `repo_id`→folded PR row; repo-scoped `create_pr` (no PR yet) keys on the audited `project_id` (== its resource_ref id == the envelope param — no second deputy; verify the identity surface per op before ruling). Close the WHOLE axis (2 cat-1 + risk-3 + risk-1 read), not the cat-1 subset — 2-of-4 is half-closed; each site its OWN commit (the end-state no-literal-read structural guard lands with the final commit). By-product: resolution now requires the folded row → out-of-band targets fail-close (a strengthening). NOT-live-exploitable today (unauth client + UI/IPC-only) → a pre-auth hardening. (P4.7/082) `pin: daemon/tests/{github_merge_pr,github_submit_review,github_executor,get_pr_diff}.rs; security-reviewer EVERY github-write arm + the shared helper` |
| 64 | 2026-06-21 | [The keychain-write primitive — a secret acquired DAEMON-side (gh-reuse / outbound OAuth, NEVER inbound-IPC) → OS keychain, ONLY the `keychain_ref` POINTER in events/rows/logs (§15 #3/#4); one `resolve_authed_token` gate (toggle-OFF→None · per-owner confused-deputy over the §63-audited owner · fail-closed); the connect-trigger is getpeereid-peer-authed + NOT a Gateway mutation (audit = integration.connect)](LESSONS.md#64) | 083's auth-bootstrap. A secret reaches the keychain WITHOUT crossing IPC inbound or landing in an event/row/log: acquire it **daemon-side** (reuse `gh auth token` transiently/`Zeroizing`, or an outbound OAuth poll) — the UI "Connect" is a TRIGGER (`connect_via_gh`, NO token field), the daemon sources it. Write via the shared `SecretStore`/`KeyringSecretStore` (`keyring`); events/rows/logs carry ONLY the per-account `keychain_ref_for(provider, account)` POINTER (§15 #3/#4 by construction). One `resolve_authed_token` gate for reads AND writes: toggle `live_writes_enabled` OFF→`None` (fail-closed→`AuthFailed`); per-owner token selection over the **§63-audited owner** (`octocrab_for(owner)`, NOT `inputs` — confused-deputy closed on the LIVE path); no-token/keychain-unavailable/gh-absent→typed `AuthError`. The toggle is a **governance vertical** (`integration.set_live_writes` risk-2/UI-IPC-only/`standing_grant_eligible=false` → `IntegrationLiveWritesSet` folded into `proj_integration_connection.live_writes_enabled` default OFF, validated-REGISTERED-else-fail-closed; freeze-then-vertical C4→C5, CONTRACT 0.45.0). The `connect_via_gh` trigger is `getpeereid`-peer-authed (§15 #7) FIRST + **NOT a Gateway mutation** (the §49 non-Gateway credential-write; audit = the subsequent `integration.connect`) — security-ruled acceptable. The network round-trip rides fakes + manual verification; the deterministic seams stay test-pinned. (P4.7/083) `pin: daemon/tests/keychain.rs (10) + daemon/tests/{policy,integration_connect,integration_connections_proj}.rs + shared/tests/contract.rs (@0.45.0); security-reviewer EVERY layer (cat-1) + the in-arc live-path re-review` |
| 65 | 2026-06-26 | [The INBOUND-secret posture — a user-typed credential MAY arrive inbound over the getpeereid-authed local UDS (`Zeroizing`, dropped post-write) → keychain-direct, POINTER-only; carry the SOURCE id in the event not the daemon-DERIVED pointer (it trips the §15 redactor); the canonical-registry UPDATE rides pipeline txn-B; the pointer-record is the AUDITED Gateway action (§49)](LESSONS.md#65) | 5.3b's profile secrets (USER-ruled Option B). **Inbound IS acceptable for a user-TYPED credential at the LOCAL trust boundary** (§64 held daemon-sourced-only because `gh` is sourceable; an arbitrary API key is not): `profile.set_secret` = a getpeereid-first (§15 #7) IPC trigger, `Zeroizing`, keychain-direct via the §64 `SecretStore`, returns the POINTER only (§15 #4) — does NOT generalize to a remote path. **Carry the SOURCE id in the event** (`ProfileSecretSet{id}`) + RE-DERIVE the `nexusops/profile/<ULID>` pointer — the path-wrapped ULID trips the §15 JSON-value redactor (LESSON §13) → it'd persist `[REDACTED]`; a daemon-DERIVABLE value is never carried as the derived form when it trips the redactor (the row keeps the full pointer). The canonical `execution_profiles` (§62, NOT a projection) UPDATE rides the pipeline txn-B (`profiles::apply_secret_set`, atomic with the event, fail-closed; forbidden-#3 forces it onto the write-actor); the proposer path is the AUDITED `profile.set_keychain_ref` Gateway action (NEW `ExecutorKind::Profile`, `entry_no_standing_grant`, UI/IPC-only); §63 ref-from-audited-id; §62 rebuild-preserves; the redacting-`Debug` pin extends to inbound-secret wire types (§43). §7.2 runtime status recompute-on-read. (P5.3b/085) `pin: daemon/tests/{profile_secret,profile_self_test,profile_runtime_status,profile_change,policy}.rs + shared/tests/contract.rs (@0.46.0); security-reviewer EVERY layer (cat-1) + the in-arc inbound-secret re-review` |
| 66 | 2026-06-26 | [Parse-don't-trust at the daemon's OWN inbound IPC boundary](LESSONS.md#66) | `#[serde(transparent)]` id newtypes accept any wire string (no parse) + `request::insert` trusts it → validate every client-supplied id at submit BEFORE the pipeline (`validate_request_ids`: action_request_id + project_id-when-Some + plan_id + per-step), malformed→`protocol_error` + no row; closed the empty-PK→collision→AuditWriteFailed-as-precondition_stale class. `pin: daemon/tests/ipc.rs` |
| 67 | 2026-06-26 | [Exhaustive-match error-code mapping (no wildcard)](LESSONS.md#67) | De-collapse GatewayError→IpcErrorCode to honest distinct codes via an EXHAUSTIVE match (a new variant = compile error); keep safety codes distinct (`fencing_conflict`≠`precondition_stale`, §17/#6); collapsing hides re-approvability. `pin: methods.rs::gateway_error_to_code + tests/ipc.rs` |
| 68 | 2026-06-26 | [A registry-only event must feed a UI-readable projection + nudge](LESSONS.md#68) | An event folding only into a not-`ProjectionName` `proj_*` never surfaces in the cockpit → also fold it into a UI-readable projection (ProjectActivity) + a `deltas_for_event` nudge (§51/§52, production-path), and relax the consumer's daemon-unserved REQUIRED row field. `pin: daemon/tests/{projections,runtime}.rs` |
| 69 | 2026-06-26 | [A daemon contract bump always pairs a UI regen slice](LESSONS.md#69) | Any `CONTRACT_VERSION` bump reds the UI's generated-version drift test → the §5.0 propagation ALWAYS needs a paired UI regen (gen-contracts.mjs); "daemon-only" is false framing — pre-pair every contract-bump brief with its UI regen. `pin: shared/tests/contract.rs + the ui generated-version drift test` |
| 70 | 2026-06-26 | [A §2.8-registry read RPC serves a typed SECRET-FREE row; within one serve, a derived hint soft-degrades (§30) while row data fails closed (§37)](LESSONS.md#70) | `get_execution_profiles` serves the §2.8 registry over a §6.1 read RPC (the get_diff precedent, NOT get_projection — no `ProjectionName`, §62): `ProfileRow` has NO `keychain_ref` field — derive `has_credential: bool` instead (§15 #4 by construction + a `deny_unknown_fields`-rejects-leak contract test). Classify each served field by whether the invariant depends on it: corrupt ROW DATA (`status`) fails the whole read closed (§37); a DERIVED UI hint (`is_default`, from the first ExecutionProfileRegistered provenance) soft-degrades to its safe default (§30 — never the wrong default, never fail-open) — document + test the asymmetry. `pin: daemon/tests/get_execution_profiles.rs + tests/ipc.rs + shared/tests/contract.rs @0.48.0` |
| 71 | 2026-06-26 | [A session-control executor body routes a typed SessionCommand (execute_kill precedent); a moved-into-the-pump PTY is written via a write-only PtyWriter grabbed before the move (#9-structural); soft-pause gates the drive loop via a flag (no §5.1 value, no OS-suspend)](LESSONS.md#71) | `session.send_message`/`pause`/`resume` bodies route `SessionCommand`s to the supervisor (validate-first + parse-target → Failed before route; `side_effect_applied=delivered`=enqueue-honesty; emit NO event + approval-gated → no new authority, §26/§30). Cross-thread PTY write = a write-only `PtyWriter` grabbed before the terminal moves into the read-pump (`killer()` precedent; #9 structural). Soft-pause = an internal flag gating the drive loop (no new §5.1 value; NOT an OS-suspend — the UI must label Pause honestly; a real suspend is a lead-surfaced follow-on). `pin: daemon/tests/{session_executor,session,session_prompt}.rs` |
| 72 | 2026-06-26 | [A per-hunk git mutation re-derives the hunk from the LIVE diff (the frozen position-only ref carries LOCATION not content) + a TWO-guard §17 race (no-match→Failed AND apply --check-first→no-apply); no event (live-read cache); the position-only accepted limitation does NOT carry to a DESTRUCTIVE op](LESSONS.md#72) | git.stage_hunk/unstage_hunk re-derive the hunk from the live `git diff` (resolve worktree via an injected read-only WAL resolver; slice the @@-block) → apply to the INDEX via git CLI (apply --cached / --cached -R, forbidden #6). §17 race: guard#1 no-position-match→Failed + guard#2 `apply --check`-first→no-apply (pin the check-fail path with a per-command fake — the re-derived patch ~always applies otherwise). Audited unit = the POSITION (executed==audited at contract granularity); same-position content-drift is unguardable + RECOVERABLE for stage/unstage (accepted, documented) — does NOT carry to destructive discard. No event (LESSON §47); structural-reason / reject-dash / resolve-from-audited / 0600-O_EXCL CLI guards (LESSON §45/§63). `pin: daemon/tests/git_executor.rs` |
| 73 | 2026-06-26 | [A DESTRUCTIVE per-hunk git mutation hashes the EXACT bytes it will APPLY (verified==destroyed by construction) — UI sends `displayed_hunk_sha256` over `inputs`; absent/mismatch/divergent-base → fail-closed; the §72 position-only accepted limitation does NOT carry to the irreversible op](LESSONS.md#73) | `git.discard_hunk` (risk-3 destructive) reverse-applies a one-hunk patch to the WORKING TREE (`git apply -R`, no --cached) gated by the lead-RULED (A) content-hash verify-before-destroy. GUARD #2 hashes the EXACT bytes GUARD #1 will apply (NOT a second `read_diff` of a possibly-divergent base) → verified==destroyed by construction (security-caught [critical]: hashing the git2 read_diff base while applying the CLI git-diff base let a PARTIALLY-STAGED file verify-pass while discarding un-reviewed staged content → fixed → partial-staging fails closed, unstage-first = MVP). FROZEN canonicalization (daemon↔UI): lowercase-hex sha256 over the hunk body, per-line `origin-byte + DiffLine.content` verbatim, `@@` header excluded; vector `sha256(" a\n-b\n+c\n")=2980a50…c39ee6`. No event (§47); §15/CLI guards (§45/§63); contract-neutral (hash rides `inputs` → no CONTRACT bump, §69 N/A). `pin: daemon/tests/git_executor.rs; security-reviewer MANDATORY` |

<!-- Starts empty. Each row links to its `LESSONS.md` anchor. Populate as the project accretes. -->

<!-- Slash commands: see root CLAUDE.md "Slash commands available." Implementer pair: /session-start + /session-end. -->
