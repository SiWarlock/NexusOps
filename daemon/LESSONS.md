# LESSONS.md — NexusOps (the Rust daemon (trust core))

> Full prose for every lesson logged during work in `daemon/`. The compact index lives in `daemon/CLAUDE.md` "Lessons logged" table.
>
> **Lesson numbers are stable IDs.** New lessons get the next sequential number. Numbers may be referenced from code comments, commit messages, and cross-references between lessons. **Don't reorder; don't reuse a deleted number's slot.**
>
> **Lessons start at §1.** Each code area has its own lesson sequence — lessons don't carry across code areas.

---

## Lesson format

```markdown
## <a id="N"></a>N. <Short topic> — <one-line rule>

**Date:** YYYY-MM-DD.
**Source slice:** <slice-id or commit hash>.

<2-5 paragraphs explaining: what was discovered, why it matters, how to
apply the rule, what edge cases are still open. Cite file:line references
where applicable.>

**Rule:** <one-sentence summary, same as the heading subtitle>.
```

---

## <a id="1"></a>1. Broken cargo/rustc proxies — `rustup default stable` won't fix them; repoint the shims

**Date:** 2026-06-07.
**Source slice:** 0.4 (`OQ-DATA-SPIKE-3` env-finding; cleared under ③).

During Phase-0 the daemon-track build was blocked: `~/.cargo/bin/{cargo,rustc}` (and 11 other proxies, 13 total) were **broken dangling symlinks** pointing at a non-existent `/Users/nozzins` path — the artifact of a moved home directory. The real stable toolchain (1.93.0) was intact at `~/.rustup/toolchains/stable-aarch64-apple-darwin/bin`.

The non-obvious part: **`rustup default stable` did NOT fix it.** `rustup` only (re)creates *missing* proxies; it leaves *existing-but-broken* symlinks untouched. The obvious first move silently no-ops. The working fix was to repoint every broken proxy at the local `rustup` binary directly — `ln -sf rustup ~/.cargo/bin/<proxy>` for each of the 13 — after which the plain shims resolve (no PATH workaround needed). Verify with `cargo --version && rustc --version` (expect 1.93.0), `cargo clippy`, and a real `cargo build`.

Edge cases still open: if the home dir moves again the same breakage recurs; a `rustup self uninstall` + reinstall would also fix it but is heavier. Prefer the targeted shim-repoint.

**Rule:** When `~/.cargo/bin` shims are *broken* (dangling symlinks, e.g. after a home-dir move), `rustup default stable` does **not** repair them (rustup only recreates *missing* proxies) — repoint each broken proxy to the local `rustup` binary (`ln -sf rustup ~/.cargo/bin/<proxy>`), then verify with the plain shims.

## <a id="2"></a>2. The wire value is the contract; the SoT propagation pattern is §5.0 — follow it for every contract addition

**Date:** 2026-06-07.
**Source slice:** 0.5 (shared contract freeze, `OQ-DATA-SPIKE-5`).

The 0.5 freeze settled two conventions every future contract surface (event-type registry, GatewayPort schema, action-type catalog, any new enum/ID) must follow.

**(a) The serialized wire/`TEXT` value is the contract — not the in-language identifier.** Enums serialize to exact snake_case strings via `#[serde(rename_all = "snake_case")]`; each language uses its idiomatic identifier (Rust `PascalCase` variants, TS string-literal unions, Python `Enum`) but the *string on the wire / in the `TEXT status` column* is what's frozen. A round-trip test (`test_every_state_machine_value_present_and_serializes`) pins this — never assume the identifier and the wire value match. Closed-enum / reject-unknown holds end-to-end (serde closed enums → JSON-Schema `enum` → `z.enum` → Pydantic): unknown values are rejected at every boundary, which is the fail-closed posture (§15), not optional.

**(b) The source-of-truth propagation is fixed by `ARCHITECTURE.md §5.0` (Option A):** Rust `shared` crate = native authority (newtypes for IDs, serde-closed enums) → `schemars` emits a **first-class, versioned, diff-gated** JSON Schema artifact (`shared/contracts/schema/`) → TS Zod + Python Pydantic are **generated** from that artifact → a self-contained 3-way value-set equality harness proves they agree. Do **not** hand-author a consumer's types or invert the authority (an external IDL/codegen-into-the-trust-core was rejected — it generates bare types in the safety-critical module and fights the newtype posture). Every new contract addition extends the Rust authority and regenerates the artifact; the diff-gate catches drift.

**Rule:** Freeze the *wire value* (snake_case `TEXT`), not the identifier — pin it with a round-trip test; and author every contract in Rust (`shared/`) per §5.0, regenerating the published schema + consumers (never hand-write a consumer or invert the authority).

## <a id="3"></a>3. Single-write-actor — one writable `Connection`; everything else read-only WAL; canonical `seq` in one `BEGIN IMMEDIATE` txn

**Date:** 2026-06-07.
**Source slice:** 1.1 L2 (event store).

The §15 single-mutator invariant has a concrete enforced shape in the daemon: **exactly one writable rusqlite `Connection`** (owned by the write-actor); **every other access opens a read-only WAL connection** (`Connection::open_with_flags(..., SQLITE_OPEN_READ_ONLY)` / `open_read_only`). A read-path that can't even *construct* a writable handle can't violate single-mutator by construction — enforce it at the type/connection level, not by convention.

The canonical `seq` (the total event order, §7.1) must be assigned **atomically**: the `SELECT max(seq)+1` and the `INSERT` go in **one `BEGIN IMMEDIATE` transaction**, not two statements. `BEGIN IMMEDIATE` takes the write lock up front, so two concurrent appends can't read the same max and produce a gap/duplicate. A borrow-checker-only "one writer" guarantee is **not** enough — the *transaction boundary* is what makes the ordering atomic (1.1 L2 security-reviewer high finding).

Pair this with an **injectable `Clock` + `IdGen`** (constructor-injected; fakes in tests) so the event log — `seq`, `event_id` (ULID), `recorded_at` — replays **byte-identically** in the golden-log test. Determinism-for-testability is a daemon-wide seam (root `CLAUDE.md` "Determinism-for-testability"), not a per-slice afterthought.

**Rule:** One writable `Connection` (the write-actor); all other access is read-only WAL (`open_read_only`). Assign the canonical `seq` via `SELECT max+1` + `INSERT` inside one `BEGIN IMMEDIATE` txn (atomic order, not borrow-checker-only). Inject `Clock`+`IdGen` so the log replays deterministically.

## <a id="4"></a>4. Projections fold in-band in the event-commit txn; savepoint-isolated per-projector; offsets never ahead of rows; recovery = catch-up + rebuild

**Date:** 2026-06-07.
**Source slice:** 1.2 (projection engine, L1–L3 — `c8c6c72`/`a1dc482`/`e66c659`).

Projections are applied **in-band, inside the same `BEGIN IMMEDIATE` event-commit txn** as the event INSERT (`§7` "within the one event-commit transaction"; DATA_MODEL `§2.4`), *after* the §15 redaction gate — so a projector only ever sees the **already-redacted** payload, and a reader never sees an event whose (healthy) projections haven't applied. Each projector runs under a **uniquely-named SAVEPOINT**: a projector **logic error** rolls back *that projector's* rows + offset and marks `projection_offsets.state='degraded'` (skip-and-continue), while a **Db error** (or the gate) aborts the whole append closed (§15). `mark_degraded` is written *outside* the savepoint but *inside* the outer txn — so it shares the event's commit/rollback fate (atomic either way). Offsets advance in the same txn — **never ahead of the rows they represent** (crash-safe).

Recovery has two paths, both built on the strict `seq > last_seq` boundary: **catch-up replay** (wired into `open`, runs every start) folds pending events per projector — and is a **strict no-op on an already-current log** (the `>` boundary, not `>=`, guards non-idempotent counters against double-counting on reopen; pin it with a "catch-up-noop-when-current" test). **Full rebuild** truncates the derived tables (a compile-time-`const` list that **excludes `events`**) + replays all — **byte-equivalent to the incremental fold**, and raw `events` are never mutated (§7.2 "projection corruption must not corrupt raw events"). A degraded projector advances past the bad event on later successes (sticky `last_seq`), never stranded. In-band apply and replay share one `apply_one`, so the degrade semantics agree by construction. Status columns **bind the frozen §5.1 `shared/` enums** (reject-unknown), never bare strings. Guard `advance_offset`/`mark_degraded` with a rows-affected check (`expect_one_row`) — a missing offset row must not silently no-op (it would re-fold every reopen). **Open caveat:** `catch_up_replay` currently reconstructs via the *strict* read path → one corrupt event row aborts `open` (daemon-won't-start); the §17-degradable-replay fix is task 1.6 (decide skip-vs-refuse semantics + audit-integrity).

**Rule:** Fold projections in-band in the event-commit txn (after the redaction gate), each under its own SAVEPOINT (logic error → degrade+skip that projector; Db error → fail the append closed); advance offsets in the same txn (never ahead of rows); recover via catch-up replay (strict `seq>last_seq`, no-op on current) + full rebuild (truncate a const derived-table list, replay-all, byte-equivalent, raw events untouched); bind status columns to the frozen §5.1 enums.

## <a id="5"></a>5. External side-effects go through the transactional outbox: written in the event-commit txn; §15 sync-sink; at-least-once with reset-on-open

**Date:** 2026-06-08.
**Source slice:** 1.3 (transactional outbox, L1–L2 — `343cc09`/`707845a`).

Every external side-effect (Brain/GitHub/Linear/notifier/JSONL mirror) goes through the **transactional outbox**, never a direct call. Outbox rows are written **in the same event-commit txn** as the event + projections (the transactional-outbox pattern: a fact is **recorded-iff its delivery-intents are**, and **delivered-iff recorded**) — atomic with everything else; a txn abort (e.g. the redaction gate) leaves 0 events **and** 0 outbox rows. The outbox is the **§15 *sync* sink** (root rule #3 "same redactor gates persist+embed+**sync**"): the per-destination payload derives **only from the already-redacted stored event**, never re-fetched raw, and per-destination filtering only *removes* fields (e.g. Brain = envelope minus restricted/secret) — it can never re-introduce a secret. Rebuild/replay must **never re-emit outbox rows** (outbox ∉ the rebuild table list) — a read-model reconstruction must not re-deliver historical events to the outside world.

Delivery is **at-least-once** with a deterministic drainer (`drain_once(clock, dest)` — the Tokio-loop unit; the loop spawn itself is 1.6-bootstrap-wired): claim `in_flight` **committed before** `deliver` (the no-loss window), transition `delivered` / `failed`+`backoff(retry_count)` / `dead` (terminal, or retryable past a bounded `MAX_RETRIES` — bounded dead-letter, never an unbounded loop). Crash recovery = **`reset_in_flight` (in_flight→pending) wired into `open`** (single-instance-safe via the pidlock); consumers must be **idempotent** (dedup). The due-comparison `next_attempt_at <= now` is a **lexical string compare** — only sound because the `Clock` RFC3339 contract is **UTC `Z`-suffixed and format-consistent** (verify the time source emits `Z`; treat it as a Clock contract). Use a separate `IdGen` counter for `out_` ids so event-id/golden-log determinism is unchanged.

**Rule:** Route all external side-effects through the outbox: write rows in the event-commit txn (recorded-iff-intended); treat it as the §15 sync sink (payload from the already-redacted event, filter-only; rebuild never re-emits); deliver at-least-once via a deterministic `drain_once` (in_flight claim-before-deliver, reset-on-open recovery, backoff + retryable/terminal + bounded dead-letter, idempotent consumers); keep timestamps UTC-`Z` for the lexical due-compare.

## <a id="6"></a>6. Cross-restart locks — a persisted monotonic fencing high-water mark per `(resource_id, lease_kind)`; authority is a LIVE lease; single-instance via a std advisory file lock

**Date:** 2026-06-08.
**Source slice:** 1.4 (lease locks + fencing + pidlock + reaper, L1–L3 — `b3f7612`/`442149a`/`0347ac6`).

Cross-restart resource locks (ADR-008) are a SQLite `leases` row per `(resource_id, lease_kind)` carrying a **monotonic fencing token** that is a **persisted high-water mark** — minted `+1` on each acquire/reclaim and **never lowered**: `release` and the reaper NULL only the holder fields (`owner_id`/`acquired_at`/`heartbeat_at`/`expires_at`) and **keep** `fencing_token`. Because the high-water mark lives on disk in the row (not an in-memory counter, not `MAX()` over deletable rows), monotonicity **survives daemon restart** — the test pins a held+expired lease at token N → drop+reopen → next acquire mints N+1 and the pre-restart token N fails validation. Mint the token under `BEGIN IMMEDIATE` (the read-current-then-write must be one txn — LESSON §3 — or two racing acquires read the same max and collide); `renew`/`release`/`validate` are single atomic statements. `locks/` writes through the **same single writable `Connection`** the event store owns (EventStore exposes thin delegating `acquire_lease`/`renew_lease`/`release_lease`/`validate_lease_held`/`reap_leases` methods) — never a second writer (Forbidden #3).

**Authority is a LIVE lease, not a merely-unsuperseded token (Option B, human-ratified — safety rule #6).** The fencing oracle the Phase-2 gateway calls is `validate_held(resource_id, lease_kind, owner, token, now) = owner-match AND token == fencing_token AND expires_at > now`. "Stale" = **NOT a live lease — expired OR superseded** — §17 (line 387) leaves the word undefined, so the team pinned it: an **expired holder is rejected even if no one has superseded it** (the zombie-holder window), not only when a higher token was minted. The textbook supersession-only check (`token == latest`) was rejected because §17's daemon-crash recovery (line 384) re-derives an old `executing` action via the same "lease check" — authority must track **liveness**, not just supersession. Consequence flagged to Phase 2 (task 2.4): the gateway must **heartbeat/renew (or re-acquire)** a long-running action so it isn't fenced by its **own** lease expiry (a self-fence → false `fencing_conflict`).

Single-instance (the pidlock) is a **std advisory exclusive file lock** (`std::fs::File::try_lock`, stable since Rust 1.89 — toolchain is 1.93, so **no dependency**) on a lock file. The OS holds the lock against the open file descriptor → it is **auto-released on process death (including crash)** and **immune to PID reuse**: the OS-fd lock is the sole oracle. Never use a `kill(pid, 0)` liveness check — a reused PID makes it falsely conclude the old daemon is alive (false-positive block), and it can't detect a crashed holder. The PID written into the lock file is **diagnostic-only** (guarded on `set_len` success so a failed truncate can't leave misleading bytes). Fail-closed: `TryLockError::WouldBlock` → `AlreadyHeld`; `TryLockError::Error(io)` → a typed IO error — **never "acquired."**

The reaper's deterministic unit is `reap_once(now)` — it frees every lease past `expires_at` (`expires_at <= now` is the **exact complement** of `validate_held`'s `> now`, so a live lease can never be reaped) and returns the reclaimed set; the Tokio interval **spawn** is 1.6-bootstrap-wired (joins the outbox drainer spawn, §12). The whole `Lease` model is **daemon-internal** — no `proj_lease`, no `lease_` among the 22 frozen IDs, not one of the 10 §5.1 status machines, and the UI/Brain don't read the `leases` table → **no `shared/` surface, no CONTRACT_VERSION bump** (the 1.3 outbox precedent).

**Rule:** Cross-restart locks = a `leases` row per `(resource_id, lease_kind)` with a **persisted monotonic fencing high-water mark** (minted +1 on acquire/reclaim under `BEGIN IMMEDIATE`; release/reap keep it; survives restart); **authority is a LIVE lease** — `validate_held` = owner + token-match + `expires_at > now`, so "stale" = expired OR superseded (safety rule #6; the gateway must heartbeat long actions); single-instance via a **std advisory file lock** (OS-fd-held, auto-released on death, immune to PID reuse — never `kill(pid,0)`); the whole model is daemon-internal (no `shared/` surface, no CONTRACT_VERSION bump).

## <a id="7"></a>7. The UDS GatewayPort transport — length-prefix framing + getpeereid-first peer-auth + two version axes + `ServerFrame` frame-mux; ship the mechanism, wire the runtime at the bootstrap

**Date:** 2026-06-08.
**Source slice:** 1.5 (UDS GatewayPort transport, L1–L4 — `ae27f1d`/`fb941e8`/`c1c54e5`/L4).

The out-of-daemon GatewayPort transport (§6.4) is a UDS server with **4-byte big-endian length-prefix + JSON-body** framing, bounded by `MAX_FRAME_SIZE` (8 MiB) — an oversized frame is rejected **from the length prefix alone, before any body allocation** (anti-DoS). Peer-auth (safety rule #7 / ADR-004) is **`getpeereid()` called FIRST in the per-connection handler** — a peer whose uid ≠ the daemon-uid is dropped + disconnected before any frame is read; **`getpeereid`, NOT `SO_PEERCRED`** (Linux-only). Isolate the FFI in one `peer_uid(fd)` fn + **fail-closed** (a getpeereid error is never "authorized"; no uid-width cast that could silently truncate). Test the peer-auth **enforcement** path, not just the predicate — give the per-connection handler `peer_uid` as a **parameter** so a foreign-uid → disconnect is pinned deterministically (you can't spawn a foreign-uid peer in a unit test).

There are **two independent version axes** — don't conflate them: **`CONTRACT_VERSION`** is the §5.0 schema/codegen artifact version (the IPC method/error/frame schemas authored in `shared/` → schemars → the ui's generated validators; bumped per additive surface, gated by the byte-diff + 3-way verify); **`protocol_version`** is the §6.4 wire-handshake compatibility check (`HelloFrame`→`HelloAck`|`VersionSkewError`; `SUPPORTED_PROTOCOL_RANGE = {min:1,max:1}`, **daemon-authored** — the ui pins provisionally + reconciles to the daemon's range). Handshake-FIRST (after peer-auth): no method is served before a successful handshake; a version-skew or malformed/non-Hello first frame fails closed → structured error + disconnect.

When the architecture's LOCKED error-code set is found incomplete at implementation time, that's a **Finding → escalate the contract correction** (here: §6.4's 6-code set had no code for a bad-first-frame / handshake-required / malformed / bad-params violation; lead-ratified Option B added **`protocol_error`** — `unknown_method` is reserved for a genuine unknown method NAME). Frame-type multiplexing (§6.4, so the Terminal Channel can share the socket) is an **internally-tagged `ServerFrame`** (`{frame_type: rpc_response | subscription_push}`, Terminal reserved) — a JSON discriminant over the **unchanged** codec, NOT a binary type-prefix (which would retrofit the committed codec; the raw-byte Terminal encoding is a **measured Phase-3 call**). The dispatch **error split**: an **infra** error (a failed frame read) disconnects; a **client** `WireError` (`protocol_error`/`unknown_method`/bad-params) is returned and the connection continues. Read methods (`get_projection`/`subscribe`) read **read-only WAL** (`open_read_only`; single-writer preserved — LESSON §3); the projection-name→`proj_*` map is a **closed-enum const** (never client input → no SQL injection); an unfed projection returns its **empty table**, not an error.

The whole server's production wiring lives at the **1.6 bootstrap** (the runtime spawn): `bind()` + the accept-loop, the live `subscribe` delta-source (`EventStore::append`→a broadcast→the subscriber push, via a `try_clone` read/write split), the **platform cfg-guard** (getpeereid is macOS/BSD-only — a Linux build won't link), and an accept-loop **concurrency cap**. 1.5 ships the **mechanisms + the cross-language contract** (testable over a temp socket + with fed deltas); the runtime sources wire at 1.6 — the same "ship the deterministic unit, wire the production source at the runtime" pattern as the 1.3 outbox drainer + the 1.4 reaper. Reachability is stated honestly per layer (pub primitive, consumer-wired later) — never claimed live.

**Rule:** The UDS GatewayPort transport (§6.4): 4-byte-BE-length-prefix + JSON framing (oversized rejected pre-alloc; `MAX_FRAME_SIZE` 8 MiB); **`getpeereid()` peer-auth FIRST** (reject uid≠daemon-uid; NOT `SO_PEERCRED`; isolated unsafe + fail-closed; test the *enforcement* path via an injectable `peer_uid`); handshake-first; **two version axes** (`CONTRACT_VERSION` schema/codegen vs `protocol_version` wire-skew, `{1,1}` daemon-authored); a LOCKED-error-set gap → escalate the contract correction (`protocol_error`); frame-mux via an internally-tagged `ServerFrame` over the unchanged codec (Terminal encoding = a Phase-3 measured call); read-only-WAL reads + a closed-enum table map (no injection); ship the mechanism + contract now, wire the runtime source (bind/accept/subscribe-broadcast) at the 1.6 bootstrap.

## <a id="8"></a>8. Cold-start bootstrap — strict ordering (pidlock-first, before any DB write) + determinism-for-testability on the FS failure path

**Date:** 2026-06-08.
**Source slice:** 1.6a L1+L2 (cold-start bootstrap + migration typed errors — `48b7a2c`/`f1de088`).

Cold-start (§16) **composes** already-shipped mechanisms in a binding order: `create_dir_all` (the app-support dir prereq) → **`PidLock::acquire` (the FIRST refusing gate)** → `EventStore::open` (which itself runs pragmas → version-floor → migrate-with-backup/rollback → catch-up-replay → outbox crash-recovery). The pidlock MUST come before any DB write: two instances that both reached `migrations::run` concurrently would corrupt the event-log spine (forbidden #3) — single-instance is a *precondition* of the single-writer, not a parallel concern (the security-reviewer pins this ordering). `EventStore::open` + `migrations.rs` **already** enforce the §16 DB lifecycle (backup on `from≥1`, restore-on-failure, `refuses_db_newer_than_supported` floor) from 1.1 — bootstrap composes them, it does **not** re-implement; the genuinely-new bootstrap work is the *ordering* + the version-info composition + the (user-gated) registration layer.

The §16 version-compat "matrix" has exactly one *enforcing* floor at bootstrap — the DB `user_version` check (`refuses_db_newer_than_supported` → refuse-safe on a DB newer than the binary). The other rows are NOT enforced here: `app_version`↔protocol-range is enforced at the 1.5 wire handshake; the agent-CLI/SDK tuple + the sidecar-MCP `initialize` check are §9.1/§13.1, deferred. So `DaemonVersionInfo` *reports* the tuple (`app_version=CARGO_PKG_VERSION`, protocol_range, db_user_version, contract_version) for diagnostics/handshake, and only the DB floor *refuses*.

Determinism-for-testability on an FS failure path: a symmetric backup/restore copy path can't be broken mid-flow deterministically (you'd need a forced disk fault). Instead **extract the decision into a pure classifier** — `on_migration_failure(restore_result, from, migration_err)` — and unit-test the *classification*: a clean rollback → a "rolled back to vN"-renderable `Migration` error that **preserves the original migration-error context** (don't drop it); a rollback-that-also-failed → a **distinct typed `RestoreFailed{from, ..}`**, never the swallowed `let _ = restore_db(..)`. The pure-helper + inline-unit-test pattern (the `outbox.rs` precedent) covers the safety-relevant decision without an un-deterministic disk fault. Typed-result discipline: kill `-1` sentinels in version-compat code (`user_version() -> Result<u32>`; an out-of-`u32` value maps to a typed `Migration` error, never `Reconstruct`).

**Rule:** Cold-start = `create_dir_all` → **`PidLock` (first refusing gate, strictly before any DB write — forbidden #3)** → `EventStore::open` (compose the already-shipped migrate/backup-rollback/floor/replay — don't re-implement); the §16 version "matrix" *enforces* only the DB `user_version` floor at bootstrap (the rest are report-only / handshake / deferred); pin an FS failure path by extracting a **pure classifier** (`on_migration_failure`) + inline-unit-testing the decision (preserve the original error context; emit a distinct typed `RestoreFailed`; no `-1` sentinels) rather than forcing a disk fault.

## <a id="9"></a>9. The daemon runtime — single write-actor (dedicated blocking thread + mpsc), publish-after-commit broadcast, readers never block the writer; unlink-before-bind

**Date:** 2026-06-08.
**Source slice:** 1.6b (daemon runtime — write-actor + `main.rs` + drainer/reaper + UDS accept-loop + broadcast — `26898a8`/`9448035`/`8d06ed7`/`f9c31b1`/`39f756e`).

The locked single-write-actor (LESSON §3) is realized at the runtime as a **dedicated OS thread** that owns the writable `EventStore`, fed by an **mpsc command channel** — rusqlite is blocking, so the writer must NOT be an async task (it would block the Tokio runtime). A `WriteHandle` (Clone+Send) exposes async methods (`append`/`drain_once`/`reap_leases`/`subscribe`) that send a command + await a oneshot reply. **Reads never touch the actor** — every read path opens `open_read_only` (forbidden #3). The outbox drainer + lease reaper are async interval tasks that send commands to the writer (a **bounded** drain pass so a backlog can't starve the writer; `MissedTickBehavior::Delay` so a slow pass doesn't burst-catch-up). Graceful shutdown closes the channel + joins the thread (a `WriteActor` `Drop` + a `biased` shutdown-priority select).

The **live subscribe delta source** is a `tokio::sync::broadcast` sender held by the write-actor that publishes the resulting `ProjectionDelta`(s) **after** the append commits (never an uncommitted/rolled-back delta). The critical forbidden-#3 corollary: **a subscriber must NEVER back-pressure the writer** — a lagging subscriber's `broadcast::error::Lagged` is detected + the subscriber resyncs (re-`get_projection`), it does not stall the sender (so no resync wire frame + no CONTRACT bump). (The socket subscribe-**SERVE** push — threading the broadcast `Sender` into `serve_connection`/the accept-loop + the post-ack streaming path — is the carved **1.6d** follow-up; the SOURCE is live here.)

UDS bind = **unlink-before-bind** (reclaim a stale socket file so `bind` doesn't `EADDRINUSE`) — safe because the 1.4 pidlock guarantees single-instance, so any existing socket is stale. The accept-loop hands each connection to `serve_connection` on a `spawn_blocking` task (the read path is sync rusqlite), under a **semaphore concurrency cap** (bound live connections + 8-MiB frame buffers; the permit releases on connection close — pin both the at-cap rejection AND the permit-release). `main.rs` (`#[tokio::main]`) is the production entry: `cold_start` → start the write-actor → spawn drainer/reaper/accept-loop → block on SIGTERM/SIGINT → graceful drain. This closes the 1.3/1.4/1.5 "ship the unit, wire the runtime at 1.6" deferral chain (`/wired main.rs` reaches them all).

**Rule:** The daemon runtime = a single **write-actor** (dedicated blocking thread + mpsc + a Clone+Send `WriteHandle`; reads always `open_read_only`, never the actor — forbidden #3); the drainer/reaper are async interval tasks sending **bounded** commands (`MissedTickBehavior::Delay`); the live subscribe source is a broadcast that publishes **after commit**, and a **lagging subscriber resyncs, never back-pressures the writer** (no wire frame, no CONTRACT bump); UDS bind = **unlink-before-bind** (pidlock makes a stale socket safe to reclaim) + `spawn_blocking(serve_connection)` per connection under a semaphore cap (pin rejection AND permit-release); `main.rs` (`#[tokio::main]`) wires it all, closing the 1.3/1.4/1.5 runtime-deferral chain.

## <a id="10"></a>10. Daemon self-registration = a System-actor system event, not a Gateway Action; desktop ids mint off `DesktopObjectKind`

**Date:** 2026-06-10.
**Source slice:** 1.6a-L3 (Device/LocalRunner cold-start registration — `980e1a0`).

The §16 cold-start ordering registers the desktop host: a **stable `Device`** (register-if-absent — reused across restarts, found via `EventStore::first_event_of_type`) + a **per-start `LocalRunner`** (a fresh `lr_` every boot). INV-SEC-1 ("no code path mutates state except via a typed Action that passed policy+approval") could read as "registration must be a Gateway Action" — but the Gateway is Phase 2, and bootstrap runs before it exists. The resolution (USER-RULED Option B): bootstrap self-registration is the daemon establishing its **own** runtime identity — *substrate*, not an untrusted proposer intent. It is appended as a **System-actor event** (`actor_type=System`) directly via the write-actor `append` path, so it still passes the **§15 redaction gate + the projector fold** (immutable + audited) — it is simply not *policy-gated*, because there is no untrusted intent to gate. **INV-SEC-1 governs proposer-driven mutations; the daemon's own lifecycle events are the layer those Actions are recorded into.** The envelope's non-null `workspace_id` is satisfied by a reserved **`SYSTEM_WORKSPACE_ID` sentinel** (a well-known all-zero `ws_` ULID + `WorkspaceId::system()`) — the workspace-less-System-event precedent, NOT a nullable-column schema change.

Identity for these objects mints off **`DesktopObjectKind`**, not the frozen-22 `IdKind`: a **`desktop_minted_id!` macro** (a sibling of `ids::minted_id!`) keyed off `DesktopObjectKind::id_prefix()` produces `DeviceId`/`LocalRunnerId` (`dev_`/`lr_`), reusing `ids::IdError` + the fail-closed ULID parse — so the **frozen-22 set stays untouched** (pinned by `IdKind::from_prefix("dev_"/"lr_") == None`). Because `dev_`/`lr_` are payload fields (not envelope typed columns), the projector sources the id from the **registration payload** to write `object_refs` (`('device',id)`/`('local_runner',id)`) — rebuild-safe (ids are stored data, read back on replay, so they bypass the injected `IdGen`/golden-log seam by design). A surfaced contract contradiction (the binding §16 "registers desktop-host Device" vs the §5.3 frozen "[DEFERRED] Device") was user-ruled Option A — **Device MVP-live (desktop-host); `is_deferred()` true only for RemoteClient**; the iOS multi-device/pairing dimension stays deferred (a §5.3 prose note, not the predicate).

**Rule:** The daemon's own cold-start lifecycle identity (desktop-host `Device` register-if-absent + per-start `LocalRunner`) is a **System-actor system event** (`actor_type=System`, `SYSTEM_WORKSPACE_ID` sentinel) appended via the write-actor — through the §15 gate + projector fold (audited + immutable) but **NOT policy-gated** (INV-SEC-1 governs untrusted proposer intents, not the substrate those Actions record into; the Gateway is Phase 2 anyway). Desktop ids mint via a `desktop_minted_id!` sibling keyed off `DesktopObjectKind` (frozen-22 `IdKind` untouched; pinned by `from_prefix==None`); object_refs are payload-sourced (rebuild-safe). When a binding §16 step contradicts a frozen §5.3 classification, the binding section wins — escalate the frozen-row reconcile (here: Device → MVP-live, Option A).

## <a id="11"></a>11. Degradable catch-up replay — quarantine + audit-integrity, the daemon never won't-start on one bad row (§17 Option C)

**Date:** 2026-06-10.
**Source slice:** 1.6c (§17 degradable replay + audit-integrity event — L1 quarantine/degrade daemon-internal; L2 `AuditIntegrityViolation` event, CONTRACT_VERSION 0.12.0→0.13.0).

The strict replay path (1.2) reconstructs every row, so **one** corrupt event row aborted `open()` → the daemon wouldn't start (no in-product recovery, `rebuild` sat behind the failing `open`). §17 Option C (user-ruled) makes the read path **degrade instead of crash**. The split is load-bearing and matches §17 as written: an **unknown `event_version`** row is a *forward-compat* situation → **DEGRADED** (mark `projection_offsets.state='degraded'`, continue; a newer binary folds it later) — **no** quarantine, **no** audit-integrity event. A **corrupt** (reconstruction-fail) **or** an **unredacted** (`redaction_status='unredacted'`, §15 defense-in-depth) row is an *integrity* event → **QUARANTINED**: recorded in a daemon-internal `quarantine` table (`seq` PK, structural reason, `detected_at`, `audit_emitted`; MIGRATION_6; outbox/leases-analogous), the raw spine left untouched, the row skipped (not folded). `replay()` is shared by `catch_up_replay` + `rebuild`, so both degrade from one change; offset semantics are unchanged from `apply_one` (a skipped row doesn't advance the offset; a later healthy row advances past it; trailing skipped rows re-read idempotently).

The "loud record" is a separate post-`open` step (replay stays append-free): the caller (`cold_start`) emits one **`AuditIntegrityViolation`{seq,reason}** System-actor event per *newly*-quarantined seq via the write-actor `append` path — like the 1.6a-L3 self-registration (LESSON §10), it passes the §15 redaction gate + projector fold and lands in `proj_audit_trail` (consumer-visible), and is NOT a Gateway Action. **Exactly-once across restart AND rebuild** needs three things together: emit only `WHERE audit_emitted=0` + set the flag after, an `idempotency_key=audit-integrity-{seq}` backstop for a crash between append and mark, and — critically — the quarantine insert is **`ON CONFLICT(seq) DO NOTHING`** (the `quarantine` table is NOT in `REBUILD_TABLES`, so a rebuild re-detects the same seqs; REPLACE would reset `audit_emitted=0` → a duplicate AIV). The persisted `reason` is a **structural descriptor** (the serde error string is discarded) so no row content reaches the audit trail (§15; the redactor on append is only the backstop). Healthy logs replay **byte-identically** to the strict path (rebuild-equivalence preserved — the degradable path must not change healthy behavior).

**Rule:** A read path that reconstructs persisted rows must **degrade, not crash**, on a bad row (§17): split *forward-compat* (unknown version → degraded-only, a newer binary folds it) from *integrity* (corrupt/unredacted → quarantine the raw row in a daemon-internal table, skip it, mark the offset degraded, **continue so the daemon starts**). Make the integrity event **loud** (a System-actor `AuditIntegrityViolation` per newly-quarantined seq, emitted post-`open` so replay stays append-free) and **exactly-once across restart AND rebuild** (`audit_emitted` + idempotency_key + `ON CONFLICT(seq) DO NOTHING`, with the quarantine table excluded from `REBUILD_TABLES`). Persist a **structural, content-free** reason (discard the serde string; the redactor is only the backstop). Pin the healthy-path with a byte-identical rebuild-equivalence test — the degradable path must never change healthy behavior.

## <a id="12"></a>12. Socket subscribe-SERVE — close-on-lag is the resync trigger; a subscribe connection is structurally single-writer

**Date:** 2026-06-10.
**Source slice:** 1.6d (socket subscribe-SERVE push — `93f70a5`; closes the ui live-subscribe path).

The broadcast subscribe-**SOURCE** (1.6b, publish-after-commit) reaches the socket here: on a `subscribe` request, `serve_connection` mints the broadcast `Receiver`, writes the ack on the main thread, then — **only if the ack succeeded** — spawns a single push thread owning a `try_clone`'d write half that streams matching `ProjectionDelta`s as `ServerFrame::subscription_push`. Minting the receiver **before** the ack is load-bearing: a broadcast only delivers to receivers live at send time, so a delta published right after the ack would be missed if you subscribed after — the receiver buffers from mint-time and the push thread drains it.

Two invariants this slice pins. **(1) Close-on-lag = the §9 resync trigger.** A pure `next_push_action(recv_result, subscribed) -> Push | Skip | Stop` classifier maps `Ok(matching)→Push`, `Ok(other)→Skip` (filter — not a gap for this subscription), `Err(Lagged)→Stop`, `Err(Closed)→Stop`. `Stop → shutdown(Shutdown::Both)` so the main read loop unblocks (EOF) → the client re-establishes (reconnect → re-subscribe → re-`get_projection`, a fresh consistent snapshot). **Lagged must NOT silently continue:** `ProjectionDelta` carries no seq/offset, so a gapped stream is undetectable by the client → its projection would silently diverge. Close-on-lag is what makes LESSON §9's "lagging subscriber resyncs, no wire frame, no CONTRACT bump" actually true — the close IS the resync trigger. Extracting the decision into a pure classifier pins it (incl. `Lagged→Stop`) deterministically, no timing flakiness (`run_push_loop_closes_connection_on_lag`: a pre-lagged cap-1/3-sent receiver → the client read-half sees EOF, never a gap). **(2) A subscribe connection is structurally single-writer.** Two concurrent writers on one socket (the ack/RPC-response writes on the main thread + the push thread) **interleave + corrupt the frame stream** (a HIGH, found + fixed + re-reviewed in-slice). Fix: subscribe is **terminal/dedicated** — main writes the ack, spawns the sole push thread only on ack-success, then the main loop is **read-only until EOF** (writes nothing); exactly one push thread per connection (bounded by the accept-loop cap). The ui must open a dedicated connection for subscribe (no multiplexing RPC + subscription on one connection — deferred with `subscription_id`). `getpeereid` stays first (rule #7; the push is post-auth/post-ack); the subscriber never back-pressures the writer (forbidden #3).

**Rule:** A push-stream over a shared socket must be **structurally single-writer** — make a subscribe connection terminal (write the ack on the main thread, spawn the *one* push thread only on ack-success, main loop read-only-until-EOF after) so the ack/RPC writes can't interleave with push frames (concurrent writers corrupt the frame stream). Mint the broadcast receiver **before** the ack (don't miss a just-after-ack delta). Drive the per-recv decision through a **pure classifier** (Push/Skip/Stop) so the policy is unit-testable without timing flakiness; **`Lagged → Stop → close the connection`** is the resync trigger (the client reconnects + re-`get_projection`) — never silently continue when the delta has no seq for the client to detect the gap. Subscriber never back-pressures the writer (forbidden #3); peer-auth stays first (rule #7).

## <a id="13"></a>13. The §15 redactor — entropy fallback + mask-in-place; quarantine-divert is the wired-but-MVP-unreached net; recall is a human gate

**Date:** 2026-06-10.
**Source slice:** 1.7 (redactor entropy fallback + quarantine-divert — L1 `c795668` + L2 `f807913`; CONTRACT_VERSION 0.13.0→0.14.0; the last Phase-1 slice). **Extended 2.0-SEC** (2026-06-11; measured recall/precision against a synthetic corpus + JSON-value detection, human-ruled Option B; engine `v2→v3`; daemon-internal, no CONTRACT_VERSION bump).

The 1.1 `PrefixRedactor` redacts-or-passes (token prefixes + PEM) and never quarantines. 1.7 raises recall (OQ-SEC-2) and adds the quarantine path. The detection now: known prefixes/PEM · **Shannon-entropy on `KEY=value`** values (≥20char/≥4.0 bits; base64/whitespace/quote-aware value span; **sub-run-scored** so a long low-entropy pad around a short secret can't dilute the average below threshold) · **bare high-entropy runs** (≥40char/≥4.5 bits — above ULID-ID length [≤31] and hex-SHA entropy [~4.0], so IDs/SHAs are spared). All three are masked **IN-PLACE** (replace the span with `[REDACTED]`), engine `prefix-entropy-v2`; the masking is a pure function of the payload (deterministic — golden-log-safe).

The key design call: **prefer mask-in-place over quarantine-divert whenever the secret span is boundable.** §15's "quarantine the event when a secret can't be safely redacted" is a *divert* — the original event is NOT persisted; a content-free `SensitiveOutputRedacted{original_event_type, reason, detector}` is recorded in its place (the gate then only ever sees Redacted/Unredacted; `RedactionStatus` stays frozen at 2). But divert is **destructive** (a false divert loses the whole event), whereas a false mask loses only the masked span — so for any *boundable* run, mask. The real `PrefixRedactor` therefore **never sets a quarantine signal** (it masks everything); the divert path is wired in `append()` + pinned by a `ForcesQuarantine` test redactor, but is **MVP-unreached** — a §15 analogue of the §17 quarantine (which only fires on real corruption). Mechanics that matter on the divert: a **namespaced** idempotency_key (`divert-{k}` — never collides with the original's key; retry-dedups), sensitivity **reclassified** to Internal (the record isn't the original's Secret), and a **fail-closed no-recurse guard** (a quarantine signal on an SOR event itself → `RedactionRequired`, never an infinite divert).

Recall has an inherent envelope: a statistical detector cannot catch JSON-`"key":"value"` short non-prefixed secrets (scoping to env-style `KEY=value` deliberately spares prefixed-ULID IDs/URLs), hex secrets at ~3.8 bits (indistinguishable from a git-SHA), or an adversarially split-and-padded secret. The §15 **fail-closed GATE invariant holds regardless** (no event persists `unredacted`; pinned by a property/fuzz test over the catchable set) — recall is about the heuristic, not the gate. Because the user made 1.7 BLOCKING + human-owned so recall can't *silently* drift, **the recall-envelope acceptance is a human gate**, not an orchestrator call; the primary secret control remains §15 rule #5 (secrets never in payloads — keychain-refs only), the redactor being defense-in-depth for accidental leaks.

**2.0-SEC measured + extended the envelope (human-ruled Option B, 2026-06-11).** A synthetic labeled corpus (correctly-shaped fakes, never real secrets — a *real*-secret corpus would itself violate rule #5; sentinel-guarded) + a pure precision/recall/FP-rate harness (`daemon/tests/redaction_recall.rs`; metric accessors are `Option<f64>` so an undefined cell is explicit not a silent 0.0/NaN; `catchable` is an **intrinsic per-sample label** — fixed at corpus-definition, never derived from redactor output — so the recall floor is non-circular and a real regression can actually fail it) turned the qualitative envelope into numbers: `prefix-entropy-v2` measured **recall_catchable 1.0 / precision 1.0 / FP-rate 0.0**, so the thresholds were **confirmed sufficient on the corpus** (not retuned — the named consts pinned with the measurement as justification). The human then ruled **Option B**: a third **JSON-`"key":"value"` value pass** (wire order **kv→json→tokens→PEM**) masking a high-entropy quoted value at the KV-confidence bar, **sub-run-scored** (a log-line's short words never trip it; whole-value entropy on prose would FP) and guarded by a **value-shape ID-allowlist** (`is_id_shape`: git-SHA / ULID / UUID by shape, not key-name — robust + avoids an unbounded maintenance surface), closing residual (a) for **≥20-char** JSON values at **zero precision cost** (FP held 0.0). Engine `v2→v3` (the recall bar moved → provenance must reflect it; free-string value → no CONTRACT bump; update every literal pin incl. the persisted-engine golden test). Composition is **additive** — a value is emitted verbatim or fully `[REDACTED]`, so a later pass never un-catches an earlier catch (no recall regression; security-reviewer-verified). **Option C (sensitivity-gated quarantine-bias) deferred** — the mask-in-place-over-divert preference above holds; a fast-follow only if real sensitive-payload traffic warrants arming the still-MVP-unreached divert. Surviving residuals are now **measured + accepted-and-owned**: (b) hex≈git-SHA (**irreducible by tuning** — can't lower the bar without masking real SHAs), (c) adversarial split-into-<20-char, (<20ch-a) sub-20-char JSON values; the corpus is a **living regression fixture** (recall-floor/FP-ceiling pinned, ratchets up only).

**Rule:** Secret redaction = high-recall prefix set + Shannon-entropy on `KEY=value` (base64-aware value span, **sub-run-scored** vs padding-dilution) + **JSON-`"key":"value"` value masking at the KV bar (sub-run-scored, value-shape ID-allowlist)** + bare-run masking, all **mask-in-place** (deterministic; engine-version-stamped). **Prefer mask-in-place over quarantine-divert** when the span is boundable (a false divert loses the whole event; a false mask loses only the span) — so the real redactor never diverts; the quarantine→`SensitiveOutputRedacted` divert is the §15 "can't-safely-bound" net, **wired + ForcesQuarantine-tested but MVP-unreached** (content-free record, namespaced dedup key, sensitivity-reclassified, fail-closed no-recurse — mirroring the 1.6c AIV). A statistical detector has an inherent recall envelope; the §15 **gate** holds regardless (pin it with a property/fuzz test), and **recall acceptance below the §15 bar is a human gate**, not a silent carry-forward. **Measure the detector against a synthetic-but-representative labeled corpus** (never commit real secrets; intrinsic per-sample `catchable` labels; `Option<f64>` metrics) and **pin recall-floor + FP-ceiling as regression consts that only ratchet up**; **extend recall via a different mechanism, not a looser threshold** (entropy can't separate a hex secret from a git-SHA — extension = JSON-value scoping + a **value-shape ID-allowlist**, sub-run-scored, at zero precision cost), bumping the engine version whenever the bar moves. Keep the *measurement* honest — retain an accepted-miss sample (e.g. a <20-char JSON value) in the corpus so the per-category number can't silently over-claim full closure.

## <a id="14"></a>14. The binding contract = `ARCHITECTURE.md` Appendix A + the DATA_MODEL DDL; the domain drafts are origin/rationale — freeze the reconciled set, flag drops as Findings

**Date:** 2026-06-11.
**Source slice:** 2.1a (§6.2 action-contract freeze — `feat(shared)`; the first freeze of a model surface whose `docs/domains/` draft is richer than the binding contract).

The §6.2 models exist in three places that DISAGREE: `docs/domains/ACTION_GATEWAY.md` sec 9.1-9.9 (the original DRAFT — rich, TypeScript-ish), `ARCHITECTURE.md` Appendix A (the reconciled BINDING rows), and `docs/planning/DATA_MODEL.md` sec 2.9 (the DDL, where one exists). The AG-draft `ActionRequest` carries `policyContext`/`preconditions`/`dryRunRequired`/`confirmationPreference`/`source`/`intent` + session/team/workflow context that Appendix A + the DDL drop (context lives on the event envelope's typed columns; policy/preconditions → later phases; expiry → `Approval`). The freeze authority is **Appendix A + the DDL**, not the richer draft — arch-finalize reconciled the draft into the binding rows; the draft is origin/rationale only. Two sub-rules surfaced: **(a) DDL-anchored rows are exhaustive.** Where Appendix A matches a `DATA_MODEL` `CREATE TABLE` (ActionRequest≡`action_requests`, Approval≡`approvals`), the DDL is the complete field set → the AG extras are genuinely reconciled out; freeze exactly the DDL. **(b) An abbreviated standalone Appendix-A row (no DDL) → consult the draft for completeness.** ActionPlan (no `action_plans` table) + the crammed one-line ResourceRef/EvidenceRef/PolicyDecision row abbreviate; freeze the load-bearing fields the row abbreviated — `EvidenceRef.label` is AG-**required** + **irreducible** (the Brain's natural-language evidence text, not reconstructable from `{type,id}`) → frozen; `ResourceRef.displayName` is id-resolvable → deferred. Fields whose SHAPE a later slice owns (RollbackPlan→2.4, ApprovalConstraint + PolicyDecision.{requiredApprovals,constraints,saferAlt}→2.2, the 6 typed ActionPreview previews→2.3, structured ActionError→2.4) defer as typed-additive-later — **non-breaking pre-consumer** (no serialized data + no consumer code exists yet, so adding even a required field later doesn't break). Every drop is a Step-9 cross-doc reconcile (the Appendix-A row updates to the as-frozen set + a deferred-→owning-slice marker), NEVER silent. The PolicyDecision stub `{status, reasons}` is KEPT (not deferred whole) because 2.1b's pipeline returns one before 2.2's engine exists; 2.2 extends it additively.

**Rule:** When freezing a model surface, the binding contract is `ARCHITECTURE.md` Appendix A + the `DATA_MODEL` DDL — the `docs/domains/*.md` draft is origin/rationale and is usually richer. DDL-anchored Appendix-A rows are exhaustive (the draft's extras are reconciled out — freeze the DDL). A standalone abbreviated Appendix-A row (no DDL) → consult the draft + freeze the fields it abbreviated that are load-bearing (required/irreducible → freeze; id-resolvable/convenience → defer). Defer fields whose shape a later slice owns as typed-additive-later (non-breaking pre-consumer). **FLAG every AG-vs-Appendix-A drop at Step-9** (reconcile the Appendix-A row + add a deferred-→owning-slice marker) — never silently drop or silently re-add; if a dropped field is load-bearing for the wire contract, it's a Finding.

## <a id="15"></a>15. Freezing a new model surface through schemars/§5.0 — the three emission gotchas that silently break the freeze

**Date:** 2026-06-11.
**Source slice:** 2.1a (§6.2 action-contract freeze — `feat(shared)`).

Three schemars-1.x / §5.0 quirks each silently break a new-model freeze; all hit at 2.1a and recur at every future model-surface freeze (the §6.3 catalog at 2.2, the §9.1 harness types at 3.1, the §13.1 Brain mapping at 8.1). **(1) `$defs` are keyed by the BARE type-name → a cross-module same-name collision SILENTLY DROPS a type.** schemars 1.2 keys the emitted `$defs` by the bare type identifier (no module path), so `actions::ActionRequest` (the new model) collided with the frozen `status::ActionRequest` (the 15-value status enum) — the enum won and the MODEL was silently absent from the schema (66 defs emitted, model missing, **no error**). The §2.5-seam schema-snapshot test catches it (a model's expected field-set has no `$def`); fix by disambiguating with `#[schemars(rename="…")]`. Here the human-ruled (Option B) value-preserving reconcile renamed the 2 status enums' `$defs` to their pre-existing Rust aliases `ActionRequestStatus`/`ApprovalStatus`, freeing the canonical bare names for the models — a `prj_→eprj_`-class frozen-**NAME** reconcile (value-sets untouched; the wire VALUE is the contract, names are idiomatic handles per status.rs). **(2) the 3-way verify reflects STRING enums only.** `verify.py` builds value-sets from Zod `z.enum` / Pydantic string-consts, so a Rust enum emitted as an INTEGER enum-array (e.g. `RiskLevel`'s `enum:[0,1,2,3,4]`) or a format type appears in the Rust schema but NOT in Zod/Pydantic → 3-way FAIL. Emit **bounded scalars** instead: `RiskLevel` → `{type:integer, minimum:0, maximum:4}` (manual `JsonSchema` via `json_schema!`; same wire 0-4, same Rust `ALL`/`Ord`/reject-out-of-range), `Timestamp` → `{type:string, format:date-time}` (transparent newtype). AND a discriminated union's inline value-set must be lifted to a **named top-level enum** (the `{kind, actor?}` + `RequiredApproverKind` pattern) — an inline union makes Pydantic's reflection diverge from Zod's → FAIL. **(3) optionals: NO `skip_serializing_if`.** The schema-snapshot test asserts a STABLE field-name set; with `skip_serializing_if="Option::is_none"` (the events.rs pattern) an optional disappears when None → the snapshot is unstable. For snapshot-pinned models, serialize optionals as explicit `null` (omit the skip attr) so the field-name set is fixed regardless of None/Some.

**Rule:** Freezing a new model surface through schemars/§5.0 has three silent traps — (1) `$defs` are keyed by **bare type-name** (no module disambiguation): a same-named type across modules COLLIDES and one is silently dropped → disambiguate with `#[schemars(rename)]` (a value-preserving `$def`-NAME change is a frozen-reconcile like `prj_→eprj_`), and rely on the schema-snapshot test to catch the dropped model; (2) the 3-way verify reflects **string enums only** → emit integer/format types as **bounded scalars** (`{integer,min,max}` / `{string,format:…}`), never enum-arrays, and lift a discriminated union's value-set to a **named top-level enum** (`{kind,…}`) — else Pydantic/Zod reflection diverges and the verify FAILs; (3) for snapshot-pinned models serialize optionals as explicit `null` (**no `skip_serializing_if`**) so the field-name set is stable. All three recur at every model-surface freeze (2.2 catalog, 3.1 harness, 8.1 Brain mapping).

## <a id="16"></a>16. The INV-SEC-1 chokepoint — per-transition atomic {row + event} on the write-actor, append_in_txn, the §15 dual-gate, staged pipeline with injectable stubs

**Date:** 2026-06-11.
**Source slice:** 2.1b (the single-action Action Gateway pipeline — L1 `1907439` + L2 `9dbb60a` + L3 `2c83b8f`; CONTRACT 0.16.0; the first state-mutating code path).

The Gateway is the single audited mutator (INV-SEC-1). Six conventions held it under security review at every layer. **(1) Per-transition atomic {durable-row write + authoritative-event append} on the write-actor → fail-closed.** Each Gateway state transition writes its `action_requests`/`approvals` row AND appends its `ActionExecution*` event in ONE txn on the single write-actor; an event-write failure rolls back the whole txn → no row, no ack, no side effect (§15 fail-closed-on-audit-write / §17-row-1 / §7.2 "row + projection in one txn"). The separate-txn alternative (row then event, or vice-versa) is rejected: a half-committed pair is the exact inconsistency §15 forbids. **(2) `append_in_txn` extraction** — to let the Gateway compose the append INTO its own txn without weakening it, extract the §15-gate + seq + `INSERT INTO events` + the 1.2 in-band fold + the 1.3 outbox into a txn-scoped `append_in_txn(&tx,…)`; keep the public `append(&mut self)` a **behavior-identical wrapper** (its own IMMEDIATE txn), re-verified byte-identical by the 1.1 tests. The §15 gate MUST stay strictly BEFORE the INSERT; **exactly ONE `INSERT INTO events` in the whole daemon** (events emit ONLY via the gateway-txn append handle — the §14 invariant test pins "no executor reachable except via the pipeline; an event row exists for every mutation"). **(3) The §15 dual-gate — events-via-append-gate AND rows-via-redact_row.** Rule #3/#4 ("secrets never in ROWS; the SAME redactor gates persist+embed+sync") covers ALL caller-supplied registry-row payload, not just events: `inputs_json` + `resource_refs_json` pass the §15 Redactor before the `action_requests` INSERT (no-op for keychain-ref inputs; masks a rule-#4-violating secret — defense-in-depth, since agent/Brain proposers are UNTRUSTED by design). `preview_json` joins at 2.3 (when real preview content exists). **(4) Staged pipeline with injectable stubs** (`Box<dyn PolicyEngine>` / `Box<dyn ActionExecutor>` + Clock/IdGen) — the chokepoint + its events are test-first NOW: policy stub = require-approval-for-ALL (risk-blind → nothing auto-executes pre-2.2; the requester-supplied `risk_level` is RECORDED but NOT trusted for the decision), executor stub = no-side-effect "would-execute" → succeeds. 2.2 swaps policy, 2.3 swaps executors, 2.4 adds fencing/stale-precondition/crash-reconcile — each behind a marked seam. **(5) The execute phase is a DISTINCT seam from `approve`** (not inlined): for the stub it can run synchronously, but it MUST be separable so 2.3's slow real executors (git CLI / octocrab) run OFF the write-actor thread — a slow executor inside an approve write-actor command starves EVERY daemon write. **(6) R-9 legal-edge guards** (`gateway::{request,approval}::can_transition`, fail-closed default-deny): the ONLY routes to `executing` are `awaiting_approval→approved→queued→executing` (the human gate) or `policy_decided→queued` (the policy-allow auto-path, gated on `PolicyDecision=allow`; unreached pre-2.2) — no edge skips both gates. `ActionRequest::Denied` is a sink (terminal — the §5.1 freeze omitted it; reconciled 2.1b).

**Rule:** The mutation chokepoint = **a per-transition atomic txn {durable-row write + authoritative-event append via the §15 gate} on the single write-actor, fail-closed** (event-write fail → rollback → no row/ack/side-effect). Compose the append via an extracted **`append_in_txn(&tx,…)` primitive** (public `append` stays a byte-identical wrapper; the §15 gate strictly before the INSERT; exactly ONE `INSERT INTO events`; events emit ONLY via the Gateway's append handle — pin with a §14 "no-executor-except-via-pipeline + event-per-mutation" invariant test). The §15 redactor is a **dual-gate**: events via the append-gate AND **registry-row payload (`inputs_json`/`resource_refs_json`/…) via a redact-before-INSERT** (rule #3/#4 — all caller-supplied row payload, defense-in-depth on untrusted proposers). Build the pipeline with **injectable stubs** (policy→later/require-approval-for-all-risk-blind, executor→later/no-side-effect, fencing→later) so the chokepoint + events are test-first before the real stages; the **execute phase is a distinct seam from approve** (so real slow executors run OFF the write-actor); R-9 **legal-edge guards** (fail-closed default-deny) make execution reachable only through the approval/policy-allow gates.

## <a id="17"></a>17. A projector reads IMMUTABLE fields from a sibling durable-registry row, but MUST derive MUTABLE state from the EVENT — never the row's current value (rebuild-determinism)

**Date:** 2026-06-11.
**Source slice:** 2.1c L1 (the `proj_approval_queue` projector — the Human Input Queue read model — + the approval-queue subscribe-delta).

A read-model fold sometimes needs fields that aren't on the event's own payload/envelope (here: the queue row's `risk_level`/`requester_*`/`project_id`/`expires_at`, which live on the `action_requests`/`approvals`/`action_plans` rows, not on the lean `ActionApprovalRequested{approval_id}` payload). A projector MAY read them from the **sibling durable-registry table in the same txn** (the fold runs INSIDE the gateway txn that just wrote those rows — generalizes the `object_refs`→graph precedent of LESSON §4 from a sibling *projection* table to the gateway's durable *registry*). **But the split is load-bearing for rebuild-determinism:** the durable-registry tables (`action_requests`/`approvals`/`action_plans`) are **NOT** in `REBUILD_TABLES` (they're authoritative state, not derived) — so a `rebuild()` reads them at their **FINAL** value, not their value at the historical event's seq. Therefore a projector may read only **IMMUTABLE** columns from them (risk/requester/project/created — identical whenever read → rebuild-safe), and MUST derive the **MUTABLE** column (the approval `status`) from the **EVENT TYPE** (`ActionApprovalRequested`→awaiting; `ActionApproved`/`Denied`/`Expired`→that state) — never from the row's current `status` (which during rebuild is the final state → the projector would stamp "succeeded" onto every historical row → non-deterministic / wrong queue). Corollary for the **subscribe-delta**: the Gateway's mutation commands bypass `Command::Append`, so they never hit `deltas_for_append` — thread a `ProjectionDelta` **accumulator** through the pipeline (public 2.1b signatures kept stable via thin delegating wrappers → zero test churn) and **publish post-commit** in the `run_actor` gateway arms (publish-after-commit; a rolled-back txn publishes nothing — forbidden #3 / LESSON §9 broadcast pattern extended to the gateway path), keyed by the touched `approval_id`.

**Rule:** A projector may read **IMMUTABLE** fields from a sibling durable-registry row (same txn — the registry tables aren't truncated on `rebuild`, so they read at FINAL value), but MUST derive every **MUTABLE** field from the **event type**, never the row's current value — else `rebuild()` is non-deterministic (stamps the final state onto every historical row). For gateway-event subscribe-deltas (the mutation commands bypass `Command::Append`): thread a delta accumulator through the signature-stable-wrapped mutation methods + publish post-commit, keyed by the touched id. `pin: daemon/tests/gateway.rs (rebuild-equivalence fold) + daemon/tests/runtime.rs (the publish-after-commit delta)` · `accepted: the "mutable-from-event-not-row" rule is semantic — not grep-enforceable; the rebuild-equivalence test is the regression guard`

## <a id="18"></a>18. A bundled ActionPlan is a durable GROUPING (a thin `action_plans` table + a nullable `plan_id` FK), NOT a new event type; one whole-plan atomic txn; the plan-level approve-all approval generalizes the single-action tables to the frozen `Option`

**Date:** 2026-06-11.
**Source slice:** 2.1c L2 (`submit_action_plan` + MIGRATION_8 + `PlanAck`; CONTRACT 0.17.0).

The O-3 bundled `ActionPlan` is modeled as a durable **grouping over `action_requests`**, NOT a new event type: a thin `action_plans` metadata table + a nullable `action_requests.plan_id` FK (single action = `plan_id` NULL). `submit_action_plan` is **ONE atomic gateway txn over the whole plan** (fail-closed — a step failure rolls back the ENTIRE plan: no `action_plans` row, no partial steps, no ack); each step is a full `ActionRequest` emitting its own per-step `Action*` events, so the plan is reconstructable by the `plan_id` join — **no per-event plan column** is added to the frozen envelope (the plan tie on a plan-scoped event is `correlation_id = plan_id`, the `{approval_id}` payload unchanged → no event-contract churn). Approvals open per `approval_mode`: **StepByStep** = one per-step approval; **ApproveAll** = ONE plan-level approval (`action_request_id` NULL, `scope=Plan`) over the non-critical steps + a SEPARATE per-step approval for each **critical (risk-4)** step — **"critical never in approve-all" (§6.2/§11.5) is the safety pin** (security-reviewer-verified). The plan-level approval (action_request_id NULL) requires the 2.1b `approvals` + `proj_approval_queue` tables to allow `action_request_id` NULLABLE + a `plan_id` — this is **CONFORMANCE to the already-frozen §6.2 `Approval.action_request_id: Option`**, not a new contract decision (the 2.1b `NOT NULL` was a single-action shortcut; SQLite needs a table-rebuild migration to loosen it; the projection table just DROP+CREATEs + resets its offset, the projector being new). A deferred/policy-assigned `approval_mode` (**Blocked**) is **REJECTED fail-closed at submit** (a policy-assigned outcome, not a valid proposer-submitted mode — never leave a phantom `awaiting_approval` step with no approval object, which `approve()` would `NotFound` + the queue would render unactionable); **Mixed → StepByStep** (safe over-approximation — more gates, never fewer; the §6.2 mode set isn't fully realized until 2.2).

**Rule:** Model a bundled plan as a **durable grouping** (a thin `action_plans` table + a nullable `plan_id` FK), NOT a new event type — one **whole-plan atomic txn** (fail-closed), per-step `Action*` events + the `plan_id` join reconstruct it; a plan-scoped event ties via `correlation_id = plan_id`, not a new envelope column. A plan-level **approve-all** approval (`action_request_id` NULL, `scope=Plan`) covers non-critical steps only (critical → its own approval; **never in approve-all**) and requires generalizing the single-action tables to the frozen `Option` — **conformance, not a fresh contract**. Reject a deferred/policy-assigned mode (**Blocked**) fail-closed at submit rather than leave a phantom `awaiting_approval`; over-approximate **Mixed → StepByStep**. `pin: daemon/tests/gateway_plan.rs (whole-plan atomicity + approve-all critical-exclusion + Blocked-reject); security-reviewer every Gateway slice`

## <a id="19"></a>19. Risk is CATALOG-authoritative, never the requester's claim; the first no-human-approval path (risk-0 allow auto-queue) is gated strictly on allow+catalog-risk-0 with a defense-in-depth re-gate

**Date:** 2026-06-11.
**Source slice:** 2.2 (the catalog-driven policy engine — L1 the §6.3 catalog contract · L2 `CatalogPolicy` · L3 risk-0 auto-execute + the §11.5 migration).

The §6.3 `ActionTypeCatalog` is the AUTHORITATIVE per-type risk source: `CatalogPolicy::decide` resolves each action's risk from `catalog::lookup(action_type)` — **NEVER the requester-supplied `req.risk_level`** (recorded-not-trusted; a proposer can under-claim). The recorded risk is **reconciled (overwritten) to the catalog risk at submit**, so `action_requests.risk_level` + the `ActionRequested` event carry the authoritative risk (audit integrity). An uncatalogued `action_type` → **deny** (`PolicyDenied` → honest `policy_denied` IPC, distinct from the unrouted `downgrade`/`needs_more_context` → `UnsupportedPolicyDecision`), never a default-allow (fail-closed lookup). 2.2 introduces the **FIRST no-human-approval execution path**: a risk-0 `allow` action auto-queues (`policy_decided→queued→execute`, NO approval row, NO `ActionApprovalRequested`), gated STRICTLY on `PolicyDecision==allow` AND catalog-risk-0 — with a **defense-in-depth re-gate**: the pipeline re-verifies catalog-risk-0 on the POST-reconcile authoritative risk EVEN AFTER `decide` returns `allow`, so a policy bug can't open a non-zero auto-queue (pinned by an adversarial `AllowAllPolicy` double that returns `allow` for everything — the re-gate still blocks risk≥1). The executor runs two-phase OFF the write-actor txn (LESSON §16). The §14 invariant extends: the ONLY no-approval execution path is `policy_decided→queued` on allow+risk-0. The §11.5 critical-exclusion (LESSON §18) **migrates onto catalog risk**: each plan step's persisted risk is reconciled to the catalog, and approve-all's `is_critical` (both the approval-opening AND the cascade SQL) keys off the catalog-authoritative risk — a step claiming risk-0 but catalog-risk-4 is excluded; an uncatalogued plan step rejects the WHOLE plan (#11, fail-closed — the unknown-type door to the bypass). **The risk-0 set is verified mutation-free** (only read/inspect/propose-only types auto-execute).

**Rule:** Risk is **catalog-authoritative**, never the requester's claim — `decide` reads `catalog::lookup`; the recorded risk is overwritten to catalog risk at submit; an uncatalogued type → `PolicyDenied` (fail-closed, never default-allow). The first no-human-approval path (risk-0 `allow` auto-queue) is gated strictly on `allow` AND catalog-risk-0, with a **defense-in-depth re-gate** that re-verifies catalog-risk-0 even when the policy returns `allow` (a policy bug can't open a non-zero auto-queue; pin with an adversarial allow-all double). EVERY risk-keyed safety gate (the §11.5 approve-all critical-exclusion, the auto-execute gate) keys off the catalog risk, never the claim. `pin: daemon/tests/policy.rs (no-non-zero-auto-queue + adversarial AllowAllPolicy + catalog-critical-exclusion + reject-uncatalogued); security-reviewer every Gateway slice`

## <a id="20"></a>20. The Gateway executor/preview/idempotency FRAMEWORK — the catalog's named bindings realized as seams, not side effects; idempotency = one-way hash of RAW inputs (§15 precedent); the §7.2 executor read-source split

**Date:** 2026-06-11.
**Source slice:** 2.3 (L1 idempotency+dedup `f4ebeaf` · L2 preview framework `c244294` · L3 executor framework `957fe65`; 229 workspace green; security-reviewer CLEAR ×3; CONTRACT held 0.18.0 — all daemon-internal; +sha2).

2.3 realizes the §6.3 catalog's previously **named-only** `preview_class`/`executor`/`idempotency_formula` as a **FRAMEWORK** — and the load-bearing insight is that *the trait is the seam, not the side effect*. The `git/`/`integrations/`/`harness/`/`brainclient/` modules don't exist yet (Phase 3/5/7/8), so 2.3 **cannot** build real git2/octocrab/session/Brain side effects; it builds the `ActionExecutor` trait (`validate`/`preview`/`execute`/optional-`rollback`) + `CatalogExecutor` dispatch by `ExecutorKind` to **side-effect-free per-namespace structured stubs** (grep-confirmed no process/fs/git2/network) + the preview-class dispatch + idempotency derivation/dedup. Each later phase swaps its namespace's stub arm for the real adapter behind the **unchanged** trait. INV-SEC-1 holds: the executor stays reachable ONLY via the 3 gated Gateway seams (post-approval execute, plan-approve cascade, risk-0 auto-execute) — `CatalogExecutor` adds no new mutator/bypass. `validate` enforces the catalog `requires_resource_refs` fail-closed (Err→`ActionFailed`, no panic; **risk-0 ≠ no-refs** — `git.status`/`git.diff`/`code.open_file` are risk-0 AND require refs, so validate correctly rejects them ref-less).

**§15 idempotency precedent (lead-ruled Option A, away-authority):** `action_requests.idempotency_key` = a **one-way SHA-256 of the RAW (unredacted) inputs** (catalog `FromInputs`/`NaturalResourceRef`/`None`; reuses the existing `ux_action_idem` UNIQUE index — no migration). It **by-design bypasses the §15 `inputs_json` row-redactor** because dedup correctness requires raw-input determinism (hashing redacted inputs would false-dedup two actions differing only in a secret → the 2nd silently dropped — a correctness + availability bug). Rule-#4-safe: a one-way hash is a **fingerprint, not the secret**; the keychain-refs-only convention (real secrets never inline) is the load-bearing control. The key is **catalog-derived, never requester-supplied** (recorded-not-trusted — a proposer-chosen key can't force a collision-to-suppress a victim's action; pinned). Dedup-on-hit = **idempotent-replay** (return the original action; at-most-one). C/HMAC-with-a-keychain-key is the documented hardening, trigger = a real low-entropy-inline-secret threat OR `idempotency_key` data leaving the local trust boundary (off-machine sync).

**§7.2 executor read-source split (pinned, pre-existing — no behavior change):** the **risk-0 auto-execute** path runs the executor off the **in-memory reconciled `ActionRequest`** (no redacted re-read); the **approve** path runs off the **durable row** (`request::load`), canonical per §7.2 (in-memory inputs are gone at approve-time, possibly post-restart). Pinned by a `RecordingExecutor` spy that sees the RAW secret on the auto path vs REDACTED inputs on the approve path; the row stays redacted at rest throughout. Real-input-fidelity for the approve path is owned when the real executors land.

**Preview framework:** `preview_action` dispatches by catalog `preview_class` (Command/Diff/Git/Api/Session/Workflow/Rollback) → render into the **FROZEN flat `ActionPreview`** (no new fields, no CONTRACT bump). Preview-impossible sets `cannot_preview_reason` (naming the owning phase) + escalates the **preview ENVELOPE** risk only (+1, cap 4; **never** `action_requests.risk_level` / policy / approval / the auto-execute re-gate). In 2.3 every preview is impossible (no adapter) — a **documented TRANSIENT** each phase drops as its real dry-run lands; `preview_json` persists THROUGH the §15 redaction gate (uniform rule-#3, no-op today).

**Rule:** Realize a catalog/contract binding as a **framework with side-effect-free per-namespace stubs** when the real adapters live in later phases — the trait is the seam (each phase swaps its stub arm), never a premature real side effect; keep INV-SEC-1 (executor only via the gated seams; grep-confirm side-effect-free). A dedup/fingerprint column derived from raw, pre-redaction inputs must be a **one-way hash** (fingerprint-not-secret; rule-#4-safe given keychain-refs-only) + **catalog-derived not requester-supplied**. Execute off **in-memory** inputs where available (auto path); the **durable redacted row** is canonical where they're not (approve path) — §7.2. Risk-escalation on preview-impossible is **envelope-only**, never the gating risk. `pin: daemon/tests/executor.rs (idempotency determinism/dedup/requester-override + preview dispatch/impossible-escalate/persist + executor ExecutorKind-dispatch/validate-fail-closed/§7.2-split-spy/reach-pin); security-reviewer every Gateway slice`

**[4.0b-R1a refinement — the executor seam became a registration registry, daemon-internal, no CONTRACT change].** When a real adapter (or a cross-track consumer) needs to *register* its per-namespace executor without editing sealed `gateway/`, evolve `CatalogExecutor` from a unit-struct uniform-stub into a **registry** (`handlers: HashMap<ExecutorKind, Arc<dyn ActionExecutor>>` + `register(kind, handler)`) that dispatches `execute`/`rollback` to the registered handler **else today's structured stub** — *incremental*: a namespace goes live exactly when its handler registers; everything unregistered is byte-for-byte unchanged (behavior-preserving — the production wiring registers NO handler until a real caller lands). **The INV-SEC-1 `Adjudication` fail-closed guard + the catalog `requires_resource_refs` precondition run BEFORE the handler dispatch** — an adjudication-only action never reaches a handler even if one is wrongly registered (the guard is execute-time-only; `register()` inserts unconditionally, so the test that registers an Adjudication handler genuinely proves the *execute* guard fires — a `register()`-side `debug_assert` would panic the test or make it vacuous). The seam stays **daemon-internal** (no `shared/` surface, no CONTRACT bump). A `rollback`-side symmetric guard is deferred (triply-unreachable: adjudication never executes → never rolls back, no production rollback caller exists; security-reviewer NO-GAP). `pin: daemon/tests/executor.rs (register-dispatch + unregistered-stub + incremental + Adjudication-refused-before-dispatch-even-if-registered + precondition-survives + rollback-delegate)`

## <a id="21"></a>21. The §17 failure-mode safety capstone — deterministic safety LOGIC + seams (fault-injection + fake-clock + fake oracle), fail-closed/fencing/stale-precondition/crash-reconcile; the cfg-gated fault-injection Cargo feature

**Date:** 2026-06-11.
**Source slice:** 2.4 (the §17 capstone — L1 contract-freeze `89bf300` [CONTRACT 0.19.0] · L2 fail-closed `8ea9e20` · L3 fencing `daed666` · L4 stale-precondition `d779671` · L5 crash-reconcile `37cfa97`; 247 workspace green; security-reviewer CLEAN ×5).

The §17 safety behaviors are built as **deterministic LOGIC + seams** (the same framework-with-seams posture as LESSON §20's executor) — the real external re-reads (git2 worktree / octocrab PR / session state) land with their adapters (Phase 3/5/7/8); 2.4 builds the seams + tests them via a **fault-injection hook + a fake `Clock` (`StepClock`) + a fake `PreconditionOracle`**.

- **The §14 fault-injection hook is a cfg-gated `fault-injection` Cargo FEATURE — NOT `cfg(test)`** (integration tests link the lib WITHOUT `cfg(test)`, so a `cfg(test)` hook is invisible to them); enabled for the daemon's own test targets via the **self-dev-dependency idiom** (`[dev-dependencies] nexusopsd = { path=".", features=["fault-injection"] }`). It compiles OUT + is un-armable in `cargo build`/`run`/release (verified empirically: `cargo tree -e features` absent from default; `nm` finds no fault symbols) — a genuine security property (no fault-injection surface ships). A checkpoint never ships without its firing site (`FaultPoint::{TerminalEventWrite, BeforeExecutingTxn, BeforeTerminalTxn}`).
- **Fail-closed on audit-write** extends to the EXECUTE completion: `execute()` splits into txn-A (Queued→Executing+ActionStarted, COMMITS — makes `executing` durable for crash-reconcile) → executor OFF the write-actor → txn-B (terminal event). A txn-B write failure → rollback → stays `executing`, NEVER acked succeeded. `side_effect_applied` (on `ExecutionOutcome::Succeeded`) + txn-B fails → txn-C `ActionPartiallySucceeded` (the loud record IS the alert; not the seq-keyed `AuditIntegrityViolation`); txn-C also fails → `AuditWriteFailed`. Fail-closed is **UNIFORM/risk-agnostic** (over-satisfies the §17 "risk≥1" scoping — not an INV-SEC-1 mandate, since risk-0 is mutation-free).
- **Record-then-throw (Option B) for the safety-refusal surfaces** (fencing L3, stale-precondition L4): the `ActionFailed{…}` txn COMMITS before the typed `Err` propagates (the durable failed state survives) → the §6.4 code distinguishes the §11.5 cards (`fencing_conflict` never-auto-resolved [rule #6] vs `precondition_stale` re-approvable). Distinct from an executor-Failed (a normal outcome → Ok ack): a safety REFUSAL is a typed Err. Fencing = acquire(owner=action_request_id)+bind-token+`validate_held` before execute; `Held{other}`→fence, `Held{self}`→idempotent-renew. Stale-precondition = the `PreconditionOracle` seam (Null default) AFTER fencing/BEFORE execute.
- **Crash-reconcile** (`reconcile_orphans` in `bootstrap::cold_start`) scans `status IN ('executing','queued')` (plan_id-AGNOSTIC — single + N-orphan cascade) → terminal + missing event. **The dedup-key split is load-bearing:** `queued` (never ran, no side effect) CLEARS the idempotency_key (safe to re-submit — closes the permanent-dedup re-run lockout); `executing` (maybe-applied) → `unknown_outcome` + KEEPS the key (dedup-protects a double-run).

**Rule:** Build §17-class safety behaviors as **deterministic logic + seams** verified by a fault-injection hook + fake clock + fake oracle; the fault hook is a **cfg-gated Cargo feature (via the self-dev-dependency idiom), not `cfg(test)`** (so integration tests see it) + compiled-out/un-armable in release. **Split the completion txn** so `executing` is durable (fail-closed → crash-reconcile). **Record-then-throw** for safety-refusals (commit the `ActionFailed` before the `Err`; distinct §6.4 codes for distinct §11.5 cards — never collapse never-auto-resolved fencing into re-approvable stale). Crash-reconcile keys the **dedup-key clear/keep on whether a side effect could have happened** (queued-clear / executing-keep). `pin: daemon/tests/recovery.rs (fail-closed/audit-fully-broken + fencing/renew + stale-precondition + crash-reconcile/cascade/Q6-key); security-reviewer every Gateway slice`

---

## <a id="22"></a>22. Perf-budget benchmarks calibrate on the AS-BUILT path, not a Phase-0 proxy — and run at their own cadence (never per-slice RED/GREEN)

**Date:** 2026-06-12.
**Source slice:** 2.5 (the §18 event-write/reader benchmark `34ec19c`, brief 038 — surfaced a Finding → **USER-RULED Option A** re-baseline) + 2.6 (`c9a0dd2`; CI wiring). `/phase-exit 2` CLEAR.

A perf-budget benchmark exists to catch a regression in a REAL production path — so it must measure that path, under a model that reflects how the path is used, at a cadence where timing is trustworthy. 2.5 got all three wrong-by-inheritance and the benchmark itself surfaced it.

- **Measure the AS-BUILT entry, not a Phase-0 proxy.** The §18 CI guards (write p95 < 30 ms / p99 < 75 ms / ≥ 1,500 commits/s) were calibrated on the 0.4 spike's **raw 1-transaction sqlite append** (5.35 ms) — a proxy for the write budget *before the Gateway existed*. The AS-BUILT submit (`Gateway::submit_action` → `CatalogPolicy` → `CatalogExecutor` → write-actor) is **3 commits × ~8–12 statements** (event INSERT + §15 redactor-v3 scan + in-band projections incl. the audit-projector FTS + outbox + object_refs + the row); a risk-0 submit = 3 commits (events/submit = 3.00 confirmed). There is **no production path that does a 5.35 ms bare append** — the redactor + in-band projections are mandatory on every commit — so the proxy-derived guards were structurally unreachable. The benchmark must drive the real entry; retire the proxy numbers.
- **Pick a DEFINED load model; the guard reflects it.** Realistic (non-saturating; N=20 sources with think-time) submit p95 ≈ **6 ms ≪ the 100 ms SLO**; the saturating closed-loop (zero-think, 30k events) hits ≈ **122 ms** — a *pathological worst case, not a realistic-use bound*. USER-RULED Option A gates the **realistic** model (p95 < 25 ms / ≥ 1,000 ev/s, ~4× margin) and keeps the saturating numbers **reported-not-gated**. The CI guard sits TIGHTER than the user SLO, at measured-baseline + margin (§18's own philosophy) — so a regression surfaces long before the ceiling.
- **Match the throughput unit to the guard's basis.** events-committed/s `==` commits/s (1 event/commit) IS the §18-basis unit; **submits/s** (≈ events/s ÷ 3) against a floor calibrated for raw appends would *false-fail*. Per-submit p95 is the user-facing latency unit. The asymmetry (latency asserted on the full submit, throughput on events/s) is **justified, not inconsistent**: the latency guard carries 4× headroom that absorbs the 3-append submit; the throughput floor sits too close to its basis to absorb a 3× unit shrink.
- **Run at the benchmark's OWN cadence.** `[[bench]] harness=false` keeps it OFF `cargo test --workspace` (timing assertions FLAKE in a per-slice RED/GREEN loop — and a benchmark isn't a unit test). The `/phase-exit` perf row + a `nightly.yml` `cargo bench` run it; a `BENCH_CEILING` env gates the heavy N=100/@1M sweep to nightly. The §18 anchor carries the `(benchmark)` spec-lint waiver class (the benchmark IS the coverage; no `spec(§18)`-tagged unit test). Watch for self-inflicted measurement artifacts (2.5's first reader probe was a continuous no-sleep full-scan that starved WAL autocheckpoint + tanked throughput 6× — pace + scope the probe).

**Rule:** A perf-budget benchmark drives the **REAL production entry** (not a Phase-0 raw-store proxy) under a **defined load model** (realistic non-saturating for the gated guard; saturating as a reported stress ceiling); calibrate the CI guard on the **as-built measurement + margin** (tighter than the user SLO), **retire** proxy-derived numbers, and **match the throughput unit to the guard's basis** (events/s == commits/s, not submits/s). Benchmarks run at their **own cadence** (`[[bench]] harness=false` → `/phase-exit` + nightly, NEVER per-slice `cargo test`). A re-baseline of a committed §18 budget number is a **load-bearing call → escalate** (2.5 = USER-RULED Option A). Applies to the upcoming **P3 Terminal-Channel + P4 survival** §18 benchmarks. `pin: daemon/benches/event_write.rs; cadence: /phase-exit perf row + nightly.yml, NEVER cargo test --workspace`

## <a id="23"></a>23. Telemetry/usage is an OBSERVATION event (write-actor, not the Gateway); a §2.5-seam freeze splits shared DATA types from the daemon-internal adapter SEAM

**Date:** 2026-06-12.
**Source slice:** 3.1 (the §9.1 HarnessAdapter contract freeze + the `proj_usage_ledger` projector, brief 040 — L1 `feat(harness)` CONTRACT 0.20.0, L2 `feat(projections)`). Phase-3 opener. security-reviewer CLEAR.

Phase 3 opens the harness layer, and its first slice freezes a contract that crosses a §2.5 seam while also adding the first **non-mutation event type emitted by a non-Gateway source**. Two distinct disciplines, easy to get wrong:

- **Telemetry is an OBSERVATION, not a mutation — so it bypasses the Gateway but never the write-actor/redactor.** `proj_usage_ledger` is a projection → it must be fed by an event (`TelemetrySampled{sample,model?,execution_profile_id?}`, §7.1). But a token/context/cost *sample* mutates no FS/git/external/session **state**, so **INV-SEC-1 (#1) does not route it** — it is appended via the single write-actor through the §15 redactor + projector fold (audited + immutable), **NOT** through the policy/approval pipeline. This is **LESSON §10 generalized** (daemon self-registration `DeviceRegistered`/`LocalRunnerRegistered` + the §17 `AuditIntegrityViolation` are the precedent family): the Gateway is the chokepoint for **mutations**; observation/lifecycle events the daemon *records* go System/adapter-actor → write-actor → redactor. Single-mutator (#2) still holds verbatim ("only the daemon writes the DB; only the Gateway executes *mutations*" — this isn't one). The projector only ever reads already-redacted events (#3). Emission is deferred to the real adapters (3.2/3.3); 3.1 defines the event + folds synthetic ones.
- **A §2.5-seam freeze splits SHARED data types from the daemon-internal SEAM — freeze only the former, only when its design is settled.** The §9.1 `HarnessAdapter` *trait* + `MutationIntercept` (non-serializable `decision_sink`) + the coverage matrix stay **daemon-internal + UNFROZEN** (sync, object-safe `Box<dyn>` per the `ActionExecutor`/`PolicyEngine`/`PreconditionOracle` seam precedent — no speculative `async-trait` dep; async I/O lives in the runtime/task layer, LESSON §9) so 3.2/3.3 reshape freely. Only the **normalized DATA types** the UI/Brain consume (`TelemetrySample`, `MetricQuality`, `TranscriptRef`, `HarnessCapabilities`) + the event are the §2.5-seam freeze (CONTRACT 0.20.0; field-name snapshots, §15-gotchas LESSON §15). **`NormalizedStatus` is the frozen `status::Session` re-exported, not a forked enum** (the adapters map INTO it). **Don't freeze a type whose design lives in a later phase:** `ResumeResult` was pulled from the 3.1 freeze and kept daemon-internal because resume/survival is a **Phase-4 §8/§17** design — the ui's own provisional already pins `ResumeMode = enum["resumed","replayed"]` + `RecoveryStatus`, so a 3.1 `{resumed_live: bool}` freeze would force a breaking reshape (the §2.5-seam freeze exists to PREVENT that). It freezes in `shared/` at P4 with the survival schema.
- **Projector fold semantics are read off the DDL column names.** `proj_usage_ledger` has `tokens_in`/`tokens_out`/`cost_estimate` (plain accumulators → SUM) but `context_pct_max` (the name IS the contract → MAX gauge); `metric_quality` = worst-quality-wins; the rollup `ledger_id` = a deterministic delimited composite with the **free-form `model` dim LAST** (a `|` in a model string can't shift the leading delimiter-free ULID/date dims → collision-free). `bucket_day` = the UTC date of the daemon-Clock-stamped `occurred_at` (UTC-`Z` per LESSON §5, guarded fail-closed). The real emission must send per-heartbeat **deltas** (not cumulative snapshots) so the SUM is correct — a 3.2/3.3 acceptance pin.

**Rule:** An event that *records an observation* (telemetry/usage/lifecycle) the daemon witnesses is **System/adapter-actor → write-actor → §15 redactor → projector**, NOT a Gateway Action — INV-SEC-1 governs state **mutations**, and a projection that needs the data must be fed by such an event (LESSON §10 family). When freezing a §2.5-seam contract, freeze **only the consumer-facing DATA types whose design is settled** (snapshot-pinned, one CONTRACT bump, reuse a frozen enum over forking it); keep the trait/SEAM daemon-internal + sync + unfrozen (the `Box<dyn>` executor-seam precedent) so the producing phase reshapes it; and **defer any type owned by a later phase** (e.g. a survival/resume shape → P4) rather than freezing a guess. Read projector fold semantics off the DDL column names (`*_max` = MAX gauge; plain = SUM); put the free-form dim LAST in a composite key. `pin: shared/tests/contract.rs (§9.1 snapshot + 0.20.0 version pin) + daemon/tests/projections.rs (fold/rebuild/degrade-skip); security-reviewer every harness/Gateway slice`

---

## <a id="24"></a>24. The embedded Terminal Channel — the PTY is a display byte-pipe + an OS exit signal, NEVER a status source (#9); backpressure = a pure watermark classifier + OS-pipe backpressure, never an unbounded queue and never back-pressuring the write-actor

**Date:** 2026-06-12.
**Source slice:** 3.4 (the §6.4 Terminal Channel daemon spine, brief 041 — L1 `feat(ipc)` CONTRACT 0.21.0 · L2 `feat(terminal)` PTY host · L3 `feat(terminal)` backpressure). security-reviewer CLEAR all 3 layers.

The embedded terminal is where safety rule #9 ("never scrape the PTY for machine state") meets a real `portable-pty` child + a backpressure problem. Three disciplines:

- **The PTY host is a byte pipe + a death signal — its public surface has NO status-derivation API (#9, structurally pinned).** `TerminalSession` reads child output into bounded frames and emits `TerminalProcessExited{terminal_id,exit_code,signal}` **from the OS exit status** (`portable-pty` waitpid / reader EOF), never by parsing output bytes. The display bytes and the lifecycle signal are different channels — status comes from the *process*, content from the *stream*. Enforced by a **structural grep-pin test** (the module source has no `infer_status`/`parse_state`/`harness::`-status-reading API) — and the pin is scoped to the pure module so a later I/O sibling (a real reader) doesn't false-alarm it. `TerminalProcessExited` is a non-mutation OBSERVATION event (write-actor via an injected `TerminalEventSink`, NOT the Gateway — LESSON §10/§23 family; the edge never writes the DB directly, forbidden #3).
- **Backpressure = a pure watermark classifier (LESSON §12 reuse) + OS-pipe backpressure — bounded, deterministic, never back-pressuring the writer.** `next_terminal_action(buffer_len, high, low, paused) -> Emit|Pause|Resume|Hold` is a **timing-free pure fn** (the `next_push_action` precedent) so it unit-tests at the watermark boundaries with no flakiness. Crossing the high watermark → emit `{pause}` AND **stop reading the child** (OS pipe backpressure propagates to the agent) — it does **not** grow an in-memory queue (no OOM/stall; the DoS bound is `buffer ≤ high+chunk << MAX_FRAME_SIZE`). Output is coalesced at ~30 fps via an injected `Clock` (not wall-clock). The terminal flow control is **entirely between the child and the socket** — it never back-pressures the single write-actor (forbidden #3 / LESSON §9). (Full-drain→immediate-resume now; the low-watermark hysteresis bites once a per-frame size cap lands at 3.5.)
- **The §6.4 wire encoding reuses the proven codec — JSON-base64 MVP, binary deferred to measured data.** The Terminal frames (`TerminalOutputFrame`/`TerminalInputFrame`/`TerminalControlFrame` + the `ServerFrame::TerminalOutput` mux slot) carry `data` as **base64** so raw PTY bytes ride the **unchanged** 4-byte-len+JSON codec (LESSON §7 — zero new framing machinery, consistent with the whole IPC surface). The binary fast-path stays an **additive future variant** the 3.5 throughput benchmark justifies — exactly what the `ServerFrame` reserved-slot doc-comment + LESSON §7 ("Terminal encoding = Phase-3 measured") anticipated. `terminal_id` is a **daemon runtime handle** (`String` wire / typed newtype via the IdGen seam), NOT a 23rd LOCKED-22 `IdKind` (it's connection/session-scoped + re-minted on resume — the `subscription_id` precedent; a new `IdKind` would be a LOCKED-22 escalation). Untrusted client→daemon ingress frames (`TerminalInputFrame`/`TerminalControlFrame`) get reject-unknown (`deny_unknown_fields`); the internally-tagged `ServerFrame` carries reject-unknown on the bare member struct (an internally-tagged enum doesn't surface `deny_unknown` reliably — the `ProjectionDelta` precedent).

**Rule:** An embedded terminal is **display-only**: the PTY host exposes byte-IO + an OS-derived exit signal and **nothing that derives session/agent status from output bytes** (#9 — pin it structurally, scoped to the pure module). Backpressure is a **pure watermark classifier** (LESSON §12) + **OS-pipe backpressure** (stop reading at the high watermark) — never an unbounded in-memory queue, never back-pressuring the write-actor (forbidden #3). Freeze the Terminal Channel wire frames over the **unchanged codec** (JSON-base64 MVP; `data` base64) and defer the binary fast-path to a measured benchmark; a runtime/connection-scoped handle (`terminal_id`) is NOT a frozen-22 ID. `pin: daemon/tests/terminal.rs (FakePty stream/exit/no-status-API + backpressure flood/batch/no-writer-backpressure) + shared/tests/contract.rs (§6.4 Terminal frame snapshots + 0.21.0); security-reviewer every terminal/harness slice`

---

## <a id="25"></a>25. The PTY-primary harness adapter — status from STRUCTURED signals via a pure (prev,signal)→Session fold (never PTY bytes, #9); the O-13 launch spec IS the #10 enforcement surface (fail-closed on no-hooks); a `PtySpawner` object-safe factory; the trait is daemon-internal + reshape-freely

**Date:** 2026-06-12.
**Source slice:** 3.2 part 1 (the Claude adapter OBSERVE path, brief 042 — L1 `feat(harness)` scaffold + O-13 launch + the trait reshape · L2 `feat(harness)` `derive_status` · L3 `feat(harness)` `read_transcript`). PTY-primary per the resolved cat-4. security-reviewer CLEAR L1+L2.

The first real `HarnessAdapter` implements the frozen 3.1 contract under the resolved cat-4 (PTY-primary). Four disciplines (generalize to the Codex adapter, 3.3):

- **Status derivation is a pure `(prev, signal) -> Session` fold over a typed STRUCTURED-signal enum — never PTY bytes (#9).** `derive_status(prev: Session, signal: ClaudeSignal) -> Session` consumes a `ClaudeSignal` (the hook events `SessionStart`/`PreToolUse`/`PostToolUse`/`Notification`/`Stop` + the 3.4 `ProcessExited`), maps to the §5.1 17-state, and **structurally cannot read the PTY** (the type signature is the #9 guarantee + a grep-pin scoped to `claude/status.rs`). It's **fail-closed on exit** (only `(exit 0, no signal) -> Completed`, every other shape -> `Failed`) and **R-9 terminal-sink-guarded** (a terminal `prev` + any signal holds `prev` — no resurrection; the full §5.1 legal-edge set is the machine's concern, not the adapter's). The signal *ingestion* I/O (the hook receiver / transcript tailer) is a deferred seam (a `push_signal` entry point; tests feed signals directly) — the pure core is test-first, the async I/O rides 043/P4 (the LESSON §9 sync-core precedent).
- **The O-13 `launch()` spec IS the #10 enforcement surface — and it fails CLOSED.** PTY-primary means `launch()` builds a `ClaudeLaunchSpec`: interactive PTY (no `-p`/`--print`), **`--permission-mode default` only** (never acceptEdits/bypass/plan), **no background subagents**, hooks+statusLine wired to the daemon. That spec — not a runtime check — is where O-13 (#10) is enforced, pinned by a test that asserts the forbidden flags are *absent by construction*. **Fail-closed:** if the generated settings can't serialize, `launch()` returns `Failed` rather than spawn a **hook-less, uninterceptable** session (a #10 hole). The settings are a **generated per-session file (`--settings`, 0600), NEVER a mutation of the user's `~/.claude/settings.json`** (clobbering the user's global config is a footgun/safety concern).
- **A `PtySpawner` object-safe factory is the seam a `launch()` injects** — because the spawned handle trait isn't itself a spawner. The 3.4 `Pty` trait is the spawned *handle* (object-safe); `PortablePtyHost::spawn` returns `Self` (not object-safe). So `launch()` owns a `Box<dyn PtySpawner>` (`spawn(program,args,cwd,rows,cols) -> io::Result<Box<dyn Pty>>`; `PortablePtySpawner` in prod, a recording/fake spawner in tests). This is the general "inject a factory, not a constructed value" pattern for the non-deterministic spawn surface (§14).
- **The daemon-internal trait reshapes FREELY to fit the real adapter (LESSON §23 mandate realized).** The real `ClaudeAdapter` owns a `Box<dyn Pty>` (Send-only, `!Sync`) + a mutable status machine, so it can't satisfy the 3.1 `Send+Sync`/`&self` trait — so the trait reshaped: **`Send+Sync`→`Send`** + **`launch(&self)`→`launch(&mut self)`**. This is *exactly* what the §2.5-seam discipline reserved — the trait is daemon-internal + UNFROZEN; only the normalized DATA types are frozen, so reshaping the SEAM is free + breaks no contract (security-reviewer confirmed dropping `Sync` is SAFE — nothing shares an adapter across threads). The adapter is built + FakePty/fixture-tested with **NO production caller** — the live session-lifecycle drive loop that calls `launch()`/`stream_status()` is **Phase 4** (the build-order: Phase 3 = adapter mechanisms, Phase 4 = the lifecycle that drives them); the interception (`MutationIntercept`→Gateway, INV-SEC-1) + telemetry emission are 043.

**Rule:** A harness adapter derives status as a **pure `(prev, signal) -> Session` fold over a typed STRUCTURED-signal enum** (hook/app-server events), never PTY bytes (#9 — the signature + a scoped grep-pin); keep the signal *ingestion* I/O a deferred seam (sync pure core, LESSON §9). The **launch spec IS the safety-rule enforcement surface** (O-13 #10: default-mode-only / no `-p` / no bg subagents — pinned as *absent by construction*) and it **fails closed** (no hooks → no launch, never an uninterceptable session); generate per-session settings, NEVER mutate the user's global config. Inject a **`PtySpawner` object-safe factory** for the non-deterministic spawn. The daemon-internal `HarnessAdapter` trait **reshapes freely** to fit the real adapter (`Send+Sync`→`Send`, `&self`→`&mut self`) — only the §2.5-seam DATA types are frozen; the production drive caller is a named Phase-4 deferral. `pin: daemon/tests/claude_adapter.rs (O-13 launch spec absent-by-construction + derive_status mapping/fail-closed-exit/terminal-sink + #9 structural pin + conformance fixture); security-reviewer every harness slice (#9/#10)`

**[4.0b-2 amendment — the #10 enforcement LOCATION moves to the launcher (lead-ruled Option A, away-authority; return-review-flagged).** When the live drive loop gives ONE claude process ONE PTY (display + drive), the spawn site — and the #10 enforcement spec INLINE — moves from `ClaudeAdapter::launch()` to **`PtyLauncher`**: enforce-at-the-spawn-site. The launcher builds the `ClaudeLaunchSpec` (default-mode / no `-p` / no bg / 0600 generated settings, **fail-closed on settings-write** → no session), spawns the single live-claude PTY → `TerminalSession`; **`ClaudeAdapter` no longer spawns — status from hook signals ONLY (rule #9, never PTY-scraped)**. The #10 CONTENT is preserved **IDENTICALLY** — a location move, NOT a relaxation. The dedicated 4.0b-2 L2 security pass adjudicates the relocation over the accumulated diff (#10 intact at its new home + the SINGLE-spawn-site pin: *"only `PtyLauncher` spawns a live claude PTY; `ClaudeAdapter::launch` no longer spawns"*).

---

## <a id="26"></a>26. The harness INV-SEC-1 interception — an agent mutation is a typed, adjudication-only Action through the ONE Gateway pipeline (audit-BEFORE-verdict); any un-interceptable path is a bypass → deny + compensate or it's not safe

**Date:** 2026-06-12.
**Source slice:** 3.2 part 2 (the Claude `MutationIntercept`→Gateway interception, brief 043 — L1 catalog `feat(catalog)` CONTRACT 0.22 · L2 ingress · L3 routing `feat(gateway)` · L4 coverage-gap `feat(harness)` · L5 record-then-deny `feat(gateway)` CONTRACT 0.23). The most INV-SEC-1-load-bearing slice of Phase 3; ruled across **3 human safety gates**. security-reviewer PASS every layer (the core adversarially-verified).

The first real harness mutation chokepoint — where INV-SEC-1 stops being a Gateway-internal property and becomes the rule a live agent's tool-use must pass. Five disciplines (generalize to the Codex adapter, 3.3):

- **An agent mutation is a typed Action through the ONE pipeline — adjudication-only, not a parallel path (Q1=A).** A `PreToolUse` hook → the daemon hook-receiver → a `MutationIntercept` → an `ActionRequest` (a §6.3 catalog **agent-mutation family** + `ExecutorKind::Adjudication`) routed through the **EXISTING** `submit_action` pipeline → policy/approval → a `MutationVerdict`. Reuse the one chokepoint — a second policy/approval path is a second thing to prove INV-SEC-1-safe. **Adjudication-only:** the daemon ADJUDICATES + AUDITS; the **agent executes** the Allow'd tool, so the ActionRequest **TERMINATES at the verdict** (`PolicyDecided`/`Approved`, never `Queued`/`executing`) — realized by the path *stopping earlier on existing edges* (no new state, no new R-9 edge), with the no-execute **code-enforced** (pipeline-terminal + a `CatalogExecutor::execute` fail-safe → Failed + a RecordingExecutor-never-called test), never comment-enforced.
- **Audit-BEFORE-verdict (§15 #5).** An `Allow` is gated on the authoritative event being **durably committed FIRST** — the verdict IS the side-effect, so it must never precede its audit record. It falls out of the existing fail-closed txn: `Allow` only when submit/approve returns `Ok` (the event committed); any `Err` (incl. an injected audit-write fault) → `Deny`. Pin it with a `FaultPoint::AuditEventWrite`.
- **Conservative risk-tier (Q2): read-only auto-allows, every mutating tool requires approval by default.** Read-only (risk-0) is NOT a mutation relaxation (read-only ≠ mutation). Among mutating tools, fail-closed: deny-rules deny known-dangerous outright; **every other mutating tool requires approval** (no auto-allow until a user standing-grant); no/failed/timed-out → Deny. The deny-rules are a **conservative redundant layer** (require-approval is the primary net) — a deny-rule false-negative falls through to require-approval (the human sees it), never a silent allow.
- **The coverage-gap crux: any un-interceptable mutation path is an INV-SEC-1 BYPASS — deny it + compensate, or the chokepoint is defeated.** The §9.1 coverage matrix is binding: only Guaranteed/GuaranteedInDefaultMode (DirectToolUse, `default` mode) → Adjudicate; **NotGuaranteed/Unsupported (MCP=BestEffort, Task, bg-subagent #27203) / non-default mode → DENY**. A default-mode hook-MISS **fails OPEN** (verified) — so denial must be MECHANICAL, not hook-reliant: a hook-INDEPENDENT `permissions.deny` (Claude enforces deny-rules even when the hook allows) hard-blocks the un-interceptable categories. The fail-open residual **closes** via defense-in-depth: per-call hook (sole allow-authority) + **NO `permissions.allow`** + the non-interactive **empty-cache block** (a daemon-driven PTY never populates the first-use cache — no human at the TUI — so a hook-miss has nothing to fall through to) + `permissions.deny` + a **5s** hook timeout (not 600s). The single uncovered mutation defeats the whole chokepoint — this layer is the slice's reason for existing.
- **A blocked dangerous attempt is NEVER silently dropped from the audit log (A1 — record-then-deny).** A policy/deny-rule denial fires before any approval exists, and the established policy-deny ROLLS BACK (nothing persists). For the agent path, **commit instead** (the 2.4 record-then-throw precedent, `is_agent_mutation`-gated — non-agent denials keep the rollback): `ActionRequested`+`ActionDenied{None, reason}` (the deny REASON is the security-relevant forensic record). This needed the honest model `ActionDenied.approval_id: Option<String>` (a policy-deny genuinely has no approval — not a synthetic phantom; not a row-state-only). A denial-audit-write fault still → Deny (no new hole).

**Rule:** Route an intercepted agent mutation as a **typed, risk-classified, adjudication-only `ActionRequest` through the ONE Gateway pipeline** (not a parallel path), **audit-written-BEFORE the verdict** (§15 #5 — the verdict is the side-effect), with the no-execute **code-enforced** (terminal + a fail-safe executor arm + a behavioral pin). Default conservative (read-only auto-allows; every mutating tool requires approval until a user standing-grant; deny-rules are a redundant layer, false-negatives fall to require-approval). **Treat every un-interceptable path as a BYPASS** — deny it MECHANICALLY (a hook-INDEPENDENT control, since a hook-miss can fail open) + compensate (no-`allow` + empty-cache-block + short timeout) or document a residual; a single uncovered mutation defeats the chokepoint. **Audit blocked dangerous attempts** (record-then-deny, the agent path commits the denial event; model the missing approval honestly as `Option`, never a phantom). `pin: daemon/tests/claude_intercept.rs (adjudication-no-executor #13 + audit-before-verdict fault + coverage-matrix disposition + deny-rule-no-false-allow + record-then-deny + cheap-closure settings); security-reviewer EVERY layer (cat-1 INV-SEC-1)`

## <a id="27"></a>27. The harness telemetry adapter — emit per-heartbeat DELTAS (not cumulative), context-% as a gauge (not a delta), via an injected sink; the daemon-Clock UTC-Z gates the projector's bucket_day

**Date:** 2026-06-12.
**Source slice:** 3.2 part 2 telemetry (the Claude adapter `telemetry_heartbeat` emission, brief 044 — `feat(harness)` `09f34aa`, one bundled commit; NON-safety; CONTRACT held 0.20.0). code-quality every-slice (0 high); security-reviewer NOT required (a non-mutation observation event consuming the already-§15-gated write-actor append — no §15/INV-SEC-1 mechanism touched).

The §9.1 telemetry path is the OTHER half of 3.2 (after the 043 interception): the daemon WITNESSES a supervised agent's usage and records it as a `TelemetrySampled` observation event (LESSON §23 — write-actor, NOT the Gateway). Three disciplines (generalize to the Codex adapter, 3.3):

- **Emit per-heartbeat DELTAS, never cumulative snapshots.** Claude reports CUMULATIVE session usage (`ResultMessage.usage` / `total_cost_usd`), but `proj_usage_ledger` SUMs (`tokens_in/out/cost_estimate += excluded.*`) — so emitting the cumulative total over-counts badly (a session at 250 cumulative tokens, sampled 5×, would log 1250). The adapter tracks `last_emitted_cumulative` and emits `current − last`; the FIRST reading is the full delta-from-zero (no double-count, no fabricated pre-reading). Delta-tracking is **sink-INDEPENDENT** — `push_usage` updates `last_cumulative` + the stored delta even with no sink, so `telemetry_heartbeat()` stays correct before the P4 drive loop binds the production sink (never couple delta-tracking to emission).
- **context-% is a GAUGE (pass-through), not a delta.** The same projector takes `context_pct_max = MAX(...)` (a gauge, not a sum). A deltaed context-% would be meaningless (and mis-MAX). So tokens/cost are differenced; context-% rides the CURRENT value through. `cost_estimate` = the delta of Claude's REPORTED cumulative cost (the "estimate" honesty — no local model-pricing table); `metric_quality` Exact when context-% is present, Estimated when it degrades (never a faked 0%, §11.4).
- **Emit a non-mutation observation event through an injected sink (the 3.4 `TerminalEventSink` precedent); the daemon-Clock UTC-Z stamps `occurred_at`, which gates the projector's `bucket_day`.** A `TelemetryEventSink` seam (`Send`, like `TerminalEventSink`) — the production impl binds `WriteHandle::append` (§15-gated, UTC-Z Clock) at the P4 drive loop; tests use a collecting double. `occurred_at` is stamped by the write-actor's `Clock::now_rfc3339()` (UTC-Z, LESSON §5), NOT the payload — and `proj_usage_ledger.bucket_day` is its first-10-chars, fail-closed on a non-Z source. Pin the WHOLE path with an integration test that appends two delta events through the REAL write-actor + projector and asserts SUM-of-deltas == the cumulative total (a cumulative-emit would mis-SUM 250→350) + bucket_day == the UTC date (a deltaed context-% would mis-MAX 55→30) — **differential negative controls**, not just the happy path.

External-upstream parse posture: the Claude transcript is an EXTERNAL source we defensively parse — the parser TOLERATES unknown fields (forward-compat across Claude versions) but fails closed on missing/wrong-typed REQUIRED fields (never a fabricated/partial reading); `deny_unknown_fields` stays on OUR emitted `TelemetrySampled` contract, NOT the upstream we read (§15 reject-unknown governs our persisted/emitted contract, not the upstream). The production sink-bind + the periodic pump (statusLine `refreshInterval`) + the trait async-ify + the live transcript/statusLine ingestion = the NAMED Phase-4 deferral (a non-monotonic-cumulative-cost clamp→≥0 + degrading `metric_quality` on the guard are P4 live-data hardening).

**Rule:** A harness telemetry adapter emits per-heartbeat token/cost DELTAS (`current_cumulative − last_emitted`, first = full-from-zero) — NEVER cumulative snapshots (the usage projector SUMs) — and rides context-% as a CURRENT GAUGE (the projector MAXes it), never a delta. Emit it as a non-mutation `TelemetrySampled` OBSERVATION event (LESSON §23 — write-actor, not the Gateway) through an injected sink (the `TerminalEventSink` precedent); the daemon-Clock UTC-Z `occurred_at` gates the projector's `bucket_day` (fail-closed on non-Z). Defensively parse the external upstream (tolerate unknown fields, fail closed on missing/bad required); keep delta-tracking sink-independent. Pin the full delta→event→projector path with a real-append integration test using **differential negative controls** (cumulative would mis-SUM; deltaed context would mis-MAX). Generalizes to Codex 3.3. `pin: daemon/tests/claude_telemetry.rs + daemon/tests/projections.rs (Test 24, SUM-of-deltas + UTC-Z bucket); code-quality every-slice (NON-safety)`

**3.3d EXTENSION (the Codex realization — `389ee28` hoist + `1f6664c` Codex telemetry; brief 074):** the pattern now spans BOTH harnesses. The 5 harness-neutral primitives (`telemetry_sample`/`UsageReading`/`TelemetryEventSink`/`UsageSource`/`TelemetrySinkFactory`) HOISTED from `claude/telemetry.rs` to a harness-neutral `harness/telemetry.rs`, **RE-EXPORTED from `claude::telemetry`** so every existing consumer stays byte-identical (a 2nd harness reuses the seam without coupling to the 1st — the relocate is its own `refactor` commit, the Claude suite green). The **genuinely-new Codex divergence:** where Claude reads an UPSTREAM-reported `total_cost_usd` (authoritative → `Exact`), **Codex `token_count` carries NO cost field** → a PURE LOCAL per-model pricing table (`codex_cost_estimate` + a const rate table, **indicative-rates + a refresh-TODO**) derives the cost, and `metric_quality` is honestly **DOWNGRADED `Exact`→`Estimated` for ANY locally-derived cost** (the Claude-Exact vs Codex-Estimated honesty line, §11.4; an unknown/absent model → `0.0`, never a fabricated cost). The Codex cumulative source is **`total_token_usage`** (the per-turn `last_token_usage` is the observe-path snapshot) — a documented **snapshot-vs-cumulative trap the live ingress MUST honor:** build CUMULATIVE readings for `push_usage`, never feed per-turn snapshots (else the SUM-of-deltas over-counts). `pin: daemon/tests/codex_telemetry.rs (the pricing fn edges + unknown-model→0.0 + the Exact→Estimated honesty) + daemon/tests/telemetry_pump.rs (the Codex SUM-of-deltas through the real write-actor)`

## <a id="28"></a>28. The opt-3 session-lifecycle spine — the write-actor idiom applied to sessions (a task + an mpsc mailbox + the §5.1 status state); an EDGE actor that drives the adapter via spawn_blocking (no async-trait) + proposes intents, never writes the DB

**Date:** 2026-06-12.
**Source slice:** 4.0a (the live drive-loop spine, brief 046 — `feat(session)` L1/L2/L3; NON-safety; deep-dive §8, forks ruled **opt-3 · B2-strict · broker-in-scope**). code-quality every-slice (7 findings, 5 fixed-in-slice incl. 2 high — a no-op `pump.abort()` on a `spawn_blocking` task removed; a panicked actor's leaked mailbox → exact `live_count` via a `JoinError`→`(id,Failed)` map); security-reviewer NOT triggered (invariant-gated; cat-1 held — the cat-1 4.0b is where it fires every layer).

P4 opens the live drive loop. The fork-(a) ruling = **opt-3 (session-as-supervised-actor)**, and the key realization is that **the daemon's existing idiom IS an actor idiom** — the write-actor (LESSON §9) is literally "a dedicated thread/task + an mpsc mailbox + a command loop." So opt-3 is the SAME pattern applied to sessions, not a new framework. Four disciplines:

- **A `SessionActor` = a Tokio task + an mpsc mailbox + the §5.1 Session status state**, owning a `Box<dyn HarnessAdapter>` + a terminal read-pump. It TRACKS the status the adapter reports (the adapter owns §5.1 derivation via `derive_status` — the harness-derived SoT, §7.2 / LESSON §25) and reaps on `is_terminal()` — it does NOT re-derive/validate edges (a duplicate validator would risk disagreement; `shared/` exposes only `ALL`+`is_terminal`, no generic edge-validator, by design). Status from the adapter's STRUCTURED stream, **never PTY-scraped (#9)** — pinned by streaming FakePty noise in parallel that does not move status.
- **A `SessionSupervisor` = a `HashMap<SessionId, mailbox>` + a `JoinSet<(SessionId, Session)>`** (spawn/track-by-id/route/reap, **NO auto-restart** — restart is a recovery concern, 4.2). A panicked actor's `JoinError` maps to `(id, Failed)` so every reap cleans up + the live count stays exact (a leaked mailbox is an over-count). `shutdown` Kills all + drains the JoinSet (no orphan task, LESSON §9). Spawned in `main.rs` as a §10 background task mirroring `spawn_reaper` (a biased shutdown-watch `select!`); the `SupervisorHandle` is the next-slice (4.0b `session.create` executor) driver entry — wired + reachable, not driven.
- **Drive the SYNC `HarnessAdapter` trait via `spawn_blocking`, do NOT async-ify it.** Only `launch` (spawns a process) + the PTY `read_step` (blocks on I/O) block → `spawn_blocking` (the write-actor-thread + terminal read-pump precedent); `stream_status`/`telemetry_heartbeat`/`intercept_mutation` are cheap inline field reads on the async tick. No method needs in-trait `await` → **no `async-trait` dep, the trait stays UNCHANGED** (LESSON §23's "no speculative async-trait dep"). The pre-build "async-ify" assumption was the heavier route; the leaner one is correct. Caveat: a `spawn_blocking` read can't be `abort()`ed — the kill-path (unblock a long-running live agent's pump via `pty.kill()` at the source) is a named 4.0b/4.2 live-path follow-on (4.0a's benign/EOF programs return promptly).
- **The session module is an EDGE actor: it DRIVES the adapter + PROPOSES intents, it NEVER writes the DB** (the layer rule). Enforce the cat-1 boundary STRUCTURALLY — the `session/` module imports no `crate::runtime`/`crate::eventstore`/`crate::gateway`, so event emission + mutation are *compile-time impossible* (pin with an import-grep test over `src/session/`; non-recursive → a future submodule must extend the scan). Launching a session is itself a Gateway mutation + a live agent is unsafe un-intercepted → the live launch + the INV-SEC-1 interception + the Gateway `session.create` executor land TOGETHER as the cat-1 next slice; everything before is FakeHarness / benign-`/bin/echo` scaffolding (no live agent even via the daemon-PTY path — the `PtyLauncher` pairs a real benign PTY with a FakeHarness placeholder adapter).

A `SessionLauncher` seam (`LaunchedSession{session_id, Box<dyn HarnessAdapter>, TerminalSession}`) abstracts the spawn so a later survival broker (B2-strict) is a drop-in impl behind it (`TODO(4.1)`).

**Rule:** Build the session-lifecycle spine as the write-actor idiom applied to sessions — a `SessionActor` (a Tokio task + an mpsc mailbox + the §5.1 status state, owning the adapter + a terminal read-pump; TRACKS the adapter-derived status, never re-validates edges, never PTY-scrapes #9) under a `SessionSupervisor` (a `HashMap` + a `JoinSet`; spawn/track/route/reap with NO auto-restart; a panicked actor → `(id, Failed)`; clean-shutdown drains the JoinSet). Drive the SYNC `HarnessAdapter` via `spawn_blocking` (only `launch` + `read_step` block) — NOT `async-trait` (no speculative dep; the trait stays unchanged). The session module is an EDGE actor that proposes intents + NEVER writes the DB — enforce the boundary STRUCTURALLY (import-grep: no `runtime`/`eventstore`/`gateway` import → emission/mutation compile-time impossible). A `SessionLauncher` seam makes the survival broker a drop-in. `pin: daemon/tests/session.rs (lifecycle + #9 + cat-1 import-grep + supervisor reap/no-restart/clean-shutdown); code-quality every-slice; security-reviewer on the cat-1 live-wiring slice (4.0b)`

## <a id="29"></a>29. A contract gate that can sit DARK is a gap — pin codegen tooling versions + add a gate-self-health check

**Date:** 2026-06-13.
**Source slice:** the 4.0b-1 L1 §5.0 3-way-verify Finding (lead-ruled away-authority; the fix = the 4.0b-T tooling-fix slice).

The §5.0 3-way contract verify (`shared/contracts/verify/run.sh`: Rust→schema→{zod,pydantic}) sat **silently RED for ~7 slices** (since the 043 seal) on a `uvx datamodel-code-generator` version auto-update that started generating a standalone `MetricQuality` pydantic enum the `verify.py` extraction didn't pair. It went undetected because the AUTHORITATIVE chain (Rust→schema via the test-9 byte-diff, run inside `cargo test --workspace`) + the zod arm stayed GREEN, and the verify is NOT in the per-slice `cargo test` loop — so the dark arm (pydantic, for the Brain, a sibling not built here) drifted invisibly while contract bumps kept landing.

- **A contract gate that isn't in the per-slice loop AND has no self-health signal can sit dark.** The per-slice snapshot tests (test-9 byte-diff) + the authoritative Rust→schema→zod chain protected contract CORRECTNESS throughout (so this was CI-health, NOT a correctness/safety regression) — but the verify gate's PURPOSE (catch any consumer drift) was silently defeated.
- **Pin the codegen tooling versions** (`datamodel-code-generator`, …) so an unrelated auto-update can't silently re-RED the gate; **root-cause-fix the extraction** (forward-compatible) over a bare version-pin; **add a gate-self-health check** (a green run asserts the gate actually exercised every arm) so a dark gate surfaces loudly.
- **Classify before reacting:** when a verify is RED, the FIRST move is a **stash-test** (stash the slice diff + re-run) to prove pre-existing-vs-regression — that's what classified this as CI-health (continue the in-flight cat-1 slice; fix the gate in a dedicated non-cat-1 slice at the boundary, not bundled into a cat-1 freeze) rather than blocking.

**Rule:** A §5.0 contract gate that runs OFF the per-slice loop must pin its codegen tooling versions AND carry a self-health signal (a green run proves it exercised every arm) — else an unrelated tooling auto-update can silently defeat it, and the gate sits dark while contract bumps keep landing (the in-loop authoritative snapshot tests still protect correctness, but the gate's drift-catching purpose is lost). When a verify is RED, **stash-test FIRST** to classify pre-existing-vs-regression. `pin: shared/contracts/verify/test_verify.py (4.0b-T REALIZED `981de9d` — 8 offline extractor/self-health regression tests + the pinned `datamodel-code-generator==0.63.0`/`json-schema-to-zod@2.8.1` in verify.py; the form-coverage self-health gate in verify.py main())`

## <a id="30"></a>30. Not every I/O on a safety path is safety I/O — a best-effort convenience degrades SOFT, never fail-closes the safety action

**Date:** 2026-06-13.
**Source slice:** the 4.0b-2-smoke harness `initial_prompt` thread (brief 053 C1, `e79fecb`; security-reviewer PASS).

The daemon's default posture is **fail-closed everywhere** (a settings-serialize fault → `launch=Failed`, LESSON §25; an audit-write fault → Deny + the §17 alarm, LESSON §26; a hook-miss → Deny, LESSON §26). So when the Option-G smoke drive added a `pty.write(initial_prompt)` on the `session.create` execute path — a path that IS safety-critical (it spawns a live agent WITH the interception) — the reflex is to fail-close it too. That would be **wrong**: a prompt-write error fail-closing the session would let a *non-safety dev convenience* veto a *safety-clean launch*.

- **The distinguishing question is not "is this I/O on a safety path?" but "is the safety INVARIANT a function of this I/O's success?"** The launch's safety invariant (INV-SEC-1: a live agent is reachable only WITH the interception live) holds **regardless** of whether the prompt reaches the PTY — the interception adjudicates every tool call no matter how (or whether) claude was prompted. The prompt-feed only makes claude *act*; its failure means "the agent sits idle," never "an un-intercepted agent." So it is **non-safety convenience I/O** that merely *sits on* a safety path.
- **Therefore it degrades SOFT** (record a launch detail, the session still spawns + auto-executes) — fail-closing it would CONVERT a clean launch into a denial on a non-safety fault, which is its own (availability) bug. Pinned by `test_prompt_write_error_does_not_fail_the_session`.
- **Contrast the genuinely-safety I/O on the SAME path, which DOES fail-closed:** the 0600 `ClaudeSettings` write (a settings-write fault → no session — an un-hooked agent would be uninterceptable, LESSON §25), the adjudication audit-event commit (audit-before-verdict, LESSON §26), and the systemic audit-write circuit-breaker (4.0b-2c — N-consecutive audit faults → daemon FAIL-STOPS). For each of those, the invariant IS a function of the I/O succeeding → fail-closed/fail-stop. The two classes coexist on one path; classify each I/O by its invariant, not by the path it rides.

**Rule:** Before fail-closing an I/O on a safety-critical path, ask whether the safety INVARIANT depends on that I/O succeeding. If yes (the settings write, the audit append, the redaction gate) → fail-closed (the daemon default). If the invariant holds regardless (a best-effort dev/UX convenience like the smoke-harness prompt-feed) → degrade SOFT, because fail-closing it converts a safety-clean operation into a denial on a non-safety fault (an availability bug). Pin the soft-degrade with a write-error test. `pin: daemon/tests/session_prompt.rs::test_prompt_write_error_does_not_fail_the_session; security-reviewer confirms the classification on any new I/O added to a safety path`

## <a id="31"></a>31. The audit-backbone circuit-breaker — per-action deny is the one-off control; the breaker declares SYSTEMIC → fail-secure quiesce-and-refuse (latched); reset only on a proven audit commit

**Date:** 2026-06-13.
**Source slice:** 4.0b-2c (`bf0ad74`; brief 054; security-reviewer CLEAR; +12 tests, 384 pass). USER-ruled posture (fail-stop = best practice); lead-ruled mechanism (B = quiesce-and-refuse).

The single audited mutator must not OPERATE when it cannot AUDIT (§15 #5 / §17). The per-action fail-closed (LESSON §21/§26) already protects the invariant on EVERY fault — a faulted audit-write → the mutation is denied + rolled back + the durable C2 alarm fires (`integrity.rs`). 4.0b-2c adds the layer ABOVE that: a daemon-wide **circuit-breaker** that declares a SYSTEMIC failure and FAIL-STOPS, so the daemon doesn't grind on denying-every-action in a known-broken state.

- **SYSTEMIC vs transient is the crux.** A single fault is NOT systemic (the per-action deny + alarm handle it; the breaker only COUNTS it). SYSTEMIC = **N=5 consecutive** audit-write faults with no intervening clean commit, OR **one clearly-unrecoverable class** (`SQLITE_FULL`/`CORRUPT`/`IOERR`/`NOTADB` = disk-full/DB-corruption/IO) → fast-trip. A transient class (`SQLITE_BUSY`/`LOCKED`) **and any UNCLASSIFIED write error** count toward N but NEVER fast-trip — unclassified→recoverable is the safe direction (the per-action deny already protects the invariant, so don't false-latch the whole daemon on an unknown transient).
- **Reset ONLY on a proven audit commit (`Ok`); every non-`AuditWriteFailed` error is no-op (NOT Reset).** A recorded deny returns `Ok(Denied)` (it committed an event) → resets via the `Ok` arm; but a pre-audit reject (`Err(PolicyDenied)` on an uncatalogued type, `Serialize`, `NotFound`) returns cleanly WITHOUT exercising the audit-write path → resetting on it would let denial-spam (an agent flooding uncatalogued/denied tool calls) MASK a concurrent systemic audit fault. Only a PROVEN audit success clears the consecutive run. "Consecutive" = "audit faults uninterrupted by a successful audit COMMIT."
- **Fail-stop = quiesce-and-refuse (RULED-B), not process-exit.** A **latched** Gateway breaker denies every mutation (`GatewayError::AuditBackboneDown`) WITHOUT attempting an audit-write, while reads/subscribe/terminals stay LIVE (the operator keeps eyes-on) + the durable off-DB alarm (`IntegrityKind::AuditBackboneSystemicFailure`, content-free) + the queryable `is_tripped()` are the loud signal. Process-exit was rejected — it crash-loops on persistent disk-full + kills observability. **Never auto-resets** (restart-only — the rule-#6 never-auto-resolved ethos; a systemic audit fault is not silently recovered mid-run).
- **Placement = the write-actor (the single Gateway-driver chokepoint, forbidden #2/#3), NOT `gateway_txn`** (which lives in `eventstore/` — below the gateway layer; coupling it to the breaker [which classifies `GatewayError`] + the integrity alarm would invert the import DAG). The gate+feed wrap the 4 mutation commands (submit/plan/approve/deny) AND the live intercept (explicitly `is_tripped`-gated → Deny without routing); every mutation path is gated-or-fails-closed when tripped (no slip — the single write-actor serializes the latch-check + the txn). **SCOPE:** the Gateway mutation backbone only — NOT the observation-event substrate (Telemetry/Terminal/Device/`AuditIntegrityViolation` — write-actor-direct, LESSON §10/§23, not mutation-audit) nor cold-start `reconcile_orphans` (boot-time, breaker freshly-zeroed; cold_start owns boot-failure).
- **Daemon-internal — no `shared/` surface, no CONTRACT bump** (`AuditBackboneSystemicFailure` is a new `IntegrityKind`; `AuditBackboneDown` reuses §6.4 `internal_error`). The C2-sink posture.

**Rule:** When the trust core can't perform its load-bearing safety I/O *systemically* (not a one-off — that's the per-action fail-closed's job), STOP rather than grind: a latched circuit-breaker over the audit-fault pipeline (N-consecutive OR an unrecoverable class) → quiesce-and-refuse (deny every mutation without attempting the broken I/O; reads stay live so the operator sees the incident; never auto-reset). Reset the consecutive run ONLY on a PROVEN success of the guarded I/O — a routine reject that never touched the I/O must not clear the run (anti-masking). Gate at the established single chokepoint, not a layer below it. `pin: daemon/tests/circuit_breaker.rs (one-off-no-trip / N-consecutive-trip / reset-only-on-Ok+interleave / unrecoverable-fast-trip / no-auto-reset / tripped-denies-without-audit-write / reads-live / intercept-gated); security-reviewer own pass (cat-1-adjacent §15/§17). NOTE (deferred-low): classify_audit_fault inspects only Write(SqliteFailure) — a future change to the append error-wrapping must re-verify the unrecoverable allow-list stays reachable.`

## <a id="32"></a>32. A non-standing-grantable action — an explicit catalog field generalizes the risk-4 floor onto ONE mechanism (both disjuncts kept + a drift-invariant test)

**Date:** 2026-06-13.
**Source slice:** 4.0b-ui1 (`9848513`; brief 052; security-reviewer CLEAR). USER-ruled: `git.discard_hunk` is destructive/irreversible → must ALWAYS get a per-action human approval, never be swept up in a bulk/standing approval.

Some actions must NEVER be standing-granted (pre-authorized so future matching actions auto-approve) regardless of risk class — for `git.discard_hunk` (risk-3) it's because the mutation is **irreversible content loss**. Before this, the only non-standing-grantable case was `workflow.command.invoke`, realized implicitly as the §11.5 "critical (risk-4) never in approve-all" exclusion (`is_critical`). 4.0b-ui1 makes it an EXPLICIT catalog field.

- **`standing_grant_eligible: bool`** on the catalog entry (default `true`; `false` for `git.discard_hunk` + `workflow.command.invoke`). The plan approve-all exclusion predicate generalizes `is_critical` → **`risk==Level4 OR !standing_grant_eligible`** — and `workflow.command.invoke` is **unified onto the field** (=false), so ONE mechanism expresses the floor.
- **Keep BOTH disjuncts (defense-in-depth)** + pin the relationship with a **drift-invariant test** (`risk-4 ⇒ !standing_grant_eligible` across the whole catalog), so a future risk-4 addition that forgets the field is still excluded AND the two conditions can't silently disagree. Don't collapse to `!standing_grant_eligible` alone — the risk-4 disjunct is the backstop.
- **Risk class ≠ standing-grant-eligibility.** Risk-3 `github.create_pr` stays standing-grantable; risk-3 `git.discard_hunk` does not. The eligibility is its OWN axis (irreversibility / blast-radius), keyed off the explicit field, never inferred from risk.
- A 2nd catalog ctor (`entry_no_standing_grant(...)`) sets the field for only the 2 floor entries; the other ~29 keep the default-true `entry()` — minimal call-site churn on a frozen surface.

**Rule:** When an action must never be standing-/bulk-approved (irreversibility, blast-radius — independent of risk class), express it as an EXPLICIT catalog field, not an implicit risk-class side effect; generalize any pre-existing implicit floor onto the same field; keep the old risk-class disjunct as defense-in-depth + pin the two with a drift-invariant test. `pin: daemon/tests/policy.rs (discard non-standing-grant adversarial + standing_grant_eligible field + risk-4⇒non-grant invariant); security-reviewer on the §6.2 floor`

## <a id="33"></a>33. A diff is a request-on-demand git2-live read, NOT a projection — large/transient data doesn't belong in the event-fed read models

**Date:** 2026-06-13.
**Source slice:** 4.0b-ui1 (`9848513`; brief 052). USER-ruled: the diff source is a `get_diff` RPC, not a `Diff` projection.

The projection-driven read model (`proj_*`, event-fed, subscribe-pushed) is the default UI read path — but it's wrong for **large, transient, on-demand** data like a file diff. A diff is big (potentially thousands of lines), ephemeral (changes every keystroke in the worktree), and only needed when a human opens a specific file — folding it into an event-fed projection would bloat the event log + the projection tables + the subscribe stream for data nobody persists.

- **`get_diff(worktree_id, file) → DiffResult{hunks}`** is a §6.1 GatewayPort **READ RPC** (request-on-demand), reading **git2 LIVE** (read-only) off the worktree — the §7.2 worktree-status live-read precedent (the git axis is a live-read cache, NOT an event). Reads `worktree_id→proj_worktree.path` over a read-only WAL conn, then git2; NO mutation, NO write-actor.
- **A read RPC genuinely needs a not-found code** — added `IpcErrorCode::NotFound` (the unpopulated-worktree case); mapping to `precondition_stale` (a re-approvable card) or `internal_error` (a fault) would be semantically wrong.
- New first git2 use in the daemon → a dedicated read-only `daemon/src/git/` module (the Phase-5 git-CLI mutation actions register their executor separately via the R1-A seam; git2 stays read-only, forbidden #6).

**Rule:** Route large/transient/on-demand data (diffs, file contents, search results) through a **request-on-demand read RPC reading the live source**, never an event-fed projection — projections are for the small, durable, subscribe-worthy state. A read RPC needs its own not-found error code. `pin: daemon/tests/get_diff.rs (structured hunks + read-only/NotFound); shared/tests/contract.rs (Hunk snapshot @ 0.28.0)`

## <a id="34"></a>34. A long-held wait on a shared accept pool starves it — give the wait its OWN bounded class (a reserved sub-bound below the general cap); exhaustion fails CLOSED, never a bypass

**Date:** 2026-06-13.
**Source slice:** 4.0b-2-F2 (`4a7572a`; brief 055; security-reviewer CLEAR). Finding origin = the 4.0b-2 C2 security pass (F2); the user's multi-Max-plan goal = real concurrent multi-agent supervision.

A connection that BLOCKS for a long time (the live `intercept`'s up-to-5-min approval-wait) while holding a slot from a SHARED, bounded accept pool can starve every other client. Concretely: enough concurrent intercept-waits exhausted the single `Semaphore(MAX_CONNECTIONS)` → the UI `approve`/`deny` connection was REFUSED → the pending intercepts timed out → fail-closed Deny → degraded availability. A **circular starvation**: the waits hold every slot waiting for an approval the UI can't deliver because it can't get a slot.

- **The fix = a reserved sub-bound, not a single pool.** Add a 2nd `Semaphore(wait_cap = MAX − RESERVED)`; a parked wait holds it ALONGSIDE its general permit. Because `wait_cap < MAX`, at most `wait_cap` general permits are ever held by waits → `RESERVED` general slots are ALWAYS free for non-wait connections (UI/reads/subscribe). No accept-time classification (you don't know a connection's purpose at accept), no mid-connection permit release.
- **Exhaustion fails CLOSED (toward Deny), never a bypass.** When the wait class is full, a new `AwaitingApproval` intercept denies immediately (`INTERCEPT_SATURATED_REASON`, distinct from the timeout Deny) — the wait is NEVER entered, no park, no bypass; the gate semantics are unchanged (a `Resolved` verdict touches no wait permit). **Acquire the wait permit AFTER the audit commit** (`intercept_blocking` adjudicates+audits first — §15 #5 audit-before-verdict) but BEFORE registering the wait, so an exhausted attempt is AUDITED, then Deny.
- **The permit is RAII** (`OwnedSemaphorePermit` dropped on the closure's return → released on EVERY terminal: verdict / timeout / cancel / death / bridge-drop). No leak.
- **Test via a PURE decision seam, not a flaky concurrent harness.** Factor the decision into `intercept_verdict_with_wait_class(outcome, &wait_class, register_and_wait: FnOnce) → verdict` + a thin `InterceptWaitClass` wrapper (the try_park/`wait_cap` arithmetic) — deterministically unit-testable (the LESSONS §9/§12 pure-classifier family). The proof of the LIVE guarantee moves to **Step-7.5 /wired** (the handler delegates with the REAL wait class, `wait_cap = MAX − RESERVED` in production) — make that reachability check load-bearing.

**Rule:** When a connection holds a slot from a shared bounded pool while blocking for a long time, give the long wait its OWN bounded class as a **reserved sub-bound below the general cap** (so non-wait connections keep guaranteed headroom), and **fail CLOSED on wait-class exhaustion** (never a bypass; acquire after the audit, before the wait). Test the decision as a PURE seam + prove the live wiring with /wired, not a flaky concurrent harness. `pin: daemon/tests/intercept_wait.rs (reserved-headroom / exhaustion-fail-closed-no-bypass / drain / release-on-terminal / resolved-touches-nothing); /wired the live handler (wait_cap=MAX−RESERVED); security-reviewer own pass`

## <a id="35"></a>35. A production observation-event sink holds the WriteHandle + is injected into the cat-1 edge via an OPAQUE factory; the launcher↔write-actor bootstrap cycle breaks via a deferred handle slot

**Date:** 2026-06-13.
**Source slice:** 4.0c (`c0a935d`; brief 056; code-quality 0 high; NON-safety). The 044 telemetry seam wired to production.

A non-mutation OBSERVATION event the daemon emits (telemetry/usage; LESSONS §10/§23) is written via the **write-actor**, NOT the Gateway — but the EDGE module that drives the adapter (the cat-1 `session/`, import-grep-enforced no-`WriteHandle`) must not hold the `WriteHandle` directly. The pattern:

- **Construct the production sink in `runtime/`** (where the `WriteHandle` lives) + **inject it into the edge adapter via an OPAQUE factory trait** (`TelemetrySinkFactory` in `harness/`, prod impl in `runtime/` closing over the `WriteHandle`). The edge imports only the FACTORY TRAIT, never `WriteHandle`/`runtime`/`eventstore`/`gateway` — the cat-1 import-grep stays clean (verify it via /wired).
- **Emit is fire-and-forget** (`WriteHandle::try_append_observation` = `try_send`, drop-on-full): an observation never back-pressures the writer (forbidden #3 / LESSONS §9) and degrades SOFT (LESSONS §30 — non-safety; a dropped sample = a slightly-under-counted ledger, accepted). It still routes through the write-actor's §15 redaction gate (same append path, not a bypass).
- **The launcher↔write-actor bootstrap CYCLE** (the launcher needs the `WriteHandle` to build the sink, but the `WriteHandle`/write-actor is constructed in the same wiring) breaks via a **deferred handle slot** (`OnceLock`): main.rs builds the factory with an unbound slot, spawns the write-actor, then `slot.bind(handle)` post-spawn; pre-bind emits drop (soft-degrade), post-bind append. Test the deferred path (pre-bind drops / post-bind appends).
- **The pump rides the session-actor tick** (a `poll_telemetry()` default-no-op on the adapter trait; the real adapter drains an injectable `UsageSource` per tick → emit). The cadence wiring is proven by **/wired** (the actor tick → poll), the per-tick logic by a pure unit — not a flaky timing test (LESSONS §9/§12/§34). The **cumulative-baseline (high-water-mark) clamp** lives in the stateful ingest (`push_usage`); the pure delta-computation (`telemetry_sample`) keeps its per-delta `.max(0)` floor — the baseline fixes a dip-then-climb OVER-count the delta-floor alone misses.

**Rule:** Wire a production observation-event sink by constructing it where the `WriteHandle` lives + injecting it into the cat-1 EDGE via an OPAQUE factory trait (the edge never imports `WriteHandle`); emit fire-and-forget (soft-degrade, never back-pressure the writer); break a launcher↔write-actor bootstrap cycle with a deferred handle slot (test pre/post-bind); prove the pump cadence with /wired + a pure per-tick unit, never a flaky timer; put a stateful high-water-mark clamp in the ingest, keep the delta-computation pure. `pin: daemon/tests/telemetry_pump.rs (production sink + ledger-SUM + factory-injection + deferred-slot) + claude_telemetry.rs (cumulative clamp + poll-drain); /wired the actor tick + main.rs factory inject + session/ cat-1 import-grep`

## <a id="36"></a>36. A multi-strategy recovery DECISION is a pure TOTAL classifier over availability inputs; attempt-failure/retry is the CALLER's loop, not a classifier input

**Date:** 2026-06-13.
**Source slice:** 4.1a (`60b3919` freeze + `e408024` decision logic; brief 057; NON-cat-1; the §8.1 B2-strict survival head). The resume/replay/reattach decision logic.

A recovery flow that picks among several strategies (the §8.1 survival ladder: reattach-live / resume / replay / relaunch) is built as a **pure, TOTAL classifier over AVAILABILITY inputs** — NOT a state machine, NOT the live attempt, NOT the attempt's failure.

- **`decide_resume(&ResumeInputs) -> ResumeResult`** reads only what's AVAILABLE (`broker_has_live_session` / `supports_resume`+`has_resume_handle` / `has_scrollback` / `replayed_event_count`) and returns the §9.1-frozen `ResumeResult{mode, replayed_event_count}` in **strict precedence**: broker-live → `ReattachedLive` · resume-capable+handle → `Resumed` · scrollback → `Replayed` · else → `Relaunched`. Total (an `else` rung, never a panic/None) + pure (no I/O) → deterministically test-first, the LESSONS §9/§12/§25/§34 pure-classifier family applied to survival.
- **Attempt-failure / retry is the CALLER's loop, NOT a classifier input.** "resume failed → relaunch" is the bootstrap (4.1b) **re-calling** with the failed option removed (`has_resume_handle=false` → the ladder falls to the next rung), NOT a `resume_failed` flag folded into the function. Folding the failure in would make it a state machine, not a total classifier — keep the function deciding STRATEGY-from-availability; keep retry/outcome in the caller.
- **Harness-agnostic by construction.** The classifier reads CAPABILITY bits (`supports_resume`), never harness identity → ONE function serves Claude AND Codex; only the CALLER populates the per-harness inputs. (Scoping corollary: the decision is testable for BOTH harness shapes via fakes NOW, even before a harness's live adapter exists — only the live attempt verification defers to that adapter, e.g. live-Codex `thread/resume` → 3.3.)
- **Test each rung's BOTH conjuncts.** The `Resumed` rung is `supports_resume && has_resume_handle` — a `capability-present, handle-missing` test pins that a capability-only bug falls THROUGH the rung (an AND whose second conjunct is never exercised as the failing one is an untested branch). Pin strict precedence (a higher rung's inputs present alongside a lower's → the higher wins) + count-carried-only-on-`Replayed`.
- **The §14 testability split.** The DECISION logic is deterministic (test-first); the LIVE SURVIVAL it decides over (the agent actually outliving the daemon via the broker, the reattach to a live turn) is a live-process property → a labelled 0.1/0.3-HITL verify-only follow-on (§8.1). Don't conflate "decide the strategy" (deterministic) with "the strategy works live" (HITL).

**Rule:** Build a multi-strategy recovery decision as a pure TOTAL classifier over availability inputs in strict precedence (returning the frozen result type), keep attempt-failure/retry in the caller's loop (re-decide with the failed option removed — never a `*_failed` classifier input), make it harness-agnostic via capability bits, and test each rung's conjuncts + the precedence + the total `else`. The live survival it decides over is the HITL follow-on, not part of this slice. `pin: daemon/tests/resume.rs (ladder precedence + Resumed-needs-both-conjuncts + harness-agnostic-both-shapes + count-only-on-replayed + total-else-relaunch)`

## <a id="37"></a>37. Surface an authoritative computed artifact on a projection by persisting it on the registry row at compute-time (the projector sibling-reads it) — and freeze a safety-critical projection-row as a TYPED contract served typed/fail-closed

**Date:** 2026-06-13.
**Source slice:** 4.0b-ui2 / the ②-mini (`4e9579d` persist + `8c4b948` freeze; brief 058; user-ruled Fork B; security PASS; the first frozen projection-row). Surfaced REAL risk + `PolicyDecision` on `proj_approval_queue` for the ui's human-approval card.

When a read model needs an authoritative artifact the Gateway/engine COMPUTES (the `PolicyDecision` at `pipeline.rs:154`), do NOT recompute it in the projector (the read layer has no policy engine; a recompute drifts from the audited decision). Instead:

- **Persist the artifact on the REGISTRY row at compute-time, then sibling-read it** (LESSON §17 generalized — not just immutable registry *fields*, but a computed *decision* artifact): the `PolicyDecision` is captured on the `approvals` row at approval-open and the projector sibling-reads `policy_decision_json` into `proj_approval_queue` (IMMUTABLE → rebuild-safe; never derive a MUTABLE field this way). Historical rows that predate the column carry NULL (no source to backfill) — still rebuild-equivalent.
- **§15 redact the new payload before INSERT** even though it's engine-generated (fail-closed): co-locate the redaction INSIDE the registry-insert helper (`approval::insert` takes `gtx` + `gtx.redact_row`, mirroring `request::insert` — the dual-gate, LESSON §16) so a call-site can't forget it. The "no secrets in it" claim is a human/Step-9 gate; the redactor is the MECHANISM (a planted secret-shaped reason masks → `[REDACTED]`, pinned).
- **Freeze a safety-critical projection-row as a TYPED `shared/` contract + serve it typed, fail-closed.** When the consumer is a safety surface (the human-approval card), the row is a CONTRACT, not opaque JSON: the first frozen projection-row lives in `shared/src/projections.rs` (`ApprovalQueueRow` — type the load-bearing fields `risk_level: RiskLevel`/`status: Approval`/`policy_decision: Option<PolicyDecision>`; `deny_unknown_fields`; optionals-as-null; OMIT internal bookkeeping like `sort_key`/`updated_at_seq`; match the consumer's existing provisional field names to minimize regen churn). `get_projection` branches the safety projection to a `read_*_typed` (deserialize each DB row → the struct → serialize) that **fails CLOSED** on a mis-typed/NULL-mishandled value — never loose-JSON parsing on the human-approval path; other projections keep the generic JSON read. Test the typed serve over BOTH a `Some` and a `None` payload (a NULL that mis-deserializes breaks the WHOLE read). The remaining projection-row provisionals (Session/ProjectActivity/PullRequest/Audit) follow this pattern. `pin: shared/tests/contract.rs (ApprovalQueueRow snapshot @0.30.0 + deny-unknown) + daemon/tests/gateway*.rs (persist + §15-redaction + sibling-read + rebuild-equivalence + typed-serve Some/None + plan-level-NULL)`

## <a id="38"></a>38. Daemon-restart session recovery = enumerate-live → decide_resume → dispatch-over-the-seam; the recovery-spawn is a System-actor AUDITED recovery (the daemon re-materializing its OWN approved session), NOT a Gateway Action

**Date:** 2026-06-13.
**Source slice:** 4.1b-1 (`475068b` orchestration + `1e68f20` SessionRecovered/driver; brief 059; security-reviewer CLEAR; the §8.1 B2-strict survival, deterministic half). The restart session-recovery orchestration — the production driver of 4.1a's `decide_resume`.

Daemon-restart session recovery is built as a **deterministic orchestration over seams**: on restart (after `cold_start` + the runtime/supervisor are up, `main.rs`, pre-accept-loop) `recover_sessions_on_restart` enumerates the **non-terminal `proj_session` rows** (read-only WAL; an `is_recoverable_status` §5.1 terminal-set predicate) → per session builds `ResumeInputs` → `decide_resume` (§36) → dispatches the strategy over a launcher/**`Broker` seam** (`FakeBroker` test-side; the real detachable-terminal broker + the LIVE survival = the next slice / the §14 HITL follow-on). Test-first with `FakeBroker`/`FakeLauncher` + injectable `Clock`/`IdGen`.

**The trust-boundary ruling (Q1=(a), lead/user 2026-06-13): a recovery-RELAUNCH spawn is a System-actor recovery event, NOT a Gateway Action.** A daemon re-materializing its OWN already-approved prior session is the **LESSON §10 substrate** (the daemon's own lifecycle — Device/LocalRunner registration is already a System-actor event in the SAME §16 restart sequence), extended to a structurally-analogous case — INV-SEC-1 governs *untrusted-proposer* intents, not the daemon re-establishing its own approved state. Reusing the Gateway `session.create` ((b)) is WRONG: it mints a FRESH `SessionId` (orphans the original — leases/transcript/§11.4 are keyed to it), re-approves the already-approved (contradicts approved-pre-crash), and buys ZERO safety.

**(a) is INV-SEC-1-faithful only if all four conditions hold (the sign-off gate + the security pass verify each):** (1) **genuinely-approved-only** — identity + §15 #8 profile sourced from the **COMMITTED prior `SessionStarted`** (the authoritative event store), never fabricate authority for a never-approved session (the `is_recoverable_status` predicate + reading the committed event enforce it); (2) **AUDITED** — the recovery emits a `SessionRecovered` OBSERVATION event via the §15-gated write-actor (rule #5; CONTRACT-additive, the LESSON §10/§23 non-Gateway observation family); (3) **§15 #8 profile PRESERVED** (the original `SessionStarted` profile — no re-resolution, no account-hop); (4) the recovered agent's **SUBSEQUENT tool calls STILL route through the live interception→Gateway** (INV-SEC-1 holds for the agent's actual mutations — the recovery spawn grants ZERO new authority). Keep the **no-2nd-mutator boundary STRUCTURAL** — the orchestration's signature carries the seam + an observation sink but NO `WriteHandle`/`submit` (recovery proposes/recovers, never a 2nd Gateway mutator; the 4.0a import-grep ethos). `pin: daemon/tests/recovery_restart.rs (the 7 — reattach/resume/replay/relaunch ladder + profile-preserved + is_recoverable_status + observation-not-Gateway) + recovery_restart_wiring.rs (profile-from-committed-SessionStarted + /wired decide_resume reachability); security-reviewer on the recovery slice (the 4 conditions)`

## <a id="39"></a>39. A detachable-terminal survival broker WRAPS the unchanged launch spec inside the multiplexer (transport, not a new #10 owner); the safety invariant is a property of the constructed argv (deterministic), the live survival is HITL; OPTIONAL upgrade behind a pure probe + consistent backend-selection, graceful-degrading

**Date:** 2026-06-13.
**Source slice:** 4.1b-2 (`0b9f78a`; brief 060; USER-ruled mechanism A = tmux; security-reviewer CLEAR ×6; the §8.1 B2-strict survival, the broker's deterministic half).

B2-strict survival (the agent outliving the daemon) needs a **detachable-terminal broker** — a surviving-PTY holder the daemon attaches/detaches to (the agent must NOT be a direct daemon PTY-child, else daemon death → SIGHUP → agent dies, §8.1). Realize it by **WRAPPING the UNCHANGED launch spec inside the multiplexer** (`TmuxLauncher` builds `tmux new-session -d -s nexusops-<sess_…> -c CWD -- env <derived> claude <args>` over the same `ClaudeLaunchSpec`): **tmux is a TRANSPORT, NOT a new #10 owner** — the launcher stays the single live-spawn site, the O-13 #10 spec (default mode / no `-p` / no bg / 0600 generated settings) is wrapped not reconstructed, so the #10 + §15 #8 invariants survive the new layer by construction.

**The safety invariant is a property of the CONSTRUCTED ARGV → deterministically pinned, even though the live survival is HITL.** The §15 #8 env hygiene is the sharp case: tmux `-e` only SETS, never UNSETS (and a pre-existing tmux server injects its own env), so it **cannot** strip a stale `ANTHROPIC_API_KEY` — derive an **`env(1)` wrapper** (`env -u K`/`K=V`) from `spec.env_mutations()` (`Remove(k)→-u k`, `Set(k,v)→k=v`): `env -u` unsets at exec time regardless of the multiplexer's server env, and the strip becomes a property of the argv the launcher builds → unit-testable (assert the `-u` + no bare leak), no live process needed. Same for fail-closed-settings (`write_settings()?` FIRST, before new-session/spawn — an earlier-step Err → no session) and the consistent **backend-selection** (a pure `select_survival_backend(probe)` yields a CONSISTENT pair — never a surviving-launcher + non-surviving-broker mismatch that would claim a survivor it never created). The **LIVE survival** (the agent actually outliving the daemon + the lossy alt-screen VT-reattach — RISKS.md's "hardest correctness problem") + the live recovery-dispatch re-materialization are the **0.1/0.3-HITL verify-only follow-on** (§14): a live-process property, NOT deterministically unit-testable; the deterministic broker just makes `ReattachedLive` reachable + signalled.

**OPTIONAL upgrade, graceful-degrade.** A surviving-PTY multiplexer is an external dependency the app must work WITHOUT: a startup **probe** (`tmux -V`) drives the selection → absent → the non-surviving `PtyLauncher` (B2-achievable: resume/replay, never `ReattachedLive`). The probe + the degrade path are themselves deterministic + test-first. The broker reports a survivor **fail-safe** (a `list-sessions` runner-error/non-zero → no-survivor, never errors recovery).

**A permanently-un-unit-testable safety surface gets a verification home in the e2e-acceptance-matrix, not an inline TODO** (it rots): the actual on-disk 0600 settings + the #10 content reaching the live tmux-spawned agent is a live property → a MANUAL matrix line (lead-directed). A separable deterministic gap (the settings-path-injection leaf-test for both launchers) is a Carry-forward hardening TODO.

**Commit structure:** a slice whose layers live in ONE cohesive file (here `session/tmux.rs` L1+L2+L3) is ONE commit when security-CLEAR as a whole + non-splittable without hunk-staging — the "safety-pin-its-own-commit" rule assumes a SEPARABLE pin; don't fabricate synthetic intermediate states via a stash-dance for marginal bisectability.

`pin: daemon/tests/tmux_broker.rs (name round-trip + argv builders + probe + list-parse + TmuxBroker fail-safe + selection-consistency + wrapped-spec + fail-closed-new-session + env-hygiene-derivation + degrade-never-reattaches); /wired select_survival_backend; security-reviewer (the #10/§15-#8 surface through the tmux layer)`

## <a id="40"></a>40. A supervised-child DEATH is a System-actor observation event emitted by a runtime/ driver over the cat-1 supervisor reap seam via an injected sink (the §35/§38 pattern, generalized); the known-shape-vs-defer principle applies to the EVENT PAYLOAD too — empty-payload until distinguishing signal exists

**Date:** 2026-06-14.
**Source slice:** 4.2 (`463abc0` C1 + `98488f2` C2; brief 061; security-reviewer CLEAR; the §17 child-death surface, the cascade HEAD; the 3 cascade arms deferred).

The session-lifecycle OBSERVATION-via-injected-sink-over-the-cat-1-seam pattern now has THREE applications — telemetry (§35), restart-recovery (§38), and now in-life child-DEATH (4.2): a runtime/ driver bridges the cat-1 `session/` supervisor to the write-actor via an INJECTED sink trait, so the cat-1 boundary holds because `session/` holds only the trait (`Box<dyn SessionDeathSink>`), NEVER a `WriteHandle`. A supervised-child death is a **System-actor observation event** (`SessionFailed`, the LESSON §10/§23 non-Gateway family — a death notification is not a state mutation; written via the §15-gated write-actor, 0 `Action*`), emitted ONLY on a `(id, Failed)` reap (a clean `Killed`/`Completed` reap is not a failure); the `proj_session` projector folds it to `status=Failed` (mutable-status-from-the-event-type → rebuild-safe, LESSON §17), which drives the §11.4 "restart session" affordance. The reap→emit router is an extracted PURE `handle_reaped(reaped, …, sink)` (timing-free, testable); the live SIGKILL/orphan survival is the §17 HITL surface.

**The known-shape-vs-defer principle applies to the EVENT PAYLOAD, not just to whole arms.** At 4.2 the supervisor reap yields ONLY `Session::Failed` — a panicked/cancelled actor and a status-machine-Failed actor reap indistinguishably — so a `reason` field would be a CONSTANT placeholder with no distinguishing signal. Freeze it **EMPTY-payload** (`SessionFailed {}` — the `WorktreeMerged`/`ActionStarted {}` precedent; the death fact + the envelope session_id suffice; the forensic "why" is the correlated `TerminalProcessExited{exit_code,signal}`, NOT duplicated) and add a `reason` as a clean additive-later bump WHEN distinguishing signal exists. Don't freeze a placeholder taxonomy against an undefined shape — the same defer-discipline the lead applied to the cascade ARMS (fail-in-flight-action → Phase 5/7 · release-lease → session-leases · Codex distinction → 3.3), applied one level down.

`pin: daemon/tests/session_failed.rs (failed-reap-emits + clean-reap-no-emit + driver-via-write-actor-0-Action + projector-fold + rebuild-equivalent) + the existing session/ cat-1 import-grep guard; shared/tests/contract.rs (SessionFailed @0.32.0)`

## <a id="41"></a>41. A periodic maintenance job (WAL checkpoint / staleness recompute) is a long-lived Tokio interval task in the spawn_drainer/spawn_reaper family; a WRITE pass rides the single write-actor command (never a rogue writer); a derived signal is a live-read recompute; a sync DB read off the async loop rides spawn_blocking

**Date:** 2026-06-14.
**Source slice:** 4.3 (`81c9242`; brief 062; security-reviewer CLEAR; the LAST Phase-4 task — the deterministic background-jobs core).

A periodic maintenance job (WAL checkpoint, staleness recompute) is built as a **long-lived Tokio interval task in the `spawn_drainer`/`spawn_reaper` family** (LESSON §9 / §10): a deterministic `*_once` bounded pass + the spawn loop (`tokio::time::interval` + `MissedTickBehavior::Delay`, shutdown-watch-aware, survives a failed pass, never back-pressures the writer). Three sub-rules:
- **A WRITE pass rides the SINGLE write-actor command** — the WAL checkpointer sends a `Command::WalCheckpoint` to the write-actor (the `wal_checkpoint` PRAGMA on the write-actor's OWN connection), NEVER a second/rogue writable connection (forbidden #3; grep-pinned + security-CLEAR, the §24/§28 structural-guard idiom). PASSIVE on the production interval (non-blocking, won't contend with the read-only WAL readers); the `WalCheckpointMode`(Passive|Truncate) enum keeps a future size-threshold TRUNCATE path a drop-in.
- **A derived signal is a live-read recompute, NOT event-folded** — session `stale` is a pure `is_stale(Option<&heartbeat>, now, threshold)` (strict `>`; None/unparseable → NOT stale, absence ≠ stale) recomputed over `proj_session` (read-only WAL) each poll, never a stored/replayed bit (the worktree-status live-read precedent). Prove "recomputed-not-replayed" via a **clock-flip on fixed rows** (same rows flip not-stale→stale as the poll clock advances) — stronger than a `rebuild_projections()` assertion, which is MOOT when the source field has no event producer.
- **A synchronous DB read off the async loop rides `spawn_blocking`** (don't stall a tokio worker on a blocking SQLite read; LESSON §9); log a derived count **on-change only** (the drainer's discipline, not per-tick spam).
A job whose live SOURCE is unbuilt is **correct-but-DORMANT** (the staleness poller until a `last_heartbeat_at` writer lands — the telemetry-pump-UsageSource precedent): build the deterministic mechanism test-first; the live source is the producer-gated follow-on.

`pin: daemon/tests/jobs.rs (is_stale + live-recompute-clock-flip + wal_checkpoint-Passive/Truncate + no-rogue-writer grep-guard + loop-shutdown/survive-failed-pass); /wired the main.rs spawns; security-reviewer (single-writer)`

## <a id="42"></a>42. A SECOND harness adapter implements the SAME frozen §9.1 trait as the first — no new trait; the divergence is the PARSER (two vendor vocabularies → one signal enum → one pure derive_status fold); classify tool calls by SEMANTICS not vendor names; a multi-turn session's turn boundaries are NON-terminal status (terminal = the process-exit signal)

**Date:** 2026-06-14.
**Source slice:** 3.3a (`c4a4652`; brief 063; security-reviewer CLEAR; the head of the Codex arc — the deterministic observe core, fixture-tested).

A second harness adapter (Codex, after Claude) implements the **SAME frozen §9.1 `HarnessAdapter` trait** — NO new trait (LESSON §25; the trait froze once at 3.1). The divergence is entirely in the **PARSER**: map the vendor's vocabularies onto the shared normalized types. Pattern (mirrors the ClaudeAdapter `ClaudeSignal`→`derive_status` precedent):
- **Two vendor vocabularies → ONE signal enum → ONE pure `derive_status` fold.** Codex has two surfaces — the persisted rollout-JSONL (`session_meta`/`turn_context`/`response_item`/`event_msg`) and the `exec --json` `ThreadEvent` stream — both normalize to ONE `CodexSignal`, folded by ONE pure `(prev,CodexSignal)→Session` (the §25 fold). The two-vocab→one-signal collapse keeps the status logic single-sourced.
- **Classify tool calls BY SEMANTICS, not vendor names.** Vendor builds diverge on tool names (Codex OSS `shell`/`local_shell` vs desktop `exec_command`; `apply_patch` = the file-patch tool) → a `CodexToolKind{ShellExec,FilePatch,McpTool,Other}` keyed on a semantics predicate, so every build maps + the downstream interception (3.3c) keys on the KIND, not the name.
- **A multi-turn session's turn boundaries are NON-TERMINAL status; TERMINAL = the process-exit signal.** §5.1 Failed/Completed/Killed are terminal (R-9 sink — no resurrection); a turn boundary (turn abort/complete) is NOT session death. So the observe-from-transcript fold produces ONLY non-terminal states (turn-end→Idle) — mapping a turn abort→terminal would wrongly R-9-sink a recoverable multi-turn session forever. The terminal states derive from the **process-exit signal** (the live launch/supervisor, a later slice), NOT transcript parsing.
- **Status from the structured stream, NEVER PTY bytes** (#9, grep-pinned); **untrusted external input** (rollout/stream) is parsed tolerantly (reject-unknown / no-panic / no-unwrap / saturating casts). Build the parser/normalizer test-first vs **redacted/synthesized golden fixtures**; the vendor-version fixture refresh + the live drive = the HITL follow-on. NON-cat-1 (no launch/interception/emission — the later arc slices); NO CONTRACT bump (consumes the frozen §9.1 types).

`pin: daemon/tests/codex_adapter.rs (rollout+ThreadEvent parse + derive_status non-terminal fold + classify-by-semantics + #9 grep-pin + golden-fixture); security-reviewer (the #9 boundary + untrusted-input parser)`

## <a id="43"></a>43. The SECOND harness's LAUNCH path mirrors the first's launch-spec-as-enforcement-surface (a pure no-bypass argv); §15 #8 = resolve-and-PIN exactly one auth (refuse ambiguous, compare-then-drop the secret, keychain-ref); §15 #11 = pre-create the agent's state dirs + umask the child, fail-closed; built mechanism-first with NO spawn (the binding condition)

**Date:** 2026-06-14.
**Source slice:** 3.3b (`16962c9` C1 §15 #8 auth/profile · `a822015` C2 §15 #11 umask · `a571a62` C3 launch-spec/resume-handle; brief 064; security-reviewer CLEAR; the two §15 pins each its own commit).

A second harness's launch path mirrors the first's **launch-spec-as-enforcement-surface** (`ClaudeLaunchSpec`→`CodexLaunchSpec`, LESSON §25) and applies the §15 invariants per-harness. Pattern:
- **The launch spec is a PURE argv where a bypass is impossible by construction.** `CodexLaunchSpec` builds `codex exec [resume <UUID>] --json --sandbox <s> --ask-for-approval <a> --model <m> --profile <p>` — `--sandbox` is ALWAYS present and there is NO code path that emits `--yolo` / `--dangerously-bypass-approvals-and-sandbox` / `--full-auto` (pinned absent in the argv AND the source, the ClaudeLaunchSpec `--permission-mode default` precedent). The pure construction is separate from the fail-closed I/O.
- **§15 #8 (no silent account-hop) = resolve-and-PIN exactly ONE auth.** `resolve_codex_auth` returns `Err(Ambiguous)` on ≥2 distinct auth METHODS **or** same-method env vars with CONFLICTING values (don't own a contestable precedence); same-value dups → canonical-pin + strip; 0 → `Err(NoAuth)`. **Compare the secret VALUE then DROP it** — the transient `AuthSources` holds env values with NO `Debug` derive, and only the var-NAME + a **keychain-ref** (never the raw secret) enter the bound `CodexExecutionProfile` (rule #4 no-Debug-leak; pin no-leak with a per-METHOD canary incl. a non-`sk-` OAuth shape — a `sk-`-only grep is vacuous). The env-hygiene then strips **every** auth env var except the pinned source → **exactly one auth source reaches the child** (the ClaudeLaunchSpec `ANTHROPIC_API_KEY`-strip generalized; cross-method AND same-method extras). The containment fields (`sandbox`/`approval_policy`) are **required, no `Default` derive** → the slice ships no silent containment value (the cat-1 sandbox-VALUE/containment-proof is a later slice).
- **§15 #11 (vendor-lax perms) = pre-create the agent's state dirs + umask the child, FAIL-CLOSED.** `harden_codex_dirs` pre-creates `~/.codex/sessions` at 0700 (mode-verified) + a `CODEX_CHILD_UMASK=0o077` on the spec (so the vendor's 0755/0644-created rollout dirs/files land 0700/0600). It **fails closed** — a precreate/permission error → `Err` → no launch with un-hardened dirs (LESSON §30: the §15 #11 invariant IS a function of this I/O succeeding). The live "vendor doesn't chmod back" verify is the HITL follow-on.
- **The resume handle keys off the vendor's native resume id, GATED on the artifact's existence.** `has_resume_handle` is the **rollout file's existence** (present→`Resumed` / missing→`Relaunched`), NOT a non-empty session_id (a non-empty-string proxy is always-true → every fresh session falsely `Resumed` — a real bug caught by code-quality); feeds the harness-agnostic `decide_resume` (§36) via `CodexAdapter::resume_inputs`. The experimental app-server `thread/resume`/`thr_` path + the UUID↔`thr_` interconversion = HITL.
- **Mechanism-first with NO spawn (the binding condition).** No production-reachable path spawns the real agent in this slice (no `SessionLauncher` impl, no `Command::new`/`.spawn` over `harness/codex/`, structurally grep-pinned) — the reachable live spawn + its `PreToolUse`→Gateway interception + the `--sandbox` containment land TOGETHER in the next slice (the 042→043 / 4.0b-1→4.0b-2 precedent). NON-cat-1; NO CONTRACT bump (`ExecutionProfileId`/`ExecutionProfile` frozen @0.24.0; `ResumeInputs` daemon-internal). The two §15 pins each get their OWN commit.

`pin: daemon/tests/{codex_auth,codex_perms,codex_launch}.rs (resolve-one-source/refuse-conflicting + no-Debug-leak-per-method-canary + 0700-fail-closed + umask-0o077 + no-bypass-argv + resume-handle-gated-on-rollout + no-spawn-grep); security-reviewer (§15 #8 single-source-pin + §15 #11 fail-closed umask)`

## <a id="44"></a>44. A credential in a git remote URL has NO key-prefix → it is OUTSIDE the §15 Redactor's recall envelope → strip the `user:token@` userinfo AT THE EMIT SOURCE before the event is constructed; the Redactor is the BACKSTOP only

**Date:** 2026-06-15.
**Source slice:** edges-019 (P5.1; `c739278`; brief edges-019; security CLEAR; the first edges FS-read executor + its §15 strip pin). _(Edges-track lesson renumbered into the daemon sequence — was edges' §30.)_

A secret can ride a git remote URL's **userinfo slot** (`https://user:token@host/o/r` — the token lives in the bare-username-or-`user:password` position), and that secret has **NO key-prefix and is not reliably high-entropy** → it falls **OUTSIDE the §15 prefix+entropy Redactor's recall envelope** (LESSON §13 — the Redactor recalls *prefixed* or *high-entropy* runs, not an arbitrary credential embedded in a structured URL). So the control is **strip-at-source, not redact-after**: the `ProjectExecutor` strips the `user:token@` userinfo **AT THE EMIT SOURCE** — before the `ProjectRescanned` payload is constructed (`strip_userinfo` rewrites `https://user:token@github.com/o/r` → `https://github.com/o/r`, scheme/host/path byte-preserved; a URL without userinfo is byte-identical out, no false mutation). The Redactor remains the **BACKSTOP** (defense-in-depth; the §15 entropy fallback still masks a high-entropy `repo_root` component), never the primary control for this field.

The strip is **scheme-scoped + prefix-free**: strip userinfo from **any scheme-bearing URL** (`scheme://[userinfo@]host…` — the credential slot, last-`@`-in-authority delimiter so a token riding the bare-username slot is caught) and leave **scp-style ssh** (`git@github.com:o/r`, no scheme) **INTACT** — there `git@` is the standard ssh username, not a secret; mangling it would corrupt a legitimate remote. The general principle: **a credential that has no detector-visible shape gets stripped at the producer; the Redactor catches what the producer missed, it is not the first line.** `pin: daemon/tests/project_executor.rs (strip_userinfo cases — https-with-creds → stripped / no-creds → byte-identical / scp-ssh → intact / ssh-scheme-userinfo per the strip ruling + the end-to-end emitted-remote_url-stripped pin)`

## <a id="45"></a>45. An external-CLI mutator MUST reject a leading-`-` operand fail-closed — a leading-dash operand is parsed by the CLI as a FLAG → the on-disk result diverges from the approved+audited Action (audit-integrity); a SHARED guard + canonical option-before-operand order; STANDING for every external-CLI mutator

**Date:** 2026-06-15.
**Source slice:** edges-020 / edges-021 (P5.2; `7dabab5` create_worktree + `51f5586` create_branch; briefs edges-020/021; the arg-injection HIGH caught + CLOSED in-slice, security else PASS). _(Edges-track lesson renumbered into the daemon sequence — was edges' §31.)_

A git-CLI mutator (the edges git seam — an injected CLI, forbidden #6 never git2 for mutations) must **REJECT a leading-`-` operand** (`reject_dash_operands`): a leading-dash operand (a branch/path argument that begins with `-`) is parsed by git **as a FLAG**, so the executed mutation **diverges from the approved + audited Action** — the on-disk worktree/branch is NOT what policy approved and the audit log records. This is an **audit-integrity violation, INV-SEC-1-adjacent** (the no-bypass invariant assumes the executed effect equals the approved Action). The guard is **fail-closed** (reject → `ExecutionOutcome::Failed`, no partial git op) + a **canonical option-before-operand arg order** (`--` operand separator / fixed positions so a crafted value can't reposition into a flag slot) + **structural failure reasons** (no raw git stderr into the §15 event — class-name only).

The guard is a **SHARED helper across both git arms** (`create_worktree` + `create_branch` call the same `reject_dash_operands`; `repo_path` is EXEMPT — the repo cwd is not a git operand, it's the working directory). This is a **STANDING REQUIREMENT for every external mutator that takes inputs** (the daemon's own future external mutators want it too): a CLI mutator guards leading-`-`; a typed-API mutator (octocrab/Linear) guards the **analogous parameter-injection vector** for its surface — fail-closed BEFORE the call + a regression test, folded into each mutator brief. `pin: daemon/tests/git_executor.rs (dash-operand reject fail-closed + canonical arg order + the shared guard across both arms + repo_path-exempt)`

## <a id="46"></a>46. A SYNC external-network executor runs on the write-actor's RAW std::thread (no entered runtime → `Handle::current()` PANICS, `spawn_blocking` is WRONG) → inject a captured runtime `Handle` + `handle.block_on`; a MANDATORY timeout on EVERY external call (liveness — the single write-actor serializes ALL mutations); `*SyncFailed` via the additive `FailedWithEvents`

**Date:** 2026-06-15.
**Source slice:** edges-023 / edges-024 (P7.1 Wave-D; `498bd21` github + `f908424` linear; briefs edges-023/024; security-reviewer PASS 0 findings; the external-network mutator pattern, both executors). _(Edges-track lesson renumbered into the daemon sequence — was edges' §32.)_

A SYNC `ActionExecutor::execute()` driving an **async** external client (octocrab/reqwest) runs on the **write-actor's RAW `std::thread`** (LESSON §9 — a dedicated blocking thread, NOT a tokio worker, with NO entered runtime) → `tokio::runtime::Handle::current()` **PANICS** there (no runtime in TLS) and `spawn_blocking` is the **WRONG tool** (it needs a runtime context too, and would re-cross the thread the write-actor already owns). The fix: **inject a captured `tokio::runtime::Handle`** from `main.rs`'s `#[tokio::main]` runtime into the executor and call **`handle.block_on(async { … })`** — `block_on` **from a non-runtime thread does NOT panic** (it drives the future to completion on the calling std::thread). `Handle::current()` is the trap; the captured handle is the fix.

Three load-bearing rules ride this: **(1) a MANDATORY `tokio::time::timeout` on EVERY external-call executor** (a shared `NETWORK_TIMEOUT`, ~30s) — this is **liveness, not politeness**: the single write-actor **serializes ALL daemon mutations**, so an unbounded octocrab/reqwest hang would **freeze the entire mutation path** (every pending Action blocks behind the hung one). **(2)** a `*SyncFailed` domain event is emitted on a **FAILED** action via the additive **`ExecutionOutcome::FailedWithEvents{detail, emitted_events}`** — the pipeline txn-B records `ActionFailed` **and** appends the event **atomically** (existing `Failed(String)` sites untouched; `side_effect_applied()==false`); emitted ONLY for **terminal-non-auth** failures (a shared exhaustive `classify_sync_failure`→`SyncFailure{Auth|TerminalNonAuth|Transient}` so github + linear can't diverge on the disposition; auth/transient → plain `Failed`); the §15 `reason` is the **structural class-name**, never raw API text. **(3) TEST-HARNESS pin:** the `execute()`-path tests are plain `#[test]` + a manually-built `Runtime` + its `.handle()`, **NEVER `#[tokio::test]`** — calling `block_on` inside an already-entered runtime context (which `#[tokio::test]` establishes) **panics**, so the test must mirror the production non-runtime-thread shape. `pin: daemon/tests/{github_executor,linear_executor}.rs (block_on-via-captured-handle + mandatory-timeout + classify_sync_failure disposition + FailedWithEvents atomic ActionFailed+event + plain-#[test]+built-Runtime-handle)`

## <a id="47"></a>47. The §7.2 worktree git-axis is a LIVE-READ cache, NOT event-sourced — refresh via a non-Gateway/non-event write-actor command (the DrainOnce/ReapLeases family) + a git-watcher interval task; persistence-core stays git-free; a rebuild RESETS the cache

**Date:** 2026-06-15.
**Source slice:** edges-026 (P5.2; `c195c7f` + `d800ef1` fmt follow-up; brief edges-026; security NOT required [read-only git2, no event, no secret]; reachability YES; the §7.2 live-read close). _(Edges-track lesson renumbered into the daemon sequence — was edges' §33 candidate.)_

The §7.2 worktree **git-axis** (`dirty_state`/`ahead_count`/`behind_count`/`last_commit_sha`/`git_checked_at` + a recomputed `status`) is a **LIVE-READ cache, NOT event-sourced** — there is **NO `WorktreeStatusRefreshed` event** (the git working-tree state is a live property of the disk, replaying it would be meaningless). Refresh it via a **NON-Gateway, NON-event write-actor command** (`RefreshWorktreeStatus`, the `DrainOnce`/`ReapLeases` family — a maintenance command on the single writer, NOT a Gateway mutation; single-writer forbidden #3 holds, no second writable connection) **triggered by a git-watcher interval task** (a ~30s `tokio::time::interval` loop in the `spawn_drainer`/`spawn_reaper` precedent, ARCHITECTURE.md:340) + **read-time `git_checked_at` staleness**. Because it is live-read, a **rebuild RESETS the cache** (the git-axis columns go to their reset state, repopulated on the next watcher tick) — and **rebuild-equivalence of the event-sourced columns still holds** (only the live-read axis resets; the event-folded axis from edges-022 rebuilds identically).

**Layer discipline:** persistence-core stays **git-free** — the git read (git2) + `derive_worktree_status` live in the **runtime layer**, and the persistence-core UPDATE takes **plain computed values** (the edges-022 LESSON §17 layer rule: the persistence/projection core must NOT import the `git/` edge). The general shape: a **derived-from-the-disk signal is a live-read recompute over the single writer, never an event fold** (the §41 staleness-poller precedent generalized to git working-tree state). `pin: daemon/tests/worktree_refresh.rs (refresh-via-write-actor-command-no-event + git-checked-at staleness + rebuild-resets-live-read-axis + event-axis rebuild-equivalence + layer-clean computed-values); /wired the git-watcher spawn`

## <a id="48"></a>48. A `proj_*` REGISTRY projection is a PROJECTION, not the DATA_MODEL §2.8 durable registry — fold the coarse event, key by the AVAILABLE identity (envelope vs payload, no sibling-read when self-contained), put every table in REBUILD_TABLES → rebuild-equivalent; defer the canonical-row/mutator model

**Date:** 2026-06-15.
**Source slice:** edges-028 / edges-030 (P5.1/P7.1; `8788210` project-registry + `25e0833` integration-connections; briefs edges-028/030; security SKIP [read-model folds]; CONTRACT-neutral; lead-ruled at the R1b `ProjectRescanned` freeze).

A `proj_*` **registry projection** is a **PROJECTION, not the DATA_MODEL §2.8 durable registry** (the canonical durable model — register-mutator + the richer §2.8 fields like `workspace_id`/`scopes_json`/`connected_at`/`expires_at` — is **DEFERRED**). Build it as a read-model fold of the **coarse event**: `ProjectRescanned` → `proj_project` (identity axis) + `proj_repository` (git-detection axis, 1:1 MVP); `IntegrationConnectionRegistered` → `proj_integration_connection`. **Lead-ruled at the R1b freeze: "the projector splits this ONE coarse event into rows"** (one fat event → multiple registry tables, NOT a separate register-mutator per table).

**Key by the AVAILABLE identity — and DON'T sibling-read when the payload is self-contained.** edges-028 keys by the **ENVELOPE `project_id`** (the project identity rides the envelope; the payload is self-contained → **NO LESSON §17 sibling-read** of `action_requests`, simpler than the edges-022 `proj_worktree` projector which needed the immutable repo_id sibling-read). edges-030 keys by the **PAYLOAD `connection_id`** (the mutator minted it on the payload, not the envelope). Every table goes in **`REBUILD_TABLES`** so the projection is **rebuild-equivalent** (LESSON §4/§17) — re-folding the event stream reconstructs identical rows; both carry `Decode`-degrade-and-skip on an unbindable payload (no-payload-echo) + a healthy-skip on missing identity. The deferred fuller model lands when a consumer needs the canonical rows. `pin: daemon/tests/{project_registry,integration_connections_proj}.rs (fold-coarse-event → multi-table + key-from-envelope vs key-from-payload + no-sibling-read-when-self-contained + REBUILD_TABLES rebuild-equivalence + Decode-degrade-skip)`

## <a id="49"></a>49. A credential-registration mutator is registration-ONLY — the secret NEVER flows through a risk-≥1 action (a risk-2 action executes off the §15-REDACTED durable row → a token in inputs is masked-by-execute-time, so the mutator STRUCTURALLY cannot carry the token); §15 #4 holds BY CONSTRUCTION; the token→keychain write is a SEPARATE deferred mechanism; `standing_grant_eligible=FALSE` (the §6.2 credential floor)

**Date:** 2026-06-15.
**Source slice:** edges-029 (P7.1 Wave-C; `355eddf`; CONTRACT 0.32→0.33 local; brief edges-029; **security-reviewer full invariant PASS**; 1 commit — the §15 #4 pin is STRUCTURAL/inseparable, the LESSON §39 carve-out).

A credential-registration mutator (`integration.connect`, risk-2, `ExecutorKind::Integration`) is **REGISTRATION-ONLY: the secret NEVER flows through the action.** The forcing argument is structural — a **risk-≥1 (approval-gated) action executes off the §15-REDACTED durable `action_requests` row** (LESSON §20, the §7.2 approve-path split: approve runs off the durable redacted row), and the LESSON §16 dual-gate **masks any secret in `inputs_json` BEFORE persist** → a token placed in inputs is **GONE by execute-time**. Therefore the connect mutator **STRUCTURALLY cannot carry the token** and store it: its inputs are `{provider, keychain_ref POINTER, account?}` with **NO token**, and **§15 #4 (secrets only in the keychain, events carry keychain_ref pointers only) holds BY CONSTRUCTION** — the event has no token slot to leak through. The **token→keychain WRITE is a SEPARATE, DEFERRED non-Gateway mechanism** (the `keyring` crate + a non-Gateway secret-store path + the live macOS-keychain write — a HITL/live-integration follow-on, the live-agents/PTY non-deterministic-surface class).

Two pins: **(a) defense-in-depth** — a **secret-shaped `keychain_ref` is REJECTED** by REUSING the canonical §15 detector (`PrefixRedactor`, LESSON §13) **READ-ONLY** (a known secret prefix / high-entropy run → reject; **no parallel or weaker detector** — the same mechanism that gates persist gates the pointer-shape). **(b)** `standing_grant_eligible = **FALSE**` — the **§6.2 credential floor**: a credential/authorization-establishing action is **never bulk-grantable** (a standing grant must not silently establish new authority), the `git.discard_hunk` precedent on the irreversibility/blast-radius eligibility axis (LESSON §32). INV-SEC-1 no-bypass holds — the executor holds only `Box<dyn IdGen>` (no WriteHandle/SQL), emits via `emitted_events`, reachable ONLY via the catalog-gated pipeline (risk-2 → approval, NOT on the risk-0 auto-execute allowlist); `side_effect_applied=false`. `pin: daemon/tests/integration_connect.rs (registration-only no-token + keychain_ref-pointer-only + secret-shaped-ref-reject-via-PrefixRedactor + standing_grant_eligible=false + no-bypass-only-IdGen); security-reviewer (§15 #4 + INV-SEC-1 no-bypass)`

## <a id="50"></a>50. A per-migration "applies" test asserts its FLOOR (`user_version >= N`) + table existence, NOT the exact-latest version (else every new migration breaks the PRIOR migration's test); keep ONE exact-latest runtime pin as the double-bump guard; use a RUNTIME read, not `assert!(CONST >= N)` (clippy `assertions_on_constants`)

**Date:** 2026-06-15.
**Source slice:** edges-030 (P7.1 Wave-C; `25e0833`, the integration-connections projector + MIGRATION_11; brief edges-030; CONTRACT-neutral; the migration-floor-test convention surfaced at the MIGRATION_10→11 double-bump).

A per-migration **"applies" test** (e.g. `test_migration_11_applies`) asserts its **FLOOR** — `user_version >= N` **+ the new table exists** — **NOT the exact-latest `user_version == N`.** Exact-latest is a maintenance trap: it couples the test to *being the newest migration*, so the **NEXT** migration (which bumps `SUPPORTED_USER_VERSION` N→N+1) silently **breaks the PRIOR migration's `== N` test** — every schema addition would have to also edit every earlier per-migration test. The floor (`>= N`) survives subsequent bumps (the table that migration created still exists; the version is at least N). Keep **exactly ONE exact-latest runtime pin** (`tests/gateway_plan.rs` reads the latest `SUPPORTED_USER_VERSION`) as the **double-bump guard** — that single canonical assertion catches an accidental skip/double-increment, and it's the *only* test that needs editing when the latest version changes.

**Mechanical pin:** assert against a **RUNTIME read** (`store.user_version() >= N`), NOT `assert!(SUPPORTED_USER_VERSION >= N)` — the latter compares two **constants**, which trips clippy's **`assertions_on_constants`** lint (a const-vs-const assertion is compile-time-decidable → clippy flags it as always-true/always-false). Reading the version off the opened `store` makes it a genuine runtime check the lint accepts. `pin: daemon/tests/gateway_plan.rs (the single exact-latest SUPPORTED_USER_VERSION pin) + the per-migration floor tests (store.user_version() >= N + table-exists, runtime read)`

## <a id="51"></a>51. A projection's live-delta SOURCE (`deltas_for_append`) and its PROJECTOR's folded-event set are TWO LISTS THAT MUST AGREE — a row-mutating event with no delta nudge is a silent stale-UI bug; key BOTH on the same token-form (the `::EVENT_TYPE` consts where they exist) so a wire-value rename can't desync them, and pin the SET agreement with a guard test (the §50 keep-two-lists-honest family, generalized to delta-source↔projector)

**Date:** 2026-06-15.
**Source slice:** D3 / P4.5 (the Session live-delta nudge; `daemon/src/runtime/writer.rs` + `daemon/tests/runtime.rs`; brief 068; CONTRACT-neutral; surfaced because `deltas_for_append` nudged only on `SessionStarted` while the `SessionProjector` had grown two more `proj_session`-folding arms — `SessionFailed` [4.2] + `SessionRecovered` [D2/4.4] — so a live subscriber went un-nudged after a death/recovery and the §11.4 cards went stale).

The live-subscribe mechanism (LESSON §9/§12) has **two cooperating lists** for each projection: the **projector** (`Projector::apply`) decides which event types MUTATE the read model, and the **delta source** (`deltas_for_append`, called in the write-actor's `Command::Append` arm, publish-after-commit) decides which event types publish a **row-less `Upsert` nudge** so a subscriber re-reads via `get_projection`. **These two sets must be identical** — an event the projector folds but the delta-source omits mutates the row with **no nudge** → the UI silently shows stale state until an unrelated event or a reconnect happens to refresh it (a quiet correctness bug, not a crash). The `proj_session` set is exactly three: `SessionStarted` / `SessionFailed` / `SessionRecovered`.

**Two drift modes, two guards:** **(a) SET drift** — a future `proj_session`-folding event (e.g. a clean-terminal `SessionKilled`/`SessionCompleted`) added to the projector but not the delta-source → caught by a **set-agreement guard test** that iterates the known folded set and asserts each publishes a Session `Upsert` delta, plus a binding comment ("extend BOTH lists together") on the delta-source. **(b) WIRE-VALUE drift** — an event type's wire string is renamed via its `EVENT_TYPE` const; the projector (keyed on `SessionFailed::EVENT_TYPE`) follows automatically but a delta-source keyed on the raw `"SessionFailed"` **literal** silently does not. Close (b) by keying the delta-source on the **same `::EVENT_TYPE` consts the projector uses** — for each event, the SAME token-form appears in BOTH lists (where no const exists, e.g. `SessionStarted`, both the projector and the source use the literal, which is still consistent). The guard test (a) then keys on the same consts, so it stays meaningful under a rename.

**Generalizes:** the same delta-source↔projector agreement applies to every projection with a subscribe-delta (D4 carries it to `proj_project_activity`/`proj_pull_request`/`proj_audit_trail`/`proj_usage_ledger`). The nudge stays **row-less by design** — the subscriber re-reads the full row; row enrichment of the delta is an explicitly-deferred future improvement (the lag-resync policy), so do NOT assert `row: None` (that would pin a deliberately-mutable aspect). `pin: daemon/tests/runtime.rs (test_proj_session_folded_events_each_publish_a_session_delta — the set-agreement guard; the two positive nudge tests + the no-session_id no-op)`

**Blanket vs. selective (D4a refinement):** the guard shape depends on the projector's fold breadth. A **SELECTIVE** projector (folds a specific event set — `proj_session` on 3 events, `proj_usage_ledger` on `{TelemetrySampled}` only) gets a **set-agreement guard WITH a NEGATIVE arm** — assert each folded event nudges AND a non-folded event does NOT (pinning the arm stays selective, not accidentally blanket). A **BLANKET** projector (folds EVERY event — `proj_audit_trail` has no event-type filter, so every append mutates an audit row) gets an **unconditional nudge** (push the delta for every event) and needs **no per-event set guard** (there is no set to drift from); pin it instead with a test that a GENERIC/unknown event still nudges (proving the arm is truly unconditional). A single append therefore yields a `Vec<ProjectionDelta>` (e.g. `TelemetrySampled` → {UsageLedger, AuditTrail}; `SessionFailed` → {Session, AuditTrail}); **tests must drain-and-find** (assert the expected delta is AMONG the published set, never rely on recv-once / push order), since adding a blanket projection makes every prior single-delta event multi-delta. A cross-path projection (`proj_audit_trail` folds events from BOTH `Command::Append` observations AND Gateway `emitted_events`) is nudged on BOTH paths. **D4b realization — ONE shared mapping, not two arms:** the per-projection event→delta mapping is extracted into a single `projections::deltas_for_event(event_type, &EventDeltaIds)` called by BOTH `deltas_for_append` (the `Command::Append` wrapper, envelope ids) AND the gateway emitted-events loop (envelope + payload ids); two PATH-SPECIFIC mappings would be two lists that drift (LESSON §52). It lives in `projections/` (below both runtime + gateway — `writer.rs` would force a gateway→runtime layer inversion; it's also the principled home, mirroring the projectors). The AuditTrail blanket stays PATH-specific (NOT in `deltas_for_event`): per-`Command::Append`-event in `deltas_for_append` (D4a) + ONE per committed gateway COMMAND in `publish_after_commit` (D4b) — Action* events append outside the emitted-events loop, so per-command is the single gateway chokepoint. A nudge can only target a projection with a `ProjectionName` variant (the closed §6.1 subscribe set) — a folded projection lacking one (proj_project/proj_repository/proj_integration_connection) is audit-blanket-only until a variant is added (a CONTRACT change). `pin: daemon/tests/runtime.rs (the UsageLedger §51 guard's negative arm + the AuditTrail generic-event blanket test + drain-and-find on the migrated D3 tests) + daemon/src/projections/mod.rs (the 6 deltas_for_event mapping unit tests) + daemon/tests/session_executor.rs (the gateway production-path tests)`

## <a id="52"></a>52. When an event's append PATH migrates, any path-SPECIFIC side-channel is silently orphaned — route the side-channel through a PATH-AGNOSTIC shared function, and pin behavior with a test that drives the REAL production entry, never a convenient proxy (a test on the non-production path MASKS the orphan)

**Date:** 2026-06-15.
**Source slice:** the D3 SessionStarted production-gap FINDING (surfaced + verified during D4b scoping; fix lands in D4b / P4.5). Lead-requested lesson.

A side-channel keyed to ONE append path silently breaks when the event's production path migrates to ANOTHER. Concretely: `deltas_for_append` (the live-subscribe nudge source) runs ONLY in the write-actor's `Command::Append` arm. `SessionStarted` originally appended there (1.6b), so its nudge arm fired. At 4.0b-1 the `SessionExecutor` took over and began emitting `SessionStarted` via the Gateway `emitted_events` path (`ExecutionOutcome::Succeeded { emitted_events }` → `gtx.append` in the pipeline's terminal txn-B) — which BYPASSES `Command::Append` entirely. The `deltas_for_append` arm was thereby **orphaned in production**: a created session never nudged a live subscriber (the L2 session card stale on creation), while session DEATH/RECOVERY (still `Command::Append` observations) DID nudge — a tell-tale asymmetry.

**Two disciplines:**
**(a) Path-agnostic side-channels.** A cross-cutting side effect (a delta nudge, an outbox row, an audit hook) must NOT be wired to a single append path. Route it through ONE shared, path-agnostic function (`deltas_for_event`) that EVERY append path calls — so a path migration cannot orphan it. (This is also the LESSON §51 "one mapping, both paths" point, seen from the failure side: TWO path-specific side-channels are TWO lists that drift; one shared function is the fix.)
**(b) A test must drive the REAL production entry.** The gap survived D3's green suite because D3's tests append `SessionStarted` via a DIRECT `store.append` (the `Command::Append` path) — exercising the orphaned arm, not the production gateway path. A test that drives a convenient proxy instead of the real production entry point gives FALSE confidence: it pins the path that isn't used in prod. **Pin behavior by driving the real production caller** (here: submit `session.create` through the gateway execute + auto-execute, assert the delta) — the same `/wired` "reachable from a production entry, not just from tests" discipline, applied to side-effects. When a bug is found this way, the regression test MUST drive the production path, or the next refactor re-orphans it. **A revert-check seals it:** neuter the fix (here: the gateway delta build) and confirm the production-path test FAILS — proving the test exercises the real path, not a proxy (D4b: neutering the build failed all 3 integration tests). `pin: daemon/tests/session_executor.rs (D4b — the EmitExecutor + 3 production-path integration tests: session.create / git.create_worktree / github.create_pr through the REAL gateway execute publish the deltas, NOT a direct store.append; revert-check confirmed)`

## <a id="53"></a>53. ENRICHING an already-frozen fail-closed projection-row is a LOCKSTEP slice — the column + the projector fold + the typed-row field + the typed-serve SELECT + the CONTRACT bump land ATOMICALLY (a column without its row field fails the fail-closed serve closed); the FIRST bool projection column needs an INTEGER(0/1)→bool coercion in the READ layer (keep the contract a pure `Option<bool>`)

**Date:** 2026-06-16.
**Source slice:** D5a / P4.6 (the `PullRequestRow` `mergeable`/`checks_summary` enrichment; `shared/src/projections.rs` + `daemon/src/{eventstore,projections,ipc}`; brief 071; CONTRACT 0.35.0→0.36.0; the P7.2 "basic-now + SPREAD" SPREAD consumed).

A typed projection-row frozen + served fail-closed (LESSON §37 — `read_*_typed` deserializes each DB row → the `deny_unknown_fields` struct, failing CLOSED on a mismatch) has a **LOCKSTEP enrichment constraint:** adding a field is NOT a "column now, row later" spread — the column + the projector fold + the typed-row field + the typed-serve SELECT + the CONTRACT bump must land in **ONE slice**. A new `proj_*` column WITHOUT the matching row field → the fail-closed serve rejects the now-extra column (drift fails closed); a row field WITHOUT the column → the deserialize sees a missing field. So a "basic-now + SPREAD" deferral (P7.2 froze `PullRequestRow` minus `mergeable`/`checks_summary`) is consumed as ONE atomic slice: MIGRATION_N (ALTER-only, the historical CREATE untouched — LESSON §50) + the fold (INSERT + DO UPDATE, None→NULL, rebuild-safe LESSON §17) + the row fields + the SELECT + the `CONTRACT_VERSION` bump + the §2.5-seam schema-snapshot + the 3-way verify.

**The first bool projection column gotcha:** SQLite has no native boolean — a `bool` column stores as **INTEGER 0/1**. A row-to-JSON read (`read_table_as_json`/`sqlite_to_json`) yields a JSON **number** (0/1), and serde deserializing a JSON number into a frozen `Option<bool>` **FAILS** (bool expects true/false). Fix = **coerce number→bool in the READ layer** (`read_*_typed` in `daemon/ipc`), so the `shared/` contract stays a pure `Option<bool>` (the impedance is a daemon-internal storage detail, not a contract concession). Pin all three: `Some(true)` [INTEGER 1], **`Some(false)` [INTEGER 0 — the falsy edge a `n==1`/truthiness coercion bug hides]**, and `None` [NULL]. A future bool projection column wants a shared coercion helper (deferred — not reachable on today's single-bool-column DDL). `pin: daemon/tests/projections.rs (the fold + the typed-serve true/false/None) + shared/tests/contract.rs (PullRequestRow schema-snapshot @0.36.0 + 3-way verify) + daemon/tests/gateway_plan.rs (MIGRATION_13 exact-latest pin)`

## <a id="54"></a>54. A NEW subscribe-able projection VERTICAL composes §37 (typed fail-closed row) + §48 (the `proj_*` fold) + §51 (the delta nudge) + a NEW `ProjectionName` closed-set variant — that closed-set add is the CONTRACT cost of subscribe-ability (a projection with no `ProjectionName` is audit-blanket-only); a frozen VALUE enum (no lifecycle) uses the plain-value-enum idiom, NOT `status_machine!` (which adds a phantom `is_terminal`)

**Date:** 2026-06-16.
**Source slice:** D5b-1 / P4.6 (the structured-review vertical — `ReviewSynced` event + `ReviewState` enum + `ReviewRow` + `ProjectionName::Review` + `proj_review`; `90cadd2`; brief 072; CONTRACT 0.36.0→0.37.0).

Standing up a WHOLE new subscribe-able projection (vs enriching an existing one, LESSON §53) is the established patterns COMPOSED, plus one new contract cost:
- **§37** — the typed `Row` (`deny_unknown_fields`) served fail-closed via `read_*_typed`.
- **§48** — the `proj_*` projection: fold the coarse event, key by the available identity (here `review_id` — GitHub review ids are GLOBALLY unique, so a single-column PK, NOT a `{repo}#{pr}#{review}` composite; the composite is only needed when the natural id isn't globally unique, like `pr_number`), every table in `REBUILD_TABLES`.
- **§51** — the `deltas_for_event` arm (the new event → its projection) so a live subscriber is nudged; for a gateway-emitted event, pin it with the §52 production-path test.
- **NEW — the `ProjectionName` closed-set CONTRACT cost.** `ProjectionDelta`/`get_projection` name the projection via the **closed `ProjectionName` enum** (§6.1). A projection with **no `ProjectionName` variant cannot be subscribed/served** — it's audit-blanket-only (the D4b future-CONTRACT flag for proj_project/proj_repository/proj_integration_connection). So making a NEW projection subscribe-able REQUIRES adding a `ProjectionName` variant — a **closed-set CONTRACT change** (bump + the §2.5-seam snapshot + the 3-way verify + the `ALL` array in declaration order). Subscribe-ability is not free; it costs a contract variant.

**The value-enum-vs-status-machine distinction:** a frozen enum that is a **VALUE** (a fixed classification — `ReviewState` = approved/changes_requested/…; a review IS a state, it doesn't TRANSITION) uses the **plain-value-enum idiom** (`#[derive(Serialize, Deserialize, JsonSchema)]` + `#[serde(rename_all="snake_case")]` + a manual `ALL` + serde's default reject-unknown — the `RequesterType`/`EvidenceType` precedent), NOT `status_machine!`. `status_machine!` is for LIFECYCLE machines (§5.1 — Session/PullRequest, which transition + have terminal states); using it for a value enum adds a phantom all-false `is_terminal()` that misrepresents the model (a reader infers a lifecycle that doesn't exist). Both give snake_case wire + reject-unknown + `ALL` + `JsonSchema`; pick by whether the thing has a LIFECYCLE. `pin: shared/tests/contract.rs (ReviewSynced/ReviewState/ReviewRow frozen-shape + ProjectionName::ALL += Review + 3-way verify @0.37.0) + daemon/tests/projections.rs (the ReviewProjector fold) + daemon/tests/session_executor.rs (the §52 gateway production-path Review delta)`

## <a id="55"></a>55. An external-network READ action is risk-1, NOT risk-0 — a LOCAL read auto-executes (risk-0) but a NETWORK read by an untrusted proposer is a rate-limit/exposure vector, so gate it (risk-1, the lowest gated tier) + `standing_grant_eligible=true` so the user can opt into auto-sync; below the risk-2 external WRITES (no mutation/credential)

**Date:** 2026-06-16.
**Source slice:** D5b-2 / P4.6 (`github.sync_reviews`; `76b7f4a`; brief 073; CONTRACT 0.37.0→0.38.0; the FIRST github-READ classification).

A catalog action that READS an EXTERNAL network resource (and emits observation events) is **risk-1**, not risk-0. The distinction is **local vs network**: `project.rescan` is a LOCAL filesystem read → risk-0 (auto-execute — no external surface). `github.sync_reviews` is a NETWORK read → an untrusted proposer (an agent) auto-triggering unbounded GitHub-API reads is a **rate-limit-exhaustion / data-exposure / telemetry vector** — so it must NOT be on the risk-0 auto-execute allowlist. It's risk-**1** (the LOWEST gated tier — a single human approval) because it has **no mutation and no credential** (below the risk-2 external WRITES like `github.create_pr`). Pair it with **`standing_grant_eligible=true`** so the user can standing-grant it (opt into auto-sync) without a per-action approval — gated-by-default, grantable-by-choice. Shape: `idempotency=None` (a re-sync RE-RUNS — re-fetching current state IS the point, NOT a dedup); `side_effect_applied=false` (a read has no external side effect → a lost terminal write is a CLEAN rollback, unlike a write's `true`); INV-SEC-1 no-bypass (emit ONLY via `Succeeded{emitted_events}` → the gateway txn-B append); the §46 SYNC-external-network pattern (captured `Handle` + `block_on` + a mandatory timeout; failure → `*SyncFailed`). The octocrab→shared enum mapping is a PURE unit-tested fn (unknown → the conservative floor), NOT buried in the not-unit-tested live-network edge. `pin: daemon/tests/github_executor.rs (the github.sync_reviews emit/empty/validate/failure + map_review_state) + shared/tests/contract.rs (the github.sync_reviews catalog entry @0.38.0); security-reviewer on the first external-READ classification`

## <a id="56"></a>56. A 2nd harness's INV-SEC-1 interception REUSES the 1st's Gateway adjudication loop by swapping ONLY the I/O envelope + a daemon-classifier harness-discriminator; for a vendor whose hook is "a guardrail not a boundary", `--sandbox` OS-containment is a MANDATORY defense-in-depth 2nd layer (hook=adjudicate+audit, sandbox=contain); the discriminator is trusted-binary-stamped, never agent-settable, so a spoof only ever HARDENS (PIN-1)

**Date:** 2026-06-17.
**Source slice:** 3.3c / P3.3c (the Codex INV-SEC-1 interception; `eb358e1`; brief 066; B2 ruled away-log Decision 18 + `docs/planning/3.3c-harness-discriminator-fork.md`; security-reviewer CLEAR 0/0/0/0 every layer; NO CONTRACT bump).

Generalizing LESSON §26 (the Claude interception) to a SECOND harness: the daemon's `intercept`→Gateway adjudication loop (submit_action / decision_sink / the intercept wait-class / verdict emit / audit-before-verdict / fail-closed) is **harness-agnostic** — the 2nd harness reuses it UNCHANGED by **swapping ONLY the I/O envelope** (Codex `PreToolUse` stdin/stdout ↔ the daemon `intercept` RPC) and **branching ONLY the parser/classifier** on a harness discriminator. Codex diverges from Claude on the classifier (its tools are `exec_command`/`apply_patch`/`mcp__*`; Codex MCP is Guaranteed-interceptable→Adjudicate where Claude's is BestEffort→Deny), so "handler untouched" (B1) can't satisfy the spec — it would force the hook to fake `apply_patch`→"Edit" and mis-deny Codex MCP. **B2** (an OPTIONAL daemon-internal `harness` tag on `HookPayload` + a 2-arm `map_to_action_type`) keeps the loop literally unchanged and the 1st harness's path byte-identical (the default/absent tag IS the strictest reading — a Codex tool through the Claude classifier → UnmappedTool → DENY).

The **genuinely-new** safety layer vs the 1st harness: a vendor whose own docs call its hook *"a guardrail, not a complete enforcement boundary"* needs **OS-level containment as a MANDATORY 2nd layer** — `--sandbox workspace-write` (writes scoped to `cwd + writable_roots = {worktree + per-profile approved}`, `network_access=false`, never a bypass flag). The hook is adjudication+audit; the sandbox is containment; **both together = INV-SEC-1**. (Grammar verified live at authoring: `workspace-write` reads ALL files — `writable_roots` is a WRITE boundary only; INV-SEC-1 governs mutation, so assert the WRITE boundary, never read-confinement.)

**PIN-1 (the by-construction safety crux):** the discriminator is **stamped by the trusted hook binary** (from its `--harness` flag), NOT parsed from agent stdin/payload — never agent-settable. A spoofed tag can ONLY select a more-conservative classifier (both arms deny-unknown; risk is catalog-authoritative + daemon-side; the only auto-allow anywhere is the 1st harness's benign plan-no-op) → **no tag value converts a real mutation into a silent allow.** Enforce PIN-1 mechanically (a test that a spoofed `harness` field in stdin is IGNORED), not just by assertion — the whole by-construction argument rests on it. `pin: daemon/tests/codex_intercept.rs (PIN-1 trusted-binary-tag-not-agent-settable + deny-unknown both arms + zero-auto-allow + sandbox-is-inv-sec-layer + no-live-spawn) + daemon/tests/codex_launch.rs (no-bypass-flag literal); security-reviewer EVERY layer (cat-1)`

---

## <a id="57"></a>57. A headless VT model wraps a purpose-built emulator crate (`vt100`) and is DISPLAY-only (#9 grep-pinned, never a status source); derive the FILLED scrollback count via a `set_scrollback(MAX)` probe cached behind `&self` (vt100's `scrollback()` is the scroll POSITION, not the count) AND guard that probe on `alternate_screen()` — the alt grid (capacity 0) otherwise clobbers the normal-screen count to 0

**Date:** 2026-06-17.
**Source slice:** 3.4-VT / 075a (the headless VT screen + scrollback model; brief 075a `@3aa0d17d`; NON-cat-1, daemon-internal, NO CONTRACT bump; security-reviewer CLEAN #9 PASS; the alt-grid-clobber MED caught by code-quality + TDD-fixed in-slice; the first sub-slice of the §8/§17 `Replayed`-survival-rung arc).

The survival `Replayed` rung (daemon-restart → "scrollback replay + relaunch"; O-2 *"accurate alt-screen VT re-render"*; the `decide_resume` classifier, LESSON §36) needs a headless terminal screen + scrollback maintained from the PTY byte stream. Realize it by **wrapping a purpose-built pure-Rust emulator (`vt100`) behind a daemon-internal `HeadlessVt` (`terminal/vt.rs`)** — `Parser::new(rows,cols,scrollback_capacity)` + `process(&[u8])` + `screen()` — NOT a bespoke grid (Approach A over B; the dep verified at authoring). It is **DISPLAY-only**: status is NEVER derived from the VT screen (#9 — the `terminal/mod.rs` PTY precedent extended to the screen model; a scoped grep-pin forbids `derive_status`/`NormalizedStatus`/`status::Session`/`harness::` in `vt.rs`, belt-and-suspenders alongside the existing whole-`terminal/`-dir scan).

**Two non-obvious vt100 traps (bank both):** (a) `Screen::scrollback()` returns the current scroll **POSITION**, not the filled scrollback length — derive the filled count by probing `set_scrollback(usize::MAX)` (vt100 clamps to the filled rows), reading `scrollback()`, then restoring to `0`; this needs `&mut`, so do it during `process()`/`resize()` and **cache** the result in a field that the `&self` accessors (`has_scrollback()`/`scrollback_len()`) read (observationally pure — the daemon always views the live tail at offset 0). (b) **The probe MUST guard on `alternate_screen()`** — while alt-screen is active, `screen_mut()`/`set_scrollback` resolve to the ALTERNATE grid (capacity 0), so an unguarded probe reads 0 and **clobbers the cached normal-screen count to 0** mid-session (a vim/htop run would zero the scrollback count); skip the probe while `alternate_screen()` (the normal grid's scrollback is frozen then, restored on `?1049l`). Parse untrusted PTY bytes tolerantly (no panic on garbage/partial-escape, the LESSON §42 family). Daemon-internal; the §6.4 Terminal-Channel wire stays frozen (0.21.0) → NO contract; the new `vt100` dep → `cargo audit` at /phase-exit + a version-pin TODO. `pin: daemon/tests/vt.rs (bytes→screen + scrollback-filled-not-capacity + alt-grid-clobber Test 8 + alt-screen toggle + #9 display-only grep-pin + garbage-no-panic)`

---

## <a id="58"></a>58. A daemon survival artifact persisted to disk (the VT scrollback snapshot) is a §15-redacted SUBSTRATE write (NOT a Gateway Action — LESSON §10) whose redaction-before-persist binds it like the event store (#3); guarantee it STRUCTURALLY by making the ONLY serializable on-disk type hold POST-redaction text; the `Replayed`-rung signal keys on has-restorable-CONTENT (not raw scrollback length) so `decide_resume` stays byte-unchanged (the caller populates the inputs)

**Date:** 2026-06-17.
**Source slice:** 3.4-VT / 075b–d (the serialize/replay + producer→seam→recovery wiring + the durable §15-redacted store; `2232463` serialize · `ca227be` producer-tap+`ScrollbackStore`-seam+recovery-wiring · `61527fa` §15 safety core + `9dabc1a` lifecycle; security-reviewer CLEAN ×7 layers at 075d; `Replayed`-after-daemon-restart now LIVE; the §8/§17 survival ladder; extends LESSON §57).

The §8/§17 `Replayed` rung (daemon-restart → re-render the prior screen + relaunch) needs the headless VT (§57) **serialized → persisted → reloaded** across a restart. Four bankable shapes:

**(1) Serialize via the emulator's own round-trip + prove it idempotently.** `snapshot()` = `vt100 state_formatted()` for the visible screen (round-trips content+cursor+attrs+modes — it prefixes `ClearAttrs`+`ClearScreen` so it overwrites the visible screen WITHOUT touching scrollback) + the scrollback as plain rows; `from_snapshot()` replays the scrollback then processes `state_formatted`. Prove no-loss with an **idempotent `snapshot(from_snapshot(s)) == s`** (a fixed-point — the strongest fidelity pin with no private-cell API). An **alt-screen-ACTIVE** snapshot captures the alt screen but NOT the hidden normal buffer → the restored model honestly reports `scrollback_len()==0` (faking N would claim unreproducible scrollback); pin the limitation, flag the two-buffer edge as a future-TODO (a full fix = a raw-byte-retain enhancement).

**(2) Wire the producer→consumer over a cat-1-NEUTRAL seam.** A `ScrollbackStore` trait in `terminal/` (NOT `runtime/`) + the durable impl INJECTED as `Arc<dyn ScrollbackStore>` (the §35 opaque-sink pattern) so the `session/` cat-1 import-grep holds (no `WriteHandle`); the `SessionActor` read-pump feeds a per-session `HeadlessVt` (`Arc<Mutex>`) and saves on the status tick + a final save AFTER `pump.await` (the deterministic fully-drained spot); recovery reads the store. The `Replayed` signal is fed from **`has_restorable_content()`** (scrollback rows OR a non-blank screen — needs a daemon-internal `screen_nonblank` bit, since a blank screen's `state_formatted` is non-empty) so a session snapshotted mid-`vim` still re-renders; crucially this keeps `decide_resume` **byte-unchanged** — the CALLER computes the input value (LESSON §36's "pure/total classifier; the caller populates inputs"), so broadening the meaning never touches the classifier (a field rename is a separate refactor).

**(3) The durable store is a §15-redacted SUBSTRATE write, fail-closed, NOT a Gateway Action.** Persisting the daemon's OWN observed terminal output for its OWN survival is the event-store/transcripts/quarantine family (LESSON §10), §15-redacted but not a proposer-gated mutation (INV-SEC-1 governs untrusted-proposer state changes). The **redaction-before-persist invariant binds the sidecar exactly as it binds the event store (#3)** — and the structural guarantee is STRONGER: make the **ONLY `Serialize` on-disk type hold ONLY post-redaction output** (`PersistedScrollback`; `VtSnapshot`/`HeadlessVt` get NO `Serialize`, grep-confirmed) so a raw secret CANNOT reach the serialized bytes (the §16 co-located-gate). **Fail-closed:** any non-clean redaction (status≠Redacted or a quarantine-divert) on the screen OR any row aborts the whole write — no partial/unredacted sidecar (the scrollback is lost → `Relaunched`, safe; the §15 invariant holds while only AVAILABILITY degrades). Reuse the canonical Redactor (LESSON §13/§49); 0700-dir/0600-file (+ the `.tmp`) set-absolute+re-read-verify+fail-closed (§15 #11, the `harden_codex_dirs` pattern); atomic temp-then-rename in the SAME dir, per-`session_id` keying (the single-writer-#3 rule is `nexusops.db`-specific — per-session sidecars are independent). USER ruling ①=A: the persisted form is redacted PLAIN-TEXT (formatting dropped); the live/in-memory re-render stays formatted (B/cell-level-formatted-redaction = a deferred fidelity upgrade).

**(4) The recall envelope is the accepted residual.** Unlike events (keychain-refs-only primary control, rule #5), terminal scrollback has NO keychain-refs backstop — an agent can echo a literal no-shape secret to the screen, so the §13 Redactor's inherent recall envelope is the SOLE control. This is the SAME posture as harness transcripts (best-effort-redacted + 0600 + the local-machine trust boundary) → precedent-consistent, an honest accepted limitation (doc'd in `scrollback/mod.rs`); test the recall on a REALISTIC terminal-shaped sample (multi-line command output with `sk-`/`AKIA`/`KEY=value`), since terminal text is a new input domain for the redactor. `pin: daemon/tests/{vt.rs (snapshot idempotent + alt-active-empty-scrollback), scrollback_recovery.rs (has_restorable_content→Replayed + cat-1), durable_scrollback.rs (redaction-before-persist + fail-closed + 0700/0600 + load-fail-safe + evict/sweep/backstop)}; security-reviewer EVERY layer (cat-1/§15)`

## <a id="59"></a>59. The first network read in the IPC read layer — a UI-initiated network read is a READ RPC (transient on-demand, LESSON §33) with a captured-Handle+block_on+MANDATORY-timeout (LESSON §46) + a fail-closed seamed client; an AGENT-initiated network read stays a risk-1 Gateway action (LESSON §55) — the discriminator is the INITIATOR (trusted UDS peer vs untrusted proposer)

**Date:** 2026-06-19.
**Source slice:** D7 / P4.7 (`get_pr_diff`; `924ebcc`; CONTRACT 0.40.0; security-reviewer CLEAR every dimension; the Review-tab remote-PR code-diff).

`get_pr_diff(repo_id, pr_number, file?)→DiffResult` is the FIRST network fetch in the IPC READ layer (every prior read RPC was local — git2 / read-only-WAL). The bankable rule reconciles three prior lessons by the REQUEST INITIATOR:

**A UI-initiated network read is a READ RPC, not a Gateway action.** A diff is a transient, on-demand, returns-data read (LESSON §33 — never a projection); the UI calls it over the getpeereid-gated UDS (a TRUSTED local peer), so the LESSON §55 untrusted-proposer rate-limit/exposure concern does NOT apply (that gates AGENT/Brain-initiated network reads → a risk-1 Gateway action that EMITS, e.g. `github.sync_reviews`). A read-that-RETURNS-data also doesn't fit the Gateway's `ActionResult` (success/fail, not a payload). So: read RPC. The INITIATOR is the discriminator (trusted UDS peer → read RPC; untrusted proposer → Gateway action), NOT the network-vs-local axis alone.

**The network read needs the §46 discipline even off the write-actor.** A captured `tokio::runtime::Handle` + `block_on` (the IPC handler runs in `spawn_blocking`, no entered runtime → `Handle::current()` would panic) + a MANDATORY `tokio::time::timeout` (DoS-bounded — a hung GitHub call must not wedge the handler). Thread the client as `Arc<dyn GithubReadClient>` through `listener → serve_connection → dispatch` (test-seamed via a `FakeGithubReadClient`); the production client may be wired UNAUTHENTICATED (mechanism-first, LESSON §28/§43 — the live private fetch is auth-gated, returns a typed error until the keychain auth lands).

**§15 no-leak is STRUCTURAL + a deferred AUTHZ gate.** The network error type (`GithubReadError`) must NOT reach the fieldless `IpcErrorCode` (pin a secret-in-message test); strip credential-bearing identifiers at the source (remote_url userinfo, LESSON §44). When the read RESOLVES an identity cross-cuttingly (here repo_id→owner/repo off ANY projected PR), a CONFUSED-DEPUTY axis opens once auth lands → bind a MANDATORY security-re-review gate onto the auth-bootstrap task (read-only PUBLIC at MVP is safe; the authenticated PRIVATE cross-account fetch is what needs the re-review). `pin: daemon/tests/get_pr_diff.rs (parser + NotFound + network-failure-typed + no-secret-in-message); security-reviewer (the §15 no-leak + the deferred AUTHZ gate)`
