//! `workflow/` — workflow/scaffolding signal detection (P5.1) and, in later phases, the multi-step
//! plays composed of Gateway actions. This slice (edges-001) lands the file-signal detector only.

pub mod detect;
