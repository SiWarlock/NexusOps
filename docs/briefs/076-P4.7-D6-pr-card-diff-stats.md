# /tdd brief — pr_card_diff_stats

## Feature
D6 (ui-unblock work order): surface PR **diff-stats** — `additions` / `deletions` / `changed_files` / `commits` — on the PR card. Capture them from the octocrab PR (already GET-fetched) → add to the `PullRequestSynced` payload → fold onto `proj_pull_request` → the typed `PullRequestRow` → the fail-closed typed serve. The D5a / LESSON 53 **LOCKSTEP** enrichment of an already-frozen fail-closed projection-row. **Additive CONTRACT bump 0.38.0 → 0.39.0.**

> **Widens phase scope because** D6 is the UI-unblock wave-2 (task 4.7), homed under Phase 4 like 4.4/4.5/4.6 — it touches §7.1 (the `PullRequestSynced` event) / §7.2 (`proj_pull_request` GitHub-authoritative cache) / §11.2 (the PR Review Workspace card) / §5.0 (the contract bump), cross-cutting beyond Phase 4's §8/§17 anchors (the 4.6 precedent).

## Use case + traceability
- **Task ID:** P4.7 (D6)
- **Architecture sections it implements:** `ARCHITECTURE.md §7.1` (the `PullRequestSynced` event payload), `§7.2` (`proj_pull_request` — the GitHub-authoritative cache), `§11.2` (the PR Review Workspace card), `§5.0` (the SoT contract bump + 3-way verify).
- **Related context:** the work order `docs/planning/daemon-unblock-work-order.md` (D6). The D5a precedent (brief 071, `fa67c11`) added `mergeable`/`checks_summary` the same LOCKSTEP way — **mirror it exactly**. LESSON **53** (the lockstep-enrichment recipe: column + fold + row-field + typed-serve + CONTRACT-bump + schema-snapshot land atomic; a column without its row field fails the typed serve closed). The LESSON-46 octocrab read pattern.

## Acceptance criteria (what "done" means)
- [ ] `PullRequestSynced` (`shared/src/events.rs:473`) gains `additions`/`deletions`/`changed_files`/`commits` (`Option<u64>` — see Q1). `deny_unknown_fields` preserved; optionals serialize as explicit `null`.
- [ ] `PullRequestRow` (`shared/src/projections.rs:58`) gains the same 4 `Option<u64>` fields (the display read model tolerates NULL — `pr_id`/`status` stay the only non-Option).
- [ ] `proj_pull_request` gains 4 columns via **MIGRATION_15 (ALTER-only, CREATE untouched — the D2/LESSON-50 precedent), `SUPPORTED_USER_VERSION` 14→15**; the table stays in `REBUILD_TABLES` (event-folded → rebuild-equivalent).
- [ ] The `PullRequestProjector` (`daemon/src/projections/pull_request.rs:101`) folds the 4 from the payload into the INSERT + ON CONFLICT (None→NULL, rebuild-safe).
- [ ] **Producer capture:** `extract_pr_signals` (`daemon/src/integrations/github.rs:43`) extracts the 4 from the octocrab `pr` → `PullRequestSignals`; they thread into `PullRequestSynced` at the `github.create_pr` emit (`daemon/src/integrations/executor.rs:181`). **No new GitHub API call** — `fetch_pr_signals` already does `.pulls().get(pr_number)` (the GET carries the stats; the list endpoint would not).
- [ ] `read_pull_request_typed` (`daemon/src/ipc/methods.rs:563`) SELECTs the 4 columns; they deserialize directly into `Option<u64>` (JSON numbers — **no** bool-coercion, unlike D5a's `mergeable`).
- [ ] **CONTRACT 0.38.0 → 0.39.0** + the regenerated `shared/contracts/schema/` snapshot + the **3-way verify** (Rust→schema→{zod,pydantic}) green.
- [ ] All unit + integration tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
No new production entry point — extends two wired paths: (a) **read** — `get_projection("PullRequest")` → `read_pull_request_typed` (the typed fail-closed serve, reachable from the IPC read surface); (b) **producer** — the 4 stats ride `PullRequestSynced` emitted by the `github.create_pr` executor (`executor.rs:181`, reachable via the Gateway pipeline). **Producer-gating (Step-2.5 flag, Q2):** the stats populate on `create_pr` now; full population for EXISTING synced PRs rides the **deferred PR-status-refresh sync** (`fetch_pr_signals` exists but is not yet wired to an emit — same gating as D5a's `mergeable`/`checks`). The UI renders a placeholder where the stats are NULL (the work-order's stated D6 UI posture).

## Files expected to touch
**Modified:**
- `shared/src/events.rs` — `PullRequestSynced` + 4 fields.
- `shared/src/projections.rs` — `PullRequestRow` + 4 fields.
- `daemon/src/eventstore/schema.rs` — `MIGRATION_15` (ALTER `proj_pull_request`) + `SUPPORTED_USER_VERSION` 14→15.
- `daemon/src/projections/pull_request.rs` — fold the 4 (INSERT + ON CONFLICT).
- `daemon/src/integrations/github.rs` — `extract_pr_signals` + `PullRequestSignals` + the 4.
- `daemon/src/integrations/executor.rs` — thread the 4 into the `create_pr` `PullRequestSynced` (`:181`).
- `daemon/src/ipc/methods.rs` — `read_pull_request_typed` SELECT the 4.
- `shared/contracts/schema/` — regenerated snapshot (CONTRACT 0.39.0).
- Tests: `daemon/tests/projections.rs`, `shared/tests/contract.rs`, `daemon/tests/gateway_plan.rs`, the github producer test.

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)
1. **`test_pr_diff_stats_folded`** (`daemon/tests/projections.rs`) — Asserts: the projector folds `additions`/`deletions`/`changed_files`/`commits` from `PullRequestSynced` → `proj_pull_request` → `read_pull_request_typed` returns them (test **Some AND None/NULL**). Why: §7.2 fold + LESSON 53 (the NULL arm is the rebuild-safe None).
2. **`test_pull_request_row_schema_snapshot` @ 0.39.0** (`shared/tests/contract.rs`) — Asserts: the `PullRequestSynced` + `PullRequestRow` field-name sets == the checked-in snapshot, tagged `spec(§7.1/§7.2)`; CONTRACT_VERSION==0.39.0; 3-way verify green. Why: the §2.5-seam contract-type change **MANDATES** the schema-snapshot test in this cycle (LESSON 15 traps: bounded-integer `u64`, explicit-null optionals).
3. **`test_extract_pr_signals_captures_diff_stats`** (github producer test) — Asserts: an octocrab PR fixture with additions/deletions/changed_files/commits → `PullRequestSignals` carries the 4. Why: the producer-side capture (work-order D6).
4. **`MIGRATION_15` applies** (floor test `user_version >= 15` + the new columns exist, LESSON 50) + the exact-latest pin in `gateway_plan.rs` bumped. Why: the ALTER landed, double-bump guard.
5. **`test_read_pull_request_typed_diff_stats`** — Asserts: the typed serve round-trips the 4 (Some + NULL→None). Why: the fail-closed typed serve accepts the new columns (LESSON 53 — column + row-field + serve must land together or the serve fails closed).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** `PullRequestSynced` + `PullRequestRow` each gain `additions`/`deletions`/`changed_files`/`commits: Option<u64>` — both are `shared/` §2.5-seam contract types.
- **Orchestrator doc rows to write hot (Step 9):** the `ARCHITECTURE.md` Appendix-A `PullRequestRow` row + the `daemon/CLAUDE.md` MVP-projections [P7.2/4.6] note (the enrichment) + the `EventTypeRegistry` `PullRequestSynced` note + **CONTRACT_VERSION 0.39.0** — orchestrator authors atomic at the seal.
- **§2.5-seam model touched? YES** → the RED outline includes the schema-snapshot test (#2), authored this cycle.

## Things to flag at Step 2.5
1. **Field types — `Option<u64>` or non-Option `u64`?** My default vote: **`Option<u64>`** — mirrors D5a `mergeable`/`checks_summary` (GitHub returns them as eventually-computed/nullable on the GET; rebuild-safe None→NULL; the display row tolerates NULL). Non-Option would force a value where the API may omit one.
2. **Producer scope — extract + `create_pr` emit only, or ALSO build the PR-status-refresh sync?** My default vote: **extract + thread into the existing `create_pr` emit ONLY** — the PR-status-refresh sync (that would call `fetch_pr_signals` and re-emit for existing PRs) stays the deferred follow-on (consistent with D5a's `mergeable`/`checks` being schema-ready-but-refresh-gated). D6 = the schema/contract/extract enrichment; building the refresh sync is its own slice. (Flag the consequence: stats populate on create_pr now; existing synced PRs get them when the refresh lands.) If the user wants the stats LIVE for existing PRs now, that expands D6 to include the refresh-sync producer — surface it.
3. **octocrab field availability — confirm the version's PR model exposes the 4.** My default vote: confirm via Context7 (`/xampprocky/octocrab`) or the Cargo.lock octocrab version that `models::pulls::PullRequest` carries `additions`/`deletions`/`changed_files`/`commits` (all `Option<u64>`, GET-only). `fetch_pr_signals` already GETs the full PR, so the data is in hand — but pin the exact field names against the version.
4. **CONTRACT-delta shape (lead asked to surface this at Step-2.5).** My default vote: the 4 GitHub-API field names verbatim (`additions`/`deletions`/`changed_files`/`commits`), `Option<u64>`, on both `PullRequestSynced` and `PullRequestRow`; the `proj_pull_request` columns same names. Snapshot delta = +4 fields on each of the 2 frozen shapes; CONTRACT 0.38.0→0.39.0.

## Dependencies + sequencing
- **Depends on:** 4.6 (✅ the `PullRequestRow`/`proj_pull_request`/`PullRequestSynced`/`read_pull_request_typed` surface, on main `c22d04d`); the edges GitHub producer (✅ on main).
- **Blocks:** the ui PR Workspace card diff-stats display (§11.2); precedes D7 (`get_pr_diff`) in the wave.

## Estimated commit count
**1.** A single LOCKSTEP slice — the column + fold + row fields + typed serve + producer capture + CONTRACT bump + schema-snapshot are NOT separable (the fail-closed typed serve forces them atomic, LESSON 53). NON-cat-1 (read surface; no mutation/secret). One commit.

## Lessons-logged candidates anticipated
- **Convention candidate** — none new; this REUSES LESSON 53 (the lockstep-enrichment recipe). If anything, a one-line note that the diff-stats are GET-only octocrab fields (absent from the list endpoint) — the producer must GET (it already does).
- **Architecture-doc note candidate** — the producer-gating: the `proj_pull_request` enrichment fields (`mergeable`/`checks`/now the diff-stats) are schema-ready but only LIVE-populated for existing PRs once the deferred PR-status-refresh sync lands; D6 wires them on the `create_pr` path. Worth an AS-BUILT note so the UI/next-session knows the gating.

## How to invoke
1. **Read this brief end-to-end** — Q2 (producer scope) is the one to confirm before RED; the schema-snapshot test (#2) is mandatory (§2.5-seam).
2. **Run `/tdd pr_card_diff_stats`** (already oriented — no `/session-start`).
3. **Step 2.5** — ping back with Q1–Q4 (or defaults); include the CONTRACT-delta shape (Q4) so the orchestrator relays it. Don't go GREEN until sign-off.
4. **Step 9** — confirm the 3-way verify green + the CONTRACT 0.39.0 bump; surface anything beyond the anticipated candidates.
