# /tdd brief — project_activity_folds_rescan_and_nudges

## Feature
Make a freshly-rescanned project APPEAR in the cockpit + resolve the "Scanning…" hang: the ProjectActivity projector ALSO folds `ProjectRescanned` (INSERT-OR-IGNORE a `proj_project_activity` row for the envelope `project_id`), and `deltas_for_event` nudges `ProjectionName::ProjectActivity` on `ProjectRescanned` so the live subscriber refetches. CONTRACT-neutral (no new column, no migration, no `shared/` change).

## Use case + traceability
- **Task ID:** P4.7 (the add-project arc TAIL — the user's ACTIVE "Scanning… forever" blocker; the daemon half, pairs with ui-082)
- **Architecture sections it implements:** `ARCHITECTURE.md §7` (projections — `proj_project_activity` fold), `§7.2` (the projection read/subscribe surface), `§6.1` (`subscribe`/`get_projection` — the live nudge the UI consumes).
- **Widens phase scope because:** a §7/§6.1 projection-surface fix pulled forward into the §4.7 live-validation arc to close the user's active add-project blocker (the 088/089/090 add-project siblings; the LESSON 51/52 delta-mapping pattern). Not a new numbered phase task; CONTRACT-neutral; lead-approved as the fast unblock.
- **Related context (the traced root cause — lead-confirmed via an independent trace):** the cockpit's Add-project grid reads **ProjectActivity** + refetches only on a ProjectActivity nudge (`ui ProjectsOverviewContainer`/`Shell.tsx`). But `ProjectRescanned` folds ONLY into `proj_project`/`proj_repository` (`projections/project_registry.rs`, which have NO `ProjectionName` variant → not UI-readable); `proj_project_activity` folds ONLY `SessionStarted` (`projections/activity.rs:27`); and `deltas_for_event` has NO arm for `ProjectRescanned` (`projections/mod.rs:72-116`; pinned audit-blanket-only by `audit_only_and_no_projector_events_map_no_projection_delta`, `mod.rs:192-206`). So a session-less rescanned project NEVER enters ProjectActivity + fires no nudge → the grid never shows it + never refetches. The rescan itself completes fine (the non-git Python-sidecar path is OK — the executor Succeeds; detect degrades gracefully).
  - **The UI half (ui-082, ui-orchestrator's lane, lands together):** `ProjectActivityRow.name` → OPTIONAL + a path/project_id fallback label (the pre-existing 5th Mock-vs-real gap — `proj_project_activity` has no `name` col; `provisional.ts:162` requires one) + a "Scanning…" timeout. **091 serves no name** — the UI derives the label; the friendly daemon-served NAME is a DEFERRED contract decision (surfaced separately to the lead).

## Acceptance criteria (what "done" means)
- [ ] The `ActivityProjector` (`projections/activity.rs`) folds `ProjectRescanned` → `INSERT INTO proj_project_activity (project_id, …, updated_at_seq) VALUES (<project_id>, defaults, <seq>) ON CONFLICT(project_id) DO UPDATE SET updated_at_seq = excluded.updated_at_seq` — ensure the row EXISTS (counters default 0 for a new project) WITHOUT clobbering an existing project's counters (a `SessionStarted`-then-`ProjectRescanned` must NOT reset `active_sessions`). Keyed on the envelope `project_id`; a `ProjectRescanned` with no `project_id` → no-op (Ok).
- [ ] `deltas_for_event("ProjectRescanned", ids)` → `[ProjectionDelta{ projection: ProjectActivity, kind: Upsert, row: None, id: Some(project_id) }]` (the LESSON 51/52 nudge; row-less by design — the subscriber re-reads `get_projection`).
- [ ] The `audit_only_and_no_projector_events_map_no_projection_delta` test is UPDATED — `ProjectRescanned` REMOVED from the audit-only set (it now nudges ProjectActivity); the other three (`IntegrationConnectionRegistered`/`GithubSyncFailed`/`BranchCreated`) stay.
- [ ] **Production-path pin (LESSON 52):** submit `project.rescan` through the REAL gateway execute (risk-0 auto-execute → `ProjectExecutor` emits `ProjectRescanned` via `emitted_events` → the gateway emitted-events delta loop calls `deltas_for_event`) → assert the ProjectActivity delta is published. NOT a direct `store.append` (which would mask the gateway-path orphan, the D4b/LESSON 52 lesson).
- [ ] Rebuild-equivalence preserved (`proj_project_activity` ∈ REBUILD_TABLES; the `ON CONFLICT DO UPDATE updated_at_seq` fold is rebuild-safe — re-folding the same log yields the same row).
- [ ] `/preflight` clean; NO CONTRACT bump (no new column, no migration, no `shared/` change; `ProjectionName::ProjectActivity` + `proj_project_activity` already exist).

## Wiring / entry point (Step 7.5)
Two daemon-internal sites, BOTH reached on the production `project.rescan` gateway-execute path: (1) `projections/activity.rs::ActivityProjector::apply` (add the `ProjectRescanned` arm); (2) `projections/mod.rs::deltas_for_event` (add the `ProjectRescanned → ProjectActivity` arm). The gateway emitted-events loop already calls `deltas_for_event` for `emitted_events` (LESSON 52/D4b), so the nudge publishes on the live `project.rescan` execute. No new IPC method / no UI-facing surface change.

## Files expected to touch
**Modified:**
- `daemon/src/projections/activity.rs` — the `ProjectRescanned` fold arm (+ the module doc note that it now folds two event types).
- `daemon/src/projections/mod.rs` — the `deltas_for_event` `ProjectRescanned → ProjectActivity` arm + the updated `audit_only_…` test + a new positive delta test.
- `daemon/tests/projections.rs` (or the existing projector test file) — the fold + rebuild-equivalence + the production-path delta pin.

If implementation needs a migration / a `name` column / a `shared/` change, **STOP and flag at Step 2.5** (that would make it the deferred friendly-name contract slice, not this unblock).

## RED test outline (Step 2)
1. **`activity_projector_folds_project_rescanned`** — a `ProjectRescanned` (env.project_id=P) → a `proj_project_activity` row for P exists (counters 0). Why: §7 — the project enters ProjectActivity.
2. **`rescan_fold_does_not_clobber_existing_counters`** — `SessionStarted`(P) [active_sessions=1] then `ProjectRescanned`(P) → active_sessions STAYS 1 (ON CONFLICT DO UPDATE touches only updated_at_seq). Why: the counters table integrity.
3. **`deltas_for_event_project_rescanned_nudges_project_activity`** — `deltas_for_event("ProjectRescanned", ids)` → exactly `[ProjectActivity nudge, id=project_id]`; + the updated audit-only test (ProjectRescanned no longer empty). Why: LESSON 51 the nudge.
4. **`project_rescan_through_gateway_publishes_project_activity_delta`** (production-path, LESSON 52) — submit `project.rescan` through the gateway execute → the ProjectActivity delta is in the published set. Why: LESSON 52 — drive the REAL emitted-events path, not `store.append`.
5. **`rescan_fold_rebuild_equivalent`** — rebuild after a `ProjectRescanned` log → the same `proj_project_activity` row. Why: §17/§4 rebuild-equivalence.

## Cross-doc invariant impact
- **Model field changes:** NONE. CONTRACT-neutral — `proj_project_activity` columns unchanged, `ProjectionName::ProjectActivity` exists, `ProjectionDelta` frozen, no `shared/` change, no migration.
- **Orchestrator doc rows to write hot:** none (the LESSON 51/52 delta-mapping is daemon-internal).
- **§2.5-seam model touched?** No.

## Things to flag at Step 2.5
1. **ON CONFLICT DO NOTHING vs DO UPDATE updated_at_seq.** My default vote: **DO UPDATE SET updated_at_seq = excluded.updated_at_seq** (touch the seq so a re-rescan registers as a fresh nudge target; PRESERVE counters — never reset). DO NOTHING also works for the unblock but a re-rescan wouldn't bump the seq. Either preserves counters; pick DO UPDATE for the re-rescan freshness.
2. **No name in 091 (confirmed cross-cutting).** My default vote: **091 serves NO name** — the UI (ui-082) relaxes `ProjectActivityRow.name` to optional + derives the label. The daemon-served friendly name is the DEFERRED contract decision (a `ProjectRescanned` +name basename-of-path vs a `proj_project` label vs a new `ProjectionName::Project` variant) — I surface those options to the lead separately; NOT this slice.
3. **Nudge id keying.** My default vote: **keyed on `project_id`** (the `proj_project_activity` PK; the subscriber refetches the projection). Row-less by design (LESSON 51 — don't assert `row: None` as load-bearing; the subscriber re-reads).

## Dependencies + sequencing
- **Depends on:** 090 (in-flight, near done — same daemon area; author 091 right after). The ProjectExecutor/`ProjectRescanned`/`proj_project_activity` all exist.
- **Pairs with:** **ui-082** (ui-orchestrator's lane — `ProjectActivityRow.name` optional + fallback label + "Scanning…" timeout). **They must land together:** the daemon row+nudge is inert until the UI parses it (name-optional); the UI fallback is degraded until the daemon serves the row. Coordinate the joint landing → then the lead tells the user to retry.
- **Blocks:** the user's add-project end-to-end (cockpit surface).

## Estimated commit count
**1.** The fold + the nudge + tests — one logical CONTRACT-neutral unit; no safety invariant (a projection completeness fix, the LESSON 51/52 pattern). security-reviewer not-triggered (no mutation/secret/invariant surface; a read-model fold + a subscribe nudge).

## Lessons-logged candidates anticipated
- **Convention candidate** — likely none new (the fold+nudge-agreement is LESSON 51/52; the rescan→activity surfacing is an application of it). If the "a registry-only event must ALSO feed a UI-readable projection to surface in the cockpit" pattern is worth a one-liner, surface it.
- **Architecture-doc note candidate** — that `ProjectRescanned` now nudges ProjectActivity (the LESSON 51 delta-mapping table, if documented).
- **Future TODO (the deferred contract decision)** — the friendly project NAME source + possibly a dedicated `ProjectionName::Project` variant over `proj_project` (closes the D4b future-contract flag) — I carry the options to the lead.
