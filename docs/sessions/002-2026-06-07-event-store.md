# Session 002 — Phase 1.1 event store (envelope + WAL store + §15 redaction gate)

| | |
|---|---|
| **Date** | 2026-06-07 |
| **Phase** | Phase 1 (daemon foundation) — task 1.1 |
| **Track / role** | `daemon` / daemon-implementer |
| **Predecessor** | [001](001-2026-06-07-phase0-spikes-and-contract-freeze.md) |
| **Successor** | _(TBD — Phase 1.2 projections, after the lead cycles)_ |
| **Commits** | `df753aa` (L1) · `b998f20` (L2) · `61089ea` (L3) — slice complete |
| **Note** | Thin doc (context HARD-STOP at L3); backfill detail later if needed. |

## Why this session existed
Implement task 1.1 — the trust-core spine: the single-writer WAL event store (the §7.1 envelope, §15 redaction-before-persist, §16 migrations, §17 resilience). Per `docs/briefs/003-P1-1-event-store.md`. Run as 3 sequenced sub-features (own commit each), a ⚠️ §15 safety slice with a human-gated redaction design question.

## What was built (3 commits)
- **L1 `df753aa` — envelope contract (`shared/`):** `event_envelope.rs` — `EventEnvelope` + `ObjectRef` + `SourceType`(15, EM §8 closed)/`Sensitivity`(5)/`Visibility`(4); `deny_unknown_fields` reject-unknown; CONTRACT_VERSION 0.5.0→0.6.0; regenerated schema + Zod/Pydantic.
- **L2 `b998f20` — event store + root workspace:** root `Cargo.toml` (members shared+daemon, spikes excluded) + `nexusopsd` crate; `eventstore/{mod,schema,migrations}.rs` — events table (DATA_MODEL §2.1) + WAL pragmas, single write-actor (atomic `seq` in one `BEGIN IMMEDIATE` txn), `user_version` migrations + WAL-safe backup/restore + version floor (§16), degrade/quarantine-on-read (§17), `Clock`/`IdGen` seams + deterministic golden-log. `rusqlite 0.34` (cfg_select avoidance). Removed `shared/Cargo.lock` (workspace root lock).
- **L3 `61089ea` — §15 redaction gate:** envelope extension (`redaction_status` + `RedactionStatus` + `redaction_engine_version`; →0.7.0); migration 2 (the columns, honest `unredacted` backfill default); `eventstore/redaction.rs` — `Redactor` trait + prefix Redactor; the **fail-closed writer GATE** (refuses non-redacted → `RedactionRequired`); §16 auto-backup-before-migrate wired into `migrations::run`; quarantine-reason hardened (no content echo).

## Decisions made
- **Freeze-then-extend** the envelope (0.6.0 L1 → 0.7.0 L3) rather than freeze-once — path of least churn once df753aa committed (orchestrator-confirmed after crossed directives).
- **Honest migration backfill** — migration 2 defaults pre-gate rows to `unredacted` (security-reviewer [high]: `redacted` would falsely launder un-redacted L2-era rows).
- **rusqlite 0.34** (satisfies rusqlite_migration 2, libsqlite3-sys 0.31, pre-cfg_select); **root Cargo workspace**; **`time`** dep for SystemClock RFC3339.
- **Redaction design = human-resolved a+** (gate + trait + fail-closed + prefix Redactor in 1.1; entropy → 1.7).

## TDD compliance
Clean. Each sub-feature RED→Step-2.5→GREEN (L3 skipped Step-2.5 per orchestrator under HARD-STOP — design pre-approved a+). Test 4 (writer refuses unredacted) is the load-bearing §15 invariant. Reviewer fixes (security high in both L2 + L3, plus code-quality) applied in-slice.

## Reachability
Trust-core spine — consumed by Phase 1.2 (projections read the log) + Phase 2 (Gateway writes through it); daemon-binary bootstrap = Phase 1.6. Contract: `emit_schema` bin + CI gates (test 8/9). belongs-to-a-phase, not silently unwired.

## Open follow-ups
- **Task 1.7 (deferred, flagged):** Shannon-entropy redactor (OQ-SEC-2); the quarantine / `SensitiveOutputRedacted` path; the §15 redaction property+fuzz test (ARCHITECTURE §14/§340).
- **§16 follow-up:** `run()` skips backup on `from==0` (fresh db); a failed restore surfaces only as `Migration` error — surface restore-failure distinctly.
- **Carry-forwards (L2):** `AppendIntent` grows as 1.2+ callers need optional envelope fields (currently minimal; visibility always DDL-default `project`); normalized `object_refs` table → 1.2; `user_version()` `-1` sentinel → consider `Result`; new deps `time`/`tempfile`/rusqlite-pinned-0.34.
- **Deferred arch-notes (orchestrator routing):** `Timestamp` newtype w/ `format:date-time`; `seq` `minimum:1` schema annotation; `workflow_run_id` kind-vs-String reconcile.
- **Cross-doc (orchestrator writes):** events DDL (DATA_MODEL §2.1) + envelope (§7.1/Appendix A) gain `redaction_status` + `redaction_engine_version` + `RedactionStatus`; CONTRACT_VERSION 0.7.0.

## Preflight
Green at commit time (no changes since `61089ea`): **31 workspace tests** + 3-way Rust/Zod/Pydantic verify (17 enums @ 0.7.0); `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` clean. security-reviewer PASS on every §15/§16/§17 invariant (the L3 [high] fixed in-slice).
