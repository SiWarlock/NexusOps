# Session 039 — the add-project arc (089/090/091/092)

- **Date:** 2026-06-26
- **Phase:** Phase 4.7 (live-validation + the add-project arc — daemon side)
- **Predecessor:** [038-2026-06-26-smoke-cli-drivers-088-rescan-084-parked.md](038-2026-06-26-smoke-cli-drivers-088-rescan-084-parked.md)
- **Successor:** [040-2026-06-26-wave1-daemon-control-lane.md](040-2026-06-26-wave1-daemon-control-lane.md) — WAVE-1 daemon control lane (W1-prof · W1-exec · W1-git-stage)

## Why this session existed

Close the user's **add-project blocker chain** end-to-end (daemon side), paired with the ui-track (ui-080/081/082):
1. **089** — the CLI "can't add a project": `smoke rescan` submitted `project.rescan` with `project_id:None` → the `proj_project` projector healthy-skipped → the project never registered + no id was surfaced.
2. **090** — the cockpit `precondition_stale` masquerade: the IPC boundary silently accepted an empty/malformed `action_request_id` (a `#[serde(transparent)]` newtype doesn't validate on the wire) → an empty-PK audit row → a 2nd submit PK-collided → `AuditWriteFailed` surfaced as a confusing `precondition_stale` (the 5→1 `gateway_error_to_code` overload).
3. **091** — the "Scanning… forever" hang: a session-less rescanned project never entered `ProjectActivity` (the cockpit's grid source) + fired no nudge → the grid never showed it + never refetched.
4. **092** — polish: the cockpit switcher showed the `proj_` ULID instead of a human-readable name.

## What was built

### 089 — smoke CLI rescan project_id mint+print (`f80de4c`)
- **Modified:** `daemon/src/smoke.rs` — `build_rescan_request` mints `Some(ProjectId::new())` on the envelope; new `cmd_rescan` reads the id back, submits, prints `project_id=<minted>` on its own greppable line; `rescan` dispatch arm repointed; USAGE note. `daemon/tests/smoke_request_builders.rs` — `+ProjectId` import, 2 builder tests (mint-is-Some+well-formed, distinct-ids-per-call) + a `_shape` project_id assertion.
- dev-client-only; NO contract.

### 090 — submit id hardening + error-map de-collapse (`0d84bc0` hardening, `4acfede` de-collapse)
- **Modified:** `daemon/src/ipc/methods.rs` — (1) `validate_request_ids(&ActionRequest)` parse-don't-trust guard (action_request_id + project_id-when-Some) called by `submit_action` + `submit_action_plan` (plan also validates `plan_id` + loops every step) **before** the pipeline → `protocol_error`, no row inserted; (2) `gateway_error_to_code` rewritten as an **exhaustive** match (no wildcard): NotFound→not_found, AuditWriteFailed/IllegalTransition/AuditBackboneDown→internal_error, StalePrecondition/ApprovalExpired→precondition_stale, **FencingConflict→fencing_conflict UNCHANGED** (the §17/rule-#6 never-auto-resolved card stays distinct). `daemon/tests/ipc.rs` — 5 dispatch tests (via the real `serve_connection` + a `serve_one` helper asserting NO row in either table) + 2 in-source pure-mapping tests.
- 2 commits (revert/reapply split — both fixes in one file); NO contract. security-reviewer CLEAR (5-axis invariant pass).

### 091 — ProjectActivity folds ProjectRescanned + nudge (`b142b5c`)
- **Modified:** `daemon/src/projections/activity.rs` — `ActivityProjector::apply` folds TWO event types: SessionStarted (unchanged increment) + ProjectRescanned (ensure-row-exists; ON CONFLICT DO UPDATE updated_at_seq ONLY → counters preserved). `daemon/src/projections/mod.rs` — `deltas_for_event` ProjectRescanned→ProjectActivity nudge arm (LESSON 51) + the "KEEP IN SYNC" inventory updated + the audit-only test updated + a positive delta test. `daemon/tests/projections.rs` — `rescan_intent` helper + 4 fold tests. `daemon/tests/runtime.rs` — a production-path test (LESSON 52, real gateway emitted-events path) + a `project_rescan_request` helper.
- CONTRACT-neutral; security not-triggered.

### 092 — friendly project name (`9bef853`)
- **Modified:** `shared/src/events.rs` — `ProjectRescanned +name: Option<String>`. `shared/src/lib.rs` — CONTRACT_VERSION 0.46.0→0.47.0 (+ bump-history doc). `shared/contracts/schema/nexusops-contract.schema.json` — REGENERATED (`cargo run --bin emit_schema`). `shared/tests/contract.rs` — snapshot +name, CONTRACT_VERSION assert→0.47.0, sample +name, bump-history doc. `daemon/src/project/executor.rs` — pure `project_name_from_path(path)` helper (basename; trailing-slash/root/empty/`.`/`..`→None) + emit `name=Some(basename)`. `daemon/src/eventstore/schema.rs` — MIGRATION_19 (ALTER proj_project_activity ADD COLUMN name TEXT). `daemon/src/eventstore/migrations.rs` — SUPPORTED_USER_VERSION 18→19 + wired. `daemon/src/projections/activity.rs` — the 091 fold arm now sets name from the payload. `daemon/tests/{project_executor,projections,project_registry,gateway_plan}.rs` — basename + non-git emit tests; migration floor + fold-sets-name(+counters-preserved) + rebuild-equiv; the 2 ProjectRescanned literals +name; the exact-latest user_version pin 18→19.
- 1 commit; CONTRACT 0.47.0 + MIGRATION_19; 3-way verify PASS (42 enums, 0.47.0); security not-triggered.

## Decisions made
- **089:** mint in `build_rescan_request` (builder stays pure args→ActionRequest, tests assert the mint directly); a dedicated greppable `project_id=<id>` line; `smoke projects` list OUT.
- **090:** the de-collapse mapping table verbatim (ApprovalExpired→precondition_stale — genuinely re-approvable, no `approval_expired` code in the closed set); guard scope = action_request_id + project_id-when-Some (resource_ref ids stay executor-validated); an **exhaustive** match (no wildcard) so a future GatewayError variant is a compile error; 2 bisectable commits via revert/reapply (both fixes in methods.rs; `git add -p` unavailable).
- **091:** ON CONFLICT DO UPDATE updated_at_seq (re-rescan freshness) + PRESERVE counters; nudge keyed on project_id, row-less (LESSON 51); production-path test drives the REAL gateway emitted-events path (LESSON 52, not store.append).
- **092:** `name` = basename of the scan path (`req.inputs["path"]`), NOT repo_root (None for a non-git project — the project-brain sidecar case); `Option<String>` additive (pre-092 events replay as None, rebuild-safe, LESSON 53); ON CONFLICT DO UPDATE name + updated_at_seq (refresh; counters preserved); `proj_project_activity` uses the generic raw-row serve so `name` rides through with no serve change.

## Decisions explicitly NOT made
- **A dedicated `ProjectionName::Project` registry projection over `proj_project`** (would close the D4b future-contract flag) — DEFERRED; the friendly name via `proj_project_activity` is the minimal-correct step (lead pre-ruled).
- **`smoke projects` list command** (089) — OUT; printing the minted id is sufficient to chain `rescan → create --project <id>`.
- **084 device-flow** — still parked (`daemon/tests/device_flow.rs.parked084`), untouched this session.

## TDD compliance
**Clean — no violations.** All four slices were RED-first (RED confirmed for the right reason before GREEN on each). The green-from-start tests are intentional **preservation guards**, not violations: `gateway_error_fencing_conflict_stays_distinct` (090 — the §17 safety pin), `rescan_fold_does_not_clobber_existing_counters` + `rescan_without_project_id_is_noop` (091 — pre-fix the rescan is a no-op so they pass trivially, then must STAY true with the fold). Each was flagged as a guard at Step 2.5.

## Reachability
- **089:** `nexusopsd smoke rescan --path <repo>` → `main.rs:101` → `smoke::run` → `cmd_rescan` → `build_rescan_request`(mints) + `submit("project.rescan")` → `submit_action` GatewayPort → ProjectExecutor → risk-0 auto-execute → ProjectRescanned (envelope id) → proj_project folds → registers; print emits the id. (`--features dev-client` binary.)
- **090:** `submit_action`/`submit_action_plan` reached via dispatch→`serve_connection` (new tests drive the real entry, LESSON 52); `gateway_error_to_code` reached via `gateway_result` on every gateway response.
- **091:** both arms reached on the production `project.rescan` gateway-execute path — proven by `test_rescan_production_path_publishes_project_activity_nudge`.
- **092:** emit + fold + migration on the same production path (091 production-path test + the new emit test + migration-applies-at-open); `name` served via the generic `get_projection(ProjectActivity)` raw serve.

No tested-but-unwired gaps.

## Open follow-ups
- **Cross-doc invariant change (orchestrator writes hot at the seal):** `ProjectRescanned +name: Option<String>` (CONTRACT 0.46→0.47) → the `daemon/CLAUDE.md` EventTypeRegistry row + the `ARCHITECTURE.md` Appendix A row + `proj_project_activity +name` (MIGRATION_19) in the projections row. Flagged at Step 9 (orch confirmed); rides the `/orchestrate-end` round commit.
- **Deferred contract model:** the dedicated `ProjectionName::Project` over `proj_project` (D4b future-contract flag) — still deferred.
- **Standing residual:** the dev-client tests stay CI-dark in default `cargo test`/`/preflight` (LESSON 29) — 089 verified via `--features dev-client`.
- **⚠️ Shared-tree note:** the workspace-wide `/preflight` is currently RED **only** from the ui-implementer's in-flight `ui/gateway-uds` (ui-083: `create_session` not in scope, mid-slice — not committed, not in main, not in this slice). This session's work is green scoped to `-p nexusops-shared -p nexusopsd` (1039/0) + the 3-way verify PASS. Run the full `/preflight` after ui-083 lands.
- **Next:** WAVE-1 — W1-prof (`get_execution_profiles` RPC) + W1-exec (the 4 stub session-executor bodies), dispatched by the fresh orchestrator post-seal.
