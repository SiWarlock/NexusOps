# ui-009 — 6.3e completion (per-hunk over the intent seam) + the L1 read-transport foundation (UDS crate + Tauri host)

- **Date:** 2026-06-14
- **Phase:** Phase 6 (ui-resume) — **P6.3e** (Code/Diff per-hunk, cat-1) · **P6.8 L1** (the live `UdsGatewayPort` read transport — go-live, slices 1–2 of 3)
- **Predecessor:** [ui-008](ui-008-2026-06-13-intent-seam-foundation-and-gatewaymodal.md)
- **Successor:** _(none yet)_
- **Track:** `track/ui` · implementer `ui-implementer` · orchestrator `ui-orchestrator` · lead `ui-team-lead`

## Why this session existed

The ui track resumed from its clean cross-track PAUSE: the daemon froze the 6.3e unblock packet on `main` at **CONTRACT 0.28.0** (per-hunk `git.*` actions + the `get_diff` RPC), boundary-merged into `track/ui` (`a154733`). This session consumed that packet end-to-end — completing the cat-1 6.3e Code/Diff per-hunk surface — and then opened the **live `UdsGatewayPort` transport go-live** (L1, reads-only, lead-GO'd): the pure-Rust transport core + the greenfield Tauri host.

## What was built (4 slices + 1 fixup)

| Slice | Commit | What |
|---|---|---|
| 047 | `fbd6adc` | regen ui-Zod 0.23.0→**0.28.0** + adopt the 6.3e **read** surface (get_diff + diff shapes + the per-hunk action handle), exposed-ahead |
| 048 | `bd07349` | **cat-1** 6.3e per-hunk git actions wired over the 043/044 intent seam |
| 048-fix | `74d26e6` | strip the origin char from the get_diff fixture content (visual-gate finding) |
| 049 | `285fee6` | the **L1 UDS read-client crate** `nexusops-gateway-uds` (codec + handshake + single-shot read RPC) |
| 050 | `3188ea9` | the greenfield **Tauri 2.x host** `ui/src-tauri/` + the Rust **read-command bridge** |

### Files created
- **047:** `ui/src/intent/hunk-resource-ref.test.ts` _(later, that's 048 — see below)_. 047 itself created no files; it regenerated `generated.ts` + extended provisional/mock/test files (see modified).
- **048:** `ui/src/intent/hunk-resource-ref.ts` (the security-critical resource_ref encoder + `buildHunkActionRequest`) + `ui/src/intent/hunk-resource-ref.test.ts` (the conformance pins) + `ui/src/views/code/DiffReview.test.tsx` (L1 render + L2 cat-1 pins).
- **049:** `ui/gateway-uds/{Cargo.toml, src/lib.rs (codec/handshake/call + ClientError + read helpers + 14 unit tests), tests/integration.rs (#[ignore] live socket)}`.
- **050:** `ui/src-tauri/{Cargo.toml, tauri.conf.json, build.rs, src/main.rs, src/lib.rs, src/commands.rs (the bridge + 4 unit tests), capabilities/default.json, .gitignore, icons/×18, tests/smoke.rs (#[ignore])}`.

### Files modified
- **047:** `ui/src/contracts/{generated.ts (regen→0.28.0/37 value-sets), index.ts (+DiffLineKind re-export), provisional.ts (+4 diff shapes .strict()), provisional.test.ts, intent-contracts.ts (+PerHunkGitActionType), intent-contracts.test.ts}`, `ui/src/gateway-client/{types.ts (+get_diff), mock.ts (+get_diff fixture), mock.test.ts}`, `ui/src/safety/model.test.ts`, `ui/src/shell/Shell.test.tsx`.
- **048:** `ui/src/views/code/DiffReview.tsx` (ReviewTab→get_diff sourcing + the per-hunk git-action bar + onAction→seam→GatewayModal), `ui/src/shell/display-meta.ts` (+`enrichHunkAction`), `ui/src/views/display-fixtures.ts` (+`diffReviewContext`), `ui/src/shell/Shell.tsx` (gateway={client} to DiffReview). 048-fix: `mock.ts` + `DiffReview.test.tsx` (origin-char strip + a fixture-fidelity guard).
- **049/050:** root `Cargo.toml` (+`ui/gateway-uds`, +`ui/src-tauri` members) + `Cargo.lock`; `ui/package.json` (+@tauri-apps/cli/api + `"tauri"` script) + `ui/pnpm-lock.yaml`.

## Decisions made
- **047 contract-precision FINDING (orchestrator-confirmed):** the brief + `daemon/CLAUDE.md` §6.3 prose write the resource_ref as `resource_type:"File"`, but the FROZEN ResourceRef is `type:"file"` (field `type`, lowercase value). Conformed to the frozen contract; the prose is informal `ResourceType::File` Rust shorthand. Pinned by `resource_ref_type_is_frozen_lowercase_file`.
- **048 (cat-1, Q1–Q7 consumed, not re-derived):** dedicated git-action bar (not the kit's closed Accept/Reject review bar — the destructive Discard gets an explicit danger-toned label); DiffReview owns its GatewayModal; `enrichHunkAction` is a daemon-SHAPED stand-in (extends the 044 [med] swap-before-real carry-forward); THE security pin = the resource_ref targets the EXACT displayed hunk (`{wt}\x1f{file}\x1f{positions}`, U+001F, submitted==displayed — `security-reviewer` adversarially verified PASS). Visual gate PASS (the danger-toned Discard rendered; the get_diff content rendered live).
- **049 (transport core):** re-implement the codec in-crate (seed from `transport.rs`; deps `nexusops-shared` for the frame TYPES only); one-shot-per-call; a `thiserror` `ClientError` with `Wire(IpcErrorCode)` verbatim. **Hardened beyond the dev-client seed:** a dual-None `RpcResponse` (neither result nor error) → `Protocol` (the §6.1 "exactly one" contract; the smoke.rs leniency is explicitly a non-fail-closed dev tool).
- **050 (Tauri host):** ⚠️ **TOOLCHAIN VERIFIED** at Step 1 (isolated probe compiled the full tauri 2.11 tree on macOS WebKit — the L1 go-live's biggest risk, cleared). Scaffolded via `tauri init --ci`; dropped the scaffold's `tauri-plugin-log` (minimal surface); **TYPED-NARROW command allowlist** (3 read commands registered == the allowlist; NO generic `gateway_call` → no arbitrary-method / L2-mutation reach); `ClientError`→`GatewayCommandError` mapping with the verbatim §6.4 code + a distinct `Internal` host-fault variant.

## Decisions explicitly NOT made (deferred)
- **L2 mutation transport (cat-1)** — HELD on the daemon's 0.30.0 ②-mini approval-enrichment; its own cat-1 checkpoint.
- **The streaming `subscribe` + reconnect→re-subscribe→re-`get_projection` recovery + unique correlation ids** — slice 052.
- **The TS `UdsGatewayPort` + invoke + Zod-parse + the Shell read-path swap** — slice 051 (the consumer of 049+050).
- **`csp:null`→a strict CSP** — pre-ship hardening (the scaffold local-bundle default is fine for dev).
- **`enrichHunkAction`/`gatewayApprovalEnrichment`→the real daemon projection/policy + preview RPC** — before any real human approves (the 044 [med] carry-forward, extended by 048).

## TDD compliance
**CLEAN across all 4 slices.** Each: tests written first → RED confirmed (047 pre-existing drift RED + new pins; 048/049/050 RED on missing impl / `todo!()` stubs) → GREEN. Step-8 review fixes were **test-strengthening / coverage additions** (047 `.strict()` reject-extra pins; 048 `buildHunkActionRequest` direct test; 049 the 6 read-helper/dual-None pins; 050 the `Internal` split) or pinned behavior changes with a new test (049 dual-None→Protocol) — no untested production logic back-filled. The cat-1 048 followed test-first (the encoder + component pins were RED before GREEN), `security-reviewer` PASS.

## Reachability
- **047:** no new wiring (exposed-ahead; the regen layer consumed by the boundary parsers + `describeRejection`).
- **048:** REAL — Shell Code/Diff view → `DiffReview(gateway={client})` → per-hunk button → `useSubmitIntent` → `port.submit_action` → `GatewayModal`. Wired + visual-gated.
- **049:** the crate's `connect_and_call` is now consumed by **050's** `call_daemon` (the bridge) → reachable. The `#[ignore]` integration test is the live exercise.
- **050:** the Tauri host IS the production app entry; the 3 read commands are registered in `generate_handler!` (invokable). The TS caller (`UdsGatewayPort` + Shell read-swap) = **051** → exposed-ahead-of-consumer; the `#[ignore]` smoke is the live exercise. (The full windowed `pnpm tauri dev` launch is the manual step; the Rust build + `pnpm dev` Vite are verified.)
- **No tested-but-unwired gaps** that a later slice removed.

## Cross-doc invariant audit (memory check — track-prefixed; orchestrator shares this checkout)
Every model/contract change was flagged at Step 9 and is being written hot by the orchestrator (the working tree shows `ui/CLAUDE.md` + `ui/LESSONS.md` modified): 047 Generated-contract row (34→37, 0.23.0→0.28.0, `IpcErrorCode`+`not_found`, +4 diff shapes + `get_diff` + `PerHunkGitActionType`); 048 GatewayPort-mutation-surface row (the per-hunk wiring); 049 the `gateway-uds` crate row; 050 the Tauri-host/bridge row. No `ARCHITECTURE.md` edit needed this session (the daemon authored §6.1/§6.3/§6.4 at 0.28.0). **No drift.**

## Open follow-ups (Step-9 categorized + wiring)
- **051 (next):** the TS `UdsGatewayPort` (`ui/src/gateway-client/uds.ts`) invoking the 050 commands + Zod-parsing the returned `Value` + the Shell read-path swap (`MockGatewayPort`→real). Its boundary Zod-validation gets its own review.
- **052:** the streaming `subscribe` + reconnect recovery + unique correlation ids (the 049 `connect_and_call` id=1 → unique ids for a persistent connection).
- **Carry-forwards (cross-track / future-phase):** the L2 mutation transport (cat-1, HELD on 0.30.0); the `enrichHunkAction`/`gatewayApprovalEnrichment`→real-daemon-projection swap (the 044 [med], extended); the 6.3e cross-track resource_ref conformance pairing (the Phase-5 git executor — the daemon-consumer half); the worktree-projection→`worktree_id` source; the concurrent-submit UI guard (benign — idempotency_key dedups); the `MetricQuality` `oneOf`-const generator extension; the codec→`shared` promotion; the **csp:null pre-ship CSP hardening**; the placeholder scaffold icons (real branding); `ExecutionProfile`/`Provider` exposed-ahead (future Settings/7.2).
- **Orchestrator-handled (round-seal):** the CI fix (ubuntu `--exclude nexusops-ui` + a macOS `cargo test -p` job — else the next push's ubuntu cargo job fails on the Tauri webkit deps); the `ui/CLAUDE.md` rows + the lessons (the read↔mutate resource_ref lesson, the transport-core boundary lesson, the Tauri bridge-allowlist lesson).

## How to use what was built
- **Run the cockpit (dev):** `pnpm dev` (Vite, unchanged) for the visual gate; `pnpm tauri dev` runs the Tauri app wrapping it.
- **The live read transport (once 051 lands):** the frontend `invoke("gateway_get_projection"|"gateway_get_diff"|"gateway_get_capabilities")` → the 050 bridge → the 049 crate's `connect_and_call` → the daemon's `gateway.sock`. The `#[ignore]` integration/smoke tests (`cargo test -p nexusops-gateway-uds --test integration -- --ignored` / `-p nexusops-ui --test smoke -- --ignored`) exercise the live round-trip against a running daemon.
