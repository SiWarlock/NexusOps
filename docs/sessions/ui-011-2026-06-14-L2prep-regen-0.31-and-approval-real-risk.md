# ui-011 — the L2-prep closeout regen round (0.28→0.31 sync + ApprovalQueueRow frozen-shadow + the approval-card real-risk swap)

- **Date:** 2026-06-14
- **Phase:** Phase 6 (ui-resume) — **P6.8 L2-prep** (the cross-track 0.31.0 reconcile + the 044 [med] approval-enrichment resolution; **NON-cat-1**)
- **Predecessor:** [ui-010](ui-010-2026-06-14-L1-read-transport-complete.md)
- **Successor:** [ui-012](ui-012-2026-06-14-pre-l2-gates-and-l2-transport-disabled.md)
- **Track:** `track/ui` · implementer `ui-implementer` · orchestrator `ui-orchestrator` · lead `ui-team-lead`

## Why this session existed

After L1 (the live read transport) completed, the daemon's L2-prep packet landed on `main` and was boundary-merged into `track/ui` at **CONTRACT 0.31.0** (`aa74674`): the survival schema (`ResumeMode`/`RecoveryState`, 0.29), the FIRST frozen projection-row `ApprovalQueueRow` (0.30, ②-mini-enriched with `risk_level`/`policy_decision`), and `SessionRecovered` (0.31). The merge left the ui Zod at 0.28.0 → the §5.0 `CONTRACT_VERSION` drift sentinel RED. This round cleared the drift, reconciled the now-frozen shadows, and swapped the approval card off its fixture risk onto the real daemon row — clearing the **044 [med]** L2 gate for the live-approvable path. The lead called the clean closeout boundary at 053; the per-hunk tail (053b) defers to the fresh team.

## What was built (slice 053 — 2 commits)

| Slice / layer | Commit | What |
|---|---|---|
| 053 A+B | `280862d` | regen `generated.ts` 0.28→**0.31.0** (drift GREEN) + reconcile the frozen shadows (`ApprovalQueueRow` 4→14-field; `ResumeMode` 2→4 + `RecoveryState` drift-pinned `oneOf`-const shadows; `RecoveryStatus` re-based) + the consumer fixes |
| 053 C | `544bd76` | swap the approval card's risk/policy from the `gatewayApprovalEnrichment` fixture → the REAL `ApprovalQueueRow` (`enrichApproval(row)`) — **044 [med] RESOLVED** (HIQ/existing-approval path); `security-reviewer` CLEAR |

**Q1 split (053):** 053's Layer C swapped only the **clean** `enrichApproval(row)` path (the `ApprovalQueueRow` in-hand). The **per-hunk** `enrichHunkAction` path (post-submit, no row in hand → re-fetch + match by `action_request_id`) was carved to **053b** at 053's Step-2.5. The lead then **sealed 053 as the closeout boundary → 053b defers to the fresh team** (pre-L2 work; exposed-ahead-of-L2, the per-hunk submit stays HELD → no live gap).

### Files created
- (053b only, then REVERTED on stand-down — see below.) No new files survive this round beyond this session doc.

### Files modified (053, committed)
- **A:** `ui/src/contracts/generated.ts` (regen → 0.31.0, 37 enum $defs unchanged).
- **B:** `ui/src/contracts/provisional.ts` (`ApprovalQueueRow` 4→14-field frozen-shadow `.strict()`; `ResumeMode` 2→4 + `RecoveryState` drift-pinned `oneOf`-const shadows; `RecoveryStatus` re-based) + `provisional.test.ts` (the field-set/`.strict` snapshot + the 2 `oneOf` value-pins); `ui/src/contracts/intent-contracts.ts` (export `RiskLevel`); `ui/src/projections/fixtures/proj_approval_queue.ts` (14-field fixture + `makeApprovalRow` helper); `ui/src/projections/items.ts` + `ui/src/overlays/HumanInputQueue.tsx` + `ui/src/views/command/CommandCenter.tsx` (`.title`→`.preview_summary`); `ui/src/recovery/model.ts` (`RESUME_MODE` 4 values) + `model.test.ts`; `ui/src/gateway-client/mock.test.ts` (version tripwire → 0.31.0); `items.test.ts` + `CommandCenter.test.tsx` (use `makeApprovalRow`).
- **C:** `ui/src/shell/display-meta.ts` (the `enrichApproval` real-row swap; `gatewayApprovalEnrichment` now test-only) + `display-meta.test.ts` (3 pins); `ui/src/shell/derive.ts` (null-project comment) + `derive.test.ts` (null-project exclusion pin); `provisional.test.ts` (`RecoveryState` `toBe(3)`).

## Decisions made
- **F1 (a brief-framing correction):** `ResumeMode`/`RecoveryState` are `oneOf`-of-`const` in the schema, which `gen-contracts.mjs` (flat-`.enum` only) does NOT generate → they reconcile as **drift-pinned provisional shadows** (the MetricQuality pattern), not generated value-sets. `RecoveryState`'s 3 values already matched (banner + pin only); `ResumeMode` grew 2→4.
- **`ApprovalQueueRow` = a hand-declared frozen-shadow** (Q1): `.strict()`, the 14-field set + types snapshot-pinned to the schema `$defs`, enum fields delegated (`RiskLevel`/`ApprovalStatus`/`RequesterType`), `policy_decision`→`PolicyDecision`, optionals present-and-nullable. The generator stays enum-only (a separate carry-forward).
- **The approval card sources real risk/policy from the row** (Layer C): `enrichApproval(row)` reads `row.risk_level`/`row.policy_decision` (never the fixture side-map / UI-derived — LESSON 17). Absent policy → an honest "awaiting" placeholder (the real risk preserved). The modal renders the displayed risk *number* from the live `preview_action` `ActionPreview` (so the row's `risk_level` is correct-but-secondary). `gatewayApprovalEnrichment` is now test-only.
- **Q2 split → then lead-sealed at 053:** the per-hunk `enrichHunkAction` real-risk swap (053b) was carved out; the lead then deferred it to the fresh team (clean GREEN boundary at 053).
- **Consumer reconciles:** `.title`→`.preview_summary` (the frozen row has no `title`); a null `project_id` (plan-level approval) is excluded from per-project switcher counts by construction (pinned + commented; a global "waiting" bucket is a future product call).

## Decisions explicitly NOT made (deferred)
- **053b — the per-hunk `enrichHunkAction` real-risk swap** — DEFERRED to the fresh team (the brief `docs/briefs/053b-…` is ready, spec-lint PASS `@78593dfa`; the design + tests are in Open follow-ups below). Exposed-ahead-of-L2; the per-hunk submit stays HELD → no live security gap from the deferral.
- **L2 mutation transport** — cat-1, HELD on the daemon's 0.30.0 ②-mini; its own cat-1-checkpointed slice (after the connection-state reconcile).
- **The two-writer connection-state reconcile** (the 052 routed Finding) — the fresh team's pre-L2 slice.
- **`SessionRecovered`/recovery-UX consumption** (Q4) — a later ui-feeding slice (this round brought the generated/shadow types only).

## TDD compliance
**CLEAN (053).** Layer A: the existing §5.0 drift test was RED → the regen made it GREEN. Layer C: the 3 `enrichApproval` pins were written first → RED-confirmed (the old fixture-side-map path) → GREEN. **Layer B note:** the provisional→frozen-shadow reconcile + its drift-pins **co-landed** — a SCHEMA-DRIVEN reconcile (the frozen schema is the authority; the pins enforce conformance, and would be RED on the old 4-field/2-value shadows). Mechanical contract-reconcile per LESSON 14, not behavior-design. The `/session-end` TDD audit added the `derive.ts` null-project pin + the reducer projection-guard pin (in 053). `security-reviewer` CLEAR on Layer C (the approval-card surface). **053b stand-down:** the 053b tests were written then **reverted** on the lead's stand-down (they referenced the not-yet-built async `enrichHunkAction` signature; reverting kept the tree GREEN at the 053-committed state).

## Reachability
- **053 A/B:** exposed-ahead-of/under the existing consumers (the regen + the reconciled shadows are consumed by the boundary parsers + the approval consumers).
- **053 C (Step 7.5):** REAL — `client.get_projection("ApprovalQueue")` → `data.approvals` → `pending[0]`/HIQ → `Shell.tsx:507 enrichApproval(overlay.approval)` → the card's risk/policy from the **real `ApprovalQueueRow`**. The mutation submit stays L2-HELD (the `UdsGatewayPort` mutation methods throw not-wired). No tested-but-unwired gaps.

## Open follow-ups
- **053b — DEFERRED to the fresh team (pre-L2):** the per-hunk `enrichHunkAction` real-risk swap. Design (ready): `enrichHunkAction(actionType, ack, gateway: Pick<GatewayPort,"get_projection">)` → re-fetch `get_projection("ApprovalQueue")` → **exact** match by `action_request_id` → `enrichApproval(row)` (DRY); absent → honest pending; malformed → `BoundaryValidationError` propagates; **no-mutation-reach by construction** (the narrowed param). `security-reviewer` REQUIRED (real-risk match + absent-pending + exact-match-no-wrong-approval). Brief `docs/briefs/053b-P6-L2prep-per-hunk-enrich-real-risk.md`. Completes the 044 [med] on both paths.
- **Carry-forwards (orchestrator records at the round seal):** the `gatewayApprovalEnrichment` → test-fixture relocation (it's now test-only; the `GatewayApprovalEnrichment` interface stays — it's `enrichApproval`'s return type) · the null-project-approval **global "waiting" bucket** product-intent question (currently excluded from per-project counts) · the `SessionRecovered`/recovery-UX consumption · the `gen-contracts.mjs` `oneOf`-const generator extension (retires the `ResumeMode`/`RecoveryState`/`MetricQuality` drift-pinned shadows) · a modal-level absent-policy render test (depth) · the 052 connection-state two-writer Finding (pre-L2 reconcile).
- **Orchestrator doc rows (Step 9 — flagged):** the `ui/CLAUDE.md` Generated-contract row → **0.31.0** + the frozen-`ApprovalQueueRow` note + the approval-card row → **044 [med] RESOLVED (HIQ path; per-hunk = 053b)** + the provisional-shapes update (`ResumeMode`(4)/`RecoveryState` drift-pinned shadows; `RecoveryStatus` still provisional) + the L2-prep LESSON. **No `ARCHITECTURE.md` edit / no new ui-authored shared model** (the `ApprovalQueueRow`/survival schema are daemon-frozen + consumed; the §5.0 snapshot pin was authored this round).

## How to use what was built
- The cockpit now reads REAL daemon data live (L1) AND the HIQ/existing-approval card shows the daemon's authoritative `risk_level`/`policy_decision` (no fixture risk). A real human can approve against real risk on the live-approvable path.
- The remaining L2 runway (fresh team): 053b (per-hunk real-risk) → the connection-state reconcile → the L2 cat-1 checkpoint → L2 (the per-hunk/approval submit goes live, exercising this plumbing).
