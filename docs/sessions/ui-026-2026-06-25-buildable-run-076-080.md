# ui-026 — buildable run (ui-076…080): graph perf · merge-method selector · prod-bundle gate · cockpit resilience · §7.4 hygiene

- **Date:** 2026-06-25
- **Phase:** 6/7 (P6.6 graph perf · P6.11 cockpit resilience · P7.2 merge-method · §10.6 + §7.4 hygiene) · track `ui`
- **Predecessor:** [ui-025](ui-025-2026-06-25-pr-mutations-cat1-go-live.md)
- **Successor:** _(next ui session — daemon-gated; resume `/team-start ui`)_
- **Commits:** `6d68090` (ui-076) · `8f9536c` (ui-077) · `e0e3b25` (ui-078) · `4f634ff` (ui-079) · `1d702b5` (ui-080) — all LOCAL on `track/ui`, push HELD. Round rides the orch's 0.46 cross-track sync (`f199405`) + the `track/ui → main` merge (`579b1ef`, push HELD — main ahead of origin, the user pushes).

## Why this session existed

After the ui-075 cat-1 PR-mutations go-live (ui-025), the orchestrator dispatched a **buildable run** of merge-independent ui slices (the lead OK'd proceeding while the `track/ui → main` merge held for a coordinated daemon window): two perf/robustness fixes, a §7.2 feature completion, and two hygiene/hardening items that closed carry-forwards from the go-live arc.

## What was built

### ui-076 — ProjectGraph `parentLabelOf` O(n²)→O(n) memo (`6d68090`, perf, NON-cat-1)
**Modified:** `src/views/graph/ProjectGraph.tsx` (NEW exported pure `parentLabels(graph)` — one O(n) `childId→parentLabel` Map built once; the Contained-by column reads `.get(id) ?? "—"` O(1)/row, replacing the per-row O(n²) double-`.find()`) · `src/views/graph/ProjectGraph.test.tsx` (4 pins). Behavior byte-identical (root/missing-parent → `"—"`, first-wins on duplicate `to`); the unchanged `list_fallback_represents_every_edge` render test is the end-to-end wiring pin; **bench:guard green** (§18 budget held).

### ui-077 — PR merge-method selector (`8f9536c`, NON-cat-1, rides the live merge gate)
**Modified:** `src/intent/pr-mutation-request.ts` (`MergePrMethod` widened merge|squash|rebase + a shared `MERGE_METHODS`) · `src/views/code/PrWorkspace.tsx` (a labeled `<select>` — `appearance:none` + kit form-control tokens + a caret + the global focus ring — threading `onMerge(method)`; default "merge") · `src/views/code/DiffReview.tsx` (`onMerge(method)` → `buildMergePrActionRequest`) + the matching test files (5 pins). The gate (`canMerge`) is unchanged + method-AGNOSTIC; no new gate/`enabledPrMutations`/action_type/contract. security-reviewer CLEAR (INV-SEC-1 holds).

### ui-078 — prod-no-Mock bundle CI gate (`e0e3b25`, §10.6 hardening, infra)
**Created:** `src/../scripts/check-prod-bundle.mjs` (pure `scanForForbidden` + `FORBIDDEN` + a guarded I/O `main` scanning `dist/**/*.js`, exit 1 + offending file) · `scripts/check-prod-bundle.test.mjs` (4 scanner pins). **Modified:** `vitest.config.ts` (include +`scripts/**/*.test.mjs`) · `package.json` (`check:bundle`) · `.github/workflows/ci.yml` (a GATING `prod-bundle gate (no Mock)` step). Mechanically backstops the ui-075 prod-no-Mock guarantee; verified end-to-end (planted leak → exit 1; clean → exit 0). **Closes the ui-075 §10.6 Future-TODO.**

### ui-079 — cockpit-load per-projection resilience (`4f634ff`, version-independent bug, NON-cat-1)
**Modified:** `src/shell/Shell.tsx` (the load `useEffect`: `Promise.all` → per-call `settle()` wrappers + `Promise.all`; caps in its own try/catch; a `degradedProjections` set; `degraded.size===7` total-fault → the existing blank; `ShellData.degraded` field; an inline honest partial-data banner in the banner-stack — distinct from the transport DegradedBanner, glyph+label, role="status") · `src/shell/Shell.test.tsx` (6 pins). One projection rejecting now degrades only its tile (honest §11.7), not the whole cockpit; caps-fault → fail-safe read-only independent. **Banked as LESSON §38.**

### ui-080 — §7.4 hygiene refactor (`1d702b5`, behavior-preserving, NON-cat-1)
**Modified:** `src/shell/display-meta.ts` + `src/views/code/DiffReview.tsx` (rename `enrichHunkAction` → `enrichActionApproval` — reflects its reuse by merge + review + per-hunk enrichment) + the matching test refs · `src/shell/Shell.uds-swap.test.tsx` (unify the `productionShellAt{CodeView,PrWorkspace}` mock boilerplate → one `installProductionShellInvoke()` + a shared `STUB_DIFF`, using the live `CONTRACT_VERSION`; −58 lines). **Closes the §7.4 hygiene carry-forward.**

## Decisions made

- **ui-076:** an exported pure `parentLabels(graph)` helper (vs an inline map) so the dangling-edge defensive `"—"` (unreachable via `buildProjectGraph`) is directly testable; first-wins on duplicate `to` preserved (matches the old `.find`).
- **ui-077:** native `<select>` (keyboard-reachable + labeled + closed options) over a split-button/dropdown; the `appearance:none` + kit-token + caret fix so the kit styling paints on macOS; no extra danger styling for squash/rebase (the per-action GatewayModal + daemon risk-3 is the gate); uniform method-agnostic `canMerge`.
- **ui-078:** GATING CI step (a safety property should block); a pure `scanForForbidden` + a guarded I/O main; tests drive off the real `FORBIDDEN` + a config-equality pin (a config drift fails the test); substring + all-matches scan.
- **ui-079:** per-call typed `settle()` wrappers over raw `Promise.allSettled` (type-clean across the heterogeneous pages); the (b)-lite §11.7 treatment (a `degraded` set + one global honest banner, not per-tile-overkill, not silent-empty); the **total-fault distinction** — ALL 7 reject → the EXISTING blank `shell-load-error` (one honest "couldn't load", not 7 banners; preserves the 2 total-fault tests), a PARTIAL failure → render + the banner; caps independent.
- **ui-080:** pure rename (typecheck-proven complete) + a test-helper DRY; the code-quality-reviewer skipped for the trivial behavior-preserving change (typecheck + suite are the guard — orch-agreed).

## Decisions explicitly NOT made (deferred)

- **ui-076:** the Shell `sidebarItems`/`commandItems` memo — scoped OUT (no confirmed per-render recompute); a separate follow-on only if one surfaces.
- **ui-077:** a history-rewrite danger hint on squash/rebase (doesn't earn its keep; the approval card shows the method).
- **ui-078:** AST/semantic bundle analysis (substring is the accepted backstop; full-mangle of a non-tree-shaken identifier is a documented residual — Vite DCE is the load-bearing guarantee).
- **ui-079:** per-tile degrade states (the tiles can't show them cheaply → the global banner is the §11.7 middle).

## TDD compliance

**CLEAN.** ui-076/077/079 were test-first (RED confirmed for the right reason on each; the regression-guard tests — all-succeed / default-merge — correctly passed pre-impl). ui-078's deterministic core (`scanForForbidden`) was test-first (RED: module missing); its thin I/O wrapper is infra, verified end-to-end (planted-leak → exit 1). ui-080 is a sanctioned behavior-preserving refactor (no /tdd; the 493/0 suite is the guard) — not a violation. No TDD violations.

## Cross-doc invariant audit (multi-track → memory check)

No frozen-contract model field changed this round. The one field add — **`ShellData.degraded`** (ui-079) — is a **UI-local view-model field**, NOT a frozen `shared/` contract; flagged at Step 9 and the orchestrator confirmed **no cross-doc row needed**. `MergePrMethod` widening (ui-077) is a UI-local type, not a contract. CLEAN.

## Reachability

- **ui-076:** Graph content-view → `ProjectGraph` → the List/table Contained-by column (`parentLabels`); the unchanged render test is the e2e pin.
- **ui-077:** Code view → `DiffReview` → `PrWorkspaceContainer` → the `<select>` → `onMerge(method)` → `buildMergePrActionRequest` → the live `submit_action`.
- **ui-078:** the GATING CI step (`pnpm check:bundle`) + the local script; the pure scanner unit-pinned.
- **ui-079:** the production cockpit-mount load `useEffect`; the partial-data banner in the production banner-stack.
- **ui-080:** same call sites, renamed; test-helper refactor (no production entry change).
No tested-but-unwired gaps.

## Open follow-ups

Step-9 items (routed hot to the orchestrator; banked/closed at its seals):
- **LESSON §37** (ui-077) — an additive parameter on an already-live gated mutation is non-cat-1, rides the existing gate, daemon validates the param fail-closed, gate stays param-agnostic. **Banked.**
- **LESSON §38** (ui-079) — multi-projection INITIAL-load resilience (per-call settle, honest §11.7 partial-data banner, caps-independent, total-fault → existing-blank; complements [[29]]). **Banked.**
- **ui-075 §10.6 Future-TODO** (bundle-grep gate) → **CLOSED @ui-078.**
- **§7.4 hygiene carry-forward** (rename + helper unify) → **CLOSED @ui-080.**
- **ui-077 scoped visual gate** (the merge-method selector + Merge region, via `dev:mock`) — non-blocking UI-correctness pixel-check; the orchestrator was to request it via lead → user. If not yet run, it's a non-blocking polish follow-up (the kit listbox is the fallback if the styled native select reads wrong).
- **ui-079 partial-data banner** — a new rendered surface; the orchestrator ruled no scoped visual gate needed (reuses the banner-stack pattern [[11]]).
- **The `track/ui → main` merge** (`579b1ef`) is DONE (preflight from main GREEN, 493/493); **push HELD** (main ahead of origin) — the user pushes. Remaining ui scope is daemon-gated → a `/team-end` pause.

## How to use what was built

- **Merge-method selector:** Code/Diff → Pull requests → a PR → the "Merge method" select (Merge commit / Squash and merge / Rebase and merge) → Merge.
- **Prod-bundle gate:** `pnpm check:bundle` (or the CI step) → exit 1 if the dev Mock leaked into the prod bundle.
- **Cockpit resilience:** a single failed projection now shows the rest + an honest "Some data couldn't load: …" banner (force one via the ui-075 `dev:mock` harness to see it).
