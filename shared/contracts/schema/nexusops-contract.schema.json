{
  "$defs": {
    "ActionAck": {
      "additionalProperties": false,
      "description": "`submit_action` result (§6.1) — the ack the ui/Brain intent seam receives: the minted\n`action_request_id` (so the client can `preview_action`/track it) + the action's current\n`status` (§5.1 ActionRequest(15); in 2.1b the stub policy lands it at `awaiting_approval`).\n2.1b L2: defined + returned over the wire; it joins the published schema + the 3-way verify\nwith the rest of the §6.1 mutation surface at L3's CONTRACT_VERSION 0.16.0 bump (`PlanAck` →\n2.1c with `submit_action_plan`).",
      "properties": {
        "action_request_id": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/ActionRequestStatus"
        }
      },
      "required": [
        "action_request_id",
        "status"
      ],
      "type": "object"
    },
    "ActionApprovalRequested": {
      "additionalProperties": false,
      "description": "`ActionApprovalRequested` payload (§6.2/§7.1; AG §17.1) — emitted when the policy decision routes\nan action to human approval (2.1b). The action + approval IDENTITY are on the envelope columns\n(`action_request_id`/`approval_id`); the payload echoes `approval_id` so a payload-only consumer\n(the Brain indexer) gets the linkage without joining the envelope.",
      "properties": {
        "approval_id": {
          "type": "string"
        }
      },
      "required": [
        "approval_id"
      ],
      "type": "object"
    },
    "ActionApproved": {
      "additionalProperties": false,
      "description": "`ActionApproved` payload (§6.2/§7.1; AG §17.1) — a human/policy approved the action (2.1b L3).\nIdentity (`action_request_id`/`approval_id`) on the envelope; the payload echoes `approval_id` +\nthe optional `decided_by` (the approver, when known).",
      "properties": {
        "approval_id": {
          "type": "string"
        },
        "decided_by": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "approval_id"
      ],
      "type": "object"
    },
    "ActionDenied": {
      "additionalProperties": false,
      "description": "`ActionDenied` payload (§6.2/§7.1; AG §17.1) — the action was denied (terminal). Carries the\n`approval_id` + the denial `reason` (the audit record of WHY).",
      "properties": {
        "approval_id": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "approval_id",
        "reason"
      ],
      "type": "object"
    },
    "ActionDependency": {
      "additionalProperties": false,
      "description": "A step-ordering edge within an [`ActionPlan`] (§6.2 ActionDependency). AG §9.2 references it\nbut never defines its shape — frozen as the minimal step-ordering edge (chosen shape recorded\nfor AG/Appendix-A adoption at Step-9).",
      "properties": {
        "depends_on_step_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "step_id": {
          "type": "string"
        }
      },
      "required": [
        "step_id",
        "depends_on_step_ids"
      ],
      "type": "object"
    },
    "ActionExpired": {
      "additionalProperties": false,
      "description": "`ActionExpired` payload (§6.2/§7.1; AG §17.1) — the approval lapsed past `expires_at` before a\ndecision (§17); the action is terminal, never executed. Carries the `approval_id`.",
      "properties": {
        "approval_id": {
          "type": "string"
        }
      },
      "required": [
        "approval_id"
      ],
      "type": "object"
    },
    "ActionFailed": {
      "additionalProperties": false,
      "description": "`ActionFailed` payload (§6.2/§7.1; AG §17.1) — the executor failed (executing→failed). Carries\nthe failure `error` message (the structured ActionError taxonomy → 2.4).",
      "properties": {
        "error": {
          "type": "string"
        }
      },
      "required": [
        "error"
      ],
      "type": "object"
    },
    "ActionPlan": {
      "additionalProperties": false,
      "description": "A bundled multi-step plan (§6.2 ActionPlan; O-3). `rollback_plan` (whose RollbackPlan shape\nAG never pins) is DEFERRED → 2.4; `summary`/`created_at`/`evidence_refs` are additive-later\n(the 2.1b `action_plans` table can carry `created_at` as a column without the model).",
      "properties": {
        "approval_mode": {
          "$ref": "#/$defs/ApprovalMode"
        },
        "dependencies": {
          "items": {
            "$ref": "#/$defs/ActionDependency"
          },
          "type": "array"
        },
        "overall_risk": {
          "$ref": "#/$defs/RiskLevel"
        },
        "plan_id": {
          "$ref": "#/$defs/ActionPlanId"
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/ActionPlanStep"
          },
          "type": "array"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "plan_id",
        "title",
        "steps",
        "dependencies",
        "overall_risk",
        "approval_mode"
      ],
      "type": "object"
    },
    "ActionPlanId": {
      "description": "`aplan_<ULID>` — an [`crate::actions::ActionPlan`] (§6.2; a bundled multi-step plan, O-3).",
      "type": "string"
    },
    "ActionPlanStep": {
      "additionalProperties": false,
      "description": "One step of a bundled [`ActionPlan`] (§6.2 ActionPlanStep; AG §9.3). Wraps a full\n[`ActionRequest`]; `status` is the step's own §5.1 [`ActionRequestStatus`] within the plan.",
      "properties": {
        "action_request": {
          "$ref": "#/$defs/ActionRequest"
        },
        "can_skip": {
          "type": "boolean"
        },
        "label": {
          "type": "string"
        },
        "required": {
          "type": "boolean"
        },
        "rollback_action_type": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/ActionRequestStatus"
        },
        "step_id": {
          "type": "string"
        }
      },
      "required": [
        "step_id",
        "label",
        "action_request",
        "required",
        "can_skip",
        "status"
      ],
      "type": "object"
    },
    "ActionPreview": {
      "additionalProperties": false,
      "description": "A dry-run preview of an action (§6.2 ActionPreview envelope; AG §9.4). The 6 typed per-class\npreviews (command/diff/api/session/workflow/rollback) are **2.3**'s deliverable. DEFERRED → 2.3.",
      "properties": {
        "action_request_id": {
          "type": "string"
        },
        "cannot_preview_reason": {
          "type": [
            "string",
            "null"
          ]
        },
        "changed_resources": {
          "items": {
            "$ref": "#/$defs/ResourceRef"
          },
          "type": "array"
        },
        "generated_at": {
          "$ref": "#/$defs/Timestamp"
        },
        "risk_level": {
          "$ref": "#/$defs/RiskLevel"
        },
        "risk_reasons": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "action_request_id",
        "generated_at",
        "risk_level",
        "risk_reasons",
        "summary",
        "changed_resources"
      ],
      "type": "object"
    },
    "ActionRequest": {
      "additionalProperties": false,
      "description": "A single typed intent submitted to the Gateway (§6.2 ActionRequest; binding = Appendix A +\nDATA_MODEL §2.9). `inputs` is the open per-action-type payload (`params`; the §6.3 catalog\npins per-type schemas in 2.2 — `action_type` is a `String` here). `status` binds the frozen\n§5.1 [`ActionRequestStatus`] (15). NO daemon behavior here — the pipeline that constructs +\npersists these is 2.1b.",
      "properties": {
        "action_request_id": {
          "type": "string"
        },
        "action_type": {
          "type": "string"
        },
        "created_at": {
          "$ref": "#/$defs/Timestamp"
        },
        "fencing_token": {
          "description": "the lease fencing token this execution must present (§17 / §2.6 — bound at execute in 2.4)",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "idempotency_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "inputs": {
          "description": "the open per-action-type input payload (`params`); the §6.3 catalog pins per-type\nschemas in 2.2 (free JSON object here, reject-unknown does not apply to its contents)"
        },
        "preview": {
          "anyOf": [
            {
              "$ref": "#/$defs/ActionPreview"
            },
            {
              "type": "null"
            }
          ]
        },
        "project_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "requester_id": {
          "type": "string"
        },
        "requester_type": {
          "$ref": "#/$defs/RequesterType"
        },
        "resource_refs": {
          "items": {
            "$ref": "#/$defs/ResourceRef"
          },
          "type": "array"
        },
        "risk_level": {
          "$ref": "#/$defs/RiskLevel"
        },
        "status": {
          "$ref": "#/$defs/ActionRequestStatus"
        }
      },
      "required": [
        "action_request_id",
        "action_type",
        "requester_type",
        "requester_id",
        "resource_refs",
        "inputs",
        "risk_level",
        "status",
        "created_at"
      ],
      "type": "object"
    },
    "ActionRequestStatus": {
      "description": "ActionRequest (15, R-5, NEW) — the execution-lifecycle axis.\nSchema name = `ActionRequestStatus` (its Rust alias) so the §6.2 `actions::ActionRequest`\nMODEL takes the canonical bare `ActionRequest` $def (2.1a Option-B rename; value-set\nunchanged).",
      "enum": [
        "submitted",
        "previewed",
        "policy_decided",
        "awaiting_approval",
        "approved",
        "denied",
        "queued",
        "executing",
        "succeeded",
        "failed",
        "partially_succeeded",
        "rolled_back",
        "rollback_failed",
        "cancelled",
        "expired"
      ],
      "type": "string"
    },
    "ActionRequested": {
      "additionalProperties": false,
      "description": "`ActionRequested` payload (§6.2/§7.1; AG §17.1) — the Action Gateway's FIRST event for a\nsubmitted action (2.1b). The action's IDENTITY (`action_request_id`, `correlation_id`,\n`project_id`, `actor`) lives on the [`crate::event_envelope::EventEnvelope`] typed columns — the\nGateway populates them. This payload carries only the request-classification DELTA: the\n`action_type` (§6.3 catalog, String until 2.2), the requester-supplied `risk_level` (RECORDED\nfor audit but NOT trusted for the decision in 2.1b — the policy stub is risk-blind, require-\napproval-for-all; 2.2 makes risk catalog-authoritative), and the `requester_type` (R-2; the\nenvelope `actor_type` is its mapped audit actor).",
      "properties": {
        "action_type": {
          "type": "string"
        },
        "requester_type": {
          "$ref": "#/$defs/RequesterType"
        },
        "risk_level": {
          "$ref": "#/$defs/RiskLevel"
        }
      },
      "required": [
        "action_type",
        "risk_level",
        "requester_type"
      ],
      "type": "object"
    },
    "ActionResult": {
      "additionalProperties": false,
      "description": "The outcome of an executed action (§6.2 ActionResult; binding = Appendix A). `emitted_events`\nare typed [`EventId`]s (uniform with the ID discipline). `error` is a message (the structured\nActionError taxonomy is DEFERRED → 2.4).",
      "properties": {
        "action_request_id": {
          "type": "string"
        },
        "changed_resources": {
          "items": {
            "$ref": "#/$defs/ResourceRef"
          },
          "type": "array"
        },
        "created_resources": {
          "items": {
            "$ref": "#/$defs/ResourceRef"
          },
          "type": "array"
        },
        "emitted_events": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "error": {
          "type": [
            "string",
            "null"
          ]
        },
        "rollback_available": {
          "type": "boolean"
        },
        "status": {
          "$ref": "#/$defs/ActionResultStatus"
        }
      },
      "required": [
        "action_request_id",
        "status",
        "created_resources",
        "changed_resources",
        "emitted_events",
        "rollback_available"
      ],
      "type": "object"
    },
    "ActionResultStatus": {
      "description": "Terminal outcome of an executed action (§6.2 ActionResult).",
      "enum": [
        "succeeded",
        "failed",
        "partially_succeeded",
        "cancelled"
      ],
      "type": "string"
    },
    "ActionStarted": {
      "additionalProperties": false,
      "description": "`ActionStarted` payload (§6.2/§7.1; AG §17.1) — the executor began running the approved action\n(queued→executing). Identity on the envelope; no delta beyond the event itself.",
      "type": "object"
    },
    "ActionSucceeded": {
      "additionalProperties": false,
      "description": "`ActionSucceeded` payload (§6.2/§7.1; AG §17.1) — the executor completed the action successfully\n(executing→succeeded). Identity on the envelope; the created/changed resources land in the\n`ActionResult` surface (2.3). No payload delta in 2.1b.",
      "type": "object"
    },
    "ActorRefBody": {
      "additionalProperties": false,
      "description": "A specific approving actor (§6.2 Approval; the `actor` of a [`RequiredApprover`]).",
      "properties": {
        "actor_id": {
          "type": "string"
        },
        "actor_type": {
          "$ref": "#/$defs/RequesterType"
        }
      },
      "required": [
        "actor_type",
        "actor_id"
      ],
      "type": "object"
    },
    "ActorType": {
      "description": "The 10 canonical audit actors (R-2). Serialized as snake_case `TEXT`.",
      "enum": [
        "user",
        "project_brain",
        "action_gateway",
        "workflow_runtime",
        "local_runner",
        "session_adapter",
        "integration_syncer",
        "system",
        "remote_client",
        "automation_policy"
      ],
      "type": "string"
    },
    "AgentTeam": {
      "description": "AgentTeam (9, R-6).",
      "enum": [
        "draft",
        "starting",
        "active",
        "waiting_on_human",
        "blocked",
        "reconciling_outputs",
        "completed",
        "failed",
        "archived"
      ],
      "type": "string"
    },
    "Approval": {
      "additionalProperties": false,
      "description": "A human/policy decision over an action or plan (§6.2/§5.1 Approval; binding = Appendix A).\n`status` binds the frozen §5.1 [`ApprovalStatus`] (10). `constraints` (unpinned\nApprovalConstraint shape) is DEFERRED → 2.2.",
      "properties": {
        "action_request_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "approval_id": {
          "$ref": "#/$defs/ApprovalId"
        },
        "decided_at": {
          "anyOf": [
            {
              "$ref": "#/$defs/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "decided_by": {
          "type": [
            "string",
            "null"
          ]
        },
        "expires_at": {
          "anyOf": [
            {
              "$ref": "#/$defs/Timestamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "plan_id": {
          "anyOf": [
            {
              "$ref": "#/$defs/ActionPlanId"
            },
            {
              "type": "null"
            }
          ]
        },
        "required_approver": {
          "$ref": "#/$defs/RequiredApprover"
        },
        "risk_level": {
          "$ref": "#/$defs/RiskLevel"
        },
        "scope": {
          "$ref": "#/$defs/ApprovalScope"
        },
        "status": {
          "$ref": "#/$defs/ApprovalStatus"
        }
      },
      "required": [
        "approval_id",
        "required_approver",
        "status",
        "risk_level",
        "scope"
      ],
      "type": "object"
    },
    "ApprovalId": {
      "description": "`appr_<ULID>` — an [`crate::actions::Approval`] (§6.2; the human/policy decision object).",
      "type": "string"
    },
    "ApprovalMode": {
      "description": "How a bundled [`ActionPlan`] is approved (§6.2, O-3).",
      "enum": [
        "approve_all",
        "step_by_step",
        "mixed",
        "blocked"
      ],
      "type": "string"
    },
    "ApprovalScope": {
      "description": "The approval's authority scope (§6.2 Approval).",
      "enum": [
        "single_action",
        "plan",
        "policy_grant"
      ],
      "type": "string"
    },
    "ApprovalStatus": {
      "description": "Approval (10, R-5) — split from ActionRequest; the human/policy decision axis.\nSchema name = `ApprovalStatus` (its Rust alias) so the §6.2 `actions::Approval` MODEL\ntakes the canonical bare `Approval` $def (2.1a Option-B rename; value-set unchanged).",
      "enum": [
        "requested",
        "previewed",
        "awaiting_approval",
        "approved",
        "denied",
        "edited",
        "auto_approved_by_policy",
        "expired",
        "cancelled",
        "escalated"
      ],
      "type": "string"
    },
    "AuditIntegrityViolation": {
      "additionalProperties": false,
      "description": "`AuditIntegrityViolation` payload (§17 Option C). Emitted when startup replay QUARANTINES a\ncorrupt / unredacted event row — the LOUD, consumer-visible record that the audit history has\na gap (the core of why Option C beats a silent skip). Carries the affected `seq` + a\nredaction-safe **structural** `reason` — NEVER the corrupt payload or any row content (§15).",
      "properties": {
        "reason": {
          "type": "string"
        },
        "seq": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "seq",
        "reason"
      ],
      "type": "object"
    },
    "Capabilities": {
      "additionalProperties": false,
      "description": "`get_capabilities` / `HelloAck` capabilities (§6.4). Matches the ui's pinned `Capabilities`\n(`protocol_version` + `contract_version`).",
      "properties": {
        "contract_version": {
          "type": "string"
        },
        "protocol_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "protocol_version",
        "contract_version"
      ],
      "type": "object"
    },
    "DeltaKind": {
      "description": "The kind of projection change in a [`ProjectionDelta`] (§6.1). snake_case wire values.",
      "enum": [
        "upsert",
        "remove"
      ],
      "type": "string"
    },
    "DesktopObjectKind": {
      "description": "The 4 desktop-addendum object kinds (§5.3). Serialized snake_case.",
      "enum": [
        "local_runner",
        "event_projection",
        "device",
        "remote_client"
      ],
      "type": "string"
    },
    "DeviceId": {
      "description": "`dev_<ULID>` — the registered desktop host (§5.3 Device; trust-boundary anchor).\nRegister-if-absent: minted once, reused across restarts.",
      "type": "string"
    },
    "DeviceRegistered": {
      "additionalProperties": false,
      "description": "`DeviceRegistered` payload (§5.3 Device / §16 bootstrap). The device's `dev_` identity is\nNOT an envelope typed column, so it lives in the payload — the `object_refs` projector\nsources the edge from here (the payload is the identity home; rebuild-safe). Register-if-\nabsent: the stable desktop host registers once and is reused across daemon restarts.",
      "properties": {
        "device_id": {
          "$ref": "#/$defs/DeviceId"
        }
      },
      "required": [
        "device_id"
      ],
      "type": "object"
    },
    "EventEnvelope": {
      "additionalProperties": false,
      "description": "The §7.1 Event envelope. `seq` is the canonical total order (not `occurred_at`;\nclocks skew — both kept). Required fields are non-`Option`; the rest are optional\nper §7.1. `payload_hash`/`previous_event_hash` are reserved (R-10).\n`redaction_status` carries the §15 redaction state (the writer GATE enforces\n`redacted` before persist — `daemon::eventstore`).",
      "properties": {
        "action_request_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "actor_id": {
          "type": "string"
        },
        "actor_type": {
          "$ref": "#/$defs/ActorType"
        },
        "agent_team_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "approval_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "causation_id": {
          "description": "the immediate prior event_id (typed as the frozen newtype, per §2.1)",
          "type": [
            "string",
            "null"
          ]
        },
        "correlation_id": {
          "type": "string"
        },
        "event_id": {
          "type": "string"
        },
        "event_type": {
          "type": "string"
        },
        "event_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "idempotency_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "object_refs": {
          "description": "optional (§7.1) — omitted when empty, so the schema does not mark it required",
          "items": {
            "$ref": "#/$defs/ObjectRef"
          },
          "type": "array"
        },
        "occurred_at": {
          "type": "string"
        },
        "payload_hash": {
          "type": [
            "string",
            "null"
          ]
        },
        "payload_json": {
          "type": "string"
        },
        "previous_event_hash": {
          "type": [
            "string",
            "null"
          ]
        },
        "project_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "recorded_at": {
          "type": "string"
        },
        "redaction_engine_version": {
          "description": "which Redactor engine redacted the payload (provenance; e.g. `prefix-v1`;\ndistinguishes 1.1 prefix-redacted from 1.7 entropy-redacted events)",
          "type": [
            "string",
            "null"
          ]
        },
        "redaction_status": {
          "$ref": "#/$defs/RedactionStatus",
          "description": "§15 redaction-before-persist state (writer gate enforces `redacted`)"
        },
        "schema_version": {
          "type": "string"
        },
        "sensitivity": {
          "$ref": "#/$defs/Sensitivity"
        },
        "seq": {
          "format": "int64",
          "type": "integer"
        },
        "session_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "source_id": {
          "type": "string"
        },
        "source_type": {
          "$ref": "#/$defs/SourceType"
        },
        "visibility": {
          "anyOf": [
            {
              "$ref": "#/$defs/Visibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "workflow_run_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "workspace_id": {
          "type": "string"
        }
      },
      "required": [
        "event_id",
        "seq",
        "event_type",
        "event_version",
        "occurred_at",
        "recorded_at",
        "workspace_id",
        "actor_type",
        "actor_id",
        "source_type",
        "source_id",
        "correlation_id",
        "sensitivity",
        "redaction_status",
        "payload_json",
        "schema_version"
      ],
      "type": "object"
    },
    "EvidenceConfidence": {
      "description": "Evidence-link confidence (§6.2 EvidenceRef; AG §9.9).",
      "enum": [
        "exact",
        "likely",
        "loose",
        "unverified"
      ],
      "type": "string"
    },
    "EvidenceRef": {
      "additionalProperties": false,
      "description": "A reference to evidence backing an action/plan (§6.2 EvidenceRef; AG §9.9). `label` is the\nrequester-supplied natural-language evidence text (required; not reconstructable from\n`{type,id}` — the Brain/PR-review evidence surface depends on it, TWEAK 2026-06-11).",
      "properties": {
        "confidence": {
          "anyOf": [
            {
              "$ref": "#/$defs/EvidenceConfidence"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/EvidenceType"
        }
      },
      "required": [
        "type",
        "id",
        "label"
      ],
      "type": "object"
    },
    "EvidenceType": {
      "description": "The kind of evidence backing an action/plan (§6.2 EvidenceRef; AG §9.9's 11 values).",
      "enum": [
        "project_brain_evidence_item",
        "file_anchor",
        "architecture_anchor",
        "plan_task",
        "session_episode",
        "commit",
        "pr_check",
        "terminal_event",
        "ticket",
        "workflow_manifest",
        "user_instruction"
      ],
      "type": "string"
    },
    "GatewayObjectKind": {
      "description": "The 2 Gateway-addendum object kinds (§6.2). Serialized snake_case.",
      "enum": [
        "approval",
        "action_plan"
      ],
      "type": "string"
    },
    "GetProjectionParams": {
      "additionalProperties": false,
      "description": "`get_projection` params (§6.1). `page` (pagination) is provisional and omitted for MVP.\n\n**MVP NOTE:** `scope` is **accepted but NOT YET enforced** — the daemon returns the full\nprojection table regardless of `scope.project_id` (the local same-uid peer has no tenancy\nboundary in MVP). Do not build client filtering that assumes the daemon honors `scope`;\nscope filtering (and the `ProjectId` newtype on the field) land when multi-project scoping does.",
      "properties": {
        "name": {
          "$ref": "#/$defs/ProjectionName"
        },
        "scope": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectionScope"
            },
            {
              "type": "null"
            }
          ],
          "description": "accepted but not yet enforced (see the struct note) — the read is unscoped in MVP."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "HelloAck": {
      "additionalProperties": false,
      "description": "daemon→client handshake success (§6.4): the negotiated protocol version + the daemon's\nbuild version + its capabilities.",
      "properties": {
        "capabilities": {
          "$ref": "#/$defs/Capabilities"
        },
        "daemon_version": {
          "type": "string"
        },
        "protocol_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "protocol_version",
        "daemon_version",
        "capabilities"
      ],
      "type": "object"
    },
    "HelloFrame": {
      "additionalProperties": false,
      "description": "The first client→daemon frame (§6.4 handshake). No method is served until a successful\nhandshake. `deny_unknown_fields` rejects a non-Hello first frame (reject-unknown, §15).",
      "properties": {
        "app_version": {
          "type": "string"
        },
        "client_kind": {
          "type": "string"
        },
        "protocol_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "protocol_version",
        "client_kind",
        "app_version"
      ],
      "type": "object"
    },
    "IdKind": {
      "description": "The 22 shared ID kinds (§5.2). Serialized snake_case (== the ID field name).",
      "enum": [
        "workspace_id",
        "project_id",
        "repo_id",
        "worktree_id",
        "branch_name",
        "commit_sha",
        "session_id",
        "agent_team_id",
        "execution_profile_id",
        "workflow_pack_id",
        "workflow_instance_id",
        "workflow_command_id",
        "implementation_plan_id",
        "plan_task_id",
        "architecture_anchor",
        "linear_issue_id",
        "github_issue_number",
        "pr_number",
        "action_request_id",
        "event_id",
        "artifact_id",
        "evidence_item_id"
      ],
      "type": "string"
    },
    "IpcErrorCode": {
      "description": "The §6.4 structured wire error codes (closed set; reject-unknown). snake_case wire values.\n`protocol_error` (a bad-first-frame / handshake-required / malformed-frame violation, distinct\nfrom `unknown_method`) was added in 1.5 per the lead-ratified §6.4 gap resolution (Option B).",
      "enum": [
        "version_skew",
        "frame_too_large",
        "unknown_method",
        "unauthorized_peer",
        "policy_denied",
        "precondition_stale",
        "protocol_error"
      ],
      "type": "string"
    },
    "LocalRunnerId": {
      "description": "`lr_<ULID>` — a daemon execution surface (§5.3 LocalRunner), minted per daemon start.",
      "type": "string"
    },
    "LocalRunnerRegistered": {
      "additionalProperties": false,
      "description": "`LocalRunnerRegistered` payload (§5.3 LocalRunner / §16 bootstrap). Minted fresh per\ndaemon start; the `lr_` identity lives in the payload (not an envelope column) so the\n`object_refs` projector sources the edge from it (rebuild-safe).",
      "properties": {
        "local_runner_id": {
          "$ref": "#/$defs/LocalRunnerId"
        }
      },
      "required": [
        "local_runner_id"
      ],
      "type": "object"
    },
    "ObjectRef": {
      "additionalProperties": false,
      "description": "A normalized event→object edge (`object_refs[]`, §7.1 / DATA_MODEL §2.2).",
      "properties": {
        "object_id": {
          "type": "string"
        },
        "object_type": {
          "type": "string"
        }
      },
      "required": [
        "object_type",
        "object_id"
      ],
      "type": "object"
    },
    "PlanAck": {
      "additionalProperties": false,
      "description": "`submit_action_plan` result (§6.1, O-3) — the bundled-plan ack: the minted `plan_id` + a\nper-step ack (mirrors [`ActionAck`]) so the client can `preview_action`/track each step + hold\nthe plan handle. Richer fields (`approval_ids`/`overall_status`) are additive-later (2.1c L2,\nQ1 default). Reject-unknown; optionals (none today) would serialize as `null`.",
      "properties": {
        "plan_id": {
          "type": "string"
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/PlanStepAck"
          },
          "type": "array"
        }
      },
      "required": [
        "plan_id",
        "steps"
      ],
      "type": "object"
    },
    "PlanStepAck": {
      "additionalProperties": false,
      "description": "One step's ack inside a [`PlanAck`] (§6.1, O-3): the step's plan-local `step_id` + the minted\n`action_request_id` (so the client can `preview_action`/track it) + its current `status`.",
      "properties": {
        "action_request_id": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/ActionRequestStatus"
        },
        "step_id": {
          "type": "string"
        }
      },
      "required": [
        "step_id",
        "action_request_id",
        "status"
      ],
      "type": "object"
    },
    "PolicyDecision": {
      "additionalProperties": false,
      "description": "The policy engine's decision (§6.2 / AG §12.2). Frozen as the stable core `{status, reasons}`\n— `required_approvals` / `constraints` / `safer_alt` (which reference the unpinned\nApprovalRequirement/ApprovalConstraint shapes + the safer-alt union) are the **2.2** policy-\nengine deliverable, added additively. DEFERRED → 2.2.",
      "properties": {
        "reasons": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "status": {
          "$ref": "#/$defs/PolicyDecisionStatus"
        }
      },
      "required": [
        "status",
        "reasons"
      ],
      "type": "object"
    },
    "PolicyDecisionStatus": {
      "description": "The policy engine's decision (§6.2 / AG §12.2). The full decision payload\n(required_approvals / constraints / safer_alt) is the 2.2 policy-engine deliverable.",
      "enum": [
        "allow",
        "require_approval",
        "require_step_approval",
        "deny",
        "downgrade",
        "needs_more_context"
      ],
      "type": "string"
    },
    "ProjectBrain": {
      "description": "ProjectBrain (10) — daemon-cached last-reported sidecar status (§7.2).",
      "enum": [
        "not_configured",
        "indexing",
        "ready",
        "partial_index",
        "stale",
        "graph_degraded",
        "transcript_ingestion_off",
        "transcript_ingestion_active",
        "reindex_required",
        "error"
      ],
      "type": "string"
    },
    "ProjectionDelta": {
      "additionalProperties": false,
      "description": "A streamed projection delta (§6.1 subscribe) — matches the ui's `ProjectionDelta`: the changed\nprojection, the change kind, and the row (on upsert) or just the id (on remove).",
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/$defs/DeltaKind"
        },
        "projection": {
          "$ref": "#/$defs/ProjectionName",
          "description": "the changed projection (closed enum — reject-unknown both ways; serializes to the same\nPascalCase string the ui's `ProjectionDelta` parses, but the daemon can't push a typo)."
        },
        "row": true
      },
      "required": [
        "projection",
        "kind"
      ],
      "type": "object"
    },
    "ProjectionName": {
      "description": "The canonical closed projection-name enum (§6.1) — both daemon + ui reject-unknown. **Wire\nvalues are PascalCase** (the variant names verbatim — NO `rename_all`), matching the ui's\npinned `get_projection(\"Session\"|…)` literals + the §7 registry / R-5/R-6 architecture labels.\n`UsageLedger` is the architecture-canonical name (the ui's provisional `Usage` reconciles to it).",
      "enum": [
        "ProjectActivity",
        "Session",
        "ApprovalQueue",
        "Worktree",
        "PullRequest",
        "PlanProgress",
        "ProjectGraph",
        "AgentTeam",
        "AuditTrail",
        "UsageLedger"
      ],
      "type": "string"
    },
    "ProjectionScope": {
      "additionalProperties": false,
      "description": "`get_projection` scope filter (§6.1; provisional — widens as scoping lands).",
      "properties": {
        "project_id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PullRequest": {
      "description": "PullRequest (11) — GitHub-authoritative cache (§7.2).",
      "enum": [
        "draft",
        "open",
        "checks_pending",
        "checks_failing",
        "needs_review",
        "changes_requested",
        "approved",
        "mergeable",
        "conflict",
        "merged",
        "closed"
      ],
      "type": "string"
    },
    "RedactionStatus": {
      "description": "§15 redaction state. The single-writer GATE refuses to persist any event\nthat is not `redacted`. A quarantined (unredactable) event is diverted —\nnot persisted — and a `SensitiveOutputRedacted` event recorded instead, so\nonly these 2 values ever reach the `events` table.",
      "enum": [
        "unredacted",
        "redacted"
      ],
      "type": "string"
    },
    "RequesterType": {
      "description": "Request-time requester identity (§6.2 / R-2). The AG §9.7 `ActorRef` variants +\n`remote_client` (§6). Mapped to the audit [`ActorType`] via [`RequesterType::to_actor_type`].",
      "enum": [
        "user",
        "project_brain",
        "agent_session",
        "workflow_pack",
        "system_policy",
        "remote_client"
      ],
      "type": "string"
    },
    "RequiredApprover": {
      "additionalProperties": false,
      "description": "Who must approve (§6.2 Approval; AG §9.5). `kind` selects the approver class; `actor` carries\nthe specific actor IFF `kind == actor` (the Gateway enforces that pairing at 2.1b). Modeled as\na `{kind, actor?}` object (not a data-carrying enum) so the wire stays uniform + 3-way\nverifiable (the §5.0 cross-language equality gate).",
      "properties": {
        "actor": {
          "anyOf": [
            {
              "$ref": "#/$defs/ActorRefBody"
            },
            {
              "type": "null"
            }
          ]
        },
        "kind": {
          "$ref": "#/$defs/RequiredApproverKind"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "RequiredApproverKind": {
      "description": "Which class of approver a §6.2 [`Approval`] requires (AG §9.5 `ActorRef | 'current_user' |\n'project_owner'`). A flat closed enum → a clean top-level schema `enum`, reflected\nuniformly by the §5.0 Zod/Pydantic generators (a data-carrying tagged enum's *inline*\nvalue-set is lifted inconsistently across the 3-way verify — so the discriminant is a\nnamed enum, and the specific actor lives in [`RequiredApprover::actor`]).",
      "enum": [
        "current_user",
        "project_owner",
        "actor"
      ],
      "type": "string"
    },
    "ResourceRef": {
      "additionalProperties": false,
      "description": "A reference to a resource an action reads/mutates (§6.2 ResourceRef; AG §9.8). The leaner\nbinding set `{type, id, uri?}` — AG's `displayName?` is resolvable from `id` (DEFERRED;\nadditive-later if a consumer needs it).",
      "properties": {
        "id": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/ResourceType"
        },
        "uri": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "type",
        "id"
      ],
      "type": "object"
    },
    "ResourceType": {
      "description": "The kind of resource an action touches (§6.2 ResourceRef; AG §9.8's 20 values VERBATIM).\n(§6.2/Appendix-A say \"21 types\"; the 20-vs-21 gap is a tracked cross-doc note — the 21st\nis NOT invented here.)",
      "enum": [
        "project",
        "repo",
        "worktree",
        "branch",
        "file",
        "diff",
        "session",
        "agent_team",
        "plan_task",
        "implementation_plan",
        "workflow_pack",
        "workflow_instance",
        "workflow_command",
        "linear_issue",
        "github_issue",
        "pull_request",
        "execution_profile",
        "memory_source",
        "decision",
        "artifact"
      ],
      "type": "string"
    },
    "RiskLevel": {
      "maximum": 4,
      "minimum": 0,
      "type": "integer"
    },
    "RpcRequest": {
      "additionalProperties": false,
      "description": "A JSON-RPC-style method request (§6.1). `id` correlates the response; `params` is the\nmethod-specific argument object (the daemon validates it per-method).",
      "properties": {
        "id": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "method": {
          "type": "string"
        },
        "params": {
          "default": null
        }
      },
      "required": [
        "method",
        "id"
      ],
      "type": "object"
    },
    "RpcResponse": {
      "additionalProperties": false,
      "description": "A JSON-RPC-style response (§6.1), correlated by `id`: exactly one of `result` / `error`.",
      "properties": {
        "error": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireError"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "result": true
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "SensitiveOutputRedacted": {
      "additionalProperties": false,
      "description": "`SensitiveOutputRedacted` payload (§15 redaction-before-persist). Emitted when the Redactor\ndetects a high-confidence secret it CANNOT safely redact in place — the original event is\n**diverted (NOT persisted)** and this record is appended in its place (`event_envelope.rs`\nRedactionStatus doc). Carries forensic metadata ONLY: the diverted event's `event_type`, a\nredaction-safe **structural** `reason`, and the `detector` that fired — NEVER the secret or\nany byte of the diverted payload (§15; mirrors the 1.6c AuditIntegrityViolation content-free\nrecord). In MVP the prefix+entropy Redactor masks in place and never triggers this; it is\nthe §15 \"can't safely redact → divert\" safety net for an alternate/future Redactor.",
      "properties": {
        "detector": {
          "type": "string"
        },
        "original_event_type": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "original_event_type",
        "reason",
        "detector"
      ],
      "type": "object"
    },
    "Sensitivity": {
      "description": "§7.1/§15 sensitivity classification (EM §9). Terminal output defaults `restricted`.",
      "enum": [
        "public",
        "internal",
        "confidential",
        "secret",
        "restricted"
      ],
      "type": "string"
    },
    "ServerFrame": {
      "description": "The server→client **multiplexed frame envelope** (§6.4 frame-type tag). The internally-tagged\n`frame_type` discriminant lets the client demultiplex one connection: an RPC response vs a\nsubscription push. The **Terminal-Channel tag space is RESERVED** — raw PTY frames are a\nPhase-3 decision (JSON-base64 vs a binary fast-path, made with throughput data); no variant yet.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "A JSON-RPC-style response (§6.1), correlated by `id`: exactly one of `result` / `error`.",
          "properties": {
            "error": {
              "anyOf": [
                {
                  "$ref": "#/$defs/WireError"
                },
                {
                  "type": "null"
                }
              ]
            },
            "frame_type": {
              "const": "rpc_response",
              "type": "string"
            },
            "id": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "result": true
          },
          "required": [
            "frame_type",
            "id"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A streamed projection delta (§6.1 subscribe) — matches the ui's `ProjectionDelta`: the changed\nprojection, the change kind, and the row (on upsert) or just the id (on remove).",
          "properties": {
            "frame_type": {
              "const": "subscription_push",
              "type": "string"
            },
            "id": {
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "$ref": "#/$defs/DeltaKind"
            },
            "projection": {
              "$ref": "#/$defs/ProjectionName",
              "description": "the changed projection (closed enum — reject-unknown both ways; serializes to the same\nPascalCase string the ui's `ProjectionDelta` parses, but the daemon can't push a typo)."
            },
            "row": true
          },
          "required": [
            "frame_type",
            "projection",
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "Session": {
      "description": "Session lifecycle (17, R-4) — the driver-agnostic normalized vocabulary the\n§9.1 harness adapters map INTO. `stale` is time-derived (§5.1).",
      "enum": [
        "creating",
        "starting",
        "active",
        "thinking",
        "running_command",
        "editing_files",
        "running_tests",
        "waiting_on_permission",
        "waiting_on_human_input",
        "waiting_on_external_service",
        "idle",
        "stale",
        "changes_ready",
        "failed",
        "completed",
        "archived",
        "killed"
      ],
      "type": "string"
    },
    "SessionStarted": {
      "additionalProperties": false,
      "description": "`SessionStarted` payload. The session's **identity** (`session_id`/`project_id`)\nlives on the [`crate::event_envelope::EventEnvelope`] typed fields (so projectors\nderive `object_refs` + read-models from columns that survive a rebuild); this\npayload carries the type-specific session attributes the `proj_session` projector\nfolds. `status` binds to the frozen §5.1 [`Session`] machine — an unknown wire\nvalue fails closed at the parse boundary (reject-unknown, §15).",
      "properties": {
        "display_name": {
          "type": [
            "string",
            "null"
          ]
        },
        "harness": {
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/Session"
        }
      },
      "required": [
        "status"
      ],
      "type": "object"
    },
    "SourceType": {
      "description": "EM §8 source model — the subsystem that emitted the event (CLOSED MVP set, 15).",
      "enum": [
        "desktop_ui",
        "local_daemon",
        "action_gateway",
        "terminal_supervisor",
        "claude_code_adapter",
        "codex_adapter",
        "git_executor",
        "github_syncer",
        "linear_syncer",
        "workflow_pack_runtime",
        "project_brain_indexer",
        "project_brain_retriever",
        "project_brain_action_planner",
        "usage_meter",
        "remote_relay"
      ],
      "type": "string"
    },
    "SubscribeParams": {
      "additionalProperties": false,
      "description": "`subscribe` params (§6.1). `filter` is provisional (a per-projection scope; widens later).",
      "properties": {
        "filter": true,
        "projection": {
          "$ref": "#/$defs/ProjectionName"
        }
      },
      "required": [
        "projection"
      ],
      "type": "object"
    },
    "Task": {
      "description": "Task / PlanTask superset (17, R-8).",
      "enum": [
        "unassigned",
        "queued",
        "assigned",
        "ready",
        "in_progress",
        "blocked",
        "needs_clarification",
        "in_review",
        "changes_ready",
        "pr_opened",
        "needs_review",
        "requested_changes",
        "done",
        "deferred",
        "merged",
        "closed",
        "abandoned"
      ],
      "type": "string"
    },
    "Timestamp": {
      "format": "date-time",
      "type": "string"
    },
    "VersionSkewError": {
      "additionalProperties": false,
      "description": "daemon→client handshake failure on version skew (§6.4): the daemon's supported range + the\nclient's offered version, so the ui can render \"update required\" (its `update_required` path).",
      "properties": {
        "client_protocol_version": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "supported_max": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "supported_min": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "supported_min",
        "supported_max",
        "client_protocol_version"
      ],
      "type": "object"
    },
    "Visibility": {
      "description": "§7.1 visibility scope.",
      "enum": [
        "user",
        "project",
        "workspace",
        "system"
      ],
      "type": "string"
    },
    "WireError": {
      "additionalProperties": false,
      "description": "A structured daemon→client error frame (§6.4). Carries one of the closed [`IpcErrorCode`]s.",
      "properties": {
        "code": {
          "$ref": "#/$defs/IpcErrorCode"
        }
      },
      "required": [
        "code"
      ],
      "type": "object"
    },
    "WorkflowInstance": {
      "description": "WorkflowInstance (12, R-7).",
      "enum": [
        "not_detected",
        "pack_available",
        "needs_personalization",
        "personalization_in_progress",
        "generated_review_required",
        "active",
        "ready_for_team_run",
        "degraded",
        "drift_detected",
        "upgrade_available",
        "archived",
        "detached"
      ],
      "type": "string"
    },
    "WorktreeGit": {
      "description": "Worktree git-sync axis (R-7). No terminal states (the overlay axis carries\nthe terminal `deleted`).",
      "enum": [
        "clean",
        "dirty",
        "untracked_files",
        "conflicts",
        "behind_base",
        "ahead_of_base"
      ],
      "type": "string"
    },
    "WorktreeOverlay": {
      "description": "Worktree lifecycle-overlay axis (R-7).",
      "enum": [
        "creating",
        "locked",
        "pr_open",
        "merged",
        "prunable",
        "deleted"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Bundles every frozen contract type so one `schema_for!` captures all value\nsets under `$defs`. Never instantiated — it exists only to drive schema gen.",
  "properties": {
    "action_ack": {
      "$ref": "#/$defs/ActionAck"
    },
    "action_approval_requested": {
      "$ref": "#/$defs/ActionApprovalRequested"
    },
    "action_approved": {
      "$ref": "#/$defs/ActionApproved"
    },
    "action_denied": {
      "$ref": "#/$defs/ActionDenied"
    },
    "action_dependency": {
      "$ref": "#/$defs/ActionDependency"
    },
    "action_expired": {
      "$ref": "#/$defs/ActionExpired"
    },
    "action_failed": {
      "$ref": "#/$defs/ActionFailed"
    },
    "action_plan": {
      "$ref": "#/$defs/ActionPlan"
    },
    "action_plan_id": {
      "$ref": "#/$defs/ActionPlanId"
    },
    "action_plan_step": {
      "$ref": "#/$defs/ActionPlanStep"
    },
    "action_preview": {
      "$ref": "#/$defs/ActionPreview"
    },
    "action_request": {
      "$ref": "#/$defs/ActionRequestStatus"
    },
    "action_request_model": {
      "$ref": "#/$defs/ActionRequest"
    },
    "action_requested": {
      "$ref": "#/$defs/ActionRequested"
    },
    "action_result": {
      "$ref": "#/$defs/ActionResult"
    },
    "action_result_status": {
      "$ref": "#/$defs/ActionResultStatus"
    },
    "action_started": {
      "$ref": "#/$defs/ActionStarted"
    },
    "action_succeeded": {
      "$ref": "#/$defs/ActionSucceeded"
    },
    "actor_ref_body": {
      "$ref": "#/$defs/ActorRefBody"
    },
    "actor_type": {
      "$ref": "#/$defs/ActorType"
    },
    "agent_team": {
      "$ref": "#/$defs/AgentTeam"
    },
    "approval": {
      "$ref": "#/$defs/ApprovalStatus"
    },
    "approval_id": {
      "$ref": "#/$defs/ApprovalId"
    },
    "approval_mode": {
      "$ref": "#/$defs/ApprovalMode"
    },
    "approval_model": {
      "$ref": "#/$defs/Approval"
    },
    "approval_scope": {
      "$ref": "#/$defs/ApprovalScope"
    },
    "audit_integrity_violation": {
      "$ref": "#/$defs/AuditIntegrityViolation"
    },
    "capabilities": {
      "$ref": "#/$defs/Capabilities"
    },
    "delta_kind": {
      "$ref": "#/$defs/DeltaKind"
    },
    "desktop_object_kind": {
      "$ref": "#/$defs/DesktopObjectKind"
    },
    "device_id": {
      "$ref": "#/$defs/DeviceId"
    },
    "device_registered": {
      "$ref": "#/$defs/DeviceRegistered"
    },
    "event_envelope": {
      "$ref": "#/$defs/EventEnvelope"
    },
    "evidence_confidence": {
      "$ref": "#/$defs/EvidenceConfidence"
    },
    "evidence_ref": {
      "$ref": "#/$defs/EvidenceRef"
    },
    "evidence_type": {
      "$ref": "#/$defs/EvidenceType"
    },
    "gateway_object_kind": {
      "$ref": "#/$defs/GatewayObjectKind"
    },
    "get_projection_params": {
      "$ref": "#/$defs/GetProjectionParams"
    },
    "hello_ack": {
      "$ref": "#/$defs/HelloAck"
    },
    "hello_frame": {
      "$ref": "#/$defs/HelloFrame"
    },
    "id_kind": {
      "$ref": "#/$defs/IdKind"
    },
    "ipc_error_code": {
      "$ref": "#/$defs/IpcErrorCode"
    },
    "local_runner_id": {
      "$ref": "#/$defs/LocalRunnerId"
    },
    "local_runner_registered": {
      "$ref": "#/$defs/LocalRunnerRegistered"
    },
    "plan_ack": {
      "$ref": "#/$defs/PlanAck"
    },
    "plan_step_ack": {
      "$ref": "#/$defs/PlanStepAck"
    },
    "policy_decision": {
      "$ref": "#/$defs/PolicyDecision"
    },
    "policy_decision_status": {
      "$ref": "#/$defs/PolicyDecisionStatus"
    },
    "project_brain": {
      "$ref": "#/$defs/ProjectBrain"
    },
    "projection_delta": {
      "$ref": "#/$defs/ProjectionDelta"
    },
    "projection_name": {
      "$ref": "#/$defs/ProjectionName"
    },
    "projection_scope": {
      "$ref": "#/$defs/ProjectionScope"
    },
    "pull_request": {
      "$ref": "#/$defs/PullRequest"
    },
    "redaction_status": {
      "$ref": "#/$defs/RedactionStatus"
    },
    "requester_type": {
      "$ref": "#/$defs/RequesterType"
    },
    "required_approver": {
      "$ref": "#/$defs/RequiredApprover"
    },
    "required_approver_kind": {
      "$ref": "#/$defs/RequiredApproverKind"
    },
    "resource_ref": {
      "$ref": "#/$defs/ResourceRef"
    },
    "resource_type": {
      "$ref": "#/$defs/ResourceType"
    },
    "risk_level": {
      "$ref": "#/$defs/RiskLevel"
    },
    "rpc_request": {
      "$ref": "#/$defs/RpcRequest"
    },
    "rpc_response": {
      "$ref": "#/$defs/RpcResponse"
    },
    "sensitive_output_redacted": {
      "$ref": "#/$defs/SensitiveOutputRedacted"
    },
    "sensitivity": {
      "$ref": "#/$defs/Sensitivity"
    },
    "server_frame": {
      "$ref": "#/$defs/ServerFrame"
    },
    "session": {
      "$ref": "#/$defs/Session"
    },
    "session_started": {
      "$ref": "#/$defs/SessionStarted"
    },
    "source_type": {
      "$ref": "#/$defs/SourceType"
    },
    "subscribe_params": {
      "$ref": "#/$defs/SubscribeParams"
    },
    "task": {
      "$ref": "#/$defs/Task"
    },
    "timestamp": {
      "$ref": "#/$defs/Timestamp"
    },
    "version_skew_error": {
      "$ref": "#/$defs/VersionSkewError"
    },
    "visibility": {
      "$ref": "#/$defs/Visibility"
    },
    "wire_error": {
      "$ref": "#/$defs/WireError"
    },
    "workflow_instance": {
      "$ref": "#/$defs/WorkflowInstance"
    },
    "worktree_git": {
      "$ref": "#/$defs/WorktreeGit"
    },
    "worktree_overlay": {
      "$ref": "#/$defs/WorktreeOverlay"
    }
  },
  "required": [
    "session",
    "task",
    "worktree_git",
    "worktree_overlay",
    "pull_request",
    "workflow_instance",
    "project_brain",
    "approval",
    "action_request",
    "agent_team",
    "actor_type",
    "id_kind",
    "desktop_object_kind",
    "event_envelope",
    "source_type",
    "sensitivity",
    "visibility",
    "redaction_status",
    "session_started",
    "device_registered",
    "local_runner_registered",
    "device_id",
    "local_runner_id",
    "audit_integrity_violation",
    "sensitive_output_redacted",
    "hello_frame",
    "hello_ack",
    "version_skew_error",
    "capabilities",
    "ipc_error_code",
    "wire_error",
    "projection_name",
    "rpc_request",
    "rpc_response",
    "get_projection_params",
    "projection_scope",
    "server_frame",
    "projection_delta",
    "delta_kind",
    "subscribe_params",
    "action_request_model",
    "action_plan",
    "action_plan_step",
    "action_dependency",
    "action_preview",
    "approval_model",
    "action_result",
    "resource_ref",
    "evidence_ref",
    "policy_decision",
    "required_approver",
    "required_approver_kind",
    "actor_ref_body",
    "requester_type",
    "risk_level",
    "approval_scope",
    "approval_mode",
    "policy_decision_status",
    "action_result_status",
    "resource_type",
    "evidence_type",
    "evidence_confidence",
    "gateway_object_kind",
    "approval_id",
    "action_plan_id",
    "timestamp",
    "action_requested",
    "action_approval_requested",
    "action_approved",
    "action_denied",
    "action_expired",
    "action_started",
    "action_succeeded",
    "action_failed",
    "action_ack",
    "plan_ack",
    "plan_step_ack"
  ],
  "title": "nexusops-contract",
  "type": "object",
  "x-contract-version": "0.17.0"
}
