# LESSONS.md — NexusOps (the Tauri desktop UI (TS frontend + thin Rust host))

> Full prose for every lesson logged during work in `ui/`. The compact index lives in `ui/CLAUDE.md` "Lessons logged" table.
>
> **Lesson numbers are stable IDs.** New lessons get the next sequential number. Numbers may be referenced from code comments, commit messages, and cross-references between lessons. **Don't reorder; don't reuse a deleted number's slot.**
>
> **Lessons start at §1.** Each code area has its own lesson sequence — lessons don't carry across code areas.

---

## Lesson format

```markdown
## <a id="N"></a>N. <Short topic> — <one-line rule>

**Date:** YYYY-MM-DD.
**Source slice:** <slice-id or commit hash>.

<2-5 paragraphs explaining: what was discovered, why it matters, how to
apply the rule, what edge cases are still open. Cite file:line references
where applicable.>

**Rule:** <one-sentence summary, same as the heading subtitle>.
```

---

## <a id="1"></a>1. UI contract enums are generated, never hand-declared — checked-in artifact + drift test + version pin

**Date:** 2026-06-07.
**Source slice:** P6.1a (`docs/briefs/003-P6-1a-ui-foundation-contract-layer.md`).

The cross-language contract authority is the Rust `shared` crate; it emits a versioned, CI-diff-gated JSON Schema (`shared/contracts/schema/nexusops-contract.schema.json`, `ARCHITECTURE.md §5.0` Option A). The UI is a **generated, drift-caught consumer** of that schema — it never re-declares status strings, ID kinds, or actor values by hand. `ui/scripts/gen-contracts.mjs` regenerates `ui/src/contracts/generated.ts` (a machine artifact — never hand-edited) from the frozen schema, read-only; `ui/src/contracts/index.ts` derives the individually-exported validators (`Session`, `Task`, …) from the generated bundle.

Three pins keep the generated layer honest and make hand-drift impossible: (1) **accept-all** — every canonical value of all 13 frozen value-sets parses; (2) **reject-unknown** — closed `z.enum`s reject any non-canonical value, preserving the `§5.0` pt4 / `§15`–`§17` fail-closed posture end-to-end; (3) **drift gate** — a test loads the frozen JSON at test time and asserts the exported validator *set* + each `.options` set === the schema `$defs[*].enum` arrays (the TS mirror of the Rust CI schema-diff gate, no `cargo` needed). A fourth pin couples versions: an exported `CONTRACT_VERSION` is asserted === the schema's `x-contract-version`, so the literal `"0.5.0"` used as a handshake tripwire elsewhere can never silently drift.

Apply this whenever a UI surface needs a contract value: import the generated validator, never type a status/ID/actor string union by hand. A status field on any view/projection type delegates to the generated enum. If the value you need isn't in the frozen schema, it isn't frozen yet — see [[2]].

**Rule:** UI contract enums are generated from the frozen `shared/` schema (checked-in artifact + accept/reject/drift/version pins), never hand-declared; status/ID/actor fields delegate to the generated validators.

## <a id="2"></a>2. Unfrozen object shapes are provisional UI-local types — enum fields delegated, reconcile on next contract bump

**Date:** 2026-06-07.
**Source slice:** P6.1a (`docs/briefs/003-P6-1a-ui-foundation-contract-layer.md`).

The 0.5.0 freeze is **enum-only** (9 status machines + `ActorType` + `IdKind` + `DesktopObjectKind`). The object models the UI must render against — projection-row shapes, `GatewayPort` params/results, `ActionRequest`/`ActionPlan` objects — live as `ARCHITECTURE.md` Appendix A *prose*, not yet a generated artifact (the daemon will freeze them as it builds Phase 1/2). The parallel-track plan (`MVP_TASKS.md §parallelization`; `§14` mandates a mock GatewayPort for UI tests) explicitly sanctions building the UI ahead of those frozen object schemas.

The resolution: hand-author **minimal provisional Zod/TS shapes** in `ui/src/contracts/provisional.ts`, each carrying a `// PROVISIONAL — not frozen; reconciles when the daemon freezes object schemas` banner, with **every enum-typed field delegating to the generated validators** from [[1]] (never a re-declared status union). This keeps the only hand-authored surface the *structure*, while the safety-load-bearing *values* stay generated + drift-pinned.

Posture note (object-KEY strictness): the boundary validator rejects unknown enum VALUES (fail-closed, [[1]]), but provisional object shapes are **tolerant of unknown object keys** on read projections (forward-compat / must-ignore-unknown) — `§15`'s reject-unknown invariant is about enum values, which are enforced; strict object-key rejection belongs on intent-submission params (mutation side), not read projections. Harden when object schemas freeze (carry-forward reconcile). When that bump lands, replace the provisional shapes with generated ones and delete the banner — track every provisional type so none survives the reconcile.

**Rule:** object shapes not yet in the frozen contract are provisional UI-local types (clearly banner-marked, enum fields delegated to the generated layer, tolerant of unknown keys on reads); reconcile to generated shapes on the next contract bump.

## <a id="3"></a>3. NexusOps-ui-kit consumption — tokens via styles.css, components via the `@ui-kit` source alias (not the global bundle)

**Date:** 2026-06-07.
**Source slice:** P6.1b (`docs/briefs/004-P6-1b-app-shell-and-kit-integration.md`).

`NexusOps-ui-kit` (the canonical design system, `§11.1` / O-5) ships three delivery forms: a token `styles.css` (`@import` manifest of `tokens/*.css`), per-component `.jsx` sources + `.d.ts` typings under `components/<group>/`, and a runtime bundle on a global namespace (`window.ControlPlaneDesignSystem_a21911`). For a React 19 + Vite + TS-strict app the chosen mechanism is: **(1)** import the token `styles.css` in `main.tsx` (CSS custom properties; product code references the **semantic** token layer only, never primitives); **(2)** import the component `.jsx` sources via a Vite **`@ui-kit` alias** → `../NexusOps-ui-kit/components`, surfaced through one re-export module (`ui/src/design-system/kit.ts`). The runtime global bundle is **not** used (it fights TS-strict ES modules); the kit is **not** vendored/copied (that would diverge from the canonical source and break the "a re-hue touches primitives only" promise).

The one non-obvious snag: the kit `.jsx` components `import React from 'react'` as a peer, but they live **out of `ui/`'s root** where there is no `node_modules` — so Vite couldn't resolve `react`/`react-dom` for them. The fix is `resolve.dedupe: ["react", "react-dom"]` in `vite.config.ts`, which forces those (and the injected `jsx-runtime`s, matched by package name) to resolve from *this* project's root — giving one React instance for app + kit. Supporting config: `server.fs.allow` must include the sibling kit dir; `tsconfig` `paths` mirror the alias; `skipLibCheck` accommodates the kit's hand-authored `.d.ts` (it skips declaration-file checking only — app-code strictness is unaffected); drop the deprecated `baseUrl` (TS6) — `paths` work under `moduleResolution: bundler`. Verified end-to-end by a scratch render + a full `vite build` (kit token CSS bundled). Some kit component contracts are still thin (e.g. `Button.d.ts` lacks `aria-label`) — fold those as they bite (see the 6.4 a11y task).

**Rule:** consume the kit as tokens-via-`styles.css` + components-via-the-`@ui-kit`-source-alias (with `resolve.dedupe(["react","react-dom"])` for the out-of-root peer react), never the global bundle and never vendored; reference the semantic token layer only.

## <a id="4"></a>4. The UI read-only/degraded gate is fail-safe + defense-in-depth — never the sole mutation guard

**Date:** 2026-06-07.
**Source slice:** P6.1c (`docs/briefs/005-P6-1c-connection-readonly-version-skew.md`).

The UI disables every intent-submitting control when the daemon is unreachable or version-skewed (`§11.4` global READ-ONLY degraded mode). Two properties make this safe rather than theater. **(1) Fail-safe by construction:** `canSubmitIntent = connected && version === compatible` (`ui/src/connection/read-only.ts`) — it is a *positive* confirmation, so every unknown/initial state falls out as FALSE, including the subtle `{connected, version=unknown}` pre-handshake window. The UI never offers a mutation until it has positively confirmed the daemon is reachable AND version-compatible; there is no "default to enabled." The `ReadOnlyProvider` with no provider also returns FALSE. **(2) Precedence:** `update_required` (version skew) outranks `disconnected`/`reconnecting` — a version mismatch is not Retry-able (`§16` is update/relaunch), so the degraded derivation checks version first and the banner shows a distinct update-required variant (no misleading transport-Retry).

Crucially this gate is **defense-in-depth, NOT the load-bearing guard**. The real INV-SEC-1 enforcement (no mutation bypasses the Action Gateway) is **daemon-side** (`§15`); the daemon rejects mutations regardless of UI state. The UI gate exists so the cockpit doesn't *offer* an action it knows can't be honored — a UX-integrity property, not the security boundary. Never let UI-side gating substitute for the daemon enforcement, and never weaken the fail-safe default for convenience. Enforced by `ui/CLAUDE.md` forbidden-pattern #6: every intent control consults `canSubmitIntent`. (Connection-state lives on the `gateway-client` transport seam — `getConnectionState`/`onConnectionChange`/`reconnect` — which are UI-client transport concerns, not the frozen `§6.1` RPC surface.)

**Rule:** the UI read-only gate is fail-safe (`canSubmitIntent` FALSE on unknown; true only when confirmed connected + version-compatible) and defense-in-depth — never the sole mutation guard; the daemon Gateway (INV-SEC-1) is.

## <a id="5"></a>5. Status→attention-rank is a (machine,status)-keyed table, drift-pinned to the generated enums, with no fall-through to idle

**Date:** 2026-06-07.
**Source slice:** P6.2a (`docs/briefs/006-P6-2a-status-model-attention-rank.md`).

`§11.3` requires "one canonical `status → attention-rank` table (covering all machines), the single source for sidebar weight, queue membership, and sort order — no silent fall-through to idle." Three properties make the UI implementation (`ui/src/status/descriptors.ts` + `attention.ts`) correct. **(1) Keyed by `(machine, status)`, not status alone** — status strings aren't globally unique (`active` is in Session/AgentTeam/WorkflowInstance; `failed`/`completed`/`archived` are shared), so a flat status→rank map would collide. **(2) Completeness drift-pinned to the generated enums** — a test iterates every frozen enum's `.options` (the [[1]] generated layer is the source of truth) and asserts a real descriptor exists for each `(machine, status)`; a daemon-added status fails the test until the table covers it. This is the same drift-gate philosophy as the contract layer, applied to render policy. **(3) No fall-through to idle** — the known kit bug (`waiting_on_permission`/`conflict`/`stale`/`blocked` flooring to rank 0 and never entering "Needs my attention") is pinned fixed: those map to 4/4/3/4, `changes_ready`=4 (review-needed), `waiting_on_human_input`/`waiting_on_human`=5; an out-of-table status resolves to a *visible* "unknown" descriptor (non-zero rank), never a silent idle.

The attention-rank lives in `ui/src/status/`, **not** `shared/` — it is **UI render policy** (how the cockpit prioritizes display: sidebar weight, the needs-attention/working/settled triage buckets, the `compareByAttention` sort), not a cross-language contract the daemon/Brain consume. If the notifier (`§10`) ever needs a canonical attention-rank it would be promoted to a shared contract via the daemon track — the UI track never writes the frozen `shared/` crate. Worktree status is **derived** from its two frozen axes (git + overlay) via a precedence function (`§5.1`/`§7.2`), not a stored enum.

**Rule:** the status→attention-rank table is `(machine,status)`-keyed, completeness-drift-pinned to the generated enums, the single source for sidebar/queue/sort, with no fall-through to idle; it is UI render policy in `ui/src/status/`, never the frozen `shared/` crate.

## <a id="6"></a>6. Status rendering = thin descriptor-bound wrappers over the kit; guard the kit's silent-idle fallback; kit props are closed

**Date:** 2026-06-07.
**Source slice:** P6.2b (`docs/briefs/007-P6-2b-status-rendering-binding.md`).

`StatusPill`/`AttentionMarker` (`ui/src/status/*.tsx`) are **thin wrappers** over the kit components, bound to the [[5]] descriptor table: `StatusPill(machine, status)` looks up `describeStatus` and renders the kit pill with the descriptor's `visualKind` + humanized `label`; the marker binds `attentionRank` → kit `level`. **Never re-declare a status's visual/label/rank at a call site** — the descriptor is the single source.

Two non-obvious guards. **(1) The kit's silent-idle trap:** the kit `StatusPill` does `STATUS[status] || STATUS.idle`, so an unrecognized kind renders **silently as idle** — exactly the `§11.3` fall-through the project forbids. The wrapper maps the descriptor's `"unknown"` fallback to a **visible `"degraded"`** kit kind, so an unknown status is always visible (pinned by a render test, not only the 6.2a model test). **(2) Drift-free kind validation:** the four-channel coverage test derives `KIT_KINDS` from the kit's **own exported `STATUS` map** (not a hand-maintained list), so the "every descriptor visualKind is a valid kit kind" guard can't rot. (Verified: all 19 descriptor visualKinds are valid kit `StatusKind`s — no `§11.7` mapping gap.)

**Kit components have CLOSED prop types** — no `HTMLAttributes`/`aria-*`/`data-*` passthrough (true for `Button`, `StatusPill`, `AttentionMarker`). So `data-*`, the R-5 surface chip ("Approval"/"Action"), and the `display:contents` marker wrapper all live on **NexusOps wrapper elements**, never on the kit component. Tracked as a `§11.7` component-contract item for the 6.4 a11y pass (HTMLAttributes passthrough) — until then, the wrapper pattern is the workaround.

**Refinement (P6.4e, 2026-06-08) — naming is NOT a wrapper job.** A wrapper carries `data-*` + *decorative* `aria-*` fine, but a wrapper `aria-label` does **NOT** name the inner kit control: an element's accessible **name** comes from its own content/aria, never an ancestor's `aria-label`. To NAME a closed-prop kit control (e.g. an icon-only `Button` with no `aria-label` prop), put a **visually-hidden child label *inside* it** — `<Button><span aria-hidden>glyph</span><span style={srOnly}>Name</span></Button>` → accessible name "Name"; verify with `getByRole({name})`. (My P6.4e brief said "aria-label on the wrapper" — wrong for naming; this is the correction.)

**Rule:** render status via thin descriptor-bound wrappers over the kit (single source = the descriptor); guard the kit's `||idle` silent fallback (unknown→visible); derive kit-kind validation from the kit's own `STATUS` map; route `data-*`/decorative `aria-*` onto wrapper elements (kit props are closed) — but the accessible **NAME** of a closed-prop kit control must come from a **visually-hidden child inside it**, never a wrapper `aria-label`.

## <a id="7"></a>7. Multi-commit ui slices: the implementer idles after each layer commit — the orchestrator drives layer→layer

**Date:** 2026-06-07.
**Source slice:** ui-track Phase 6 round 1 (operational note; carried into round 2).

A ui `/tdd` slice that spans multiple commits (e.g. a refactor/extraction layer followed by the feature layer) does **not** run end-to-end autonomously on the implementer side: after each layer's Step-10 commit the implementer's turn ends and it goes **idle**, waiting for the next instruction. It does not self-advance to the next layer of the same brief.

So the orchestrator must **drive the slice layer-by-layer**. When the implementer reports a layer's commit (a `TaskUpdate` + one-line wake), the orchestrator immediately wakes it onto the next layer of the *same* brief (a terse "proceed to layer 2: <what>" via `SendMessage`) rather than treating the layer commit as the whole slice landing and moving on. Never let a multi-commit slice sit idle mid-slice between its layers. Slice atomicity (root `CLAUDE.md`) still holds — the *current layer* always finishes through its Step-10 commit; the driving happens at the clean commit boundary *between* layers, never as a mid-layer interrupt.

Practical consequence for brief authoring: when a brief estimates ≥2 commits, **enumerate the layers explicitly** (layer 1 = …, layer 2 = …) in the "Estimated commit count" section, so both sides know the layer boundaries up front and the orchestrator knows how many layer→layer wakes to expect.

**Rule:** for multi-commit ui slices the implementer idles after each layer commit; the orchestrator drives it layer→layer (one wake per layer, at the commit boundary), and briefs estimating ≥2 commits enumerate the layers explicitly.

## <a id="8"></a>8. Projection→item mappers are the single source for the `ProjectionItem` shape; `data-item-id` is namespaced `<namespace>:<id>` on every emitter

**Date:** 2026-06-07.
**Source slice:** P6.3b Layer 1 (`docs/briefs/009-P6-3b-project-graph-a11y-fallback.md`).

Two related conventions settled once the Command Center (6.3a), the sidebar (6.2b chrome), and the Project Graph (6.3b) all needed the same `{id, label, machine, status}` item shape derived from projection rows.

**(1) Mapper-as-single-source.** `ui/src/projections/items.ts` owns the projection→item mappers (`toSessionItems`/`toPrItems`/`toApprovalItems`), each producing one generic `ProjectionItem` shape; `CommandItem`/`SidebarItem` are aliases of it. Views and chrome **consume the mappers — they never re-map a projection row → item shape inline at a call site.** Before the extraction the Shell mapped `data.sessions` → items at three sites (sidebar, command items, and the would-be graph nodes) — a silent drift hazard the moment one site's mapping diverged. `ProjectionItem.machine`/`status` stay typed `string` for now (the descriptor table is string-keyed with an unknown→degraded fallback — [[6]]); narrowing them to the generated machine-name/status enum unions folds into the provisional→generated reconcile at the daemon object-schema freeze (tracked in `MVP_TASKS.md` Carry-forward).

**(2) Namespaced locator.** Every `data-item-id` emitter namespaces its locator as `<namespace>:<id>`, **never a bare id** — `${machine}:${id}` for status items (Command Center, sidebar), `${type}:${id}` for graph nodes (the machine-less project root). The namespace is whatever dimension disambiguates a same-id/different-kind collision. **One rule, no special-case emitter:** even where a collision is impossible by construction (the sidebar is sessions-only, so all its ids share `machine="Session"`), the locator is still namespaced — so a test or the PRD §25 demo that locates by `data-item-id` never has to remember an exception. (The React `key` was already collision-safe via `machine:id`; this brings the DOM locator to the same footing.)

Apply both whenever a new view needs projection-derived items: import the mapper (add one to `items.ts` if a new projection appears), and namespace every `data-item-id`. The graph view (6.3b L2) and 6.3c–e are the first consumers.

**(3) Entity-specific display data rides a side map, NOT a widened `ProjectionItem`** (P6.4 sidebar resume-mode, `025`). When a shared-shape widget (the sidebar, fed `ProjectionItem`s) needs a field specific to ONE entity kind — the sidebar resume-mode indicator needs a session's `resume_mode`, which is meaningless for PR/Approval items — do **not** add the field to the shared `ProjectionItem` (that pollutes every consumer + breaks the single-source mapper). Instead pass an **id-keyed side map** alongside the items (`resumeModesBySessionId(sessions) → Record<sessionId, ResumeMode>`, a pure builder; only entities with the field appear) and have the widget look it up **guarded by `machine`** (only `"Session"` items query the resume map — defensive even when the list is single-kind today). Reuse the existing single descriptor (`describeResumeMode`) — never re-derive the glyph/label. Pin the discipline with a test that renders the SAME items WITHOUT the map → no indicator (proves the data path is the side map, `ProjectionItem` untouched).

**Rule:** projection→item mappers in `items.ts` are the single source for the `ProjectionItem {id,label,machine,status}` shape (no inline re-mapping); `data-item-id` is namespaced `<namespace>:<id>` on every emitter (machine for status items, type for graph nodes), one rule with no exceptions; **entity-specific display data rides an id-keyed side-map prop (machine-guarded lookup, reuse the single descriptor), never a widened `ProjectionItem`.**

## <a id="9"></a>9. A11y foundation — global focus-visible ring (one stylesheet, kit tokens), reduced-motion via the kit guard + `useReducedMotion`, every control keyboard-reachable

**Date:** 2026-06-07.
**Source slice:** P6.4a (`docs/briefs/011-P6-4a-a11y-musts-focus-reduced-motion.md`).

The §11.6 a11y MUSTs are merge-gates (PRD §14.8). Three patterns settled when applying the daemon-independent ones (Decision-C reorder).

**(1) Focus-visible ring = ONE global stylesheet, never per-component.** `ui/src/a11y/focus.css` carries a single `:where(button, a[href], [role="button"], [tabindex]:not([tabindex="-1"]), input, select, summary):focus-visible` rule applying the kit ring tokens (`--focus-ring` / `--ring-w` / `--ring-offset`), imported in `main.tsx` after the kit `styles.css`. `:where()` keeps specificity 0 so a component can override if it must; the selector enumerates the interactive element types. **Maintenance:** extend the `:where(...)` selector when a NEW interactive element type lands — `textarea`/`contenteditable` are deliberately omitted today (no such control exists; the PTY terminal + a deny-with-reason `textarea` arrive with the **parked 6.3d**). Never add per-component focus styles; the global rule is the single source.

**(2) Reduced-motion = the kit's global guard + a hook for JS-gated motion.** The kit `tokens/motion.css` already ships a global `@media (prefers-reduced-motion: reduce)` guard that neutralizes ALL animation/transition app-wide — so **CSS** motion is handled for free (just verify `motion.css` is in the `styles.css` import chain — [[3]]). For **JS-driven** motion (conditionally rendering/animating in JS), gate on `useReducedMotion()` (`ui/src/a11y/useReducedMotion.ts`): reads `matchMedia('(prefers-reduced-motion: reduce)')`, change-subscribed, **defaults `false` (motion allowed)** when `matchMedia` is absent (reduced-motion is an explicit OS opt-in; absence ≠ reduce). No animated consumer exists yet — gate the FIRST one (live-pulse / attention-beacon / overlay entrances) through the hook.

**(3) Keyboard-reachability is the substance the ring decorates — pin it with a multi-view audit.** Every interactive control must be keyboard-focusable (a semantic element, or a `role` on a natively-focusable element; never `tabindex="-1"` on an actionable, never a `div`/`span`-onClick). `reachability.test.tsx` is the §11.6 **merge-gate NET**: it sweeps the rendered `<Shell/>` across ALL content views (CC → Graph → Sessions via the view-switch) so a non-focusable control added to ANY view is caught by ONE test, not left to each slice to remember. Unify the focusability check on **`el.tabIndex >= 0`** (one robust predicate that also catches a non-focusable `div[role="button"]`). Keep each view's audit non-vacuous (it must actually find controls).

**(4) Composite widgets use WAI-ARIA roving tabindex — and the audit must be taught it** (P6.4, `docs/briefs/023-P6-4-tablist-roving-and-audit-refinement.md`). A composite widget (the Settings tablist; later the ProjectSwitcher dropdown radiogroup/listbox) puts **exactly one member in the tab order** (`tabIndex=0`), the rest `tabIndex=-1`; Arrow/Home/End move focus + (automatic activation, panels instant) select. The handler **reads the focused index from the DOM at event time** — never a captured/closure index (a stale-closure race; canonical APG). A pure `nextTabIndex(currentIndex, count, key)` (`views/settings/roving.ts`, widget-agnostic) is the test-first core. **Critical coupling:** roving makes inactive members `tabIndex=-1`, which the point-(3) audit (`el.tabIndex >= 0`) would flag as unreachable — so the audit is **roving-aware**: a `role="tab"` at `tabIndex=-1` is reachable **iff** its `role="tablist"` has **exactly one** `tabIndex=0` tab (whitelists roving AND pins the one-tabstop invariant — zero/multiple tabstops still fail). The audit also now covers `role="tabpanel"` (a visible panel must be focusable; `hidden` excluded). `auditFocusable` was extracted to a **vitest-free** `ui/src/a11y/reachability.ts` (throws a plain `Error`) so the whole-Shell sweep's call form is unchanged AND the classifier is unit-testable via `.toThrow()`. **Boundary (documented, not hardened):** `isRovingTabMember` assumes **flat** tablists (`closest('[role="tablist"]')` + a descendant `[role="tab"]` count) — nested composite widgets would miscount; YAGNI until a nested widget lands (none today).

**(5) Dropdown/popover composite + the orientation + audit generalizations** (P7.3 ProjectSwitcher, `docs/briefs/026-P7-3-projectswitcher-dropdown-popover.md`). A **dropdown** is a `button[aria-haspopup="listbox"][aria-expanded]` trigger + a roving `role="listbox"` popover of `role="option"`s — point (4)'s roving pattern, plus popover lifecycle: **open focuses the active option** (`useLayoutEffect` keyed on the open transition only, so focus is set once on open and never yanked mid-navigation); **Enter/Space select+close and Escape close both return focus to the trigger**; a **click outside closes** (focus follows the click — NOT returned to the trigger; an outside click is the user pointing focus elsewhere). The trigger is **disabled at zero options** ("No project" — wire-or-disable). Two generalizations this forced, both done cleanly: **(a) the roving helper is orientation-aware** — `nextTabIndex(i, count, key, orientation="horizontal")` re-homed to the shared **`ui/src/a11y/roving.ts`** (default horizontal keeps the tablist byte-identical; the vertical listbox passes `"vertical"` → ArrowDown/Up; Home/End both) — orientation lives IN the primitive, never key-lying (`ArrowDown→"ArrowRight"`) at the call site; **(b) the audit's roving-awareness generalizes** from tab-only to a `ROVING_CONTAINER` map `{tab→tablist, option→listbox}` — a `tabIndex=-1` member is reachable iff its container has exactly one tabstop, enforced for **both**, and the swept set now includes `role="option"` so the open-dropdown whole-Shell sweep genuinely audits the listbox (it passed trivially before). The shared `a11y/roving.ts` now serves Settings (horizontal) + the ProjectSwitcher (vertical).

**(6) The reachability audit now asserts accessible-NAME coverage, not just keyboard-reachability** (P6.4, `docs/briefs/045-P6-4-accessible-names.md`). A control can be keyboard-focusable yet **nameless** (a screen-reader dead end) — so `ui/src/a11y/reachability.ts` gained `auditAccessibleNames(container)` alongside `auditFocusable`, wired into every whole-Shell view sweep via a combined `auditView(c) = {auditFocusable; auditAccessibleNames}`. Every interactive control (the `INTERACTIVE_SELECTOR`) must compute a non-empty accessible name: `aria-label` ‖ `aria-labelledby` target text ‖ non-`aria-hidden` visible text (incl. a `.sr-only` child) ‖ `title` ‖ an input's associated `<label>` — and **`aria-hidden` text never counts** (a decorative glyph alone is not a name; the exclusion applies to the `aria-labelledby` target text too — the one code-quality med). **Roving `tabIndex=-1` members are SKIPPED** (the one tabstop carries the group name — symmetric with `isRovingMember`; the tabstop member itself IS still name-checked). The name comes from a **visually-hidden child inside the control** (Lesson 6), never a wrapper `aria-label`. The whole-Shell sweep was **green on first run (0 controls caught)** — every control was already named via the `.sr-only`-child / kit-`IconButton.label` pattern — so the deliverable is the **standing §11.6 regression net** for future controls (the same value `auditFocusable` provides), NOT a fix; **no kit-contract change needed** (§11.7 stands on the established wrapper pattern).

**Rule:** apply the focus-visible ring globally via one `a11y/focus.css :where(...):focus-visible` (kit tokens, never per-component; extend the selector for new element types); handle reduced-motion via the kit's global `motion.css` guard + `useReducedMotion()` for JS-gated motion; keep every interactive control keyboard-reachable (`tabIndex >= 0`) **AND named** (a non-empty accessible name — `aria-label`/labelledby[aria-hidden-excluded]/non-hidden text incl `.sr-only`/title/input-`<label>`; `aria-hidden` text never counts; the name from a visually-hidden child, never a wrapper `aria-label`), both pinned by the multi-view reachability audit's `auditView` (the §11.6 merge-gate net — `auditFocusable` + `auditAccessibleNames`); composite widgets use **roving tabindex** (one tabstop; Arrow/Home/End move + auto-activate; index read from focus at event time) and the audit is **roving-aware** (a `tabIndex=-1` roving member is reachable iff its tablist has exactly one tabstop; such members are name-skipped, the tabstop name-checked) + covers `role="tabpanel"`.

## <a id="10"></a>10. Green tests + clean build ≠ "looks right" — TDD-exempt visual/theme layers need a rendered-product visual gate

**Date:** 2026-06-07.
**Source slice:** USER FINDING (post-6.4b) — the app rendered functionally-correct but **completely unstyled**.

A visual/theme layer is **TDD-exempt** (per the root `CLAUDE.md` TDD posture, visual rendering is covered by fixtures/review, not failing unit tests). The trap: the standard "green" gates verify everything EXCEPT appearance. **jsdom render tests** assert DOM structure/presence (a control renders, a `data-item-id` is right) but **never compute CSS/layout** (jsdom has no layout engine — we already knew this for `focus.css`/`reachability.test.tsx`); **`tsc`/`oxlint`/`vite build`** verify types/lint/bundling, not pixels. So a slice can be **fully green + build-clean while the visual deliverable is absent or wrong**.

This is exactly what happened across 6.1–6.4: the kit `styles.css` only **defines** tokens (`:root { --vars }`) — nothing in the app **applies** them (no `body{}` background/font, no layout grid, no panel chrome). Only the kit's self-theming components (StatusPill/AttentionMarker) rendered themed; the whole app shell + views fell back to browser defaults (serif, white, no layout). Every slice was "complete + verified" for FUNCTIONAL/structural correctness while the **Graphite Arc visual layer was never built or verified.**

**Rule:** for any TDD-exempt visual/theme layer, "green tests + clean build" is **NOT** verification — it must be an **explicit tracked deliverable with VISUAL acceptance**, gated by a **rendered-product check**: run the actual app and compare against the design reference. NexusOps automates this as a **gstack-browser visual gate** — the running dev server vs the runnable Graphite Arc prototype (`NexusOps-ui-kit/ui_kits/control-plane/index.html`) via `/browse` · `/design-review` · `/qa` — run at phase boundaries + when a styling slice lands. A lightweight wiring-guard (assert the theme stylesheet is imported + applies `body` styling) catches gross absence in unit tests but **does not replace** the rendered comparison. Never report a UI slice "done/looks right" from green tests alone.

## <a id="11"></a>11. Degraded + safety states are always EXPLAINED, fail-closed — distinct surfaces, never silent / color-alone / auto-resolving

**Date:** 2026-06-08.
**Source slices:** P6.4d-2 (§17 safety-state display — L1 fencing/hard-conflict card `ff2f8d6`, L2 fail-closed/audit-integrity alert `503b6a2`) + P6.4 checking/handshaking banner (`5f40149`).

Every state where the UI is **read-only** or a **§15/§17 safety invariant is in play** must render an **explicit, distinct display surface** — never silent, never color-alone, never auto-resolving what the daemon must resolve. The UI *displays* the safety state; enforcement stays daemon-side (INV-SEC-1). Five binding parts:

1. **Distinct surfaces, never conflated.** Three+ separate concerns get separate surfaces: **transport-degraded** (`DegradedBanner`, 6.1c) · **session-survival** (`RecoveryBanner`, 6.4d) · **§17 safety-state** (`HardConflictCard` / `AuditIntegrityAlert`, 6.4d-2). The "checking" handshake window is its own `DegradedBanner` variant. Don't fold one into another (a render test asserts each surface is distinct — `data-degraded` ≠ `data-recovery-kind` ≠ `data-conflict-reason` ≠ `data-treatment`).
2. **Fail-closed display.** Conflict cards are **never-auto-resolved** (#6 — the card offers NO auto-resolve path; the "never auto-resolved" copy must be true of every state it shows, so scope it to the genuine hard conflict `fencing_conflict`, NOT a re-approvable `stale_precondition`). Audit-integrity alerts are **non-dismissible** (#5 — no local dismiss; the signal must be seen). Read-only is **never silently unexplained** — every `canSubmitIntent===FALSE` state has a `DegradedState` banner variant (the "checking" connected+version-unknown window was the gap).
3. **Parked intents rendered disabled-but-present.** Resolution / acknowledge / restart are daemon-1.5 **intents** — rendered **disabled**, gated on `canSubmitIntent` (forbidden #6); the UX is complete (the affordance is visible) but no mutation is offered. A present-but-disabled control is tracked, not a false wire.
4. **Non-color channels carry meaning.** glyph + label + severity, never color alone (§11.6 / forbidden #5); **derive the glyph FROM the severity** so the two can't drift.
5. **Drift-pinned reuse + completeness.** Reuse frozen enums via `z.enum().extract([...])` (no re-declared values — throws at load if the frozen enum renames; Lesson [[2]]); model provisional shapes ONLY for genuinely net-new states; **enumerate display coverage from `.options`** (completeness test) so a future-added state is *forced* to render, never silently dropped — a silently-unrendered safety signal is itself a #5 violation.

## <a id="12"></a>12. The theme/visual layer — apply semantic tokens, ground against the prototype, gate visually (extends [[10]])

**Date:** 2026-06-08.
**Source slices:** the 6.5 Graphite Arc theme pass — 6.5a base/global (`c439379`), 6.5b shell-chrome + flexbox→CSS-Grid (`b5618af`), 6.5c view-panels + severity surfaces + the full visual gate (`3b6135d`).

A theme/visual layer is **TDD-exempt** ([[10]]); how to execute one correctly, concretely:

1. **APPLY the kit SEMANTIC tokens** via app stylesheets (`ui/src/theme/{global,shell,components}.css`, imported after the kit `styles.css`). The kit ships **tokens-only** (`:root` vars) — *applying* them (`body` reset, the layout grid, region/panel chrome) is the app's job; the unstyled-layer Finding was exactly this gap. Reference the **semantic** layer only (`--surface-*`/`--text-*`/`--border-*`/`--sp-*`/`--r-*`/`--elev-*`), never primitives (`--n-*`/`--ink-*`/`--line-*`) — the §11.1 re-hue promise.
2. **GROUND every surface against the prototype's ACTUAL rendering**, don't guess-then-fix-at-gate. The prototype's region/panel chrome lives in its kit components (`kit-shell.jsx`/`kit-views.jsx`), NOT the `index.html` `<style>` (which defined only `.shell`+`.main`) — sampling the real source caught the 6.5b `--surface-canvas`→`--surface-panel` divergence at the source.
3. **The VISUAL GATE is the acceptance**, not green tests ([[10]]). Run the dev server vs the prototype **served over HTTP** (`file://` blocks the prototype's relative kit scripts), compare every view + driven fixtures, capture per-view screenshots + a match/divergence notes table. It caught what tsc/oxlint/jsdom structurally CANNOT: the **`*/`-in-CSS-comment bug** (a comment token-glob like `--n-*/--ink-*` prematurely closes the comment and silently drops the *next* CSS rule — here the whole `body` reset) + the 6.5b surface divergence. Classify each divergence: **theme defect** (fix) vs **unbuilt later-phase feature / approved app-specific adaptation** (note, not a defect).
4. **Structural refactors preserve reachability.** Flexbox→CSS-Grid restructured the Shell DOM; the suite (incl. the a11y/reachability audit) stayed green because landmarks/roles/testids/order were preserved — verify this, don't assume.
5. **Theming preserves never-color-alone.** Severity colors are **ADDITIVE** to the existing glyph+label channel (§11.6 / forbidden #5), never a collapse to color-only; **derive the glyph from the severity** so they can't drift.

## <a id="13"></a>13. UI selection/scope state lives over a FROZEN projection — daemon-independent, never a mutation

**Date:** 2026-06-08.
**Source slice:** P7.3(fwd) active-project selection (`86727ec`) — completing the inert ProjectSwitcher.

A UI **selection / scope** (the active project; future filters/scopes) is **plain UI state** read over the **FROZEN projections** — it is **NOT** a daemon mutation, **NOT** a provisional contract, and **NOT** a Gateway intent. So it has **no `canSubmitIntent` gate** (that gate is for *mutations* — [[4]]) and **no daemon dependency**: it ships now without waiting on the parked mutation seam. Distinguish it sharply from the daemon-1.5 intent work — selecting/scoping/filtering what you *view* is daemon-independent; *changing* state is the parked intent path.

Mechanics that worked:
- **Context provider, mirroring `ReadOnlyProvider`** (`ActiveProjectProvider`/`useActiveProject`) — the project-scoped views (`ProjectGraph` re-roots, `SessionsTable` filters) read the selection via context, no prop-drill through `TopBar`; **the provider wraps the WHOLE shell** so a control inside `TopBar` can read it.
- **Single-select via `aria-pressed`** — consistent with the existing view-switch segmented-control; the stricter WAI-ARIA radiogroup/listbox + the dropdown-popover widget are a deferred presentation polish.
- **Stale selection re-scopes to a default** (`resolveActiveProject`) — a selected-then-removed id never becomes a ghost; `null` (no projects) = the unscoped/no-project-guard state.
- **Cross-cutting triage stays GLOBAL** — the Command Center is "needs my attention *everywhere*"; don't scope it to the active project (only the genuinely project-scoped views re-root/filter).

**Second instance — view-navigation history** (P6.4 TopBar back/forward, `022`): the same family. Content-view **navigation history** (back/forward over `command|graph|sessions|settings`) is also plain UI state — a pure `viewHistoryReducer` (browser semantics: `navigate` pushes + truncates forward entries; back/forward move the cursor, never the stack; `navigate`-to-current collapses to a no-op preserving any forward entry) + a thin `useViewHistory()` hook, mirroring this pattern (no daemon dep, no contract, no intent gate). Note it's history *over the view selection*, not over a projection — the "frozen projection" in the lesson title generalizes to "frozen/local UI state." **Paired a11y rule (wire-or-disable):** a **named control is never a dead click** — wire it or `disable` it. The TopBar back/forward were named-but-inert (accessible names, no handler); making them live means `disabled={!canBack}`/`disabled={!canForward}` at the history boundaries (the kit `Button` supports `disabled` natively). An enabled-but-noop control is still a dead affordance (§11.6); this rule also flags the still-placeholder Brain TopBar trigger (Phase 8) and any future named-but-inert control.

**Third instance — table filtering** (P6.3 Sessions table, `024`): the same family — pure UI **filter** state over the frozen projection (the lesson title already covers "filters"). Mechanics that worked: **compose `filter → sort`** (filter first; §5.2 attention order then holds within the subset); derive the filter's status options via `distinctStatuses(rows)` from the **unfiltered** set (stable options that don't collapse as the filter narrows); **filtered-empty is a distinct state from truly-empty** ("No sessions match the filters" + a Clear control vs the empty-projection "No sessions." — a filtered table that looks like an empty projection is a confusing dead-end). **Gotcha (caught in review):** a multi-field text match must **OR independent per-field checks**, never match against a **joined** string (`label + " " + project`) — a joined string matches a term straddling the field boundary (false positive). **Facet selection:** don't add a redundant facet — project scoping already lives in the active-project switcher (the Shell pre-filters the table's rows), so the within-table facets are status + text only.

## <a id="14"></a>14. A contract bump is a pure regen + a delegation retarget — `validators` derives from the bundle, an enum rename retargets (never re-declare)

**Date:** 2026-06-12.
**Source slice:** P6.2 (`docs/briefs/040-P6-2-contract-regen-0190-reconcile.md`) — the 0.12.0→0.19.0 ui-resume reconcile.

The 0.12.0→0.19.0 reconcile drove three §5.0 drift checks RED at HEAD (the merge that froze 0.19.0 left the ui's generated layer at 0.12.0 — 20 value-sets vs 33, version mismatch, the new value-sets unvalidated). The clean path is mechanical, and the failure that *forced* this slice was avoidable: the `index.ts` `validators` registry was **hand-listed** (20 entries), so it silently went stale when the schema grew to 33 — the exact hand-drift [[1]] exists to prevent, leaking back in through the registry that *feeds* the drift test.

The durable rule, in three parts. **(1) Regenerate, never hand-edit** `generated.ts` (`npm run gen:contracts`; [[1]]) — `CONTRACT_VERSION` and every value-set follow the frozen schema. **(2) Derive `validators` from the generated bundle** (`= shape`), never hand-list — the drift test's "exported keys === `$defs`" check then **self-maintains** across every future bump, and named exports exist only for the *consumed* enums (a new value-set is reachable for the drift test without an `index.ts` edit; a future bump with no new typed consumer becomes a pure `gen:contracts` run). **(3) An enum RENAME retargets, never re-declares** — `ActionRequest`→`ActionRequestStatus` / `Approval`→`ApprovalStatus` changed the schema `$def` names, so the `validators` keys follow the `$def` names (the drift test pins them) and every provisional delegation + `.extract` source ([[2]]) retargets to the renamed export; no enum is re-declared by hand.

Two boundary points this slice settled. The UI's **status-machine identifiers** (`"ActionRequest"`/`"Approval"` — the descriptor-table keys, `StatusPill machine=`, the `data-machine`/`data-item-id` namespace) are **UI-render-policy naming, decoupled from the frozen `$def` name** (the A-lite decision): they stay stable across the rename, bridged by a small `VALIDATOR_KEY` alias map in the completeness lookup, so the `data-*` namespace and the `overlays`/`StatusPill` selectors don't churn on a contract rename. And a **new §2.5-seam type adopted ahead of its consumer** (`ServerFrame`, `WireError` — §6.4 frame-mux, consumed by the intent seam in slice 2) gets a **schema-snapshot drift test the same day it's defined** — the field-*name* snapshot alone is not enough (it missed `rpc_response.id` being typed `integer`/`uint64`, not `string`, so a real RPC frame would have failed `.parse()` at runtime); add a per-variant field-*type* pin too, so a daemon frame-shape change fails loudly in CI rather than at first live parse.

**Rule:** on a contract bump, regenerate `generated.ts` + derive `validators` from the bundle (never hand-list); an enum rename retargets delegations + `.extract` sources to the new `$def` name (never re-declare); UI status-machine identifiers stay UI-render-policy (decoupled from `$def` names via an alias); a seam type adopted ahead of its consumer carries a field-*type* schema-snapshot drift pin.

**Enforcement:** `pin: ui/src/contracts/generated.test.ts` (member-set + validators-keys-equal-`$defs` drift, now self-maintaining since `validators = shape`) + `ui/src/contracts/provisional.test.ts` (`serverframe_variant_fields` / `serverframe_variant_id_type` snapshot). `accepted: the hand-list→bundle-derivation discipline is not separately grep-enforceable — the drift test is the mechanical guard.`

> **Update (ui-065):** the generator now also emits **`oneOf`-of-const-string** `$def`s as Zod enums (synthesize a flat enum from the pure-const oneOf; discriminated-union oneOf SKIPPED via `every(m.const is string)`) — so the "regenerate, never hand-declare" discipline is now mechanically realized for the doc-commented (oneOf-const) enum class too: the `ResumeMode`/`RecoveryState`/`MetricQuality` hand-declared drift-pinned provisional shadows (the [[2]] class) are RETIRED → generated, and the §5.0 drift gate's `enumDefs` filter normalizes flat + oneOf-const so it auto-pins both. One fewer manual-shadow class; CONTRACT-neutral (a representation swap, value-sets identical).

**Validated in practice (slice 041, 0.19.0→0.23.0):** the first re-application was a pure `gen:contracts` + one provisional `ServerFrame` variant (`terminal_output`) — **zero `index.ts` churn**, `validators` self-maintained, exactly as the rule predicts. The field-**type** pin discipline earned its keep again: the `terminal_output.seq` pin (numeric parses, string rejected) extends to reject **negatives** for the schema's `minimum:0` uint64 bound — "fail loudly" covers the range, not just the JS type.

## <a id="15"></a>15. A discriminator that gates a fail-closed/critical state is a REQUIRED field — never optional-with-default

**Date:** 2026-06-12.
**Source slice:** P6.4b (`docs/briefs/042-P6-4b-two-pool-credit-pool-reconcile.md`) — the §11.4/§9.1 two-pool credit-pool reconcile.

The §9.1 billing model is **two-pool**: the SDK/`-p` pool is a separate capped *monthly* pool that **hard-stops with no fallback**; the interactive pool runs on auto-resetting rolling-window limits, **exempt** from hard-stop (this asymmetry is *why* the supervisor drives via PTY-PRIMARY — "a control plane cannot let a supervised run die on the capped pool"). So credit-pool exhaustion is **pool-kind-dependent**: `creditPoolState(used, limit, kind)` returns `hard_stop` only for `kind="sdk"`; the interactive pool's exhaustion is the recoverable `near_exhaustion` signal, never `hard_stop`. Only `hard_stop` is kind-gated; the `near_exhaustion` (≤ 15% remaining) warning is kind-independent.

The load-bearing choice was making `CreditPool.kind` a **required** field, not optional-defaulting-to-`"sdk"`. The optional-default reading *looks* fail-closed (assume the hard-stopping pool when unknown), but it hides a real failure mode: a future interactive pool whose construction site **forgets** `kind` would silently inherit `"sdk"` and render a **false `hard_stop` alarm** — a §11 "never a silent false safety state" violation (the user sees a non-dismissible hard-stop that isn't real). A **required** field eliminates the unknown case entirely: `tsc` forces every construction site to declare its kind, so the false state can't compile. The cost was one trivial test-fixture edit (`CommandCenter.test.tsx`).

Generalize past credit-pools: **any discriminator that gates whether a fail-closed / critical / non-dismissible state is shown is a required field, never optional-with-default.** An optional-with-default discriminator trades a compile-time guarantee for a silent runtime assumption — and for a safety surface (§11/§17), a silent wrong assumption (false alarm *or* false all-clear) is the exact thing the fail-closed posture exists to prevent. If the value can be genuinely absent at the boundary, model that absence as an explicit (drift-pinned) state, not as a defaulted discriminator.

**Rule:** a discriminator gating a fail-closed/critical UI state (e.g. `CreditPool.kind` gating `hard_stop`) is a **required** field — never optional-with-default; `tsc`-enforced declaration beats a silent default that can produce a false safety state (§11). Model genuine absence as an explicit state, not a default. Extends [[11]] (fail-closed safety states) + [[13]] (UI logic over frozen/local state).

**Enforcement:** `pin: ui/src/views/usage/model.test.ts` (`credit_pool_interactive_exhaustion_is_never_hard_stop` + the kind-gated cases) + the required `z.enum(["sdk","interactive"])` shape (no `.optional()`/`.default()`) — `tsc` rejects a kind-less `CreditPool` construction. `accepted: "required discriminator for a safety-gating field" is a review-time judgement, not a grep — the pin + the tsc-required shape are the mechanical guards.`

## <a id="16"></a>16. The UI intent-submission seam — a `canSubmitIntent`-gated PURE SUBMITTER (the daemon Gateway is the real chokepoint); classify caught errors by `instanceof Error`, never by shape

**Date:** 2026-06-12.
**Source slice:** P6.3d (`docs/briefs/043-P6-3d-intent-seam-foundation.md`) — the UI's FIRST mutation/intent-submission path (cat-1; `security-reviewer` PASS). The 7 safety invariants were lead-ruled in `docs/planning/intent-seam-cat1-safety-design.md`.

The UI's first mutation path is a **CLIENT of the daemon's frozen single-mutator Gateway contract**, so its safety design FOLLOWS from that contract. Seven invariants, each a pinned test:
- **Pure submitter (INV-SEC-1 / §4.2 law 1):** the seam exposes only `submit_action`/`preview_action`/`approve`/`deny` and routes every mutation through the single `GatewayPort` — there is **NO UI execution path**. The `GatewayPort` mirrors the §6.1 **id-based wire** (`preview_action(action_request_id)`, `approve(approval_id, step_id?)`, `deny(approval_id, reason)` — the daemon source `daemon/src/ipc/methods.rs` is the param SoT, **NOT** an inference from the object models). A higher-level seam ACCEPTS the daemon's rendered `Approval`/`ActionRequest` and **extracts the verbatim id** to the wire (caller-friendly + provenance-preserving).
- **Fail-closed gate, defense-in-depth:** `canSubmitIntent` (fail-safe FALSE on any unknown/degraded — [[4]]) gates every submit; when false the seam returns `{readOnly:true}` and NEVER calls the port. **Critically, this gate is defense-in-depth, NOT the sole guard — the daemon Gateway (INV-SEC-1) is the real chokepoint** (a UI-enable bug must still be rejected daemon-side).
- **No optimistic render (§4.2 law 2):** the result carries the daemon's `ActionAck.status`; the seam NEVER synthesizes "succeeded" — "done" comes only from a confirming projection/`ActionResult` (the UI holds no authoritative state).
- **Policy from the daemon:** `approve`/`deny` carry the daemon's `approval_id` verbatim; the seam NEVER computes its own risk/approval-requirement (the daemon's `PolicyDecision` is authoritative).
- **No invented preview (forbidden #2):** `preview_action` surfaces the daemon's `ActionPreview`/`cannot_preview_reason`, never a fabricated one.
- **§6.4 codes verbatim:** the seam surfaces the daemon's `IpcErrorCode` (`WireError`) unchanged — never collapse/remap (a consumer routes each to its mandated §11.5 card; `fencing_conflict` stays never-auto-resolved #6).
- **No intent state (Q7-A):** no cache, no auto-retry across a disconnect (the fail-safe baseline). Cache+retry is **parked-for-user** — lead lean: **MANUAL resubmit, never auto-replay** (`idempotency_key` is dedup-safe but not consent-fresh; auto-acting after a gap violates human-gated-mutation).

The seam shape: a pure factory `createIntentSeam(port, canSubmit)` + a thin `useSubmitIntent` hook (mirror [[13]]); the result is a **discriminated union** `{ok} | {error: WireError} | {readOnly}` so a consumer is FORCED to handle each case (no silent success). The frozen contract shapes are **provisional Zod shadows** ([[2]]) with enum fields delegated to the generated bundle ([[1]]) and `inputs` typed `z.unknown()` (an opaque daemon payload the UI passes through, never interprets), **field-set drift-pinned to the frozen schema** ([[14]] — the §2.5-seam discipline that makes "no UI-invented shape" enforceable).

**The load-bearing footgun (security [high]): classify a caught error by `instanceof Error`, NOT by shape.** The seam catches a rejected port call and must decide "daemon `WireError` (→ `{error}`) or an unexpected runtime error (→ re-throw, never mask as fake success)?" A keys/shape check — **even Zod `.strict()`** — is INSUFFICIENT: a JS `Error`'s `message`/`stack` are **non-enumerable**, so an `Error` carrying a colliding `code` passes a keys-only parse and gets silently swallowed as a handled `{error}` (a real failure masked as benign). A daemon `WireError` frame is **deserialized plain data, never an `Error` instance** → use `instanceof Error` to re-throw any real Error; keep `.strict()` on `WireError` too (reject malformed extra-key frames). Pin it adversarially (a plain Error + a colliding-code Error both re-thrown, never `{error}`).

**Rule:** the UI intent seam is a `canSubmitIntent`-gated PURE SUBMITTER — submits only (no execution path), surfaces the daemon's status/preview/error without optimism or remapping, holds no intent state, treats the daemon Gateway (not the UI gate) as the real chokepoint, and mirrors the daemon's **id-based** wire (the daemon source is the param SoT). Classify caught errors by `instanceof Error` (a frame is plain data), never by shape; field-set-drift-pin the provisional contract shadows.

**Enforcement:** `pin: ui/src/intent/submit-intent.test.ts` (the 7 cat-1 pins + `intent_contracts_field_sets_match_frozen_schema` + `seam_rethrows_non_wireerror_never_fake_success`) + **`security-reviewer` on every intent/mutation slice (cat-1)**. `accepted: the "daemon Gateway is the real chokepoint" posture is the INV-SEC-1 invariant the daemon enforces — the UI pins are defense-in-depth.`

## <a id="17"></a>17. The intent-seam CONSUMER — a daemon-DRIVEN approval card: render the daemon's decision/preview (never UI-derived), daemon-status-never-"done", route each §6.4 reject code to its DISTINCT §11.5 treatment (`fencing_conflict` never re-approvable by construction)

**Date:** 2026-06-12.
**Source slice:** P6.3d slice 2 (`docs/briefs/044-P6-3d-gatewaymodal-real.md`) — `GatewayModal`-real, the UI's first LIVE human-approval card over the mutation path (cat-1; `security-reviewer` PASS). Consumes the same 7 lead-ruled invariants as the seam ([[16]]), re-pinned AT THE CARD the human clicks.

[[16]] built the seam (the submit path); this is its **CONSUMER** — the card a human actually approves against. The seam's invariants move from "the function" to "the surface": the card is a **pure renderer of daemon-provided data** + a thin driver of the seam.
- **Pure renderer, no UI-derived risk (Q4):** the card renders the daemon's `PolicyDecision` (`status`/`reasons`/`required_approvals`/`constraints`/`safer_alt`) + `ActionPreview` — it **NEVER computes its own risk/approval-requirement**. Pin it by rendering the SAME card with a DIFFERENT `PolicyDecision` → the displayed requirement changes (proves read-from-data, not hardcoded). Source the `{Approval, PolicyDecision}` as **props from a daemon-SHAPED side-map** (mirror the display-meta side-maps, [[8]]); fetch the `ActionPreview` via `seam.previewAction(action_request_id)` on open. The fixture-enrichment is daemon-shaped (not UI-invented), but the live transport slice MUST swap it for the real daemon projection before a human approves against fixture values (carry-forward).
- **No invented preview (Q5 / forbidden #2):** "What will happen" renders the daemon's `ActionPreview` or the honest `cannot_preview_reason`/pending — the hardcoded stub lines are GONE. A `{readOnly}` preview result (degraded) renders honest pending, **never blank/synthesized**; a thrown preview error renders an honest note, never blank. Give each preview state a **distinct testid** (`preview-no-target`/`-failed`/`-loading`/`-readonly`/`-error`) — the distinct-surface discipline ([[11]]).
- **Daemon-status-never-"done" (Q3):** on `{ok: ActionAck}` the card renders the daemon-reported decision status (`approved`/`denied`/`submitted`) inline and stays open — it **NEVER** shows "succeeded"/"executed"/auto-closes-as-complete. A confirmed completion is a later projection/`ActionResult` slice.
- **Fail-closed gate (Q2):** Approve/Deny are enabled only when `canSubmitIntent`; a `{readOnly}` seam result renders no mutation. Defense-in-depth — the daemon Gateway stays the real chokepoint.
- **§6.4 codes → DISTINCT §11.5 cards, `fencing_conflict` never re-approvable (Q6 / #6):** a pure `describeRejection(code)` maps each `IpcErrorCode` to its mandated treatment — `fencing_conflict` → never-auto-resolved (NO retry affordance, #6); `internal_error` → fail-closed integrity (#5); `precondition_stale` → the **re-approvable** card (regenerate preview + fresh approval — distinct from fencing, NOT a hard conflict); `policy_denied` → deny. **Reuse the safety-model descriptor SEMANTICS** (`describeConflict`/`describeAuditIntegrity`, [[11]]) rendered within the modal's reject state — the pins assert the semantics, not the component instance. **The unmapped/transport set** (`version_skew`/`frame_too_large`/`unknown_method`/`unauthorized_peer`/`protocol_error`) → an **honest generic rejection** (the code shown, no success/pending implied) — the default case must render SOMETHING, never silently swallow a rejection (a swallowed reject reads as pending/success — the exact false-state §11.7/Q3 forbid).

**The footgun (code-quality [high]): a preview `useEffect` keyed on the per-render seam identity re-fetches on every connection-status change.** `useSubmitIntent` re-memoizes when `status` changes, so keying the preview fetch on the `seam` object churns the fetch on every benign status tick. Key the effect on the **stable primitives** (`[port, action_request_id, canSubmit]`) via an inline `createIntentSeam`, so a no-op status change doesn't re-fetch but a real gate flip (recovery) re-fetches once.

**Deferral locked by a pin:** the "Always allow in this project" control (a `policy_grant` STANDING grant — pre-authorizes a CLASS of future mutations) stays **DISABLED**, pinned (`always_allow_policy_grant_stays_disabled`). A standing grant is a distinct trust surface NOT covered by the per-action Q1–Q7 rulings — it gets its OWN cat-1 checkpoint when its dedicated slice is authored (carry-forward; orchestrator escalates first).

**Rule:** the intent-seam consumer (an approval card) is a **pure renderer of daemon-provided `PolicyDecision`/`ActionPreview`** (never UI-derived risk, never invented consequences) + a thin seam driver that shows the **daemon-reported status, never optimistic "done"**, and routes each §6.4 reject code to its **distinct §11.5 treatment** (`fencing_conflict` never re-approvable by construction #6; the unmapped/transport set → an honest generic rejection, never swallowed). Key preview fetches on stable primitives, not the per-render seam identity. Lock a deferred standing-grant (`policy_grant`) with a stays-disabled pin. Extends [[16]].

**Enforcement:** `pin: ui/src/overlays/GatewayModal.test.tsx` (the 10 cat-1 modal pins incl. `card_risk_and_requirement_read_from_policydecision_never_ui_derived`, `modal_renders_daemon_status_never_optimistic_done`, `reject_codes_route_to_distinct_cards` + `fencing_conflict_never_rendered_as_reapprovable`, `unmapped_reject_code_renders_honest_rejection_never_swallowed`, `always_allow_policy_grant_stays_disabled`, `preview_failed_renders_honest_note_never_blank`) + the `precondition_stale` re-approvable card test + **`security-reviewer` on every intent/mutation slice (cat-1)**. `accepted: the live UdsGatewayPort slice replaces the daemon-shaped fixture enrichment with the real daemon projection before any real human approval.`

## <a id="18"></a>18. The terminal-well display pattern — a pure frozen-frame consumer split from the xterm.js host; DISPLAY-ONLY #9 (no input path, no output-scraping; ended-state from the projection); faithful subscription topology (an EVENT is not a ServerFrame)

**Date:** 2026-06-13.
**Source slice:** P6.3d slice 3 (`docs/briefs/046-P6-3d-session-terminal-display.md`) — the Session Terminal DISPLAY half (`security-reviewer` PASS; visual gate PASS). The LAST in-lane ui slice before the cross-track pause.

The terminal-well renders the daemon's PTY output — the surface where **safety rule #9** ("never scrape the PTY for machine state — PTY is display-only; §9.1") is enforced UI-side, and where an input path would silently sneak in.

- **Pure consumer split from the xterm host.** `xterm.js` renders to canvas/DOM — NOT deterministically unit-testable in jsdom — so the **pure frame-consumer** (`consumeTerminalFrame(state{lastSeq}, frame, sink)`: base64-decode the frozen `terminal_output` `data` → bytes → a sink; **skip an undecodable frame** [display-degrade, never throw/crash the stream]; **monotonic `seq` assumed, NO client-side gap-fill** — the recovery model is reconnect/re-subscribe, P4) is the **TDD core** (xterm-free units), and the `TerminalDisplay` xterm host is a **thin adapter** verified by the **visual gate** (Lesson 10/12) + **structural #9 tests** (xterm mocked via `vi.mock`). Mirror the logic-vs-visual split of the theme layer.
- **DISPLAY-ONLY #9 — three pinned axes.** (1) **No PTY input path** — `disableStdin`, no `onData`→gateway, and the `GatewayPort` exposes **no terminal-input method** (input is a P4 mutation surface, not this slice; the Pause button stays disabled — a session mutation). (2) **No output-scraping-for-state** — terminal status is **never** derived from the `data` bytes; it reads the **Session projection** (`StatusPill`/`session.status`) — scary output bytes never flip the status. (3) **Frozen-frame fidelity** — decode the frozen frame, never invent transcript content. `security-reviewer` verifies all three.
- **A subscription that maps 1:1 to ONE daemon source — an EVENT is not a ServerFrame.** `subscribe_terminal(terminal_id): AsyncIterable<TerminalOutputFrame>` is **output-only** because the §6.4 terminal channel carries **only** the `terminal_output` `ServerFrame` variant (the `ServerFrame` oneOf is `rpc_response` + `terminal_output`). **`TerminalProcessExited` is a daemon-internal OBSERVATION EVENT** (`daemon/src/terminal/mod.rs`; event store → projection), **NOT** a `ServerFrame` — so it reaches the UI as an event→**projection**, never over the terminal subscription. A tempting "merged" stream (`{kind:'output'|'exited'}`) is faithful in SHAPE but **invents the delivery TOPOLOGY** — the real `UdsGatewayPort` `subscribe_terminal` would demux `terminal_output` only and have no exit member to source, so the merged member reworks at P4 (a Decision-C anti-pattern in miniature). **Rule of thumb: a UI subscription method maps 1:1 to ONE daemon delivery channel; check whether a payload is a ServerFrame (pushed on a channel) or an EVENT (→ event store → projection) before routing it.** The well's **ended-state** is therefore sourced from the **Session projection** (a drift-pinned **ENDED/LIVE session-status partition** — a completeness pin, [[5]] — so a new daemon `Session` status forces a conscious ended-vs-live decision: never a live terminal for a dead session); the `exit_code`/`signal` DETAIL has no frozen UI-readable source today → **deferred** (a projection-wiring slice, P4), never invented (#2).
- **The visual gate catches reachability, not just appearance.** The gate caught that the `terminalId` fixture sat only on sessions the sidebar never opens → `TerminalDisplay` was **unreachable in the running product** despite green tests (Lesson 10 — green ≠ wired-and-visible). Fixed by routing `terminalId` to a sidebar-reachable live session (which also exercised terminal+permission-card coexistence). Always confirm a new view is reachable in the running product, not just rendered in a test harness.

**Rule:** build a terminal-well as a **pure frozen-frame consumer** (decode/seq/skip-undecodable/no-gap-fill) split from a **visual-gated xterm host**; enforce **#9 display-only** by pinned structural tests (no input path, status-from-projection-not-bytes, no invented transcript) + `security-reviewer`; make each UI subscription map **1:1 to one daemon channel** (an EVENT is not a ServerFrame — route exit via the projection, with a drift-pinned ENDED/LIVE status partition); the live transport stream + inbound flow-control + the exit detail are deferred to P4 (the fixture swaps for the live source there).

**Enforcement:** `pin: ui/src/views/terminal/{terminal-stream.test.ts (decode/seq/no-gap-fill/no-invention/skip-undecodable), TerminalDisplay.test.tsx (no-input-path, status-never-from-bytes, dispose-on-unmount), SessionTerminal.test.tsx (renders-display/keeps-placeholder/ended-state-from-status), session-lifecycle.test.ts (ENDED/LIVE completeness drift-pin)} + the TerminalOutputFrame field-set drift-pin; security-reviewer on the terminal display (#9)`. `accepted: the live UdsGatewayPort terminal demux + per-frame/total buffer bounds + inbound pause/resume + the exit_code/signal detail are P4 (cross-track).`

## <a id="19"></a>19. A resource-precise mutation forms its resource_ref VERBATIM from the displayed read source — submitted target == displayed target (read↔mutate consistency at the UI producer)

**Date:** 2026-06-13.
**Source slice:** P6.3e (`docs/briefs/048-P6-3e-per-hunk-actions-over-the-intent-seam.md`) — the per-hunk git actions over the intent seam (`security-reviewer` PASS; the resource_ref correctness adversarially verified).

A per-hunk `git.*` action targets ONE precise hunk via a `ResourceRef` whose `id` packs the hunk's identity. The UI is the **PRODUCER** of that resource_ref; the daemon's Phase-5 git executor is the **CONSUMER**. **The load-bearing safety property: the submitted resource_ref must target the EXACT hunk the human is looking at** — a mismatch (off-by-one, wrong file, reordered positions) stages or **irreversibly discards the wrong content** while the human approves what they think is a different hunk.

- **Form the resource_ref VERBATIM from the displayed read source.** The hunk's identity (`{worktree_id, file, old_start, old_lines, new_start, new_lines}`) comes from the SAME `get_diff` `Hunk` object that is rendered — never recomputed, re-parsed, or re-derived. Submitting the displayed object's fields (not a parallel computation) makes "submitted == displayed" true **by construction**. Pin it with a `submitted == displayed` test (the same `Hunk` ref rendered + encoded) + an exact-encoding test.
- **The encoding is a FROZEN daemon convention the UI conforms to (a conformance pin, NOT a UI invention).** `ResourceRef{ type: "file", id: "{worktree_id}\x1f{file}\x1f{old_start},{old_lines},{new_start},{new_lines}" }` — the `\x1f` (**U+001F** unit-separator) delimiter + the lowercase frozen `"file"` `ResourceType` value + the `{type, id}` (**NOT** `resource_type`) frozen `ResourceRef` shape. **The frozen schema is the authority; informal prose (`ResourceType::File` Rust shorthand → serdes `"file"`) is origin, not contract** — conform to `ResourceRef.parse`, never the prose ([[14]] / §5.0). The encoding is conformance-pinned on BOTH sides (the UI producer here; the daemon git-executor consumer pairs — a cross-track conformance test).
- **Daemon mints + reconciles; the UI fills daemon-owned fields as placeholders.** `action_request_id: ""` (the daemon mints), `risk_level` a non-authoritative hint **never displayed** (catalog-authoritative; the card renders the daemon's `PolicyDecision`, [[17]] Q4), `created_at` = the real UI submission time (a fact, not an invented consequence). The UI never derives risk / standing-grant-eligibility — those are daemon policy.

**Rule:** a resource-precise mutation (per-hunk, per-line, per-region) forms its `ResourceRef` **verbatim from the displayed read source** so submitted-target == displayed-target by construction; conform to the FROZEN resource_ref encoding (the schema, not informal prose), conformance-pinned both sides; daemon-owned fields are placeholders (the UI never derives risk). Extends the read↔mutate-consistency discipline to the UI producer; pairs with [[16]]/[[17]] (the seam + the approval card).

**Enforcement:** `pin: ui/src/intent/hunk-resource-ref.test.ts (encoder exact + round-trip + U+001F + type:"file" + distinct-hunks-distinct-id + buildHunkActionRequest placeholders) + DiffReview.test.tsx (submitted resource_ref == displayed Hunk); security-reviewer every intent/mutation slice (submitted == displayed).` `accepted: the daemon-CONSUMER half of the \x1f conformance (Phase-5 git executor) pairs cross-track; the enrichHunkAction risk/approval stand-in swaps for the real daemon projection before any real human approves (the 044 carry-forward).`

## <a id="20"></a>20. The ui-side UDS transport core is a pure Rust crate (codec/handshake/demux over a generic stream, TDD'd vs a fake stream) — frozen frame types from `nexusops-shared`, never the daemon; fail-closed boundary discipline

**Date:** 2026-06-13.
**Source slice:** P6.8 L1 slice 1/3 (`docs/briefs/049-P6-L1-uds-read-client-crate.md`) — the `ui/gateway-uds` read-client crate (`security-reviewer` PASS, 0 findings). The first slice of the live `UdsGatewayPort` go-live transport.

The ui consumes the daemon over a UDS JSON-RPC wire. The ui (a Tauri app) **OWNS** the transport client; the daemon **OWNS + documents** the wire contract (route wire questions to the daemon orchestrator; seed from `daemon/src/smoke.rs` `nexusopsd smoke dev-client`). The deterministic transport logic (the §6.4 codec, the handshake, the `ServerFrame` demux) is built as a **standalone pure Rust crate** — separable from Tauri AND from the socket I/O, so it is TDD-able in isolation.

- **A pure crate over a generic stream — TDD'd against a fake stream.** The codec/handshake/`call` operate over a generic `io::Read`/`io::Write` (a fake in-memory stream in tests: a pre-loaded reads-cursor + a writes-capture), so the deterministic core is fully unit-tested with **no Tauri toolchain, no running daemon**. The real `UnixStream` connect is a thin `connect_and_call` adapter exercised ONLY by a `#[ignore]`/feature-gated integration test (the LESSON [[18]]/§24-daemon logic-vs-I/O split, on the Rust side).
- **Depend on `nexusops-shared` for the FROZEN frame types — never the daemon crate.** The client (de)serializes the shared `HelloFrame`/`RpcRequest`/`ServerFrame`/`RpcResponse`/`WireError`/`Capabilities` (`deny_unknown_fields` = parse-don't-trust); it re-implements only the trivial 4-byte-BE-len+JSON codec (seeded from `daemon/src/ipc/transport.rs`). The ui must NOT pull the daemon crate (a layering inversion). Codec-to-`shared` promotion is a cross-track follow-on if the ~15-line dup ever bites.
- **Fail-closed boundary discipline (the security surface, even reads-only).** (1) The **8 MiB frame bound is enforced from the 4-byte PREFIX alone** — rejected before any body alloc/read (the anti-DoS pin: prove the body is ABSENT). (2) A `WireError` returns the **verbatim §6.4 `IpcErrorCode`** (no swallow — the consumer maps it). (3) **Be STRICTER than the dev-client seed:** a dual-`None` `RpcResponse` (neither result nor error) is a `Protocol` error, NOT `Ok(Null)` — the §6.1 "exactly one of result/error" contract (the `smoke.rs` dev rig's leniency is a dev affordance, not a production contract). (4) A non-`RpcResponse` frame on a single-shot read, an id-mismatch, a version-skew, an unexpected handshake frame → typed errors, never a silent proceed. **The handshake reads a BARE `HelloAck`|`VersionSkewError`** (not `ServerFrame`-wrapped) pre-RPC; the call reads a `ServerFrame` — distinct demuxes.

**Rule:** build the ui-side transport core as a pure Rust crate over a generic stream (codec/handshake/demux TDD'd vs a fake stream; the real socket a thin gated adapter); depend on `nexusops-shared` for the frozen frame types (`deny_unknown_fields`), never the daemon; fail-closed at the boundary (8 MiB pre-alloc bound, WireError-verbatim, dual-None→Protocol, typed errors over silent-proceed) — be stricter than a dev-rig seed. `security-reviewer` on the transport boundary even for reads. Slice 050 wires it (Tauri host + `#[tauri::command]` bridge + the TS `UdsGatewayPort` + the Shell read-swap); 051 the streaming `subscribe` + reconnect recovery + unique correlation ids.

**Enforcement:** `pin: ui/gateway-uds/src/lib.rs #[cfg(test)] (codec roundtrip + 8MiB-pre-alloc + handshake-ack/skew/protocol + call demux/wire-verbatim/non-rpcresponse/id-mismatch/dual-None→Protocol + unknown-field) + tests/integration.rs (#[ignore] live socket); security-reviewer the transport boundary.` `accepted: connect_and_call is exercised only by the #[ignore] integration (the deterministic core is fake-stream-covered); the one-shot id=1 → slice 051's persistent connection needs unique correlation ids.`

## <a id="21"></a>21. The Tauri read-command bridge is a NARROW typed allowlist — one `#[tauri::command]` per read method, never a generic `gateway_call`; the registered set IS the allowlist

**Date:** 2026-06-13.
**Source slice:** P6.8 L1 slice 2/3 (`docs/briefs/050-P6-L1-tauri-host-and-read-command-bridge.md`) — the Tauri host + the Rust read-command bridge (`security-reviewer` PASS, 0 findings). The UI became a Tauri app; the frontend reaches the daemon (via the [[20]] transport core) ONLY through these commands.

The Tauri frontend can't open a Unix socket — it goes through `#[tauri::command]` Rust bridge fns. **Custom app commands are accessible-by-default** (no per-command capability entry; `core:default` covers them) — so **the registered command set IS the allowlist.** That makes the command granularity a SECURITY decision.

- **Typed-per-method, NEVER a generic `gateway_call(method, params)`.** Register one command per read method (`gateway_get_projection`/`gateway_get_diff`/`gateway_get_capabilities`) with the **method as a hardcoded `&'static str`** — so the frontend can invoke ONLY the enumerated reads. A generic `gateway_call` would expose **arbitrary daemon methods** to the frontend, including the L2 mutations (`submit_action`/`approve`/`deny`) — a privilege escalation past INV-SEC-1's human-gating. The narrow set is least-privilege **by construction**; the security-reviewer checks the registered set.
- **The error mapping carries the verbatim §6.4 code + leaks nothing.** `ClientError` → a serializable `GatewayCommandError` (`#[serde(tag="kind")]`): `Wire{code}` carries the **verbatim §6.4 `IpcErrorCode` via the frozen serde repr** (never hand-mapped → no drift; the TS `describeRejection` routes it); a **distinct `Internal` variant** for a host runtime fault (a `spawn_blocking` join error ≠ a wire `Protocol` violation); the daemon `Value` crosses ONLY on success; `UnixStream::connect`'s `io::Error` does not embed the socket path. Reads-only, structural-error-only — nothing sensitive crosses.
- **Pure-fn TDD core + `spawn_blocking` for the blocking connect.** The param-marshaling + the `ClientError`→`GatewayCommandError` mapping are pure fns (TDD'd); the `#[tauri::command]` wrapper is a thin `marshal → connect_and_call → map` glue; the blocking [[20]] connect runs **off the Tauri async runtime via `tauri::async_runtime::spawn_blocking`**. The Tauri macro + the socket aren't unit-tested (a `#[ignore]`'d smoke covers the live round-trip — a `#[tauri::command]` needs a running app).
- **CI: the macOS app crate is excluded from the ubuntu `cargo --workspace`.** `ui/src-tauri` (a workspace member) can't build the Linux webkit deps → exclude `nexusops-ui` from the ubuntu daemon job (`--exclude`) + cover it in a **`macos-latest` job** (`cargo test -p nexusops-ui`, WebKit built-in). `cargo fmt --all` is fine on ubuntu (it parses, doesn't compile). The `gateway-uds` crate ([[20]], pure Rust) stays a Linux-CI member.

**Rule:** a Tauri command bridge to a privileged backend is a NARROW typed allowlist (one command per safe operation, the method hardcoded — the registered set is the allowlist; never a generic pass-through that exposes arbitrary backend methods); the error mapping carries the verbatim backend code + leaks nothing; pure-fn marshal/map TDD'd, the blocking call off the async runtime via `spawn_blocking`; the macOS app crate is excluded from the cross-platform (ubuntu) `cargo --workspace` CI + covered by a macOS job. `security-reviewer` on the bridge boundary. The TS consumer (`UdsGatewayPort` + the Shell read-swap + the boundary Zod-parse) = slice 051.

**Enforcement:** `pin: ui/src-tauri/src/commands.rs #[cfg(test)] (wire-verbatim-code + distinct-kind-no-collapse + the params frozen-round-trip) + tests/smoke.rs (#[ignore]); .github/workflows/ci.yml (ubuntu --exclude nexusops-ui + the ui-tauri macOS job); security-reviewer the bridge boundary (allowlist + leak-free + least-privilege core:default).` `accepted: csp:null (the scaffold local-bundle default) → a pre-ship CSP-hardening carry-forward; the placeholder scaffold icons → real branding later.`

## <a id="22"></a>22. The TS `UdsGatewayPort` is a parse-don't-trust read client — a daemon wire-rejection is PLAIN data, a transport fault is an Error (so the consumer classifies them right)

**Date:** 2026-06-13.
**Source slice:** P6.8 L1 slice 3a (`docs/briefs/051-P6-L1-ts-uds-gateway-port-and-shell-read-swap.md`) — the TS `UdsGatewayPort` single-shot reads + the Shell read-swap (`security-reviewer` CLEAR). The app now reads REAL daemon data on load (the live read transport's TS half; the subscribe streaming = 052).

The real `UdsGatewayPort` (`ui/src/gateway-client/uds.ts`) implements the `GatewayPort` READ methods by `invoke`-ing the [[21]] Tauri commands. It is the seam where untrusted daemon payloads enter the TS view layer — so **every payload is Zod-`.parse()`d at the `boundary.ts` seam** (parse-don't-trust; the read methods gained `parseDiff`/`parseCapabilities` alongside `parseProjectionPage`/`parseDelta`); a malformed payload → `BoundaryValidationError`, **never returned to view code**.

- **The load-bearing distinction: a daemon WIRE-rejection vs a TRANSPORT fault — classify by `instanceof Error`.** A daemon `WireError{code}` (a §6.4 rejection — the daemon spoke, and said no) is surfaced as **PLAIN data** (`{code}`, **NOT an `Error`**) so `WireError.safeParse` succeeds and the consumer (the seam / `DiffReview` / `describeRejection`) routes the **verbatim §6.4 code** via the `instanceof Error`-NO branch ([[16]]). A **transport / host fault** (the socket died, the Tauri bridge faulted) is surfaced as an **`Error` instance** (honest degrade, §11.7) — a real failure, not a daemon verdict. Conflating them (throwing a wire-rejection as an `Error`, or a transport-fault as plain data) breaks the consumer's classification + the §6.4-card routing.
- **Fail-safe degrade gate.** A **non-`wire` fault, except `version_skew`** (the version-negotiation axis, handled separately), drives `markDisconnected` (connected→disconnected → the §11.1/§11.4 degraded banner; `canSubmitIntent` stays false). The fail-safe connection state goes `connecting`→`connected` on the first daemon response ([[4]] / §11.4); never optimistic.
- **The read client cannot reach a mutation.** The mutation methods (`submit_action`/`approve`/`deny`/`preview_action`) + `subscribe` + `subscribe_terminal` **throw a not-wired (L2/052) error + never call `invoke`** — pinned (reads-only L1; the mutation transport is cat-1 HELD on the daemon's 0.30.0 ②-mini). The Shell defaults to `UdsGatewayPort` for reads (the `MockGatewayPort` stays injectable for tests).

**Rule:** a transport-client at the untrusted-backend boundary Zod-`.parse()`s EVERY payload (parse-don't-trust); it classifies a **backend protocol-rejection as PLAIN data** (so the consumer routes the verbatim code) vs a **transport/host fault as an `Error`** (honest degrade) — never conflate (the consumer classifies by `instanceof Error`, [[16]]); a non-protocol fault drives the fail-safe degraded gate; un-wired methods throw + never reach the backend. Extends [[16]]/[[20]]/[[21]]. `security-reviewer` on the read boundary even reads-only.

**Enforcement:** `pin: ui/src/gateway-client/uds.test.ts (wire_error_is_plain_code_not_error + transport_io_fault_is_error_instance + malformed→BoundaryValidationError + connecting→connected + io-fault→disconnected + mutation_methods_throw_not_wired_and_never_invoke) + Shell.uds-swap.test.tsx; security-reviewer the read boundary.` `accepted: the LIVE rendered visual gate (real daemon data in the Tauri window) is a manual cross-track operator step (no daemon in the ui worktree) — the real-shaped render + the degrade surface are unit-verified + the production build is green ([[10]] honesty); the subscribe streaming + the robust cross-state reconnect() = 052.`

## <a id="24"></a>24. A frozen projection-row reconciles provisional→frozen-shadow; a oneOf-const enum stays a drift-pinned shadow; the approval card reads real risk from the served row (resolves the fixture-risk [med])

**Date:** 2026-06-14.
**Source slice:** P6.8 L2-prep `053` (`docs/briefs/053-P6-L2prep-regen-0.31-and-approval-row-swap.md`) — the 0.28→0.31 boundary-merge regen + the `ApprovalQueueRow`/survival reconcile + the approval-card fixture→real-risk swap (`security-reviewer` CLEAR). The 044 [med] is RESOLVED on the live-approvable (HIQ) path.

On a contract-bump reconcile ([[14]] is the regen discipline; this is the **object/enum reconcile shapes** it produces):

- **A frozen PROJECTION-ROW (e.g. `ApprovalQueueRow`, the first) → a hand-declared FROZEN-SHADOW** (NOT generated — `gen-contracts.mjs` emits flat value-sets, not objects): the banner is removed, the field-set + field-**types** are **snapshot-pinned to `shared/src/projections.rs`** (the cross-area shared-contract seam — field-name + type, [[14]] trap), enum fields **delegate to the generated value-sets** (`risk_level`→`RiskLevel` / `status`→`ApprovalStatus` / `requester_type`→`RequesterType`), object fields to their shadow (`policy_decision`→`PolicyDecision`), `.strict()` per `deny_unknown_fields`, optionals present-and-nullable.
- **A doc-commented `oneOf`-of-`const` enum (`ResumeMode`/`RecoveryState`) stays a DRIFT-PINNED SHADOW** (the [[2]]/MetricQuality pattern — the flat-enum generator can't emit it; a generator `oneOf`-const extension is a carry-forward). A grown enum (`ResumeMode` 2→4: `relaunched`/`reattached_live`) must cover the new values downstream **with no fall-through** — a `Record<ResumeMode>` (`RESUME_MODE`) makes the compiler force it. A wrapper the daemon did NOT freeze (`RecoveryStatus`) stays provisional, re-based over the generated/shadow enum.
- **The approval card sources real `risk_level`/`policy_decision` from the SERVED `ApprovalQueueRow` projection, never fixture/UI-derived** ([[17]] — resolves the 044 fixture-risk [med]). **Two enrichment paths:** `enrichApproval(row)` (the HIQ/existing-approval card — the row is IN HAND, the clean swap, landed 053) vs `enrichHunkAction(actionType, ack, gateway)` (the DiffReview per-hunk POST-submit — no row in hand; a `get_projection("ApprovalQueue")` **re-fetch + EXACT `action_request_id` match** [never fuzzy/first-row — a wrong approval's risk must never surface] + an **honest "awaiting"** when absent; the per-hunk path = 053b, **deferred** [exposed-ahead-of-L2 — the submit stays HELD, no live gap]). The re-fetch is parse-don't-trust ([[22]]). **A `Pick<GatewayPort,"get_projection">`-narrowed enrich param is a COMPILE-TIME no-mutation-reach proof** (stronger than a runtime spy — the fn structurally cannot call submit/approve/deny) — the 053b technique, kept for the fresh team.

**Rule:** a frozen projection-row reconciles to a field/type-snapshot-pinned **frozen-shadow** (enums delegated, `.strict()`); a `oneOf`-const enum stays a **drift-pinned shadow** (force new values via a `Record<Enum>`); the approval card reads real risk from the **served row** (never fixture, [[17]]) — in-hand → direct, post-submit → an exact-id re-fetch (absent → honest pending, never fabricated); narrow an enrich's gateway param to the methods it needs (a compile-time no-mutation-reach proof). Extends [[2]]/[[14]]/[[17]]/[[22]]. `security-reviewer` on the approval-card swap.

**Enforcement:** `pin: ui/src/contracts/provisional.test.ts (ApprovalQueueRow 14-field/.strict snapshot vs the schema; ResumeMode 4-value + RecoveryState oneOf pins) + generated.test.ts (CONTRACT_VERSION 0.31.0 + member-set, self-maintaining) + views/.../model.test.ts (describeResumeMode all-4 no-fall-through) + shell/display-meta.test.ts (enrichApproval real-row risk/policy; the per-hunk enrichHunkAction exact-match/absent-pending = 053b); security-reviewer the approval-card swap.` `deferred: 053b (the per-hunk enrichHunkAction real-risk swap) → the fresh team, pre-L2; the gen-contracts.mjs oneOf-const generator extension (retires the ResumeMode/RecoveryState/MetricQuality shadows).`

## <a id="23"></a>23. The subscribe transport is a dedicated persistent connection → a Tauri Channel → a TS AsyncIterable; it terminates on close/lag and recovers by re-snapshot, never client gap-fill (L1 COMPLETE)

**Date:** 2026-06-14.
**Source slice:** P6.8 L1 slice 4 (`docs/briefs/052-P6-L1-subscribe-streaming-and-reconnect-recovery.md`) — the streaming `subscribe` consumer + the reconnect recovery (`security-reviewer` CLEAR on all 5 streaming invariants). **L1 — the live read transport — is now COMPLETE:** the app reads real daemon data on load ([[22]]) AND stays live as the daemon mutates.

The single-shot read transport ([[20]]/[[21]]/[[22]]) is request/response; **streaming is the FIRST persistent connection** and a different shape end-to-end:

- **The Rust core is a dedicated persistent connection** (`ui/gateway-uds` `subscribe_stream`): handshake → the `subscribe` RPC (id 1) → demux the ack via the shared `demux_rpc_response` helper (a `WireError` ack fails closed verbatim; the `call`→helper refactor is behaviour-preserving so the single-shot tests stay the regression guard) → **loop reading `ServerFrame::SubscriptionPush(ProjectionDelta)`** to a sink. The connection adapter (`connect_and_subscribe`) sets **NO read timeout** (unlike the 30 s single-shot — a healthy idle subscription blocks on the next push; only the daemon's lag-close ends it); it issues **one** RPC then only reads, so id 1 is correct and **multiplexed correlation-ids stay deferred** (the daemon MVP is 1-subscription-per-connection, terminal post-ack). The 8 MiB pre-alloc frame bound holds on every push; a non-push frame mid-stream is a `Protocol` violation; a sink `ControlFlow::Break` (the dropped-Channel teardown) ends the loop with **no leaked read**.
- **The bridge is a Tauri `ipc::Channel<SubscriptionEvent>`** — a tagged `{delta|closed|error}` marshaling enum (a **ui-host-local** type, NOT a shared contract) carrying distinct stream-end vs error (§11.7); the command `spawn_blocking`s the dedicated connection and **drops the old thread/stream before a re-subscribe** (no leaked subscription thread). Still a NARROW typed allowlist — **no mutation / `gateway_call`** ([[21]] holds).
- **The TS consumer is an `AsyncIterable`** (`subscriptionIterable`) that **`parseDelta`-parses every frame at `boundary.ts`** (parse-don't-trust — a malformed delta → `BoundaryValidationError`, never yielded), ends on `closed`, throws on `error`. A `delta-reducer` applies each `ProjectionDelta` to the projection page (upsert/remove by id) as a **read cache only** (forbidden #2 — never authoritative), guarded to ignore a delta for another projection.
- **Close-on-lag recovery, never client gap-fill.** `ProjectionDelta` carries **no `seq`** → a gap is client-undetectable → the daemon **closes the connection on lag** (daemon LESSON 12) and the client's recovery (`runSubscriptionSupervisor`) is **reconnect → re-`get_projection` (a fresh snapshot baseline) → re-subscribe** — NOT a client gap-fill. **Re-snapshot BEFORE re-subscribe** (deltas resume onto a fresh base — avoids a stale-base race); the cost is a tiny re-subscribe gap window (a delta in the snapshot↔subscribe interval is missed until the next delta for that row), an accepted MVP limitation (self-heals; the seq-less model can't detect gaps anyway). The machine degrades the connection on close (`canSubmitIntent` false §11.1), walks only legal `canTransition` hops, exits cleanly on abort (no leaked loop), and **never shows stale-as-live** (§11.7).
- **Land the live mechanism on ONE representative projection (Session), spread the rest.** The transport mechanism is the L1 deliverable and is proven end-to-end on the most-dynamic projection; the other 5 reuse it (a per-projection subscribe-effect + reducer call) — a mechanical follow-on, not new capability.

**Rule:** a streaming subscription is a dedicated persistent connection (no read timeout; one RPC then push-only) demuxed to a sink, bridged by a typed host-local Channel (drop the thread before re-subscribe — no leak), consumed as a parse-don't-trust `AsyncIterable`; when the wire has no gap-detection (`seq`-less deltas) the recovery is reconnect → **re-snapshot → re-subscribe** (never client gap-fill), degraded-gated and never stale-as-live. Prove the mechanism on one representative stream + spread. Extends [[16]]/[[20]]/[[21]]/[[22]]. `security-reviewer` on the streaming boundary.

**Enforcement:** `pin: ui/gateway-uds/src/lib.rs (subscribe_stream_yields_each_push_delta + terminates_on_close + ack_wire_error_fails_closed + push_over_8mib_rejected + non_push_frame_mid_stream_is_protocol + sink_break_stops_early) + ui/src/gateway-client/{uds,delta-reducer,subscribe-recovery}.test.ts (parseDelta-per-frame + ends/errors + mutation-still-throws + upsert/remove-by-id + reconnect→re-get_projection→re-subscribe + legal-transition walk + abort-no-leak) + Shell.subscribe.test.tsx; security-reviewer the streaming boundary.` `accepted: the LIVE Tauri-window visual gate (real deltas rendering) is a manual cross-track operator step (no daemon in the ui worktree) — verified via the integration test + the reducer/recovery unit tests + the production build + the #[ignore] live subscribe probe ([[10]] honesty).` `follow-on: the two-writer connection-state reconcile (a single liveness authority) BEFORE L2 — docs/planning/052-connection-state-two-writer-finding.md; the idle-unmount thread linger (deterministic teardown); the other-5-projection live-delta spread.`

## <a id="25"></a>25. The daemon-connection state has a SINGLE authority (the port) with a fail-safe asymmetric upgrade — an ad-hoc read can never mask a degraded subscribe stream

**Date:** 2026-06-14.
**Source slice:** 054 (`docs/briefs/054-P6-L2prep-connection-state-single-authority-reconcile.md`) — the two-writer connection-state reconcile; resolves the 052 routed Finding (`docs/planning/052-connection-state-two-writer-finding.md`). `security-reviewer` CLEAR (race/TOCTOU pass, 0 findings). One of the two pre-L2 NON-cat-1 gates (with 053b); the last before the L2 cat-1 checkpoint.

After 052 the Shell's `connection` React state had **two unreconciled writers**: the read path (`onConnectionChange(setConnection)` — `UdsGatewayPort.markConnected/markDisconnected` on read success/fault) and the subscribe supervisor (a 2nd raw React `setConnection` driving `disconnected/reconnecting/connected` from the live stream's health). A successful single-shot read (e.g. a `get_diff` from the Code view) could fire `markConnected` and **momentarily mask a supervisor stream-degrade** → `canSubmitIntent` read `true` while the live subscription was actually down — the exact inversion the fail-safe gate forbids ([[4]], forbidden #6). The daemon Gateway is the load-bearing INV-SEC-1 chokepoint, so this is a defense-in-depth weakening, not an exploit — but L2 makes `canSubmitIntent` load-bearing (a real human approving a real, risk-classified mutation), so the gate must be single-authority + fail-safe-correct first.

- **Single authority = the port.** `GatewayPort` gains `notifyConnectionState(next)` — the supervisor's drive, routed through the port's ONE guarded `setConnection` (`canTransition` + notify). The Shell binds the supervisor's `setConnection` dep to `client.notifyConnectionState`; the 2nd raw React setter is removed → **`onConnectionChange` is the SOLE React connection writer.** Every liveness signal (read + stream) now flows through one source of truth.
- **Fail-safe asymmetry.** A `streamDegraded` axis (set by `notifyConnectionState`, **derived from the COMMITTED connection state AFTER the guarded transition** — so a rejected/no-op hop never leaves it set for a state never entered) gates the read path: `markConnected` (the UPGRADE) is **suppressed while `streamDegraded` ∈ {disconnected, reconnecting}**; `markDisconnected` (the DEGRADE) is **never** suppressed (fail-safe on both axes). Recovery (`notifyConnectionState("connected")` after the supervisor's re-snapshot) clears the suppression. So a DEGRADE flows freely from either signal, but an UPGRADE to `connected` requires the stronger continuous signal (the subscribe stream) to be healthy — an ad-hoc read can never claim `connected` over a down stream.
- **The supervisor stays pure.** `runSubscriptionSupervisor` is UNCHANGED (it keeps its injected `setConnection: (state)=>void` dep — unit-testable with a fake); only the **Shell binding** moved (raw React setter → `client.notifyConnectionState`). The reconcile is a wiring change, not a supervisor rewrite.
- **The mock keeps two setters.** `MockGatewayPort.notifyConnectionState` applies the guarded transition (mirrors the real port); `setConnectionState` stays the **raw, unguarded** test-staging setter — so the §11 `DegradedBanner` tests that stage connection states directly still pass (the entanglement the Finding named is preserved, not broken).

**Rule:** the daemon-connection state has a SINGLE authority — the port; every liveness signal flows through one guarded `setConnection` and the view has exactly one writer (`onConnectionChange`). The fail-safe asymmetry: a DEGRADE flows freely; an UPGRADE to `connected` is suppressed while a stronger continuous signal (the subscribe stream) is degraded — derive the suppression flag from the COMMITTED state (a rejected hop must not set it). Keep the orchestrator (supervisor) pure — bind its connection dep to the port, don't add a 2nd raw view-state writer. Extends [[4]]/[[22]]/[[23]].

**Enforcement:** `pin: ui/src/gateway-client/uds.test.ts (read_success_does_not_upgrade_while_stream_degraded + read_success_suppressed_during_reconnecting_too + read_success_upgrades_when_stream_healthy + both_axes_degrade_fail_safe + stream_recovery_returns_to_connected_and_clears_suppression + notify_connection_state_applies_guarded_transition + read-after-rejected-hop-still-upgrades) + mock.test.ts (notifyConnectionState guarded; setConnectionState raw) + Shell.test.tsx (supervisor drives connection THROUGH the port; single writer); security-reviewer the fail-safe gate.` `accepted: the SOLE-React-writer guarantee rests on inspection (the raw setter was removed) + the port-level masking tests (the real behavioral proof) — a mechanical "no other React writer" assertion would need a brittle setState intercept; not worth the brittleness.` `follow-on: L2 makes canSubmitIntent load-bearing — this single-authority gate is the prerequisite.`

## <a id="26"></a>26. The L2 mutation transport REUSES the L1 read crate's demux unchanged — a mutation helper is params-formation + typed-deserialize, and the verbatim §6.4 code is what keeps the rejection cards distinct

**Date:** 2026-06-14.
**Source slice:** 055 / L2-A (`docs/briefs/055-P6-L2A-crate-mutation-rpcs.md`) — the FIRST L2 sub-slice (lead-ruled L2-O1=(B) foundation-first: A crate RPCs → B Tauri+TS wire [disabled] → C enable-live [USER-gated]). `security-reviewer` CLEAR (0 findings). The L2 cat-1 checkpoint Part A (`docs/planning/L2-live-mutation-transport-cat1-checkpoint.md`).

The ui-side live mutation transport begins where the L1 read transport ([[20]]) left off: the `ui/gateway-uds` crate gains the 4 §6.1 mutation-intent RPC helpers (`submit_action(ActionRequest)→ActionAck` · `preview_action(action_request_id)→ActionPreview` · `approve(approval_id, step_id?)→ActionAck` · `deny(approval_id, reason)→ActionAck`) — and each is just **params-formation + the EXISTING `call(stream, method, params, id)` + typed-deserialize**, mirroring `get_diff`/`get_capabilities`. The shared `call`/`demux_rpc_response` is REUSED unchanged.

- **The §6.4 contract is identical for reads and mutations.** A `WireError{code}` → `Err(ClientError::Wire(code))` **verbatim, never collapsed/remapped**; a non-`RpcResponse`/id-mismatch/dual-None → `Protocol`; a malformed/extra-field or non-`ActionAck` result → `Serde` (typed fail-closed). So no mutation-specific demux is needed (or wanted — a fork would risk drift).
- **The verbatim code is load-bearing downstream.** Because the crate returns the §6.4 code untouched, the card layer (L2-C) can route `fencing_conflict`→the never-auto-resolved hard-conflict card (#6) distinct from `precondition_stale`→re-approvable / `policy_denied`→deny / `internal_error`→fail-closed. A collapse here would break #6 at the UI — the inverse of [[16]]/[[17]].
- **Pure pass-through (L2-D1).** The helpers SEND a typed RPC and return the daemon's typed result — no execution, no state-holding (the daemon Gateway is the sole mutator + the INV-SEC-1 chokepoint). The crate deps `nexusops-shared` ONLY (no daemon dep — layering, [[20]]).
- **Idempotency/fencing pass-through (L2-O4).** `submit_action` serializes the `ActionRequest` as-is — its `idempotency_key`/`fencing_token`/`resource_refs` ride opaquely; the daemon owns dedup + fencing. Pinned by a round-trip (the written params deserialize back to the same `ActionRequest`).
- **Foundation-first exposed-ahead.** No Tauri command + no TS caller yet — the helpers are reachable only from the crate's own tests (the 049 `get_diff` exposed-ahead pattern); the production wire is L2-B, the live-enable L2-C (USER-gated).

**Rule:** the L2 mutation transport is the L1 read transport's demux reused unchanged + params-formation + typed-deserialize; keep the §6.4 `WireError` code VERBATIM (never collapse — the distinct §11.5 rejection cards depend on it) and the helper a pure pass-through (SEND only, no execution — the daemon Gateway is the chokepoint). Build it foundation-first (the pure crate in isolation, security-reviewed, before any UI path reaches it). Extends [[20]]/[[16]]/[[17]].

**Enforcement:** `pin: ui/gateway-uds/src/lib.rs mod tests (submit_action/preview_action/approve(+omit-step_id-when-none)/deny params+typed-return + mutation_wire_error_returns_verbatim_code [submit_action fencing_conflict + approve precondition_stale] + mutation_malformed_result_is_serde_error + mutation_non_rpcresponse_or_id_mismatch_is_protocol + the ActionRequest round-trip opaque pin); security-reviewer the mutation transport (L2 cat-1 Part A — verbatim §6.4 / no execution path / typed fail-closed / nexusops-shared-only / no param-injection).` `follow-on: L2-B (the Tauri mutation commands + the TS UdsGatewayPort live wire, consumer-disabled) + L2-C (the USER-gated live-enable); the mutation connect adapter lands in L2-B.`

## <a id="27"></a>27. The L2 mutation bridge mirrors the read allowlist; ONE `mutationsEnabled` gate the port AND the controls honor is the single user-gated go-live switch

**Date:** 2026-06-14.
**Source slice:** 056 / L2-B (`docs/briefs/056-P6-L2B-mutation-bridge-and-ts-wire-disabled.md`) — the SECOND L2 sub-slice (lead-ruled L2-O1=(B) foundation-first). `security-reviewer` CLEAR (0 findings). The L2 cat-1 checkpoint Part A + the L2-O3 user-gate (`docs/planning/L2-live-mutation-transport-cat1-checkpoint.md`).

L2-B builds the FULL mutation wire — the host bridge + the TS port — but keeps it **provably unreachable in production** until the USER-gated L2-C flip.

- **The bridge mirrors the read allowlist.** Four typed `#[tauri::command]`s (`gateway_submit_action`/`preview_action`/`approve`/`deny`) mirror the L1 read commands: marshal params → the EXISTING `call_daemon(method, params)` → `connect_and_call` → raw `Value` / leak-free `GatewayCommandError`, reusing `map_client_error` so the §6.4 `Wire{code}` rides VERBATIM; registered in `lib.rs`. **Still NO generic `gateway_call`** — the registered set IS the allowlist ([[21]]). The TS `UdsGatewayPort` methods `invoke` those commands + boundary-parse the typed `ActionAck`/`ActionPreview` (parse-don't-trust), with the read path's wire-rejection-plain vs transport-fault-`Error` classification ([[22]]) — both correct for mutations (a `WireError` leaves the connection unchanged; a transport fault degrades; the [[25]] suppression applies).
- **One go-live gate, two honorers.** `mutationsEnabled` is a single **`GatewayPort` interface field** that BOTH the port (the mutation methods **throw-not-enabled + NEVER `invoke`** when false) AND the UI controls (`GatewayModal` approve/deny + `DiffReview` submit disabled via `canSubmitIntent && mutationsEnabled`) honor. So the disabled state is **honest** (§11.7) — wired but disabled, NOT enabled-buttons-that-throw (the poor decision surface the user would otherwise review).
- **Three defense-in-depth layers** when `mutationsEnabled` is false: the control disabled, the seam returns readOnly, the port throws-never-invoke. So NO production path reaches a live mutation — **verified repo-wide** that nothing passes `mutationsEnabled:true`.
- **UdsGatewayPort false / Mock true.** The production port defaults **false** (the live mutation path off); the Mock defaults **true** so the existing Mock-driven UI-flow tests (the seam-routing click) stay green — and the Mock **can't leak** (production uses `UdsGatewayPort`, never the Mock). A `readonly` write-once flag (no runtime flip — fixed at construction).
- **The user-gated go-live is one flip.** L2-C is `new UdsGatewayPort({mutationsEnabled:true})` — the single switch that lights up the transport AND the controls together. The full wire is built + security-reviewed under lead authority (slices A/B); the live-enable (slice C) is **HELD for the user's explicit sign-off** (the lead's `AskUserQuestion`, the L2-O3 ruling) — the trust boundary going live is the user's call.

**Rule:** the mutation bridge mirrors the read allowlist (one typed command per method, no `gateway_call`, `map_client_error` verbatim); gate the go-live on ONE `mutationsEnabled` flag that BOTH the transport (throw-never-invoke) AND the controls (disabled) honor — the honest-disabled interim (§11.7), three defense-in-depth layers, provably unreachable until the single user-gated flip. Build the wire under lead authority; gate the live-enable on the user. Extends [[21]]/[[22]]/[[26]].

**Enforcement:** `pin: ui/src-tauri/src/commands.rs mod tests (4 mutation commands params + verbatim map_client_error) + ui/src/gateway-client/uds.test.ts (mutation_methods_throw_not_enabled_by_default_and_never_invoke + invoke-and-parse-when-enabled + wire-rejection-plain-vs-transport-Error) + GatewayModal.test (approve_deny_disabled_when_mutations_not_enabled) + DiffReview.test (per_hunk_buttons_disabled_when_mutations_not_enabled); security-reviewer the mutation bridge (L2 cat-1 Part A — 3 no-reach layers / narrow allowlist / verbatim §6.4 / parse-don't-trust / Mock-true-can't-leak).` `follow-on: L2-C (the USER-gated live-enable) — HELD for the user sign-off via the lead's AskUserQuestion.`

## <a id="28"></a>28. The cat-1 go-live = a single flag flip gated on explicit user sign-off + a live-verification operator gate; the deterministic surface is one line + the production-click→live-invoke pin

**Date:** 2026-06-14.
**Source slice:** 057 / L2-C (`docs/briefs/057-P6-L2C-enable-live-mutation-USER-GATED.md`) — the THIRD + FINAL L2 sub-slice, the **USER-SIGNED-OFF go-live** (L2-O3). `security-reviewer` CLEAR (0 findings). **L2 COMPLETE.**

L2-C turns on everything L2-A/B built: one line in the production Shell — `new UdsGatewayPort()` → `new UdsGatewayPort({ mutationsEnabled: true })` — the single switch (the [[27]] go-live gate) that lights up the transport (the port methods invoke the live Tauri commands → the daemon Gateway) AND the controls (`canSubmitIntent && mutationsEnabled`) together. A real human can now approve/deny/submit a real, daemon-risk-classified mutation from the cockpit.

- **Gated on EXPLICIT USER SIGN-OFF (L2-O3).** The transport (A/B) is built + security-reviewed under lead authority, but the actual go-live — the trust boundary going live — is the USER's call (the lead's `AskUserQuestion`). The orchestrator escalates a file-based go-live verification surface (`docs/planning/L2C-go-live-verification-surface.md`); the lead obtains the sign-off; only then is slice C dispatched (its build brief held uncommitted until the GO).
- **The deterministic surface is ONE line + 2 production-Shell pins.** Because the Mock defaults `mutationsEnabled=true` ([[27]]), every existing component test already runs mutations-enabled, so the only genuinely-NEW deterministic surface is the **production Shell flip**: (1) the per-hunk submit control ENABLES when connected (the production `UdsGatewayPort` is now mutations-enabled — the flip); (2) a **click** on it fires the live `invoke("gateway_submit_action")` through the production port (the go-live CORE — a real user action reaches the live mutation transport, not just that the control is enabled). The component behaviors (enable/disable/standing-grant/no-optimistic-done/verbatim-cards) are gateway-agnostic → cited from the Mock-default-true coverage.
- **The load-bearing acceptance is the real-daemon live-verification operator gate** — launch the cockpit vs a real daemon → submit/approve a real action → the daemon executes + audits → the cockpit reflects the daemon-confirmed status. This may be **DEFERRED to a documented post-commit operator follow-on** when the user chooses GO-now (NOT a commit gate; the deterministic pins + `security-reviewer` gate the commit).
- **The frame is unchanged (re-pinned live).** The daemon Action Gateway stays the single mutator + the INV-SEC-1 chokepoint (the UI submits intents only); `canSubmitIntent` stays fail-safe + single-authority ([[25]]); no-optimistic-done (status from the daemon ack/projection); verbatim §6.4 rejection cards (`fencing_conflict` never collapses #6, [[26]]/[[27]]); the `policy_grant` "always allow" standing-grant stays DISABLED (its own cat-1).

**Rule:** a cat-1 go-live is a SINGLE flag flip the prior slices' wire honors, gated on EXPLICIT user sign-off (the trust boundary going live is the user's call); the deterministic surface is one line + a production-path pin that a real user action reaches the live transport (not just that a control is enabled); the load-bearing acceptance is a real-daemon live-verification operator gate, deferrable to a post-commit follow-on on a user GO-now. Build the wire under lead authority; gate the live-enable on the user. Completes [[26]]/[[27]].

**Enforcement:** `pin: ui/src/shell/Shell.uds-swap.test.tsx (production_shell_enables_per_hunk_submit_when_connected + production_shell_click_reaches_live_mutation_transport); security-reviewer the go-live (the full live path — UI-submits-only / daemon-is-the-chokepoint / fail-safe-gate-not-bypassed / no-optimistic-done / verbatim §6.4 cards / standing-grant-disabled).` `follow-on: the DEFERRED real-daemon operator walkthrough (post-commit); the policy_grant standing-grant (own cat-1); Q7-B/C caching (parked-for-user); submit_action_plan/per-step; the other-5-projection live deltas.`

## <a id="29"></a>29. A daemon ProjectionDelta is a row:None id-NUDGE → consume via a coalesced refetch-on-nudge (re-read get_projection), never a row-apply reducer; a 2nd live stream composes with the single connection authority via per-stream worst-of aggregation

**Date:** 2026-06-15.
**Source slice:** ui-059 (`docs/briefs/ui-059-approvalqueue-live-subscription.md`) — the live ApprovalQueue subscription (052-Q3, the first served-delta projection beyond Session). `security-reviewer` CLEAR (4 axes). NON-cat-1.

The daemon's `ProjectionDelta` is an **id-only NUDGE** (`row: None` — `runtime/writer.rs` Session + `gateway/pipeline.rs` ApprovalQueue): "row X changed, re-read it." The documented contract (writer.rs) is "the subscriber re-reads the full row via get_projection." So a row-apply reducer (`applySessionDelta`: `if (!row) return rows`) **no-ops against the real daemon** — it only appears to work because a **Mock fixture carries a row the real daemon doesn't** (the 052 Session "live" was Mock-validated, not real-daemon). The CORRECT consumer is **refetch-on-nudge**: onDelta → a **coalesced** re-read `get_projection`, the rows replacing the cache.

- **Coalesce the re-reads** (at-most-one-in-flight + one-trailing, timer-free/injectable): a burst of nudges → ≤2 re-reads, never `get_projection` spam. A re-read REJECTION is swallowed in the coalescer (no unhandled rejection; `inFlight` resets; a queued trailing read still fires) — but the swallow does NOT mask the degrade: the port's read-path (`get_projection`→`markDisconnected` on a transport/boundary fault, [[22]]) already degraded BEFORE the rejection reaches the coalescer, so §11.7 honest-degrade holds; the coalescer just keeps the queue's liveness from wedging on a transient fault.
- **Pin the row:None path against a daemon-SHAPED Mock** (`{projection, kind:"upsert", id, row omitted}`) so the class of Mock-only-green never recurs — the changed row surfaces ONLY via the re-read, never a row-apply.
- **A 2nd live stream composes with the single connection authority ([[25]]/054) via per-stream WORST-OF aggregation, NEVER a 2nd writer:** each stream reports via `notifyConnectionState(streamId, next)` into a per-stream `streamStates` map → a **worst-of aggregate TARGET** (`disconnected > reconnecting > connected`) drives the ONE guarded `setConnection`; **`streamDegraded` still derives from the COMMITTED global post-guard** (NOT the reported map — that preserves 054's rejected-hop guard: a rejected/illegal hop never falsely flips the flag) — and the **aggregate is load-bearing, not cosmetic** (`canSubmitIntent = connection==="connected" && compatible`, so any-stream-degraded ⇒ global non-connected ⇒ the fail-safe gate FALSE; only the disconnected-vs-reconnecting LABEL is cosmetic). A healthy stream can NEVER clear another's degrade (worst-of includes all streams); an empty map (pre-report, at mount) ⇒ `streamDegraded` false ⇒ the load-reads still upgrade (happy path unchanged). The worst-of helper lives in `connection/state.ts` (one copy for a fail-safe aggregate — no drift between uds/mock). `onConnectionChange` stays the Shell's ONE React connection writer.

**Verified daemon surface (do NOT re-assume):** only **Session + ApprovalQueue** emit deltas today (`writer.rs:725` / `pipeline.rs:79`); the other served projections (ProjectActivity/PullRequest/AuditTrail/UsageLedger) have NO emission → live-consuming them is daemon-gated. **Session's FULL real-daemon liveness needs BOTH a UI refetch-on-nudge (it still uses the no-op row-apply) AND a daemon-side status-change delta-emission** (`deltas_for_append` only fires on `SessionStarted`) — a carry-forward Finding.

**Rule:** treat a daemon `ProjectionDelta` as a row:None id-NUDGE → coalesced refetch-on-nudge (re-read `get_projection`), never a row-apply reducer (it no-ops against the real daemon; a row-bearing Mock fixture masks this → pin row:None against a daemon-shaped Mock). Compose a 2nd live stream with the single connection authority via per-stream worst-of aggregation with `streamDegraded` from the committed global (054's guard preserved), never a 2nd writer; the aggregate gates `canSubmitIntent` (fail-safe). Extends [[22]]/[[23]]/[[25]].

**Enforcement:** `pin: ui/src/gateway-client/refetch-on-nudge.test.ts (coalesce + swallow-no-mask) + connection/state.test.ts (worstOfConnection) + uds.test.ts (per-stream aggregation + masking + rejected-hop preserved) + Shell.subscribe.test.tsx (approvalqueue_nudge_refetches_not_row_apply [row:None daemon-shaped] + lag-recovery); security-reviewer the per-stream fail-safe gate.` `follow-on: the Session UI refetch-on-nudge + the daemon-side Session/other-projection delta-emission (daemon-gated).`

> **Update (ui-062/ui-063/ui-064):** the row:None refetch-on-nudge mechanism is now spread across the WHOLE live-relevant served set — Session (ui-062), ProjectActivity/PullRequest/UsageLedger (ui-063), Review (ui-064) — so the "only Session+ApprovalQueue emit" snapshot above is the ui-059-era state; post-D4 the daemon emits for all of them. **AuditTrail stays EXCLUDED** (a blanket-nudge-on-every-event projection → a whole-page refetch storm; a daemon seq-cursor delta is the fix). The **recount-iff-a-counts-input** discipline: a refetch recomputes the switcher `counts` ONLY if the projection is a `deriveProjectSwitcherCounts` input (ProjectActivity/PullRequest/Session/ApprovalQueue recompute; UsageLedger/Review plain-replace).

## <a id="30"></a>30. A value-only generated enum gets a co-located verdict-badge descriptor (Record-completeness), NOT a row in the status→attention-rank table (that table is status MACHINES only)

**Date:** 2026-06-17.
**Source slice:** ui-064 Layer 1 (`docs/briefs/ui-064-P7-2-pr-review-workspace-shell.md`) — the read-only PR Review Workspace reviews-list (`ReviewRow` consumer). NON-cat-1.

`ReviewState` (approved/changes_requested/commented/dismissed/pending) is a generated enum, but it is a fixed **VERDICT/classification** — no lifecycle, no transitions, no terminal/attention semantics. So it is NOT a status MACHINE, and it must NOT go in the cross-doc-tracked status→attention-rank descriptor table (`status/descriptors.ts` — keyed by `(machine, status)`, drift-pinned to the 9-10 frozen status-machine enums, the single source for sidebar weight / queue membership / sort). Adding a verdict there would mis-model it as a lifecycle state and pollute the attention-rank policy.

- **Render it via a co-located verdict-badge descriptor** — `views/code/review-model.ts` `describeReviewState(state) → {glyph, label, tone}`, the badge **never color alone** (glyph + label are the non-color channels; tone is additive).
- **`Record<Enum, …>` completeness** — declare the descriptor map as `Record<ReviewState, BadgeDescriptor>` so `tsc` forces every value (and any future-added value) to render a distinct non-blank badge. This is the [[5]]/[[11]] "force a future state to render" net applied to a VALUE enum, WITHOUT pulling it into the attention-rank table.
- **Type-derivation idiom** — when the generated layer exports a value enum as a value (not a re-usable type), derive the field type from the consumed row: `ReviewRow["state"]` (the row's drift-pinned shadow is the type source).

Mirrors the daemon's value-enum-vs-`status_machine!` distinction (a verdict enum uses the plain-value-enum idiom, not the lifecycle-machine macro).

**Rule:** a generated enum that is a fixed verdict/classification (no lifecycle) gets a co-located badge descriptor with `Record`-enforced completeness (never color alone), kept OUT of the `(machine, status)` attention-rank table (status machines only); derive its field type from the consumed row shadow. Extends [[5]]/[[11]].

**Enforcement:** `pin: views/code/review-model.test.ts (Record-completeness + all-N distinct non-blank labels) + the status/descriptors.ts completeness test staying status-machine-only (a verdict enum never appears there).`

## <a id="31"></a>31. A ui perf bench is the daemon §22 analogue — drive the REAL production component via vitest `bench()` (an honest JSDOM PROXY, not browser paint); realistic-gated guard tighter than the SLO; OWN-cadence, excluded from `test:run`

**Date:** 2026-06-17.
**Source slice:** ui-066 (`docs/briefs/ui-066-P6-6-graph-render-benchmark.md`) — the §18 graph-render benchmark, the FIRST ui bench. NOT a `/tdd` RED/GREEN slice.

The ui-side §18 perf bench mirrors the daemon's bench discipline (`daemon/LESSONS.md` §22) on the Tauri/vitest side:

- **Drive the REAL production component**, not a proxy — render the as-built `ProjectGraph` (the same component the Shell mounts) via vitest `bench()` in the testing-library/JSDOM path, with a **realistic-gated** fixture (built from the real Row shapes, statuses cycled across the frozen enums so the descriptor/attention render path is exercised — never a degenerate fixture). Report a **saturating** case for visibility; **guard only the typical** (the realistic-gated basis).
- **The JSDOM honest-proxy caveat (document it IN the bench):** JSDOM has no layout/paint, so the bench measures **React-render + DOM-construction + model-derivation** (the regression-sensitive logic), a meaningful PROXY for the §18 render budget — it does NOT measure browser paint. A real-browser bench (tauri-driver/playwright) is the heavier follow-up. Per §22 "drive the REAL production entry": don't let a proxy imply it measures what it can't.
- **Calibrate the guard on the as-built measurement + margin, tighter than the SLO** — graph render guard `median < 150 ms` (~3.75× over the as-built p99 to absorb shared-runner noise; ~3.3× under the 500 ms SLO so a real ~4× regression trips first). If the as-built is near/over the SLO, STOP + flag a **§18 re-baseline = USER-ruled** (§22), not a guard tweak.
- **OWN-cadence, never per-slice** — the bench + its guard run at nightly + `/phase-exit` (a `nightly.yml` job mirroring the daemon `perf` job — non-gating, allowed-to-fail-loudly; a breach is a red-nightly Finding-to-review, not a PR block). **Exclude `*.bench.ts` from `test:run`** (timing is flaky in the per-slice loop). **`bench()` + `test()` CANNOT share a file** (`bench()` throws "only available in benchmark mode" under `vitest run`) → a 3-file split: `*.bench.ts` (pure `bench()`, run by `vitest bench`) + `*.bench.guard.ts` (the guard `test()`, run by a dedicated `vitest.bench.config.ts` that includes ONLY the guard) + a shared load-model module.

**Rule:** a ui perf bench drives the real production component via `bench()` (an honest JSDOM proxy — document the no-paint caveat), realistic-gated + saturating-reported, with a guard calibrated tighter than the SLO; it runs OWN-cadence (nightly + `/phase-exit`, excluded from `test:run`, non-gating); `bench()`/`test()` split across files. An as-built near the SLO is a USER-ruled §18 re-baseline, not a guard tweak. The daemon `LESSONS.md` §22 analogue. _(Carry-forward: the `parentLabelOf` O(n²) in ProjectGraph — a known model inefficiency, NOT a current issue at typical sizes; the bench now guards against it worsening.)_

**Enforcement:** `pin: views/graph/graph.bench.ts + graph.bench.guard.ts (the guard median < GUARD_MS) + vitest.bench.config.ts (guard-only include) + .github/workflows/nightly.yml (the ui-graph-bench job); test:run excludes *.bench.ts.`

## <a id="32"></a>32. A nullable display field gets null-safe treatment at EVERY render site, not just the primary label

**Date:** 2026-06-17.
**Source slice:** ui-067 / P6.10 (`docs/briefs/ui-067-P6-10-ui-quality-hardening-bundle.md`) — the null-safe PR-number chip. NON-cat-1.

When a projection field becomes nullable (here `pr_number`, a `u64` → nullable since the ui-061 reconcile), the null-safe handling must be applied at **every** site that renders it — not just the primary/most-visible one. The ui-061 reconcile null-guarded the PR **label** (`DiffReview.tsx:485` `p.title ?? (p.pr_number != null ? PR #${p.pr_number} : p.pr_id)`) but the secondary **`MetaChip`** at `:475` still rendered `#{p.pr_number}` → a bare `#` for a null number. A "the obvious site is fixed" reconcile silently leaves the lagging sites broken.

- **Fix the lagging site to the same null-discipline** — `{p.pr_number != null ? <MetaChip…>#{p.pr_number}</MetaChip> : null}`. A meaning-free badge (a "#&lt;number&gt;" chip with no number) is **omitted**, not rendered empty; the always-present identity (the `pr_id` PK via the label) carries the card.
- **Pin BOTH clauses in one render** — the test renders a dual-null fixture (`title:null` AND `pr_number:null`) and asserts (a) no bare-`#` chip AND (b) identity is preserved via the `: p.pr_id` label fallback (which also covers that otherwise-untested deepest-fallback branch). The omit must never strand the card identity-less.

**Rule:** when a field goes nullable, grep EVERY render site (not just the label) and apply the same null-guard; omit a meaning-free decorative element rather than render it empty, and pin that identity is preserved by the always-present fallback. Extends [[8]] (the projection-item mapper single-source discipline).

**Enforcement:** `pin: views/code/DiffReview.test.tsx (pr_chip_null_safe_when_pr_number_absent — dual-null fixture: no bare-# + pr_id identity preserved). accepted: cross-render-site consistency for a newly-nullable field is not fully mechanically greppable — the discipline is the grep-every-site habit at reconcile time.`

## <a id="33"></a>33. Client input that feeds an audited/recorded field is trimmed/normalized so whitespace can't become the recorded value

**Date:** 2026-06-17.
**Source slice:** ui-067 / P6.10 (`docs/briefs/ui-067-P6-10-ui-quality-hardening-bundle.md`) — the empty-reason deny client guard. NON-cat-1 (defense-in-depth). security-reviewer CLEAR (full §15 invariant PASS).

A UI control whose value rides into an **audited** daemon field (here the `GatewayModal` deny `reason`, recorded on the audit event) must normalize before submit so a blank/whitespace-only entry can't become the recorded value. The old `denyReason || "Denied by the operator"` only caught the empty string — a whitespace-only `"   "` is **truthy** → it slipped through as the audit reason. Fix: `denyReason.trim() || "Denied by the operator"`.

- **Defense-in-depth, NOT the authority** — the daemon Action Gateway is the INV-SEC-1 chokepoint and the real reason-content guard; the UI submits an intent only. The client trim is audit-quality hygiene, not a security boundary (don't over-claim it).
- **Preserve the product decision** — the deny reason is "(optional)" by product choice; the guard NORMALIZES (whitespace → explicit default), it does not force-require a reason (that would be a product change — surface it, don't silently impose it).
- **Known residual (deferred):** zero-width chars (U+200B/ZWNJ/ZWJ/U+2060) survive `.trim()` — a non-whitespace-invisible reason could still ride. Pre-existing + cosmetic (the daemon is the real guard); carry-forward, not in this slice.

**Rule:** trim/normalize any client input that feeds an audited/recorded daemon field so whitespace-only never becomes the recorded value; frame it as defense-in-depth (the daemon is the authority), and normalize rather than newly-mandate unless the product decision says otherwise. Extends [[16]]/[[17]] (the intent-seam is submit-only; the daemon Gateway is the chokepoint).

**Enforcement:** `pin: overlays/GatewayModal.test.tsx (deny_whitespace_reason_never_sent_as_blank — "   " → the default rides, not the whitespace); security-reviewer the deny path. accepted: a general "audited-field input is trimmed" grep is too broad to encode as a forbidden-pattern; the pin covers the deny path.`

## <a id="34"></a>34. The N-step plan-approval modal is a PURE renderer of the daemon's ActionPlan; a daemon-FORMED plan's per-step `risk_level` IS displayable (unlike a UI-formed request's hint, [[19]]); plan-approve rides the existing L2-C gate (no new go-live)

**Date:** 2026-06-21.
**Source slice:** ui-073 / P8.2 (`docs/briefs/ui-073-P8-2-gateway-modal-actionplan-render.md`) — the §11.5 multi-step Gateway Review Modal, frozen-§6.2-contract, exposed-ahead of a live plan-submitter. cat-1 / invariant-touching. security-reviewer CLEAR (8 invariants, 0 findings).

The `PlanModal` renders a bundled `ActionPlan` (1..N steps) as a PURE renderer of daemon-provided values — header `overall_risk` + per-step `risk_level` shown verbatim (never UI-derived); per-step `policy_decision`/preview honest-absent ("—"/pending) when the daemon value is absent (daemon-NULL today), never a synthesized consequence (forbidden #2). The UI never computes which steps are critical/eligible — the approve-all critical-exclusion is **daemon-authoritative** (catalog risk, 2.1c L3); the UI submits plan-level `approve(approval_id)` (no step_id) and lets the daemon exclude.

- **Daemon-formed vs UI-formed risk (extends [[17]]/[[19]]):** [[19]] called `action_request.risk_level` a "never-displayed hint" — but that was a **UI-formed** per-hunk ActionRequest (the UI minted a placeholder the daemon overrode; the card's real number came from `preview_action`). A plan's steps are **daemon-formed** (the daemon assembles the ActionPlan via CatalogPolicy), so each step's `risk_level` IS the daemon's catalog-authoritative per-step risk → displayable, labeled as the daemon's value, honest-absent when absent. The distinction is WHO formed the request, not the field.
- **No new gate (ride L2-C):** the plan approve/deny IS the human control that ENFORCES INV-SEC-1 #10 ("Brain proposes, never executes") — NOT a mutation to gate. It reuses the existing live, user-signed-off L2-C seam (`approve(approval_id, step_id?)`, same `canSubmitIntent && mutationsEnabled`) with NO new gate/flag. The capability that stays gated is the Brain **SUBMITTER** ("Run via Gateway", guarded-disabled in slice A). An empty-gate here would gate the wrong thing + risk regressing the signed-off single-step approve.
- **Additive / zero-regression seam thread:** `IntentSeam.approve` threads an OPTIONAL `step_id`; the single-action call stays `approve(approval_id)` length-1 (never `(id, undefined)`) — pinned as a load-bearing guardrail so the N-step extension never changes the live single-step L2-C path.
- **Post-decision = read-only:** per-step controls lock after a plan-level result (no 2nd mutation from a post-result surface) — the footer-gate discipline applied to the rows. A NEW rendered surface gets its own visual gate ([[10]]/[[12]]) — exposed-ahead, no prod plan data → a manual operator step.

**Rule:** an N-step plan-approval modal is a pure renderer of daemon-provided plan values (per-step risk/eligibility never UI-derived; honest-absent over synthesized); a daemon-FORMED plan's per-step `risk_level` is authoritative + displayable (vs a UI-formed request's placeholder hint, [[19]]); the plan-approve submit rides the existing L2-C gate with no new go-live (the gated capability is the Brain submitter); the optional-`step_id` seam thread stays additive (single-action length-1, guardrail-pinned). Extends [[16]]/[[17]]/[[19]].

**Enforcement:** `pin: overlays/PlanModal.test.tsx (per_step_risk_absent / per_step_policy_absent / approve_all_submits_plan_level_no_step_id / single_action_l2c_approve_path_unchanged [#13 guardrail] / per_step_controls_locked_after_plan_result) + intent/submit-intent.test.ts (approve threads optional step_id; single-action length-1) + overlays/GatewayOverlay.test.tsx (plan_id routing); security-reviewer the cat-1 plan-approval card + the seam thread. accepted: the live-plan visual gate is a manual operator step (exposed-ahead, no prod plan data).`

## <a id="35"></a>35. A Brain shell-binding is a FakeBrain provider exposing the FROZEN ProjectBrain §5.1 status as the single swap-point for the live daemon 8.1 source; the header renders it descriptor-bound; §13.1 honest-degraded is a DISTINCT additive surface that never blocks

**Date:** 2026-06-21.
**Source slice:** ui-074 / P8.2 (`docs/briefs/ui-074-P8-2-brain-status-header-binding.md`) — the Brain drawer/page status-header binding (slice A of the Phase-8 un-deferred Brain arc). NON-cat-1 (read/display + a UI provider; no INV-SEC-1 surface — Run-via-Gateway untouched-disabled). code-quality-reviewer (2 medium fixed in-slice); no security-reviewer.

A Brain UI shell-binding consumes the FROZEN ProjectBrain §5.1 status through a **FakeBrain provider** (`BrainStatusProvider`/`useBrainStatus`, the [[13]] pure-model + thin-context pattern, Shell-root) — a UI-local provisional value `{status, grounded_at, absent}` that is the **single swap-point** the live daemon 8.1 source replaces (there is NO ProjectBrain projection yet; its status is daemon-cached, §13.1). The header renders the status via the **descriptor-bound StatusPill** (glyph+label, never color alone — [[6]]/forbidden #5); `deriveBrainHeader` READS the descriptor (never recomputes) + resolves `absent → not_configured` (never a faked "ready", forbidden #4).

- **§13.1 honest-degraded = a DISTINCT additive surface ([[11]]):** the degraded ProjectBrain states render a Brain-specific degraded block (own testid/class — NOT the connection `DegradedBanner`, never conflated); the platform **never hard-depends / never blocks** — the drawer/page content renders regardless (additive, no throw). The degraded set is a **completeness-drift-pinned `Record` over `ProjectBrain.options`** so a new daemon status value is forced to classify ([[11]]).
- **Staleness is daemon-reported, never client-computed (forbidden #4):** render the daemon's `stale` value + `grounded_at` verbatim; a reserved `_now` is **display-only** (relative-time), NEVER used to derive `stale`. A benign config state (`transcript_ingestion_off`) classifies NON-degraded (not a fault).
- **Shell-binding ≠ content:** the rich answer/evidence/plan panes + EvidenceChip real-freshness stay the existing fixture (deferred until `brain/` output is observable); Run-via-Gateway stays disabled (INV-SEC-1 #10). One header surface covers BOTH page + drawer (the drawer hosts `<BrainPage drawer/>`) — no duplicated binding, every production change test-covered.

**Rule:** bind a Brain UI surface to the FROZEN ProjectBrain §5.1 status via a FakeBrain provider (the [[13]] pattern) that is the single swap-point for the live daemon source; render descriptor-bound (never recompute); §13.1 honest-degraded is a DISTINCT additive surface that never blocks the platform; staleness is daemon-reported, never client-invented; classify ALL enum values via a `.options` completeness drift-pin ([[11]]); keep shell-binding separate from rich content. Extends [[6]]/[[11]]/[[13]].

**Enforcement:** `pin: views/brain/brain-status.test.ts (derive-reads-descriptor / degraded-set-completeness-over-.options / absent→not_configured / staleness-reported-not-computed) + views/brain/BrainStatusHeader.test.tsx (pill-glyph+label / distinct-brain-degraded-surface / healthy→no-degraded / content-renders-when-degraded-never-blocks); code-quality-reviewer. accepted: the FakeBrain "ready" default feeds the dev shell → a real visual gate (degraded-block placement + grounded-at format) is feasible here (flagged for sign-off), unlike the no-live-data exposed-ahead surfaces.`

## <a id="36"></a>36. The PR-mutations cat-1 go-live = the per-action `enabledPrMutations` flip (both, at the single sanctioned Shell site) gated on user sign-off + a visual gate; a daemon-free visual gate uses a build-time env-gated Mock dev entry; a go-live UPDATES the held-flip guard, never retires it

**Date:** 2026-06-25.
**Source slice:** ui-075 / P7.4 (`docs/briefs/ui-075-P7-4-pr-mutations-go-live-flip.md`) — the cat-1 PR-mutations go-live (both `github.merge_pr` + `github.submit_review`, both-at-once, USER-ruled). 2 commits: C1 `caf3d32` (the dev-shell Mock visual-gate harness, NON-cat-1) + C2 `b0ddc39` (the production flip, cat-1; security-reviewer CLEAR, INV-SEC-1 holds). USER cat-1 sign-off GRANTED + visual gate PASSED.

The PR-mutations go-live is the **per-action analogue of the L2-C go-live ([[28]])**: the whole guarded-disabled wire ([[27]], ui-070/071) is lit by a single production constructor flip — `new UdsGatewayPort({ mutationsEnabled: true, enabledPrMutations: PR_MUTATION_ACTION_TYPES })` — gated on EXPLICIT USER cat-1 sign-off + a HITL visual gate. The flip enables EXACTLY the sanctioned set (the constant IS both PR types — no gate-vs-enabled drift); the deterministic surface is the production-shell pins (each control enables when connected + a click reaches the live `gateway_submit_action` transport). The UI flip is **necessary-not-sufficient** — the real live-write gate is the daemon's per-connection `live_writes_enabled` toggle (default OFF, 083); the UI gate (`canSubmitIntent && isPrMutationEnabled && headSha != null`) is defense-in-depth, the daemon Gateway the INV-SEC-1 chokepoint.

- **The daemon-free visual-gate harness = a build-time env-gated Mock dev entry:** to pixel-check a surface that needs ENABLED controls without a live daemon, `main.tsx` exports `resolveEntryGateway()` — `import.meta.env.VITE_NEXUSOPS_MOCK === "1"` → a `MockGatewayPort` (full `enabledPrMutations`, connected, fixture `head_sha`); unset/any other value → `undefined` → the production `UdsGatewayPort`. Vite inlines `import.meta.env.VITE_*` → a production build **tree-shakes the Mock branch to dead code** (empirically: 0 `MockGatewayPort`/`VITE_NEXUSOPS_MOCK` in `vite build` output). Retires the "no Mock-injection in main.tsx / manual operator step" caveat ([[22]]/[[23]]) for enabled-control gates. **Fail-closed on the env, NOT truthiness:** `"0"`/`"false"` are JS-truthy → a truthiness guard would invert-leak the Mock into prod; use an explicit `=== "1"` allowlist, pinned (`main_falsy_string_env_fails_closed_to_production`). The flip line must not appear as a colon-value LITERAL in a doc-comment — the held-flip guard's source-grep would match the comment too (a removed flip with a surviving comment would still "pass").
- **A go-live UPDATES the held-flip guard, never retires it:** the guarded-disabled era ([[27]]) was protected by a meta-test asserting ZERO production `enabledPrMutations:` flips (`production_construction_never_enables_pr_mutations`). The go-live legitimately introduces EXACTLY ONE such site — so the guard is **confined** to that single sanctioned site (`pr_mutation_flip_confined_to_the_signed_off_shell_go_live`, asserting `["src/shell/Shell.tsx"]`), NOT deleted; any OTHER production flip still fails it (stays load-bearing; security-reviewer empirically confirmed).
- **RED-first across the HITL checkpoint:** the cat-1 production-shell pins ship in C1 as `describe.skip` (so C1's suite stays green); C2 unskips them — confirmed RED (Merge `disabled` with an empty set) BEFORE the Shell flip lands, then GREEN. The flip is genuinely test-driven, not skip→already-green.

**Rule:** a per-action mutation go-live is a single production-constructor flip of the per-action enable-set (the full sanctioned constant — no gate-vs-enabled drift), gated on explicit user sign-off + a HITL visual gate, with the daemon-side per-connection toggle the real live-write gate and the UI gate defense-in-depth (the [[28]] go-live, per-action). A daemon-free visual gate uses a build-time env-gated (`=== "1"` fail-closed) Mock dev entry that prod tree-shakes. A go-live UPDATES the held-flip guard to a single-sanctioned-site assertion, never retires it. Extends [[22]]/[[23]]/[[27]]/[[28]].

**Enforcement:** `pin: src/main.mock.test.tsx (default-no-Mock / env-injects-Mock / falsy-fails-closed / fixture-enables-controls) + src/shell/Shell.uds-swap.test.tsx (production_shell_enables_pr_{merge,review}_when_connected + production_shell_pr_{merge,review}_click_reaches_live_transport) + src/intent/pr-mutation-request.test.ts (pr_mutation_flip_confined_to_the_signed_off_shell_go_live); security-reviewer the cat-1 flip. accepted: the prod-no-Mock guarantee rests on Vite tree-shaking + the source-level unit pin — a bundle-grep CI gate (assert MockGatewayPort absent from `vite build` output) is a deferred §10.6 hardening (green today).`

## <a id="37"></a>37. An additive parameter/method selector on an ALREADY-live gated mutation is NON-cat-1 — it rides the existing gate, the daemon validates the parameter fail-closed, and the gate stays parameter-agnostic

**Date:** 2026-06-25.
**Source slice:** ui-077 / P7.2 (`docs/briefs/ui-077-P7-2-merge-method-selector.md`) — the PR merge-method selector (merge/squash/rebase) on the ui-075-live `github.merge_pr`. NON-cat-1 (lead/user-ruled); security-reviewer CLEAR (INV-SEC-1 holds, 0 findings).

Adding a user-selectable PARAMETER (here `merge_method`, incl. the history-rewriting squash/rebase) to an ALREADY-live, already-gated mutation is **NON-cat-1** — it needs NO new go-live sign-off:
- **Rides the EXISTING gate — no new gate:** no new `enabledPrMutations` entry, no new action_type (still `github.merge_pr`), no new go-live flip. The `pr_mutation_flip_confined_to_the_signed_off_shell_go_live` guard ([[36]]) **staying green PROVES zero new flip site** — the "no-new-gate" claim is verified by the unchanged guard, not a fresh test.
- **The gate stays PARAMETER-AGNOSTIC:** the enablement (`canMerge = canSubmit && isPrMutationEnabled && headSha != null`) is unchanged + uniform across parameter values — no value bypasses the gate (pinned: `!canMerge` disables the selector AND the action).
- **The daemon is the parameter authority (fail-closed):** the UI offers a CLOSED option set (`MERGE_METHODS` / the tsc-enforced `MergePrMethod` union) but does NOT over-validate — the daemon's `map_merge_method` rejects unknown fail-closed; the value rides into `inputs.merge_method` VERBATIM (the UI is a pure submitter, [[16]]).
- **The per-action approval surfaces the parameter:** every mutation stays per-action approved + audited regardless of the value; the GatewayModal preview shows the chosen value → the user confirms WITH it visible (so a more-destructive value like squash/rebase is gated by the same human approval).
- **Build-discipline (NOT a sign-off):** a NEW rendered control still earns a SCOPED visual gate ([[10]]/[[12]]) — UI-correctness, non-blocking (a fail → polish). A native `<select>` needs `appearance:none` + kit form-control tokens to read as the theme (else OS chrome paints through).

**Rule:** an additive parameter/method selector on an already-live gated mutation is NON-cat-1 (no new go-live) when it rides the existing gate (no new enable-set entry/action_type — verify via the held-flip guard staying green), the gate stays parameter-agnostic (no value bypasses it), the daemon validates the parameter fail-closed (UI offers a closed set + submits verbatim + doesn't over-validate), and the per-action approval surfaces the value. A new control still gets a scoped UI-correctness visual gate (build-discipline). Extends [[16]]/[[27]]/[[28]]/[[36]].

**Enforcement:** `pin: src/views/code/PrWorkspace.test.tsx (offers-three / gated-method-agnostic) + DiffReview.test.tsx (selected-method-rides-verbatim / default-merge) + pr-mutation-request.test.ts (closed-set + MergePrMethod tsc-closure) + the unchanged pr_mutation_flip_confined_to_the_signed_off_shell_go_live guard (no new flip site); security-reviewer the live-merge-path parameter add.`

## <a id="38"></a>38. A multi-projection INITIAL load is per-projection-resilient (per-call settle + Promise.all), never all-or-nothing; a failed projection degrades honestly (§11.7 partial-data banner); caps-fault → fail-safe read-only independent; total-fault → the existing blank

**Date:** 2026-06-25.
**Source slice:** ui-079 / P6.11 (`docs/briefs/ui-079-P6-1-shell-per-projection-resilient-load.md`) — the Shell cockpit-load resilience fix. NON-cat-1; a version-independent robustness bug surfaced by the integrated-cockpit 0.46-skew investigation.

The Shell's initial load did `Promise.all([7 get_projection + get_capabilities])` → **ANY one rejecting** (a boundary Zod-parse `BoundaryValidationError` [[22]] or a daemon read-fault) **blanked the WHOLE cockpit**. The fix:
- **Per-projection-resilient load:** each `get_projection` is a typed per-call `settle()` wrapper (`Promise<Page<K> | null>`) + `Promise.all` over the wrappers — same concurrency, but one failure → `null` for that slice, not a whole-load rejection. **Typed per-call wrappers beat raw `Promise.allSettled`** here: the 7 pages are heterogeneous (`ProjectionPageByName[K]`), and `allSettled`'s destructured-union narrowing is awkward — per-call wrappers keep each page type precise.
- **Honest §11.7 degrade (not silent-empty):** a failed projection joins a `degradedProjections: ReadonlySet<ProjectionName>` (a UI-local `ShellData` view-model field — NOT a frozen contract) → an INLINE **partial-data banner** (`role="status"`, glyph+label never color-alone, **DISTINCT from the transport `DegradedBanner`** [[11]]) that NAMES the unavailable projection(s) + `console.error` each. Never silent-empty/fabricated (empty-rows-without-a-banner would read as "no data" = stale-as-live).
- **caps-fault is independent:** `get_capabilities` rejecting → version `unknown` → read-only ([[4]]) via the existing "checking" banner, WITHOUT blanking the data tiles; caps ∉ the partial-data set (a data-tile fault never forces read-only, a caps fault never blanks tiles).
- **TOTAL fault → the existing blank:** ALL 7 projections rejecting → the EXISTING `shell-load-error` blank (a fully-unreachable daemon = ONE honest "couldn't load," not 7 "unavailable" banners) — preserves the prior total-fault tests.

**Rule:** a multi-projection initial load is per-projection-resilient (typed per-call settle + `Promise.all`, never all-or-nothing `Promise.all`); a failed projection degrades honestly via a distinct §11.7 partial-data banner that names it (not silent-empty); caps-fault → fail-safe read-only independent of the data tiles; a TOTAL fault collapses to the existing single "couldn't load" blank. Complements [[29]] (the live-SUBSCRIBE per-stream worst-of aggregation) for the INITIAL-load path. Extends [[4]]/[[11]]/[[22]].

**Enforcement:** `pin: src/shell/Shell.test.tsx (one-fail-renders-rest / honest-degrade-banner-names-it / counts-survive-failed-input / caps-fail-safe-read-only / all-succeed-regression / multi-fail-aggregates) + the preserved total-fault tests (shell_reads_only_through_gateway_boundary / shell_transport_fault_renders_read_only_degraded).`

## <a id="39"></a>39. Wiring a daemon session-lifecycle control into the cockpit: a generic `submit_action` builder (client-mint the PK + session resource_ref) behind a per-control default-OFF go-live gate, with the on-OK notice keyed to the catalog RISK CLASS

**Date:** 2026-06-26.
**Source slices:** ui-084 (`session.kill`) / ui-086 (`session.profile_change`) / ui-087 (`session.send_message`/`pause`/`resume`) — the WAVE-1 session-control lane (the cockpit can finally DRIVE agents, not just view them).

A daemon session-control action (`session.kill`/`profile_change`/`send_message`/`pause`/`resume`) is a **GENERIC `submit_action`** — no dedicated IPC method (unlike `session.create`, which is a daemon-mints-the-id dedicated method). So the UI:
- **Forms a typed builder that CLIENT-mints the `action_request_id`** (the daemon trusts the wire id as the `action_requests` TEXT PK — an empty id collides on the 2nd same-session mutation → `AuditWriteFailed`→`precondition_stale`; the empty-PK class) + `resource_refs:[{type:"session", id}]` (the daemon executor keys on the first resource_ref via `SessionId::parse`) + the action `inputs` (`{}` for kill/pause/resume; `{execution_profile_id}` for profile_change; `{message}` `.trim()`'d for send_message — empty blocks submit). `risk_level` is a non-authoritative hint (catalog-authoritative, daemon-reconciled).
- **Rides a DEFAULT-OFF go-live gate** (the `enabledPrMutations`/`enabledSessionLaunch` mirror), enforced in `UdsGatewayPort.submit_action` (**throws-never-invokes**, action-type-scoped [a non-gated action is unaffected], and **INDEPENDENT of the already-live `mutationsEnabled`** so a new high-consequence write never auto-rides the L2 go-live flip), default OFF/empty in prod (HELD until a USER cat-1 sign-off + visual gate) + a `no_production_file_enables_*` held-flip guard. As the controls accrue, consolidate the per-control booleans → an `enabledSessionControls: ReadonlySet<actionType>` (per-action granularity, the `enabledPrMutations` shape; introduced at ui-087 for the drive set, with the existing launch/kill/profile_change booleans a deferred migration).
- **Keys the on-OK notice to the catalog RISK CLASS:** a **risk-0 auto-execute** (kill) → **error-only** (NO persistent success — the result surfaces via the live projection nudge; never a fabricated "killed"); a **risk≥1 approval-gated** (profile_change/send_message/pause/resume) → a **non-optimistic "submitted for approval"** notice (the daemon's `ActionAck.status` — the action enters the live ApprovalQueue, the §15 #8 human checkpoint; never a fabricated "changed/sent/paused").
- **Eligibility + defense-in-depth:** disabled unless `canSubmitIntent && mutationsEnabled && <the gate>`; a synchronous `inFlight` ref guards a rapid double-submit; hidden/disabled for ended sessions (reuse the single-source `isEndedSession`).

**Rule:** a daemon session-lifecycle control wires as a generic `submit_action` builder (CLIENT-mint the PK + a `{type:"session"}` resource_ref) behind a per-control DEFAULT-OFF go-live gate (action-scoped + `mutationsEnabled`-independent + held-flip-guarded; consolidate the booleans → a Set as they accrue), with the on-OK notice keyed to the catalog risk class (risk-0 → error-only; risk≥1 → "submitted for approval"; never a fabricated success), disabled-unless-eligible + inFlight-guarded + hidden-for-ended. The daemon Gateway + interception are the INV-SEC-1 chokepoint; the UI gate is the go-live hold (defense-in-depth). The gate is the OPERATIVE hold for a risk-0 control (the daemon auto-executes on receipt, no daemon-side toggle); for a risk≥1 control the daemon approval is the operative checkpoint and the gate is additional defense. Extends [[27]]/[[28]]/[[36]].

**Enforcement:** `pin: src/intent/{kill-session-request,profile-change-request,session-drive-request}.test.ts (client-mint + shape) + src/gateway-client/uds.test.ts (throws-never-invoke-when-gated + action-scoped + mutationsEnabled-independent + per-action Set) + src/views/sessions/{KillSessionControl,ProfileChangeControl,SessionDriveControls}.test.tsx (notice-non-optimistic-keyed-to-risk + disabled-unless + inFlight + hidden-ended) + the no_production_file_enables_{session_kill,profile_change,session_controls} held-flip guards; security-reviewer every session-control slice.`

## <a id="40"></a>40. A daemon read-RPC RESULT type is consumed as a UI gateway-client read method + a hand-modeled provisional `.strict()` shadow + a dedicated boundary parser — DIRECTLY, NOT a projection-row (never `parseProjectionPage`)

**Date:** 2026-06-26.
**Source slice:** ui-085 (W1-prof UI half — `get_execution_profiles` + the 0.47→0.48 regen).

A daemon read-RPC **result type** (frozen in `shared/src/ipc.rs` — `GetExecutionProfilesResult`/`ProfileRow`; the `get_pr_diff`/`DiffResult` precedent) is **NOT a projection-row** (no `ProjectionName`, not `get_projection`-served). So the UI consumer is **NOT** a projection shadow + `parseProjectionPage`:
- It's a typed **gateway-client read METHOD** (`get_execution_profiles`) + a **dedicated fail-closed boundary parser** (`parseExecutionProfilesResult`) + the 5-layer read transport (the `ui/gateway-uds` crate helper + a Tauri command on the `generate_handler!` allowlist — **NEVER a generic `gateway_call`**, [[21]]) + a Mock — consumed **DIRECTLY**.
- The result type is a **HAND-MODELED provisional shadow:** `gen-contracts.mjs` emits ENUM `$defs` ONLY — a CONTRACT bump regens generated.ts's `CONTRACT_VERSION` + enums (the value-set count is HELD when a new field reuses an existing enum, e.g. `status: ExecutionProfile` adds no flat enum), but object result types are hand-modeled in `provisional.ts` (the DiffResult precedent), **`.strict()`** (so a §15 #4 secret leak — a `keychain_ref` the daemon must never serve — is REJECTED at the boundary; a UI mirror of the daemon's secret-free guarantee, surfaced as `has_credential: bool`), with the field-set drift-pinned to `shared/src/ipc.rs`.
- It is a **READ** (not a mutation) → **NOT gated on `mutationsEnabled`** (mirrors `get_diff`/`get_pr_diff`).

**Rule:** a daemon read-RPC RESULT type (`shared/src/ipc.rs`, the `get_pr_diff` precedent — NOT a `ProjectionName` projection-row) is consumed by the UI as a gateway-client read method + a dedicated fail-closed boundary parser + the typed 5-layer transport (no generic `gateway_call`), consumed DIRECTLY (never `parseProjectionPage`); the result is a hand-modeled provisional `.strict()` shadow (gen-contracts emits enums only) drift-pinned to ipc.rs, secret-free-by-reject; a read is not gated on `mutationsEnabled`. Extends [[22]] (boundary parse-don't-trust) + the DiffResult/get_pr_diff read-transport pattern.

**Enforcement:** `pin: src/gateway-client/boundary.test.ts (parseExecutionProfilesResult accept / fail-close / keychain-leak reject) + uds.test.ts (invokes-without-mutationsEnabled) + src/contracts/provisional.test.ts (ProfileRow field-set drift-pin + status→generated-ExecutionProfile + .strict secret-free + optionals-null) + the ui/gateway-uds crate tests; security-reviewer the §15 no-leak boundary.`

## <a id="41"></a>41. Honest labeling for a SOFT capability — label a control to its REAL effect (a soft "pause" gates monitoring, not the agent), pinned across visible text AND `title`/`aria`

**Date:** 2026-06-26.
**Source slice:** ui-087 (session drive controls — a lead-routed daemon finding, daemon LESSON §71).

When an MVP action does **LESS than its verb implies**, the control MUST be labeled to its REAL effect — never the aspirational verb:
- MVP `session.pause` is **SOFT**: it gates the cockpit's drive/observe loop but does NOT OS-suspend the agent (the agent keeps running; its tool calls are still Gateway-intercepted, so INV-SEC-1 holds). So the control reads **"Pause monitoring" / "Resume monitoring"**, NEVER "stop"/"suspend"/"halt the agent".
- An honest label is **LOAD-BEARING (safety-adjacent):** a user who believes "Pause" halts the agent when it doesn't holds a false belief about a safety-relevant capability — they might walk away thinking the agent is stopped while it keeps acting.
- **Pin it across the FULL rendered surface** — visible TEXT *and* `title`/`aria` attributes. A forbidden word hiding in a tooltip is still a lie (a security-LOW caught a bare "stop" in the Pause tooltip that a `textContent`-only check missed). **Assert the ABSENCE of the forbidden verbs**, not merely the presence of the honest one.
- The soft pause is **not projected** (no `paused` field in `proj_session`/`SessionRow`/the §5.1 Session enum) → there is no accurate stateful toggle → **BOTH** "Pause monitoring" + "Resume monitoring" are shown (no fabricated toggle state). The real OS-suspend + a projected paused-state are deferred follow-ons (daemon LESSON §71).

**Rule:** when an MVP capability does less than its verb implies (a soft pause that gates monitoring, not the agent), label the control to its REAL effect ("Pause monitoring"), never the aspirational verb — an honest label is load-bearing (a false belief about a safety capability is a cockpit lie). Pin it by asserting the ABSENCE of the forbidden verbs across the FULL surface (visible text + `title`/`aria`), not just the presence of the honest word. An unprojected soft state → both actions available (no fabricated toggle). Extends [[4]] (honest-degrade) + the never-fabricate posture.

**Enforcement:** `pin: src/views/sessions/SessionDriveControls.test.tsx (pause_resume_labeled_monitoring_not_suspend — scans text + title + aria for the ABSENCE of stop/suspend/halt + both_available_no_toggle); security-reviewer the soft-pause INV-SEC-1 reflection.`

## <a id="42"></a>42. A daemon-readiness hold (a wired live-write control gated by a code flag because its daemon executor isn't built yet) is DISTINCT from a user-go-live gate — it auto-flips when the daemon lands (no held-flip guard); the go-live gate needs a USER cat-1 sign-off

**Date:** 2026-06-26.
**Source slice:** ui-088 (W1-git-ui — the git-hunk staging activation; the held Discard control).

Two different reasons a wired live-write UI control stays DEFAULT-HELD — keep them distinct (the trigger that releases each differs):
- **User-go-live gate** (the session controls' `enabledSessionKill`/`enabledProfileChange`/`enabledSessionControls`; PR's `enabledPrMutations`, [[36]]): held until a USER cat-1 sign-off + visual gate. **NEVER auto-flips** — a `no_production_file_enables_*` held-flip guard pins that no prod file enables it. **Release trigger = the USER.**
- **Daemon-readiness hold** (ui-088's `DISCARD_AVAILABLE=false`): the daemon EXECUTOR isn't built yet (`git.discard_hunk` was a stub → a submit→approve→**Failed** approve-then-fail). Held by a plain module const, flipped `true` when the daemon lands the executor — it is **MEANT to flip in prod**, so there is **NO held-flip guard** (a guard would wrongly forbid the intended flip). **Release trigger = the DAEMON.**

Corollaries:
- **The git-mutation user-gate is the per-action APPROVAL MODAL** (risk-2/3; discard's `preview_class=diff` shows the exact hunk being destroyed), NOT a default-OFF user-go-live gate — so the readiness flip ALONE activates Discard when the daemon lands (no separate user sign-off, contrast the session controls).
- **Don't offer a wired live-write control whose daemon executor is a stub** — the approve-then-fail is a dishonest UX (worse for a DESTRUCTIVE action). Hold it honestly: disabled + a daemon-READINESS tooltip (`/executor lands/`, never a go-live/policy framing), pinned by a disabled-even-when-`canSubmit` test + a no-submit-on-click assertion. The held control's submit-path tests repoint to a now-live sibling action (action-agnostic flows) + an `it.skip` signpost re-enabled at the flip.
- **The flip is not always a one-line `=true`:** ui-088's Discard flip additionally requires a `displayed_hunk_sha256` content-hash input (the lead-ruled §17 verify-before-destroy — daemon re-derives + compares the hash, mismatch → Failed "hunk changed"). So the flip slice's security pass treats the content-hash as a **GATING precondition** (a flag-only flip would re-enable a destructive submit WITHOUT the guard).

**Rule:** distinguish a daemon-readiness hold (a wired live-write control gated by a code flag because its daemon executor isn't built yet — auto-flips when the daemon lands, NO held-flip guard, the daemon is the release trigger) from a user-go-live gate (held for a USER cat-1 sign-off, never auto-flips, a held-flip guard pins it, the user is the release trigger). A destructive control whose user-gate is the per-action approval modal needs only the readiness flip, not a separate go-live sign-off. Hold a stub-backed control honestly (disabled + a readiness tooltip). When the flip also re-enables a destructive submit with a new safety precondition (a content-hash verify-before-destroy), that precondition GATES the flip slice's security pass. Extends [[36]] (the go-live-gate side).

**Enforcement:** `pin: src/views/code/DiffReview.test.tsx (discard_held_until_daemon_executor — disabled-while-canSubmit + no-submit + honest-readiness-tooltip; the HunkGitActions unit both-flag-states) + the it.skip'd discard-submit signpost (re-enable at the W1-git-discard-UI flip + the content-hash send).`

## <a id="43"></a>43. A UI-mirrored cross-language content-hash — canonicalize byte-verbatim to a daemon-frozen spec, pin BOTH the canonical string AND the digest against ONE shared frozen conformance vector; the format map is `Record`-complete over the generated enum

**Date:** 2026-06-26.
**Source slice:** ui-089-A (the W1-git-discard UI half — the `displayed_hunk_sha256` verify-before-destroy content-hash send).

A destructive mutation's UI half sends a **content hash of the displayed read** so the daemon can re-derive the live target, re-compute the hash, and **refuse on drift** (mismatch → `Failed`, no mutation). This extends [[19]] (submitted == displayed) from **position-identity** (the resource_ref's `{wt}\x1f{file}\x1f{positions}`) to **content-identity** — closing the same-position content-drift race a position-only posture accepts (daemon LESSON §72).

How to build it so the two languages can't silently disagree:
- **Canonicalize byte-verbatim to the daemon-frozen spec** — here: one origin byte per line (`Record<DiffLineKind,string>` = `{context:" ", added:"+", removed:"-"}`) immediately followed by the RAW `get_diff` `content` field (origin-marker already stripped by the daemon; trailing `\n` retained), concatenated with NO separator. **Do NOT re-render / strip / re-add newlines or collapse leading whitespace** — an indented context line's content legitimately starts with a space (`" "` prefix + `" x\n"` content = `"  x\n"`).
- **Pin BOTH the intermediate canonical string AND the digest against ONE frozen conformance vector the daemon pins identically** — the vector (`" a\n-b\n+c\n"` → `2980a502…ee6`, `printf … | shasum -a 256`-verifiable) is the cross-language contract LOCK. **Split the canonical fn from the hash fn** (`displayedHunkCanonical` / `displayedHunkSha256`) so a vector failure localizes which side drifted (canonicalization vs hashing/encoding).
- **The format map is `Record<GeneratedEnum>`-complete** — derive the key type from the generated enum (`Hunk["lines"][number]["kind"]`) + a drift test over `bundle.shape.DiffLineKind.options`, so a daemon-added enum value FAILS the build until the canonicalization covers it (the [[5]]/[[30]] "force a future value to render" net applied to a canonicalization map — a missing mapping is a silent divergence, not a render gap).
- **Use a SYNC audited hash** (`@noble/hashes`, lowercase hex over the UTF-8 canonical string) — keeps the existing sync request-builder seam sync (no async ripple into the signed-off `buildHunkActionRequest`/`onAction`/tests), makes the conformance pin a trivial deterministic assertion, and carries zero test-env risk (vs `crypto.subtle`'s async + jsdom-availability uncertainty). The +1 audited zero-transitive dep is justified for a destroy-gating hash.
- **Additive + scoped + daemon-authoritative** — the hash rides the action `inputs` (`z.unknown()` opaque → NO `shared/`/Zod/CONTRACT change), is scoped to the DESTRUCTIVE action only (stage/unstage keep `inputs:null`), and the UI ALWAYS-sends a 64-hex (no UI-side empty/odd-hunk special-casing) because the **daemon is the verify authority** (absent/empty/mismatch → `Failed`); the UI is the pure submitter ([[16]]).

**Rule:** a UI-side content hash that gates a daemon verify-before-destroy is canonicalized **byte-verbatim** to the daemon's frozen spec and pinned — BOTH the canonical string AND the digest — against ONE shared frozen conformance vector (the cross-language lock; split canonical-fn from hash-fn so a failure localizes the drift); the format map is `Record`-complete over the generated enum; a sync audited hash keeps the seam sync and the pin deterministic; the hash is additive-opaque + destructive-action-scoped; the daemon stays the verify authority (mismatch → `Failed`). Extends [[19]] (position-identity → content-identity) + [[16]] (pure submitter).

**Enforcement:** `pin: src/intent/hunk-resource-ref.test.ts (displayed_hunk_sha256_matches_frozen_conformance_vector — hash AND canonical string; discard_request_carries_displayed_hunk_sha256_input; stage_unstage_requests_have_null_inputs; diff_line_prefix_map_is_complete_over_generated_kinds; canonicalization_is_byte_verbatim); security-reviewer the destroy-gating content-hash.`

## <a id="44"></a>44. A daemon-RENDERED display field is consumed as a forward-compat plain string (UNBOUND from the source enum, tolerant unknown→raw, no completeness-pin); and VERIFY a relayed shape against the sealed code before keying logic to it

**Date:** 2026-06-26.
**Source slice:** ui-090 (the W2-audit `AuditEventRow` reconcile — `headline`/`actor_label`/`event_type`/`occurred_at`).

A projection field the daemon **renders for display** — a redaction-safe `headline`, an `actor_label` = `wire_value(&env.actor_type)` — is consumed as a **plain `z.string()` (nullable as served), UNBOUND from the source Zod enum**, *even when its values currently match an enum*. Binding it to the enum (`ActorType`/`Sensitivity`) would **fail-closed the moment the daemon adds a value**, blanking the degradable tile ([[38]]). Instead:
- A UI-side **tolerant humanization map** keyed on the REAL current values, **unknown → raw-string fallback** (the raw snake_case is a readable degrade). **NO completeness-pin to the enum** — the raw fallback IS the forward-compat. (Contrast a render-MUST field like a status→attention-rank descriptor [[5]], which IS completeness-pinned *because there is no safe fallback*. The discriminator: does an uncovered value have a safe degrade? Plain-string display → yes → tolerant; safety/attention render → no → completeness-pinned.)
- **ONE shared humanization helper, not duplicated per consumer.** A field rename (`actor_type`→`actor_label`, `summary`→`headline`) makes tsc surface **every** consumer — ui-090 had THREE (`AuditTrail`/`CommandCenter`/`EventDock`), each with its own `ACTOR_LABEL` map. Collapse them into one `humanizeActorLabel` (one source of truth — duplicate maps were the latent divergence the phantom below would have exposed unevenly). The frozen row → `.strict()` shadow drift-pinned to the schema `$defs` ([[24]]/[[40]]).

**Verify-the-surface before keying logic to a relayed shape.** A relayed spec detail — the `actor_label` EXAMPLE values `human/agent/brain/pack` — was **phantom**: it didn't match the sealed `audit.rs:28` + `actor.rs` (the real values are the 10 `ActorType` wire values `user/project_brain/action_gateway/…/automation_policy`). The implementer caught it by **reading the sealed code, not taking the relay on faith**; keying the map on the phantom values would have dead-faulted EVERY real row to raw — shipping the opposite of the un-degrade goal. **When a relayed correction contradicts the code, defer to the code** (the phantom-message defense — the relay can be the phantom, even from an authoritative-sounding source). Crucially, **separate the relay's STRUCTURE from its VALUES**: here the structure was right (snake_case wire value, `Option<String>`, unbound, tolerant map) while only the example keys were wrong — adopt the structure, correct the values against the code.

**Rule:** a daemon-rendered display field is consumed as a forward-compat **plain string** (nullable as served, UNBOUND from the source enum, a tolerant map keyed on the real values with unknown→raw fallback, NO enum completeness-pin — the raw fallback IS the forward-compat; completeness-pin only a render-MUST field with no safe degrade); one shared humanization helper across all consumers (a rename forces reconciling them all — collapse duplicates). And **verify any relayed shape/values against the sealed code before keying logic to it** — defer to the code when a relay contradicts it (even an authoritative-sounding relay can carry phantom values; separate its correct structure from its wrong values). Extends [[24]] (frozen-shadow) + [[2]] (provisional discipline) + [[38]] (degradable tile).

**Enforcement:** `pin: src/views/audit/actor-label.test.ts (humanizeActorLabel — all 10 real ActorType values humanize + unknown→raw) + src/contracts/provisional.test.ts (AuditEventRow 8-field .strict() + field-set vs frozen $defs.AuditEventRow) + src/views/audit/AuditTrail.test.tsx (headline/actor_label/occurred_at render + un-degrade).` `accepted: verify-the-surface is a discipline, not mechanically greppable — read the sealed code before keying logic to a relayed shape.`

## <a id="45"></a>45. Honest-OMIT a display the daemon cannot source — DELETE the field + every consumer + the Mock fake (do NOT null-guard a Mock-faked field, which HIDES the Mock-vs-real gap)

**Date:** 2026-06-26.
**Source slice:** ui-091 (the W2-usage `UsageRow` reconcile — the `creditPool` honest-omit).

When a display field has **no daemon source** (the daemon genuinely cannot observe it — here the SDK credit-pool remaining balance: `usage_policy_json` opaque, `credit_exhausted` binary, the pool not telemetry-observable), and the **Mock fakes it**, the field is **null in real data** → the consumer's null-guard renders nothing → the gap is INVISIBLE: the affordance (the credit-pool meter + its `hard_stop` state) only ever shows in Mock/dev, NEVER in production. That is a **Mock-vs-real gap** ([[38]] family). The honest fix is **NOT a better null-guard — it is DELETION**: remove the field from the shadow + the page type + **EVERY** consumer (the meter, the gating logic, the Mock fixture's fake, the prop-threading — here 4 sites: Shell/Settings/CommandCenter/display-meta, tsc-surfaced) and render an honest **"not reported by the daemon" note** in its place ([[11]]/[[38]] — explain the absence, never silently drop). Tolerating an always-null faked field leaves dead UI + invites a future dev to "fix" the null-guard and re-fake it.

**Removing an un-sourceable SAFETY-STATE display is the HONEST direction of [[15]]/[[11]]** (never a silent/false safety state): a safety signal the daemon **cannot actually observe** (the §11.4 SDK `hard_stop`) is a potentially-FALSE alarm — removing it **RESOLVES** the false-safety-state risk [[15]] warned of, it does NOT regress safety. (AUTHORING such an unsourceable safety display is the violation; REMOVING it is the fix.) The **disposition is the lead/product's** (is it ever sourceable?) — surface the open product question (handoff/carry-forward), don't decide it in code; the UI's job is to stop faking it now.

**Rule:** a display field the daemon cannot source (always-null in real data, faked only by the Mock) is a Mock-vs-real gap → resolve by **DELETING** the field + every consumer + the Mock fake + an honest "not reported" note — NOT by null-guarding it (which hides the gap + leaves dead/dev-only UI). Removing an un-sourceable SAFETY-STATE display is the honest direction of [[15]]/[[11]] (a daemon-unobservable safety signal is a potentially-FALSE alarm; removal resolves it). Surface the "is it ever sourceable" product question; don't decide it in code. A contract-driven reshape tsc-surfaces every consumer — fix them all in the one reconcile. Extends [[11]]/[[15]]/[[38]].

**Enforcement:** `pin: src/views/usage/UsageDashboard.test.tsx (renders_no_credit_pool_meter — no meter + the honest credit-pool-unavailable note) + src/contracts/provisional.test.ts (credit_pool_type_removed — CreditPool unexported + UsageProjectionPage has no creditPool) + src/views/usage/model.test.ts (credit_pool_state_removed).` `accepted: "no creditPool in production code" is grep-verifiable at the slice (CreditPool/creditPool → only comments + removal-asserting tests).`
